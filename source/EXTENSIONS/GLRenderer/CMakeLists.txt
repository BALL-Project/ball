PROJECT(GLRenderer)

SET(DIRECTORY source)

### list all filenames of the directory here
SET(SOURCES_LIST
	${DIRECTORY}/configDialog.C
	${DIRECTORY}/glDisplayList.C
	${DIRECTORY}/glPlugin.C
	${DIRECTORY}/glRenderer.C
	${DIRECTORY}/glRenderSetup.C
	${DIRECTORY}/vertexBuffer.C
)

SET(RCC_SOURCES_LIST
	${DIRECTORY}/logo.qrc
)

SET(UI_SOURCES_LIST
	${DIRECTORY}/configDialog.ui
)

SET(DIRECTORY include)

### the list of all files requiring a moc run
SET(MOC_SOURCES_LIST
	${DIRECTORY}/glPlugin.h
	${DIRECTORY}/configDialog.h
)

QT5_WRAP_CPP(MOC_OUTFILES ${MOC_SOURCES_LIST})
QT5_ADD_RESOURCES(RCC_OUTFILES ${RCC_SOURCES_LIST})
QT5_WRAP_UI(UIC_OUTFILES ${UI_SOURCES_LIST})

INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/include)
INCLUDE_DIRECTORIES(${PROJECT_BINARY_DIR})

ADD_DEFINITIONS(${QT_DEFINITIONS})
ADD_DEFINITIONS(-DQT_PLUGIN)
ADD_DEFINITIONS(-DQT_NO_DEBUG)
ADD_DEFINITIONS(-DQT_SHARED)

ADD_LIBRARY(pluginGLRenderer SHARED
	${SOURCES_LIST}
	${MOC_OUTFILES}
	${RCC_OUTFILES}
	${UIC_OUTFILES}
)

SET_TARGET_PROPERTIES(pluginGLRenderer PROPERTIES PREFIX "")

INSTALL(TARGETS pluginGLRenderer
	COMPONENT "${COMPONENT_PLUGINS}"
	RUNTIME DESTINATION "${BALL_PLUGIN_INSTALL_DIRECTORY}"
	LIBRARY DESTINATION "${BALL_PLUGIN_INSTALL_DIRECTORY}"
)

TARGET_LINK_LIBRARIES(pluginGLRenderer BALL VIEW)
