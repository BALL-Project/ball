#! /bin/sh
# Guess values for system-dependent variables and create Makefiles.
#  $Id: configure,v 1.153 2001/07/16 13:13:18 oliver Exp $


##################################################
##  PLEASE DO NOT CHANGE ANYTHING BELOW IF YOU  ##
##  ARE NOT COMPLETELY SURE WHAT YOU ARE DOING  ##
##  INSTEAD, RERUN CONFIGURE WITH --help        ##
##################################################





























































































# Generated by Autoconf 2.50.
#
# Copyright 1992, 1993, 1994, 1995, 1996, 1998, 1999, 2000, 2001
# Free Software Foundation, Inc.
# This configure script is free software; the Free Software Foundation
# gives unlimited permission to copy, distribute and modify it.

# Be Bourne compatible
if test -n "${ZSH_VERSION+set}" && (emulate sh) >/dev/null 2>&1; then
  emulate sh
  NULLCMD=:
elif test -n "${BASH_VERSION+set}" && (set -o posix) >/dev/null 2>&1; then
  set -o posix
fi

# Name of the executable.
as_me=`echo "$0" |sed 's,.*[\\/],,'`

if expr a : '\(a\)' >/dev/null 2>&1; then
  as_expr=expr
else
  as_expr=false
fi

rm -f conf$$ conf$$.exe conf$$.file
echo >conf$$.file
if ln -s conf$$.file conf$$ 2>/dev/null; then
  # We could just check for DJGPP; but this test a) works b) is more generic
  # and c) will remain valid once DJGPP supports symlinks (DJGPP 2.04).
  if test -f conf$$.exe; then
    # Don't use ln at all; we don't have any links
    as_ln_s='cp -p'
  else
    as_ln_s='ln -s'
  fi
elif ln conf$$.file conf$$ 2>/dev/null; then
  as_ln_s=ln
else
  as_ln_s='cp -p'
fi
rm -f conf$$ conf$$.exe conf$$.file

as_executable_p="test -f"

# Support unset when possible.
if (FOO=FOO; unset FOO) >/dev/null 2>&1; then
  as_unset=unset
else
  as_unset=false
fi

# NLS nuisances.
$as_unset LANG || test "${LANG+set}" != set || { LANG=C; export LANG; }
$as_unset LC_ALL || test "${LC_ALL+set}" != set || { LC_ALL=C; export LC_ALL; }
$as_unset LC_TIME || test "${LC_TIME+set}" != set || { LC_TIME=C; export LC_TIME; }
$as_unset LC_CTYPE || test "${LC_CTYPE+set}" != set || { LC_CTYPE=C; export LC_CTYPE; }
$as_unset LANGUAGE || test "${LANGUAGE+set}" != set || { LANGUAGE=C; export LANGUAGE; }
$as_unset LC_COLLATE || test "${LC_COLLATE+set}" != set || { LC_COLLATE=C; export LC_COLLATE; }
$as_unset LC_NUMERIC || test "${LC_NUMERIC+set}" != set || { LC_NUMERIC=C; export LC_NUMERIC; }
$as_unset LC_MESSAGES || test "${LC_MESSAGES+set}" != set || { LC_MESSAGES=C; export LC_MESSAGES; }

# IFS
# We need space, tab and new line, in precisely that order.
as_nl='
'
IFS=" 	$as_nl"

# CDPATH.
$as_unset CDPATH || test "${CDPATH+set}" != set || { CDPATH=:; export CDPATH; }

# Name of the host.
# hostname on some systems (SVR3.2, Linux) returns a bogus exit status,
# so uname gets run too.
ac_hostname=`(hostname || uname -n) 2>/dev/null | sed 1q`

exec 6>&1

#
# Initializations.
#
ac_default_prefix=/usr/local
cross_compiling=no
subdirs=
MFLAGS= MAKEFLAGS=
SHELL=${CONFIG_SHELL-/bin/sh}

# Maximum number of lines to put in a shell here document.
# This variable seems obsolete.  It should probably be removed, and
# only ac_max_sed_lines should be used.
: ${ac_max_here_lines=38}

# Avoid depending upon Character Ranges.
ac_cr_az='abcdefghijklmnopqrstuvwxyz'
ac_cr_AZ='ABCDEFGHIJKLMNOPQRSTUVWXYZ'
ac_cr_09='0123456789'
ac_cr_alnum=$ac_cr_az$ac_cr_AZ$ac_cr_09

# Sed expression to map a string onto a valid sh and CPP variable names.
ac_tr_sh="sed y%*+%pp%;s%[^_$ac_cr_alnum]%_%g"
ac_tr_cpp="sed y%*$ac_cr_az%P$ac_cr_AZ%;s%[^_$ac_cr_alnum]%_%g"

ac_unique_file="KERNEL/atom.C"
# Factoring default headers for most tests.
ac_includes_default="\
#include <stdio.h>
#include <sys/types.h>
#include <sys/stat.h>
#if STDC_HEADERS
# include <stdlib.h>
# include <stddef.h>
#else
# if HAVE_STDLIB_H
#  include <stdlib.h>
# endif
#endif
#if HAVE_STRING_H
# if !STDC_HEADERS && HAVE_MEMORY_H
#  include <memory.h>
# endif
# include <string.h>
#else
# if HAVE_STRINGS_H
#  include <strings.h>
# endif
#endif
#if HAVE_INTTYPES_H
# include <inttypes.h>
#endif
#if HAVE_UNISTD_H
# include <unistd.h>
#endif"

# Initialize some variables set by options.
ac_init_help=
ac_init_version=false
# The variables have the same names as the options, with
# dashes changed to underlines.
cache_file=/dev/null
exec_prefix=NONE
no_create=
no_recursion=
prefix=NONE
program_prefix=NONE
program_suffix=NONE
program_transform_name=s,x,x,
silent=
site=
srcdir=
verbose=
x_includes=NONE
x_libraries=NONE

# Installation directory options.
# These are left unexpanded so users can "make install exec_prefix=/foo"
# and all the variables that are supposed to be based on exec_prefix
# by default will actually change.
# Use braces instead of parens because sh, perl, etc. also accept them.
bindir='${exec_prefix}/bin'
sbindir='${exec_prefix}/sbin'
libexecdir='${exec_prefix}/libexec'
datadir='${prefix}/share'
sysconfdir='${prefix}/etc'
sharedstatedir='${prefix}/com'
localstatedir='${prefix}/var'
libdir='${exec_prefix}/lib'
includedir='${prefix}/include'
oldincludedir='/usr/include'
infodir='${prefix}/info'
mandir='${prefix}/man'

# Identity of this package.
PACKAGE_NAME=
PACKAGE_TARNAME=
PACKAGE_VERSION=
PACKAGE_STRING=
PACKAGE_BUGREPORT=

ac_prev=
for ac_option
do
  # If the previous option needs an argument, assign it.
  if test -n "$ac_prev"; then
    eval "$ac_prev=\$ac_option"
    ac_prev=
    continue
  fi

  ac_optarg=`expr "x$ac_option" : 'x[^=]*=\(.*\)'`

  # Accept the important Cygnus configure options, so we can diagnose typos.

  case $ac_option in

  -bindir | --bindir | --bindi | --bind | --bin | --bi)
    ac_prev=bindir ;;
  -bindir=* | --bindir=* | --bindi=* | --bind=* | --bin=* | --bi=*)
    bindir=$ac_optarg ;;

  -build | --build | --buil | --bui | --bu)
    ac_prev=build_alias ;;
  -build=* | --build=* | --buil=* | --bui=* | --bu=*)
    build_alias=$ac_optarg ;;

  -cache-file | --cache-file | --cache-fil | --cache-fi \
  | --cache-f | --cache- | --cache | --cach | --cac | --ca | --c)
    ac_prev=cache_file ;;
  -cache-file=* | --cache-file=* | --cache-fil=* | --cache-fi=* \
  | --cache-f=* | --cache-=* | --cache=* | --cach=* | --cac=* | --ca=* | --c=*)
    cache_file=$ac_optarg ;;

  --config-cache | -C)
    cache_file=config.cache ;;

  -datadir | --datadir | --datadi | --datad | --data | --dat | --da)
    ac_prev=datadir ;;
  -datadir=* | --datadir=* | --datadi=* | --datad=* | --data=* | --dat=* \
  | --da=*)
    datadir=$ac_optarg ;;

  -disable-* | --disable-*)
    ac_feature=`expr "x$ac_option" : 'x-*disable-\(.*\)'`
    # Reject names that are not valid shell variable names.
    expr "x$ac_feature" : ".*[^-_$ac_cr_alnum]" >/dev/null &&
      { echo "$as_me: error: invalid feature name: $ac_feature" >&2
   { (exit 1); exit 1; }; }
    ac_feature=`echo $ac_feature | sed 's/-/_/g'`
    eval "enable_$ac_feature=no" ;;

  -enable-* | --enable-*)
    ac_feature=`expr "x$ac_option" : 'x-*enable-\([^=]*\)'`
    # Reject names that are not valid shell variable names.
    expr "x$ac_feature" : ".*[^-_$ac_cr_alnum]" >/dev/null &&
      { echo "$as_me: error: invalid feature name: $ac_feature" >&2
   { (exit 1); exit 1; }; }
    ac_feature=`echo $ac_feature | sed 's/-/_/g'`
    case $ac_option in
      *=*) ac_optarg=`echo "$ac_optarg" | sed "s/'/'\\\\\\\\''/g"`;;
      *) ac_optarg=yes ;;
    esac
    eval "enable_$ac_feature='$ac_optarg'" ;;

  -exec-prefix | --exec_prefix | --exec-prefix | --exec-prefi \
  | --exec-pref | --exec-pre | --exec-pr | --exec-p | --exec- \
  | --exec | --exe | --ex)
    ac_prev=exec_prefix ;;
  -exec-prefix=* | --exec_prefix=* | --exec-prefix=* | --exec-prefi=* \
  | --exec-pref=* | --exec-pre=* | --exec-pr=* | --exec-p=* | --exec-=* \
  | --exec=* | --exe=* | --ex=*)
    exec_prefix=$ac_optarg ;;

  -gas | --gas | --ga | --g)
    # Obsolete; use --with-gas.
    with_gas=yes ;;

  -help | --help | --hel | --he | -h)
    ac_init_help=long ;;
  -help=r* | --help=r* | --hel=r* | --he=r* | -hr*)
    ac_init_help=recursive ;;
  -help=s* | --help=s* | --hel=s* | --he=s* | -hs*)
    ac_init_help=short ;;

  -host | --host | --hos | --ho)
    ac_prev=host_alias ;;
  -host=* | --host=* | --hos=* | --ho=*)
    host_alias=$ac_optarg ;;

  -includedir | --includedir | --includedi | --included | --include \
  | --includ | --inclu | --incl | --inc)
    ac_prev=includedir ;;
  -includedir=* | --includedir=* | --includedi=* | --included=* | --include=* \
  | --includ=* | --inclu=* | --incl=* | --inc=*)
    includedir=$ac_optarg ;;

  -infodir | --infodir | --infodi | --infod | --info | --inf)
    ac_prev=infodir ;;
  -infodir=* | --infodir=* | --infodi=* | --infod=* | --info=* | --inf=*)
    infodir=$ac_optarg ;;

  -libdir | --libdir | --libdi | --libd)
    ac_prev=libdir ;;
  -libdir=* | --libdir=* | --libdi=* | --libd=*)
    libdir=$ac_optarg ;;

  -libexecdir | --libexecdir | --libexecdi | --libexecd | --libexec \
  | --libexe | --libex | --libe)
    ac_prev=libexecdir ;;
  -libexecdir=* | --libexecdir=* | --libexecdi=* | --libexecd=* | --libexec=* \
  | --libexe=* | --libex=* | --libe=*)
    libexecdir=$ac_optarg ;;

  -localstatedir | --localstatedir | --localstatedi | --localstated \
  | --localstate | --localstat | --localsta | --localst \
  | --locals | --local | --loca | --loc | --lo)
    ac_prev=localstatedir ;;
  -localstatedir=* | --localstatedir=* | --localstatedi=* | --localstated=* \
  | --localstate=* | --localstat=* | --localsta=* | --localst=* \
  | --locals=* | --local=* | --loca=* | --loc=* | --lo=*)
    localstatedir=$ac_optarg ;;

  -mandir | --mandir | --mandi | --mand | --man | --ma | --m)
    ac_prev=mandir ;;
  -mandir=* | --mandir=* | --mandi=* | --mand=* | --man=* | --ma=* | --m=*)
    mandir=$ac_optarg ;;

  -nfp | --nfp | --nf)
    # Obsolete; use --without-fp.
    with_fp=no ;;

  -no-create | --no-create | --no-creat | --no-crea | --no-cre \
  | --no-cr | --no-c)
    no_create=yes ;;

  -no-recursion | --no-recursion | --no-recursio | --no-recursi \
  | --no-recurs | --no-recur | --no-recu | --no-rec | --no-re | --no-r)
    no_recursion=yes ;;

  -oldincludedir | --oldincludedir | --oldincludedi | --oldincluded \
  | --oldinclude | --oldinclud | --oldinclu | --oldincl | --oldinc \
  | --oldin | --oldi | --old | --ol | --o)
    ac_prev=oldincludedir ;;
  -oldincludedir=* | --oldincludedir=* | --oldincludedi=* | --oldincluded=* \
  | --oldinclude=* | --oldinclud=* | --oldinclu=* | --oldincl=* | --oldinc=* \
  | --oldin=* | --oldi=* | --old=* | --ol=* | --o=*)
    oldincludedir=$ac_optarg ;;

  -prefix | --prefix | --prefi | --pref | --pre | --pr | --p)
    ac_prev=prefix ;;
  -prefix=* | --prefix=* | --prefi=* | --pref=* | --pre=* | --pr=* | --p=*)
    prefix=$ac_optarg ;;

  -program-prefix | --program-prefix | --program-prefi | --program-pref \
  | --program-pre | --program-pr | --program-p)
    ac_prev=program_prefix ;;
  -program-prefix=* | --program-prefix=* | --program-prefi=* \
  | --program-pref=* | --program-pre=* | --program-pr=* | --program-p=*)
    program_prefix=$ac_optarg ;;

  -program-suffix | --program-suffix | --program-suffi | --program-suff \
  | --program-suf | --program-su | --program-s)
    ac_prev=program_suffix ;;
  -program-suffix=* | --program-suffix=* | --program-suffi=* \
  | --program-suff=* | --program-suf=* | --program-su=* | --program-s=*)
    program_suffix=$ac_optarg ;;

  -program-transform-name | --program-transform-name \
  | --program-transform-nam | --program-transform-na \
  | --program-transform-n | --program-transform- \
  | --program-transform | --program-transfor \
  | --program-transfo | --program-transf \
  | --program-trans | --program-tran \
  | --progr-tra | --program-tr | --program-t)
    ac_prev=program_transform_name ;;
  -program-transform-name=* | --program-transform-name=* \
  | --program-transform-nam=* | --program-transform-na=* \
  | --program-transform-n=* | --program-transform-=* \
  | --program-transform=* | --program-transfor=* \
  | --program-transfo=* | --program-transf=* \
  | --program-trans=* | --program-tran=* \
  | --progr-tra=* | --program-tr=* | --program-t=*)
    program_transform_name=$ac_optarg ;;

  -q | -quiet | --quiet | --quie | --qui | --qu | --q \
  | -silent | --silent | --silen | --sile | --sil)
    silent=yes ;;

  -sbindir | --sbindir | --sbindi | --sbind | --sbin | --sbi | --sb)
    ac_prev=sbindir ;;
  -sbindir=* | --sbindir=* | --sbindi=* | --sbind=* | --sbin=* \
  | --sbi=* | --sb=*)
    sbindir=$ac_optarg ;;

  -sharedstatedir | --sharedstatedir | --sharedstatedi \
  | --sharedstated | --sharedstate | --sharedstat | --sharedsta \
  | --sharedst | --shareds | --shared | --share | --shar \
  | --sha | --sh)
    ac_prev=sharedstatedir ;;
  -sharedstatedir=* | --sharedstatedir=* | --sharedstatedi=* \
  | --sharedstated=* | --sharedstate=* | --sharedstat=* | --sharedsta=* \
  | --sharedst=* | --shareds=* | --shared=* | --share=* | --shar=* \
  | --sha=* | --sh=*)
    sharedstatedir=$ac_optarg ;;

  -site | --site | --sit)
    ac_prev=site ;;
  -site=* | --site=* | --sit=*)
    site=$ac_optarg ;;

  -srcdir | --srcdir | --srcdi | --srcd | --src | --sr)
    ac_prev=srcdir ;;
  -srcdir=* | --srcdir=* | --srcdi=* | --srcd=* | --src=* | --sr=*)
    srcdir=$ac_optarg ;;

  -sysconfdir | --sysconfdir | --sysconfdi | --sysconfd | --sysconf \
  | --syscon | --sysco | --sysc | --sys | --sy)
    ac_prev=sysconfdir ;;
  -sysconfdir=* | --sysconfdir=* | --sysconfdi=* | --sysconfd=* | --sysconf=* \
  | --syscon=* | --sysco=* | --sysc=* | --sys=* | --sy=*)
    sysconfdir=$ac_optarg ;;

  -target | --target | --targe | --targ | --tar | --ta | --t)
    ac_prev=target_alias ;;
  -target=* | --target=* | --targe=* | --targ=* | --tar=* | --ta=* | --t=*)
    target_alias=$ac_optarg ;;

  -v | -verbose | --verbose | --verbos | --verbo | --verb)
    verbose=yes ;;

  -version | --version | --versio | --versi | --vers | -V)
    ac_init_version=: ;;

  -with-* | --with-*)
    ac_package=`expr "x$ac_option" : 'x-*with-\([^=]*\)'`
    # Reject names that are not valid shell variable names.
    expr "x$ac_package" : ".*[^-_$ac_cr_alnum]" >/dev/null &&
      { echo "$as_me: error: invalid package name: $ac_package" >&2
   { (exit 1); exit 1; }; }
    ac_package=`echo $ac_package| sed 's/-/_/g'`
    case $ac_option in
      *=*) ac_optarg=`echo "$ac_optarg" | sed "s/'/'\\\\\\\\''/g"`;;
      *) ac_optarg=yes ;;
    esac
    eval "with_$ac_package='$ac_optarg'" ;;

  -without-* | --without-*)
    ac_package=`expr "x$ac_option" : 'x-*without-\(.*\)'`
    # Reject names that are not valid shell variable names.
    expr "x$ac_package" : ".*[^-_$ac_cr_alnum]" >/dev/null &&
      { echo "$as_me: error: invalid package name: $ac_package" >&2
   { (exit 1); exit 1; }; }
    ac_package=`echo $ac_package | sed 's/-/_/g'`
    eval "with_$ac_package=no" ;;

  --x)
    # Obsolete; use --with-x.
    with_x=yes ;;

  -x-includes | --x-includes | --x-include | --x-includ | --x-inclu \
  | --x-incl | --x-inc | --x-in | --x-i)
    ac_prev=x_includes ;;
  -x-includes=* | --x-includes=* | --x-include=* | --x-includ=* | --x-inclu=* \
  | --x-incl=* | --x-inc=* | --x-in=* | --x-i=*)
    x_includes=$ac_optarg ;;

  -x-libraries | --x-libraries | --x-librarie | --x-librari \
  | --x-librar | --x-libra | --x-libr | --x-lib | --x-li | --x-l)
    ac_prev=x_libraries ;;
  -x-libraries=* | --x-libraries=* | --x-librarie=* | --x-librari=* \
  | --x-librar=* | --x-libra=* | --x-libr=* | --x-lib=* | --x-li=* | --x-l=*)
    x_libraries=$ac_optarg ;;

  -*) { echo "$as_me: error: unrecognized option: $ac_option
Try \`$0 --help' for more information." >&2
   { (exit 1); exit 1; }; }
    ;;

  *=*)
    ac_envvar=`expr "x$ac_option" : 'x\([^=]*\)='`
    # Reject names that are not valid shell variable names.
    expr "x$ac_envvar" : ".*[^_$ac_cr_alnum]" >/dev/null &&
      { echo "$as_me: error: invalid variable name: $ac_envvar" >&2
   { (exit 1); exit 1; }; }
    ac_optarg=`echo "$ac_optarg" | sed "s/'/'\\\\\\\\''/g"`
    eval "$ac_envvar='$ac_optarg'"
    export $ac_envvar ;;

  *)
    # FIXME: should be removed in autoconf 3.0.
    echo "$as_me: WARNING: you should use --build, --host, --target" >&2
    expr "x$ac_option" : ".*[^-._$ac_cr_alnum]" >/dev/null &&
      echo "$as_me: WARNING: invalid host type: $ac_option" >&2
    : ${build_alias=$ac_option} ${host_alias=$ac_option} ${target_alias=$ac_option}
    ;;

  esac
done

if test -n "$ac_prev"; then
  ac_option=--`echo $ac_prev | sed 's/_/-/g'`
  { echo "$as_me: error: missing argument to $ac_option" >&2
   { (exit 1); exit 1; }; }
fi

# Be sure to have absolute paths.
for ac_var in bindir sbindir libexecdir datadir sysconfdir sharedstatedir \
              localstatedir libdir includedir oldincludedir infodir mandir \
              exec_prefix prefix
do
  eval ac_val=$`echo $ac_var`
  case $ac_val in
    [\\/$]* | ?:[\\/]* ) ;;
    NONE ) ;;
    *)  { echo "$as_me: error: expected an absolute path for --$ac_var: $ac_val" >&2
   { (exit 1); exit 1; }; };;
  esac
done

# There might be people who depend on the old broken behavior: `$host'
# used to hold the argument of --host etc.
build=$build_alias
host=$host_alias
target=$target_alias

# FIXME: should be removed in autoconf 3.0.
if test "x$host_alias" != x; then
  if test "x$build_alias" = x; then
    cross_compiling=maybe
    echo "$as_me: WARNING: If you wanted to set the --build type, don't use --host.
    If a cross compiler is detected then cross compile mode will be used." >&2
  elif test "x$build_alias" != "x$host_alias"; then
    cross_compiling=yes
  fi
fi

ac_tool_prefix=
test -n "$host_alias" && ac_tool_prefix=$host_alias-

test "$silent" = yes && exec 6>/dev/null

# Find the source files, if location was not specified.
if test -z "$srcdir"; then
  ac_srcdir_defaulted=yes
  # Try the directory containing this script, then its parent.
  ac_prog=$0
  ac_confdir=`echo "$ac_prog" | sed 's%[\\/][^\\/][^\\/]*$%%'`
  test "x$ac_confdir" = "x$ac_prog" && ac_confdir=.
  srcdir=$ac_confdir
  if test ! -r $srcdir/$ac_unique_file; then
    srcdir=..
  fi
else
  ac_srcdir_defaulted=no
fi
if test ! -r $srcdir/$ac_unique_file; then
  if test "$ac_srcdir_defaulted" = yes; then
    { echo "$as_me: error: cannot find sources in $ac_confdir or .." >&2
   { (exit 1); exit 1; }; }
  else
    { echo "$as_me: error: cannot find sources in $srcdir" >&2
   { (exit 1); exit 1; }; }
  fi
fi
srcdir=`echo "$srcdir" | sed 's%\([^\\/]\)[\\/]*$%\1%'`
ac_env_build_alias_set=${build_alias+set}
ac_env_build_alias_value=$build_alias
ac_cv_env_build_alias_set=${build_alias+set}
ac_cv_env_build_alias_value=$build_alias
ac_env_host_alias_set=${host_alias+set}
ac_env_host_alias_value=$host_alias
ac_cv_env_host_alias_set=${host_alias+set}
ac_cv_env_host_alias_value=$host_alias
ac_env_target_alias_set=${target_alias+set}
ac_env_target_alias_value=$target_alias
ac_cv_env_target_alias_set=${target_alias+set}
ac_cv_env_target_alias_value=$target_alias
ac_env_CXX_set=${CXX+set}
ac_env_CXX_value=$CXX
ac_cv_env_CXX_set=${CXX+set}
ac_cv_env_CXX_value=$CXX
ac_env_CXXFLAGS_set=${CXXFLAGS+set}
ac_env_CXXFLAGS_value=$CXXFLAGS
ac_cv_env_CXXFLAGS_set=${CXXFLAGS+set}
ac_cv_env_CXXFLAGS_value=$CXXFLAGS
ac_env_LDFLAGS_set=${LDFLAGS+set}
ac_env_LDFLAGS_value=$LDFLAGS
ac_cv_env_LDFLAGS_set=${LDFLAGS+set}
ac_cv_env_LDFLAGS_value=$LDFLAGS
ac_env_CPPFLAGS_set=${CPPFLAGS+set}
ac_env_CPPFLAGS_value=$CPPFLAGS
ac_cv_env_CPPFLAGS_set=${CPPFLAGS+set}
ac_cv_env_CPPFLAGS_value=$CPPFLAGS
ac_env_CXXCPP_set=${CXXCPP+set}
ac_env_CXXCPP_value=$CXXCPP
ac_cv_env_CXXCPP_set=${CXXCPP+set}
ac_cv_env_CXXCPP_value=$CXXCPP

#
# Report the --help message.
#
if test "$ac_init_help" = "long"; then
  # Omit some internal or obsolete options to make the list less imposing.
  # This message is too long to be a string in the A/UX 3.1 sh.
  cat <<EOF
\`configure' configures this package to adapt to many kinds of systems.

Usage: $0 [OPTION]... [VAR=VALUE]...

To assign environment variables (e.g., CC, CFLAGS...), specify them as
VAR=VALUE.  See below for descriptions of some of the useful variables.

Defaults for the options are specified in brackets.

Configuration:
  -h, --help              display this help and exit
      --help=short        display options specific to this package
      --help=recursive    display the short help of all the included packages
  -V, --version           display version information and exit
  -q, --quiet, --silent   do not print \`checking...' messages
      --cache-file=FILE   cache test results in FILE [disabled]
  -C, --config-cache      alias for \`--cache-file=config.cache'
  -n, --no-create         do not create output files
      --srcdir=DIR        find the sources in DIR [configure dir or \`..']

EOF

  cat <<EOF
Installation directories:
  --prefix=PREFIX         install architecture-independent files in PREFIX
                          [$ac_default_prefix]
  --exec-prefix=EPREFIX   install architecture-dependent files in EPREFIX
                          [PREFIX]

By default, \`make install' will install all the files in
\`$ac_default_prefix/bin', \`$ac_default_prefix/lib' etc.  You can specify
an installation prefix other than \`$ac_default_prefix' using \`--prefix',
for instance \`--prefix=\$HOME'.

For better control, use the options below.

Fine tuning of the installation directories:
  --bindir=DIR           user executables [EPREFIX/bin]
  --sbindir=DIR          system admin executables [EPREFIX/sbin]
  --libexecdir=DIR       program executables [EPREFIX/libexec]
  --datadir=DIR          read-only architecture-independent data [PREFIX/share]
  --sysconfdir=DIR       read-only single-machine data [PREFIX/etc]
  --sharedstatedir=DIR   modifiable architecture-independent data [PREFIX/com]
  --localstatedir=DIR    modifiable single-machine data [PREFIX/var]
  --libdir=DIR           object code libraries [EPREFIX/lib]
  --includedir=DIR       C header files [PREFIX/include]
  --oldincludedir=DIR    C header files for non-gcc [/usr/include]
  --infodir=DIR          info documentation [PREFIX/info]
  --mandir=DIR           man documentation [PREFIX/man]
EOF

  cat <<\EOF

X features:
  --x-includes=DIR    X include files are in DIR
  --x-libraries=DIR   X library files are in DIR
EOF
fi

if test -n "$ac_init_help"; then

  cat <<\EOF

Optional Features:
  --disable-FEATURE       do not include FEATURE (same as --enable-FEATURE=no)
  --enable-FEATURE[=ARG]  include FEATURE [ARG=yes]
  --enable-optimization   optimize the library for speed. Omits debug info!
  --enable-debuginfo      add -g to the compiler flags (to create debug information)
  --disable-BALLVIEW      disable the compilation of BALLVIEW, the visualization component
  --enable-64             create 64 bit binaries (if allowed by the compiler)

Optional Packages:
  --with-PACKAGE[=ARG]    use PACKAGE [ARG=yes]
  --without-PACKAGE       do not use PACKAGE (same as --with-PACKAGE=no)
  --with-compiler=CXX     use CXX as C++ compiler
  --with-cxxflags=FLAGS   add FLAGS to the C++ compiler flags (commas are converted to blanks)
  --with-ldflags=FLAGS    add FLAGS to the linker flags (commas are converted to blanks)
  --with-arflags=FLAGS    add FLAGS to the flags for the creation of the static libraries
  --with-dynarflags=FLAGS add FLAGS to the flags for the creation of the shared libraries
  --with-qt-incl=DIR      QT header files are in DIR
  --with-qt-libs=DIR      QT libraries are in DIR
  --with-threadsafe-qt    use a threadsafe version of the QT lib (libqt-mt)
  --with-moc=MOC          use MOC as the absolute path to the QT meta object compiler (moc)
  --with-opengl-incl=DIR  OpenGL/Mesa header files are in DIR/GL
  --with-opengl-libs=DIR  OpenGL/Mesa libraries are in DIR/GL
  --with-mesa             use MESA instead of OpenGL
  --without-libxnet       use -lsocket -lnsl rather than -lxnet (under Solaris)
  --with-python           enable Python support
  --with-python-incl=DIR  Python includes (Python.h) is in DIR
  --with-python-libs=DIR  Python library (libpython*.a) is in DIR
  --with-python-ldopts=X  Use additional options X when linking with the Python library
  --with-sip-lib=DIR      the SIP library resides in DIR
  --with-sip-incl=DIR     the SIP header file resides in DIR
  --with-sip=DIR          the SIP executable resides in DIR
  --without-xdr           no RPC/XDR headers available - do not build portable binary persistence support
  --with-x                use the X Window System

Some influential environment variables:
  CXX         C++ compiler command
  CXXFLAGS    C++ compiler flags
  LDFLAGS     linker flags, e.g. -L<lib dir> if you have libraries in a
              nonstandard directory <lib dir>
  CPPFLAGS    C/C++ preprocessor flags, e.g. -I<include dir> if you have
              headers in a nonstandard directory <include dir>
  CXXCPP      C++ preprocessor

Use these variables to override the choices made by `configure' or to help
it to find libraries and programs with nonstandard names/locations.

EOF
fi

if test "$ac_init_help" = "recursive"; then
  # If there are subdirs, report their specific --help.
  ac_popdir=`pwd`
  for ac_subdir in : $ac_subdirs_all; do test "x$ac_subdir" = x: && continue
    cd $ac_subdir
    # A "../" for each directory in /$ac_subdir.
    ac_dots=`echo $ac_subdir |
             sed 's,^\./,,;s,[^/]$,&/,;s,[^/]*/,../,g'`

    case $srcdir in
    .) # No --srcdir option.  We are building in place.
      ac_sub_srcdir=$srcdir ;;
    [\\/]* | ?:[\\/]* ) # Absolute path.
      ac_sub_srcdir=$srcdir/$ac_subdir ;;
    *) # Relative path.
      ac_sub_srcdir=$ac_dots$srcdir/$ac_subdir ;;
    esac

    # Check for guested configure; otherwise get Cygnus style configure.
    if test -f $ac_sub_srcdir/configure.gnu; then
      echo
      $SHELL $ac_sub_srcdir/configure.gnu  --help=recursive
    elif test -f $ac_sub_srcdir/configure; then
      echo
      $SHELL $ac_sub_srcdir/configure  --help=recursive
    elif test -f $ac_sub_srcdir/configure.ac ||
           test -f $ac_sub_srcdir/configure.in; then
      echo
      $ac_configure --help
    else
      echo "$as_me: WARNING: no configuration information is in $ac_subdir" >&2
    fi
    cd $ac_popdir
  done
fi

test -n "$ac_init_help" && exit 0
if $ac_init_version; then
  cat <<\EOF

Copyright 1992, 1993, 1994, 1995, 1996, 1998, 1999, 2000, 2001
Free Software Foundation, Inc.
This configure script is free software; the Free Software Foundation
gives unlimited permission to copy, distribute and modify it.
EOF
  exit 0
fi
exec 5>config.log
cat >&5 <<EOF
This file contains any messages produced by compilers while
running configure, to aid debugging if configure makes a mistake.

It was created by $as_me, which was
generated by GNU Autoconf 2.50.  Invocation command line was

  $ $0 $@

EOF
{
cat <<_ASUNAME
## ---------- ##
## Platform.  ##
## ---------- ##

hostname = `(hostname || uname -n) 2>/dev/null | sed 1q`
uname -m = `(uname -m) 2>/dev/null || echo unknown`
uname -r = `(uname -r) 2>/dev/null || echo unknown`
uname -s = `(uname -s) 2>/dev/null || echo unknown`
uname -v = `(uname -v) 2>/dev/null || echo unknown`

/usr/bin/uname -p = `(/usr/bin/uname -p) 2>/dev/null || echo unknown`
/bin/uname -X     = `(/bin/uname -X) 2>/dev/null     || echo unknown`

/bin/arch              = `(/bin/arch) 2>/dev/null              || echo unknown`
/usr/bin/arch -k       = `(/usr/bin/arch -k) 2>/dev/null       || echo unknown`
/usr/convex/getsysinfo = `(/usr/convex/getsysinfo) 2>/dev/null || echo unknown`
hostinfo               = `(hostinfo) 2>/dev/null               || echo unknown`
/bin/machine           = `(/bin/machine) 2>/dev/null           || echo unknown`
/usr/bin/oslevel       = `(/usr/bin/oslevel) 2>/dev/null       || echo unknown`
/bin/universe          = `(/bin/universe) 2>/dev/null          || echo unknown`

PATH = $PATH

_ASUNAME
} >&5

cat >&5 <<EOF
## ------------ ##
## Core tests.  ##
## ------------ ##

EOF

# Keep a trace of the command line.
# Strip out --no-create and --no-recursion so they do not pile up.
# Also quote any args containing shell meta-characters.
ac_configure_args=
ac_sep=
for ac_arg
do
  case $ac_arg in
  -no-create | --no-create | --no-creat | --no-crea | --no-cre \
  | --no-cr | --no-c) ;;
  -no-recursion | --no-recursion | --no-recursio | --no-recursi \
  | --no-recurs | --no-recur | --no-recu | --no-rec | --no-re | --no-r) ;;
  *" "*|*"	"*|*[\[\]\~\#\$\^\&\*\(\)\{\}\\\|\;\<\>\?\"\']*)
    ac_arg=`echo "$ac_arg" | sed "s/'/'\\\\\\\\''/g"`
    ac_configure_args="$ac_configure_args$ac_sep'$ac_arg'"
    ac_sep=" " ;;
  *) ac_configure_args="$ac_configure_args$ac_sep$ac_arg"
     ac_sep=" " ;;
  esac
  # Get rid of the leading space.
done

# When interrupted or exit'd, cleanup temporary files, and complete
# config.log.  We remove comments because anyway the quotes in there
# would cause problems or look ugly.
trap 'exit_status=$?
  # Save into config.log some information that might help in debugging.
  echo >&5
  echo "## ----------------- ##" >&5
  echo "## Cache variables.  ##" >&5
  echo "## ----------------- ##" >&5
  echo >&5
  # The following way of writing the cache mishandles newlines in values,
{
  (set) 2>&1 |
    case `(ac_space='"'"' '"'"'; set | grep ac_space) 2>&1` in
    *ac_space=\ *)
      sed -n \
        "s/'"'"'/'"'"'\\\\'"'"''"'"'/g;
    	  s/^\\([_$ac_cr_alnum]*_cv_[_$ac_cr_alnum]*\\)=\\(.*\\)/\\1='"'"'\\2'"'"'/p"
      ;;
    *)
      sed -n \
        "s/^\\([_$ac_cr_alnum]*_cv_[_$ac_cr_alnum]*\\)=\\(.*\\)/\\1=\\2/p"
      ;;
    esac;
} >&5
  sed "/^$/d" confdefs.h >conftest.log
  if test -s conftest.log; then
    echo >&5
    echo "## ------------ ##" >&5
    echo "## confdefs.h.  ##" >&5
    echo "## ------------ ##" >&5
    echo >&5
    cat conftest.log >&5
  fi
  (echo; echo) >&5
  test "$ac_signal" != 0 &&
    echo "$as_me: caught signal $ac_signal" >&5
  echo "$as_me: exit $exit_status" >&5
  rm -rf conftest* confdefs* core core.* *.core conf$$* $ac_clean_files &&
    exit $exit_status
     ' 0
for ac_signal in 1 2 13 15; do
  trap 'ac_status=$?; ac_signal='$ac_signal'; { (exit $ac_status); exit $ac_status; }' $ac_signal
done
ac_signal=0

# confdefs.h avoids OS command line length limits that DEFS can exceed.
rm -rf conftest* confdefs.h
# AIX cpp loses on an empty file, so make sure it contains at least a newline.
echo >confdefs.h

# Let the site file select an alternate cache file if it wants to.
# Prefer explicitly selected file to automatically selected ones.
if test -z "$CONFIG_SITE"; then
  if test "x$prefix" != xNONE; then
    CONFIG_SITE="$prefix/share/config.site $prefix/etc/config.site"
  else
    CONFIG_SITE="$ac_default_prefix/share/config.site $ac_default_prefix/etc/config.site"
  fi
fi
for ac_site_file in $CONFIG_SITE; do
  if test -r "$ac_site_file"; then
    { echo "$as_me:874: loading site script $ac_site_file" >&5
echo "$as_me: loading site script $ac_site_file" >&6;}
    cat "$ac_site_file" >&5
    . "$ac_site_file"
  fi
done

if test -r "$cache_file"; then
  # Some versions of bash will fail to source /dev/null (special
  # files actually), so we avoid doing that.
  if test -f "$cache_file"; then
    { echo "$as_me:885: loading cache $cache_file" >&5
echo "$as_me: loading cache $cache_file" >&6;}
    case $cache_file in
      [\\/]* | ?:[\\/]* ) . $cache_file;;
      *)                      . ./$cache_file;;
    esac
  fi
else
  { echo "$as_me:893: creating cache $cache_file" >&5
echo "$as_me: creating cache $cache_file" >&6;}
  >$cache_file
fi

# Check that the precious variables saved in the cache have kept the same
# value.
ac_suggest_removing_cache=false
for ac_var in `(set) 2>&1 |
               sed -n 's/^ac_env_\([a-zA-Z_0-9]*\)_set=.*/\1/p'`; do
  eval ac_old_set=\$ac_cv_env_${ac_var}_set
  eval ac_new_set=\$ac_env_${ac_var}_set
  eval ac_old_val="\$ac_cv_env_${ac_var}_value"
  eval ac_new_val="\$ac_env_${ac_var}_value"
  case $ac_old_set,$ac_new_set in
    set,)
      { echo "$as_me:909: WARNING: \`$ac_var' was set to \`$ac_old_val' in the previous run" >&5
echo "$as_me: WARNING: \`$ac_var' was set to \`$ac_old_val' in the previous run" >&2;}
      ac_suggest_removing_cache=: ;;
    ,set)
      { echo "$as_me:913: WARNING: \`$ac_var' was not set in the previous run" >&5
echo "$as_me: WARNING: \`$ac_var' was not set in the previous run" >&2;}
      ac_suggest_removing_cache=: ;;
    ,);;
    *)
      if test "x$ac_old_val" != "x$ac_new_val"; then
        { echo "$as_me:919: WARNING: \`$ac_var' has changed since the previous run:" >&5
echo "$as_me: WARNING: \`$ac_var' has changed since the previous run:" >&2;}
        { echo "$as_me:921: WARNING:   former value:  $ac_old_val" >&5
echo "$as_me: WARNING:   former value:  $ac_old_val" >&2;}
        { echo "$as_me:923: WARNING:   current value: $ac_new_val" >&5
echo "$as_me: WARNING:   current value: $ac_new_val" >&2;}
        ac_suggest_removing_cache=:
      fi;;
  esac
done
if $ac_suggest_removing_cache; then
  { echo "$as_me:930: WARNING: changes in the environment can compromise the build" >&5
echo "$as_me: WARNING: changes in the environment can compromise the build" >&2;}
  { echo "$as_me:932: WARNING: consider removing $cache_file and starting over" >&5
echo "$as_me: WARNING: consider removing $cache_file and starting over" >&2;}
fi

ac_ext=c
ac_cpp='$CPP $CPPFLAGS'
ac_compile='$CC -c $CFLAGS $CPPFLAGS conftest.$ac_ext >&5'
ac_link='$CC -o conftest$ac_exeext $CFLAGS $CPPFLAGS $LDFLAGS conftest.$ac_ext $LIBS >&5'
ac_compiler_gnu=$ac_cv_c_compiler_gnu

case `echo "testing\c"; echo 1,2,3`,`echo -n testing; echo 1,2,3` in
  *c*,-n*) ECHO_N= ECHO_C='
' ECHO_T='	' ;;
  *c*,*  ) ECHO_N=-n ECHO_C= ECHO_T= ;;
  *)      ECHO_N= ECHO_C='\c' ECHO_T= ;;
esac
echo "#! $SHELL" >conftest.sh
echo  "exit 0"   >>conftest.sh
chmod +x conftest.sh
if { (echo "$as_me:951: PATH=\".;.\"; conftest.sh") >&5
  (PATH=".;."; conftest.sh) 2>&5
  ac_status=$?
  echo "$as_me:954: \$? = $ac_status" >&5
  (exit $ac_status); }; then
  ac_path_separator=';'
else
  ac_path_separator=:
fi
PATH_SEPARATOR="$ac_path_separator"
rm -f conftest.sh

ac_config_headers="$ac_config_headers config.h:config/config.h.in"

ac_ext=cc
ac_cpp='$CXXCPP $CPPFLAGS'
ac_compile='$CXX -c $CXXFLAGS $CPPFLAGS conftest.$ac_ext >&5'
ac_link='$CXX -o conftest$ac_exeext $CXXFLAGS $CPPFLAGS $LDFLAGS conftest.$ac_ext $LIBS >&5'
ac_compiler_gnu=$ac_cv_cxx_compiler_gnu

# Extract the first word of "more", so it can be a program name with args.
set dummy more; ac_word=$2
echo "$as_me:973: checking for $ac_word" >&5
echo $ECHO_N "checking for $ac_word... $ECHO_C" >&6
if test "${ac_cv_path_PAGER+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  case $PAGER in
  [\\/]* | ?:[\\/]*)
  ac_cv_path_PAGER="$PAGER" # Let the user override the test with a path.
  ;;
  *)
  ac_save_IFS=$IFS; IFS=$ac_path_separator
ac_dummy="$PATH"
for ac_dir in $ac_dummy; do
  IFS=$ac_save_IFS
  test -z "$ac_dir" && ac_dir=.
  if $as_executable_p "$ac_dir/$ac_word"; then
   ac_cv_path_PAGER="$ac_dir/$ac_word"
   echo "$as_me:990: found $ac_dir/$ac_word" >&5
   break
fi
done

  test -z "$ac_cv_path_PAGER" && ac_cv_path_PAGER="no"
  ;;
esac
fi
PAGER=$ac_cv_path_PAGER

if test -n "$PAGER"; then
  echo "$as_me:1002: result: $PAGER" >&5
echo "${ECHO_T}$PAGER" >&6
else
  echo "$as_me:1005: result: no" >&5
echo "${ECHO_T}no" >&6
fi

if test "${PAGER}" = "no" ; then
	PAGER=cat
fi
if test ! -f config.lic ; then
	${PAGER} LICENSE
	echo " "
	echo "Do you accept this license terms (y/n)?"
	answer=""
	while test "$answer" != "y" -a "$answer" != "n" ; do
		read answer
	done
	if test "$answer" = "n" ; then
		exit
	else
		echo "accepted" > config.lic
	fi
fi

# Check whether --enable-optimization or --disable-optimization was given.
if test "${enable_optimization+set}" = set; then
  enableval="$enable_optimization"

fi;
# Check whether --enable-debuginfo or --disable-debuginfo was given.
if test "${enable_debuginfo+set}" = set; then
  enableval="$enable_debuginfo"

fi;
# Check whether --enable-BALLVIEW or --disable-BALLVIEW was given.
if test "${enable_BALLVIEW+set}" = set; then
  enableval="$enable_BALLVIEW"

fi;
# Check whether --enable-64 or --disable-64 was given.
if test "${enable_64+set}" = set; then
  enableval="$enable_64"

fi;

# Check whether --with-compiler or --without-compiler was given.
if test "${with_compiler+set}" = set; then
  withval="$with_compiler"

fi;

# Check whether --with-cxxflags or --without-cxxflags was given.
if test "${with_cxxflags+set}" = set; then
  withval="$with_cxxflags"

fi;

# Check whether --with-ldflags or --without-ldflags was given.
if test "${with_ldflags+set}" = set; then
  withval="$with_ldflags"

fi;

# Check whether --with-arflags or --without-arflags was given.
if test "${with_arflags+set}" = set; then
  withval="$with_arflags"

fi;

# Check whether --with-dynarflags or --without-dynarflags was given.
if test "${with_dynarflags+set}" = set; then
  withval="$with_dynarflags"

fi;

# Check whether --with-qt-incl or --without-qt-incl was given.
if test "${with_qt_incl+set}" = set; then
  withval="$with_qt_incl"

fi;

# Check whether --with-qt-libs or --without-qt-libs was given.
if test "${with_qt_libs+set}" = set; then
  withval="$with_qt_libs"

fi;

# Check whether --with-qt-mt or --without-qt-mt was given.
if test "${with_qt_mt+set}" = set; then
  withval="$with_qt_mt"

fi;

# Check whether --with-moc or --without-moc was given.
if test "${with_moc+set}" = set; then
  withval="$with_moc"

fi;

# Check whether --with-opengl-incl or --without-opengl-incl was given.
if test "${with_opengl_incl+set}" = set; then
  withval="$with_opengl_incl"

fi;

# Check whether --with-opengl-libs or --without-opengl-libs was given.
if test "${with_opengl_libs+set}" = set; then
  withval="$with_opengl_libs"

fi;

# Check whether --with-mesa or --without-mesa was given.
if test "${with_mesa+set}" = set; then
  withval="$with_mesa"

fi;

# Check whether --with-libxnet or --without-libxnet was given.
if test "${with_libxnet+set}" = set; then
  withval="$with_libxnet"

fi;

# Check whether --with-python or --without-python was given.
if test "${with_python+set}" = set; then
  withval="$with_python"

fi;

# Check whether --with-python-incl or --without-python-incl was given.
if test "${with_python_incl+set}" = set; then
  withval="$with_python_incl"

fi;

# Check whether --with-python-libs or --without-python-libs was given.
if test "${with_python_libs+set}" = set; then
  withval="$with_python_libs"

fi;

# Check whether --with-python-ldopts or --without-python-ldopts was given.
if test "${with_python_ldopts+set}" = set; then
  withval="$with_python_ldopts"

fi;

# Check whether --with-sip-lib or --without-sip-lib was given.
if test "${with_sip_lib+set}" = set; then
  withval="$with_sip_lib"

fi;

# Check whether --with-sip-incl or --without-sip-incl was given.
if test "${with_sip_incl+set}" = set; then
  withval="$with_sip_incl"

fi;

# Check whether --with-sip or --without-sip was given.
if test "${with_sip+set}" = set; then
  withval="$with_sip"

fi;

# Check whether --with-xdr or --without-xdr was given.
if test "${with_xdr+set}" = set; then
  withval="$with_xdr"

fi;

if test "$enable_optimization" = "yes" ; then
	DEBUG=false
else
	DEBUG=true
fi

if test "$enable_debuginfo" = "yes" ; then
	DEBUG_INFO=true
else
	DEBUG_INFO=false
fi

if test "$enable_64" = "yes" ; then
	BINFMT_64_BIT=true
else
	BINFMT_64_BIT=false
fi

if test "$with_compiler" != "" ; then
	CXX=$with_compiler
fi

if test "$with_cxxflags" != "" ; then
	CXXFLAGS="${CXXFLAGS}`echo $with_cxxflags|tr , \  `"
fi

if test "$with_ldflags" != "" ; then
	LDFLAGS="${LDFLAGS}`echo $with_ldflags|tr ,  \  `"
fi

if test "$with_dynarflags" != "" ; then
	DYNAROPTS="${DYNAROPTS}`echo $with_dynarflags|tr ,  \  `"
fi

if test "$with_arflags" != "" ; then
	AROPTS="${AROPTS}`echo $with_arflags|tr ,  \  `"
fi

if test "$with_qt_incl" != "" ; then
	QT_INCPATH=$with_qt_incl
fi
if test "$with_qt_libs" != "" ; then
	QT_LIBPATH=$with_qt_libs
fi
if test "$with_qt_mt" = "yes" ; then
	USE_THREADSAFE_QT=true
	QT_MT_SUFFIX="-mt"
else
	USE_THREADSAFE_QT=false
	QT_MT_SUFFIX=""
fi
if test "$with_opengl_incl" != "" ; then
	OPENGL_INCPATH=$with_opengl_incl
fi
if test "$with_opengl_libs" != "" ; then
	OPENGL_LIBPATH=$with_opengl_libs
fi

if test "$with_mesa" != "yes" ; then
	BALLVIEW_PLATFORM=OpenGL
else
 	BALLVIEW_PLATFORM=Mesa
fi

if test "$with_moc" != "" ; then
	MOC=$with_moc
else
 	MOC=moc
fi

if test "$with_libxnet" != "no" ; then
	USE_LIBXNET=true
else
	USE_LIBXNET=false
fi

if test "$with_BALLVIEW" = "no" ; then
	USE_BALLVIEW=false
else
	USE_BALLVIEW=true
fi

if test "$with_xdr" = "no" ; then
	BALL_NO_XDR=true
else
	BALL_NO_XDR=false
fi

# use this switch to enable the python support
# this also builds libBALLc.so and requires
# Python.h and libsip.a
if test "$with_python" = "yes" -o "$enable_python" = yes ; then
	PYTHON_SUPPORT=true
else
	PYTHON_SUPPORT=
fi

# the path to the python includes (Python.h)
PYTHON_INCLUDE_PATH=$with_python_incl

# the path to the python library (libpython*.a)
# and additional linker options (e.g. -ltermcap etc.)
PYTHON_LIBPATH=$with_python_libs
PYTHON_LDOPTS=$with_python_ldopts

# the paths to the SIP library, header, and executable
SIP_LIBPATH=$with_sip_lib
SIP_INCLUDE_PATH=$with_sip_incl
SIP=$with_sip

BINFORMAT_FILE=config/binary_formats

BALL_PATH=`cd ..; pwd`

	# Extract the first word of "uname", so it can be a program name with args.
set dummy uname; ac_word=$2
echo "$as_me:1290: checking for $ac_word" >&5
echo $ECHO_N "checking for $ac_word... $ECHO_C" >&6
if test "${ac_cv_path_UNAME+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  case $UNAME in
  [\\/]* | ?:[\\/]*)
  ac_cv_path_UNAME="$UNAME" # Let the user override the test with a path.
  ;;
  *)
  ac_save_IFS=$IFS; IFS=$ac_path_separator
ac_dummy="$PATH"
for ac_dir in $ac_dummy; do
  IFS=$ac_save_IFS
  test -z "$ac_dir" && ac_dir=.
  if $as_executable_p "$ac_dir/$ac_word"; then
   ac_cv_path_UNAME="$ac_dir/$ac_word"
   echo "$as_me:1307: found $ac_dir/$ac_word" >&5
   break
fi
done

  test -z "$ac_cv_path_UNAME" && ac_cv_path_UNAME="no"
  ;;
esac
fi
UNAME=$ac_cv_path_UNAME

if test -n "$UNAME"; then
  echo "$as_me:1319: result: $UNAME" >&5
echo "${ECHO_T}$UNAME" >&6
else
  echo "$as_me:1322: result: no" >&5
echo "${ECHO_T}no" >&6
fi

	if test $UNAME = no ; then
		echo "$as_me:1327: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:1329: result: This script requires the unix command uname, but cannot find it." >&5
echo "${ECHO_T}This script requires the unix command uname, but cannot find it." >&6
		echo "$as_me:1331: result: Please add the correct path to uname to your \$PATH variable" >&5
echo "${ECHO_T}Please add the correct path to uname to your \$PATH variable" >&6
		echo "$as_me:1333: result: and restart configure." >&5
echo "${ECHO_T}and restart configure." >&6
		echo "$as_me:1335: result: " >&5
echo "${ECHO_T}" >&6
		{ { echo "$as_me:1337: error: aborted" >&5
echo "$as_me: error: aborted" >&2;}
   { (exit 1); exit 1; }; }
		exit
	fi

	# Extract the first word of "cut", so it can be a program name with args.
set dummy cut; ac_word=$2
echo "$as_me:1345: checking for $ac_word" >&5
echo $ECHO_N "checking for $ac_word... $ECHO_C" >&6
if test "${ac_cv_path_CUT+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  case $CUT in
  [\\/]* | ?:[\\/]*)
  ac_cv_path_CUT="$CUT" # Let the user override the test with a path.
  ;;
  *)
  ac_save_IFS=$IFS; IFS=$ac_path_separator
ac_dummy="$PATH"
for ac_dir in $ac_dummy; do
  IFS=$ac_save_IFS
  test -z "$ac_dir" && ac_dir=.
  if $as_executable_p "$ac_dir/$ac_word"; then
   ac_cv_path_CUT="$ac_dir/$ac_word"
   echo "$as_me:1362: found $ac_dir/$ac_word" >&5
   break
fi
done

  test -z "$ac_cv_path_CUT" && ac_cv_path_CUT="no"
  ;;
esac
fi
CUT=$ac_cv_path_CUT

if test -n "$CUT"; then
  echo "$as_me:1374: result: $CUT" >&5
echo "${ECHO_T}$CUT" >&6
else
  echo "$as_me:1377: result: no" >&5
echo "${ECHO_T}no" >&6
fi

	if test $CUT = no ; then
		echo "$as_me:1382: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:1384: result: This script requires the unix command cut, but cannot find it." >&5
echo "${ECHO_T}This script requires the unix command cut, but cannot find it." >&6
		echo "$as_me:1386: result: Please add the correct path to cut to your \$PATH variable" >&5
echo "${ECHO_T}Please add the correct path to cut to your \$PATH variable" >&6
		echo "$as_me:1388: result: and restart configure." >&5
echo "${ECHO_T}and restart configure." >&6
		echo "$as_me:1390: result: " >&5
echo "${ECHO_T}" >&6
		{ { echo "$as_me:1392: error: aborted" >&5
echo "$as_me: error: aborted" >&2;}
   { (exit 1); exit 1; }; }
		exit
	fi

	# Extract the first word of "tr", so it can be a program name with args.
set dummy tr; ac_word=$2
echo "$as_me:1400: checking for $ac_word" >&5
echo $ECHO_N "checking for $ac_word... $ECHO_C" >&6
if test "${ac_cv_path_TR+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  case $TR in
  [\\/]* | ?:[\\/]*)
  ac_cv_path_TR="$TR" # Let the user override the test with a path.
  ;;
  *)
  ac_save_IFS=$IFS; IFS=$ac_path_separator
ac_dummy="$PATH"
for ac_dir in $ac_dummy; do
  IFS=$ac_save_IFS
  test -z "$ac_dir" && ac_dir=.
  if $as_executable_p "$ac_dir/$ac_word"; then
   ac_cv_path_TR="$ac_dir/$ac_word"
   echo "$as_me:1417: found $ac_dir/$ac_word" >&5
   break
fi
done

  test -z "$ac_cv_path_TR" && ac_cv_path_TR="no"
  ;;
esac
fi
TR=$ac_cv_path_TR

if test -n "$TR"; then
  echo "$as_me:1429: result: $TR" >&5
echo "${ECHO_T}$TR" >&6
else
  echo "$as_me:1432: result: no" >&5
echo "${ECHO_T}no" >&6
fi

	if test $TR = no ; then
		echo "$as_me:1437: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:1439: result: This script requires the unix command tr, but cannot find it." >&5
echo "${ECHO_T}This script requires the unix command tr, but cannot find it." >&6
		echo "$as_me:1441: result: Please add the correct path to tr to your \$PATH variable" >&5
echo "${ECHO_T}Please add the correct path to tr to your \$PATH variable" >&6
		echo "$as_me:1443: result: and restart configure." >&5
echo "${ECHO_T}and restart configure." >&6
		echo "$as_me:1445: result: " >&5
echo "${ECHO_T}" >&6
		{ { echo "$as_me:1447: error: aborted" >&5
echo "$as_me: error: aborted" >&2;}
   { (exit 1); exit 1; }; }
		exit
	fi

	# Extract the first word of "ar", so it can be a program name with args.
set dummy ar; ac_word=$2
echo "$as_me:1455: checking for $ac_word" >&5
echo $ECHO_N "checking for $ac_word... $ECHO_C" >&6
if test "${ac_cv_path_AR+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  case $AR in
  [\\/]* | ?:[\\/]*)
  ac_cv_path_AR="$AR" # Let the user override the test with a path.
  ;;
  *)
  ac_save_IFS=$IFS; IFS=$ac_path_separator
ac_dummy="$PATH"
for ac_dir in $ac_dummy; do
  IFS=$ac_save_IFS
  test -z "$ac_dir" && ac_dir=.
  if $as_executable_p "$ac_dir/$ac_word"; then
   ac_cv_path_AR="$ac_dir/$ac_word"
   echo "$as_me:1472: found $ac_dir/$ac_word" >&5
   break
fi
done

  test -z "$ac_cv_path_AR" && ac_cv_path_AR="no"
  ;;
esac
fi
AR=$ac_cv_path_AR

if test -n "$AR"; then
  echo "$as_me:1484: result: $AR" >&5
echo "${ECHO_T}$AR" >&6
else
  echo "$as_me:1487: result: no" >&5
echo "${ECHO_T}no" >&6
fi

	if test $AR = no ; then
		echo "$as_me:1492: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:1494: result: This script requires the unix command ar, but cannot find it." >&5
echo "${ECHO_T}This script requires the unix command ar, but cannot find it." >&6
		echo "$as_me:1496: result: Please add the correct path to ar to your \$PATH variable" >&5
echo "${ECHO_T}Please add the correct path to ar to your \$PATH variable" >&6
		echo "$as_me:1498: result: and restart configure." >&5
echo "${ECHO_T}and restart configure." >&6
		echo "$as_me:1500: result: " >&5
echo "${ECHO_T}" >&6
		{ { echo "$as_me:1502: error: aborted" >&5
echo "$as_me: error: aborted" >&2;}
   { (exit 1); exit 1; }; }
		exit
	fi

echo "$as_me:1508: checking your OS" >&5
echo $ECHO_N "checking your OS... $ECHO_C" >&6
OS=`${UNAME} -s`
OSREV=`${UNAME} -r`
OSMAJOR=`echo $OSREV|${CUT} -d"." -f1`

BINFMT="${OS}"

if test "$OS" = SunOS ; then
	if test "$OSMAJOR" = 5 ; then
		OS=Solaris
		ARCHITECTURE=`${UNAME} -p`
		BINFMT="${OS}-${OSREV}-${ARCHITECTURE}"
	else
		OS=SunOS
	fi
fi

if test "$OS" = Linux ; then
	PROCESSOR=`${UNAME} -m`
	ARCHITECTURE=unknown
	if test "${PROCESSOR}" = sparc -o "${PROCESSOR}" = SPARC ; then
		ARCHITECTURE=sparc
		BINFMT=Linux-sparc
	fi
	if test `echo $PROCESSOR|${CUT} -c1` = i ; then
		ARCHITECTURE=i386
		BINFMT=Linux-i386
	fi
	if test `echo $PROCESSOR` = alpha ; then
		ARCHITECTURE=alpha
		BINFMT=Linux-alpha
	fi

	if test "${ARCHITECTURE}" = "unknown" ; then
		echo "$as_me:1543: result: OS: ${OS} / hardware: ${PROCESSOR}" >&5
echo "${ECHO_T}OS: ${OS} / hardware: ${PROCESSOR}" >&6
		echo "$as_me:1545: result: Sorry - this architecture is currently not supported..." >&5
echo "${ECHO_T}Sorry - this architecture is currently not supported..." >&6
		{ { echo "$as_me:1547: error: aborted" >&5
echo "$as_me: error: aborted" >&2;}
   { (exit 1); exit 1; }; }
	fi
fi

if test "${OS}" = IRIX64 ; then
	OS=IRIX
fi

if test "${OS}" = IRIX ; then
	BINFMT=IRIX-${OSREV}
fi

if test "${OS}" = OSF1 ; then
	BINFMT="OSF1-${OSREV}"
	PROCESSOR=`${UNAME} -m`
	ARCHITECTURE=unknown
	if test `echo $PROCESSOR` = alpha ; then
		ARCHITECTURE=alpha
	fi
fi

if test "`echo $OS | ${CUT} -d_ -f1`" = "CYGWIN" ; then
	OS="CYGWIN"
	BALL_NO_XDR=true
	USE_BALLVIEW=false
fi

if test "$OS" != Linux -a "$OS" != Solaris -a "$OS" != IRIX -a  "$OS" != OSF1 -a "$OS" != FreeBSD -a "$OS" != "CYGWIN" ; then
	echo "$as_me:1577: result: Sorry - your OS is currently not supported..." >&5
echo "${ECHO_T}Sorry - your OS is currently not supported..." >&6
	{ { echo "$as_me:1579: error: aborted" >&5
echo "$as_me: error: aborted" >&2;}
   { (exit 1); exit 1; }; }
fi

if test "${OS}" = Linux ; then
	cat >>confdefs.h <<\EOF
#define BALL_OS_LINUX LINUX
EOF

fi
if test "${OS}" = Solaris ; then
	cat >>confdefs.h <<\EOF
#define BALL_OS_SOLARIS SOLARIS
EOF

fi
if test "${OS}" = IRIX ; then
	cat >>confdefs.h <<\EOF
#define BALL_OS_IRIX IRIX
EOF

fi
if test "${OS}" = OSF1 ; then
	cat >>confdefs.h <<\EOF
#define BALL_OS_OSF1 OSF1
EOF

fi
if test "${OS}" = FreeBSD ; then
	cat >>confdefs.h <<\EOF
#define BALL_OS_FREEBSD FREEBSD
EOF

fi

if test "$ARCHITECTURE" = sparc ; then
	cat >>confdefs.h <<\EOF
#define BALL_ARCH_SPARC SPARC
EOF

fi
if test "$ARCHITECTURE" = i386 ; then
	cat >>confdefs.h <<\EOF
#define BALL_ARCH_I386 I386
EOF

fi
if test "$ARCHITECTURE" = mips ; then
	cat >>confdefs.h <<\EOF
#define BALL_ARCH_MIPS MIPS
EOF

fi
if test "$ARCHITECTURE" = alpha ; then
	cat >>confdefs.h <<\EOF
#define BALL_ARCH_ALPHA ALPHA
EOF

fi

echo "$as_me:1640: result: $OS $OSREV (BINFMT=$BINFMT)" >&5
echo "${ECHO_T}$OS $OSREV (BINFMT=$BINFMT)" >&6

SHARED_LIB_SUFFIX=so
if test "${OS}" = HP-UX ; then
	SHARED_LIB_SUFFIX=sl
fi

echo "$as_me:1648: checking whether ln -s works" >&5
echo $ECHO_N "checking whether ln -s works... $ECHO_C" >&6
LN_S=$as_ln_s
if test "$LN_S" = "ln -s"; then
  echo "$as_me:1652: result: yes" >&5
echo "${ECHO_T}yes" >&6
else
  echo "$as_me:1655: result: no, using $LN_S" >&5
echo "${ECHO_T}no, using $LN_S" >&6
fi

	# Extract the first word of "cp", so it can be a program name with args.
set dummy cp; ac_word=$2
echo "$as_me:1661: checking for $ac_word" >&5
echo $ECHO_N "checking for $ac_word... $ECHO_C" >&6
if test "${ac_cv_path_CP+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  case $CP in
  [\\/]* | ?:[\\/]*)
  ac_cv_path_CP="$CP" # Let the user override the test with a path.
  ;;
  *)
  ac_save_IFS=$IFS; IFS=$ac_path_separator
ac_dummy="$PATH"
for ac_dir in $ac_dummy; do
  IFS=$ac_save_IFS
  test -z "$ac_dir" && ac_dir=.
  if $as_executable_p "$ac_dir/$ac_word"; then
   ac_cv_path_CP="$ac_dir/$ac_word"
   echo "$as_me:1678: found $ac_dir/$ac_word" >&5
   break
fi
done

  test -z "$ac_cv_path_CP" && ac_cv_path_CP="no"
  ;;
esac
fi
CP=$ac_cv_path_CP

if test -n "$CP"; then
  echo "$as_me:1690: result: $CP" >&5
echo "${ECHO_T}$CP" >&6
else
  echo "$as_me:1693: result: no" >&5
echo "${ECHO_T}no" >&6
fi

	if test $CP = no ; then
		echo "$as_me:1698: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:1700: result: This script requires the unix command cp, but cannot find it." >&5
echo "${ECHO_T}This script requires the unix command cp, but cannot find it." >&6
		echo "$as_me:1702: result: Please add the correct path to cp to your \$PATH variable" >&5
echo "${ECHO_T}Please add the correct path to cp to your \$PATH variable" >&6
		echo "$as_me:1704: result: and restart configure." >&5
echo "${ECHO_T}and restart configure." >&6
		echo "$as_me:1706: result: " >&5
echo "${ECHO_T}" >&6
		{ { echo "$as_me:1708: error: aborted" >&5
echo "$as_me: error: aborted" >&2;}
   { (exit 1); exit 1; }; }
		exit
	fi

	# Extract the first word of "rm", so it can be a program name with args.
set dummy rm; ac_word=$2
echo "$as_me:1716: checking for $ac_word" >&5
echo $ECHO_N "checking for $ac_word... $ECHO_C" >&6
if test "${ac_cv_path_RM+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  case $RM in
  [\\/]* | ?:[\\/]*)
  ac_cv_path_RM="$RM" # Let the user override the test with a path.
  ;;
  *)
  ac_save_IFS=$IFS; IFS=$ac_path_separator
ac_dummy="$PATH"
for ac_dir in $ac_dummy; do
  IFS=$ac_save_IFS
  test -z "$ac_dir" && ac_dir=.
  if $as_executable_p "$ac_dir/$ac_word"; then
   ac_cv_path_RM="$ac_dir/$ac_word"
   echo "$as_me:1733: found $ac_dir/$ac_word" >&5
   break
fi
done

  test -z "$ac_cv_path_RM" && ac_cv_path_RM="no"
  ;;
esac
fi
RM=$ac_cv_path_RM

if test -n "$RM"; then
  echo "$as_me:1745: result: $RM" >&5
echo "${ECHO_T}$RM" >&6
else
  echo "$as_me:1748: result: no" >&5
echo "${ECHO_T}no" >&6
fi

	if test $RM = no ; then
		echo "$as_me:1753: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:1755: result: This script requires the unix command rm, but cannot find it." >&5
echo "${ECHO_T}This script requires the unix command rm, but cannot find it." >&6
		echo "$as_me:1757: result: Please add the correct path to rm to your \$PATH variable" >&5
echo "${ECHO_T}Please add the correct path to rm to your \$PATH variable" >&6
		echo "$as_me:1759: result: and restart configure." >&5
echo "${ECHO_T}and restart configure." >&6
		echo "$as_me:1761: result: " >&5
echo "${ECHO_T}" >&6
		{ { echo "$as_me:1763: error: aborted" >&5
echo "$as_me: error: aborted" >&2;}
   { (exit 1); exit 1; }; }
		exit
	fi

	# Extract the first word of "mv", so it can be a program name with args.
set dummy mv; ac_word=$2
echo "$as_me:1771: checking for $ac_word" >&5
echo $ECHO_N "checking for $ac_word... $ECHO_C" >&6
if test "${ac_cv_path_MV+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  case $MV in
  [\\/]* | ?:[\\/]*)
  ac_cv_path_MV="$MV" # Let the user override the test with a path.
  ;;
  *)
  ac_save_IFS=$IFS; IFS=$ac_path_separator
ac_dummy="$PATH"
for ac_dir in $ac_dummy; do
  IFS=$ac_save_IFS
  test -z "$ac_dir" && ac_dir=.
  if $as_executable_p "$ac_dir/$ac_word"; then
   ac_cv_path_MV="$ac_dir/$ac_word"
   echo "$as_me:1788: found $ac_dir/$ac_word" >&5
   break
fi
done

  test -z "$ac_cv_path_MV" && ac_cv_path_MV="no"
  ;;
esac
fi
MV=$ac_cv_path_MV

if test -n "$MV"; then
  echo "$as_me:1800: result: $MV" >&5
echo "${ECHO_T}$MV" >&6
else
  echo "$as_me:1803: result: no" >&5
echo "${ECHO_T}no" >&6
fi

	if test $MV = no ; then
		echo "$as_me:1808: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:1810: result: This script requires the unix command mv, but cannot find it." >&5
echo "${ECHO_T}This script requires the unix command mv, but cannot find it." >&6
		echo "$as_me:1812: result: Please add the correct path to mv to your \$PATH variable" >&5
echo "${ECHO_T}Please add the correct path to mv to your \$PATH variable" >&6
		echo "$as_me:1814: result: and restart configure." >&5
echo "${ECHO_T}and restart configure." >&6
		echo "$as_me:1816: result: " >&5
echo "${ECHO_T}" >&6
		{ { echo "$as_me:1818: error: aborted" >&5
echo "$as_me: error: aborted" >&2;}
   { (exit 1); exit 1; }; }
		exit
	fi

	# Extract the first word of "ln", so it can be a program name with args.
set dummy ln; ac_word=$2
echo "$as_me:1826: checking for $ac_word" >&5
echo $ECHO_N "checking for $ac_word... $ECHO_C" >&6
if test "${ac_cv_path_LN+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  case $LN in
  [\\/]* | ?:[\\/]*)
  ac_cv_path_LN="$LN" # Let the user override the test with a path.
  ;;
  *)
  ac_save_IFS=$IFS; IFS=$ac_path_separator
ac_dummy="$PATH"
for ac_dir in $ac_dummy; do
  IFS=$ac_save_IFS
  test -z "$ac_dir" && ac_dir=.
  if $as_executable_p "$ac_dir/$ac_word"; then
   ac_cv_path_LN="$ac_dir/$ac_word"
   echo "$as_me:1843: found $ac_dir/$ac_word" >&5
   break
fi
done

  test -z "$ac_cv_path_LN" && ac_cv_path_LN="no"
  ;;
esac
fi
LN=$ac_cv_path_LN

if test -n "$LN"; then
  echo "$as_me:1855: result: $LN" >&5
echo "${ECHO_T}$LN" >&6
else
  echo "$as_me:1858: result: no" >&5
echo "${ECHO_T}no" >&6
fi

	if test $LN = no ; then
		echo "$as_me:1863: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:1865: result: This script requires the unix command ln, but cannot find it." >&5
echo "${ECHO_T}This script requires the unix command ln, but cannot find it." >&6
		echo "$as_me:1867: result: Please add the correct path to ln to your \$PATH variable" >&5
echo "${ECHO_T}Please add the correct path to ln to your \$PATH variable" >&6
		echo "$as_me:1869: result: and restart configure." >&5
echo "${ECHO_T}and restart configure." >&6
		echo "$as_me:1871: result: " >&5
echo "${ECHO_T}" >&6
		{ { echo "$as_me:1873: error: aborted" >&5
echo "$as_me: error: aborted" >&2;}
   { (exit 1); exit 1; }; }
		exit
	fi

	# Extract the first word of "sed", so it can be a program name with args.
set dummy sed; ac_word=$2
echo "$as_me:1881: checking for $ac_word" >&5
echo $ECHO_N "checking for $ac_word... $ECHO_C" >&6
if test "${ac_cv_path_SED+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  case $SED in
  [\\/]* | ?:[\\/]*)
  ac_cv_path_SED="$SED" # Let the user override the test with a path.
  ;;
  *)
  ac_save_IFS=$IFS; IFS=$ac_path_separator
ac_dummy="$PATH"
for ac_dir in $ac_dummy; do
  IFS=$ac_save_IFS
  test -z "$ac_dir" && ac_dir=.
  if $as_executable_p "$ac_dir/$ac_word"; then
   ac_cv_path_SED="$ac_dir/$ac_word"
   echo "$as_me:1898: found $ac_dir/$ac_word" >&5
   break
fi
done

  test -z "$ac_cv_path_SED" && ac_cv_path_SED="no"
  ;;
esac
fi
SED=$ac_cv_path_SED

if test -n "$SED"; then
  echo "$as_me:1910: result: $SED" >&5
echo "${ECHO_T}$SED" >&6
else
  echo "$as_me:1913: result: no" >&5
echo "${ECHO_T}no" >&6
fi

	if test $SED = no ; then
		echo "$as_me:1918: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:1920: result: This script requires the unix command sed, but cannot find it." >&5
echo "${ECHO_T}This script requires the unix command sed, but cannot find it." >&6
		echo "$as_me:1922: result: Please add the correct path to sed to your \$PATH variable" >&5
echo "${ECHO_T}Please add the correct path to sed to your \$PATH variable" >&6
		echo "$as_me:1924: result: and restart configure." >&5
echo "${ECHO_T}and restart configure." >&6
		echo "$as_me:1926: result: " >&5
echo "${ECHO_T}" >&6
		{ { echo "$as_me:1928: error: aborted" >&5
echo "$as_me: error: aborted" >&2;}
   { (exit 1); exit 1; }; }
		exit
	fi

	# Extract the first word of "find", so it can be a program name with args.
set dummy find; ac_word=$2
echo "$as_me:1936: checking for $ac_word" >&5
echo $ECHO_N "checking for $ac_word... $ECHO_C" >&6
if test "${ac_cv_path_FIND+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  case $FIND in
  [\\/]* | ?:[\\/]*)
  ac_cv_path_FIND="$FIND" # Let the user override the test with a path.
  ;;
  *)
  ac_save_IFS=$IFS; IFS=$ac_path_separator
ac_dummy="$PATH"
for ac_dir in $ac_dummy; do
  IFS=$ac_save_IFS
  test -z "$ac_dir" && ac_dir=.
  if $as_executable_p "$ac_dir/$ac_word"; then
   ac_cv_path_FIND="$ac_dir/$ac_word"
   echo "$as_me:1953: found $ac_dir/$ac_word" >&5
   break
fi
done

  test -z "$ac_cv_path_FIND" && ac_cv_path_FIND="no"
  ;;
esac
fi
FIND=$ac_cv_path_FIND

if test -n "$FIND"; then
  echo "$as_me:1965: result: $FIND" >&5
echo "${ECHO_T}$FIND" >&6
else
  echo "$as_me:1968: result: no" >&5
echo "${ECHO_T}no" >&6
fi

	if test $FIND = no ; then
		echo "$as_me:1973: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:1975: result: This script requires the unix command find, but cannot find it." >&5
echo "${ECHO_T}This script requires the unix command find, but cannot find it." >&6
		echo "$as_me:1977: result: Please add the correct path to find to your \$PATH variable" >&5
echo "${ECHO_T}Please add the correct path to find to your \$PATH variable" >&6
		echo "$as_me:1979: result: and restart configure." >&5
echo "${ECHO_T}and restart configure." >&6
		echo "$as_me:1981: result: " >&5
echo "${ECHO_T}" >&6
		{ { echo "$as_me:1983: error: aborted" >&5
echo "$as_me: error: aborted" >&2;}
   { (exit 1); exit 1; }; }
		exit
	fi

	# Extract the first word of "diff", so it can be a program name with args.
set dummy diff; ac_word=$2
echo "$as_me:1991: checking for $ac_word" >&5
echo $ECHO_N "checking for $ac_word... $ECHO_C" >&6
if test "${ac_cv_path_DIFF+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  case $DIFF in
  [\\/]* | ?:[\\/]*)
  ac_cv_path_DIFF="$DIFF" # Let the user override the test with a path.
  ;;
  *)
  ac_save_IFS=$IFS; IFS=$ac_path_separator
ac_dummy="$PATH"
for ac_dir in $ac_dummy; do
  IFS=$ac_save_IFS
  test -z "$ac_dir" && ac_dir=.
  if $as_executable_p "$ac_dir/$ac_word"; then
   ac_cv_path_DIFF="$ac_dir/$ac_word"
   echo "$as_me:2008: found $ac_dir/$ac_word" >&5
   break
fi
done

  test -z "$ac_cv_path_DIFF" && ac_cv_path_DIFF="no"
  ;;
esac
fi
DIFF=$ac_cv_path_DIFF

if test -n "$DIFF"; then
  echo "$as_me:2020: result: $DIFF" >&5
echo "${ECHO_T}$DIFF" >&6
else
  echo "$as_me:2023: result: no" >&5
echo "${ECHO_T}no" >&6
fi

	if test $DIFF = no ; then
		echo "$as_me:2028: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:2030: result: This script requires the unix command diff, but cannot find it." >&5
echo "${ECHO_T}This script requires the unix command diff, but cannot find it." >&6
		echo "$as_me:2032: result: Please add the correct path to diff to your \$PATH variable" >&5
echo "${ECHO_T}Please add the correct path to diff to your \$PATH variable" >&6
		echo "$as_me:2034: result: and restart configure." >&5
echo "${ECHO_T}and restart configure." >&6
		echo "$as_me:2036: result: " >&5
echo "${ECHO_T}" >&6
		{ { echo "$as_me:2038: error: aborted" >&5
echo "$as_me: error: aborted" >&2;}
   { (exit 1); exit 1; }; }
		exit
	fi

	# Extract the first word of "tail", so it can be a program name with args.
set dummy tail; ac_word=$2
echo "$as_me:2046: checking for $ac_word" >&5
echo $ECHO_N "checking for $ac_word... $ECHO_C" >&6
if test "${ac_cv_path_TAIL+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  case $TAIL in
  [\\/]* | ?:[\\/]*)
  ac_cv_path_TAIL="$TAIL" # Let the user override the test with a path.
  ;;
  *)
  ac_save_IFS=$IFS; IFS=$ac_path_separator
ac_dummy="$PATH"
for ac_dir in $ac_dummy; do
  IFS=$ac_save_IFS
  test -z "$ac_dir" && ac_dir=.
  if $as_executable_p "$ac_dir/$ac_word"; then
   ac_cv_path_TAIL="$ac_dir/$ac_word"
   echo "$as_me:2063: found $ac_dir/$ac_word" >&5
   break
fi
done

  test -z "$ac_cv_path_TAIL" && ac_cv_path_TAIL="no"
  ;;
esac
fi
TAIL=$ac_cv_path_TAIL

if test -n "$TAIL"; then
  echo "$as_me:2075: result: $TAIL" >&5
echo "${ECHO_T}$TAIL" >&6
else
  echo "$as_me:2078: result: no" >&5
echo "${ECHO_T}no" >&6
fi

	if test $TAIL = no ; then
		echo "$as_me:2083: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:2085: result: This script requires the unix command tail, but cannot find it." >&5
echo "${ECHO_T}This script requires the unix command tail, but cannot find it." >&6
		echo "$as_me:2087: result: Please add the correct path to tail to your \$PATH variable" >&5
echo "${ECHO_T}Please add the correct path to tail to your \$PATH variable" >&6
		echo "$as_me:2089: result: and restart configure." >&5
echo "${ECHO_T}and restart configure." >&6
		echo "$as_me:2091: result: " >&5
echo "${ECHO_T}" >&6
		{ { echo "$as_me:2093: error: aborted" >&5
echo "$as_me: error: aborted" >&2;}
   { (exit 1); exit 1; }; }
		exit
	fi

	# Extract the first word of "grep", so it can be a program name with args.
set dummy grep; ac_word=$2
echo "$as_me:2101: checking for $ac_word" >&5
echo $ECHO_N "checking for $ac_word... $ECHO_C" >&6
if test "${ac_cv_path_GREP+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  case $GREP in
  [\\/]* | ?:[\\/]*)
  ac_cv_path_GREP="$GREP" # Let the user override the test with a path.
  ;;
  *)
  ac_save_IFS=$IFS; IFS=$ac_path_separator
ac_dummy="$PATH"
for ac_dir in $ac_dummy; do
  IFS=$ac_save_IFS
  test -z "$ac_dir" && ac_dir=.
  if $as_executable_p "$ac_dir/$ac_word"; then
   ac_cv_path_GREP="$ac_dir/$ac_word"
   echo "$as_me:2118: found $ac_dir/$ac_word" >&5
   break
fi
done

  test -z "$ac_cv_path_GREP" && ac_cv_path_GREP="no"
  ;;
esac
fi
GREP=$ac_cv_path_GREP

if test -n "$GREP"; then
  echo "$as_me:2130: result: $GREP" >&5
echo "${ECHO_T}$GREP" >&6
else
  echo "$as_me:2133: result: no" >&5
echo "${ECHO_T}no" >&6
fi

	if test $GREP = no ; then
		echo "$as_me:2138: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:2140: result: This script requires the unix command grep, but cannot find it." >&5
echo "${ECHO_T}This script requires the unix command grep, but cannot find it." >&6
		echo "$as_me:2142: result: Please add the correct path to grep to your \$PATH variable" >&5
echo "${ECHO_T}Please add the correct path to grep to your \$PATH variable" >&6
		echo "$as_me:2144: result: and restart configure." >&5
echo "${ECHO_T}and restart configure." >&6
		echo "$as_me:2146: result: " >&5
echo "${ECHO_T}" >&6
		{ { echo "$as_me:2148: error: aborted" >&5
echo "$as_me: error: aborted" >&2;}
   { (exit 1); exit 1; }; }
		exit
	fi

	# Extract the first word of "cat", so it can be a program name with args.
set dummy cat; ac_word=$2
echo "$as_me:2156: checking for $ac_word" >&5
echo $ECHO_N "checking for $ac_word... $ECHO_C" >&6
if test "${ac_cv_path_CAT+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  case $CAT in
  [\\/]* | ?:[\\/]*)
  ac_cv_path_CAT="$CAT" # Let the user override the test with a path.
  ;;
  *)
  ac_save_IFS=$IFS; IFS=$ac_path_separator
ac_dummy="$PATH"
for ac_dir in $ac_dummy; do
  IFS=$ac_save_IFS
  test -z "$ac_dir" && ac_dir=.
  if $as_executable_p "$ac_dir/$ac_word"; then
   ac_cv_path_CAT="$ac_dir/$ac_word"
   echo "$as_me:2173: found $ac_dir/$ac_word" >&5
   break
fi
done

  test -z "$ac_cv_path_CAT" && ac_cv_path_CAT="no"
  ;;
esac
fi
CAT=$ac_cv_path_CAT

if test -n "$CAT"; then
  echo "$as_me:2185: result: $CAT" >&5
echo "${ECHO_T}$CAT" >&6
else
  echo "$as_me:2188: result: no" >&5
echo "${ECHO_T}no" >&6
fi

	if test $CAT = no ; then
		echo "$as_me:2193: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:2195: result: This script requires the unix command cat, but cannot find it." >&5
echo "${ECHO_T}This script requires the unix command cat, but cannot find it." >&6
		echo "$as_me:2197: result: Please add the correct path to cat to your \$PATH variable" >&5
echo "${ECHO_T}Please add the correct path to cat to your \$PATH variable" >&6
		echo "$as_me:2199: result: and restart configure." >&5
echo "${ECHO_T}and restart configure." >&6
		echo "$as_me:2201: result: " >&5
echo "${ECHO_T}" >&6
		{ { echo "$as_me:2203: error: aborted" >&5
echo "$as_me: error: aborted" >&2;}
   { (exit 1); exit 1; }; }
		exit
	fi

if test "${FIND}" != "no" ; then
	RESULT=`${FIND} KERNEL -path . -print 2>&1`
	if test "${RESULT}" != "" ; then     		FIND_KNOWS_PATH=false
	else
		FIND_KNOWS_PATH=true
	fi
fi

if test "${OS}" = "Solaris" ; then
	CXX_SEARCH_ORDER="egcs g++ eg++ CC "
else
	if test "${OS}" = "OSF1" ; then
		CXX_SEARCH_ORDER="cxx CC egcs g++ eg++ "
	else
		CXX_SEARCH_ORDER="CC egcs g++ eg++ "
	fi
fi
CXX_NAME=""

if test "${OS}" = "IRIX" ; then
	CXX_SEARCH_ORDER="${CXX_SEARCH_ORDER} NCC DCC OCC"
	CC_SEARCH_ORDER="${CC_SEARCH_ORDER} ncc dcc occ"
fi

echo "$as_me:2233: result: searching for a C++ compiler:" >&5
echo "${ECHO_T}searching for a C++ compiler:" >&6
if test "${CXX}" != "" ; then
	if test -x "${CXX}" ; then
		echo "$as_me:2237: result: from the command line: ${CXX}" >&5
echo "${ECHO_T}from the command line: ${CXX}" >&6
	else
		# Extract the first word of "${CXX}", so it can be a program name with args.
set dummy ${CXX}; ac_word=$2
echo "$as_me:2242: checking for $ac_word" >&5
echo $ECHO_N "checking for $ac_word... $ECHO_C" >&6
if test "${ac_cv_path_CXXPATH+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  case $CXXPATH in
  [\\/]* | ?:[\\/]*)
  ac_cv_path_CXXPATH="$CXXPATH" # Let the user override the test with a path.
  ;;
  *)
  ac_save_IFS=$IFS; IFS=$ac_path_separator
ac_dummy="$PATH"
for ac_dir in $ac_dummy; do
  IFS=$ac_save_IFS
  test -z "$ac_dir" && ac_dir=.
  if $as_executable_p "$ac_dir/$ac_word"; then
   ac_cv_path_CXXPATH="$ac_dir/$ac_word"
   echo "$as_me:2259: found $ac_dir/$ac_word" >&5
   break
fi
done

  test -z "$ac_cv_path_CXXPATH" && ac_cv_path_CXXPATH="no"
  ;;
esac
fi
CXXPATH=$ac_cv_path_CXXPATH

if test -n "$CXXPATH"; then
  echo "$as_me:2271: result: $CXXPATH" >&5
echo "${ECHO_T}$CXXPATH" >&6
else
  echo "$as_me:2274: result: no" >&5
echo "${ECHO_T}no" >&6
fi

		if test "${CXXPATH}" = no ; then
			echo "$as_me:2279: result: " >&5
echo "${ECHO_T}" >&6
			echo "$as_me:2281: result: Cannot find ${CXX}. Please add it to your PATH" >&5
echo "${ECHO_T}Cannot find ${CXX}. Please add it to your PATH" >&6
			echo "$as_me:2283: result: or specify an absolute path in configure." >&5
echo "${ECHO_T}or specify an absolute path in configure." >&6
			{ { echo "$as_me:2285: error: aborted" >&5
echo "$as_me: error: aborted" >&2;}
   { (exit 1); exit 1; }; }
		else
			CXX=${CXXPATH}
		fi
	fi
else
	CXXPATH=""
	while test "${CXXPATH}" = "" ; do
		CXX=`echo ${CXX_SEARCH_ORDER}|${CUT} -d\  -f1`
		if test _`echo ${CXX} | ${TR} -d " "`_ = __ ; then
			CXXPATH="END"
		fi
		if test "${CXXPATH}" != "END" ; then
			# Extract the first word of "${CXX}", so it can be a program name with args.
set dummy ${CXX}; ac_word=$2
echo "$as_me:2302: checking for $ac_word" >&5
echo $ECHO_N "checking for $ac_word... $ECHO_C" >&6
if test "${ac_cv_path_CXXPATH+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  case $CXXPATH in
  [\\/]* | ?:[\\/]*)
  ac_cv_path_CXXPATH="$CXXPATH" # Let the user override the test with a path.
  ;;
  *)
  ac_save_IFS=$IFS; IFS=$ac_path_separator
ac_dummy="$PATH"
for ac_dir in $ac_dummy; do
  IFS=$ac_save_IFS
  test -z "$ac_dir" && ac_dir=.
  if $as_executable_p "$ac_dir/$ac_word"; then
   ac_cv_path_CXXPATH="$ac_dir/$ac_word"
   echo "$as_me:2319: found $ac_dir/$ac_word" >&5
   break
fi
done

  test -z "$ac_cv_path_CXXPATH" && ac_cv_path_CXXPATH="no"
  ;;
esac
fi
CXXPATH=$ac_cv_path_CXXPATH

if test -n "$CXXPATH"; then
  echo "$as_me:2331: result: $CXXPATH" >&5
echo "${ECHO_T}$CXXPATH" >&6
else
  echo "$as_me:2334: result: no" >&5
echo "${ECHO_T}no" >&6
fi

			if test "${CXXPATH}" = no ; then
				CXXPATH=""
				unset ac_cv_path_CXXPATH
			else
				CXX=${CXXPATH}
			fi
		fi

		CXX_SEARCH_ORDER=`echo "${CXX_SEARCH_ORDER} " |${CUT} -d\  -f2-`
	done

	if test "${CXXPATH}" = "end" ; then
		echo "$as_me:2350: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:2352: result: Could not find a C++ compiler. Please change the settings" >&5
echo "${ECHO_T}Could not find a C++ compiler. Please change the settings" >&6
		echo "$as_me:2354: result: of your PATH environment variable (using setenv export)" >&5
echo "${ECHO_T}of your PATH environment variable (using setenv export)" >&6
		echo "$as_me:2356: result: or specify an absolute path in configure by setting the variable" >&5
echo "${ECHO_T}or specify an absolute path in configure by setting the variable" >&6
		echo "$as_me:2358: result: CXX=<pathname> or specify the compiler by passing the option" >&5
echo "${ECHO_T}CXX=<pathname> or specify the compiler by passing the option" >&6
		echo "$as_me:2360: result: --with-compiler=<compiler> to configure." >&5
echo "${ECHO_T}--with-compiler=<compiler> to configure." >&6
		echo "$as_me:2362: result: " >&5
echo "${ECHO_T}" >&6
		{ { echo "$as_me:2364: error: aborted" >&5
echo "$as_me: error: aborted" >&2;}
   { (exit 1); exit 1; }; }
	fi
fi

if test "${CXX_PATH}" = "" ; then
	if test "${CXX}" = "" ; then
		CXX_NAME=unknown
	else
		CXX_NAME="${CXX}"
	fi
else
	CXX_NAME="${CXX_PATH}"
fi

while test "`echo ${CXX_NAME}|  ${GREP} /`" != "" ; do
	CXX_NAME=`echo ${CXX_NAME} |  ${CUT} -d/ -f2-`
done

echo "$as_me:2384: checking for GNU compiler" >&5
echo $ECHO_N "checking for GNU compiler... $ECHO_C" >&6
cat > /tmp/$$.conftest.c << EOF
#ifdef __GNUC__
GXX:true
#else
GXX:false
#endif
EOF

IS_GXX=`${CXX} -E /tmp/$$.conftest.c 2>/dev/null |egrep GXX|${CUT} -d: -f2|${TR} -d " "`
if test "${IS_GXX}" = "true" ; then
	echo "$as_me:2396: result: yes" >&5
echo "${ECHO_T}yes" >&6
	HAS_GPLUSPLUS=true
	CXX_NAME="g++"
else
	echo "$as_me:2401: result: no" >&5
echo "${ECHO_T}no" >&6
	HAS_GPLUSPLUS=false
fi
rm /tmp/$$.conftest.c

if test "${IS_GXX}" = "false" ; then
	echo "$as_me:2408: checking for KAI C++ compiler" >&5
echo $ECHO_N "checking for KAI C++ compiler... $ECHO_C" >&6
	KAI=`${CXX} -v --version 2>&1 | sed "s/.*KAI.*/__KAI__/g" |sed "s/.*kai.*/__KAI__/g" | egrep "^__KAI__$" | sed -n 1p`
	if test "${KAI}" = "__KAI__" ; then
		IS_KCC=true
		echo "$as_me:2413: result: yes" >&5
echo "${ECHO_T}yes" >&6
		CXX_NAME="KCC"
	else
		IS_KCC=false
		echo "$as_me:2418: result: no" >&5
echo "${ECHO_T}no" >&6
	fi
fi

if test "${IS_GXX}" = false -a "${IS_KCC}" = false ; then
	echo "$as_me:2424: checking for Digital/Compaq C++ compiler" >&5
echo $ECHO_N "checking for Digital/Compaq C++ compiler... $ECHO_C" >&6
	DIGITAL_CXX=`${CXX} -V 2>/dev/null | ${GREP} "C++" | ${CUT} -d" " -f-2`
	if test "${DIGITAL_CXX}" = "DIGITAL C++" -o "${DIGITAL_CXX}" = "Compaq C++"; then
		IS_DIGITALCXX=true
		echo "$as_me:2429: result: yes" >&5
echo "${ECHO_T}yes" >&6
		CXX_NAME="Digital"
	else
		IS_DIGITALCXX=false
		echo "$as_me:2434: result: no" >&5
echo "${ECHO_T}no" >&6
	fi
fi

if test "${IS_GXX}" = "true" ; then
	echo "$as_me:2440: checking compiler version" >&5
echo $ECHO_N "checking compiler version... $ECHO_C" >&6
	CXX_VERSION=`${CXX} --version`
	if test `echo ${CXX_VERSION}|${CUT} -c1-4` = "egcs" ; then
		IS_EGXX=true
		CXX_NAME="egcs"
		CXX_VERSION=`${CXX} -v 2>&1 | grep release | ${CUT} -d\( -f2 | cut -d\) -f1 | ${SED} "s/egcs-//" | ${CUT} -d" " -f1`
		VERSION_OUTPUT="egcs ${CXX_VERSION}"
		CXX_COMPILER_NAME="egcs"
	else
		IS_EGXX=false
		VERSION_OUTPUT="g++ ${CXX_VERSION}"
		CXX_COMPILER_NAME="g++"
	fi

	echo "$as_me:2455: result: ${VERSION_OUTPUT}" >&5
echo "${ECHO_T}${VERSION_OUTPUT}" >&6

	CXX_VERSION_1=`echo ${CXX_VERSION} | ${CUT} -d. -f1`
	CXX_VERSION_LENGTH=`echo ${CXX_VERSION} | sed "s/^.//g" | wc -c`
	if test "${CXX_VERSION_LENGTH}" -ge 2 ; then
		CXX_VERSION_2=`echo ${CXX_VERSION} | ${CUT} -d. -f2`
	fi
	if test "${CXX_VERSION_LENGTH}" -ge 3 ; then
		CXX_VERSION_3=`echo ${CXX_VERSION} | ${CUT} -d. -f3`
	fi
	if test "${CXX_VERSION_LENGTH}" -ge 4 ; then
		CXX_VERSION_4=`echo ${CXX_VERSION} | ${CUT} -d. -f4`
	fi

	if test "${IS_EGXX}" = "true" ; then
		echo "$as_me:2471: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:2473: result: egcs won't compile BALL due to several internal compiler errors." >&5
echo "${ECHO_T}egcs won't compile BALL due to several internal compiler errors." >&6
		echo "$as_me:2475: result: Please upgrade to a more recent version of GNU g++ (e.g. 2.95.2)." >&5
echo "${ECHO_T}Please upgrade to a more recent version of GNU g++ (e.g. 2.95.2)." >&6
		echo "$as_me:2477: result: If you have already installed a different compiler" >&5
echo "${ECHO_T}If you have already installed a different compiler" >&6
		echo "$as_me:2479: result: specify the path to this compiler by passing the option" >&5
echo "${ECHO_T}specify the path to this compiler by passing the option" >&6
		echo "$as_me:2481: result: --with-compiler=<compiler> to configure." >&5
echo "${ECHO_T}--with-compiler=<compiler> to configure." >&6
		echo "$as_me:2483: result: " >&5
echo "${ECHO_T}" >&6
		{ { echo "$as_me:2485: error: aborted" >&5
echo "$as_me: error: aborted" >&2;}
   { (exit 1); exit 1; }; }
	fi
	if test "${CXX_VERSION_1}" -lt 2 \
		-o "${CXX_VERSION_1}" = 2 -a "${CXX_VERSION_2}" -lt 95 ; then
		echo "$as_me:2491: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:2493: result: The version of gcc you are using is not supported by BALL." >&5
echo "${ECHO_T}The version of gcc you are using is not supported by BALL." >&6
		echo "$as_me:2495: result: Please update to a newer version of g++ (at least 2.95.x)" >&5
echo "${ECHO_T}Please update to a newer version of g++ (at least 2.95.x)" >&6
		echo "$as_me:2497: result: which can be obtained from" >&5
echo "${ECHO_T}which can be obtained from" >&6
		echo "$as_me:2499: result:   ftp://gcc.gnu.org/pub/gcc/releases/index.html" >&5
echo "${ECHO_T}  ftp://gcc.gnu.org/pub/gcc/releases/index.html" >&6
		echo "$as_me:2501: result: or specify a different compiler using the option --with-compiler=" >&5
echo "${ECHO_T}or specify a different compiler using the option --with-compiler=" >&6
		echo "$as_me:2503: result: " >&5
echo "${ECHO_T}" >&6
		{ { echo "$as_me:2505: error: aborted" >&5
echo "$as_me: error: aborted" >&2;}
   { (exit 1); exit 1; }; }
	fi

	if test "${CXX_VERSION_1}" -ge 3 ; then
		LIBS="${LIBS} -liberty"
	fi
fi

if test "${IS_KCC}" = true ; then
	echo "$as_me:2516: checking compiler version" >&5
echo $ECHO_N "checking compiler version... $ECHO_C" >&6
	echo "int main(){}" > conftest.C
	CXX_VERSION=`${CXX} -v --version conftest.C 2>&1| ${GREP} "KAI C++ " | ${CUT} -d" " -f3`
	CXX_NAME="KCC"
	VERSION_OUTPUT="KAI C++ ${CXX_VERSION}"
	CXX_COMPILER_NAME="KCC"

	echo "$as_me:2524: result: ${VERSION_OUTPUT}" >&5
echo "${ECHO_T}${VERSION_OUTPUT}" >&6

	CXX_VERSION_1=`echo ${CXX_VERSION} | ${CUT} -d. -f1`
	CXX_VERSION_LENGTH=`echo ${CXX_VERSION} | sed "s/^.//g" | wc -c`
	if test "${CXX_VERSION_LENGTH}" -ge 2 ; then
		CXX_VERSION_2=`echo ${CXX_VERSION} | ${CUT} -d. -f2`
	fi
	if test "${CXX_VERSION_LENGTH}" -ge 3 ; then
		CXX_VERSION_3=`echo ${CXX_VERSION} | ${CUT} -d. -f3`
	fi
	if test "${CXX_VERSION_LENGTH}" -ge 4 ; then
		CXX_VERSION_4=`echo ${CXX_VERSION} | ${CUT} -d. -f4`
	fi
fi

if test "${IS_DIGITALCXX}" = true ; then
	echo "$as_me:2541: checking compiler version" >&5
echo $ECHO_N "checking compiler version... $ECHO_C" >&6
	echo "int main(){}" > conftest.C
	CXX_VERSION=`${CXX} -V  2>/dev/null| ${GREP} "C++" | ${CUT} -d" " -f3 | tr -d V`
	CXX_NAME=`${CXX} -V | ${GREP} "C++" | ${CUT} -d" " -f1`
	VERSION_OUTPUT="${CXX_NAME} C++ ${CXX_VERSION}"
	CXX_COMPILER_NAME="Digital"

	echo "$as_me:2549: result: ${VERSION_OUTPUT}" >&5
echo "${ECHO_T}${VERSION_OUTPUT}" >&6

	CXX_VERSION_1=`echo ${CXX_VERSION} | ${CUT} -d. -f1 | tr -d "A-Za-z"`
	CXX_VERSION_LENGTH=`echo ${CXX_VERSION} | sed "s/^.-//g" | wc -c`
	if test "${CXX_VERSION_LENGTH}" -ge 2 ; then
		CXX_VERSION_2=`echo ${CXX_VERSION} | ${CUT} -d. -f2 | ${CUT} -d- -f1`
	fi
	if test "${CXX_VERSION_LENGTH}" -ge 3 ; then
		CXX_VERSION_3=`echo ${CXX_VERSION} | ${CUT} -d. -f2 | ${CUT} -d- -f2`
	fi

	if test "${CXX_VERSION_1}" -lt 6 -o "${CXX_VERSION_1}" -eq 6 -a "${CXX_VERSION_2}" -lt 2 ; then
		echo "$as_me:2562: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:2564: result: Your version of Digital/Compaq C++ does not provide all" >&5
echo "${ECHO_T}Your version of Digital/Compaq C++ does not provide all" >&6
		echo "$as_me:2566: result: ANSI C++ features required by BALL." >&5
echo "${ECHO_T}ANSI C++ features required by BALL." >&6
		echo "$as_me:2568: result: Please upgrade to release 6.2 or above." >&5
echo "${ECHO_T}Please upgrade to release 6.2 or above." >&6
		echo "$as_me:2570: result: " >&5
echo "${ECHO_T}" >&6
		{ { echo "$as_me:2572: error: aborted." >&5
echo "$as_me: error: aborted." >&2;}
   { (exit 1); exit 1; }; }
	fi
fi

if test "${IS_GXX}" != true -a "${IS_KCC}" != true -a "${IS_DIGITALCXX}" != true ; then
					if test "${OS}" = "IRIX" ; then
		echo "$as_me:2580: checking compiler version" >&5
echo $ECHO_N "checking compiler version... $ECHO_C" >&6
		CXX_VERSION_STRING=`${CXX} -n32 -version 2>&1 | egrep ersion`
		if test "${CXX_VERSION_STRING}" = "" ; then

	TMP__DIR="${CXX}/"
	while test "${TMP__DIR}" != "" ; do
		TMP__NAME=`echo ${TMP__DIR}|${CUT} -d/ -f1`
		TMP__DIR=`echo ${TMP__DIR}|${CT} -d/ -f2-`
		if test "${TMP__DIR}" = "${TMP__NAME}" ; then
			TMP__DIR=""
		fi
	done

			CXX_VERSION="${TMP__NAME}"
			CXX_VERSION_OUTPUT="${CXX_VERSION} (unknown version)"
		else
			CXX_VERSION=`echo ${CXX_VERSION_STRING} | ${SED} "s/^.*ersion //g"`
			CXX_COMPILER_NAME=`echo ${CXX_VERSION_STRING} | ${CUT} -d\  -f1`
			CXX_VERSION_OUTPUT="${CXX_VERSION} (${CXX_COMPILER_NAME})"
		fi

		CXX_VERSION_1=`echo ${CXX_VERSION} | ${CUT} -d. -f1`
		CXX_VERSION_LENGTH=`echo ${CXX_VERSION} | sed "s/^.//g" | wc -c`
		if test "${CXX_VERSION_LENGTH}" -ge 2 ; then
			CXX_VERSION_2=`echo ${CXX_VERSION} | ${CUT} -d. -f2`
		fi
		if test "${CXX_VERSION_LENGTH}" -ge 3 ; then
			CXX_VERSION_3=`echo ${CXX_VERSION} | ${CUT} -d. -f3`
		fi
		if test "${CXX_VERSION_LENGTH}" -ge 4 ; then
			CXX_VERSION_4=`echo ${CXX_VERSION} | ${CUT} -d. -f4`
		fi

		echo "$as_me:2614: result: ${CXX_VERSION_OUTPUT}" >&5
echo "${ECHO_T}${CXX_VERSION_OUTPUT}" >&6
	fi

	if test "${OS}" = "Solaris" ; then
		echo "$as_me:2619: checking compiler version" >&5
echo $ECHO_N "checking compiler version... $ECHO_C" >&6

		CXX_VERSION_STRING=`${CXX} -V 2>&1 | egrep CC:`

		CXX_VERSION=`echo ${CXX_VERSION_STRING} | ${TR} -d "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ:" | ${TR} -s " " | ${CUT} -d\  -f2`

		CXX_VERSION_TEST=`echo ${CXX_VERSION} | ${SED} s/\^\\[0-9\\.\\]*[a-zA-Z\\.]*//g`

		if test "${CXX_VERSION_TEST}" != "" ; then

	TMP__DIR="${CXX}/"
	while test "${TMP__DIR}" != "" ; do
		TMP__NAME=`echo ${TMP__DIR}|${CUT} -d/ -f1`
		TMP__DIR=`echo ${TMP__DIR}|${CT} -d/ -f2-`
		if test "${TMP__DIR}" = "${TMP__NAME}" ; then
			TMP__DIR=""
		fi
	done

			CXX_VERSION="${TMP__NAME}"
			CXX_VERSION_OUTPUT="${CXX_VERSION} (unknown version)"
		else
			CXX_VERSION_1=`echo ${CXX_VERSION} | ${CUT} -d. -f1`
			CXX_VERSION_LENGTH=`echo ${CXX_VERSION} | sed "s/^.//g" | wc -c`
			if test "${CXX_VERSION_LENGTH}" -ge 2 ; then
				CXX_VERSION_2=`echo ${CXX_VERSION} | ${CUT} -d. -f2`
			fi
			if test "${CXX_VERSION_LENGTH}" -ge 3 ; then
				CXX_VERSION_3=`echo ${CXX_VERSION} | ${CUT} -d. -f3`
			fi
			if test "${CXX_VERSION_LENGTH}" -ge 4 ; then
				CXX_VERSION_4=`echo ${CXX_VERSION} | ${CUT} -d. -f4`
			fi
			CXX_VERSION_OUTPUT="${CXX_VERSION}"
		fi
		echo "$as_me:2655: result: ${CXX_VERSION_OUTPUT}" >&5
echo "${ECHO_T}${CXX_VERSION_OUTPUT}" >&6

		if test "${CXX_VERSION_1}" -lt 6 ; then
			echo "$as_me:2659: result: " >&5
echo "${ECHO_T}" >&6
			echo "$as_me:2661: result: Wrong version of CC! CC before release 6 is not ANSI C++ compliant" >&5
echo "${ECHO_T}Wrong version of CC! CC before release 6 is not ANSI C++ compliant" >&6
			echo "$as_me:2663: result: Please upgrade to a more recent version of CC." >&5
echo "${ECHO_T}Please upgrade to a more recent version of CC." >&6
			{ { echo "$as_me:2665: error: aborted" >&5
echo "$as_me: error: aborted" >&2;}
   { (exit 1); exit 1; }; }
		fi
	fi
fi

echo "$as_me:2672: checking standardized compiler name" >&5
echo $ECHO_N "checking standardized compiler name... $ECHO_C" >&6
if test "${CXX_VERSION_1}" != "" ; then
	CXX_NAME="${CXX_NAME}_${CXX_VERSION_1}"
	if test "${CXX_VERSION_2}" != "" ; then
		CXX_NAME="${CXX_NAME}.${CXX_VERSION_2}"
		if test "${CXX_VERSION_3}" != "" ; then
			CXX_NAME="${CXX_NAME}.${CXX_VERSION_3}"
			if test "${CXX_VERSION_4}" != "" ; then
				CXX_NAME="${CXX_NAME}.${CXX_VERSION_4}"
			fi
		fi
	fi
fi

echo "$as_me:2687: result: ${CXX_NAME}" >&5
echo "${ECHO_T}${CXX_NAME}" >&6

CXX_MAKEDEPEND="makedepend"

MAKEDEP_CXX_OPTS="-f.Dependencies -D__cplusplus --"

MAKEDEP_CXX_SUFFIX=

TEMPLATE_DIR=

if test "${HAS_GPLUSPLUS}" = "true" ; then
	CXX_MAKEDEPEND="${CXX}"
	MAKEDEP_CXX_OPTS="-M"
	CXXFLAGS_D="${CXXFLAGS_D} -Wall -W -pedantic -Wno-long-long"
	CXXFLAGS_DI="${CXXFLAGS_DI} -g"
	CXXFLAGS_O="${CXXFLAGS_O} -O2"
	MAKEDEP_CXX_SUFFIX=" >.Dependencies"

		  	if test "${OS}" != "CYGWIN" ; then
		CXXFLAGS="${CXXFLAGS} -fPIC"
	fi

	DYNAR="${CXX}"
	if test "${OS}" != "Solaris" ; then
		DYNAROPTS="${DYNAROPTS} -shared -fPIC -o"
	else
		DYNAROPTS="${DYNAROPTS} -G -fPIC -o"
	fi

	if test "${IS_EGXX}" = true; then
		BALL_TYPENAME=typename
	else
		if test "${CXX_VERSION_1}" -gt 2 -o "${CXX_VERSION_1}" -eq 2 -a "${CXX_VERSION_2}" -ge 8 ; then
			BALL_TYPENAME=typename
		fi
	fi
	COMPILER_DONE=true
fi

if test "${COMPILER_DONE}" != true -a "${IS_KCC}" = true ; then
				TEMPLATE_DIR="ti_files"
	AR="${CXX}"
	DYNAR="${CXX}"
	AROPTS="${AROPTS} -o"
	DYNAROPTS="${DYNAROPTS} -o"
	CXX_MAKEDEPEND="${CXX}"
	MAKEDEP_CXX_OPTS="-M"
	MAKEDEP_CXX_SUFFIX=" >.Dependencies"

							CXXFLAGS="${CXXFLAGS} --one_per"
	if test "${OS}" = Linux ; then
		CXXFLAGS="${CXXFLAGS} -fPIC"
	fi
	if test "${OS}" = Solaris ; then
		CXXFLAGS="${CXXFLAGS} -KPIC"
	fi
	if test "${OS}" = IRIX ; then
		CXXFLAGS="${CXXFLAGS} -KPIC"
	fi

			CXXFLAGS_O="${CXXFLAGS_O} +K3"

			CXXFLAGS_D="${CXXFLAGS_D} +K0"
	CXXFLAGS_DI="${CXXFLAGS_DI}"

		  		if test "${OS}" = "Solaris" -a "${ARCHITECTURE}" = sparc ; then
		if test "${BINFMT_64_BIT}" = true ; then
			CXX_NAME="${CXX_NAME}_V9"
			LDFLAGS="${LDFLAGS} -xarch=v9"
			CXXFLAGS="${CXXFLAGS} -xarch=v9"
			AROPTS="${AROPTS} -xarch=v9"
			DYNAROPTS="-xarch=v9 ${DYNAROPTS}"
		else
			CXX_NAME="${CXX_NAME}_V8"
		fi
	fi

	COMPILER_DONE=true
fi

if test "${COMPILER_DONE}" != true -a "${IS_DIGITALCXX}" = true ; then
	TEMPLATE_DIR="cxx_repository"
	AR="ar"
	DYNAR="${CXX}"
	AROPTS="${AROPTS} -o"
	DYNAROPTS="${DYNAROPTS} -shared -nocxxstd -ptr \$(BALL_PATH)/source/cxx_rep -o"
	CXX_MAKEDEPEND="${CXX}"
	MAKEDEP_CXX_OPTS="-M -noimplicit_include"
	MAKEDEP_CXX_SUFFIX=" >.Dependencies"

	CXXFLAGS="${CXXFLAGS} -ieee -nopure_cname"
	LIB_CXXFLAGS="${LIB_CXXFLAGS} -ptr \$(BALL_PATH)/source/cxx_rep"
	CXXFLAGS_O="${CXXFLAGS_O} -O1"

	CXXFLAGS_D="${CXXFLAGS_D}"
	CXXFLAGS_DI="${CXXFLAGS_DI} -g"

				if test "${OS}" != "Linux" ; then
		CXXFLAGS="${CXXFLAGS} -std strict_ansi"
		MAKEDEP_CXX_OPTS="${MAKEDEP_CXX_OPTS} -std strict_ansi"
	fi

	COMPILER_DONE=true
fi

if test "${COMPILER_DONE}" != true ; then
	if test "${OS}" = IRIX ; then
						TEMPLATE_DIR="ii_files"

						if test "${BINFMT_64_BIT}" = true ; then
			IRIX_BINFMT=64
			CXX_NAME="${CXX_NAME}_64"
		else
			IRIX_BINFMT=N32
			CXX_NAME="${CXX_NAME}_N32"
		fi

		BALL_TYPENAME=typename

								if test "${CXX_VERSION_1}" -lt 7\
						-o "${CXX_VERSION_1}" -eq 7 -a "${CXX_VERSION_2}" = 10\
						-o "${CXX_VERSION_1}" -eq 7 -a "${CXX_VERSION_2}" = 20\
						-o "${CXX_VERSION_1}" -eq 7 -a "${CXX_VERSION_2}" -lt 2; then
			echo "$as_me:2811: result: " >&5
echo "${ECHO_T}" >&6
			echo "$as_me:2813: result: MipsPro CC version 7.30 or above is required. Please update your compiler." >&5
echo "${ECHO_T}MipsPro CC version 7.30 or above is required. Please update your compiler." >&6
			{ { echo "$as_me:2815: error: Aborted" >&5
echo "$as_me: error: Aborted" >&2;}
   { (exit 1); exit 1; }; }
		fi

		AR=${CXX}
		AROPTS="${AROPTS} -ar -o"
		DYNAR=${CXX}
		DYNAROPTS="${DYNAROPTS} -LANG:std -shared -quickstart_info -no_unresolved -o"

								if test "${CXX_VERSION_1}" = 7 -a "${CXX_VERSION_2}" = 3 \
						-a "${CXX_VERSION_3}" = 1m -a "${CXX_VERSION_4}" = ""\
						-o "${CXX_VERSION_1}" = 7 -a "{CXX_VERSION_2}" = 30 ; then
			COMMENTS="${COMMENTS}\nPlease take care - this version of SGI CC (7.3.1m) contains serious bugs\n"
			COMMENTS="${COMMENTS}in its implementaion of fstream/iostream. This may lead to strange behaviour\n"
			COMMENTS="${COMMENTS}and causes PDBFile_test to fail. Please update your compiler.\n\n"
			echo "$as_me:2831: result: ${COMMENTS}" >&5
echo "${ECHO_T}${COMMENTS}" >&6
			ADDITIONAL_COMMENTS="${ADDITIONAL_COMMENTS}${COMMENTS}"
			COMMENTS=""
		fi

		CXX_MAKEDEPEND="${CXX}"
		MAKEDEP_CXX_OPTS="-M 2>/dev/null"
		MAKEDEP_CXX_SUFFIX=" >.Dependencies"

		if test "${IRIX_BINFMT}" = 64 ; then
			DEF_BOOL=false
			CXXFLAGS="$CXXFLAGS -64 -LANG:std"
			DYNAROPTS="-64 ${DYNAROPTS}"
			CXXFLAGS_O="${CXXFLAGS_O} -O3 -OPT:Olimit=60000 -multigot -G 5 -DEBUG:woff=3333"
			CXXFLAGS_D="${CXXFLAGS_D} -fullwarn -multigot -G 5 -DEBUG:woff=1375,3201,1424,3333,1110,1209"
			CXXFLAGS_DI="${CXXFLAGS_DI} -g"
			LDFLAGS="$LDFLAGS -64 -LANG:std"
			cat >>confdefs.h <<\EOF
#define IRIX64
EOF

		fi
		if test "${IRIX_BINFMT}" = N32 ; then
			DEF_BOOL=false
			CXXFLAGS="$CXXFLAGS -n32 -LANG:std"
			DYNAROPTS="-n32 ${DYNAROPTS}"
			CXXFLAGS_O="${CXXFLAGS_O} -O3 -OPT:Olimit=60000 -multigot -G 5 -DEBUG:woff=3333"
			CXXFLAGS_D="${CXXFLAGS_D} -fullwarn -multigot -G 5 -DEBUG:woff=1375,3201,1424,3333,1110,1209"
			CXXFLAGS_DI="${CXXFLAGS_DI} -g"
			LDFLAGS="$LDFLAGS -n32 -LANG:std"
			cat >>confdefs.h <<\EOF
#define IRIX32
EOF

		fi

								if test "${DEBUG}" = yes ; then
			DYNAROPTS="-IPA ${DYNAROPTS}"
		fi

		cat >>confdefs.h <<\EOF
#define MIPS
EOF

		cat >>confdefs.h <<\EOF
#define IRIX
EOF

	fi

				if test $OS = SunOS ; then
		CXXFLAGS_D="$CXXFLAGS_D}"
		CXXFLAGS_DI="$CXXFLAGS_DI} -g"
		CXXFLAGS_O="${CXXFLAGS_O} -O2"
		cat >>confdefs.h <<\EOF
#define SPARC
EOF

		cat >>confdefs.h <<\EOF
#define SUNOS
EOF

		{ { echo "$as_me:2894: error: SunOS not supported - please use Solaris" >&5
echo "$as_me: error: SunOS not supported - please use Solaris" >&2;}
   { (exit 1); exit 1; }; }
	fi

				if test $OS = Solaris ; then
										if test "${CXX_VERSION_1}" -lt 6 ; then
			echo "$as_me:2901: result: " >&5
echo "${ECHO_T}" >&6
			echo "$as_me:2903: result: BALL requires an ANSI C++ compliant compiler" >&5
echo "${ECHO_T}BALL requires an ANSI C++ compliant compiler" >&6
			echo "$as_me:2905: result: SUNPro compilers are ANSI compliant for version 6.0 and above" >&5
echo "${ECHO_T}SUNPro compilers are ANSI compliant for version 6.0 and above" >&6
			echo "$as_me:2907: result: Please upgrade your compiler!" >&5
echo "${ECHO_T}Please upgrade your compiler!" >&6
			echo "$as_me:2909: result: " >&5
echo "${ECHO_T}" >&6
			{ { echo "$as_me:2911: error: Abort" >&5
echo "$as_me: error: Abort" >&2;}
   { (exit 1); exit 1; }; }
		fi

		cat >>confdefs.h <<\EOF
#define SOLARIS
EOF

		TEMPLATE_DIR="SunWS_cache"

						if test "${BINFMT_64_BIT}" = true ; then
			SUN_BINFMT=V9
			CXX_NAME="${CXX_NAME}_V9"
			LDFLAGS="${LDFLAGS} -xarch=v9"
			CXXFLAGS="${CXXFLAGS} -xarch=v9"
			AROPTS="${AROPTS} -xarch=v9"
			DYNAROPTS="-xarch=v9 ${DYNAROPTS}"
		else
			SUN_BINFMT=V8
			CXX_NAME="${CXX_NAME}_V8"
		fi

		DEF_BOOL=true
		AR="${CXX}"
		DYNAR="${CXX}"
		AROPTS="${AROPTS} -xar -KPIC -o"
		DYNAROPTS="${DYNAROPTS} -pto -G -KPIC -o"
		NONLIB_CXXFLAGS="-pto"
		LIB_CXXFLAGS=""
		CXX_MAKEDEPEND="${CXX}"
		MAKEDEP_CXX_OPTS="-xM1"
		MAKEDEP_CXX_SUFFIX=" >.Dependencies"

		cat >>confdefs.h <<\EOF
#define BALL_NO_INLINE_FUNCTIONS
EOF

		CXXFLAGS="${CXXFLAGS} -KPIC"
		CXXFLAGS_O="${CXXFLAGS_O} -xO5"
		CXXFLAGS_D="${CXXFLAGS_D}"
		CXXFLAGS_DI="${CXXFLAGS_DI} -g"
	fi
fi

echo "$as_me:2956: checking for DEBUG flag" >&5
echo $ECHO_N "checking for DEBUG flag... $ECHO_C" >&6
if test "$DEBUG" != "" ; then
				if test "$DEBUG" = true ; then
						if test "${DEBUG_INFO}" = true -a "$CXXFLAGS_DI" != "" ; then
			CXXFLAGS_D="${CXXFLAGS_D} ${CXXFLAGS_DI}"
		fi
		cat >>confdefs.h <<\EOF
#define BALL_DEBUG
EOF

		cat >>confdefs.h <<\EOF
#define BALL_NO_INLINE_FUNCTIONS
EOF

		echo "$as_me:2971: result: enabled" >&5
echo "${ECHO_T}enabled" >&6
		CPP_MODE_FLAGS="${CXXFLAGS_D}"
		CPP_MODE_FLAGS_NO_OPTIMIZATION="${CXXFLAGS_D}"
	else
		echo "$as_me:2976: result: disabled" >&5
echo "${ECHO_T}disabled" >&6
		CPP_MODE_FLAGS="${CXXFLAGS_O}"
		CPP_MODE_FLAGS_NO_OPTIMIZATION=""
	fi
else
	echo "$as_me:2982: result: disabled" >&5
echo "${ECHO_T}disabled" >&6
	CPP_MODE_FLAGS="${CXXFLAGS_O}"
	CPP_MODE_FLAGS_NO_OPTIMIZATION=""
fi

if test "${MULTI_BUILD}" = "true" ; then
	echo "$as_me:2989: checking multi-platform build" >&5
echo $ECHO_N "checking multi-platform build... $ECHO_C" >&6
	echo "$as_me:2991: result: enabled" >&5
echo "${ECHO_T}enabled" >&6
fi

if test "$OS" = IRIX -o "$OS" = Solaris ; then
	INCLUDE_IEEEFP=true
	cat >>confdefs.h <<\EOF
#define BALL_INCLUDE_IEEEFP
EOF

fi

ac_ext=cc
ac_cpp='$CXXCPP $CPPFLAGS'
ac_compile='$CXX -c $CXXFLAGS $CPPFLAGS conftest.$ac_ext >&5'
ac_link='$CXX -o conftest$ac_exeext $CXXFLAGS $CPPFLAGS $LDFLAGS conftest.$ac_ext $LIBS >&5'
ac_compiler_gnu=$ac_cv_cxx_compiler_gnu
if test -n "$ac_tool_prefix"; then
  for ac_prog in $CCC g++ c++ gpp aCC CC cxx cc++ cl KCC RCC xlC_r xlC
  do
    # Extract the first word of "$ac_tool_prefix$ac_prog", so it can be a program name with args.
set dummy $ac_tool_prefix$ac_prog; ac_word=$2
echo "$as_me:3013: checking for $ac_word" >&5
echo $ECHO_N "checking for $ac_word... $ECHO_C" >&6
if test "${ac_cv_prog_CXX+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  if test -n "$CXX"; then
  ac_cv_prog_CXX="$CXX" # Let the user override the test.
else
  ac_save_IFS=$IFS; IFS=$ac_path_separator
ac_dummy="$PATH"
for ac_dir in $ac_dummy; do
  IFS=$ac_save_IFS
  test -z "$ac_dir" && ac_dir=.
  $as_executable_p "$ac_dir/$ac_word" || continue
ac_cv_prog_CXX="$ac_tool_prefix$ac_prog"
echo "$as_me:3028: found $ac_dir/$ac_word" >&5
break
done

fi
fi
CXX=$ac_cv_prog_CXX
if test -n "$CXX"; then
  echo "$as_me:3036: result: $CXX" >&5
echo "${ECHO_T}$CXX" >&6
else
  echo "$as_me:3039: result: no" >&5
echo "${ECHO_T}no" >&6
fi

    test -n "$CXX" && break
  done
fi
if test -z "$CXX"; then
  ac_ct_CXX=$CXX
  for ac_prog in $CCC g++ c++ gpp aCC CC cxx cc++ cl KCC RCC xlC_r xlC
do
  # Extract the first word of "$ac_prog", so it can be a program name with args.
set dummy $ac_prog; ac_word=$2
echo "$as_me:3052: checking for $ac_word" >&5
echo $ECHO_N "checking for $ac_word... $ECHO_C" >&6
if test "${ac_cv_prog_ac_ct_CXX+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  if test -n "$ac_ct_CXX"; then
  ac_cv_prog_ac_ct_CXX="$ac_ct_CXX" # Let the user override the test.
else
  ac_save_IFS=$IFS; IFS=$ac_path_separator
ac_dummy="$PATH"
for ac_dir in $ac_dummy; do
  IFS=$ac_save_IFS
  test -z "$ac_dir" && ac_dir=.
  $as_executable_p "$ac_dir/$ac_word" || continue
ac_cv_prog_ac_ct_CXX="$ac_prog"
echo "$as_me:3067: found $ac_dir/$ac_word" >&5
break
done

fi
fi
ac_ct_CXX=$ac_cv_prog_ac_ct_CXX
if test -n "$ac_ct_CXX"; then
  echo "$as_me:3075: result: $ac_ct_CXX" >&5
echo "${ECHO_T}$ac_ct_CXX" >&6
else
  echo "$as_me:3078: result: no" >&5
echo "${ECHO_T}no" >&6
fi

  test -n "$ac_ct_CXX" && break
done
test -n "$ac_ct_CXX" || ac_ct_CXX="g++"

  CXX=$ac_ct_CXX
fi

cat >conftest.$ac_ext <<_ACEOF
#line 3090 "configure"
#include "confdefs.h"

int
main ()
{

  ;
  return 0;
}
_ACEOF
ac_clean_files_save=$ac_clean_files
ac_clean_files="$ac_clean_files a.out a.exe"
# Try to create an executable without -o first, disregard a.out.
# It will help us diagnose broken compiler, and finding out an intuition
# of exeext.
echo "$as_me:3106: checking for C++ compiler default output" >&5
echo $ECHO_N "checking for C++ compiler default output... $ECHO_C" >&6
ac_link_default=`echo "$ac_link" | sed 's/ -o *conftest[^ ]*//'`
if { (eval echo "$as_me:3109: \"$ac_link_default\"") >&5
  (eval $ac_link_default) 2>&5
  ac_status=$?
  echo "$as_me:3112: \$? = $ac_status" >&5
  (exit $ac_status); }; then
  for ac_file in `ls a.exe conftest.exe a.* conftest conftest.* 2>/dev/null`; do
  case $ac_file in
    *.$ac_ext | *.o | *.obj | *.xcoff | *.tds | *.d | *.pdb ) ;;
    a.out ) # We found the default executable, but exeext='' is most
            # certainly right.
            break;;
    *.* ) ac_cv_exeext=`expr "$ac_file" : '[^.]*\(\..*\)'`
          export ac_cv_exeext
          break;;
    * ) break;;
  esac
done
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
{ { echo "$as_me:3129: error: C++ compiler cannot create executables" >&5
echo "$as_me: error: C++ compiler cannot create executables" >&2;}
   { (exit 77); exit 77; }; }
fi

ac_exeext=$ac_cv_exeext
echo "$as_me:3135: result: $ac_file" >&5
echo "${ECHO_T}$ac_file" >&6

# Check the compiler produces executables we can run.  If not, either
# the compiler is broken, or we cross compile.
echo "$as_me:3140: checking whether the C++ compiler works" >&5
echo $ECHO_N "checking whether the C++ compiler works... $ECHO_C" >&6
# FIXME: These cross compiler hacks should be removed for Autoconf 3.0
# If not cross compiling, check that we can run a simple program.
if test "$cross_compiling" != yes; then
  if { ac_try='./$ac_file'
  { (eval echo "$as_me:3146: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:3149: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
    cross_compiling=no
  else
    if test "$cross_compiling" = maybe; then
	cross_compiling=yes
    else
	{ { echo "$as_me:3156: error: cannot run C++ compiled programs.
If you meant to cross compile, use \`--host'." >&5
echo "$as_me: error: cannot run C++ compiled programs.
If you meant to cross compile, use \`--host'." >&2;}
   { (exit 1); exit 1; }; }
    fi
  fi
fi
echo "$as_me:3164: result: yes" >&5
echo "${ECHO_T}yes" >&6

rm -f a.out a.exe conftest$ac_cv_exeext
ac_clean_files=$ac_clean_files_save
# Check the compiler produces executables we can run.  If not, either
# the compiler is broken, or we cross compile.
echo "$as_me:3171: checking whether we are cross compiling" >&5
echo $ECHO_N "checking whether we are cross compiling... $ECHO_C" >&6
echo "$as_me:3173: result: $cross_compiling" >&5
echo "${ECHO_T}$cross_compiling" >&6

echo "$as_me:3176: checking for executable suffix" >&5
echo $ECHO_N "checking for executable suffix... $ECHO_C" >&6
if { (eval echo "$as_me:3178: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:3181: \$? = $ac_status" >&5
  (exit $ac_status); }; then
  # If both `conftest.exe' and `conftest' are `present' (well, observable)
# catch `conftest.exe'.  For instance with Cygwin, `ls conftest' will
# work properly (i.e., refer to `conftest.exe'), while it won't with
# `rm'.
for ac_file in `(ls conftest.exe; ls conftest; ls conftest.*) 2>/dev/null`; do
  case $ac_file in
    *.$ac_ext | *.o | *.obj | *.xcoff | *.tds | *.d | *.pdb ) ;;
    *.* ) ac_cv_exeext=`expr "$ac_file" : '[^.]*\(\..*\)'`
          export ac_cv_exeext
          break;;
    * ) break;;
  esac
done
else
  { { echo "$as_me:3197: error: cannot compute EXEEXT: cannot compile and link" >&5
echo "$as_me: error: cannot compute EXEEXT: cannot compile and link" >&2;}
   { (exit 1); exit 1; }; }
fi

rm -f conftest$ac_cv_exeext
echo "$as_me:3203: result: $ac_cv_exeext" >&5
echo "${ECHO_T}$ac_cv_exeext" >&6

rm -f conftest.$ac_ext
EXEEXT=$ac_cv_exeext
ac_exeext=$EXEEXT
echo "$as_me:3209: checking for object suffix" >&5
echo $ECHO_N "checking for object suffix... $ECHO_C" >&6
if test "${ac_cv_objext+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 3215 "configure"
#include "confdefs.h"

int
main ()
{

  ;
  return 0;
}
_ACEOF
rm -f conftest.o conftest.obj
if { (eval echo "$as_me:3227: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:3230: \$? = $ac_status" >&5
  (exit $ac_status); }; then
  for ac_file in `(ls conftest.o conftest.obj; ls conftest.*) 2>/dev/null`; do
  case $ac_file in
    *.$ac_ext | *.xcoff | *.tds | *.d | *.pdb ) ;;
    *) ac_cv_objext=`expr "$ac_file" : '.*\.\(.*\)'`
       break;;
  esac
done
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
{ { echo "$as_me:3242: error: cannot compute OBJEXT: cannot compile" >&5
echo "$as_me: error: cannot compute OBJEXT: cannot compile" >&2;}
   { (exit 1); exit 1; }; }
fi

rm -f conftest.$ac_cv_objext conftest.$ac_ext
fi
echo "$as_me:3249: result: $ac_cv_objext" >&5
echo "${ECHO_T}$ac_cv_objext" >&6
OBJEXT=$ac_cv_objext
ac_objext=$OBJEXT
echo "$as_me:3253: checking whether we are using the GNU C++ compiler" >&5
echo $ECHO_N "checking whether we are using the GNU C++ compiler... $ECHO_C" >&6
if test "${ac_cv_cxx_compiler_gnu+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 3259 "configure"
#include "confdefs.h"

int
main ()
{
#ifndef __GNUC__
       choke me
#endif

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:3274: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:3277: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:3280: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:3283: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_compiler_gnu=yes
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_compiler_gnu=no
fi
rm -f conftest.$ac_objext conftest.$ac_ext
ac_cv_cxx_compiler_gnu=$ac_compiler_gnu

fi
echo "$as_me:3295: result: $ac_cv_cxx_compiler_gnu" >&5
echo "${ECHO_T}$ac_cv_cxx_compiler_gnu" >&6
GXX=`test $ac_compiler_gnu = yes && echo yes`
ac_test_CXXFLAGS=${CXXFLAGS+set}
ac_save_CXXFLAGS=$CXXFLAGS
CXXFLAGS="-g"
echo "$as_me:3301: checking whether $CXX accepts -g" >&5
echo $ECHO_N "checking whether $CXX accepts -g... $ECHO_C" >&6
if test "${ac_cv_prog_cxx_g+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 3307 "configure"
#include "confdefs.h"

int
main ()
{

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:3319: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:3322: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:3325: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:3328: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_cv_prog_cxx_g=yes
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_cv_prog_cxx_g=no
fi
rm -f conftest.$ac_objext conftest.$ac_ext
fi
echo "$as_me:3338: result: $ac_cv_prog_cxx_g" >&5
echo "${ECHO_T}$ac_cv_prog_cxx_g" >&6
if test "$ac_test_CXXFLAGS" = set; then
  CXXFLAGS=$ac_save_CXXFLAGS
elif test $ac_cv_prog_cxx_g = yes; then
  if test "$GXX" = yes; then
    CXXFLAGS="-g -O2"
  else
    CXXFLAGS="-g"
  fi
else
  if test "$GXX" = yes; then
    CXXFLAGS="-O2"
  else
    CXXFLAGS=
  fi
fi
for ac_declaration in \
   ''\
   '#include <stdlib.h>' \
   'extern "C" void std::exit (int) throw (); using std::exit;' \
   'extern "C" void std::exit (int); using std::exit;' \
   'extern "C" void exit (int) throw ();' \
   'extern "C" void exit (int);' \
   'void exit (int);'
do
  cat >conftest.$ac_ext <<_ACEOF
#line 3365 "configure"
#include "confdefs.h"
#include <stdlib.h>
$ac_declaration
int
main ()
{
exit (42);
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:3378: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:3381: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:3384: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:3387: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  :
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
continue
fi
rm -f conftest.$ac_objext conftest.$ac_ext
  cat >conftest.$ac_ext <<_ACEOF
#line 3397 "configure"
#include "confdefs.h"
$ac_declaration
int
main ()
{
exit (42);
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:3409: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:3412: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:3415: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:3418: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  break
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest.$ac_ext
done
echo '#ifdef __cplusplus' >>confdefs.h
echo $ac_declaration      >>confdefs.h
echo '#endif'             >>confdefs.h

ac_ext=cc
ac_cpp='$CXXCPP $CPPFLAGS'
ac_compile='$CXX -c $CXXFLAGS $CPPFLAGS conftest.$ac_ext >&5'
ac_link='$CXX -o conftest$ac_exeext $CXXFLAGS $CPPFLAGS $LDFLAGS conftest.$ac_ext $LIBS >&5'
ac_compiler_gnu=$ac_cv_cxx_compiler_gnu

ac_ext=cc
ac_cpp='$CXXCPP $CPPFLAGS'
ac_compile='$CXX -c $CXXFLAGS $CPPFLAGS conftest.$ac_ext >&5'
ac_link='$CXX -o conftest$ac_exeext $CXXFLAGS $CPPFLAGS $LDFLAGS conftest.$ac_ext $LIBS >&5'
ac_compiler_gnu=$ac_cv_cxx_compiler_gnu
echo "$as_me:3442: checking how to run the C++ preprocessor" >&5
echo $ECHO_N "checking how to run the C++ preprocessor... $ECHO_C" >&6
if test -z "$CXXCPP"; then
  if test "${ac_cv_prog_CXXCPP+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
      # Double quotes because CXXCPP needs to be expanded
    for CXXCPP in "$CXX -E" "/lib/cpp"
    do
      # break 2 since there is a loop in there.
      ac_preproc_ok=false
for ac_cxx_preproc_warn_flag in '' yes
do
  # Use a header file that comes with gcc, so configuring glibc
  # with a fresh cross-compiler works.
  # On the NeXT, cc -E runs the code through the compiler's parser,
  # not just through cpp. "Syntax error" is here to catch this case.
  cat >conftest.$ac_ext <<_ACEOF
#line 3460 "configure"
#include "confdefs.h"
#include <assert.h>
                     Syntax error
_ACEOF
if { (eval echo "$as_me:3465: \"$ac_cpp conftest.$ac_ext\"") >&5
  (eval $ac_cpp conftest.$ac_ext) 2>conftest.er1
  ac_status=$?
  egrep -v '^ *\+' conftest.er1 >conftest.err
  rm -f conftest.er1
  cat conftest.err >&5
  echo "$as_me:3471: \$? = $ac_status" >&5
  (exit $ac_status); } >/dev/null; then
  if test -s conftest.err; then
    ac_cpp_err=$ac_cxx_preproc_warn_flag
  else
    ac_cpp_err=
  fi
else
  ac_cpp_err=yes
fi
if test -z "$ac_cpp_err"; then
  :
else
  echo "$as_me: failed program was:" >&5
  cat conftest.$ac_ext >&5
  # Broken: fails on valid input.
continue
fi
rm -f conftest.err conftest.$ac_ext

  # OK, works on sane cases.  Now check whether non-existent headers
  # can be detected and how.
  cat >conftest.$ac_ext <<_ACEOF
#line 3494 "configure"
#include "confdefs.h"
#include <ac_nonexistent.h>
_ACEOF
if { (eval echo "$as_me:3498: \"$ac_cpp conftest.$ac_ext\"") >&5
  (eval $ac_cpp conftest.$ac_ext) 2>conftest.er1
  ac_status=$?
  egrep -v '^ *\+' conftest.er1 >conftest.err
  rm -f conftest.er1
  cat conftest.err >&5
  echo "$as_me:3504: \$? = $ac_status" >&5
  (exit $ac_status); } >/dev/null; then
  if test -s conftest.err; then
    ac_cpp_err=$ac_cxx_preproc_warn_flag
  else
    ac_cpp_err=
  fi
else
  ac_cpp_err=yes
fi
if test -z "$ac_cpp_err"; then
  # Broken: success on invalid input.
continue
else
  echo "$as_me: failed program was:" >&5
  cat conftest.$ac_ext >&5
  # Passes both tests.
ac_preproc_ok=:
break
fi
rm -f conftest.err conftest.$ac_ext

done
# Because of `break', _AC_PREPROC_IFELSE's cleaning code was skipped.
rm -f conftest.err conftest.$ac_ext
if $ac_preproc_ok; then
  break 2
fi

    done
    ac_cv_prog_CXXCPP=$CXXCPP

fi
  CXXCPP=$ac_cv_prog_CXXCPP
else
  ac_cv_prog_CXXCPP=$CXXCPP
fi
echo "$as_me:3541: result: $CXXCPP" >&5
echo "${ECHO_T}$CXXCPP" >&6
ac_preproc_ok=false
for ac_cxx_preproc_warn_flag in '' yes
do
  # Use a header file that comes with gcc, so configuring glibc
  # with a fresh cross-compiler works.
  # On the NeXT, cc -E runs the code through the compiler's parser,
  # not just through cpp. "Syntax error" is here to catch this case.
  cat >conftest.$ac_ext <<_ACEOF
#line 3551 "configure"
#include "confdefs.h"
#include <assert.h>
                     Syntax error
_ACEOF
if { (eval echo "$as_me:3556: \"$ac_cpp conftest.$ac_ext\"") >&5
  (eval $ac_cpp conftest.$ac_ext) 2>conftest.er1
  ac_status=$?
  egrep -v '^ *\+' conftest.er1 >conftest.err
  rm -f conftest.er1
  cat conftest.err >&5
  echo "$as_me:3562: \$? = $ac_status" >&5
  (exit $ac_status); } >/dev/null; then
  if test -s conftest.err; then
    ac_cpp_err=$ac_cxx_preproc_warn_flag
  else
    ac_cpp_err=
  fi
else
  ac_cpp_err=yes
fi
if test -z "$ac_cpp_err"; then
  :
else
  echo "$as_me: failed program was:" >&5
  cat conftest.$ac_ext >&5
  # Broken: fails on valid input.
continue
fi
rm -f conftest.err conftest.$ac_ext

  # OK, works on sane cases.  Now check whether non-existent headers
  # can be detected and how.
  cat >conftest.$ac_ext <<_ACEOF
#line 3585 "configure"
#include "confdefs.h"
#include <ac_nonexistent.h>
_ACEOF
if { (eval echo "$as_me:3589: \"$ac_cpp conftest.$ac_ext\"") >&5
  (eval $ac_cpp conftest.$ac_ext) 2>conftest.er1
  ac_status=$?
  egrep -v '^ *\+' conftest.er1 >conftest.err
  rm -f conftest.er1
  cat conftest.err >&5
  echo "$as_me:3595: \$? = $ac_status" >&5
  (exit $ac_status); } >/dev/null; then
  if test -s conftest.err; then
    ac_cpp_err=$ac_cxx_preproc_warn_flag
  else
    ac_cpp_err=
  fi
else
  ac_cpp_err=yes
fi
if test -z "$ac_cpp_err"; then
  # Broken: success on invalid input.
continue
else
  echo "$as_me: failed program was:" >&5
  cat conftest.$ac_ext >&5
  # Passes both tests.
ac_preproc_ok=:
break
fi
rm -f conftest.err conftest.$ac_ext

done
# Because of `break', _AC_PREPROC_IFELSE's cleaning code was skipped.
rm -f conftest.err conftest.$ac_ext
if $ac_preproc_ok; then
  :
else
  { { echo "$as_me:3623: error: C++ preprocessor \"$CXXCPP\" fails sanity check" >&5
echo "$as_me: error: C++ preprocessor \"$CXXCPP\" fails sanity check" >&2;}
   { (exit 1); exit 1; }; }
fi

ac_ext=cc
ac_cpp='$CXXCPP $CPPFLAGS'
ac_compile='$CXX -c $CXXFLAGS $CPPFLAGS conftest.$ac_ext >&5'
ac_link='$CXX -o conftest$ac_exeext $CXXFLAGS $CPPFLAGS $LDFLAGS conftest.$ac_ext $LIBS >&5'
ac_compiler_gnu=$ac_cv_cxx_compiler_gnu

echo "$as_me:3634: checking for ANSI C header files" >&5
echo $ECHO_N "checking for ANSI C header files... $ECHO_C" >&6
if test "${ac_cv_header_stdc+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 3640 "configure"
#include "confdefs.h"
#include <stdlib.h>
#include <stdarg.h>
#include <string.h>
#include <float.h>

_ACEOF
if { (eval echo "$as_me:3648: \"$ac_cpp conftest.$ac_ext\"") >&5
  (eval $ac_cpp conftest.$ac_ext) 2>conftest.er1
  ac_status=$?
  egrep -v '^ *\+' conftest.er1 >conftest.err
  rm -f conftest.er1
  cat conftest.err >&5
  echo "$as_me:3654: \$? = $ac_status" >&5
  (exit $ac_status); } >/dev/null; then
  if test -s conftest.err; then
    ac_cpp_err=$ac_cxx_preproc_warn_flag
  else
    ac_cpp_err=
  fi
else
  ac_cpp_err=yes
fi
if test -z "$ac_cpp_err"; then
  ac_cv_header_stdc=yes
else
  echo "$as_me: failed program was:" >&5
  cat conftest.$ac_ext >&5
  ac_cv_header_stdc=no
fi
rm -f conftest.err conftest.$ac_ext

if test $ac_cv_header_stdc = yes; then
  # SunOS 4.x string.h does not declare mem*, contrary to ANSI.
  cat >conftest.$ac_ext <<_ACEOF
#line 3676 "configure"
#include "confdefs.h"
#include <string.h>

_ACEOF
if (eval "$ac_cpp conftest.$ac_ext") 2>&5 |
  egrep "memchr" >/dev/null 2>&1; then
  :
else
  ac_cv_header_stdc=no
fi
rm -f conftest*

fi

if test $ac_cv_header_stdc = yes; then
  # ISC 2.0.2 stdlib.h does not declare free, contrary to ANSI.
  cat >conftest.$ac_ext <<_ACEOF
#line 3694 "configure"
#include "confdefs.h"
#include <stdlib.h>

_ACEOF
if (eval "$ac_cpp conftest.$ac_ext") 2>&5 |
  egrep "free" >/dev/null 2>&1; then
  :
else
  ac_cv_header_stdc=no
fi
rm -f conftest*

fi

if test $ac_cv_header_stdc = yes; then
  # /bin/cc in Irix-4.0.5 gets non-ANSI ctype macros unless using -ansi.
  if test "$cross_compiling" = yes; then
  :
else
  cat >conftest.$ac_ext <<_ACEOF
#line 3715 "configure"
#include "confdefs.h"
#include <ctype.h>
#if ((' ' & 0x0FF) == 0x020)
# define ISLOWER(c) ('a' <= (c) && (c) <= 'z')
# define TOUPPER(c) (ISLOWER(c) ? 'A' + ((c) - 'a') : (c))
#else
# define ISLOWER(c) (('a' <= (c) && (c) <= 'i') \
                     || ('j' <= (c) && (c) <= 'r') \
                     || ('s' <= (c) && (c) <= 'z'))
# define TOUPPER(c) (ISLOWER(c) ? ((c) | 0x40) : (c))
#endif

#define XOR(e, f) (((e) && !(f)) || (!(e) && (f)))
int
main ()
{
  int i;
  for (i = 0; i < 256; i++)
    if (XOR (islower (i), ISLOWER (i))
        || toupper (i) != TOUPPER (i))
      exit(2);
  exit (0);
}
_ACEOF
rm -f conftest$ac_exeext
if { (eval echo "$as_me:3741: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:3744: \$? = $ac_status" >&5
  (exit $ac_status); } && { ac_try='./conftest$ac_exeext'
  { (eval echo "$as_me:3746: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:3749: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  :
else
  echo "$as_me: program exited with status $ac_status" >&5
echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_cv_header_stdc=no
fi
rm -f core core.* *.core conftest$ac_exeext conftest.$ac_ext
fi
fi
fi
echo "$as_me:3762: result: $ac_cv_header_stdc" >&5
echo "${ECHO_T}$ac_cv_header_stdc" >&6
if test $ac_cv_header_stdc = yes; then

cat >>confdefs.h <<\EOF
#define STDC_HEADERS 1
EOF

fi

echo "$as_me:3772: checking for numeric_limits class" >&5
echo $ECHO_N "checking for numeric_limits class... $ECHO_C" >&6
cat >conftest.$ac_ext <<_ACEOF
#line 3775 "configure"
#include "confdefs.h"

		#include <limits>

int
main ()
{

		float f = std::numeric_limits<float>::min();

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:3791: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:3794: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:3797: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:3800: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  BALL_HAVE_NUMERIC_LIMITS=true

else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest.$ac_ext
if test "${BALL_HAVE_NUMERIC_LIMITS}" = true ; then
	echo "$as_me:3810: result: available" >&5
echo "${ECHO_T}available" >&6
	cat >>confdefs.h <<\EOF
#define BALL_HAVE_NUMERIC_LIMITS 1
EOF

else
	echo "$as_me:3817: result: not available" >&5
echo "${ECHO_T}not available" >&6
	BALL_HAVE_NUMERIC_LIMITS=false

						echo "$as_me:3821: checking whether float.h has to be included" >&5
echo $ECHO_N "checking whether float.h has to be included... $ECHO_C" >&6
	cat >conftest.$ac_ext <<_ACEOF
#line 3824 "configure"
#include "confdefs.h"

			#include <limits.h>

int
main ()
{

			float a = FLT_MAX;
			float b = FLT_MIN;
			float c = DBL_MAX;
			float d = DBL_MIN;

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:3843: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:3846: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:3849: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:3852: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  BALL_INCLUDE_FLOAT_H=false

else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest.$ac_ext
	if test "${BALL_INCLUDE_FLOAT_H}" != false ; then
		cat >conftest.$ac_ext <<_ACEOF
#line 3863 "configure"
#include "confdefs.h"

				#include <float.h>

int
main ()
{

				float a = FLT_MAX;
				float b = FLT_MIN;
				float c = DBL_MAX;
				float d = DBL_MIN;

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:3882: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:3885: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:3888: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:3891: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  BALL_INCLUDE_FLOAT_H=true

else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest.$ac_ext
	fi
	if test "${BALL_INCLUDE_FLOAT_H+set}" != set ; then
		echo "$as_me:3902: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:3904: result: limits.h seems to be corrupt or float.h is missing!" >&5
echo "${ECHO_T}limits.h seems to be corrupt or float.h is missing!" >&6
		echo "$as_me:3906: result: " >&5
echo "${ECHO_T}" >&6
	else
		if test "${BALL_INCLUDE_FLOAT_H}" = true ; then
			echo "$as_me:3910: result: yes" >&5
echo "${ECHO_T}yes" >&6
			cat >>confdefs.h <<\EOF
#define BALL_INCLUDE_FLOAT_H 1
EOF

		else
			echo "$as_me:3917: result: no" >&5
echo "${ECHO_T}no" >&6
		fi
	fi
fi
rm /tmp/$$.conftest.c a.out 2>/dev/null

echo "$as_me:3924: checking for null template arguments" >&5
echo $ECHO_N "checking for null template arguments... $ECHO_C" >&6
BALL_NULL_TEMPLATE_ARGS="NULL"
cat >conftest.$ac_ext <<_ACEOF
#line 3928 "configure"
#include "confdefs.h"

		template <typename T>
		class A
		{
			public:
			friend bool operator == <> (const A&, const A&);
		};

int
main ()
{

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:3947: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:3950: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:3953: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:3956: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  BALL_NULL_TEMPLATE_ARGS="<>"
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest.$ac_ext
if test "${BALL_NULL_TEMPLATE_ARGS}" = "NULL" ; then
	cat >conftest.$ac_ext <<_ACEOF
#line 3966 "configure"
#include "confdefs.h"

			template <typename T>
			class A
			{
				public:
				friend bool operator == (const A&, const A&);
			};

int
main ()
{

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:3985: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:3988: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:3991: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:3994: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  BALL_NULL_TEMPLATE_ARGS=""
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest.$ac_ext
fi
echo "$as_me:4003: result: \"$BALL_NULL_TEMPLATE_ARGS\"" >&5
echo "${ECHO_T}\"$BALL_NULL_TEMPLATE_ARGS\"" >&6
if test "${BALL_NULL_TEMPLATE_ARGS}" = "NULL" ; then
	echo "$as_me:4006: result: could not find a suitable argument for null templates" >&5
echo "${ECHO_T}could not find a suitable argument for null templates" >&6
	{ { echo "$as_me:4008: error: aborted" >&5
echo "$as_me: error: aborted" >&2;}
   { (exit 1); exit 1; }; }
fi

echo "$as_me:4013: checking for ANSI compliant iostream" >&5
echo $ECHO_N "checking for ANSI compliant iostream... $ECHO_C" >&6
BALL_HAS_ANSI_IOSTREAM=no
cat >conftest.$ac_ext <<_ACEOF
#line 4017 "configure"
#include "confdefs.h"

		#include <iostream>
		class A:public std::iostream
		{
			A():std::basic_ios<char>(0),std::iostream(0)
			{}
		};

int
main ()
{

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:4036: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:4039: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:4042: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:4045: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  BALL_HAS_ANSI_IOSTREAM=yes

else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest.$ac_ext
echo "$as_me:4054: result: $BALL_HAS_ANSI_IOSTREAM" >&5
echo "${ECHO_T}$BALL_HAS_ANSI_IOSTREAM" >&6

echo "$as_me:4057: checking for ANSI or ARM style access modification" >&5
echo $ECHO_N "checking for ANSI or ARM style access modification... $ECHO_C" >&6
BALL_CFG_USING_METHOD_DIRECTIVE=none
cat >conftest.$ac_ext <<_ACEOF
#line 4061 "configure"
#include "confdefs.h"

		class A
		{
			protected: void foo(){};
		};

		class B : public A
		{
			public: using A::foo;
		};

int
main ()
{

		B b;
		b.foo();

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:4086: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:4089: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:4092: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:4095: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  BALL_CFG_USING_METHOD_DIRECTIVE=ANSI

else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest.$ac_ext
if test ${BALL_CFG_USING_METHOD_DIRECTIVE} = none ; then
	cat >conftest.$ac_ext <<_ACEOF
#line 4106 "configure"
#include "confdefs.h"

			class A
			{
				protected: void foo(){};
			};

			class B : public A
			{
				public: A::foo;
			};

int
main ()
{

			B b;
			b.foo();

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:4131: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:4134: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:4137: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:4140: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  BALL_CFG_USING_METHOD_DIRECTIVE=ARM

else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest.$ac_ext
fi
echo "$as_me:4150: result: ${BALL_CFG_USING_METHOD_DIRECTIVE}" >&5
echo "${ECHO_T}${BALL_CFG_USING_METHOD_DIRECTIVE}" >&6
if test ${BALL_CFG_USING_METHOD_DIRECTIVE} = ANSI ; then
	cat >>confdefs.h <<\EOF
#define BALL_CFG_USING_METHOD_DIRECTIVE 1
EOF

fi
if test ${BALL_CFG_USING_METHOD_DIRECTIVE} = none ; then
	echo "$as_me:4159: result: " >&5
echo "${ECHO_T}" >&6
	echo "$as_me:4161: result: Compiler does not understand ARM or ANSI style method access modification." >&5
echo "${ECHO_T}Compiler does not understand ARM or ANSI style method access modification." >&6
	echo "$as_me:4163: result: Please specify a different compiler (e.g. g++ 2.95.2) using the option" >&5
echo "${ECHO_T}Please specify a different compiler (e.g. g++ 2.95.2) using the option" >&6
	echo "$as_me:4165: result: --with-compiler=<compiler>." >&5
echo "${ECHO_T}--with-compiler=<compiler>." >&6
	echo "$as_me:4167: result: " >&5
echo "${ECHO_T}" >&6
	{ { echo "$as_me:4169: error: aborted." >&5
echo "$as_me: error: aborted." >&2;}
   { (exit 1); exit 1; }; }
fi

for ac_header in stdlib.h string.h memory.h strings.h inttypes.h unistd.h
do
ac_ac_Header=`echo "ac_cv_header_$ac_header" | $ac_tr_sh`
echo "$as_me:4177: checking for $ac_header" >&5
echo $ECHO_N "checking for $ac_header... $ECHO_C" >&6
if eval "test \"\${$ac_ac_Header+set}\" = set"; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 4183 "configure"
#include "confdefs.h"
#include <$ac_header>
_ACEOF
if { (eval echo "$as_me:4187: \"$ac_cpp conftest.$ac_ext\"") >&5
  (eval $ac_cpp conftest.$ac_ext) 2>conftest.er1
  ac_status=$?
  egrep -v '^ *\+' conftest.er1 >conftest.err
  rm -f conftest.er1
  cat conftest.err >&5
  echo "$as_me:4193: \$? = $ac_status" >&5
  (exit $ac_status); } >/dev/null; then
  if test -s conftest.err; then
    ac_cpp_err=$ac_cxx_preproc_warn_flag
  else
    ac_cpp_err=
  fi
else
  ac_cpp_err=yes
fi
if test -z "$ac_cpp_err"; then
  eval "$ac_ac_Header=yes"
else
  echo "$as_me: failed program was:" >&5
  cat conftest.$ac_ext >&5
  eval "$ac_ac_Header=no"
fi
rm -f conftest.err conftest.$ac_ext
fi
echo "$as_me:4212: result: `eval echo '${'$ac_ac_Header'}'`" >&5
echo "${ECHO_T}`eval echo '${'$ac_ac_Header'}'`" >&6
if test `eval echo '${'$ac_ac_Header'}'` = yes; then
  cat >>confdefs.h <<EOF
#define `echo "HAVE_$ac_header" | $ac_tr_cpp` 1
EOF

fi
done

echo "$as_me:4222: checking for size_t" >&5
echo $ECHO_N "checking for size_t... $ECHO_C" >&6
if test "${ac_cv_type_size_t+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 4228 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
if ((size_t *) 0)
  return 0;
if (sizeof (size_t))
  return 0;
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:4243: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:4246: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:4249: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:4252: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_cv_type_size_t=yes
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_cv_type_size_t=no
fi
rm -f conftest.$ac_objext conftest.$ac_ext
fi
echo "$as_me:4262: result: $ac_cv_type_size_t" >&5
echo "${ECHO_T}$ac_cv_type_size_t" >&6
if test $ac_cv_type_size_t = yes; then
  :
else

cat >>confdefs.h <<EOF
#define size_t unsigned
EOF

fi

echo "$as_me:4274: checking whether time.h and sys/time.h may both be included" >&5
echo $ECHO_N "checking whether time.h and sys/time.h may both be included... $ECHO_C" >&6
if test "${ac_cv_header_time+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 4280 "configure"
#include "confdefs.h"
#include <sys/types.h>
#include <sys/time.h>
#include <time.h>

int
main ()
{
struct tm *tp;
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:4295: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:4298: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:4301: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:4304: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_cv_header_time=yes
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_cv_header_time=no
fi
rm -f conftest.$ac_objext conftest.$ac_ext
fi
echo "$as_me:4314: result: $ac_cv_header_time" >&5
echo "${ECHO_T}$ac_cv_header_time" >&6
if test $ac_cv_header_time = yes; then

cat >>confdefs.h <<\EOF
#define TIME_WITH_SYS_TIME 1
EOF

fi

echo "$as_me:4324: checking for char" >&5
echo $ECHO_N "checking for char... $ECHO_C" >&6
if test "${ac_cv_type_char+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 4330 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
if ((char *) 0)
  return 0;
if (sizeof (char))
  return 0;
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:4345: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:4348: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:4351: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:4354: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_cv_type_char=yes
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_cv_type_char=no
fi
rm -f conftest.$ac_objext conftest.$ac_ext
fi
echo "$as_me:4364: result: $ac_cv_type_char" >&5
echo "${ECHO_T}$ac_cv_type_char" >&6

echo "$as_me:4367: checking size of char" >&5
echo $ECHO_N "checking size of char... $ECHO_C" >&6
if test "${ac_cv_sizeof_char+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  if test "$ac_cv_type_char" = yes; then
  if test "$cross_compiling" = yes; then
  # Depending upon the size, compute the lo and hi bounds.
cat >conftest.$ac_ext <<_ACEOF
#line 4376 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
int _array_ [1 - 2 * !((sizeof (char)) >= 0)]
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:4388: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:4391: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:4394: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:4397: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_lo=0 ac_mid=0
  while :; do
    cat >conftest.$ac_ext <<_ACEOF
#line 4402 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
int _array_ [1 - 2 * !((sizeof (char)) <= $ac_mid)]
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:4414: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:4417: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:4420: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:4423: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_hi=$ac_mid; break
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_lo=`expr $ac_mid + 1`; ac_mid=`expr 2 '*' $ac_mid + 1`
fi
rm -f conftest.$ac_objext conftest.$ac_ext
  done
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_hi=-1 ac_mid=-1
  while :; do
    cat >conftest.$ac_ext <<_ACEOF
#line 4439 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
int _array_ [1 - 2 * !((sizeof (char)) >= $ac_mid)]
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:4451: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:4454: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:4457: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:4460: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_lo=$ac_mid; break
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_hi=`expr $ac_mid - 1`; ac_mid=`expr 2 '*' $ac_mid`
fi
rm -f conftest.$ac_objext conftest.$ac_ext
  done
fi
rm -f conftest.$ac_objext conftest.$ac_ext
# Binary search between lo and hi bounds.
while test "x$ac_lo" != "x$ac_hi"; do
  ac_mid=`expr '(' $ac_hi - $ac_lo ')' / 2 + $ac_lo`
  cat >conftest.$ac_ext <<_ACEOF
#line 4476 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
int _array_ [1 - 2 * !((sizeof (char)) <= $ac_mid)]
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:4488: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:4491: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:4494: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:4497: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_hi=$ac_mid
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_lo=`expr $ac_mid + 1`
fi
rm -f conftest.$ac_objext conftest.$ac_ext
done
ac_cv_sizeof_char=$ac_lo
else
  if test "$cross_compiling" = yes; then
  { { echo "$as_me:4510: error: cannot run test program while cross compiling" >&5
echo "$as_me: error: cannot run test program while cross compiling" >&2;}
   { (exit 1); exit 1; }; }
else
  cat >conftest.$ac_ext <<_ACEOF
#line 4515 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
FILE *f = fopen ("conftest.val", "w");
if (!f)
  exit (1);
fprintf (f, "%d", (sizeof (char)));
fclose (f);
  ;
  return 0;
}
_ACEOF
rm -f conftest$ac_exeext
if { (eval echo "$as_me:4531: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:4534: \$? = $ac_status" >&5
  (exit $ac_status); } && { ac_try='./conftest$ac_exeext'
  { (eval echo "$as_me:4536: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:4539: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_cv_sizeof_char=`cat conftest.val`
else
  echo "$as_me: program exited with status $ac_status" >&5
echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f core core.* *.core conftest$ac_exeext conftest.$ac_ext
fi
fi
rm -f conftest.val
else
  ac_cv_sizeof_char=0
fi
fi
echo "$as_me:4555: result: $ac_cv_sizeof_char" >&5
echo "${ECHO_T}$ac_cv_sizeof_char" >&6
cat >>confdefs.h <<EOF
#define SIZEOF_CHAR $ac_cv_sizeof_char
EOF

echo "$as_me:4561: checking for int" >&5
echo $ECHO_N "checking for int... $ECHO_C" >&6
if test "${ac_cv_type_int+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 4567 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
if ((int *) 0)
  return 0;
if (sizeof (int))
  return 0;
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:4582: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:4585: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:4588: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:4591: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_cv_type_int=yes
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_cv_type_int=no
fi
rm -f conftest.$ac_objext conftest.$ac_ext
fi
echo "$as_me:4601: result: $ac_cv_type_int" >&5
echo "${ECHO_T}$ac_cv_type_int" >&6

echo "$as_me:4604: checking size of int" >&5
echo $ECHO_N "checking size of int... $ECHO_C" >&6
if test "${ac_cv_sizeof_int+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  if test "$ac_cv_type_int" = yes; then
  if test "$cross_compiling" = yes; then
  # Depending upon the size, compute the lo and hi bounds.
cat >conftest.$ac_ext <<_ACEOF
#line 4613 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
int _array_ [1 - 2 * !((sizeof (int)) >= 0)]
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:4625: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:4628: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:4631: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:4634: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_lo=0 ac_mid=0
  while :; do
    cat >conftest.$ac_ext <<_ACEOF
#line 4639 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
int _array_ [1 - 2 * !((sizeof (int)) <= $ac_mid)]
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:4651: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:4654: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:4657: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:4660: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_hi=$ac_mid; break
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_lo=`expr $ac_mid + 1`; ac_mid=`expr 2 '*' $ac_mid + 1`
fi
rm -f conftest.$ac_objext conftest.$ac_ext
  done
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_hi=-1 ac_mid=-1
  while :; do
    cat >conftest.$ac_ext <<_ACEOF
#line 4676 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
int _array_ [1 - 2 * !((sizeof (int)) >= $ac_mid)]
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:4688: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:4691: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:4694: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:4697: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_lo=$ac_mid; break
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_hi=`expr $ac_mid - 1`; ac_mid=`expr 2 '*' $ac_mid`
fi
rm -f conftest.$ac_objext conftest.$ac_ext
  done
fi
rm -f conftest.$ac_objext conftest.$ac_ext
# Binary search between lo and hi bounds.
while test "x$ac_lo" != "x$ac_hi"; do
  ac_mid=`expr '(' $ac_hi - $ac_lo ')' / 2 + $ac_lo`
  cat >conftest.$ac_ext <<_ACEOF
#line 4713 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
int _array_ [1 - 2 * !((sizeof (int)) <= $ac_mid)]
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:4725: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:4728: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:4731: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:4734: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_hi=$ac_mid
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_lo=`expr $ac_mid + 1`
fi
rm -f conftest.$ac_objext conftest.$ac_ext
done
ac_cv_sizeof_int=$ac_lo
else
  if test "$cross_compiling" = yes; then
  { { echo "$as_me:4747: error: cannot run test program while cross compiling" >&5
echo "$as_me: error: cannot run test program while cross compiling" >&2;}
   { (exit 1); exit 1; }; }
else
  cat >conftest.$ac_ext <<_ACEOF
#line 4752 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
FILE *f = fopen ("conftest.val", "w");
if (!f)
  exit (1);
fprintf (f, "%d", (sizeof (int)));
fclose (f);
  ;
  return 0;
}
_ACEOF
rm -f conftest$ac_exeext
if { (eval echo "$as_me:4768: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:4771: \$? = $ac_status" >&5
  (exit $ac_status); } && { ac_try='./conftest$ac_exeext'
  { (eval echo "$as_me:4773: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:4776: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_cv_sizeof_int=`cat conftest.val`
else
  echo "$as_me: program exited with status $ac_status" >&5
echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f core core.* *.core conftest$ac_exeext conftest.$ac_ext
fi
fi
rm -f conftest.val
else
  ac_cv_sizeof_int=0
fi
fi
echo "$as_me:4792: result: $ac_cv_sizeof_int" >&5
echo "${ECHO_T}$ac_cv_sizeof_int" >&6
cat >>confdefs.h <<EOF
#define SIZEOF_INT $ac_cv_sizeof_int
EOF

echo "$as_me:4798: checking for long" >&5
echo $ECHO_N "checking for long... $ECHO_C" >&6
if test "${ac_cv_type_long+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 4804 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
if ((long *) 0)
  return 0;
if (sizeof (long))
  return 0;
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:4819: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:4822: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:4825: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:4828: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_cv_type_long=yes
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_cv_type_long=no
fi
rm -f conftest.$ac_objext conftest.$ac_ext
fi
echo "$as_me:4838: result: $ac_cv_type_long" >&5
echo "${ECHO_T}$ac_cv_type_long" >&6

echo "$as_me:4841: checking size of long" >&5
echo $ECHO_N "checking size of long... $ECHO_C" >&6
if test "${ac_cv_sizeof_long+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  if test "$ac_cv_type_long" = yes; then
  if test "$cross_compiling" = yes; then
  # Depending upon the size, compute the lo and hi bounds.
cat >conftest.$ac_ext <<_ACEOF
#line 4850 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
int _array_ [1 - 2 * !((sizeof (long)) >= 0)]
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:4862: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:4865: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:4868: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:4871: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_lo=0 ac_mid=0
  while :; do
    cat >conftest.$ac_ext <<_ACEOF
#line 4876 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
int _array_ [1 - 2 * !((sizeof (long)) <= $ac_mid)]
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:4888: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:4891: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:4894: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:4897: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_hi=$ac_mid; break
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_lo=`expr $ac_mid + 1`; ac_mid=`expr 2 '*' $ac_mid + 1`
fi
rm -f conftest.$ac_objext conftest.$ac_ext
  done
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_hi=-1 ac_mid=-1
  while :; do
    cat >conftest.$ac_ext <<_ACEOF
#line 4913 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
int _array_ [1 - 2 * !((sizeof (long)) >= $ac_mid)]
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:4925: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:4928: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:4931: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:4934: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_lo=$ac_mid; break
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_hi=`expr $ac_mid - 1`; ac_mid=`expr 2 '*' $ac_mid`
fi
rm -f conftest.$ac_objext conftest.$ac_ext
  done
fi
rm -f conftest.$ac_objext conftest.$ac_ext
# Binary search between lo and hi bounds.
while test "x$ac_lo" != "x$ac_hi"; do
  ac_mid=`expr '(' $ac_hi - $ac_lo ')' / 2 + $ac_lo`
  cat >conftest.$ac_ext <<_ACEOF
#line 4950 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
int _array_ [1 - 2 * !((sizeof (long)) <= $ac_mid)]
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:4962: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:4965: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:4968: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:4971: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_hi=$ac_mid
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_lo=`expr $ac_mid + 1`
fi
rm -f conftest.$ac_objext conftest.$ac_ext
done
ac_cv_sizeof_long=$ac_lo
else
  if test "$cross_compiling" = yes; then
  { { echo "$as_me:4984: error: cannot run test program while cross compiling" >&5
echo "$as_me: error: cannot run test program while cross compiling" >&2;}
   { (exit 1); exit 1; }; }
else
  cat >conftest.$ac_ext <<_ACEOF
#line 4989 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
FILE *f = fopen ("conftest.val", "w");
if (!f)
  exit (1);
fprintf (f, "%d", (sizeof (long)));
fclose (f);
  ;
  return 0;
}
_ACEOF
rm -f conftest$ac_exeext
if { (eval echo "$as_me:5005: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:5008: \$? = $ac_status" >&5
  (exit $ac_status); } && { ac_try='./conftest$ac_exeext'
  { (eval echo "$as_me:5010: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:5013: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_cv_sizeof_long=`cat conftest.val`
else
  echo "$as_me: program exited with status $ac_status" >&5
echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f core core.* *.core conftest$ac_exeext conftest.$ac_ext
fi
fi
rm -f conftest.val
else
  ac_cv_sizeof_long=0
fi
fi
echo "$as_me:5029: result: $ac_cv_sizeof_long" >&5
echo "${ECHO_T}$ac_cv_sizeof_long" >&6
cat >>confdefs.h <<EOF
#define SIZEOF_LONG $ac_cv_sizeof_long
EOF

echo "$as_me:5035: checking for long long" >&5
echo $ECHO_N "checking for long long... $ECHO_C" >&6
if test "${ac_cv_type_long_long+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 5041 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
if ((long long *) 0)
  return 0;
if (sizeof (long long))
  return 0;
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:5056: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:5059: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:5062: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:5065: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_cv_type_long_long=yes
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_cv_type_long_long=no
fi
rm -f conftest.$ac_objext conftest.$ac_ext
fi
echo "$as_me:5075: result: $ac_cv_type_long_long" >&5
echo "${ECHO_T}$ac_cv_type_long_long" >&6

echo "$as_me:5078: checking size of long long" >&5
echo $ECHO_N "checking size of long long... $ECHO_C" >&6
if test "${ac_cv_sizeof_long_long+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  if test "$ac_cv_type_long_long" = yes; then
  if test "$cross_compiling" = yes; then
  # Depending upon the size, compute the lo and hi bounds.
cat >conftest.$ac_ext <<_ACEOF
#line 5087 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
int _array_ [1 - 2 * !((sizeof (long long)) >= 0)]
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:5099: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:5102: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:5105: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:5108: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_lo=0 ac_mid=0
  while :; do
    cat >conftest.$ac_ext <<_ACEOF
#line 5113 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
int _array_ [1 - 2 * !((sizeof (long long)) <= $ac_mid)]
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:5125: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:5128: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:5131: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:5134: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_hi=$ac_mid; break
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_lo=`expr $ac_mid + 1`; ac_mid=`expr 2 '*' $ac_mid + 1`
fi
rm -f conftest.$ac_objext conftest.$ac_ext
  done
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_hi=-1 ac_mid=-1
  while :; do
    cat >conftest.$ac_ext <<_ACEOF
#line 5150 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
int _array_ [1 - 2 * !((sizeof (long long)) >= $ac_mid)]
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:5162: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:5165: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:5168: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:5171: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_lo=$ac_mid; break
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_hi=`expr $ac_mid - 1`; ac_mid=`expr 2 '*' $ac_mid`
fi
rm -f conftest.$ac_objext conftest.$ac_ext
  done
fi
rm -f conftest.$ac_objext conftest.$ac_ext
# Binary search between lo and hi bounds.
while test "x$ac_lo" != "x$ac_hi"; do
  ac_mid=`expr '(' $ac_hi - $ac_lo ')' / 2 + $ac_lo`
  cat >conftest.$ac_ext <<_ACEOF
#line 5187 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
int _array_ [1 - 2 * !((sizeof (long long)) <= $ac_mid)]
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:5199: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:5202: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:5205: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:5208: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_hi=$ac_mid
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_lo=`expr $ac_mid + 1`
fi
rm -f conftest.$ac_objext conftest.$ac_ext
done
ac_cv_sizeof_long_long=$ac_lo
else
  if test "$cross_compiling" = yes; then
  { { echo "$as_me:5221: error: cannot run test program while cross compiling" >&5
echo "$as_me: error: cannot run test program while cross compiling" >&2;}
   { (exit 1); exit 1; }; }
else
  cat >conftest.$ac_ext <<_ACEOF
#line 5226 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
FILE *f = fopen ("conftest.val", "w");
if (!f)
  exit (1);
fprintf (f, "%d", (sizeof (long long)));
fclose (f);
  ;
  return 0;
}
_ACEOF
rm -f conftest$ac_exeext
if { (eval echo "$as_me:5242: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:5245: \$? = $ac_status" >&5
  (exit $ac_status); } && { ac_try='./conftest$ac_exeext'
  { (eval echo "$as_me:5247: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:5250: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_cv_sizeof_long_long=`cat conftest.val`
else
  echo "$as_me: program exited with status $ac_status" >&5
echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f core core.* *.core conftest$ac_exeext conftest.$ac_ext
fi
fi
rm -f conftest.val
else
  ac_cv_sizeof_long_long=0
fi
fi
echo "$as_me:5266: result: $ac_cv_sizeof_long_long" >&5
echo "${ECHO_T}$ac_cv_sizeof_long_long" >&6
cat >>confdefs.h <<EOF
#define SIZEOF_LONG_LONG $ac_cv_sizeof_long_long
EOF

echo "$as_me:5272: checking for size_t" >&5
echo $ECHO_N "checking for size_t... $ECHO_C" >&6
if test "${ac_cv_type_size_t+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 5278 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
if ((size_t *) 0)
  return 0;
if (sizeof (size_t))
  return 0;
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:5293: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:5296: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:5299: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:5302: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_cv_type_size_t=yes
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_cv_type_size_t=no
fi
rm -f conftest.$ac_objext conftest.$ac_ext
fi
echo "$as_me:5312: result: $ac_cv_type_size_t" >&5
echo "${ECHO_T}$ac_cv_type_size_t" >&6

echo "$as_me:5315: checking size of size_t" >&5
echo $ECHO_N "checking size of size_t... $ECHO_C" >&6
if test "${ac_cv_sizeof_size_t+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  if test "$ac_cv_type_size_t" = yes; then
  if test "$cross_compiling" = yes; then
  # Depending upon the size, compute the lo and hi bounds.
cat >conftest.$ac_ext <<_ACEOF
#line 5324 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
int _array_ [1 - 2 * !((sizeof (size_t)) >= 0)]
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:5336: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:5339: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:5342: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:5345: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_lo=0 ac_mid=0
  while :; do
    cat >conftest.$ac_ext <<_ACEOF
#line 5350 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
int _array_ [1 - 2 * !((sizeof (size_t)) <= $ac_mid)]
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:5362: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:5365: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:5368: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:5371: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_hi=$ac_mid; break
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_lo=`expr $ac_mid + 1`; ac_mid=`expr 2 '*' $ac_mid + 1`
fi
rm -f conftest.$ac_objext conftest.$ac_ext
  done
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_hi=-1 ac_mid=-1
  while :; do
    cat >conftest.$ac_ext <<_ACEOF
#line 5387 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
int _array_ [1 - 2 * !((sizeof (size_t)) >= $ac_mid)]
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:5399: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:5402: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:5405: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:5408: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_lo=$ac_mid; break
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_hi=`expr $ac_mid - 1`; ac_mid=`expr 2 '*' $ac_mid`
fi
rm -f conftest.$ac_objext conftest.$ac_ext
  done
fi
rm -f conftest.$ac_objext conftest.$ac_ext
# Binary search between lo and hi bounds.
while test "x$ac_lo" != "x$ac_hi"; do
  ac_mid=`expr '(' $ac_hi - $ac_lo ')' / 2 + $ac_lo`
  cat >conftest.$ac_ext <<_ACEOF
#line 5424 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
int _array_ [1 - 2 * !((sizeof (size_t)) <= $ac_mid)]
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:5436: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:5439: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:5442: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:5445: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_hi=$ac_mid
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_lo=`expr $ac_mid + 1`
fi
rm -f conftest.$ac_objext conftest.$ac_ext
done
ac_cv_sizeof_size_t=$ac_lo
else
  if test "$cross_compiling" = yes; then
  { { echo "$as_me:5458: error: cannot run test program while cross compiling" >&5
echo "$as_me: error: cannot run test program while cross compiling" >&2;}
   { (exit 1); exit 1; }; }
else
  cat >conftest.$ac_ext <<_ACEOF
#line 5463 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
FILE *f = fopen ("conftest.val", "w");
if (!f)
  exit (1);
fprintf (f, "%d", (sizeof (size_t)));
fclose (f);
  ;
  return 0;
}
_ACEOF
rm -f conftest$ac_exeext
if { (eval echo "$as_me:5479: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:5482: \$? = $ac_status" >&5
  (exit $ac_status); } && { ac_try='./conftest$ac_exeext'
  { (eval echo "$as_me:5484: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:5487: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_cv_sizeof_size_t=`cat conftest.val`
else
  echo "$as_me: program exited with status $ac_status" >&5
echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f core core.* *.core conftest$ac_exeext conftest.$ac_ext
fi
fi
rm -f conftest.val
else
  ac_cv_sizeof_size_t=0
fi
fi
echo "$as_me:5503: result: $ac_cv_sizeof_size_t" >&5
echo "${ECHO_T}$ac_cv_sizeof_size_t" >&6
cat >>confdefs.h <<EOF
#define SIZEOF_SIZE_T $ac_cv_sizeof_size_t
EOF

echo "$as_me:5509: checking for void*" >&5
echo $ECHO_N "checking for void*... $ECHO_C" >&6
if test "${ac_cv_type_voidp+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 5515 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
if ((void* *) 0)
  return 0;
if (sizeof (void*))
  return 0;
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:5530: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:5533: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:5536: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:5539: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_cv_type_voidp=yes
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_cv_type_voidp=no
fi
rm -f conftest.$ac_objext conftest.$ac_ext
fi
echo "$as_me:5549: result: $ac_cv_type_voidp" >&5
echo "${ECHO_T}$ac_cv_type_voidp" >&6

echo "$as_me:5552: checking size of void*" >&5
echo $ECHO_N "checking size of void*... $ECHO_C" >&6
if test "${ac_cv_sizeof_voidp+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  if test "$ac_cv_type_voidp" = yes; then
  if test "$cross_compiling" = yes; then
  # Depending upon the size, compute the lo and hi bounds.
cat >conftest.$ac_ext <<_ACEOF
#line 5561 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
int _array_ [1 - 2 * !((sizeof (void*)) >= 0)]
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:5573: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:5576: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:5579: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:5582: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_lo=0 ac_mid=0
  while :; do
    cat >conftest.$ac_ext <<_ACEOF
#line 5587 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
int _array_ [1 - 2 * !((sizeof (void*)) <= $ac_mid)]
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:5599: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:5602: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:5605: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:5608: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_hi=$ac_mid; break
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_lo=`expr $ac_mid + 1`; ac_mid=`expr 2 '*' $ac_mid + 1`
fi
rm -f conftest.$ac_objext conftest.$ac_ext
  done
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_hi=-1 ac_mid=-1
  while :; do
    cat >conftest.$ac_ext <<_ACEOF
#line 5624 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
int _array_ [1 - 2 * !((sizeof (void*)) >= $ac_mid)]
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:5636: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:5639: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:5642: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:5645: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_lo=$ac_mid; break
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_hi=`expr $ac_mid - 1`; ac_mid=`expr 2 '*' $ac_mid`
fi
rm -f conftest.$ac_objext conftest.$ac_ext
  done
fi
rm -f conftest.$ac_objext conftest.$ac_ext
# Binary search between lo and hi bounds.
while test "x$ac_lo" != "x$ac_hi"; do
  ac_mid=`expr '(' $ac_hi - $ac_lo ')' / 2 + $ac_lo`
  cat >conftest.$ac_ext <<_ACEOF
#line 5661 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
int _array_ [1 - 2 * !((sizeof (void*)) <= $ac_mid)]
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:5673: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:5676: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:5679: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:5682: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_hi=$ac_mid
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_lo=`expr $ac_mid + 1`
fi
rm -f conftest.$ac_objext conftest.$ac_ext
done
ac_cv_sizeof_voidp=$ac_lo
else
  if test "$cross_compiling" = yes; then
  { { echo "$as_me:5695: error: cannot run test program while cross compiling" >&5
echo "$as_me: error: cannot run test program while cross compiling" >&2;}
   { (exit 1); exit 1; }; }
else
  cat >conftest.$ac_ext <<_ACEOF
#line 5700 "configure"
#include "confdefs.h"
$ac_includes_default
int
main ()
{
FILE *f = fopen ("conftest.val", "w");
if (!f)
  exit (1);
fprintf (f, "%d", (sizeof (void*)));
fclose (f);
  ;
  return 0;
}
_ACEOF
rm -f conftest$ac_exeext
if { (eval echo "$as_me:5716: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:5719: \$? = $ac_status" >&5
  (exit $ac_status); } && { ac_try='./conftest$ac_exeext'
  { (eval echo "$as_me:5721: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:5724: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_cv_sizeof_voidp=`cat conftest.val`
else
  echo "$as_me: program exited with status $ac_status" >&5
echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f core core.* *.core conftest$ac_exeext conftest.$ac_ext
fi
fi
rm -f conftest.val
else
  ac_cv_sizeof_voidp=0
fi
fi
echo "$as_me:5740: result: $ac_cv_sizeof_voidp" >&5
echo "${ECHO_T}$ac_cv_sizeof_voidp" >&6
cat >>confdefs.h <<EOF
#define SIZEOF_VOIDP $ac_cv_sizeof_voidp
EOF

SIZEOF_INT=$ac_cv_sizeof_int
SIZEOF_CHAR=$ac_cv_sizeof_char
SIZEOF_LONG=$ac_cv_sizeof_long
SIZEOF_SIZE_T=$ac_cv_sizeof_size_t
SIZEOF_VOID_P=$ac_cv_sizeof_voidp
SIZEOF_UINT=$ac_cv_sizeof_int
SIZEOF_ULONG=$ac_cv_sizeof_long
SIZEOF_ULONGLONG=$ac_cv_sizeof_long_long

cat >>confdefs.h <<EOF
#define BALL_CHAR_SIZE ${SIZEOF_CHAR}
EOF

cat >>confdefs.h <<EOF
#define BALL_INT_SIZE ${SIZEOF_INT}
EOF

cat >>confdefs.h <<EOF
#define BALL_LONG_SIZE ${SIZEOF_LONG}
EOF

cat >>confdefs.h <<EOF
#define BALL_SIZE_T_SIZE ${SIZEOF_SIZE_T}
EOF

cat >>confdefs.h <<EOF
#define BALL_POINTER_SIZE ${SIZEOF_VOID_P}
EOF

cat >>confdefs.h <<EOF
#define BALL_UINT_SIZE ${SIZEOF_UINT}
EOF

cat >>confdefs.h <<EOF
#define BALL_ULONG_SIZE ${SIZEOF_ULONG}
EOF

cat >>confdefs.h <<EOF
#define BALL_ULONGLONG_SIZE ${SIZEOF_ULONGLONG}
EOF

if test "${SIZEOF_UINT}" = 8; then
	BALL_64BIT_UINT="unsigned int"
else
	if test "${SIZEOF_ULONG}" = 8; then
		BALL_64BIT_UINT="unsigned long"
	else
		if test "${SIZEOF_ULONGLONG}" = 8 ; then
			BALL_64BIT_UINT="unsigned long long"
		else
			echo "$as_me:5796: result: " >&5
echo "${ECHO_T}" >&6
			echo "$as_me:5798: result: cannot find appropriate numeric type for 64bit unsigned int" >&5
echo "${ECHO_T}cannot find appropriate numeric type for 64bit unsigned int" >&6
			{ { echo "$as_me:5800: error: abort" >&5
echo "$as_me: error: abort" >&2;}
   { (exit 1); exit 1; }; }
		fi
	fi
fi
cat >>confdefs.h <<EOF
#define BALL_64BIT_UINT_TYPE ${BALL_64BIT_UINT}
EOF

if test "${SIZEOF_UINT}" = "${SIZEOF_VOID_P}" ; then
	BALL_POINTER_TYPE="unsigned int"
else
	if test "${SIZEOF_ULONG}" = "${SIZEOF_VOID_P}" ; then
		BALL_POINTER_TYPE="unsigned long"
	else
		echo "$as_me:5816: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:5818: result: cannot find appropriate integer type of same size as void*" >&5
echo "${ECHO_T}cannot find appropriate integer type of same size as void*" >&6
		{ { echo "$as_me:5820: error: abort" >&5
echo "$as_me: error: abort" >&2;}
   { (exit 1); exit 1; }; }
	fi
fi
cat >>confdefs.h <<EOF
#define BALL_POINTERSIZEINT_TYPE ${BALL_POINTER_TYPE}
EOF

if test "${SIZEOF_INT}" = 4 ; then
	BALL_INDEX_TYPE="int"
	BALL_SIZE_TYPE="unsigned int"
else
	if test "${SIZEOF_LONG}" = 4 ; then
		BALL_INDEX_TYPE="long"
		BALL_SIZE_TYPE="unsigned long"
	else
		echo "$as_me:5837: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:5839: result: cannot find appropriate numeric type for 32bit int" >&5
echo "${ECHO_T}cannot find appropriate numeric type for 32bit int" >&6
		{ { echo "$as_me:5841: error: abort" >&5
echo "$as_me: error: abort" >&2;}
   { (exit 1); exit 1; }; }
	fi
fi
cat >>confdefs.h <<EOF
#define BALL_SIZE_TYPE ${BALL_SIZE_TYPE}
EOF

cat >>confdefs.h <<EOF
#define BALL_INDEX_TYPE ${BALL_INDEX_TYPE}
EOF

if test "${SIZEOF_ULONG}" = "8" ; then
	BALL_ULONG64_TYPE="unsigned long"
	cat >>confdefs.h <<\EOF
#define BALL_64BIT_ARCHITECTURE 1
EOF

else
	if test "${SIZEOF_ULONGLONG}" = "8" ; then
		BALL_ULONG64_TYPE="unsigned long long"
	else
		echo "$as_me:5864: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:5866: result: cannot find unsigned 64bit type." >&5
echo "${ECHO_T}cannot find unsigned 64bit type." >&6
		echo "$as_me:5868: result: " >&5
echo "${ECHO_T}" >&6
		{ { echo "$as_me:5870: error: abort" >&5
echo "$as_me: error: abort" >&2;}
   { (exit 1); exit 1; }; }
	fi
fi
cat >>confdefs.h <<EOF
#define BALL_ULONG64_TYPE ${BALL_ULONG64_TYPE}
EOF

echo "$as_me:5879: checking for byte order" >&5
echo $ECHO_N "checking for byte order... $ECHO_C" >&6
if test "$cross_compiling" = yes; then
  DUMMY=0

else
  cat >conftest.$ac_ext <<_ACEOF
#line 5886 "configure"
#include "confdefs.h"

			#include <iostream>
			#include <fstream>
			int main(int, char**)
			{
		 ${BALL_SIZE_TYPE} endian_one = 1;
			std::ofstream os("config.endian.log", std::ios::out);

	    if (*(char*)&endian_one == '\001')
      {
				// big endian
				os << "LITTLE" << std::endl;
      }
	    else
      {
				// little endian
				os << "BIG" << std::endl;
      }
			os.close();

			return 0;
			}

_ACEOF
rm -f conftest$ac_exeext
if { (eval echo "$as_me:5913: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:5916: \$? = $ac_status" >&5
  (exit $ac_status); } && { ac_try='./conftest$ac_exeext'
  { (eval echo "$as_me:5918: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:5921: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  BALL_ENDIAN_TEST=true
else
  echo "$as_me: program exited with status $ac_status" >&5
echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
DUMMY=0
fi
rm -f core core.* *.core conftest$ac_exeext conftest.$ac_ext
fi
if test "${BALL_ENDIAN_TEST+set}" != set ; then
	echo "$as_me:5933: result: <cannot determine>" >&5
echo "${ECHO_T}<cannot determine>" >&6
	echo "$as_me:5935: result: " >&5
echo "${ECHO_T}" >&6
	{ { echo "$as_me:5937: error: Cannot determine endianness of machine." >&5
echo "$as_me: error: Cannot determine endianness of machine." >&2;}
   { (exit 1); exit 1; }; }
else
					ENDIAN_TYPE=`${CAT} config.endian.log`
	if test "${ENDIAN_TYPE}" = "LITTLE" ; then
		BALL_LITTLE_ENDIAN=true
		cat >>confdefs.h <<\EOF
#define BALL_LITTLE_ENDIAN true
EOF

		echo "$as_me:5948: result: little endian" >&5
echo "${ECHO_T}little endian" >&6
	else
		if test "${ENDIAN_TYPE}" = "BIG" ; then
			BALL_BIG_ENDIAN=true
			cat >>confdefs.h <<\EOF
#define BALL_BIG_ENDIAN true
EOF

			echo "$as_me:5957: result: big endian" >&5
echo "${ECHO_T}big endian" >&6
		else
			echo "$as_me:5960: result: <cannot determine>" >&5
echo "${ECHO_T}<cannot determine>" >&6
			echo "$as_me:5962: result: " >&5
echo "${ECHO_T}" >&6
			{ { echo "$as_me:5964: error: Cannot determine endianness of machine." >&5
echo "$as_me: error: Cannot determine endianness of machine." >&2;}
   { (exit 1); exit 1; }; }
		fi
	fi
fi

for ac_func in vsnprintf
do
ac_ac_var=`echo "ac_cv_func_$ac_func" | $ac_tr_sh`
echo "$as_me:5974: checking for $ac_func" >&5
echo $ECHO_N "checking for $ac_func... $ECHO_C" >&6
if eval "test \"\${$ac_ac_var+set}\" = set"; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 5980 "configure"
#include "confdefs.h"
/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char $ac_func (); below.  */
#include <assert.h>
/* Override any gcc2 internal prototype to avoid an error.  */
#ifdef __cplusplus
extern "C"
#endif
/* We use char because int might match the return type of a gcc2
   builtin and then its argument prototype would still apply.  */
char $ac_func ();
char (*f) ();

int
main ()
{
/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined (__stub_$ac_func) || defined (__stub___$ac_func)
choke me
#else
f = $ac_func;
#endif

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:6011: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:6014: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:6017: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:6020: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  eval "$ac_ac_var=yes"
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
eval "$ac_ac_var=no"
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
fi
echo "$as_me:6030: result: `eval echo '${'$ac_ac_var'}'`" >&5
echo "${ECHO_T}`eval echo '${'$ac_ac_var'}'`" >&6
if test `eval echo '${'$ac_ac_var'}'` = yes; then
  cat >>confdefs.h <<EOF
#define `echo "HAVE_$ac_func" | $ac_tr_cpp` 1
EOF
 HAVE_VSNPRINTF=1
fi
done

if test "${HAVE_VSNPRINTF}" = 1 ; then
					if test "$cross_compiling" = yes; then
  DUMMY=0

else
  cat >conftest.$ac_ext <<_ACEOF
#line 6046 "configure"
#include "confdefs.h"

			#include <stdlib.h>
			int main()
			{
				char* buffer[50];
				vsnprintf(buffer, "%1s", "TEST");

				if (!strcmp(buffer, "T"))
				{
					return 0;
				}
				else
				{
					return 1;
				}
			}

_ACEOF
rm -f conftest$ac_exeext
if { (eval echo "$as_me:6067: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:6070: \$? = $ac_status" >&5
  (exit $ac_status); } && { ac_try='./conftest$ac_exeext'
  { (eval echo "$as_me:6072: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:6075: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  VSNPRINTF_OK=1
else
  echo "$as_me: program exited with status $ac_status" >&5
echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
DUMMY=0
fi
rm -f core core.* *.core conftest$ac_exeext conftest.$ac_ext
fi

	if test "${VSNPRINTF_OK}" = 1 ; then
		cat >>confdefs.h <<\EOF
#define BALL_HAVE_VSNPRINTF 1
EOF

	fi
fi

for ac_func in gethostname
do
ac_ac_var=`echo "ac_cv_func_$ac_func" | $ac_tr_sh`
echo "$as_me:6098: checking for $ac_func" >&5
echo $ECHO_N "checking for $ac_func... $ECHO_C" >&6
if eval "test \"\${$ac_ac_var+set}\" = set"; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 6104 "configure"
#include "confdefs.h"
/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char $ac_func (); below.  */
#include <assert.h>
/* Override any gcc2 internal prototype to avoid an error.  */
#ifdef __cplusplus
extern "C"
#endif
/* We use char because int might match the return type of a gcc2
   builtin and then its argument prototype would still apply.  */
char $ac_func ();
char (*f) ();

int
main ()
{
/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined (__stub_$ac_func) || defined (__stub___$ac_func)
choke me
#else
f = $ac_func;
#endif

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:6135: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:6138: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:6141: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:6144: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  eval "$ac_ac_var=yes"
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
eval "$ac_ac_var=no"
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
fi
echo "$as_me:6154: result: `eval echo '${'$ac_ac_var'}'`" >&5
echo "${ECHO_T}`eval echo '${'$ac_ac_var'}'`" >&6
if test `eval echo '${'$ac_ac_var'}'` = yes; then
  cat >>confdefs.h <<EOF
#define `echo "HAVE_$ac_func" | $ac_tr_cpp` 1
EOF
 HAVE_GETHOSTNAME=1
fi
done

if test "${HAVE_GETHOSTNAME}" = 1 ; then
	cat >>confdefs.h <<\EOF
#define BALL_HAVE_GETHOSTNAME 1
EOF

else

for ac_func in sysinfo
do
ac_ac_var=`echo "ac_cv_func_$ac_func" | $ac_tr_sh`
echo "$as_me:6174: checking for $ac_func" >&5
echo $ECHO_N "checking for $ac_func... $ECHO_C" >&6
if eval "test \"\${$ac_ac_var+set}\" = set"; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 6180 "configure"
#include "confdefs.h"
/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char $ac_func (); below.  */
#include <assert.h>
/* Override any gcc2 internal prototype to avoid an error.  */
#ifdef __cplusplus
extern "C"
#endif
/* We use char because int might match the return type of a gcc2
   builtin and then its argument prototype would still apply.  */
char $ac_func ();
char (*f) ();

int
main ()
{
/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined (__stub_$ac_func) || defined (__stub___$ac_func)
choke me
#else
f = $ac_func;
#endif

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:6211: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:6214: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:6217: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:6220: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  eval "$ac_ac_var=yes"
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
eval "$ac_ac_var=no"
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
fi
echo "$as_me:6230: result: `eval echo '${'$ac_ac_var'}'`" >&5
echo "${ECHO_T}`eval echo '${'$ac_ac_var'}'`" >&6
if test `eval echo '${'$ac_ac_var'}'` = yes; then
  cat >>confdefs.h <<EOF
#define `echo "HAVE_$ac_func" | $ac_tr_cpp` 1
EOF
 HAVE_SYSINFO=1
fi
done

	if test "${HAVE_SYSINFO}" = 1  ; then
		cat >>confdefs.h <<\EOF
#define BALL_HAVE_SYSINFO 1
EOF

	fi
fi

if test "${HAVE_GETHOSTNAME}" = 1 ; then
	echo "$as_me:6249: checking for gethostname in unistd.h" >&5
echo $ECHO_N "checking for gethostname in unistd.h... $ECHO_C" >&6
	cat >conftest.$ac_ext <<_ACEOF
#line 6252 "configure"
#include "confdefs.h"

			#include <unistd.h>

int
main ()
{

			char name[1024];
			gethostname(name, 1023);

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:6269: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:6272: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:6275: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:6278: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  HAVE_GETHOSTNAME_HEADER=1

else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest.$ac_ext
	if test "${HAVE_GETHOSTNAME_HEADER+set}" != set ; then
		echo "$as_me:6288: result: no" >&5
echo "${ECHO_T}no" >&6
		cat >>confdefs.h <<\EOF
#define BALL_DEFINE_GETHOSTNAME 1
EOF

	else
		echo "$as_me:6295: result: yes" >&5
echo "${ECHO_T}yes" >&6
	fi
fi

for ac_func in inet_addr
do
ac_ac_var=`echo "ac_cv_func_$ac_func" | $ac_tr_sh`
echo "$as_me:6303: checking for $ac_func" >&5
echo $ECHO_N "checking for $ac_func... $ECHO_C" >&6
if eval "test \"\${$ac_ac_var+set}\" = set"; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 6309 "configure"
#include "confdefs.h"
/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char $ac_func (); below.  */
#include <assert.h>
/* Override any gcc2 internal prototype to avoid an error.  */
#ifdef __cplusplus
extern "C"
#endif
/* We use char because int might match the return type of a gcc2
   builtin and then its argument prototype would still apply.  */
char $ac_func ();
char (*f) ();

int
main ()
{
/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined (__stub_$ac_func) || defined (__stub___$ac_func)
choke me
#else
f = $ac_func;
#endif

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:6340: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:6343: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:6346: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:6349: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  eval "$ac_ac_var=yes"
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
eval "$ac_ac_var=no"
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
fi
echo "$as_me:6359: result: `eval echo '${'$ac_ac_var'}'`" >&5
echo "${ECHO_T}`eval echo '${'$ac_ac_var'}'`" >&6
if test `eval echo '${'$ac_ac_var'}'` = yes; then
  cat >>confdefs.h <<EOF
#define `echo "HAVE_$ac_func" | $ac_tr_cpp` 1
EOF
 HAVE_INET_ADDR=1
fi
done

for ac_func in gethostbyname
do
ac_ac_var=`echo "ac_cv_func_$ac_func" | $ac_tr_sh`
echo "$as_me:6372: checking for $ac_func" >&5
echo $ECHO_N "checking for $ac_func... $ECHO_C" >&6
if eval "test \"\${$ac_ac_var+set}\" = set"; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 6378 "configure"
#include "confdefs.h"
/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char $ac_func (); below.  */
#include <assert.h>
/* Override any gcc2 internal prototype to avoid an error.  */
#ifdef __cplusplus
extern "C"
#endif
/* We use char because int might match the return type of a gcc2
   builtin and then its argument prototype would still apply.  */
char $ac_func ();
char (*f) ();

int
main ()
{
/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined (__stub_$ac_func) || defined (__stub___$ac_func)
choke me
#else
f = $ac_func;
#endif

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:6409: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:6412: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:6415: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:6418: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  eval "$ac_ac_var=yes"
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
eval "$ac_ac_var=no"
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
fi
echo "$as_me:6428: result: `eval echo '${'$ac_ac_var'}'`" >&5
echo "${ECHO_T}`eval echo '${'$ac_ac_var'}'`" >&6
if test `eval echo '${'$ac_ac_var'}'` = yes; then
  cat >>confdefs.h <<EOF
#define `echo "HAVE_$ac_func" | $ac_tr_cpp` 1
EOF
 HAVE_GETHOSTBYNAME=1
fi
done

if test "${HAVE_INET_ADDR+set}" = set ; then
	echo "$as_me:6439: checking for inet_aton" >&5
echo $ECHO_N "checking for inet_aton... $ECHO_C" >&6
if test "${ac_cv_func_inet_aton+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 6445 "configure"
#include "confdefs.h"
/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char inet_aton (); below.  */
#include <assert.h>
/* Override any gcc2 internal prototype to avoid an error.  */
#ifdef __cplusplus
extern "C"
#endif
/* We use char because int might match the return type of a gcc2
   builtin and then its argument prototype would still apply.  */
char inet_aton ();
char (*f) ();

int
main ()
{
/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined (__stub_inet_aton) || defined (__stub___inet_aton)
choke me
#else
f = inet_aton;
#endif

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:6476: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:6479: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:6482: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:6485: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_cv_func_inet_aton=yes
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_cv_func_inet_aton=no
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
fi
echo "$as_me:6495: result: $ac_cv_func_inet_aton" >&5
echo "${ECHO_T}$ac_cv_func_inet_aton" >&6
if test $ac_cv_func_inet_aton = yes; then
  HAVE_INET_ATON=1
fi

fi

if test "${HAVE_GETHOSTBYNAME+set}" != set -a "${USE_LIBXNET}" != false; then

echo "$as_me:6505: checking for gethostbyname in -lxnet" >&5
echo $ECHO_N "checking for gethostbyname in -lxnet... $ECHO_C" >&6
if test "${ac_cv_lib_xnet_gethostbyname+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  ac_check_lib_save_LIBS=$LIBS
LIBS="-lxnet  $LIBS"
cat >conftest.$ac_ext <<_ACEOF
#line 6513 "configure"
#include "confdefs.h"

/* Override any gcc2 internal prototype to avoid an error.  */
#ifdef __cplusplus
extern "C"
#endif
/* We use char because int might match the return type of a gcc2
   builtin and then its argument prototype would still apply.  */
char gethostbyname ();
int
main ()
{
gethostbyname ();
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:6532: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:6535: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:6538: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:6541: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_cv_lib_xnet_gethostbyname=yes
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_cv_lib_xnet_gethostbyname=no
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
LIBS=$ac_check_lib_save_LIBS
fi
echo "$as_me:6552: result: $ac_cv_lib_xnet_gethostbyname" >&5
echo "${ECHO_T}$ac_cv_lib_xnet_gethostbyname" >&6
if test $ac_cv_lib_xnet_gethostbyname = yes; then
  cat >>confdefs.h <<EOF
#define HAVE_LIBXNET 1
EOF

  LIBS="-lxnet $LIBS"

fi

	unset ac_cv_func_gethostbyname

for ac_func in gethostbyname
do
ac_ac_var=`echo "ac_cv_func_$ac_func" | $ac_tr_sh`
echo "$as_me:6568: checking for $ac_func" >&5
echo $ECHO_N "checking for $ac_func... $ECHO_C" >&6
if eval "test \"\${$ac_ac_var+set}\" = set"; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 6574 "configure"
#include "confdefs.h"
/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char $ac_func (); below.  */
#include <assert.h>
/* Override any gcc2 internal prototype to avoid an error.  */
#ifdef __cplusplus
extern "C"
#endif
/* We use char because int might match the return type of a gcc2
   builtin and then its argument prototype would still apply.  */
char $ac_func ();
char (*f) ();

int
main ()
{
/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined (__stub_$ac_func) || defined (__stub___$ac_func)
choke me
#else
f = $ac_func;
#endif

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:6605: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:6608: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:6611: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:6614: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  eval "$ac_ac_var=yes"
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
eval "$ac_ac_var=no"
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
fi
echo "$as_me:6624: result: `eval echo '${'$ac_ac_var'}'`" >&5
echo "${ECHO_T}`eval echo '${'$ac_ac_var'}'`" >&6
if test `eval echo '${'$ac_ac_var'}'` = yes; then
  cat >>confdefs.h <<EOF
#define `echo "HAVE_$ac_func" | $ac_tr_cpp` 1
EOF
 HAVE_GETHOSTBYNAME=1
fi
done

fi
if test "${HAVE_INET_ADDR+set}" != set ; then
	unset ac_cv_func_inet_addr

for ac_func in inet_addr
do
ac_ac_var=`echo "ac_cv_func_$ac_func" | $ac_tr_sh`
echo "$as_me:6641: checking for $ac_func" >&5
echo $ECHO_N "checking for $ac_func... $ECHO_C" >&6
if eval "test \"\${$ac_ac_var+set}\" = set"; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 6647 "configure"
#include "confdefs.h"
/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char $ac_func (); below.  */
#include <assert.h>
/* Override any gcc2 internal prototype to avoid an error.  */
#ifdef __cplusplus
extern "C"
#endif
/* We use char because int might match the return type of a gcc2
   builtin and then its argument prototype would still apply.  */
char $ac_func ();
char (*f) ();

int
main ()
{
/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined (__stub_$ac_func) || defined (__stub___$ac_func)
choke me
#else
f = $ac_func;
#endif

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:6678: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:6681: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:6684: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:6687: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  eval "$ac_ac_var=yes"
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
eval "$ac_ac_var=no"
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
fi
echo "$as_me:6697: result: `eval echo '${'$ac_ac_var'}'`" >&5
echo "${ECHO_T}`eval echo '${'$ac_ac_var'}'`" >&6
if test `eval echo '${'$ac_ac_var'}'` = yes; then
  cat >>confdefs.h <<EOF
#define `echo "HAVE_$ac_func" | $ac_tr_cpp` 1
EOF
 HAVE_INET_ADDR=1
fi
done

	if test "${HAVE_INET_ADDR+set}" != set -a "${USE_LIBXNET}" != false; then

echo "$as_me:6709: checking for inet_addr in -lxnet" >&5
echo $ECHO_N "checking for inet_addr in -lxnet... $ECHO_C" >&6
if test "${ac_cv_lib_xnet_inet_addr+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  ac_check_lib_save_LIBS=$LIBS
LIBS="-lxnet  $LIBS"
cat >conftest.$ac_ext <<_ACEOF
#line 6717 "configure"
#include "confdefs.h"

/* Override any gcc2 internal prototype to avoid an error.  */
#ifdef __cplusplus
extern "C"
#endif
/* We use char because int might match the return type of a gcc2
   builtin and then its argument prototype would still apply.  */
char inet_addr ();
int
main ()
{
inet_addr ();
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:6736: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:6739: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:6742: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:6745: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_cv_lib_xnet_inet_addr=yes
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_cv_lib_xnet_inet_addr=no
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
LIBS=$ac_check_lib_save_LIBS
fi
echo "$as_me:6756: result: $ac_cv_lib_xnet_inet_addr" >&5
echo "${ECHO_T}$ac_cv_lib_xnet_inet_addr" >&6
if test $ac_cv_lib_xnet_inet_addr = yes; then
  cat >>confdefs.h <<EOF
#define HAVE_LIBXNET 1
EOF

  LIBS="-lxnet $LIBS"

fi

		unset ac_cv_func_inet_addr

for ac_func in inet_addr
do
ac_ac_var=`echo "ac_cv_func_$ac_func" | $ac_tr_sh`
echo "$as_me:6772: checking for $ac_func" >&5
echo $ECHO_N "checking for $ac_func... $ECHO_C" >&6
if eval "test \"\${$ac_ac_var+set}\" = set"; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 6778 "configure"
#include "confdefs.h"
/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char $ac_func (); below.  */
#include <assert.h>
/* Override any gcc2 internal prototype to avoid an error.  */
#ifdef __cplusplus
extern "C"
#endif
/* We use char because int might match the return type of a gcc2
   builtin and then its argument prototype would still apply.  */
char $ac_func ();
char (*f) ();

int
main ()
{
/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined (__stub_$ac_func) || defined (__stub___$ac_func)
choke me
#else
f = $ac_func;
#endif

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:6809: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:6812: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:6815: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:6818: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  eval "$ac_ac_var=yes"
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
eval "$ac_ac_var=no"
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
fi
echo "$as_me:6828: result: `eval echo '${'$ac_ac_var'}'`" >&5
echo "${ECHO_T}`eval echo '${'$ac_ac_var'}'`" >&6
if test `eval echo '${'$ac_ac_var'}'` = yes; then
  cat >>confdefs.h <<EOF
#define `echo "HAVE_$ac_func" | $ac_tr_cpp` 1
EOF
 HAVE_INET_ADDR=1
fi
done

	fi
fi

if test "${HAVE_GETHOSTBYNAME+set}" != set ; then

echo "$as_me:6843: checking for gethostbyname in -lnsl" >&5
echo $ECHO_N "checking for gethostbyname in -lnsl... $ECHO_C" >&6
if test "${ac_cv_lib_nsl_gethostbyname+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  ac_check_lib_save_LIBS=$LIBS
LIBS="-lnsl  $LIBS"
cat >conftest.$ac_ext <<_ACEOF
#line 6851 "configure"
#include "confdefs.h"

/* Override any gcc2 internal prototype to avoid an error.  */
#ifdef __cplusplus
extern "C"
#endif
/* We use char because int might match the return type of a gcc2
   builtin and then its argument prototype would still apply.  */
char gethostbyname ();
int
main ()
{
gethostbyname ();
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:6870: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:6873: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:6876: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:6879: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_cv_lib_nsl_gethostbyname=yes
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_cv_lib_nsl_gethostbyname=no
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
LIBS=$ac_check_lib_save_LIBS
fi
echo "$as_me:6890: result: $ac_cv_lib_nsl_gethostbyname" >&5
echo "${ECHO_T}$ac_cv_lib_nsl_gethostbyname" >&6
if test $ac_cv_lib_nsl_gethostbyname = yes; then
  cat >>confdefs.h <<EOF
#define HAVE_LIBNSL 1
EOF

  LIBS="-lnsl $LIBS"

fi

	unset ac_cv_func_gethostbyname

for ac_func in gethostbyname
do
ac_ac_var=`echo "ac_cv_func_$ac_func" | $ac_tr_sh`
echo "$as_me:6906: checking for $ac_func" >&5
echo $ECHO_N "checking for $ac_func... $ECHO_C" >&6
if eval "test \"\${$ac_ac_var+set}\" = set"; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 6912 "configure"
#include "confdefs.h"
/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char $ac_func (); below.  */
#include <assert.h>
/* Override any gcc2 internal prototype to avoid an error.  */
#ifdef __cplusplus
extern "C"
#endif
/* We use char because int might match the return type of a gcc2
   builtin and then its argument prototype would still apply.  */
char $ac_func ();
char (*f) ();

int
main ()
{
/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined (__stub_$ac_func) || defined (__stub___$ac_func)
choke me
#else
f = $ac_func;
#endif

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:6943: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:6946: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:6949: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:6952: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  eval "$ac_ac_var=yes"
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
eval "$ac_ac_var=no"
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
fi
echo "$as_me:6962: result: `eval echo '${'$ac_ac_var'}'`" >&5
echo "${ECHO_T}`eval echo '${'$ac_ac_var'}'`" >&6
if test `eval echo '${'$ac_ac_var'}'` = yes; then
  cat >>confdefs.h <<EOF
#define `echo "HAVE_$ac_func" | $ac_tr_cpp` 1
EOF
 HAVE_GETHOSTBYNAME=1
fi
done

fi

if test "${HAVE_INET_ADDR+set}" != set ; then

echo "$as_me:6976: checking for inet_addr in -lsocket" >&5
echo $ECHO_N "checking for inet_addr in -lsocket... $ECHO_C" >&6
if test "${ac_cv_lib_socket_inet_addr+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  ac_check_lib_save_LIBS=$LIBS
LIBS="-lsocket  $LIBS"
cat >conftest.$ac_ext <<_ACEOF
#line 6984 "configure"
#include "confdefs.h"

/* Override any gcc2 internal prototype to avoid an error.  */
#ifdef __cplusplus
extern "C"
#endif
/* We use char because int might match the return type of a gcc2
   builtin and then its argument prototype would still apply.  */
char inet_addr ();
int
main ()
{
inet_addr ();
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:7003: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:7006: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:7009: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:7012: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_cv_lib_socket_inet_addr=yes
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_cv_lib_socket_inet_addr=no
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
LIBS=$ac_check_lib_save_LIBS
fi
echo "$as_me:7023: result: $ac_cv_lib_socket_inet_addr" >&5
echo "${ECHO_T}$ac_cv_lib_socket_inet_addr" >&6
if test $ac_cv_lib_socket_inet_addr = yes; then
  cat >>confdefs.h <<EOF
#define HAVE_LIBSOCKET 1
EOF

  LIBS="-lsocket $LIBS"

fi

	unset ac_cv_func_inet_addr

for ac_func in inet_addr
do
ac_ac_var=`echo "ac_cv_func_$ac_func" | $ac_tr_sh`
echo "$as_me:7039: checking for $ac_func" >&5
echo $ECHO_N "checking for $ac_func... $ECHO_C" >&6
if eval "test \"\${$ac_ac_var+set}\" = set"; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 7045 "configure"
#include "confdefs.h"
/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char $ac_func (); below.  */
#include <assert.h>
/* Override any gcc2 internal prototype to avoid an error.  */
#ifdef __cplusplus
extern "C"
#endif
/* We use char because int might match the return type of a gcc2
   builtin and then its argument prototype would still apply.  */
char $ac_func ();
char (*f) ();

int
main ()
{
/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined (__stub_$ac_func) || defined (__stub___$ac_func)
choke me
#else
f = $ac_func;
#endif

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:7076: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:7079: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:7082: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:7085: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  eval "$ac_ac_var=yes"
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
eval "$ac_ac_var=no"
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
fi
echo "$as_me:7095: result: `eval echo '${'$ac_ac_var'}'`" >&5
echo "${ECHO_T}`eval echo '${'$ac_ac_var'}'`" >&6
if test `eval echo '${'$ac_ac_var'}'` = yes; then
  cat >>confdefs.h <<EOF
#define `echo "HAVE_$ac_func" | $ac_tr_cpp` 1
EOF
 HAVE_INET_ADDR=1
fi
done

fi

if test "${HAVE_INET_ATON+set}" != set ; then
	unset ac_cv_func_inet_aton
	echo "$as_me:7109: checking for inet_aton" >&5
echo $ECHO_N "checking for inet_aton... $ECHO_C" >&6
if test "${ac_cv_func_inet_aton+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 7115 "configure"
#include "confdefs.h"
/* System header to define __stub macros and hopefully few prototypes,
    which can conflict with char inet_aton (); below.  */
#include <assert.h>
/* Override any gcc2 internal prototype to avoid an error.  */
#ifdef __cplusplus
extern "C"
#endif
/* We use char because int might match the return type of a gcc2
   builtin and then its argument prototype would still apply.  */
char inet_aton ();
char (*f) ();

int
main ()
{
/* The GNU C library defines this for functions which it implements
    to always fail with ENOSYS.  Some functions are actually named
    something starting with __ and the normal name is an alias.  */
#if defined (__stub_inet_aton) || defined (__stub___inet_aton)
choke me
#else
f = inet_aton;
#endif

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:7146: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:7149: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:7152: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:7155: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_cv_func_inet_aton=yes
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_cv_func_inet_aton=no
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
fi
echo "$as_me:7165: result: $ac_cv_func_inet_aton" >&5
echo "${ECHO_T}$ac_cv_func_inet_aton" >&6
if test $ac_cv_func_inet_aton = yes; then
  HAVE_INET_ATON=1
fi

fi

if test "${HAVE_INET_ATON+set}" = set ; then
	cat >>confdefs.h <<\EOF
#define HAVE_INET_ATON
EOF

fi

echo "$as_me:7180: checking for socketlen type" >&5
echo $ECHO_N "checking for socketlen type... $ECHO_C" >&6
cat >conftest.$ac_ext <<_ACEOF
#line 7183 "configure"
#include "confdefs.h"

		#include <sys/types.h>
		#include <sys/socket.h>
		#include <netinet/in.h>

int
main ()
{
	sockaddr_in   addr;
	  socklen_t     len = 0;
	  getsockname(0, (struct sockaddr*)&addr, &len);

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:7202: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:7205: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:7208: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:7211: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  BALL_SOCKLEN_TYPE=socklen_t
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest.$ac_ext
if test "${BALL_SOCKLEN_TYPE}" = "" ; then
	cat >conftest.$ac_ext <<_ACEOF
#line 7221 "configure"
#include "confdefs.h"

			#include <sys/types.h>
			#include <sys/socket.h>
			#include <netinet/in.h>

int
main ()
{
	sockaddr_in   addr;
		  size_t    len = 0;
		  getsockname(0, (struct sockaddr*)&addr, &len);

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:7240: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:7243: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:7246: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:7249: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  BALL_SOCKLEN_TYPE=size_t
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest.$ac_ext
fi
if test "${BALL_SOCKLEN_TYPE}" = "" ; then
	cat >conftest.$ac_ext <<_ACEOF
#line 7260 "configure"
#include "confdefs.h"

			#include <sys/types.h>
			#include <sys/socket.h>
			#include <netinet/in.h>

int
main ()
{
	sockaddr_in   addr;
		  unsigned int  len = 0;
		  getsockname(0, (struct sockaddr*)&addr, &len);

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:7279: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:7282: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:7285: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:7288: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  BALL_SOCKLEN_TYPE="unsigned int"
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest.$ac_ext
fi
if test "${BALL_SOCKLEN_TYPE}" = "" ; then
	cat >conftest.$ac_ext <<_ACEOF
#line 7299 "configure"
#include "confdefs.h"

			#include <sys/types.h>
			#include <sys/socket.h>
			#include <netinet/in.h>

int
main ()
{
	sockaddr_in   addr;
		  int 					len = 0;
		  getsockname(0, (struct sockaddr*)&addr, &len);

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:7318: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:7321: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:7324: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:7327: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  BALL_SOCKLEN_TYPE="int"
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest.$ac_ext
fi
if test "${BALL_SOCKLEN_TYPE}" = "" ; then
	echo "$as_me:7337: result: FAILED" >&5
echo "${ECHO_T}FAILED" >&6
	echo "$as_me:7339: result: -------------WARNING!---------------" >&5
echo "${ECHO_T}-------------WARNING!---------------" >&6
	echo "$as_me:7341: result: could not find a matching type for socket length argument" >&5
echo "${ECHO_T}could not find a matching type for socket length argument" >&6
	echo "$as_me:7343: result: in call to getsockname" >&5
echo "${ECHO_T}in call to getsockname" >&6
	echo "$as_me:7345: result: please check the setting for BALL_SOCKLEN_TYPE in config.mak" >&5
echo "${ECHO_T}please check the setting for BALL_SOCKLEN_TYPE in config.mak" >&6
	echo "$as_me:7347: result: and set it to the type needed for the third arguemnt of  getsockname" >&5
echo "${ECHO_T}and set it to the type needed for the third arguemnt of  getsockname" >&6
	echo "$as_me:7349: result: " >&5
echo "${ECHO_T}" >&6
else
	echo "$as_me:7352: result: $BALL_SOCKLEN_TYPE" >&5
echo "${ECHO_T}$BALL_SOCKLEN_TYPE" >&6
fi

cat >>confdefs.h <<EOF
#define BALL_SOCKLEN_TYPE ${BALL_SOCKLEN_TYPE}
EOF

if test "${BALL_NO_XDR}" = "true" ; then

	echo "$as_me:7362: result: No XDR headers available - building of XDR persistence support disabled" >&5
echo "${ECHO_T}No XDR headers available - building of XDR persistence support disabled" >&6
	cat >>confdefs.h <<\EOF
#define BALL_HAS_XDR
EOF

	BALL_HAS_XDR=false

else

	echo "$as_me:7372: checking for rpc/types.h" >&5
echo $ECHO_N "checking for rpc/types.h... $ECHO_C" >&6
if test "${ac_cv_header_rpc_types_h+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 7378 "configure"
#include "confdefs.h"
#include <rpc/types.h>
_ACEOF
if { (eval echo "$as_me:7382: \"$ac_cpp conftest.$ac_ext\"") >&5
  (eval $ac_cpp conftest.$ac_ext) 2>conftest.er1
  ac_status=$?
  egrep -v '^ *\+' conftest.er1 >conftest.err
  rm -f conftest.er1
  cat conftest.err >&5
  echo "$as_me:7388: \$? = $ac_status" >&5
  (exit $ac_status); } >/dev/null; then
  if test -s conftest.err; then
    ac_cpp_err=$ac_cxx_preproc_warn_flag
  else
    ac_cpp_err=
  fi
else
  ac_cpp_err=yes
fi
if test -z "$ac_cpp_err"; then
  ac_cv_header_rpc_types_h=yes
else
  echo "$as_me: failed program was:" >&5
  cat conftest.$ac_ext >&5
  ac_cv_header_rpc_types_h=no
fi
rm -f conftest.err conftest.$ac_ext
fi
echo "$as_me:7407: result: $ac_cv_header_rpc_types_h" >&5
echo "${ECHO_T}$ac_cv_header_rpc_types_h" >&6
if test $ac_cv_header_rpc_types_h = yes; then
  HAS_RPC_TYPES_H=true
else
  HAS_RPC_TYPES_H=false
fi

	if test "${HAS_RPC_TYPES_H}" = false ; then
		echo "$as_me:7416: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:7418: result: Cannot find RPC headers (rpc/types.h)." >&5
echo "${ECHO_T}Cannot find RPC headers (rpc/types.h)." >&6
		echo "$as_me:7420: result: If your system does not provide an RPC/XDR implementation (e.g., CYGWIN)," >&5
echo "${ECHO_T}If your system does not provide an RPC/XDR implementation (e.g., CYGWIN)," >&6
		echo "$as_me:7422: result: please specify the option --without-xdr to avoid this error." >&5
echo "${ECHO_T}please specify the option --without-xdr to avoid this error." >&6
		echo "$as_me:7424: result: " >&5
echo "${ECHO_T}" >&6
		{ { echo "$as_me:7426: error: Aborted." >&5
echo "$as_me: error: Aborted." >&2;}
   { (exit 1); exit 1; }; }
	fi

	echo "$as_me:7431: checking for rpc/xdr.h" >&5
echo $ECHO_N "checking for rpc/xdr.h... $ECHO_C" >&6
if test "${ac_cv_header_rpc_xdr_h+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 7437 "configure"
#include "confdefs.h"
#include <rpc/xdr.h>
_ACEOF
if { (eval echo "$as_me:7441: \"$ac_cpp conftest.$ac_ext\"") >&5
  (eval $ac_cpp conftest.$ac_ext) 2>conftest.er1
  ac_status=$?
  egrep -v '^ *\+' conftest.er1 >conftest.err
  rm -f conftest.er1
  cat conftest.err >&5
  echo "$as_me:7447: \$? = $ac_status" >&5
  (exit $ac_status); } >/dev/null; then
  if test -s conftest.err; then
    ac_cpp_err=$ac_cxx_preproc_warn_flag
  else
    ac_cpp_err=
  fi
else
  ac_cpp_err=yes
fi
if test -z "$ac_cpp_err"; then
  ac_cv_header_rpc_xdr_h=yes
else
  echo "$as_me: failed program was:" >&5
  cat conftest.$ac_ext >&5
  ac_cv_header_rpc_xdr_h=no
fi
rm -f conftest.err conftest.$ac_ext
fi
echo "$as_me:7466: result: $ac_cv_header_rpc_xdr_h" >&5
echo "${ECHO_T}$ac_cv_header_rpc_xdr_h" >&6
if test $ac_cv_header_rpc_xdr_h = yes; then
  HAS_XDR_H=true
else
  HAS_XDR_H=false
fi

	if test "${HAS_XDR_H}" = false ; then
		echo "$as_me:7475: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:7477: result: Cannot find XDR headers (rpc/xdr.h)." >&5
echo "${ECHO_T}Cannot find XDR headers (rpc/xdr.h)." >&6
		echo "$as_me:7479: result: " >&5
echo "${ECHO_T}" >&6
		{ { echo "$as_me:7481: error: Aborted." >&5
echo "$as_me: error: Aborted." >&2;}
   { (exit 1); exit 1; }; }
	fi

	echo "$as_me:7486: checking arg types for xdrrec_create" >&5
echo $ECHO_N "checking arg types for xdrrec_create... $ECHO_C" >&6
	cat >conftest.$ac_ext <<_ACEOF
#line 7489 "configure"
#include "confdefs.h"

				#include <rpc/types.h>
				#include <rpc/xdr.h>
				extern "C" int dummy(void*, void*, unsigned int) {return 0;}
				void foo(){
					XDR xdrs;
					xdrrec_create(&xdrs, 0, 0, 0, dummy, dummy);
				}

int
main ()
{

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:7509: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:7512: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:7515: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:7518: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  BALL_XDRREC_VOID_VOID_UINT=true
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
BALL_XDRREC_VOID_VOID_UINT=false

fi
rm -f conftest.$ac_objext conftest.$ac_ext
	if test "${BALL_XDRREC_VOID_VOID_UINT}" = false ; then
		cat >conftest.$ac_ext <<_ACEOF
#line 7530 "configure"
#include "confdefs.h"

					#include <rpc/types.h>
					#include <rpc/xdr.h>
					extern "C" int dummy(void*, char*, int) {return 0;}
					void foo(){
						XDR xdrs;
						xdrrec_create(&xdrs, 0, 0, 0, dummy, dummy);
					}

int
main ()
{

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:7550: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:7553: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:7556: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:7559: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  BALL_XDRREC_VOID_CHAR_INT=true
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
BALL_XDRREC_VOID_CHAR_INT=false

fi
rm -f conftest.$ac_objext conftest.$ac_ext
		if test "${BALL_XDRREC_VOID_CHAR_INT}" = true ; then
			echo "$as_me:7570: result: (void*, char*, int)" >&5
echo "${ECHO_T}(void*, char*, int)" >&6
		else
			cat >conftest.$ac_ext <<_ACEOF
#line 7574 "configure"
#include "confdefs.h"

						#include <rpc/types.h>
						#include <rpc/xdr.h>
						extern "C" int dummy(char*, char*, int) {return 0;}
						void foo(){
							XDR xdrs;
							xdrrec_create(&xdrs, 0, 0, 0, dummy, dummy);
						}

int
main ()
{

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:7594: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:7597: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:7600: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:7603: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  BALL_XDRREC_CHAR_CHAR_INT=true
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
BALL_XDRREC_CHAR_CHAR_INT=false

fi
rm -f conftest.$ac_objext conftest.$ac_ext
			if test "${BALL_XDRREC_CHAR_CHAR_INT}" = true ; then
				echo "$as_me:7614: result: (char*, char*, int)" >&5
echo "${ECHO_T}(char*, char*, int)" >&6
			else
				cat >conftest.$ac_ext <<_ACEOF
#line 7618 "configure"
#include "confdefs.h"

						#include <rpc/types.h>
						#include <rpc/xdr.h>
						extern "C" int dummy() {return 0;}
						void foo(){
							XDR xdrs;
							xdrrec_create(&xdrs, 0, 0, 0, dummy, dummy);
						}

int
main ()
{

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:7638: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:7641: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:7644: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:7647: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  BALL_XDRREC_VOID=true
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
BALL_XDRREC_VOID=false

fi
rm -f conftest.$ac_objext conftest.$ac_ext
				if test "${BALL_XDRREC_VOID}" = true ; then
					echo "$as_me:7658: result: ()" >&5
echo "${ECHO_T}()" >&6
				else
					echo "$as_me:7661: result: not found!" >&5
echo "${ECHO_T}not found!" >&6
					{ { echo "$as_me:7663: error: Aborted" >&5
echo "$as_me: error: Aborted" >&2;}
   { (exit 1); exit 1; }; }
				fi
			fi
		fi
	else
		echo "$as_me:7670: result: (void*, void*, unsigned int)" >&5
echo "${ECHO_T}(void*, void*, unsigned int)" >&6
	fi

					echo "$as_me:7674: checking for xdr_u_hyper function" >&5
echo $ECHO_N "checking for xdr_u_hyper function... $ECHO_C" >&6
	BALL_HAS_XDR_U_HYPER=false
	cat >conftest.$ac_ext <<_ACEOF
#line 7678 "configure"
#include "confdefs.h"

			#include <rpc/types.h>
			#include <rpc/xdr.h>

int
main ()
{

			xdr_u_hyper(0, 0);

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:7695: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:7698: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:7701: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:7704: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  BALL_HAS_XDR_U_HYPER=true

else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest.$ac_ext

	if test "${BALL_HAS_XDR_U_HYPER}" = "true" ; then
		echo "$as_me:7715: result: found" >&5
echo "${ECHO_T}found" >&6

		echo "$as_me:7718: checking for 64-bit XDR type (for xdr_u_hyper)" >&5
echo $ECHO_N "checking for 64-bit XDR type (for xdr_u_hyper)... $ECHO_C" >&6
		BALL_U_QUAD_TYPE=""
		cat >conftest.$ac_ext <<_ACEOF
#line 7722 "configure"
#include "confdefs.h"

				#include <rpc/types.h>
				#include <rpc/xdr.h>

int
main ()
{
	u_quad_t   q;
				XDR xdrs;
		  		xdr_u_hyper(&xdrs, &q);

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:7740: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:7743: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:7746: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:7749: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  BALL_U_QUAD_TYPE=u_quad_t

else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest.$ac_ext

		if test "${BALL_U_QUAD_TYPE}" = "" ; then
			cat >conftest.$ac_ext <<_ACEOF
#line 7761 "configure"
#include "confdefs.h"

					#include <rpc/types.h>
					#include <rpc/xdr.h>

int
main ()
{
	u_longlong_t   q;
					XDR xdrs;
					xdr_u_hyper(&xdrs, &q);

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:7779: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:7782: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:7785: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:7788: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  BALL_U_QUAD_TYPE=u_longlong_t

else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest.$ac_ext
		fi

		if test "${BALL_U_QUAD_TYPE}" = "" ; then
			cat >conftest.$ac_ext <<_ACEOF
#line 7801 "configure"
#include "confdefs.h"

					#include <rpc/types.h>
					#include <rpc/xdr.h>

int
main ()
{
	unsigned long long int  q;
					XDR xdrs;
					xdr_u_hyper(&xdrs, &q);

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:7819: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:7822: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:7825: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:7828: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  BALL_U_QUAD_TYPE="unsigned long long int"

else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest.$ac_ext
		fi

		if test "${BALL_U_QUAD_TYPE}" = "" ; then
			cat >conftest.$ac_ext <<_ACEOF
#line 7841 "configure"
#include "confdefs.h"

					#include <rpc/types.h>
					#include <rpc/xdr.h>

int
main ()
{
	__uint64_t  q;
					XDR xdrs;
					xdr_u_hyper(&xdrs, &q);

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:7859: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:7862: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:7865: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:7868: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  BALL_U_QUAD_TYPE=__uint64_t

else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest.$ac_ext
		fi
		if test "${BALL_U_QUAD_TYPE}" = "" ; then
			echo "$as_me:7879: result: Could not identify an appropriate type for xdr_u_hyper." >&5
echo "${ECHO_T}Could not identify an appropriate type for xdr_u_hyper." >&6
			{ { echo "$as_me:7881: error: Aborted." >&5
echo "$as_me: error: Aborted." >&2;}
   { (exit 1); exit 1; }; }
		fi

		echo "$as_me:7886: result: ${BALL_U_QUAD_TYPE}" >&5
echo "${ECHO_T}${BALL_U_QUAD_TYPE}" >&6
		cat >>confdefs.h <<EOF
#define BALL_XDR_UINT64_TYPE ${BALL_U_QUAD_TYPE}
EOF

		cat >>confdefs.h <<\EOF
#define BALL_HAS_XDR_U_HYPER 1
EOF

	else

														cat >conftest.$ac_ext <<_ACEOF
#line 7899 "configure"
#include "confdefs.h"

int
main ()
{

				unsigned long long int  q = 1234567890123456789LL;

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext
if { (eval echo "$as_me:7913: \"$ac_compile\"") >&5
  (eval $ac_compile) 2>&5
  ac_status=$?
  echo "$as_me:7916: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest.$ac_objext'
  { (eval echo "$as_me:7919: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:7922: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  BALL_U_QUAD_TYPE="unsigned long long int"

else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest.$ac_ext
		if test "${BALL_U_QUAD_TYPE}" = "" ; then
			echo "$as_me:7932: result: Could not identify an 64 bit unsigned type (long long)." >&5
echo "${ECHO_T}Could not identify an 64 bit unsigned type (long long)." >&6
			{ { echo "$as_me:7934: error: aborted" >&5
echo "$as_me: error: aborted" >&2;}
   { (exit 1); exit 1; }; }
		fi

		cat >>confdefs.h <<EOF
#define BALL_XDR_UINT64_TYPE ${BALL_U_QUAD_TYPE}
EOF

		echo "$as_me:7943: result: unsigned long long int" >&5
echo "${ECHO_T}unsigned long long int" >&6

	fi

					if test "${BALL_XDRREC_VOID_CHAR_INT}" = true ; then
		cat >>confdefs.h <<\EOF
#define BALL_XDRREC_CREATE_VOID_CHAR_INT 1
EOF

	fi
	if test "${BALL_XDRREC_CHAR_CHAR_INT}" = true ; then
		cat >>confdefs.h <<\EOF
#define BALL_XDRREC_CREATE_CHAR_CHAR_INT 1
EOF

	fi
	if test "${BALL_XDRREC_VOID}" = true ; then
		cat >>confdefs.h <<\EOF
#define BALL_XDRREC_CREATE_VOID 1
EOF

	fi

					echo "$as_me:7967: checking for XDR symbols in libc" >&5
echo $ECHO_N "checking for XDR symbols in libc... $ECHO_C" >&6
	cat >conftest.$ac_ext <<_ACEOF
#line 7970 "configure"
#include "confdefs.h"

			#include <rpc/types.h>
			#include <rpc/xdr.h>

int
main ()
{

			XDR xdrs;
			int i;
			xdr_int(&xdrs, &i);

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:7989: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:7992: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:7995: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:7998: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  XDR_IN_LIBC=true

else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
	if test "${XDR_IN_LIBC}" != true ; then
		echo "$as_me:8008: result: not found!" >&5
echo "${ECHO_T}not found!" >&6
		echo "$as_me:8010: checking for XDR symbols in libnsl" >&5
echo $ECHO_N "checking for XDR symbols in libnsl... $ECHO_C" >&6

		SAVE_LIBS=${LIBS}
		LIBS="-lnsl ${LIBS}"
		cat >conftest.$ac_ext <<_ACEOF
#line 8016 "configure"
#include "confdefs.h"

				#include <rpc/types.h>
				#include <rpc/xdr.h>

int
main ()
{

				XDR xdrs;
				int i;
				xdr_int(&xdrs, &i);

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:8035: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:8038: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:8041: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:8044: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  XDR_IN_LIBNSL=true

else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
		if test "${XDR_IN_LIBNSL}" = true ; then
			echo "$as_me:8054: result: yes" >&5
echo "${ECHO_T}yes" >&6
		else
			echo "$as_me:8057: result: no" >&5
echo "${ECHO_T}no" >&6
			echo "$as_me:8059: result: " >&5
echo "${ECHO_T}" >&6
			echo "$as_me:8061: result: Did not find XDR symbols in libc or libnsl." >&5
echo "${ECHO_T}Did not find XDR symbols in libc or libnsl." >&6
			{ { echo "$as_me:8063: error: Aborted." >&5
echo "$as_me: error: Aborted." >&2;}
   { (exit 1); exit 1; }; }
		fi
	else
								echo "$as_me:8068: result: yes" >&5
echo "${ECHO_T}yes" >&6
	fi
	cat >>confdefs.h <<\EOF
#define BALL_HAS_XDR true
EOF

	BALL_HAS_XDR=true

fi

if test "$INCLUDE_IEEEFP" = true ; then

for ac_header in ieeefp.h
do
ac_ac_Header=`echo "ac_cv_header_$ac_header" | $ac_tr_sh`
echo "$as_me:8084: checking for $ac_header" >&5
echo $ECHO_N "checking for $ac_header... $ECHO_C" >&6
if eval "test \"\${$ac_ac_Header+set}\" = set"; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 8090 "configure"
#include "confdefs.h"
#include <$ac_header>
_ACEOF
if { (eval echo "$as_me:8094: \"$ac_cpp conftest.$ac_ext\"") >&5
  (eval $ac_cpp conftest.$ac_ext) 2>conftest.er1
  ac_status=$?
  egrep -v '^ *\+' conftest.er1 >conftest.err
  rm -f conftest.er1
  cat conftest.err >&5
  echo "$as_me:8100: \$? = $ac_status" >&5
  (exit $ac_status); } >/dev/null; then
  if test -s conftest.err; then
    ac_cpp_err=$ac_cxx_preproc_warn_flag
  else
    ac_cpp_err=
  fi
else
  ac_cpp_err=yes
fi
if test -z "$ac_cpp_err"; then
  eval "$ac_ac_Header=yes"
else
  echo "$as_me: failed program was:" >&5
  cat conftest.$ac_ext >&5
  eval "$ac_ac_Header=no"
fi
rm -f conftest.err conftest.$ac_ext
fi
echo "$as_me:8119: result: `eval echo '${'$ac_ac_Header'}'`" >&5
echo "${ECHO_T}`eval echo '${'$ac_ac_Header'}'`" >&6
if test `eval echo '${'$ac_ac_Header'}'` = yes; then
  cat >>confdefs.h <<EOF
#define `echo "HAVE_$ac_header" | $ac_tr_cpp` 1
EOF

fi
done

	cat >>confdefs.h <<\EOF
#define INCLUDE_IEEEFP
EOF

fi

echo "$as_me:8135: checking for regex.h" >&5
echo $ECHO_N "checking for regex.h... $ECHO_C" >&6
if test "${ac_cv_header_regex_h+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 8141 "configure"
#include "confdefs.h"
#include <regex.h>
_ACEOF
if { (eval echo "$as_me:8145: \"$ac_cpp conftest.$ac_ext\"") >&5
  (eval $ac_cpp conftest.$ac_ext) 2>conftest.er1
  ac_status=$?
  egrep -v '^ *\+' conftest.er1 >conftest.err
  rm -f conftest.er1
  cat conftest.err >&5
  echo "$as_me:8151: \$? = $ac_status" >&5
  (exit $ac_status); } >/dev/null; then
  if test -s conftest.err; then
    ac_cpp_err=$ac_cxx_preproc_warn_flag
  else
    ac_cpp_err=
  fi
else
  ac_cpp_err=yes
fi
if test -z "$ac_cpp_err"; then
  ac_cv_header_regex_h=yes
else
  echo "$as_me: failed program was:" >&5
  cat conftest.$ac_ext >&5
  ac_cv_header_regex_h=no
fi
rm -f conftest.err conftest.$ac_ext
fi
echo "$as_me:8170: result: $ac_cv_header_regex_h" >&5
echo "${ECHO_T}$ac_cv_header_regex_h" >&6
if test $ac_cv_header_regex_h = yes; then
  HAS_REGEX_H=true
else
  HAS_REGEX_H=false
fi

if test "${HAS_REGEX_H}" = "false" ; then
	echo "$as_me:8179: checking for regexp.h" >&5
echo $ECHO_N "checking for regexp.h... $ECHO_C" >&6
if test "${ac_cv_header_regexp_h+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  cat >conftest.$ac_ext <<_ACEOF
#line 8185 "configure"
#include "confdefs.h"
#include <regexp.h>
_ACEOF
if { (eval echo "$as_me:8189: \"$ac_cpp conftest.$ac_ext\"") >&5
  (eval $ac_cpp conftest.$ac_ext) 2>conftest.er1
  ac_status=$?
  egrep -v '^ *\+' conftest.er1 >conftest.err
  rm -f conftest.er1
  cat conftest.err >&5
  echo "$as_me:8195: \$? = $ac_status" >&5
  (exit $ac_status); } >/dev/null; then
  if test -s conftest.err; then
    ac_cpp_err=$ac_cxx_preproc_warn_flag
  else
    ac_cpp_err=
  fi
else
  ac_cpp_err=yes
fi
if test -z "$ac_cpp_err"; then
  ac_cv_header_regexp_h=yes
else
  echo "$as_me: failed program was:" >&5
  cat conftest.$ac_ext >&5
  ac_cv_header_regexp_h=no
fi
rm -f conftest.err conftest.$ac_ext
fi
echo "$as_me:8214: result: $ac_cv_header_regexp_h" >&5
echo "${ECHO_T}$ac_cv_header_regexp_h" >&6
if test $ac_cv_header_regexp_h = yes; then
  HAS_REGEX_H=true
else
  HAS_REGEX_H=false
fi

	cat >>confdefs.h <<\EOF
#define BALL_USE_REGEXP_H 1
EOF

fi
if test "${HAS_REGEX_H}" = "false" ; then
	echo "$as_me:8228: result: " >&5
echo "${ECHO_T}" >&6
	echo "$as_me:8230: result: Regular expression headers regex.h not found!" >&5
echo "${ECHO_T}Regular expression headers regex.h not found!" >&6
	echo "$as_me:8232: result: If you do not have this header on your system," >&5
echo "${ECHO_T}If you do not have this header on your system," >&6
	echo "$as_me:8234: result: please install the GNU regexp package from" >&5
echo "${ECHO_T}please install the GNU regexp package from" >&6
	echo "$as_me:8236: result: " >&5
echo "${ECHO_T}" >&6
	echo "$as_me:8238: result:   ftp://ftp.gnu.org/gnu/regex/regex-0.12.tar.gz" >&5
echo "${ECHO_T}  ftp://ftp.gnu.org/gnu/regex/regex-0.12.tar.gz" >&6
	echo "$as_me:8240: result: " >&5
echo "${ECHO_T}" >&6
	{ { echo "$as_me:8242: error: Aborted." >&5
echo "$as_me: error: Aborted." >&2;}
   { (exit 1); exit 1; }; }
fi

if test "${USE_BALLVIEW}" = true ; then
	echo "$as_me:8248: checking for X" >&5
echo $ECHO_N "checking for X... $ECHO_C" >&6

# Check whether --with-x or --without-x was given.
if test "${with_x+set}" = set; then
  withval="$with_x"

fi;
# $have_x is `yes', `no', `disabled', or empty when we do not yet know.
if test "x$with_x" = xno; then
  # The user explicitly disabled X.
  have_x=disabled
else
  if test "x$x_includes" != xNONE && test "x$x_libraries" != xNONE; then
    # Both variables are already set.
    have_x=yes
  else
    if test "${ac_cv_have_x+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  # One or both of the vars are not set, and there is no cached value.
ac_x_includes=no ac_x_libraries=no
rm -fr conftest.dir
if mkdir conftest.dir; then
  cd conftest.dir
  # Make sure to not put "make" in the Imakefile rules, since we grep it out.
  cat >Imakefile <<'EOF'
acfindx:
	@echo 'ac_im_incroot="${INCROOT}"; ac_im_usrlibdir="${USRLIBDIR}"; ac_im_libdir="${LIBDIR}"'
EOF
  if (xmkmf) >/dev/null 2>/dev/null && test -f Makefile; then
    # GNU make sometimes prints "make[1]: Entering...", which would confuse us.
    eval `${MAKE-make} acfindx 2>/dev/null | grep -v make`
    # Open Windows xmkmf reportedly sets LIBDIR instead of USRLIBDIR.
    for ac_extension in a so sl; do
      if test ! -f $ac_im_usrlibdir/libX11.$ac_extension &&
         test -f $ac_im_libdir/libX11.$ac_extension; then
        ac_im_usrlibdir=$ac_im_libdir; break
      fi
    done
    # Screen out bogus values from the imake configuration.  They are
    # bogus both because they are the default anyway, and because
    # using them would break gcc on systems where it needs fixed includes.
    case $ac_im_incroot in
	/usr/include) ;;
	*) test -f "$ac_im_incroot/X11/Xos.h" && ac_x_includes=$ac_im_incroot;;
    esac
    case $ac_im_usrlibdir in
	/usr/lib | /lib) ;;
	*) test -d "$ac_im_usrlibdir" && ac_x_libraries=$ac_im_usrlibdir ;;
    esac
  fi
  cd ..
  rm -fr conftest.dir
fi

# Standard set of common directories for X headers.
# Check X11 before X11Rn because it is often a symlink to the current release.
ac_x_header_dirs='
/usr/X11/include
/usr/X11R6/include
/usr/X11R5/include
/usr/X11R4/include

/usr/include/X11
/usr/include/X11R6
/usr/include/X11R5
/usr/include/X11R4

/usr/local/X11/include
/usr/local/X11R6/include
/usr/local/X11R5/include
/usr/local/X11R4/include

/usr/local/include/X11
/usr/local/include/X11R6
/usr/local/include/X11R5
/usr/local/include/X11R4

/usr/X386/include
/usr/x386/include
/usr/XFree86/include/X11

/usr/include
/usr/local/include
/usr/unsupported/include
/usr/athena/include
/usr/local/x11r5/include
/usr/lpp/Xamples/include

/usr/openwin/include
/usr/openwin/share/include'

if test "$ac_x_includes" = no; then
  # Guess where to find include files, by looking for Intrinsic.h.
  # First, try using that file with no special directory specified.
  cat >conftest.$ac_ext <<_ACEOF
#line 8345 "configure"
#include "confdefs.h"
#include <X11/Intrinsic.h>
_ACEOF
if { (eval echo "$as_me:8349: \"$ac_cpp conftest.$ac_ext\"") >&5
  (eval $ac_cpp conftest.$ac_ext) 2>conftest.er1
  ac_status=$?
  egrep -v '^ *\+' conftest.er1 >conftest.err
  rm -f conftest.er1
  cat conftest.err >&5
  echo "$as_me:8355: \$? = $ac_status" >&5
  (exit $ac_status); } >/dev/null; then
  if test -s conftest.err; then
    ac_cpp_err=$ac_cxx_preproc_warn_flag
  else
    ac_cpp_err=
  fi
else
  ac_cpp_err=yes
fi
if test -z "$ac_cpp_err"; then
  # We can compile using X headers with no special include directory.
ac_x_includes=
else
  echo "$as_me: failed program was:" >&5
  cat conftest.$ac_ext >&5
  for ac_dir in $ac_x_header_dirs; do
  if test -r "$ac_dir/X11/Intrinsic.h"; then
    ac_x_includes=$ac_dir
    break
  fi
done
fi
rm -f conftest.err conftest.$ac_ext
fi # $ac_x_includes = no

if test "$ac_x_libraries" = no; then
  # Check for the libraries.
  # See if we find them without any special options.
  # Don't add to $LIBS permanently.
  ac_save_LIBS=$LIBS
  LIBS="-lXt $LIBS"
  cat >conftest.$ac_ext <<_ACEOF
#line 8388 "configure"
#include "confdefs.h"
#include <X11/Intrinsic.h>
int
main ()
{
XtMalloc (0)
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:8400: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:8403: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:8406: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:8409: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  LIBS=$ac_save_LIBS
# We can link X programs with no special library path.
ac_x_libraries=
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
LIBS=$ac_save_LIBS
for ac_dir in `echo "$ac_x_includes $ac_x_header_dirs" | sed s/include/lib/g`
do
  # Don't even attempt the hair of trying to link an X program!
  for ac_extension in a so sl; do
    if test -r $ac_dir/libXt.$ac_extension; then
      ac_x_libraries=$ac_dir
      break 2
    fi
  done
done
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
fi # $ac_x_libraries = no

if test "$ac_x_includes" = no || test "$ac_x_libraries" = no; then
  # Didn't find X anywhere.  Cache the known absence of X.
  ac_cv_have_x="have_x=no"
else
  # Record where we found X for the cache.
  ac_cv_have_x="have_x=yes \
	        ac_x_includes=$ac_x_includes ac_x_libraries=$ac_x_libraries"
fi
fi

  fi
  eval "$ac_cv_have_x"
fi # $with_x != no

if test "$have_x" != yes; then
  echo "$as_me:8447: result: $have_x" >&5
echo "${ECHO_T}$have_x" >&6
  no_x=yes
else
  # If each of the values was on the command line, it overrides each guess.
  test "x$x_includes" = xNONE && x_includes=$ac_x_includes
  test "x$x_libraries" = xNONE && x_libraries=$ac_x_libraries
  # Update the cache value to reflect the command line values.
  ac_cv_have_x="have_x=yes \
		ac_x_includes=$x_includes ac_x_libraries=$x_libraries"
  echo "$as_me:8457: result: libraries $x_libraries, headers $x_includes" >&5
echo "${ECHO_T}libraries $x_libraries, headers $x_includes" >&6
fi

	X11_INCPATH=${x_includes}
	X11_LIBPATH=${x_libraries}

	if test "${no_x}" = "yes" ; then
		USE_BALLVIEW=false
	fi

	if test "${USE_BALLVIEW}" = true ; then
		if test "${X11_LIBPATH}" = "/usr/lib" -o "${X11_LIBPATH}" = "" ; then
			X11_LIBPATH=""
			X11_LIBPATHOPT=""
		else
			X11_LIBPATHOPT="-L${X11_LIBPATH}"
		fi
	fi

	if test "${USE_BALLVIEW}" = true ; then
		if test "${BALLVIEW_PLATFORM}" = Mesa ; then
			echo "$as_me:8479: checking for Mesa includes" >&5
echo $ECHO_N "checking for Mesa includes... $ECHO_C" >&6

	_INCLUDES=

		if test "${MESA_INCLUDES}" != "" ; then
		_INCLUDES=${MESA_INCLUDES}
	fi

	if test "${_INCLUDES}" = "" ; then
		for i in /usr/include /opt/include ${OPENGL_INCPATH} ${X11_INCPATH} ; do
			if test -f "$i/GL/gl.h" -a "${_INCLUDES}" = ""; then
				_INCLUDES="$i"
			fi
		done
	fi

	if test "${_INCLUDES}" = "" ; then
		for i in /usr/*/include /opt/*/include ; do
			if test -f "$i/GL/gl.h" -a "${_INCLUDES}" = ""; then
				_INCLUDES="$i"
			fi
		done
	fi

	if test "${_INCLUDES}" = "" ; then
		for i in /opt/*/*/include /usr/*/*/include /usr/local/*/*/include ; do
			if test -f "$i/GL/gl.h" -a "${_INCLUDES}" = ""; then
				_INCLUDES="$i"
			fi
		done
	fi

	if test "${_INCLUDES}" = "" -a "${FIND}" != "-" ; then
		if test "${FIND_KNOWS_PATH}" = false ; then
			FIND_OPT="-name"
			_TMP_FIND_NAME="GL/gl.h"
			while test _`egrep / $_TMP_FIND_NAME`_ != __ ; do
				_TMP_FIND_NAME=`echo ${_TMP_FIND_NAME}|${CUT} -d/ -f2-`
			done

			FIND_ARG="\*${_TMP_FIND_NAME}\*"
		else
			FIND_OPT="-path"
			FIND_ARG="\*GL/gl.h\*"
		fi

		_TMP=`${FIND} /usr ${FIND_OPT} ${FIND_ARG} -print 2>/dev/null`
		for j in ${_TMP} ; do
			if test "${_INCLUDES}" = "" ; then
				_INCLUDES=`echo $j|${SED} "s/include\/.\*\$/include/"`
			fi
		done

		if test "${_INCLUDES}" = "" ; then
			_TMP=`${FIND} /opt ${FIND_OPT} ${FIND_ARG} -print 2>/dev/null`
			for j in ${_TMP} ; do
				if test "${_INCLUDES}" = "" ; then
					_INCLUDES=`echo $j|${SED} "s/include\/.\*\$/include/"`
				fi
			done
		fi

		if test "${_INCLUDES}" = "" -a "${OPENGL_INCPATH} ${X11_INCPATH}" != ""; then
			for i in ${OPENGL_INCPATH} ${X11_INCPATH} /dev/null ; do
				_TMP=`${FIND} $i ${FIND_OPT} ${FIND_ARG} -print 2>/dev/null`
				for j in ${_TMP} ; do
					if test "${_INCLUDES}" = "" ; then
						_INCLUDES=`echo $j|${SED} "s/include\/.\*\$/include/"`
					fi
				done
			done
		fi
	fi

	MESA_INCLUDES="${_INCLUDES}"

			if test "${MESA_INCLUDES}" = "" ; then
				echo "$as_me:8557: result: (not found!)" >&5
echo "${ECHO_T}(not found!)" >&6
				echo "$as_me:8559: result: " >&5
echo "${ECHO_T}" >&6
				echo "$as_me:8561: result: No Mesa headers found! Please specify the path to the directory" >&5
echo "${ECHO_T}No Mesa headers found! Please specify the path to the directory" >&6
				echo "$as_me:8563: result: containing the Mesa headers using --with-opengl-incl=DIR." >&5
echo "${ECHO_T}containing the Mesa headers using --with-opengl-incl=DIR." >&6
				echo "$as_me:8565: result: Mesa can be obtained from www.mesa3d.org." >&5
echo "${ECHO_T}Mesa can be obtained from www.mesa3d.org." >&6
				echo "$as_me:8567: result: Disabling support for visualization component BALLVIEW" >&5
echo "${ECHO_T}Disabling support for visualization component BALLVIEW" >&6
				USE_BALLVIEW=false
			else
				echo "$as_me:8571: result: ${MESA_INCLUDES}" >&5
echo "${ECHO_T}${MESA_INCLUDES}" >&6
			fi

			if test "${USE_BALLVIEW}" = true ; then
				echo "$as_me:8576: checking for Mesa library" >&5
echo $ECHO_N "checking for Mesa library... $ECHO_C" >&6

	_LIBS=

		if test "${MESA_LIBS}" != "" ; then
		_LIBS=${MESA_LIBS}
	fi

	if test "${_LIBS}" = "" ; then
		for i in ${OPENGL_LIBPATH} ${X11_LIBPATH} /usr/lib /opt/lib ; do
			for j in $i/libMesaGL.* ; do
				if test -f "$j" -a "${_LIBS}" = ""; then
					_LIBS="$i"
				fi
			done
		done
	fi

	if test "${_LIBS}" = "" ; then
		for i in /usr/*/lib /opt/*/lib ; do
			for j in $i/libMesaGL.* ; do
				if test -f "$j" -a "${_LIBS}" = ""; then
					_LIBS="$i"
				fi
			done
		done
	fi

	if test "${_LIBS}" = "" ; then
		for i in /opt/*/*/lib /usr/*/*/lib /usr/local/*/*/lib ; do
			for j in $i/libMesaGL.* ; do
				if test -f "$j" -a "${_LIBS}" = ""; then
					_LIBS="$i"
				fi
			done
		done
	fi

	if test "${_LIBS}" = "" -a "${FIND}" != "-" ; then
		if test "${_LIBS}" = "" -a "${OPENGL_LIBPATH} ${X11_LIBPATH}" != ""; then
			for i in ${OPENGL_LIBPATH} ${X11_LIBPATH} /dev/null; do
				if test "${_LIBS}" = "" ; then
					_TMP=`${FIND} $i -name "libMesaGL*" -print 2>/dev/null`
					for j in ${_TMP} ; do
						if test "${_LIBS}" = "" ; then
							_LIBS=`echo $j|${SED} "s/\/libMesaGL.*/\//"`
						fi
					done
				fi
			done
		fi

		if test "${_LIBS}" = "" ; then
			_TMP=`${FIND} /opt -name "libMesaGL*" -print 2>/dev/null`
			for j in ${_TMP} ; do
				if test "${_LIBS}" = "" ; then
					_LIBS=`echo $j|${SED} "s/\/libMesaGL.*/\//"`
				fi
			done
		fi

		if test "${_LIBS}" = "" ; then
			_TMP=`${FIND} /usr -name "libMesaGL*" -print 2>/dev/null`
			for j in ${_TMP} ; do
				if test "${_LIBS}" = "" ; then
					_LIBS=`echo $j|${SED} "s/\/libMesaGL.*/\//"`
				fi
			done
		fi

	fi

	MESA_LIBS="${_LIBS}"

				if test "${MESA_LIBS}" = "" ; then

	_LIBS=

		if test "${MESA_LIBS}" != "" ; then
		_LIBS=${MESA_LIBS}
	fi

	if test "${_LIBS}" = "" ; then
		for i in ${OPENGL_LIBPATH} ${X11_LIBPATH} /usr/lib /opt/lib ; do
			for j in $i/libGL.* ; do
				if test -f "$j" -a "${_LIBS}" = ""; then
					_LIBS="$i"
				fi
			done
		done
	fi

	if test "${_LIBS}" = "" ; then
		for i in /usr/*/lib /opt/*/lib ; do
			for j in $i/libGL.* ; do
				if test -f "$j" -a "${_LIBS}" = ""; then
					_LIBS="$i"
				fi
			done
		done
	fi

	if test "${_LIBS}" = "" ; then
		for i in /opt/*/*/lib /usr/*/*/lib /usr/local/*/*/lib ; do
			for j in $i/libGL.* ; do
				if test -f "$j" -a "${_LIBS}" = ""; then
					_LIBS="$i"
				fi
			done
		done
	fi

	if test "${_LIBS}" = "" -a "${FIND}" != "-" ; then
		if test "${_LIBS}" = "" -a "${OPENGL_LIBPATH} ${X11_LIBPATH}" != ""; then
			for i in ${OPENGL_LIBPATH} ${X11_LIBPATH} /dev/null; do
				if test "${_LIBS}" = "" ; then
					_TMP=`${FIND} $i -name "libGL*" -print 2>/dev/null`
					for j in ${_TMP} ; do
						if test "${_LIBS}" = "" ; then
							_LIBS=`echo $j|${SED} "s/\/libGL.*/\//"`
						fi
					done
				fi
			done
		fi

		if test "${_LIBS}" = "" ; then
			_TMP=`${FIND} /opt -name "libGL*" -print 2>/dev/null`
			for j in ${_TMP} ; do
				if test "${_LIBS}" = "" ; then
					_LIBS=`echo $j|${SED} "s/\/libGL.*/\//"`
				fi
			done
		fi

		if test "${_LIBS}" = "" ; then
			_TMP=`${FIND} /usr -name "libGL*" -print 2>/dev/null`
			for j in ${_TMP} ; do
				if test "${_LIBS}" = "" ; then
					_LIBS=`echo $j|${SED} "s/\/libGL.*/\//"`
				fi
			done
		fi

	fi

	MESA_LIBS="${_LIBS}"

				fi
				if test "${MESA_LIBS}" = "" ; then
					echo "$as_me:8727: result: (not found!)" >&5
echo "${ECHO_T}(not found!)" >&6
					echo "$as_me:8729: result: " >&5
echo "${ECHO_T}" >&6
					echo "$as_me:8731: result: No Mesa library libMesaGL or libGL found! Please specify the path" >&5
echo "${ECHO_T}No Mesa library libMesaGL or libGL found! Please specify the path" >&6
					echo "$as_me:8733: result: to the directory containing the library using the --with-opengl-libs=DIR." >&5
echo "${ECHO_T}to the directory containing the library using the --with-opengl-libs=DIR." >&6
					echo "$as_me:8735: result: Mesa can be obtained from www.mesa3d.org." >&5
echo "${ECHO_T}Mesa can be obtained from www.mesa3d.org." >&6
					echo "$as_me:8737: result: Disabling support for visualization component BALLVIEW" >&5
echo "${ECHO_T}Disabling support for visualization component BALLVIEW" >&6
					USE_BALLVIEW=false
				else
					echo "$as_me:8741: result: (${MESA_LIBS})" >&5
echo "${ECHO_T}(${MESA_LIBS})" >&6
				fi
			fi

									if test "${MESA_INCLUDES}" != /usr/include -a "${MESA_INCLUDES}" != "" ; then
				BALLVIEW_INCLUDES="${BALLVIEW_INCLUDES} -I${MESA_INCLUDES}"
			fi
		fi

		if test ${BALLVIEW_PLATFORM} = OpenGL ; then
			echo "$as_me:8752: checking for OpenGL includes" >&5
echo $ECHO_N "checking for OpenGL includes... $ECHO_C" >&6

	_INCLUDES=

		if test "${OPENGL_INCPATH}" != "" ; then
		_INCLUDES=${OPENGL_INCPATH}
	fi

	if test "${_INCLUDES}" = "" ; then
		for i in /usr/include /opt/include  ; do
			if test -f "$i/GL/gl.h" -a "${_INCLUDES}" = ""; then
				_INCLUDES="$i"
			fi
		done
	fi

	if test "${_INCLUDES}" = "" ; then
		for i in /usr/*/include /opt/*/include ; do
			if test -f "$i/GL/gl.h" -a "${_INCLUDES}" = ""; then
				_INCLUDES="$i"
			fi
		done
	fi

	if test "${_INCLUDES}" = "" ; then
		for i in /opt/*/*/include /usr/*/*/include /usr/local/*/*/include ; do
			if test -f "$i/GL/gl.h" -a "${_INCLUDES}" = ""; then
				_INCLUDES="$i"
			fi
		done
	fi

	if test "${_INCLUDES}" = "" -a "${FIND}" != "-" ; then
		if test "${FIND_KNOWS_PATH}" = false ; then
			FIND_OPT="-name"
			_TMP_FIND_NAME="GL/gl.h"
			while test _`egrep / $_TMP_FIND_NAME`_ != __ ; do
				_TMP_FIND_NAME=`echo ${_TMP_FIND_NAME}|${CUT} -d/ -f2-`
			done

			FIND_ARG="\*${_TMP_FIND_NAME}\*"
		else
			FIND_OPT="-path"
			FIND_ARG="\*GL/gl.h\*"
		fi

		_TMP=`${FIND} /usr ${FIND_OPT} ${FIND_ARG} -print 2>/dev/null`
		for j in ${_TMP} ; do
			if test "${_INCLUDES}" = "" ; then
				_INCLUDES=`echo $j|${SED} "s/include\/.\*\$/include/"`
			fi
		done

		if test "${_INCLUDES}" = "" ; then
			_TMP=`${FIND} /opt ${FIND_OPT} ${FIND_ARG} -print 2>/dev/null`
			for j in ${_TMP} ; do
				if test "${_INCLUDES}" = "" ; then
					_INCLUDES=`echo $j|${SED} "s/include\/.\*\$/include/"`
				fi
			done
		fi

		if test "${_INCLUDES}" = "" -a "" != ""; then
			for i in  /dev/null ; do
				_TMP=`${FIND} $i ${FIND_OPT} ${FIND_ARG} -print 2>/dev/null`
				for j in ${_TMP} ; do
					if test "${_INCLUDES}" = "" ; then
						_INCLUDES=`echo $j|${SED} "s/include\/.\*\$/include/"`
					fi
				done
			done
		fi
	fi

	OPENGL_INCPATH="${_INCLUDES}"

			if test "${OPENGL_INCPATH}" = "" ; then
				echo "$as_me:8830: result: (not found!)" >&5
echo "${ECHO_T}(not found!)" >&6
				echo "$as_me:8832: result: " >&5
echo "${ECHO_T}" >&6
				echo "$as_me:8834: result: no OpenGL headers found! Please use the option --with-opengl-incl=DIR" >&5
echo "${ECHO_T}no OpenGL headers found! Please use the option --with-opengl-incl=DIR" >&6
				echo "$as_me:8836: result: of configure to specify the correct path to these headers." >&5
echo "${ECHO_T}of configure to specify the correct path to these headers." >&6
				{ { echo "$as_me:8838: error: aborted" >&5
echo "$as_me: error: aborted" >&2;}
   { (exit 1); exit 1; }; }
			else
				echo "$as_me:8842: result: (${OPENGL_INCPATH})" >&5
echo "${ECHO_T}(${OPENGL_INCPATH})" >&6
			fi

			echo "$as_me:8846: checking for OpenGL library" >&5
echo $ECHO_N "checking for OpenGL library... $ECHO_C" >&6

	_LIBS=

		if test "${OPENGL_LIBPATH}" != "" ; then
		_LIBS=${OPENGL_LIBPATH}
	fi

	if test "${_LIBS}" = "" ; then
		for i in  /usr/lib /opt/lib ; do
			for j in $i/libGL.* ; do
				if test -f "$j" -a "${_LIBS}" = ""; then
					_LIBS="$i"
				fi
			done
		done
	fi

	if test "${_LIBS}" = "" ; then
		for i in /usr/*/lib /opt/*/lib ; do
			for j in $i/libGL.* ; do
				if test -f "$j" -a "${_LIBS}" = ""; then
					_LIBS="$i"
				fi
			done
		done
	fi

	if test "${_LIBS}" = "" ; then
		for i in /opt/*/*/lib /usr/*/*/lib /usr/local/*/*/lib ; do
			for j in $i/libGL.* ; do
				if test -f "$j" -a "${_LIBS}" = ""; then
					_LIBS="$i"
				fi
			done
		done
	fi

	if test "${_LIBS}" = "" -a "${FIND}" != "-" ; then
		if test "${_LIBS}" = "" -a "" != ""; then
			for i in  /dev/null; do
				if test "${_LIBS}" = "" ; then
					_TMP=`${FIND} $i -name "libGL*" -print 2>/dev/null`
					for j in ${_TMP} ; do
						if test "${_LIBS}" = "" ; then
							_LIBS=`echo $j|${SED} "s/\/libGL.*/\//"`
						fi
					done
				fi
			done
		fi

		if test "${_LIBS}" = "" ; then
			_TMP=`${FIND} /opt -name "libGL*" -print 2>/dev/null`
			for j in ${_TMP} ; do
				if test "${_LIBS}" = "" ; then
					_LIBS=`echo $j|${SED} "s/\/libGL.*/\//"`
				fi
			done
		fi

		if test "${_LIBS}" = "" ; then
			_TMP=`${FIND} /usr -name "libGL*" -print 2>/dev/null`
			for j in ${_TMP} ; do
				if test "${_LIBS}" = "" ; then
					_LIBS=`echo $j|${SED} "s/\/libGL.*/\//"`
				fi
			done
		fi

	fi

	OPENGL_LIBPATH="${_LIBS}"

			if test "${OPENGL_LIBPATH}" = "" ; then
				echo "$as_me:8922: result: (not found!)" >&5
echo "${ECHO_T}(not found!)" >&6
				echo "$as_me:8924: result: " >&5
echo "${ECHO_T}" >&6
				echo "$as_me:8926: result: no OpenGL lib found! Please use the option --with-opengl-libs=DIR" >&5
echo "${ECHO_T}no OpenGL lib found! Please use the option --with-opengl-libs=DIR" >&6
				echo "$as_me:8928: result: of configure to specify the correct path to these libraries." >&5
echo "${ECHO_T}of configure to specify the correct path to these libraries." >&6
				{ { echo "$as_me:8930: error: aborted" >&5
echo "$as_me: error: aborted" >&2;}
   { (exit 1); exit 1; }; }
			else
				echo "$as_me:8934: result: (${OPENGL_LIBPATH})" >&5
echo "${ECHO_T}(${OPENGL_LIBPATH})" >&6
			fi

			if test "${OPENGL_INCPATH}" != /usr/include && test "${OPENGL_INCPATH}" != "" ; then
				BALLVIEW_INCLUDES="${BALLVIEW_INCLUDES} -I${OPENGL_INCPATH}"
			fi
		fi

		if test "${USE_BALLVIEW}" = true ; then
			echo "$as_me:8944: checking for QT headers" >&5
echo $ECHO_N "checking for QT headers... $ECHO_C" >&6
			if test "${QTDIR}" != "" ; then

	_INCLUDES=

		if test "${QT_INCPATH}" != "" ; then
		_INCLUDES=${QT_INCPATH}
	fi

	if test "${_INCLUDES}" = "" ; then
		for i in /usr/include /opt/include ${QTDIR}/include ${BALL_PATH}/contrib/qt/include ; do
			if test -f "$i/qgl.h" -a "${_INCLUDES}" = ""; then
				_INCLUDES="$i"
			fi
		done
	fi

	if test "${_INCLUDES}" = "" ; then
		for i in /usr/*/include /opt/*/include ; do
			if test -f "$i/qgl.h" -a "${_INCLUDES}" = ""; then
				_INCLUDES="$i"
			fi
		done
	fi

	if test "${_INCLUDES}" = "" ; then
		for i in /opt/*/*/include /usr/*/*/include /usr/local/*/*/include ; do
			if test -f "$i/qgl.h" -a "${_INCLUDES}" = ""; then
				_INCLUDES="$i"
			fi
		done
	fi

	if test "${_INCLUDES}" = "" -a "${FIND}" != "-" ; then
		if test "${FIND_KNOWS_PATH}" = false ; then
			FIND_OPT="-name"
			_TMP_FIND_NAME="qgl.h"
			while test _`egrep / $_TMP_FIND_NAME`_ != __ ; do
				_TMP_FIND_NAME=`echo ${_TMP_FIND_NAME}|${CUT} -d/ -f2-`
			done

			FIND_ARG="\*${_TMP_FIND_NAME}\*"
		else
			FIND_OPT="-path"
			FIND_ARG="\*qgl.h\*"
		fi

		_TMP=`${FIND} /usr ${FIND_OPT} ${FIND_ARG} -print 2>/dev/null`
		for j in ${_TMP} ; do
			if test "${_INCLUDES}" = "" ; then
				_INCLUDES=`echo $j|${SED} "s/include\/.\*\$/include/"`
			fi
		done

		if test "${_INCLUDES}" = "" ; then
			_TMP=`${FIND} /opt ${FIND_OPT} ${FIND_ARG} -print 2>/dev/null`
			for j in ${_TMP} ; do
				if test "${_INCLUDES}" = "" ; then
					_INCLUDES=`echo $j|${SED} "s/include\/.\*\$/include/"`
				fi
			done
		fi

		if test "${_INCLUDES}" = "" -a "${QTDIR}/include ${BALL_PATH}/contrib/qt/include" != ""; then
			for i in ${QTDIR}/include ${BALL_PATH}/contrib/qt/include /dev/null ; do
				_TMP=`${FIND} $i ${FIND_OPT} ${FIND_ARG} -print 2>/dev/null`
				for j in ${_TMP} ; do
					if test "${_INCLUDES}" = "" ; then
						_INCLUDES=`echo $j|${SED} "s/include\/.\*\$/include/"`
					fi
				done
			done
		fi
	fi

	QT_INCPATH="${_INCLUDES}"

			else

	_INCLUDES=

		if test "${QT_INCPATH}" != "" ; then
		_INCLUDES=${QT_INCPATH}
	fi

	if test "${_INCLUDES}" = "" ; then
		for i in /usr/include /opt/include ${BALL_PATH}/contrib/qt/include ; do
			if test -f "$i/qgl.h" -a "${_INCLUDES}" = ""; then
				_INCLUDES="$i"
			fi
		done
	fi

	if test "${_INCLUDES}" = "" ; then
		for i in /usr/*/include /opt/*/include ; do
			if test -f "$i/qgl.h" -a "${_INCLUDES}" = ""; then
				_INCLUDES="$i"
			fi
		done
	fi

	if test "${_INCLUDES}" = "" ; then
		for i in /opt/*/*/include /usr/*/*/include /usr/local/*/*/include ; do
			if test -f "$i/qgl.h" -a "${_INCLUDES}" = ""; then
				_INCLUDES="$i"
			fi
		done
	fi

	if test "${_INCLUDES}" = "" -a "${FIND}" != "-" ; then
		if test "${FIND_KNOWS_PATH}" = false ; then
			FIND_OPT="-name"
			_TMP_FIND_NAME="qgl.h"
			while test _`egrep / $_TMP_FIND_NAME`_ != __ ; do
				_TMP_FIND_NAME=`echo ${_TMP_FIND_NAME}|${CUT} -d/ -f2-`
			done

			FIND_ARG="\*${_TMP_FIND_NAME}\*"
		else
			FIND_OPT="-path"
			FIND_ARG="\*qgl.h\*"
		fi

		_TMP=`${FIND} /usr ${FIND_OPT} ${FIND_ARG} -print 2>/dev/null`
		for j in ${_TMP} ; do
			if test "${_INCLUDES}" = "" ; then
				_INCLUDES=`echo $j|${SED} "s/include\/.\*\$/include/"`
			fi
		done

		if test "${_INCLUDES}" = "" ; then
			_TMP=`${FIND} /opt ${FIND_OPT} ${FIND_ARG} -print 2>/dev/null`
			for j in ${_TMP} ; do
				if test "${_INCLUDES}" = "" ; then
					_INCLUDES=`echo $j|${SED} "s/include\/.\*\$/include/"`
				fi
			done
		fi

		if test "${_INCLUDES}" = "" -a "${BALL_PATH}/contrib/qt/include" != ""; then
			for i in ${BALL_PATH}/contrib/qt/include /dev/null ; do
				_TMP=`${FIND} $i ${FIND_OPT} ${FIND_ARG} -print 2>/dev/null`
				for j in ${_TMP} ; do
					if test "${_INCLUDES}" = "" ; then
						_INCLUDES=`echo $j|${SED} "s/include\/.\*\$/include/"`
					fi
				done
			done
		fi
	fi

	QT_INCPATH="${_INCLUDES}"

			fi

			if test "${QT_INCPATH}" = "" ; then
				echo "$as_me:9101: result: (not found!)" >&5
echo "${ECHO_T}(not found!)" >&6
				echo "$as_me:9103: result: " >&5
echo "${ECHO_T}" >&6
				echo "$as_me:9105: result: No QT header files found! Please specify the path to the QT headers" >&5
echo "${ECHO_T}No QT header files found! Please specify the path to the QT headers" >&6
				echo "$as_me:9107: result: by passing the option --with-qt-incl=DIR to configure." >&5
echo "${ECHO_T}by passing the option --with-qt-incl=DIR to configure." >&6
				echo "$as_me:9109: result: You may also set the environment variable QTDIR to the correct" >&5
echo "${ECHO_T}You may also set the environment variable QTDIR to the correct" >&6
				echo "$as_me:9111: result: path - configure will recognize this" >&5
echo "${ECHO_T}path - configure will recognize this" >&6
				echo "$as_me:9113: result: The QT package can be found under the following URL:" >&5
echo "${ECHO_T}The QT package can be found under the following URL:" >&6
				echo "$as_me:9115: result: http://www.troll.no/qt" >&5
echo "${ECHO_T}http://www.troll.no/qt" >&6
				{ { echo "$as_me:9117: error: Building of the visualization component BALLVIEW was disabled!" >&5
echo "$as_me: error: Building of the visualization component BALLVIEW was disabled!" >&2;}
   { (exit 1); exit 1; }; }
			else
				echo "$as_me:9121: result: (${QT_INCPATH})" >&5
echo "${ECHO_T}(${QT_INCPATH})" >&6
			fi

			echo "$as_me:9125: checking for libqt${QT_MT_SUFFIX}" >&5
echo $ECHO_N "checking for libqt${QT_MT_SUFFIX}... $ECHO_C" >&6
			if test "${QTDIR}" != "" ; then

	_LIBS=

		if test "${QT_LIBPATH}" != "" ; then
		_LIBS=${QT_LIBPATH}
	fi

	if test "${_LIBS}" = "" ; then
		for i in ${QTDIR}/lib ${QTDIR}/lib/${BINFMT} ${BALL_PATH}/contrib/qt/include /usr/lib /opt/lib ; do
			for j in $i/libqt${QT_MT_SUFFIX}.* ; do
				if test -f "$j" -a "${_LIBS}" = ""; then
					_LIBS="$i"
				fi
			done
		done
	fi

	if test "${_LIBS}" = "" ; then
		for i in /usr/*/lib /opt/*/lib ; do
			for j in $i/libqt${QT_MT_SUFFIX}.* ; do
				if test -f "$j" -a "${_LIBS}" = ""; then
					_LIBS="$i"
				fi
			done
		done
	fi

	if test "${_LIBS}" = "" ; then
		for i in /opt/*/*/lib /usr/*/*/lib /usr/local/*/*/lib ; do
			for j in $i/libqt${QT_MT_SUFFIX}.* ; do
				if test -f "$j" -a "${_LIBS}" = ""; then
					_LIBS="$i"
				fi
			done
		done
	fi

	if test "${_LIBS}" = "" -a "${FIND}" != "-" ; then
		if test "${_LIBS}" = "" -a "${QTDIR}/lib ${QTDIR}/lib/${BINFMT} ${BALL_PATH}/contrib/qt/include" != ""; then
			for i in ${QTDIR}/lib ${QTDIR}/lib/${BINFMT} ${BALL_PATH}/contrib/qt/include /dev/null; do
				if test "${_LIBS}" = "" ; then
					_TMP=`${FIND} $i -name "libqt${QT_MT_SUFFIX}*" -print 2>/dev/null`
					for j in ${_TMP} ; do
						if test "${_LIBS}" = "" ; then
							_LIBS=`echo $j|${SED} "s/\/libqt${QT_MT_SUFFIX}.*/\//"`
						fi
					done
				fi
			done
		fi

		if test "${_LIBS}" = "" ; then
			_TMP=`${FIND} /opt -name "libqt${QT_MT_SUFFIX}*" -print 2>/dev/null`
			for j in ${_TMP} ; do
				if test "${_LIBS}" = "" ; then
					_LIBS=`echo $j|${SED} "s/\/libqt${QT_MT_SUFFIX}.*/\//"`
				fi
			done
		fi

		if test "${_LIBS}" = "" ; then
			_TMP=`${FIND} /usr -name "libqt${QT_MT_SUFFIX}*" -print 2>/dev/null`
			for j in ${_TMP} ; do
				if test "${_LIBS}" = "" ; then
					_LIBS=`echo $j|${SED} "s/\/libqt${QT_MT_SUFFIX}.*/\//"`
				fi
			done
		fi

	fi

	QT_LIBPATH="${_LIBS}"

			else

	_LIBS=

		if test "${QT_LIBPATH}" != "" ; then
		_LIBS=${QT_LIBPATH}
	fi

	if test "${_LIBS}" = "" ; then
		for i in ${BALL_PATH}/contrib/qt/lib ${BALL_PATH}/contrib/qt/lib/${BINFMT} /usr/lib /opt/lib ; do
			for j in $i/libqt${QT_MT_SUFFIX}.* ; do
				if test -f "$j" -a "${_LIBS}" = ""; then
					_LIBS="$i"
				fi
			done
		done
	fi

	if test "${_LIBS}" = "" ; then
		for i in /usr/*/lib /opt/*/lib ; do
			for j in $i/libqt${QT_MT_SUFFIX}.* ; do
				if test -f "$j" -a "${_LIBS}" = ""; then
					_LIBS="$i"
				fi
			done
		done
	fi

	if test "${_LIBS}" = "" ; then
		for i in /opt/*/*/lib /usr/*/*/lib /usr/local/*/*/lib ; do
			for j in $i/libqt${QT_MT_SUFFIX}.* ; do
				if test -f "$j" -a "${_LIBS}" = ""; then
					_LIBS="$i"
				fi
			done
		done
	fi

	if test "${_LIBS}" = "" -a "${FIND}" != "-" ; then
		if test "${_LIBS}" = "" -a "${BALL_PATH}/contrib/qt/lib ${BALL_PATH}/contrib/qt/lib/${BINFMT}" != ""; then
			for i in ${BALL_PATH}/contrib/qt/lib ${BALL_PATH}/contrib/qt/lib/${BINFMT} /dev/null; do
				if test "${_LIBS}" = "" ; then
					_TMP=`${FIND} $i -name "libqt${QT_MT_SUFFIX}*" -print 2>/dev/null`
					for j in ${_TMP} ; do
						if test "${_LIBS}" = "" ; then
							_LIBS=`echo $j|${SED} "s/\/libqt${QT_MT_SUFFIX}.*/\//"`
						fi
					done
				fi
			done
		fi

		if test "${_LIBS}" = "" ; then
			_TMP=`${FIND} /opt -name "libqt${QT_MT_SUFFIX}*" -print 2>/dev/null`
			for j in ${_TMP} ; do
				if test "${_LIBS}" = "" ; then
					_LIBS=`echo $j|${SED} "s/\/libqt${QT_MT_SUFFIX}.*/\//"`
				fi
			done
		fi

		if test "${_LIBS}" = "" ; then
			_TMP=`${FIND} /usr -name "libqt${QT_MT_SUFFIX}*" -print 2>/dev/null`
			for j in ${_TMP} ; do
				if test "${_LIBS}" = "" ; then
					_LIBS=`echo $j|${SED} "s/\/libqt${QT_MT_SUFFIX}.*/\//"`
				fi
			done
		fi

	fi

	QT_LIBPATH="${_LIBS}"

			fi

			if test "${QT_LIBPATH}" = "" ; then
				echo "$as_me:9278: result: (not found!)" >&5
echo "${ECHO_T}(not found!)" >&6
				echo "$as_me:9280: result: " >&5
echo "${ECHO_T}" >&6
				echo "$as_me:9282: result: The QT library could not be found. Please specify the path to libqt" >&5
echo "${ECHO_T}The QT library could not be found. Please specify the path to libqt" >&6
				echo "$as_me:9284: result: by passing the option --with-qt-libs=DIR to configure." >&5
echo "${ECHO_T}by passing the option --with-qt-libs=DIR to configure." >&6
				echo "$as_me:9286: result: You may also set the environment variable QTDIR to the correct" >&5
echo "${ECHO_T}You may also set the environment variable QTDIR to the correct" >&6
				echo "$as_me:9288: result: path - configure will recognize this, too." >&5
echo "${ECHO_T}path - configure will recognize this, too." >&6
				echo "$as_me:9290: result: If the QT library was built with thread support enabled (liqt-mt" >&5
echo "${ECHO_T}If the QT library was built with thread support enabled (liqt-mt" >&6
				echo "$as_me:9292: result: instead of libqt), please specify the option --with-threadsafe-qt." >&5
echo "${ECHO_T}instead of libqt), please specify the option --with-threadsafe-qt." >&6
				echo "$as_me:9294: result: The QT package can be found under the following URL:" >&5
echo "${ECHO_T}The QT package can be found under the following URL:" >&6
				echo "$as_me:9296: result: http://www.troll.no/qt" >&5
echo "${ECHO_T}http://www.troll.no/qt" >&6
				echo "$as_me:9298: result: " >&5
echo "${ECHO_T}" >&6
				{ { echo "$as_me:9300: error: Aborted." >&5
echo "$as_me: error: Aborted." >&2;}
   { (exit 1); exit 1; }; }
			else
				echo "$as_me:9304: result: (${QT_LIBPATH})" >&5
echo "${ECHO_T}(${QT_LIBPATH})" >&6
			fi

			QGL_LIBPATH=${QT_LIBPATH}
			echo "$as_me:9309: checking for libqgl" >&5
echo $ECHO_N "checking for libqgl... $ECHO_C" >&6
			if test "${QTDIR}" != "" ; then

	_LIBS=

		if test "${QGL_LIBPATH}" != "" ; then
		_LIBS=${QGL_LIBPATH}
	fi

	if test "${_LIBS}" = "" ; then
		for i in ${QTDIR}/lib ${QTDIR}/lib/${BINFMT} ${BALL_PATH}/contrib/qt/include /usr/lib /opt/lib ; do
			for j in $i/libqgl.* ; do
				if test -f "$j" -a "${_LIBS}" = ""; then
					_LIBS="$i"
				fi
			done
		done
	fi

	if test "${_LIBS}" = "" ; then
		for i in /usr/*/lib /opt/*/lib ; do
			for j in $i/libqgl.* ; do
				if test -f "$j" -a "${_LIBS}" = ""; then
					_LIBS="$i"
				fi
			done
		done
	fi

	if test "${_LIBS}" = "" ; then
		for i in /opt/*/*/lib /usr/*/*/lib /usr/local/*/*/lib ; do
			for j in $i/libqgl.* ; do
				if test -f "$j" -a "${_LIBS}" = ""; then
					_LIBS="$i"
				fi
			done
		done
	fi

	if test "${_LIBS}" = "" -a "${FIND}" != "-" ; then
		if test "${_LIBS}" = "" -a "${QTDIR}/lib ${QTDIR}/lib/${BINFMT} ${BALL_PATH}/contrib/qt/include" != ""; then
			for i in ${QTDIR}/lib ${QTDIR}/lib/${BINFMT} ${BALL_PATH}/contrib/qt/include /dev/null; do
				if test "${_LIBS}" = "" ; then
					_TMP=`${FIND} $i -name "libqgl*" -print 2>/dev/null`
					for j in ${_TMP} ; do
						if test "${_LIBS}" = "" ; then
							_LIBS=`echo $j|${SED} "s/\/libqgl.*/\//"`
						fi
					done
				fi
			done
		fi

		if test "${_LIBS}" = "" ; then
			_TMP=`${FIND} /opt -name "libqgl*" -print 2>/dev/null`
			for j in ${_TMP} ; do
				if test "${_LIBS}" = "" ; then
					_LIBS=`echo $j|${SED} "s/\/libqgl.*/\//"`
				fi
			done
		fi

		if test "${_LIBS}" = "" ; then
			_TMP=`${FIND} /usr -name "libqgl*" -print 2>/dev/null`
			for j in ${_TMP} ; do
				if test "${_LIBS}" = "" ; then
					_LIBS=`echo $j|${SED} "s/\/libqgl.*/\//"`
				fi
			done
		fi

	fi

	QGL_LIBPATH="${_LIBS}"

			else

	_LIBS=

		if test "${QGL_LIBPATH}" != "" ; then
		_LIBS=${QGL_LIBPATH}
	fi

	if test "${_LIBS}" = "" ; then
		for i in ${BALL_PATH}/contrib/qt/lib ${BALL_PATH}/contrib/qt/lib/${BINFMT} /usr/lib /opt/lib ; do
			for j in $i/libqgl.* ; do
				if test -f "$j" -a "${_LIBS}" = ""; then
					_LIBS="$i"
				fi
			done
		done
	fi

	if test "${_LIBS}" = "" ; then
		for i in /usr/*/lib /opt/*/lib ; do
			for j in $i/libqgl.* ; do
				if test -f "$j" -a "${_LIBS}" = ""; then
					_LIBS="$i"
				fi
			done
		done
	fi

	if test "${_LIBS}" = "" ; then
		for i in /opt/*/*/lib /usr/*/*/lib /usr/local/*/*/lib ; do
			for j in $i/libqgl.* ; do
				if test -f "$j" -a "${_LIBS}" = ""; then
					_LIBS="$i"
				fi
			done
		done
	fi

	if test "${_LIBS}" = "" -a "${FIND}" != "-" ; then
		if test "${_LIBS}" = "" -a "${BALL_PATH}/contrib/qt/lib ${BALL_PATH}/contrib/qt/lib/${BINFMT}" != ""; then
			for i in ${BALL_PATH}/contrib/qt/lib ${BALL_PATH}/contrib/qt/lib/${BINFMT} /dev/null; do
				if test "${_LIBS}" = "" ; then
					_TMP=`${FIND} $i -name "libqgl*" -print 2>/dev/null`
					for j in ${_TMP} ; do
						if test "${_LIBS}" = "" ; then
							_LIBS=`echo $j|${SED} "s/\/libqgl.*/\//"`
						fi
					done
				fi
			done
		fi

		if test "${_LIBS}" = "" ; then
			_TMP=`${FIND} /opt -name "libqgl*" -print 2>/dev/null`
			for j in ${_TMP} ; do
				if test "${_LIBS}" = "" ; then
					_LIBS=`echo $j|${SED} "s/\/libqgl.*/\//"`
				fi
			done
		fi

		if test "${_LIBS}" = "" ; then
			_TMP=`${FIND} /usr -name "libqgl*" -print 2>/dev/null`
			for j in ${_TMP} ; do
				if test "${_LIBS}" = "" ; then
					_LIBS=`echo $j|${SED} "s/\/libqgl.*/\//"`
				fi
			done
		fi

	fi

	QGL_LIBPATH="${_LIBS}"

			fi

			if test "${QGL_LIBPATH}" = "" ; then
				echo "$as_me:9462: result: (not found!)" >&5
echo "${ECHO_T}(not found!)" >&6
				echo "$as_me:9464: result: " >&5
echo "${ECHO_T}" >&6
				echo "$as_me:9466: result: The QT Opengl library libqgl.a could not be found. Please specify" >&5
echo "${ECHO_T}The QT Opengl library libqgl.a could not be found. Please specify" >&6
				echo "$as_me:9468: result: the path to libqt/libqgl by passing the option --with-qt-libs=DIR" >&5
echo "${ECHO_T}the path to libqt/libqgl by passing the option --with-qt-libs=DIR" >&6
				echo "$as_me:9470: result: to configure." >&5
echo "${ECHO_T}to configure." >&6
				echo "$as_me:9472: result: You may also set the environment variable QTDIR to the correct" >&5
echo "${ECHO_T}You may also set the environment variable QTDIR to the correct" >&6
				echo "$as_me:9474: result: path - configure will recognize this, too." >&5
echo "${ECHO_T}path - configure will recognize this, too." >&6
				echo "$as_me:9476: result: Perhaps you simple forgot to compile the OpenGl extsions, too?" >&5
echo "${ECHO_T}Perhaps you simple forgot to compile the OpenGl extsions, too?" >&6
				echo "$as_me:9478: result: You find these extensions in ${QTDIR}/extensions/opengl" >&5
echo "${ECHO_T}You find these extensions in ${QTDIR}/extensions/opengl" >&6
				echo "$as_me:9480: result: " >&5
echo "${ECHO_T}" >&6
				echo "$as_me:9482: result: The complete QT package can be found under the following URL:" >&5
echo "${ECHO_T}The complete QT package can be found under the following URL:" >&6
				echo "$as_me:9484: result:   http://www.troll.no/q" >&5
echo "${ECHO_T}  http://www.troll.no/q" >&6
				echo "$as_me:9486: result: " >&5
echo "${ECHO_T}" >&6
				echo "$as_me:9488: result: Building of the visualization component BALLVIEW was disabled!" >&5
echo "${ECHO_T}Building of the visualization component BALLVIEW was disabled!" >&6
			else
				echo "$as_me:9491: result: (${QT_LIBPATH})" >&5
echo "${ECHO_T}(${QT_LIBPATH})" >&6
			fi
			if test "${QGL_PATH}" != "${QT_PATH}" ; then
				QT_PATH=${QGL_PATH}
				echo "$as_me:9496: result: using ${QT_PATH} to look for liqt.so and libqgl.s" >&5
echo "${ECHO_T}using ${QT_PATH} to look for liqt.so and libqgl.s" >&6
			fi

			if test "${QT_INCPATH}" != /usr/include && test "${QT_INCPATH}" != "" ; then
				BALLVIEW_INCLUDES="${BALLVIEW_INCLUDES} -I${QT_INCPATH}"
			fi
		fi
	fi
fi

if test "${USE_BALLVIEW}" = true ; then

	echo "$as_me:9509: checking linking against X11 libraries" >&5
echo $ECHO_N "checking linking against X11 libraries... $ECHO_C" >&6
				if test "${X11_LIBS}" != "" ; then
		SAVE_LIBS=${LIBS}
		LIBS="${X11_LIBPATHOPT} ${X11_LIBS} ${LIBS}"
		cat >conftest.$ac_ext <<_ACEOF
#line 9515 "configure"
#include "confdefs.h"

int
main ()
{

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:9527: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:9530: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:9533: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:9536: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  X_LINKING_OK=1
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
		LIBS=${SAVE_LIBS}
	fi

				if test "${X_LINKING_OK+set}" != set ; then
		X11_LIBS="-lXmu -lXext -lXt -lX11 -lm"
		SAVE_LIBS=${LIBS}
		LIBS="${X11_LIBPATHOPT} ${X11_LIBS} ${LIBS}"
		cat >conftest.$ac_ext <<_ACEOF
#line 9552 "configure"
#include "confdefs.h"

int
main ()
{

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:9564: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:9567: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:9570: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:9573: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  X_LINKING_OK=1
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
		LIBS=${SAVE_LIBS}
	fi

				if test "${X_LINKING_OK+set}" != set ; then
		X11_LIBS="-lXmu -lXext -lXt -lX11 -lSM -lICE -lm"
		SAVE_LIBS=${LIBS}
		LIBS="${X11_LIBPATHOPT} ${X11_LIBS} ${LIBS}"
		cat >conftest.$ac_ext <<_ACEOF
#line 9589 "configure"
#include "confdefs.h"

int
main ()
{

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:9601: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:9604: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:9607: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:9610: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  X_LINKING_OK=1
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
		LIBS=${SAVE_LIBS}
	fi

				if test "${X_LINKING_OK+set}" != set ; then
		X11_LIBS="-lXmu -lXt -lX11 -lm"
		SAVE_LIBS=${LIBS}
		LIBS="${X11_LIBPATHOPT} ${X11_LIBS} ${LIBS}"
		cat >conftest.$ac_ext <<_ACEOF
#line 9626 "configure"
#include "confdefs.h"

int
main ()
{

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:9638: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:9641: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:9644: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:9647: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  X_LINKING_OK=1
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
		LIBS=${SAVE_LIBS}
	fi

				if test "${X_LINKING_OK+set}" != set ; then
		X11_LIBS="-lXmu -lXt -lX11 -lSM -lICE -lm"
		SAVE_LIBS=${LIBS}
		LIBS="${X11_LIBPATHOPT} ${X11_LIBS} ${LIBS}"
		cat >conftest.$ac_ext <<_ACEOF
#line 9663 "configure"
#include "confdefs.h"

int
main ()
{

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:9675: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:9678: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:9681: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:9684: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  X_LINKING_OK=1
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
		LIBS=${SAVE_LIBS}
	fi

				if test "${X_LINKING_OK+set}" = set ; then
		echo "$as_me:9696: result: yes" >&5
echo "${ECHO_T}yes" >&6
	else
		echo "$as_me:9699: result: no" >&5
echo "${ECHO_T}no" >&6
		echo "$as_me:9701: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:9703: result: Don't know how to link with X11 libraries." >&5
echo "${ECHO_T}Don't know how to link with X11 libraries." >&6
		echo "$as_me:9705: result: Please specify the correct libraries (e.g. -lXmu -lXt -lX11) in the" >&5
echo "${ECHO_T}Please specify the correct libraries (e.g. -lXmu -lXt -lX11) in the" >&6
		echo "$as_me:9707: result: environment variable X11_LIBS" >&5
echo "${ECHO_T}environment variable X11_LIBS" >&6
		echo "$as_me:9709: result: If you are running Solaris 2.x you might also try the option --without-libxnet" >&5
echo "${ECHO_T}If you are running Solaris 2.x you might also try the option --without-libxnet" >&6
		echo "$as_me:9711: result: if your X libraries were linked against libsocket and libnsl instead of libxnet." >&5
echo "${ECHO_T}if your X libraries were linked against libsocket and libnsl instead of libxnet." >&6
		echo "$as_me:9713: result: Built of visualization component BALLVIEW disabled." >&5
echo "${ECHO_T}Built of visualization component BALLVIEW disabled." >&6
		echo "$as_me:9715: result: " >&5
echo "${ECHO_T}" >&6
		USE_BALLVIEW=false
	fi

				X11_LIBOPTS="${X11_LIBPATHOPT} ${X11_LIBS}"
fi

if test "${USE_BALLVIEW}" = true ; then
	if test "${BALLVIEW_PLATFORM}" = OpenGL ; then
		if test "${OPENGL_LIBPATH}" != "/usr/lib" -a "${OPENGL_LIBPATH}" != "" ; then
			OPENGL_LIBOPTS="-L${OPENGL_LIBPATH} -lGLU -lGL"
		else
			OPENGL_LIBPATH=""
			OPENGL_LIBOPTS="-lGLU -lGL"
		fi

						SAVE_LIBS=${LIBS}
		SAVE_LDFLAGS=${LDFLAGS}
		LIBS="${LIBS} ${X11_LIBOPTS}"
		if test "${OPENGL_LIBPATH}" != "" ; then
			LDFLAGS="${LDFLAGS} -L${OPENGL_LIBPATH}"
		fi
		echo "$as_me:9738: checking for XMesaGarbageCollect in -lGL" >&5
echo $ECHO_N "checking for XMesaGarbageCollect in -lGL... $ECHO_C" >&6
if test "${ac_cv_lib_GL_XMesaGarbageCollect+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  ac_check_lib_save_LIBS=$LIBS
LIBS="-lGL  $LIBS"
cat >conftest.$ac_ext <<_ACEOF
#line 9746 "configure"
#include "confdefs.h"

/* Override any gcc2 internal prototype to avoid an error.  */
#ifdef __cplusplus
extern "C"
#endif
/* We use char because int might match the return type of a gcc2
   builtin and then its argument prototype would still apply.  */
char XMesaGarbageCollect ();
int
main ()
{
XMesaGarbageCollect ();
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:9765: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:9768: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:9771: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:9774: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  ac_cv_lib_GL_XMesaGarbageCollect=yes
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
ac_cv_lib_GL_XMesaGarbageCollect=no
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
LIBS=$ac_check_lib_save_LIBS
fi
echo "$as_me:9785: result: $ac_cv_lib_GL_XMesaGarbageCollect" >&5
echo "${ECHO_T}$ac_cv_lib_GL_XMesaGarbageCollect" >&6
if test $ac_cv_lib_GL_XMesaGarbageCollect = yes; then
  BALLVIEW_PLATFORM=Mesa
fi

		LIBS=${SAVE_LIBS}
		LDFLAGS=${SAVE_LDFLAGS}
		if test "${BALLVIEW_PLATFORM}" != Mesa ; then
			echo "$as_me:9794: checking linking against OpenGL libraries" >&5
echo $ECHO_N "checking linking against OpenGL libraries... $ECHO_C" >&6
			SAVE_LIBS=${LIBS}
			LIBS="${OPENGL_LIBOPTS} ${LIBS}"
			cat >conftest.$ac_ext <<_ACEOF
#line 9799 "configure"
#include "confdefs.h"

int
main ()
{

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:9811: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:9814: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:9817: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:9820: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  OPENGL_LINKING_OK=1
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
			LIBS=${SAVE_LIBS}
			if test "${OPENGL_LINKING_OK+set}" != set ; then
				echo "$as_me:9830: result: no" >&5
echo "${ECHO_T}no" >&6
				echo "$as_me:9832: result: " >&5
echo "${ECHO_T}" >&6
				echo "$as_me:9834: result: Cannot link against libGL/GLU - disabling visualization support!" >&5
echo "${ECHO_T}Cannot link against libGL/GLU - disabling visualization support!" >&6
				echo "$as_me:9836: result: Please specify the path to OpenGL libraries using --with-opengl-libs=DIR" >&5
echo "${ECHO_T}Please specify the path to OpenGL libraries using --with-opengl-libs=DIR" >&6
				echo "$as_me:9838: result: " >&5
echo "${ECHO_T}" >&6
				USE_BALLVIEW=false
			else
				echo "$as_me:9842: result: yes" >&5
echo "${ECHO_T}yes" >&6
			fi
		fi
	fi
fi

if test "${USE_BALLVIEW}" = true ; then
	if test "${BALLVIEW_PLATFORM}" = Mesa ; then

		if test "${MESA_LIBS}" = "" ; then
			MESA_LIBS=${OPENGL_LIBPATH}
		fi
		if test "${MESA_LIBS}" != "/usr/lib" -a "${MESA_LIBS}" != "" ; then
			OPENGL_LIBPATH="${MESA_LIBS}"
			OPENGL_LIBPATHOPT="-L${MESA_LIBS}"
		else
			OPENGL_LIBPATH=""
			OPENGL_LIBPATHOPT=""
		fi

								OPENGL_LIBS="-lGLU -lGL"

								echo "$as_me:9865: checking linking against Mesa libs" >&5
echo $ECHO_N "checking linking against Mesa libs... $ECHO_C" >&6
		SAVE_LIBS=${LIBS}
		LIBS="${OPENGL_LIBPATHOPT} ${OPENGL_LIBS} ${X11_LIBOPTS} ${LIBS} "
		cat >conftest.$ac_ext <<_ACEOF
#line 9870 "configure"
#include "confdefs.h"

int
main ()
{

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:9882: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:9885: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:9888: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:9891: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  HAVE_MESALIBS=1
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
		LIBS=${SAVE_LIBS}

				    				if test "${HAVE_MESALIBS+set}" != set ; then
			OPENGL_LIBS="-lMesaGLU -lMesaGL"
			SAVE_LIBS=${LIBS}
			LIBS="${OPENGL_LIBPATHOPT} ${OPENGL_LIBS} ${X11_LIBOPTS} ${LIBS} "
			cat >conftest.$ac_ext <<_ACEOF
#line 9906 "configure"
#include "confdefs.h"

int
main ()
{

  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:9918: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:9921: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:9924: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:9927: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  HAVE_MESALIBS=1
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
			LIBS=${SAVE_LIBS}
		fi

		if test "${HAVE_MESALIBS+set}" != set ; then
			echo "$as_me:9939: result: no" >&5
echo "${ECHO_T}no" >&6
			echo "$as_me:9941: result: " >&5
echo "${ECHO_T}" >&6
			echo "$as_me:9943: result: Cannot link against libMesaGL/GLU - disabling visualization support!" >&5
echo "${ECHO_T}Cannot link against libMesaGL/GLU - disabling visualization support!" >&6
			echo "$as_me:9945: result: Please specify the path to libMesaGL using --with-opengl-libs=DIR" >&5
echo "${ECHO_T}Please specify the path to libMesaGL using --with-opengl-libs=DIR" >&6
			echo "$as_me:9947: result: " >&5
echo "${ECHO_T}" >&6
			USE_BALLVIEW=false
		else
			echo "$as_me:9951: result: yes" >&5
echo "${ECHO_T}yes" >&6
			OPENGL_LIBOPTS="${OPENGL_LIBPATHOPT} ${OPENGL_LIBS}"
		fi
	fi
fi
if test "${USE_BALLVIEW}" = true ; then
	if test "${QT_LIBPATH}" != "/usr/lib" ; then
		QTQGL_LIBOPTS="-L${QT_LIBPATH} -lqgl -lqt${QT_MT_SUFFIX}"
		QT_LIBOPTS="-L${QT_LIBPATH} -lqt${QT_MT_SUFFIX}"
	else
		QT_LIBPATH=""
		QTQGL_LIBOPTS="-lqgl -lqt${QT_MT_SUFFIX}"
		QT_LIBOPTS="-lqt${QT_MT_SUFFIX}"
	fi
fi
if test "${USE_BALLVIEW}" = true ; then
	echo "$as_me:9968: checking linking against QT libraries" >&5
echo $ECHO_N "checking linking against QT libraries... $ECHO_C" >&6

	SAVE_LIBS=${LIBS}
	LIBS="${QTQGL_LIBOPTS} ${OPENGL_LIBOPTS} ${X11_LIBOPTS} ${LIBS}"
	cat >conftest.$ac_ext <<_ACEOF
#line 9974 "configure"
#include "confdefs.h"
const char* qGLVersion();
int
main ()
{
qGLVersion();
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:9986: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:9989: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:9992: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:9995: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  QT_LINKING_OK=1
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
	LIBS=${SAVE_LIBS}

	if test "${QT_LINKING_OK+set}" != set ; then
		SAVE_LIBS=${LIBS}
		LIBS="${QT_LIBOPTS} ${OPENGL_LIBOPTS} ${X11_LIBOPTS} ${LIBS}"
		cat >conftest.$ac_ext <<_ACEOF
#line 10009 "configure"
#include "confdefs.h"
const char* qGLVersion();
int
main ()
{
qGLVersion();
  ;
  return 0;
}
_ACEOF
rm -f conftest.$ac_objext conftest$ac_exeext
if { (eval echo "$as_me:10021: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:10024: \$? = $ac_status" >&5
  (exit $ac_status); } &&
         { ac_try='test -s conftest$ac_exeext'
  { (eval echo "$as_me:10027: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:10030: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  QT_LINKING_OK=1
else
  echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
fi
rm -f conftest.$ac_objext conftest$ac_exeext conftest.$ac_ext
		LIBS=${SAVE_LIBS}
	else
				QT_LIBOPTS="${QTQGL_LIBOPTS}"
	fi

	if test "${QT_LINKING_OK+set}" != set ; then
		echo "$as_me:10044: result: no" >&5
echo "${ECHO_T}no" >&6
		echo "$as_me:10046: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:10048: result: Cannot link against libqgl/qt - disabling support for visualization!" >&5
echo "${ECHO_T}Cannot link against libqgl/qt - disabling support for visualization!" >&6
		echo "$as_me:10050: result: If QT is installed, please specify the path to the library" >&5
echo "${ECHO_T}If QT is installed, please specify the path to the library" >&6
		echo "$as_me:10052: result: using the option --with-qt-libs=DIR" >&5
echo "${ECHO_T}using the option --with-qt-libs=DIR" >&6
		echo "$as_me:10054: result: " >&5
echo "${ECHO_T}" >&6
		USE_BALLVIEW=false
	else
		echo "$as_me:10058: result: yes" >&5
echo "${ECHO_T}yes" >&6

								echo "$as_me:10061: checking QT library version" >&5
echo $ECHO_N "checking QT library version... $ECHO_C" >&6
		SAVE_LIBS=${LIBS}
		LIBS="${QT_LIBOPTS} ${OPENGL_LIBOPTS} ${X11_LIBOPTS} ${LIBS}"
		LD_LIBRARY_PATH="${X11_LIBPATH}:${OPENGL_LIBPATH}:${QT_LIBPATH}:${LD_LIBRARY_PATH}"
		export LD_LIBRARY_PATH
		echo "LD_LIBRARY_PATH = ${LD_LIBRARY_PATH}" 1>&5
		if test "$cross_compiling" = yes; then
  DUMMY=0

else
  cat >conftest.$ac_ext <<_ACEOF
#line 10073 "configure"
#include "confdefs.h"

				#include <stdio.h>
				const char* qVersion();
				int main()
				{
					FILE* f = fopen("qt.version", "w");
					fprintf(f, "%s\n", qVersion());
					fclose(f);
					exit(0);
				}

_ACEOF
rm -f conftest$ac_exeext
if { (eval echo "$as_me:10088: \"$ac_link\"") >&5
  (eval $ac_link) 2>&5
  ac_status=$?
  echo "$as_me:10091: \$? = $ac_status" >&5
  (exit $ac_status); } && { ac_try='./conftest$ac_exeext'
  { (eval echo "$as_me:10093: \"$ac_try\"") >&5
  (eval $ac_try) 2>&5
  ac_status=$?
  echo "$as_me:10096: \$? = $ac_status" >&5
  (exit $ac_status); }; }; then
  QT_VERSION_OK=1
else
  echo "$as_me: program exited with status $ac_status" >&5
echo "$as_me: failed program was:" >&5
cat conftest.$ac_ext >&5
DUMMY=0
fi
rm -f core core.* *.core conftest$ac_exeext conftest.$ac_ext
fi
		LIBS=${SAVE_LIBS}

										if test "${QT_VERSION_OK+set}" != set; then
			echo "$as_me:10110: result: no" >&5
echo "${ECHO_T}no" >&6
			echo "$as_me:10112: result: " >&5
echo "${ECHO_T}" >&6
			echo "$as_me:10114: result: The execution of a program linked against the QT" >&5
echo "${ECHO_T}The execution of a program linked against the QT" >&6
			echo "$as_me:10116: result: library failed. Please have a look at config.log" >&5
echo "${ECHO_T}library failed. Please have a look at config.log" >&6
			echo "$as_me:10118: result: (the last few lines) to find out what happened." >&5
echo "${ECHO_T}(the last few lines) to find out what happened." >&6
			echo "$as_me:10120: result: Perhaps you specified the wrong library or the" >&5
echo "${ECHO_T}Perhaps you specified the wrong library or the" >&6
			echo "$as_me:10122: result: X11 libraries are in conflict with any other library." >&5
echo "${ECHO_T}X11 libraries are in conflict with any other library." >&6
			echo "$as_me:10124: result: You might also want to check your LD_LIBRARY_PATH." >&5
echo "${ECHO_T}You might also want to check your LD_LIBRARY_PATH." >&6
			echo "$as_me:10126: result: Support for visualization is disabled." >&5
echo "${ECHO_T}Support for visualization is disabled." >&6
			echo "$as_me:10128: result: " >&5
echo "${ECHO_T}" >&6
			USE_BALLVIEW=false
		else
			QT_VERSION_STRING=`cat qt.version`
			echo "$as_me:10133: result: ${QT_VERSION_STRING}" >&5
echo "${ECHO_T}${QT_VERSION_STRING}" >&6

															rm qt.version 2>/dev/null
			QT_MAJOR=`echo ${QT_VERSION_STRING} | ${CUT} -d. -f1`
			if test "${QT_MAJOR}" != 2 ; then
				echo "$as_me:10139: result: " >&5
echo "${ECHO_T}" >&6
				echo "$as_me:10141: result: QT version 2.x is required." >&5
echo "${ECHO_T}QT version 2.x is required." >&6
				echo "$as_me:10143: result: Please install version QT Version 2 (at least 2.0.2)" >&5
echo "${ECHO_T}Please install version QT Version 2 (at least 2.0.2)" >&6
				echo "$as_me:10145: result: which can be obtained from" >&5
echo "${ECHO_T}which can be obtained from" >&6
				echo "$as_me:10147: result: " >&5
echo "${ECHO_T}" >&6
				echo "$as_me:10149: result: www.troll.no/qt" >&5
echo "${ECHO_T}www.troll.no/qt" >&6
				echo "$as_me:10151: result: " >&5
echo "${ECHO_T}" >&6
				echo "$as_me:10153: result: Support for visualization is disabled." >&5
echo "${ECHO_T}Support for visualization is disabled." >&6
				echo "$as_me:10155: result: " >&5
echo "${ECHO_T}" >&6
				USE_BALLVIEW=false
			else
												        								if test "${QT_VERSION_STRING}" = "2.00" ; then
					echo "$as_me:10160: result: " >&5
echo "${ECHO_T}" >&6
					echo "$as_me:10162: result: QT verison 2.00 is known to contain a bug" >&5
echo "${ECHO_T}QT verison 2.00 is known to contain a bug" >&6
					echo "$as_me:10164: result: that affects some features of MOLVIEW." >&5
echo "${ECHO_T}that affects some features of MOLVIEW." >&6
					echo "$as_me:10166: result: Please install at least version 2.0.2" >&5
echo "${ECHO_T}Please install at least version 2.0.2" >&6
					echo "$as_me:10168: result: which can be obtained from" >&5
echo "${ECHO_T}which can be obtained from" >&6
					echo "$as_me:10170: result: " >&5
echo "${ECHO_T}" >&6
					echo "$as_me:10172: result: www.troll.no/qt" >&5
echo "${ECHO_T}www.troll.no/qt" >&6
					echo "$as_me:10174: result: " >&5
echo "${ECHO_T}" >&6
					echo "$as_me:10176: result: " >&5
echo "${ECHO_T}" >&6
				fi
			fi
		fi
	fi
fi

if test "${USE_BALLVIEW}" = true ; then
	if test "${MOC}" = moc ; then
		if test "${QTDIR}" != "" ; then
			MOC=${QTDIR}/bin/moc
		fi
	fi

				# Extract the first word of "moc", so it can be a program name with args.
set dummy moc; ac_word=$2
echo "$as_me:10193: checking for $ac_word" >&5
echo $ECHO_N "checking for $ac_word... $ECHO_C" >&6
if test "${ac_cv_path_MOC+set}" = set; then
  echo $ECHO_N "(cached) $ECHO_C" >&6
else
  case $MOC in
  [\\/]* | ?:[\\/]*)
  ac_cv_path_MOC="$MOC" # Let the user override the test with a path.
  ;;
  *)
  ac_save_IFS=$IFS; IFS=$ac_path_separator
ac_dummy="$PATH"
for ac_dir in $ac_dummy; do
  IFS=$ac_save_IFS
  test -z "$ac_dir" && ac_dir=.
  if $as_executable_p "$ac_dir/$ac_word"; then
   ac_cv_path_MOC="$ac_dir/$ac_word"
   echo "$as_me:10210: found $ac_dir/$ac_word" >&5
   break
fi
done

  test -z "$ac_cv_path_MOC" && ac_cv_path_MOC="moc"
  ;;
esac
fi
MOC=$ac_cv_path_MOC

if test -n "$MOC"; then
  echo "$as_me:10222: result: $MOC" >&5
echo "${ECHO_T}$MOC" >&6
else
  echo "$as_me:10225: result: no" >&5
echo "${ECHO_T}no" >&6
fi

	if test "${MOC}" = moc ; then
		echo "$as_me:10230: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:10232: result: Could not find the QT Meta Object Compiler (moc)!" >&5
echo "${ECHO_T}Could not find the QT Meta Object Compiler (moc)!" >&6
		echo "$as_me:10234: result: You might run into trouble if you want to compile MolVIEW." >&5
echo "${ECHO_T}You might run into trouble if you want to compile MolVIEW." >&6
		echo "$as_me:10236: result: Please include the correct path to moc into your" >&5
echo "${ECHO_T}Please include the correct path to moc into your" >&6
		echo "$as_me:10238: result: PATH environment variable or specify the path to moc" >&5
echo "${ECHO_T}PATH environment variable or specify the path to moc" >&6
		echo "$as_me:10240: result: using the option --with-moc=PATH to rerun configure." >&5
echo "${ECHO_T}using the option --with-moc=PATH to rerun configure." >&6
		echo "$as_me:10242: result: " >&5
echo "${ECHO_T}" >&6
	fi
fi

if test "${USE_BALLVIEW}" = "true" ; then
	LIBBALLVIEW="libVIEW.a libMOLVIEW.a"
	BALLVIEW="VIEW MOLVIEW"
else
	BALLVIEW=
fi

if test "${PYTHON_SUPPORT}" = true ; then
	cat >>confdefs.h <<\EOF
#define BALL_PYTHON_SUPPORT 1
EOF

fi

if test "${PYTHON_SUPPORT}" = true ; then
	echo "$as_me:10262: checking stuff required for Python extensions" >&5
echo $ECHO_N "checking stuff required for Python extensions... $ECHO_C" >&6
	echo "$as_me:10264: result: ." >&5
echo "${ECHO_T}." >&6

				echo "$as_me:10267: checking for Python.h" >&5
echo $ECHO_N "checking for Python.h... $ECHO_C" >&6

	_INCLUDES=

		if test "${PYTHON_INC_PATH}" != "" ; then
		_INCLUDES=${PYTHON_INC_PATH}
	fi

	if test "${_INCLUDES}" = "" ; then
		for i in /usr/include /opt/include ${PYTHON_INCLUDE_PATH} ; do
			if test -f "$i/Python.h" -a "${_INCLUDES}" = ""; then
				_INCLUDES="$i"
			fi
		done
	fi

	if test "${_INCLUDES}" = "" ; then
		for i in /usr/*/include /opt/*/include ; do
			if test -f "$i/Python.h" -a "${_INCLUDES}" = ""; then
				_INCLUDES="$i"
			fi
		done
	fi

	if test "${_INCLUDES}" = "" ; then
		for i in /opt/*/*/include /usr/*/*/include /usr/local/*/*/include ; do
			if test -f "$i/Python.h" -a "${_INCLUDES}" = ""; then
				_INCLUDES="$i"
			fi
		done
	fi

	if test "${_INCLUDES}" = "" -a "${FIND}" != "-" ; then
		if test "${FIND_KNOWS_PATH}" = false ; then
			FIND_OPT="-name"
			_TMP_FIND_NAME="Python.h"
			while test _`egrep / $_TMP_FIND_NAME`_ != __ ; do
				_TMP_FIND_NAME=`echo ${_TMP_FIND_NAME}|${CUT} -d/ -f2-`
			done

			FIND_ARG="\*${_TMP_FIND_NAME}\*"
		else
			FIND_OPT="-path"
			FIND_ARG="\*Python.h\*"
		fi

		_TMP=`${FIND} /usr ${FIND_OPT} ${FIND_ARG} -print 2>/dev/null`
		for j in ${_TMP} ; do
			if test "${_INCLUDES}" = "" ; then
				_INCLUDES=`echo $j|${SED} "s/include\/.\*\$/include/"`
			fi
		done

		if test "${_INCLUDES}" = "" ; then
			_TMP=`${FIND} /opt ${FIND_OPT} ${FIND_ARG} -print 2>/dev/null`
			for j in ${_TMP} ; do
				if test "${_INCLUDES}" = "" ; then
					_INCLUDES=`echo $j|${SED} "s/include\/.\*\$/include/"`
				fi
			done
		fi

		if test "${_INCLUDES}" = "" -a "${PYTHON_INCLUDE_PATH}" != ""; then
			for i in ${PYTHON_INCLUDE_PATH} /dev/null ; do
				_TMP=`${FIND} $i ${FIND_OPT} ${FIND_ARG} -print 2>/dev/null`
				for j in ${_TMP} ; do
					if test "${_INCLUDES}" = "" ; then
						_INCLUDES=`echo $j|${SED} "s/include\/.\*\$/include/"`
					fi
				done
			done
		fi
	fi

	PYTHON_INC_PATH="${_INCLUDES}"

	if test "${PYTHON_INC_PATH}" = "" ; then
		echo "$as_me:10345: result: not found!" >&5
echo "${ECHO_T}not found!" >&6
		echo "$as_me:10347: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:10349: result: Please specify the path to the directory that contains" >&5
echo "${ECHO_T}Please specify the path to the directory that contains" >&6
		echo "$as_me:10351: result: Python.h using the option --with-python-incl=DIR" >&5
echo "${ECHO_T}Python.h using the option --with-python-incl=DIR" >&6
		{ { echo "$as_me:10353: error: Aborted." >&5
echo "$as_me: error: Aborted." >&2;}
   { (exit 1); exit 1; }; }
	else
		echo "$as_me:10357: result: ${PYTHON_INC_PATH}" >&5
echo "${ECHO_T}${PYTHON_INC_PATH}" >&6
		PYTHON_INCLUDES="-I${PYTHON_INC_PATH}"
	fi

				echo "$as_me:10362: checking for libpython" >&5
echo $ECHO_N "checking for libpython... $ECHO_C" >&6
	PYTHON_LIBS=`${FIND} ${PYTHON_LIBPATH} -name libpython*.a 2>/dev/null`
	if test "${PYTHON_LIBS}" = "" ; then
			echo "$as_me:10366: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:10368: result: No libpython*a found in ${PYTHON_LIBPATH}. Please specify" >&5
echo "${ECHO_T}No libpython*a found in ${PYTHON_LIBPATH}. Please specify" >&6
		echo "$as_me:10370: result: the path where your Python library resides using" >&5
echo "${ECHO_T}the path where your Python library resides using" >&6
		echo "$as_me:10372: result: --with-python-libs=DIR" >&5
echo "${ECHO_T}--with-python-libs=DIR" >&6
		echo "$as_me:10374: result: " >&5
echo "${ECHO_T}" >&6
		{ { echo "$as_me:10376: error: Aborted." >&5
echo "$as_me: error: Aborted." >&2;}
   { (exit 1); exit 1; }; }
	fi
	echo "$as_me:10380: result: ${PYTHON_LIBS}" >&5
echo "${ECHO_T}${PYTHON_LIBS}" >&6

	if test "${PYTHON_LDOPTS}" = "" ; then
		PYTHON_MAKEFILE=`${FIND} ${PYTHON_LIBPATH} -name Makefile 2>/dev/null`
		if test "${PYTHON_MAKEFILE}" = "" ; then
			echo "$as_me:10386: result: " >&5
echo "${ECHO_T}" >&6
			echo "$as_me:10388: result: Makefile in the Python lib/config directory not found!" >&5
echo "${ECHO_T}Makefile in the Python lib/config directory not found!" >&6
			echo "$as_me:10390: result: Please specify the correct options needed to link" >&5
echo "${ECHO_T}Please specify the correct options needed to link" >&6
			echo "$as_me:10392: result: against the Python library using" >&5
echo "${ECHO_T}against the Python library using" >&6
			echo "$as_me:10394: result: --with-python-ldopts=OPTIONS" >&5
echo "${ECHO_T}--with-python-ldopts=OPTIONS" >&6
			echo "$as_me:10396: result: (e.g. --with-python-ldopts=\"-ltermcap -lm\")" >&5
echo "${ECHO_T}(e.g. --with-python-ldopts=\"-ltermcap -lm\")" >&6
			echo "$as_me:10398: result: " >&5
echo "${ECHO_T}" >&6
			{ { echo "$as_me:10400: error: Aborted." >&5
echo "$as_me: error: Aborted." >&2;}
   { (exit 1); exit 1; }; }
		fi
		PYTHON_LIBS="${PYTHON_LIBS} `${GREP} \^LIBS= ${PYTHON_MAKEFILE} | ${CUT} -d=  -f2-`"
		PYTHON_LIBS="${PYTHON_LIBS} `${GREP} \^BASEMODLIBS= ${PYTHON_MAKEFILE} | ${CUT} -d=  -f2-`"
		PYTHON_LIBS="${PYTHON_LIBS} `${GREP} \^LOCALMODLIBS= ${PYTHON_MAKEFILE} | ${CUT} -d=  -f2-` -lm"
		PYTHON_LIBS=`echo ${PYTHON_LIBS} | ${TR} -s " "`
	fi
	echo "$as_me:10409: result: Linker options for Python library: ${PYTHON_LIBS}" >&5
echo "${ECHO_T}Linker options for Python library: ${PYTHON_LIBS}" >&6

				echo "$as_me:10412: checking sip executable" >&5
echo $ECHO_N "checking sip executable... $ECHO_C" >&6
	echo "$as_me:10414: result: ${SIP}" >&5
echo "${ECHO_T}${SIP}" >&6

				echo "$as_me:10417: checking libsip.a" >&5
echo $ECHO_N "checking libsip.a... $ECHO_C" >&6
	if test -r "${SIP_LIBPATH}/libsip.a" ; then
		SIP_LIB=${SIP_LIBPATH}/libsip.a
		echo "$as_me:10421: result: ${SIP_LIB}" >&5
echo "${ECHO_T}${SIP_LIB}" >&6
	else
		echo "$as_me:10424: result: not found in ${SIP_LIBPATH}" >&5
echo "${ECHO_T}not found in ${SIP_LIBPATH}" >&6
		echo "$as_me:10426: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:10428: result: Please specify the path to the directory that contains" >&5
echo "${ECHO_T}Please specify the path to the directory that contains" >&6
		echo "$as_me:10430: result: libsip.a using the option --with-sip-lib=DIR." >&5
echo "${ECHO_T}libsip.a using the option --with-sip-lib=DIR." >&6
		echo "$as_me:10432: result: If you do not have that file, you should obtain SIP" >&5
echo "${ECHO_T}If you do not have that file, you should obtain SIP" >&6
		echo "$as_me:10434: result: from" >&5
echo "${ECHO_T}from" >&6
		echo "$as_me:10436: result: www.thekompany.com/projects/pykde" >&5
echo "${ECHO_T}www.thekompany.com/projects/pykde" >&6
		{ { echo "$as_me:10438: error: Aborted." >&5
echo "$as_me: error: Aborted." >&2;}
   { (exit 1); exit 1; }; }
	fi

				echo "$as_me:10443: checking sip headers (sip.h)" >&5
echo $ECHO_N "checking sip headers (sip.h)... $ECHO_C" >&6

	_INCLUDES=

		if test "${SIP_INC_PATH}" != "" ; then
		_INCLUDES=${SIP_INC_PATH}
	fi

	if test "${_INCLUDES}" = "" ; then
		for i in /usr/include /opt/include ${SIP_INCLUDE_PATH} ; do
			if test -f "$i/sip.h" -a "${_INCLUDES}" = ""; then
				_INCLUDES="$i"
			fi
		done
	fi

	if test "${_INCLUDES}" = "" ; then
		for i in /usr/*/include /opt/*/include ; do
			if test -f "$i/sip.h" -a "${_INCLUDES}" = ""; then
				_INCLUDES="$i"
			fi
		done
	fi

	if test "${_INCLUDES}" = "" ; then
		for i in /opt/*/*/include /usr/*/*/include /usr/local/*/*/include ; do
			if test -f "$i/sip.h" -a "${_INCLUDES}" = ""; then
				_INCLUDES="$i"
			fi
		done
	fi

	if test "${_INCLUDES}" = "" -a "${FIND}" != "-" ; then
		if test "${FIND_KNOWS_PATH}" = false ; then
			FIND_OPT="-name"
			_TMP_FIND_NAME="sip.h"
			while test _`egrep / $_TMP_FIND_NAME`_ != __ ; do
				_TMP_FIND_NAME=`echo ${_TMP_FIND_NAME}|${CUT} -d/ -f2-`
			done

			FIND_ARG="\*${_TMP_FIND_NAME}\*"
		else
			FIND_OPT="-path"
			FIND_ARG="\*sip.h\*"
		fi

		_TMP=`${FIND} /usr ${FIND_OPT} ${FIND_ARG} -print 2>/dev/null`
		for j in ${_TMP} ; do
			if test "${_INCLUDES}" = "" ; then
				_INCLUDES=`echo $j|${SED} "s/include\/.\*\$/include/"`
			fi
		done

		if test "${_INCLUDES}" = "" ; then
			_TMP=`${FIND} /opt ${FIND_OPT} ${FIND_ARG} -print 2>/dev/null`
			for j in ${_TMP} ; do
				if test "${_INCLUDES}" = "" ; then
					_INCLUDES=`echo $j|${SED} "s/include\/.\*\$/include/"`
				fi
			done
		fi

		if test "${_INCLUDES}" = "" -a "${SIP_INCLUDE_PATH}" != ""; then
			for i in ${SIP_INCLUDE_PATH} /dev/null ; do
				_TMP=`${FIND} $i ${FIND_OPT} ${FIND_ARG} -print 2>/dev/null`
				for j in ${_TMP} ; do
					if test "${_INCLUDES}" = "" ; then
						_INCLUDES=`echo $j|${SED} "s/include\/.\*\$/include/"`
					fi
				done
			done
		fi
	fi

	SIP_INC_PATH="${_INCLUDES}"

	if test "${SIP_INC_PATH}" = "" ; then
		echo "$as_me:10521: result: not found!" >&5
echo "${ECHO_T}not found!" >&6
		echo "$as_me:10523: result: " >&5
echo "${ECHO_T}" >&6
		echo "$as_me:10525: result: Please specify the path to the directory that contains" >&5
echo "${ECHO_T}Please specify the path to the directory that contains" >&6
		echo "$as_me:10527: result: sip.h using the option --with-sip-incl=DIR." >&5
echo "${ECHO_T}sip.h using the option --with-sip-incl=DIR." >&6
		echo "$as_me:10529: result: If you do not have that file, you should obtain SIP" >&5
echo "${ECHO_T}If you do not have that file, you should obtain SIP" >&6
		echo "$as_me:10531: result: from" >&5
echo "${ECHO_T}from" >&6
		echo "$as_me:10533: result: www.thekompany.com/projects/pykde" >&5
echo "${ECHO_T}www.thekompany.com/projects/pykde" >&6
		{ { echo "$as_me:10535: error: Aborted." >&5
echo "$as_me: error: Aborted." >&2;}
   { (exit 1); exit 1; }; }
	else
		echo "$as_me:10539: result: ${SIP_INC_PATH}" >&5
echo "${ECHO_T}${SIP_INC_PATH}" >&6
		SIP_INCLUDES="-I${SIP_INC_PATH}"
	fi
fi

if test "${PYTHON_SUPPORT}" = true ; then
	PYTHON=PYTHON
else
	PYTHON=
fi

BALL_INCLUDES="-I$BALL_PATH/include"

BINFMT="${BINFMT}-${CXX_NAME}"

if test "${MULTI_BUILD}" = "true" ; then
	touch ${BINFORMAT_FILE}
	if test "`${GREP} \^${BINFMT}\\$ ${BINFORMAT_FILE}`" = "" ; then
		echo ${BINFMT} >> ${BINFORMAT_FILE}
	fi
fi

if test "${MULTI_BUILD}" = "true" ; then
	${CAT} config/config.h.header | ${SED} 1,2d > config.h

					echo "#ifndef BFMT" >> config.h
	echo "# error BALL was configured in MULTI BUILD mode! Please specify -DBMFT=<BINFMT_INDEX> or rerun configure" >> config.h
	echo "#endif" >> config.h
	echo "" >> config.h

	LINES=`cat config/binary_formats | wc -l`
	i=1
	while test $i -le $LINES ; do
		BFMT=`cat ${BINFORMAT_FILE} | ${SED} -n ${i}p`
		echo "#if ( BFMT == $i )" >> config.h
		echo "#	include <BALL/CONFIG/config.h.${BFMT}>" >> config.h
		echo "#endif" >> config.h
		echo " " >> config.h
		i=`expr $i + 1`
	done
	${CAT} config/config.h.footer | ${SED} 1,2d >> config.h
	${MKDIR} ${BALL_PATH}/include/BALL/CONFIG 2>/dev/null
	if test -f ${BALL_PATH}/include/BALL/CONFIG/config.h ; then
		if test "`${DIFF} ${BALL_PATH}/include/BALL/CONFIG/config.h config.h`" != "" ; then
			${RM} ${BALL_PATH}/include/BALL/CONFIG/config.h
			${MV} config.h  ${BALL_PATH}/include/BALL/CONFIG/config.h
		else
			${RM} config.h
		fi
	else
		${MV} config.h  ${BALL_PATH}/include/BALL/CONFIG/config.h
	fi

	BINFMT_INDEX="-DBFMT="`${GREP} -n ${BINFMT} ${BINFORMAT_FILE} | ${CUT} -d: -f1 | ${TAIL} -1`
else
	BINFMT_INDEX=""
fi

LIBS="${LIBS} -lm"
BALLVIEW_LIBS="${BALLVIEW_LIBS} ${QT_LIBOPTS} ${OPENGL_LIBOPTS} ${X11_LIBOPTS} ${LIBS}"

BALL_LIBS="${BALL_LIBS} -L${BALL_PATH}/lib/${BINFMT} -lBALL ${LIBS}"
if test "${USE_BALLVIEW}" = true ; then
	BALLVIEW_INCLUDES="${BALL_INCLUDES} ${BALLVIEW_INCLUDES}"
	BALLVIEW_LIBS="-L${BALL_PATH}/lib/${BINFMT} -lMOLVIEW -lVIEW -lBALL ${BALLVIEW_LIBS}"
fi

LDFLAGS="$LDFLAGS"
LIBS="$BALL_LIBS"

CXXFLAGS="$CXXFLAGS $DEFINES"

if test "${HAS_GPLUSPLUS}" = false -a "${MAKEDEP_CXX_SUFFIX}" != "" ; then
	MAKEDEP_CXX_SUFFIX="|egrep \"(${BALL_PATH}|^[^/]*\$\$)\" ${MAKEDEP_CXX_SUFFIX}"
fi

MAKEDEP_CXX_OPTS="${MAKEDEP_CXX_OPTS} ${CXXFLAGS}"
if test ${CXX_MAKEDEPEND} = "makedepend" ; then
	MAKEDEP_CXX_OPTS="${MAKEDEP_CXX_OPTS} -- "
fi

if test "${AR}" = "no" ; then
	AR="ar"
fi
if test "${AROPTS}" = "" ; then
	AROPTS="cru"
fi

if test "${ADDITIONAL_COMMENTS}" = "" ; then
	ADDITIONAL_COMMENTS=""
fi
if test "${USE_BALLVIEW}" = true ; then
	ADDITIONAL_COMMENTS="${ADDITIONAL_COMMENTS}\n\nTo build the molecule viewer MolVIEW, please type\n\n   make molview\n\nThe executable is built in the directory APPLICATIONS/MOLVIEW\n"
fi

cat >>confdefs.h <<EOF
#define BALL_PATH "${BALL_PATH}"
EOF

cat >>confdefs.h <<EOF
#define BALL_BINFMT "${BINFMT}"
EOF

cat >>confdefs.h <<EOF
#define BALL_NULL_TEMPLATE_ARGS ${BALL_NULL_TEMPLATE_ARGS}
EOF

if test "$BALL_HAS_ANSI_IOSTREAM" = yes ; then
	cat >>confdefs.h <<\EOF
#define BALL_HAS_ANSI_IOSTREAM 1
EOF

fi

SUBDIRS="COMMON CONCEPT DATATYPE ENERGY FORMAT KERNEL MATHS MOLMEC NMR SOLVATION STRUCTURE SYSTEM ${BALLVIEW} ${PYTHON}"

if test "${MULTI_BUILD}" = "true" ; then
	echo "$as_me:10657: result: creating shadow directories..." >&5
echo "${ECHO_T}creating shadow directories..." >&6
	config/shadowsource.sh `pwd`"/${BINFMT}" `pwd` "${SUBDIRS} TEST BENCHMARKS EXAMPLES TUTORIAL APPLICATIONS"
	${RM} -fr `pwd`/${BINFMT}/TEST/data 2>/dev/null
	${RM} -fr `pwd`/${BINFMT}/BENCHMARKS/data 2>/dev/null
	${LN} -s `pwd`/TEST/data `pwd`/${BINFMT}/TEST 2>/dev/null
	${LN} -s `pwd`/TEST/runtests `pwd`/${BINFMT}/TEST 2>/dev/null
	${LN} -s `pwd`/BENCHMARKS/data `pwd`/${BINFMT}/BENCHMARKS 2>/dev/null
	${LN} -s `pwd`/BENCHMARKS/runbenchmarks `pwd`/${BINFMT}/BENCHMARKS 2>/dev/null

	${CP} config/Makefile.multiplatform Makefile

		BINFMT_PATH="/${BINFMT}"
else
	BINFMT_PATH=""
fi

ac_config_files="$ac_config_files Makefile.tmp:config/Makefile.in config.mak.tmp:config/config.mak.in common.mak.tmp:config/common.mak.in"
cat >confcache <<\_ACEOF
# This file is a shell script that caches the results of configure
# tests run on this system so they can be shared between configure
# scripts and configure runs, see configure's option --config-cache.
# It is not useful on other systems.  If it contains results you don't
# want to keep, you may remove or edit it.
#
# config.status only pays attention to the cache file if you give it
# the --recheck option to rerun configure.
#
# `ac_cv_env_foo' variables (set or unset) will be overriden when
# loading this file, other *unset* `ac_cv_foo' will be assigned the
# following values.

_ACEOF

# The following way of writing the cache mishandles newlines in values,
# but we know of no workaround that is simple, portable, and efficient.
# So, don't put newlines in cache variables' values.
# Ultrix sh set writes to stderr and can't be redirected directly,
# and sets the high bit in the cache file unless we assign to the vars.
{
  (set) 2>&1 |
    case `(ac_space=' '; set | grep ac_space) 2>&1` in
    *ac_space=\ *)
      # `set' does not quote correctly, so add quotes (double-quote
      # substitution turns \\\\ into \\, and sed turns \\ into \).
      sed -n \
        "s/'/'\\\\''/g;
    	  s/^\\([_$ac_cr_alnum]*_cv_[_$ac_cr_alnum]*\\)=\\(.*\\)/\\1='\\2'/p"
      ;;
    *)
      # `set' quotes correctly as required by POSIX, so do not add quotes.
      sed -n \
        "s/^\\([_$ac_cr_alnum]*_cv_[_$ac_cr_alnum]*\\)=\\(.*\\)/\\1=\\2/p"
      ;;
    esac;
} |
  sed '
     t clear
     : clear
     s/^\([^=]*\)=\(.*[{}].*\)$/test "${\1+set}" = set || &/
     t end
     /^ac_cv_env/!s/^\([^=]*\)=\(.*\)$/\1=${\1=\2}/
     : end' >>confcache
if cmp -s $cache_file confcache; then :; else
  if test -w $cache_file; then
    test "x$cache_file" != "x/dev/null" && echo "updating cache $cache_file"
    cat confcache >$cache_file
  else
    echo "not updating unwritable cache $cache_file"
  fi
fi
rm -f confcache

test "x$prefix" = xNONE && prefix=$ac_default_prefix
# Let make expand exec_prefix.
test "x$exec_prefix" = xNONE && exec_prefix='${prefix}'

# VPATH may cause trouble with some makes, so we remove $(srcdir),
# ${srcdir} and @srcdir@ from VPATH if srcdir is ".", strip leading and
# trailing colons and then remove the whole line if VPATH becomes empty
# (actually we leave an empty line to preserve line numbers).
if test "x$srcdir" = x.; then
  ac_vpsub='/^[ 	]*VPATH[ 	]*=/{
s/:*\$(srcdir):*/:/;
s/:*\${srcdir}:*/:/;
s/:*@srcdir@:*/:/;
s/^\([^=]*=[ 	]*\):*/\1/;
s/:*$//;
s/^[^=]*=[ 	]*$//;
}'
fi

DEFS=-DHAVE_CONFIG_H

: ${CONFIG_STATUS=./config.status}
ac_clean_files_save=$ac_clean_files
ac_clean_files="$ac_clean_files $CONFIG_STATUS"
{ echo "$as_me:10754: creating $CONFIG_STATUS" >&5
echo "$as_me: creating $CONFIG_STATUS" >&6;}
cat >$CONFIG_STATUS <<_ACEOF
#! $SHELL
# Generated automatically by configure.
# Run this file to recreate the current configuration.
# Compiler output produced by configure, useful for debugging
# configure, is in config.log if it exists.

debug=false
SHELL=\${CONFIG_SHELL-$SHELL}
ac_cs_invocation="\$0 \$@"

_ACEOF

cat >>$CONFIG_STATUS <<\_ACEOF
# Be Bourne compatible
if test -n "${ZSH_VERSION+set}" && (emulate sh) >/dev/null 2>&1; then
  emulate sh
  NULLCMD=:
elif test -n "${BASH_VERSION+set}" && (set -o posix) >/dev/null 2>&1; then
  set -o posix
fi

# Name of the executable.
as_me=`echo "$0" |sed 's,.*[\\/],,'`

if expr a : '\(a\)' >/dev/null 2>&1; then
  as_expr=expr
else
  as_expr=false
fi

rm -f conf$$ conf$$.exe conf$$.file
echo >conf$$.file
if ln -s conf$$.file conf$$ 2>/dev/null; then
  # We could just check for DJGPP; but this test a) works b) is more generic
  # and c) will remain valid once DJGPP supports symlinks (DJGPP 2.04).
  if test -f conf$$.exe; then
    # Don't use ln at all; we don't have any links
    as_ln_s='cp -p'
  else
    as_ln_s='ln -s'
  fi
elif ln conf$$.file conf$$ 2>/dev/null; then
  as_ln_s=ln
else
  as_ln_s='cp -p'
fi
rm -f conf$$ conf$$.exe conf$$.file

as_executable_p="test -f"

# Support unset when possible.
if (FOO=FOO; unset FOO) >/dev/null 2>&1; then
  as_unset=unset
else
  as_unset=false
fi

# NLS nuisances.
$as_unset LANG || test "${LANG+set}" != set || { LANG=C; export LANG; }
$as_unset LC_ALL || test "${LC_ALL+set}" != set || { LC_ALL=C; export LC_ALL; }
$as_unset LC_TIME || test "${LC_TIME+set}" != set || { LC_TIME=C; export LC_TIME; }
$as_unset LC_CTYPE || test "${LC_CTYPE+set}" != set || { LC_CTYPE=C; export LC_CTYPE; }
$as_unset LANGUAGE || test "${LANGUAGE+set}" != set || { LANGUAGE=C; export LANGUAGE; }
$as_unset LC_COLLATE || test "${LC_COLLATE+set}" != set || { LC_COLLATE=C; export LC_COLLATE; }
$as_unset LC_NUMERIC || test "${LC_NUMERIC+set}" != set || { LC_NUMERIC=C; export LC_NUMERIC; }
$as_unset LC_MESSAGES || test "${LC_MESSAGES+set}" != set || { LC_MESSAGES=C; export LC_MESSAGES; }

# IFS
# We need space, tab and new line, in precisely that order.
as_nl='
'
IFS=" 	$as_nl"

# CDPATH.
$as_unset CDPATH || test "${CDPATH+set}" != set || { CDPATH=:; export CDPATH; }

exec 6>&1

_ACEOF

# Files that config.status was made for.
if test -n "$ac_config_files"; then
  echo "config_files=\"$ac_config_files\"" >>$CONFIG_STATUS
fi

if test -n "$ac_config_headers"; then
  echo "config_headers=\"$ac_config_headers\"" >>$CONFIG_STATUS
fi

if test -n "$ac_config_links"; then
  echo "config_links=\"$ac_config_links\"" >>$CONFIG_STATUS
fi

if test -n "$ac_config_commands"; then
  echo "config_commands=\"$ac_config_commands\"" >>$CONFIG_STATUS
fi

cat >>$CONFIG_STATUS <<\EOF

ac_cs_usage="\
\`$as_me' instantiates files from templates according to the
current configuration.

Usage: $0 [OPTIONS] [FILE]...

  -h, --help       print this help, then exit
  -V, --version    print version number, then exit
  -d, --debug      don't remove temporary files
      --recheck    update $as_me by reconfiguring in the same conditions
  --file=FILE[:TEMPLATE]
                   instantiate the configuration file FILE
  --header=FILE[:TEMPLATE]
                   instantiate the configuration header FILE

Configuration files:
$config_files

Configuration headers:
$config_headers

Report bugs to <bug-autoconf@gnu.org>."
EOF

cat >>$CONFIG_STATUS <<EOF
ac_cs_version="\\
config.status
configured by $0, generated by GNU Autoconf 2.50,
  with options \\"`echo "$ac_configure_args" | sed 's/[\\""\`\$]/\\\\&/g'`\\"

Copyright 1992, 1993, 1994, 1995, 1996, 1998, 1999, 2000, 2001
Free Software Foundation, Inc.
This config.status script is free software; the Free Software Foundation
gives unlimited permission to copy, distribute and modify it."
srcdir=$srcdir
EOF

cat >>$CONFIG_STATUS <<\EOF
# If no file are specified by the user, then we need to provide default
# value.  By we need to know if files were specified by the user.
ac_need_defaults=:
while test $# != 0
do
  case $1 in
  --*=*)
    ac_option=`expr "x$1" : 'x\([^=]*\)='`
    ac_optarg=`expr "x$1" : 'x[^=]*=\(.*\)'`
    shift
    set dummy "$ac_option" "$ac_optarg" ${1+"$@"}
    shift
    ;;
  -*);;
  *) # This is not an option, so the user has probably given explicit
     # arguments.
     ac_need_defaults=false;;
  esac

  case $1 in
  # Handling of the options.
EOF
cat >>$CONFIG_STATUS <<EOF
  -recheck | --recheck | --rechec | --reche | --rech | --rec | --re | --r)
    echo "running $SHELL $0 " $ac_configure_args " --no-create --no-recursion"
    exec $SHELL $0 $ac_configure_args --no-create --no-recursion ;;
EOF
cat >>$CONFIG_STATUS <<\EOF
  --version | --vers* | -V )
    echo "$ac_cs_version"; exit 0 ;;
  --he | --h)
    # Conflict between --help and --header
    { { echo "$as_me:10926: error: ambiguous option: $1
Try \`$0 --help' for more information." >&5
echo "$as_me: error: ambiguous option: $1
Try \`$0 --help' for more information." >&2;}
   { (exit 1); exit 1; }; };;
  --help | --hel | -h )
    echo "$ac_cs_usage"; exit 0 ;;
  --debug | --d* | -d )
    debug=: ;;
  --file | --fil | --fi | --f )
    shift
    CONFIG_FILES="$CONFIG_FILES $1"
    ac_need_defaults=false;;
  --header | --heade | --head | --hea )
    shift
    CONFIG_HEADERS="$CONFIG_HEADERS $1"
    ac_need_defaults=false;;

  # Handling of arguments.
  'Makefile.tmp' ) CONFIG_FILES="$CONFIG_FILES Makefile.tmp:config/Makefile.in" ;;
  'config.mak.tmp' ) CONFIG_FILES="$CONFIG_FILES config.mak.tmp:config/config.mak.in" ;;
  'common.mak.tmp' ) CONFIG_FILES="$CONFIG_FILES common.mak.tmp:config/common.mak.in" ;;
  'config.h' ) CONFIG_HEADERS="$CONFIG_HEADERS config.h:config/config.h.in" ;;

  # This is an error.
  -*) { { echo "$as_me:10951: error: unrecognized option: $1
Try \`$0 --help' for more information." >&5
echo "$as_me: error: unrecognized option: $1
Try \`$0 --help' for more information." >&2;}
   { (exit 1); exit 1; }; } ;;
  *) { { echo "$as_me:10956: error: invalid argument: $1" >&5
echo "$as_me: error: invalid argument: $1" >&2;}
   { (exit 1); exit 1; }; };;
  esac
  shift
done

exec 5>>config.log
cat >&5 << _ACEOF

## ----------------------- ##
## Running config.status.  ##
## ----------------------- ##

This file was extended by $as_me 2.50, executed with
  > $ac_cs_invocation
on `(hostname || uname -n) 2>/dev/null | sed 1q`

_ACEOF
EOF

cat >>$CONFIG_STATUS <<\EOF
# If the user did not use the arguments to specify the items to instantiate,
# then the envvar interface is used.  Set only those that are not.
# We use the long form for the default assignment because of an extremely
# bizarre bug on SunOS 4.1.3.
if $ac_need_defaults; then
  test "${CONFIG_FILES+set}" = set || CONFIG_FILES=$config_files
  test "${CONFIG_HEADERS+set}" = set || CONFIG_HEADERS=$config_headers
fi

# Create a temporary directory, and hook for its removal unless debugging.
$debug ||
{
  trap 'exit_status=$?; rm -rf $tmp && exit $exit_status' 0
  trap '{ (exit $?); exit $?; }' 1 2 13 15
}

# Create a (secure) tmp directory for tmp files.
: ${TMPDIR=/tmp}
{
  tmp=`(umask 077 && mktemp -d -q "$TMPDIR/csXXXXXX") 2>/dev/null` &&
  test -n "$tmp" && test -d "$tmp"
}  ||
{
  tmp=$TMPDIR/cs$$-$RANDOM
  (umask 077 && mkdir $tmp)
} ||
{
   echo "$me: cannot create a temporary directory in $TMPDIR" >&2
   { (exit 1); exit 1; }
}

EOF

cat >>$CONFIG_STATUS <<EOF

#
# CONFIG_FILES section.
#

# No need to generate the scripts if there are no CONFIG_FILES.
# This happens for instance when ./config.status config.h
if test -n "\$CONFIG_FILES"; then
  # Protect against being on the right side of a sed subst in config.status.
  sed 's/,@/@@/; s/@,/@@/; s/,;t t\$/@;t t/; /@;t t\$/s/[\\\\&,]/\\\\&/g;
   s/@@/,@/; s/@@/@,/; s/@;t t\$/,;t t/' >\$tmp/subs.sed <<\\CEOF
s,@SHELL@,$SHELL,;t t
s,@exec_prefix@,$exec_prefix,;t t
s,@prefix@,$prefix,;t t
s,@program_transform_name@,$program_transform_name,;t t
s,@bindir@,$bindir,;t t
s,@sbindir@,$sbindir,;t t
s,@libexecdir@,$libexecdir,;t t
s,@datadir@,$datadir,;t t
s,@sysconfdir@,$sysconfdir,;t t
s,@sharedstatedir@,$sharedstatedir,;t t
s,@localstatedir@,$localstatedir,;t t
s,@libdir@,$libdir,;t t
s,@includedir@,$includedir,;t t
s,@oldincludedir@,$oldincludedir,;t t
s,@infodir@,$infodir,;t t
s,@mandir@,$mandir,;t t
s,@PACKAGE_NAME@,$PACKAGE_NAME,;t t
s,@PACKAGE_TARNAME@,$PACKAGE_TARNAME,;t t
s,@PACKAGE_VERSION@,$PACKAGE_VERSION,;t t
s,@PACKAGE_STRING@,$PACKAGE_STRING,;t t
s,@PACKAGE_BUGREPORT@,$PACKAGE_BUGREPORT,;t t
s,@ECHO_C@,$ECHO_C,;t t
s,@ECHO_N@,$ECHO_N,;t t
s,@ECHO_T@,$ECHO_T,;t t
s,@PATH_SEPARATOR@,$PATH_SEPARATOR,;t t
s,@DEFS@,$DEFS,;t t
s,@LIBS@,$LIBS,;t t
s,@PAGER@,$PAGER,;t t
s,@UNAME@,$UNAME,;t t
s,@CUT@,$CUT,;t t
s,@TR@,$TR,;t t
s,@AR@,$AR,;t t
s,@OSMAJOR@,$OSMAJOR,;t t
s,@OS@,$OS,;t t
s,@OSREV@,$OSREV,;t t
s,@BINFMT@,$BINFMT,;t t
s,@BINFMT_PATH@,$BINFMT_PATH,;t t
s,@ARCHITECTURE@,$ARCHITECTURE,;t t
s,@SHARED_LIB_SUFFIX@,$SHARED_LIB_SUFFIX,;t t
s,@LN_S@,$LN_S,;t t
s,@CP@,$CP,;t t
s,@RM@,$RM,;t t
s,@MV@,$MV,;t t
s,@LN@,$LN,;t t
s,@SED@,$SED,;t t
s,@FIND@,$FIND,;t t
s,@DIFF@,$DIFF,;t t
s,@TAIL@,$TAIL,;t t
s,@GREP@,$GREP,;t t
s,@CAT@,$CAT,;t t
s,@CXXPATH@,$CXXPATH,;t t
s,@CXX@,$CXX,;t t
s,@CXXFLAGS@,$CXXFLAGS,;t t
s,@LDFLAGS@,$LDFLAGS,;t t
s,@CPPFLAGS@,$CPPFLAGS,;t t
s,@ac_ct_CXX@,$ac_ct_CXX,;t t
s,@EXEEXT@,$EXEEXT,;t t
s,@OBJEXT@,$OBJEXT,;t t
s,@CXXCPP@,$CXXCPP,;t t
s,@BALL_HAS_XDR@,$BALL_HAS_XDR,;t t
s,@MOC@,$MOC,;t t
s,@SIP@,$SIP,;t t
s,@SIP_LIB@,$SIP_LIB,;t t
s,@SIP_INCLUDES@,$SIP_INCLUDES,;t t
s,@PYTHON_INCLUDES@,$PYTHON_INCLUDES,;t t
s,@PYTHON_SUPPORT@,$PYTHON_SUPPORT,;t t
s,@PYTHON_LIBS@,$PYTHON_LIBS,;t t
s,@BALL_PATH@,$BALL_PATH,;t t
s,@BALL_INCLUDES@,$BALL_INCLUDES,;t t
s,@CXX_MAKEDEPEND@,$CXX_MAKEDEPEND,;t t
s,@MAKEDEP_CXX_OPTS@,$MAKEDEP_CXX_OPTS,;t t
s,@MAKEDEP_CXX_SUFFIX@,$MAKEDEP_CXX_SUFFIX,;t t
s,@DEPENDENCY_GREPS@,$DEPENDENCY_GREPS,;t t
s,@ADD_CXXFLAGS@,$ADD_CXXFLAGS,;t t
s,@LIB_CXXFLAGS@,$LIB_CXXFLAGS,;t t
s,@NONLIB_CXXFLAGS@,$NONLIB_CXXFLAGS,;t t
s,@HAS_GPLUSPLUS@,$HAS_GPLUSPLUS,;t t
s,@STD_CPP_INCLUDES@,$STD_CPP_INCLUDES,;t t
s,@MAKEDEP_OPTS@,$MAKEDEP_OPTS,;t t
s,@BALL_TYPENAME@,$BALL_TYPENAME,;t t
s,@BINFMT_INDEX@,$BINFMT_INDEX,;t t
s,@BALLVIEW_LIBS@,$BALLVIEW_LIBS,;t t
s,@OPENGL_LIBOPTS@,$OPENGL_LIBOPTS,;t t
s,@OPENGL_LIBPATH@,$OPENGL_LIBPATH,;t t
s,@QT_LIBOPTS@,$QT_LIBOPTS,;t t
s,@QT_LIBPATH@,$QT_LIBPATH,;t t
s,@X11_LIBOPTS@,$X11_LIBOPTS,;t t
s,@X11_LIBPATH@,$X11_LIBPATH,;t t
s,@X11_LIBS@,$X11_LIBS,;t t
s,@BALL_LIBS@,$BALL_LIBS,;t t
s,@DEFINES@,$DEFINES,;t t
s,@CXXFLAGS_D@,$CXXFLAGS_D,;t t
s,@CXXFLAGS_O@,$CXXFLAGS_O,;t t
s,@CPP_MODE_FLAGS@,$CPP_MODE_FLAGS,;t t
s,@CPP_MODE_FLAGS_NO_OPTIMIZATION@,$CPP_MODE_FLAGS_NO_OPTIMIZATION,;t t
s,@AROPTS@,$AROPTS,;t t
s,@DYNAR@,$DYNAR,;t t
s,@DYNAROPTS@,$DYNAROPTS,;t t
s,@ADDITIONAL_COMMENTS@,$ADDITIONAL_COMMENTS,;t t
s,@USE_BALLVIEW@,$USE_BALLVIEW,;t t
s,@BALLVIEW_PLATFORM@,$BALLVIEW_PLATFORM,;t t
s,@BALLVIEW_INCLUDES@,$BALLVIEW_INCLUDES,;t t
s,@LIBBALLVIEW@,$LIBBALLVIEW,;t t
s,@TEMPLATE_DIR@,$TEMPLATE_DIR,;t t
s,@SUBDIRS@,$SUBDIRS,;t t
CEOF

EOF

  cat >>$CONFIG_STATUS <<\EOF
  # Split the substitutions into bite-sized pieces for seds with
  # small command number limits, like on Digital OSF/1 and HP-UX.
  ac_max_sed_lines=48
  ac_sed_frag=1 # Number of current file.
  ac_beg=1 # First line for current file.
  ac_end=$ac_max_sed_lines # Line after last line for current file.
  ac_more_lines=:
  ac_sed_cmds=
  while $ac_more_lines; do
    if test $ac_beg -gt 1; then
      sed "1,${ac_beg}d; ${ac_end}q" $tmp/subs.sed >$tmp/subs.frag
    else
      sed "${ac_end}q" $tmp/subs.sed >$tmp/subs.frag
    fi
    if test ! -s $tmp/subs.frag; then
      ac_more_lines=false
    else
      # The purpose of the label and of the branching condition is to
      # speed up the sed processing (if there are no `@' at all, there
      # is no need to browse any of the substitutions).
      # These are the two extra sed commands mentioned above.
      (echo ':t
  /@[a-zA-Z_][a-zA-Z_0-9]*@/!b' && cat $tmp/subs.frag) >$tmp/subs-$ac_sed_frag.sed
      if test -z "$ac_sed_cmds"; then
  	ac_sed_cmds="sed -f $tmp/subs-$ac_sed_frag.sed"
      else
  	ac_sed_cmds="$ac_sed_cmds | sed -f $tmp/subs-$ac_sed_frag.sed"
      fi
      ac_sed_frag=`expr $ac_sed_frag + 1`
      ac_beg=$ac_end
      ac_end=`expr $ac_end + $ac_max_sed_lines`
    fi
  done
  if test -z "$ac_sed_cmds"; then
    ac_sed_cmds=cat
  fi
fi # test -n "$CONFIG_FILES"

EOF
cat >>$CONFIG_STATUS <<\EOF
for ac_file in : $CONFIG_FILES; do test "x$ac_file" = x: && continue
  # Support "outfile[:infile[:infile...]]", defaulting infile="outfile.in".
  case $ac_file in
  - | *:- | *:-:* ) # input from stdin
        cat >$tmp/stdin
        ac_file_in=`echo "$ac_file" | sed 's,[^:]*:,,'`
        ac_file=`echo "$ac_file" | sed 's,:.*,,'` ;;
  *:* ) ac_file_in=`echo "$ac_file" | sed 's,[^:]*:,,'`
        ac_file=`echo "$ac_file" | sed 's,:.*,,'` ;;
  * )   ac_file_in=$ac_file.in ;;
  esac

  # Compute @srcdir@, @top_srcdir@, and @INSTALL@ for subdirectories.
  ac_dir=`$as_expr X"$ac_file" : 'X\(.*[^/]\)//*[^/][^/]*/*$' \| \
         X"$ac_file" : 'X\(//\)[^/]' \| \
         X"$ac_file" : 'X\(//\)$' \| \
         X"$ac_file" : 'X\(/\)' \| \
         .     : '\(.\)' 2>/dev/null ||
echo X"$ac_file" |
    sed '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{ s//\1/; q; }
  	  /^X\(\/\/\)[^/].*/{ s//\1/; q; }
  	  /^X\(\/\/\)$/{ s//\1/; q; }
  	  /^X\(\/\).*/{ s//\1/; q; }
  	  s/.*/./; q'`
  if test "$ac_dir" != "$ac_file" && test "$ac_dir" != .; then
    { case "$ac_dir" in
  [\\/]* | ?:[\\/]* ) as_incr_dir=;;
  *)                      as_incr_dir=.;;
esac
as_dummy="$ac_dir"
for as_mkdir_dir in `IFS='/\\'; set X $as_dummy; shift; echo "$@"`; do
  case $as_mkdir_dir in
    # Skip DOS drivespec
    ?:) as_incr_dir=$as_mkdir_dir ;;
    *)
      as_incr_dir=$as_incr_dir/$as_mkdir_dir
      test -d "$as_incr_dir" || mkdir "$as_incr_dir"
    ;;
  esac
done; }

    ac_dir_suffix="/`echo $ac_dir|sed 's,^\./,,'`"
    # A "../" for each directory in $ac_dir_suffix.
    ac_dots=`echo "$ac_dir_suffix" | sed 's,/[^/]*,../,g'`
  else
    ac_dir_suffix= ac_dots=
  fi

  case $srcdir in
  .)  ac_srcdir=.
      if test -z "$ac_dots"; then
         ac_top_srcdir=.
      else
         ac_top_srcdir=`echo $ac_dots | sed 's,/$,,'`
      fi ;;
  [\\/]* | ?:[\\/]* )
      ac_srcdir=$srcdir$ac_dir_suffix;
      ac_top_srcdir=$srcdir ;;
  *) # Relative path.
    ac_srcdir=$ac_dots$srcdir$ac_dir_suffix
    ac_top_srcdir=$ac_dots$srcdir ;;
  esac

  if test x"$ac_file" != x-; then
    { echo "$as_me:11237: creating $ac_file" >&5
echo "$as_me: creating $ac_file" >&6;}
    rm -f "$ac_file"
  fi
  # Let's still pretend it is `configure' which instantiates (i.e., don't
  # use $as_me), people would be surprised to read:
  #    /* config.h.  Generated automatically by config.status.  */
  configure_input="Generated automatically from `echo $ac_file_in |
                                                 sed 's,.*/,,'` by configure."

  # First look for the input files in the build tree, otherwise in the
  # src tree.
  ac_file_inputs=`IFS=:
    for f in $ac_file_in; do
      case $f in
      -) echo $tmp/stdin ;;
      [\\/$]*)
         # Absolute (can't be DOS-style, as IFS=:)
         test -f "$f" || { { echo "$as_me:11255: error: cannot find input file: $f" >&5
echo "$as_me: error: cannot find input file: $f" >&2;}
   { (exit 1); exit 1; }; }
         echo $f;;
      *) # Relative
         if test -f "$f"; then
           # Build tree
           echo $f
         elif test -f "$srcdir/$f"; then
           # Source tree
           echo $srcdir/$f
         else
           # /dev/null tree
           { { echo "$as_me:11268: error: cannot find input file: $f" >&5
echo "$as_me: error: cannot find input file: $f" >&2;}
   { (exit 1); exit 1; }; }
         fi;;
      esac
    done` || { (exit 1); exit 1; }
EOF
cat >>$CONFIG_STATUS <<EOF
  sed "$ac_vpsub
$extrasub
EOF
cat >>$CONFIG_STATUS <<\EOF
:t
/@[a-zA-Z_][a-zA-Z_0-9]*@/!b
s,@configure_input@,$configure_input,;t t
s,@srcdir@,$ac_srcdir,;t t
s,@top_srcdir@,$ac_top_srcdir,;t t
" $ac_file_inputs | (eval "$ac_sed_cmds") >$tmp/out
  rm -f $tmp/stdin
  if test x"$ac_file" != x-; then
    mv $tmp/out $ac_file
  else
    cat $tmp/out
    rm -f $tmp/out
  fi

done
EOF
cat >>$CONFIG_STATUS <<\EOF

#
# CONFIG_HEADER section.
#

# These sed commands are passed to sed as "A NAME B NAME C VALUE D", where
# NAME is the cpp macro being defined and VALUE is the value it is being given.
#
# ac_d sets the value in "#define NAME VALUE" lines.
ac_dA='s,^\([ 	]*\)#\([ 	]*define[ 	][ 	]*\)'
ac_dB='[ 	].*$,\1#\2'
ac_dC=' '
ac_dD=',;t'
# ac_u turns "#undef NAME" without trailing blanks into "#define NAME VALUE".
ac_uA='s,^\([ 	]*\)#\([ 	]*\)undef\([ 	][ 	]*\)'
ac_uB='$,\1#\2define\3'
ac_uC=' '
ac_uD=',;t'

for ac_file in : $CONFIG_HEADERS; do test "x$ac_file" = x: && continue
  # Support "outfile[:infile[:infile...]]", defaulting infile="outfile.in".
  case $ac_file in
  - | *:- | *:-:* ) # input from stdin
        cat >$tmp/stdin
        ac_file_in=`echo "$ac_file" | sed 's,[^:]*:,,'`
        ac_file=`echo "$ac_file" | sed 's,:.*,,'` ;;
  *:* ) ac_file_in=`echo "$ac_file" | sed 's,[^:]*:,,'`
        ac_file=`echo "$ac_file" | sed 's,:.*,,'` ;;
  * )   ac_file_in=$ac_file.in ;;
  esac

  test x"$ac_file" != x- && { echo "$as_me:11328: creating $ac_file" >&5
echo "$as_me: creating $ac_file" >&6;}

  # First look for the input files in the build tree, otherwise in the
  # src tree.
  ac_file_inputs=`IFS=:
    for f in $ac_file_in; do
      case $f in
      -) echo $tmp/stdin ;;
      [\\/$]*)
         # Absolute (can't be DOS-style, as IFS=:)
         test -f "$f" || { { echo "$as_me:11339: error: cannot find input file: $f" >&5
echo "$as_me: error: cannot find input file: $f" >&2;}
   { (exit 1); exit 1; }; }
         echo $f;;
      *) # Relative
         if test -f "$f"; then
           # Build tree
           echo $f
         elif test -f "$srcdir/$f"; then
           # Source tree
           echo $srcdir/$f
         else
           # /dev/null tree
           { { echo "$as_me:11352: error: cannot find input file: $f" >&5
echo "$as_me: error: cannot find input file: $f" >&2;}
   { (exit 1); exit 1; }; }
         fi;;
      esac
    done` || { (exit 1); exit 1; }
  # Remove the trailing spaces.
  sed 's/[ 	]*$//' $ac_file_inputs >$tmp/in

EOF

# Transform confdefs.h into two sed scripts, `conftest.defines' and
# `conftest.undefs', that substitutes the proper values into
# config.h.in to produce config.h.  The first handles `#define'
# templates, and the second `#undef' templates.
# And first: Protect against being on the right side of a sed subst in
# config.status.  Protect against being in an unquoted here document
# in config.status.
rm -f conftest.defines conftest.undefs
# Using a here document instead of a string reduces the quoting nightmare.
# Putting comments in sed scripts is not portable.
#
# `end' is used to avoid that the second main sed command (meant for
# 0-ary CPP macros) applies to n-ary macro definitions.
# See the Autoconf documentation for `clear'.
cat >confdef2sed.sed <<\EOF
s/[\\&,]/\\&/g
s,[\\$`],\\&,g
t clear
: clear
s,^[ 	]*#[ 	]*define[ 	][ 	]*\(\([^ 	(][^ 	(]*\)([^)]*)\)[ 	]*\(.*\)$,${ac_dA}\2${ac_dB}\1${ac_dC}\3${ac_dD},gp
t end
s,^[ 	]*#[ 	]*define[ 	][ 	]*\([^ 	][^ 	]*\)[ 	]*\(.*\)$,${ac_dA}\1${ac_dB}\1${ac_dC}\2${ac_dD},gp
: end
EOF
# If some macros were called several times there might be several times
# the same #defines, which is useless.  Nevertheless, we may not want to
# sort them, since we want the *last* AC-DEFINE to be honored.
uniq confdefs.h | sed -n -f confdef2sed.sed >conftest.defines
sed 's/ac_d/ac_u/g' conftest.defines >conftest.undefs
rm -f confdef2sed.sed

# This sed command replaces #undef with comments.  This is necessary, for
# example, in the case of _POSIX_SOURCE, which is predefined and required
# on some systems where configure will not decide to define it.
cat >>conftest.undefs <<\EOF
s,^[ 	]*#[ 	]*undef[ 	][ 	]*[a-zA-Z_][a-zA-Z_0-9]*,/* & */,
EOF

# Break up conftest.defines because some shells have a limit on the size
# of here documents, and old seds have small limits too (100 cmds).
echo '  # Handle all the #define templates only if necessary.' >>$CONFIG_STATUS
echo '  if egrep "^[ 	]*#[ 	]*define" $tmp/in >/dev/null; then' >>$CONFIG_STATUS
echo '  # If there are no defines, we may have an empty if/fi' >>$CONFIG_STATUS
echo '  :' >>$CONFIG_STATUS
rm -f conftest.tail
while grep . conftest.defines >/dev/null
do
  # Write a limited-size here document to $tmp/defines.sed.
  echo '  cat >$tmp/defines.sed <<CEOF' >>$CONFIG_STATUS
  # Speed up: don't consider the non `#define' lines.
  echo '/^[ 	]*#[ 	]*define/!b' >>$CONFIG_STATUS
  # Work around the forget-to-reset-the-flag bug.
  echo 't clr' >>$CONFIG_STATUS
  echo ': clr' >>$CONFIG_STATUS
  sed ${ac_max_here_lines}q conftest.defines >>$CONFIG_STATUS
  echo 'CEOF
  sed -f $tmp/defines.sed $tmp/in >$tmp/out
  rm -f $tmp/in
  mv $tmp/out $tmp/in
' >>$CONFIG_STATUS
  sed 1,${ac_max_here_lines}d conftest.defines >conftest.tail
  rm -f conftest.defines
  mv conftest.tail conftest.defines
done
rm -f conftest.defines
echo '  fi # egrep' >>$CONFIG_STATUS
echo >>$CONFIG_STATUS

# Break up conftest.undefs because some shells have a limit on the size
# of here documents, and old seds have small limits too (100 cmds).
echo '  # Handle all the #undef templates' >>$CONFIG_STATUS
rm -f conftest.tail
while grep . conftest.undefs >/dev/null
do
  # Write a limited-size here document to $tmp/undefs.sed.
  echo '  cat >$tmp/undefs.sed <<CEOF' >>$CONFIG_STATUS
  # Speed up: don't consider the non `#undef'
  echo '/^[ 	]*#[ 	]*undef/!b' >>$CONFIG_STATUS
  # Work around the forget-to-reset-the-flag bug.
  echo 't clr' >>$CONFIG_STATUS
  echo ': clr' >>$CONFIG_STATUS
  sed ${ac_max_here_lines}q conftest.undefs >>$CONFIG_STATUS
  echo 'CEOF
  sed -f $tmp/undefs.sed $tmp/in >$tmp/out
  rm -f $tmp/in
  mv $tmp/out $tmp/in
' >>$CONFIG_STATUS
  sed 1,${ac_max_here_lines}d conftest.undefs >conftest.tail
  rm -f conftest.undefs
  mv conftest.tail conftest.undefs
done
rm -f conftest.undefs

cat >>$CONFIG_STATUS <<\EOF
  # Let's still pretend it is `configure' which instantiates (i.e., don't
  # use $as_me), people would be surprised to read:
  #    /* config.h.  Generated automatically by config.status.  */
  if test x"$ac_file" = x-; then
    echo "/* Generated automatically by configure.  */" >$tmp/config.h
  else
    echo "/* $ac_file.  Generated automatically by configure.  */" >$tmp/config.h
  fi
  cat $tmp/in >>$tmp/config.h
  rm -f $tmp/in
  if test x"$ac_file" != x-; then
    if cmp -s $ac_file $tmp/config.h 2>/dev/null; then
      { echo "$as_me:11469: $ac_file is unchanged" >&5
echo "$as_me: $ac_file is unchanged" >&6;}
    else
      ac_dir=`$as_expr X"$ac_file" : 'X\(.*[^/]\)//*[^/][^/]*/*$' \| \
         X"$ac_file" : 'X\(//\)[^/]' \| \
         X"$ac_file" : 'X\(//\)$' \| \
         X"$ac_file" : 'X\(/\)' \| \
         .     : '\(.\)' 2>/dev/null ||
echo X"$ac_file" |
    sed '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{ s//\1/; q; }
  	  /^X\(\/\/\)[^/].*/{ s//\1/; q; }
  	  /^X\(\/\/\)$/{ s//\1/; q; }
  	  /^X\(\/\).*/{ s//\1/; q; }
  	  s/.*/./; q'`
      if test "$ac_dir" != "$ac_file" && test "$ac_dir" != .; then
        { case "$ac_dir" in
  [\\/]* | ?:[\\/]* ) as_incr_dir=;;
  *)                      as_incr_dir=.;;
esac
as_dummy="$ac_dir"
for as_mkdir_dir in `IFS='/\\'; set X $as_dummy; shift; echo "$@"`; do
  case $as_mkdir_dir in
    # Skip DOS drivespec
    ?:) as_incr_dir=$as_mkdir_dir ;;
    *)
      as_incr_dir=$as_incr_dir/$as_mkdir_dir
      test -d "$as_incr_dir" || mkdir "$as_incr_dir"
    ;;
  esac
done; }

      fi
      rm -f $ac_file
      mv $tmp/config.h $ac_file
    fi
  else
    cat $tmp/config.h
    rm -f $tmp/config.h
  fi
done
EOF

cat >>$CONFIG_STATUS <<\EOF

{ (exit 0); exit 0; }
EOF
chmod +x $CONFIG_STATUS
ac_clean_files=$ac_clean_files_save

# configure is writing to config.log, and then calls config.status.
# config.status does its own redirection, appending to config.log.
# Unfortunately, on DOS this fails, as config.log is still kept open
# by configure, so config.status won't be able to write to it; its
# output is simply discarded.  So we exec the FD to /dev/null,
# effectively closing config.log, so it can be properly (re)opened and
# appended to by config.status.  When coming back to configure, we
# need to make the FD available again.
if test "$no_create" != yes; then
  ac_cs_success=:
  exec 5>/dev/null
  $SHELL $CONFIG_STATUS || ac_cs_success=false
  exec 5>>config.log
  # Use ||, not &&, to avoid exiting from the if with $? = 1, which
  # would make configure fail if this is the last instruction.
  $ac_cs_success || { (exit 1); exit 1; }
fi

if test "${MULTI_BUILD}" = "true" ; then
	${MV} Makefile.tmp ${BINFMT}/Makefile
	${MV} common.mak.tmp ${BINFMT}/common.mak
	${MV} config.mak.tmp ${BINFMT}/config.mak
else
	${MV} Makefile.tmp Makefile
	${MV} common.mak.tmp common.mak
	${MV} config.mak.tmp config.mak
fi

if test "${MULTI_BUILD}" = "true" ; then
	mkdir ${BALL_PATH}/include/BALL/CONFIG 2>/dev/null
	${MV} -f config.h $BALL_PATH/include/BALL/CONFIG/config.h.${BINFMT}
else
					if test -f $BALL_PATH/include/BALL/CONFIG/config.h ; then
		if test "`${DIFF} config.h $BALL_PATH/include/BALL/CONFIG/config.h`" != "" ; then
			${MV} -f config.h $BALL_PATH/include/BALL/CONFIG/config.h
		fi
	else
										mkdir ${BALL_PATH}/include/BALL/CONFIG 2>/dev/null
		${MV} -f config.h $BALL_PATH/include/BALL/CONFIG/config.h
	fi
fi

if test "${MULTI_BUILD}" = "true" ; then
	${RM}  ${BINFMT}/.Dependencies 2>/dev/null
	${RM}  ${BINFMT}/lib*.objects 2>/dev/null
else
	${RM}  .Dependencies 2>/dev/null
	${RM}  lib*.objects 2>/dev/null
fi

echo "$as_me:11568: result: " >&5
echo "${ECHO_T}" >&6
echo "$as_me:11570: result: " >&5
echo "${ECHO_T}" >&6
echo "$as_me:11572: result: Your Makefiles have been created. If everything went OK: just type" >&5
echo "${ECHO_T}Your Makefiles have been created. If everything went OK: just type" >&6
echo "$as_me:11574: result: " >&5
echo "${ECHO_T}" >&6
echo "$as_me:11576: result: make" >&5
echo "${ECHO_T}make" >&6
echo "$as_me:11578: result: " >&5
echo "${ECHO_T}" >&6
echo "$as_me:11580: result: to build BALL." >&5
echo "${ECHO_T}to build BALL." >&6
echo "$as_me:11582: result: " >&5
echo "${ECHO_T}" >&6
