---
phase: 05.1-build-warnings-and-latent-bugs
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - source/VIEW/KERNEL/representation.C
autonomous: true
requirements: []
must_haves:
  truths:
    - "The DrawingPrecision half of the bounds check at representation.C:~269-270 still catches DRAWING_PRECISION_INVALID = -1 (preserves the meaningful invariant)"
    - "The DrawingMode < 0 half of the check is dropped (or made meaningful by introducing DrawingMode::INVALID = -1) — whichever the executor chooses while reading the enum"
    - "clang emits zero `-Wtautological-constant-out-of-range-compare` warnings on representation.C in the next CI run"
    - "Build remains green on all three CI OSes (macOS / Linux / Windows) on the post-fix commit"
  artifacts:
    - path: "source/VIEW/KERNEL/representation.C"
      provides: "an asymmetric bounds check that preserves the DrawingPrecision invariant and drops the tautological DrawingMode < 0 half (or, if the executor introduces DrawingMode::INVALID = -1, a fully symmetric check survives)"
  key_links:
    - from: "representation.C:~269-270"
      to: "the DrawingPrecision / DrawingMode enum definitions in include/BALL/VIEW/KERNEL/"
      via: "value-range validation at representation construction / draw time"
      pattern: "drawing_precision_ < 0|drawing_mode_ < 0"
---

<objective>
Fix BACKLOG.md Task **A4** — `-Wtautological-constant-out-of-range-compare` in
`source/VIEW/KERNEL/representation.C:269-270`.

**Codex correction (CONTEXT.md D-09):** the two halves of this check are NOT symmetric.
`DrawingPrecision` has `DRAWING_PRECISION_INVALID = -1` so its `< 0` half MEANINGFULLY
catches the invalid sentinel — preserve it. `DrawingMode` has only non-negative
enumerators so its `< 0` half IS tautological — drop it.

The cleaner alternative — adding `INVALID = -1` to `DrawingMode` to make the symmetric
check survive — is in scope and is the preferred path if reading the enum definitions
during implementation suggests it is feasible without callsite churn.

Codex-verified with scope correction. Severity: code quality (no behavioural impact from
the tautological half; the meaningful half MUST be preserved).

Purpose: BACKLOG.md task A4.
Output: 1-line or 2-line edit in `representation.C`, optionally plus an `INVALID = -1`
enumerator addition to `DrawingMode`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-build-warnings-and-latent-bugs/05.1-CONTEXT.md
@.planning/phases/05.1-build-warnings-and-latent-bugs/05.1-BACKLOG.md

<interfaces>
<!-- The two enums whose value ranges representation.C checks. -->

DrawingPrecision (in include/BALL/VIEW/KERNEL/representation.h — verify exact location):
  // Has an explicit invalid sentinel = -1. The `< 0` check is meaningful.
  enum DrawingPrecision {
    DRAWING_PRECISION_INVALID = -1,
    DRAWING_PRECISION_LOW = 0,
    ...
  };

DrawingMode (in include/BALL/VIEW/KERNEL/representation.h — verify exact location):
  // Only non-negative enumerators. The `< 0` check is tautological — clang flags it.
  enum DrawingMode {
    DRAWING_MODE_DOTS = 0,
    DRAWING_MODE_WIREFRAME,
    ...
    // No INVALID = -1 sentinel — adding one is option B of this plan.
  };
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the bounds check asymmetric — preserve the DrawingPrecision invariant, drop the tautological DrawingMode half</name>
  <files>source/VIEW/KERNEL/representation.C</files>
  <read_first>
    - source/VIEW/KERNEL/representation.C (the file being modified — read around lines 260-280 for the full conditional)
    - include/BALL/VIEW/KERNEL/representation.h (find the `DrawingPrecision` and `DrawingMode` enum definitions; confirm `DRAWING_PRECISION_INVALID = -1` is present and `DrawingMode` has only non-negative values)
    - .planning/phases/05.1-build-warnings-and-latent-bugs/05.1-BACKLOG.md §"Task A4: -Wtautological-constant-out-of-range-compare" (the Codex-corrected scope)
    - .planning/phases/05.1-build-warnings-and-latent-bugs/05.1-CONTEXT.md §"Verification" D-09 (the asymmetric-preservation decision)
  </read_first>
  <action>
    Read both enum definitions in `include/BALL/VIEW/KERNEL/representation.h` first to
    confirm the asymmetry the BACKLOG describes (DrawingPrecision has `INVALID = -1`,
    DrawingMode does not).

    **Option A — asymmetric drop** (default, mechanically smallest):
    At `source/VIEW/KERNEL/representation.C:~269-270`, change

        if (drawing_precision_ < 0 || drawing_precision_ > BALL_VIEW_MAXIMAL_DRAWING_PRECISION ||
            drawing_mode_      < 0 || drawing_mode_      > BALL_VIEW_MAXIMAL_DRAWING_MODE)

    to

        if (drawing_precision_ < 0 || drawing_precision_ > BALL_VIEW_MAXIMAL_DRAWING_PRECISION ||
            drawing_mode_      > BALL_VIEW_MAXIMAL_DRAWING_MODE)

    Add a one-line `// drawing_mode_ has no negative sentinel; < 0 was tautological per
    `-Wtautological-constant-out-of-range-compare`` comment above the changed line.

    **Option B — symmetric restoration** (cleaner, choose ONLY if reading the enum
    definition shows that adding `DRAWING_MODE_INVALID = -1` won't break callsites that
    iterate enumerators sequentially from 0):
    Add `DRAWING_MODE_INVALID = -1,` as the first enumerator in `DrawingMode` (mirroring
    `DRAWING_PRECISION_INVALID`) and leave the conditional as-is. This restores symmetry.
    Requires the additional file edit on `include/BALL/VIEW/KERNEL/representation.h`
    (declare its `files_modified` add in the SUMMARY if you go this route).

    Decision criterion: if `DrawingMode` is iterated via `for (int i = 0; i < N; ++i)`
    or used as an array index anywhere, prefer Option A. If `DrawingMode` is only ever
    compared / switched against named enumerators, Option B is the cleaner long-term
    fix. Record the chosen option and rationale in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && grep -nE "drawing_mode_\s*<\s*0" source/VIEW/KERNEL/representation.C; OPT_A_REMOVED=$?; grep -nE "DRAWING_MODE_INVALID\s*=\s*-1" include/BALL/VIEW/KERNEL/representation.h; OPT_B_ADDED=$?; if [ $OPT_A_REMOVED -ne 0 ] || [ $OPT_B_ADDED -eq 0 ]; then echo "OK: either drawing_mode_<0 dropped, or DRAWING_MODE_INVALID added"; else echo "FAIL: neither option applied"; exit 1; fi; grep -nE "drawing_precision_\s*<\s*0" source/VIEW/KERNEL/representation.C && echo "OK: DrawingPrecision invariant preserved"</automated>
  </verify>
  <acceptance_criteria>
    - **Source assertion 1 (DrawingPrecision invariant preserved):** `grep -nE "drawing_precision_\s*<\s*0" source/VIEW/KERNEL/representation.C` returns a match (the meaningful half stays).
    - **Source assertion 2 (one of the two options applied):** EITHER `grep -nE "drawing_mode_\s*<\s*0" source/VIEW/KERNEL/representation.C` returns NO match (Option A: tautological half dropped) OR `grep -nE "DRAWING_MODE_INVALID\s*=\s*-1" include/BALL/VIEW/KERNEL/representation.h` returns a match (Option B: sentinel added).
    - **CI assertion:** `gh run view <id> --log | grep -E "representation\.C.*tautological-constant-out-of-range-compare" | wc -l` reports `0` on the macOS clang job.
    - **CI assertion (no regression):** all three OS jobs report `success`.
    - **SUMMARY assertion:** the SUMMARY records which option (A or B) was chosen and the rationale (per D-09).
  </acceptance_criteria>
  <done>The DrawingPrecision invariant is preserved; the tautological DrawingMode half is either dropped (A) or restored as meaningful via a new sentinel (B); clang emits zero `-Wtautological-constant-out-of-range-compare` on `representation.C`; tri-OS CI remains green.</done>
</task>

</tasks>

<threat_model>
No new attack surface — code-quality fix in a value-range validation check; behaviour preserved on all legal enum values.
</threat_model>

<verification>
- Push the branch, trigger ci.yml, watch the run.
- Assert zero `-Wtautological-constant-out-of-range-compare` on `representation.C` in the macOS clang log.
- Assert all three OS jobs report `success`.
- Plan SUMMARY records the A-vs-B decision.
</verification>

<success_criteria>
- The DrawingPrecision `< 0` invariant survives.
- The DrawingMode tautological half is resolved (drop or sentinel).
- clang `-Wtautological-constant-out-of-range-compare` count on representation.C drops to 0 in CI.
- No tri-OS CI regression.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-build-warnings-and-latent-bugs/05.1-04-SUMMARY.md` recording the chosen option (A or B), the rationale, and the post-fix CI run number.
</output>
