---
phase: 05.1-build-warnings-and-latent-bugs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - source/SOLVATION/pairExpInteractionEnergyProcessor.C
autonomous: true
requirements: []
must_haves:
  truths:
    - "The copy-assignment operator in pairExpInteractionEnergyProcessor.C correctly propagates rdf_parameter_ from the source object (proc.rdf_parameter_) instead of self-assigning"
    - "clang emits zero `-Wself-assign-field` warnings on pairExpInteractionEnergyProcessor.C in the next CI run"
    - "Build remains green on all three CI OSes (macOS / Linux / Windows) on the post-fix commit"
  artifacts:
    - path: "source/SOLVATION/pairExpInteractionEnergyProcessor.C"
      provides: "operator= that propagates rdf_parameter_ from the source — matches the copy ctor's existing initialization order"
      contains: "rdf_parameter_ = proc.rdf_parameter_"
  key_links:
    - from: "pairExpInteractionEnergyProcessor.C:~124 (operator=)"
      to: "the source object's rdf_parameter_ field"
      via: "proc.rdf_parameter_ rvalue read on the rhs"
      pattern: "rdf_parameter_ = proc\\.rdf_parameter_"
---

<objective>
Fix BACKLOG.md Task **A3** — `-Wself-assign-field` in
`source/SOLVATION/pairExpInteractionEnergyProcessor.C:124`.

The copy-assignment operator contains the line `rdf_parameter_ = rdf_parameter_;` —
a copy-paste error. The copy *constructor* in the same file correctly initializes
`rdf_parameter_(proc.rdf_parameter_)`, so the field-propagation pattern is already
established; the operator= just needs the `proc.` prefix.

Codex-verified. Severity: real bug — copy-assignment doesn't propagate the field. Has
existed since the file was written; surfaced now because clang's `-Wself-assign-field`
is a newer diagnostic.

Purpose: BACKLOG.md task A3.
Output: 1-line edit in `source/SOLVATION/pairExpInteractionEnergyProcessor.C`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-build-warnings-and-latent-bugs/05.1-CONTEXT.md
@.planning/phases/05.1-build-warnings-and-latent-bugs/05.1-BACKLOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Propagate rdf_parameter_ from the source in operator=</name>
  <files>source/SOLVATION/pairExpInteractionEnergyProcessor.C</files>
  <read_first>
    - source/SOLVATION/pairExpInteractionEnergyProcessor.C (the file being modified — read around line 124 to see the broken operator= AND the copy ctor a few lines earlier that already does it correctly via member-initializer list)
    - .planning/phases/05.1-build-warnings-and-latent-bugs/05.1-BACKLOG.md §"Task A3: -Wself-assign-field" (authoritative fix proposal)
  </read_first>
  <action>
    Edit `source/SOLVATION/pairExpInteractionEnergyProcessor.C`. Around line 124, in the
    copy-assignment operator (`operator=`), change the line

        rdf_parameter_ = rdf_parameter_;

    to

        rdf_parameter_ = proc.rdf_parameter_;

    The function-parameter name on the RHS of `=` must match the parameter name in the
    same operator= signature — the copy ctor in this same file already does this
    correctly via its member-initializer list `rdf_parameter_(proc.rdf_parameter_)`, so
    the parameter is named `proc` (verify by reading the operator= signature; if BALL
    uses a different conventional parameter name in this method, use that — but the BACKLOG
    fix proposal documents the parameter as `proc`).

    Pure body edit. No header changes. No ABI / link-compat impact.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && grep -nE "^\s*rdf_parameter_\s*=\s*rdf_parameter_\s*;" source/SOLVATION/pairExpInteractionEnergyProcessor.C; test $? -ne 0 && grep -nE "rdf_parameter_\s*=\s*proc\.rdf_parameter_\s*;" source/SOLVATION/pairExpInteractionEnergyProcessor.C && echo "OK: self-assign removed, propagating from proc"</automated>
  </verify>
  <acceptance_criteria>
    - **Source assertion 1:** `grep -nE "^\s*rdf_parameter_\s*=\s*rdf_parameter_\s*;" source/SOLVATION/pairExpInteractionEnergyProcessor.C` returns NO match.
    - **Source assertion 2:** `grep -nE "rdf_parameter_\s*=\s*proc\.rdf_parameter_\s*;" source/SOLVATION/pairExpInteractionEnergyProcessor.C` returns exactly one match.
    - **CI assertion:** `gh run view <id> --log | grep -E "pairExpInteractionEnergyProcessor\.C.*-Wself-assign-field" | wc -l` reports `0` on the macOS clang job.
    - **CI assertion (no regression):** all three OS jobs report `success`.
  </acceptance_criteria>
  <done>operator= propagates rdf_parameter_ from the source; clang emits zero `-Wself-assign-field` on this file; tri-OS CI remains green.</done>
</task>

</tasks>

<threat_model>
No new attack surface — internal correctness fix. Restores a long-broken field propagation.
</threat_model>

<verification>
- Push the branch, trigger ci.yml, watch the run.
- Assert zero `-Wself-assign-field` on `pairExpInteractionEnergyProcessor.C` in the macOS clang log.
- Assert all three OS jobs report `success`.
</verification>

<success_criteria>
- One-line fix lands.
- clang `-Wself-assign-field` count on this file drops to 0 in CI.
- No tri-OS CI regression.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-build-warnings-and-latent-bugs/05.1-03-SUMMARY.md` recording the post-fix CI run number.
</output>
