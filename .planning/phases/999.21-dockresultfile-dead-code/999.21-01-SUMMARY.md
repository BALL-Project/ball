---
phase: 999.21-dockresultfile-dead-code
plan: 01
status: complete
completed: 2026-05-16
requirements_satisfied:
  - DEADCODE-01
landed_via:
  - 914622d8f
---

# Phase 999.21 — SUMMARY: DockResultFile QtXml dead-code cleanup (DEADCODE-01 complete)

## Outcome

Dropped the stubbed Qt 5 SAX overload of `attributesToHashMap()` that had
been dead code since Phase 5 Qt 6 migration (commit `3691232`). All BALL
readers in `dockResultFile.C` use the modern `QXmlStreamReader` path
(`xmlIn_->attributes()` returns `QXmlStreamAttributes`); the SAX overload
had zero callers and was kept only as ifdef-stubbed source-compat.

**30 lines removed** across header + impl. Single commit `914622d8f`.
Phase 5 BLOCKER-A (deferred-items.md) is now closed.

## Execution

Done inline, no executor spawn. Total elapsed: <30 minutes.

### Files modified

| File | Change |
|---|---|
| `include/BALL/FORMAT/dockResultFile.h` | Removed `#include <QtCore/qglobal.h>` + `#if QT_VERSION` block (L21-24); removed `#if`-guarded SAX-overload declaration (L266-268); rewrote leading comment block to reflect Qt-6-only state |
| `source/FORMAT/dockResultFile.C` | Removed `#if QT_VERSION` block at L2085-2099 — 14-line SAX-overload impl body + guard + 6-line comment |

### Verification

- **Local BALL build:** `cmake --build build/ci-macos --target BALL -j16` → green (100% Built target BALL). No new warnings, no missing-include errors.
- **CI verification:** push `914622d8f` triggers tri-OS CI (source change, not paths-ignored). Result: see CI run starting 2026-05-16T21:21Z post-push.
- **No residual refs:** `grep -n "QXmlAttributes\|QXmlDefaultHandler\|QT_VERSION\|qglobal" include/BALL/FORMAT/dockResultFile.h source/FORMAT/dockResultFile.C` returns only the historical-reference comment line in the new dockResultFile.h leader (intentional).

## Context — why this is the last v1.6.2 phase

Phase 999.21 closes the final v1.6.2 substantive REQ (DEADCODE-01). All 12
v1.6.2 REQs are now Complete:

| Cluster | REQs |
|---|---|
| Build acceleration (999.16-999.20) | BUILD-ACCEL-01..05 ✓ |
| Triage (999.14) | TRIAGE-01/02/03 ✓ |
| Census (999.22, CENSUS-ONLY) | WARN-CENSUS-01 ✓ |
| Test close (Phase 9) | TEST-CLOSE-01/02 ✓ |
| Dead code (999.21) | DEADCODE-01 ✓ |
| Bison stretch (999.23) | GRAMMAR-01 — deferred to v1.7 per stretch shape |

v1.6.2 milestone is content-complete. Next steps: release-notes draft
(already started in `7f2e851a` parallel-session commit) + tag.

## Bonus profile context

Phase 999.19's tri-OS ninja-log baseline identified `dockResultFile.C` as
the top cross-toolchain compile-time outlier: **14.6s on Windows MSVC,
7.6s on Linux GCC, 4.4s on macOS clang**. A 14-line removal in the impl
file won't measurably move that number — the bulk of the compile cost
is the rest of the file's StreamReader handlers + heavy template
instantiation. But the cleanup reduces cognitive load for the next
reader and closes a documented BLOCKER from the Phase 5 migration.

## OOS commits during phase window

None this phase — too short for parallel-session interference.

## DEADCODE-01 status

REQUIREMENTS.md row 111 flipped to `[x]` + status table row 218 updated to
Complete. Phase 999.21 marked Complete in ROADMAP.md.
