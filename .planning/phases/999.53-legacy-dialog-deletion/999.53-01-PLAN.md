---
phase: 999.53-legacy-dialog-deletion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - source/VIEW/WIDGETS/geometricControl.C
  - include/BALL/VIEW/WIDGETS/geometricControl.h
  - source/VIEW/DIALOGS/modifyRepresentationDialog.C
  - include/BALL/VIEW/DIALOGS/modifyRepresentationDialog.h
  - source/APPLICATIONS/BALLVIEW/mainframe.C
autonomous: true
requirements: [VIEW-CLEAN-01]

must_haves:
  truths:
    - "No source file (outside the dialog's own .C/.h/.ui) constructs or includes ClippingDialog, LabelDialog, or MaterialSettings"
    - "geometricControl's clip-plane context menu still lets the user clip representations (via the Inspector ClippingController path, not the legacy modal dialog)"
    - "modifyRepresentationDialog (Display > Modify Representation) still applies material settings to a surface representation, with no MaterialSettings child widget"
    - "BALLView links clean with BALL_UI_V2=ON after this plan"
  artifacts:
    - path: "source/VIEW/WIDGETS/geometricControl.C"
      provides: "geometricControl free of ClippingDialog stack construction"
      contains: "selectClipRepresentations"
    - path: "source/VIEW/DIALOGS/modifyRepresentationDialog.C"
      provides: "modify-representation dialog with material apply migrated off MaterialSettings"
      contains: "material"
  key_links:
    - from: "source/VIEW/WIDGETS/geometricControl.C"
      to: "ClippingController / ClippingPlane direct mutation"
      via: "selectClipRepresentations reimplemented without ClippingDialog"
      pattern: "selectClipRepresentations"
    - from: "source/VIEW/DIALOGS/modifyRepresentationDialog.C"
      to: "material apply backend (Representation material setters)"
      via: "direct material mutation, no MaterialSettings child"
      pattern: "material"
---

<objective>
Remove the THREE remaining LIVE (non-comment) references to the deletable legacy dialogs so that
`clippingDialog`, `labelDialog`, and `materialSettings` become unreferenced and deletable in Plan 03:

1. `geometricControl.C:686` — a stack-allocated `ClippingDialog dialog;` inside `selectClipRepresentations()`.
2. `mainframe.C:251` — `new LabelDialog(this, ...)` (the LabelDialog self-registers the legacy
   `Display > Create > Label` menu entry; the Inspector LabelSection supersedes it).
3. `modifyRepresentationDialog.C:60` + `.h:37,135` — `modifyRepresentationDialog` (a still-reachable
   dialog reached via `Display > Modify Representation`, NOT in the deletion list) embeds a
   `MaterialSettings` child widget and calls `material_settings_->apply()`. This is the ONLY live
   `MaterialSettings` consumer left after 999.58 cut over scene.C.

Purpose: These are the last live code dependencies blocking deletion of the clipping/label/material
dialog triples. Clearing them (this plan) + the scene/control comment-only state from 999.58 leaves
those triples unreferenced for Plan 03's atomic deletion. Per VIEW-CLEAN-01.

Output: geometricControl, mainframe, and modifyRepresentationDialog with zero live references to the
three dialogs; BALLView still links and the clip / label / surface-material user paths still work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/999.48-a11y-flag-flip-removal/deferred-items.md
@.planning/phases/999.57-inspector-controller-cutover-displayproperties/999.57-CONSUMER-AUDIT.md

<read_first>
MANDATORY before editing. Read these exact regions:

- `source/VIEW/WIDGETS/geometricControl.C:78-82` — the clip-plane context-menu wiring
  (`Select Representations` action -> `selectClipRepresentations()` slot).
- `source/VIEW/WIDGETS/geometricControl.C:682-700` — `selectClipRepresentations()` (the stack
  `ClippingDialog dialog; dialog.setClippingPlane(context_plane_); dialog.exec();`) and the
  surrounding `createNewClippingPlane()` for the ClippingPlane API in use.
- `source/VIEW/WIDGETS/inspector/sections/clippingSection.C` + `source/VIEW/KERNEL/controllers/clippingController.C`
  — the Inspector clip path that already supersedes ClippingDialog (read its `apply()` /
  clip-representations logic to mirror the same backend mutation directly).
- `source/VIEW/DIALOGS/clippingDialog.C` — read `setClippingPlane()` + `accept()` so the direct
  reimplementation in `selectClipRepresentations()` performs the SAME ClippingPlane / Representation
  mutation the dialog did (clip every current Representation against `context_plane_`).
- `source/APPLICATIONS/BALLVIEW/mainframe.C:248-252` — the `new LabelDialog(...)` registration line.
- `source/VIEW/DIALOGS/labelDialog.C:120-130,218-230` — the LabelDialog's self-registered
  `Display|Create|Label` menu entry + `checkMenu` (so you understand what menu surface disappears).
- `source/VIEW/WIDGETS/inspector/sections/labelSection.C` + `source/VIEW/KERNEL/controllers/labelController.C`
  — confirm the Inspector LabelSection/LabelController already provides label creation (the
  supersession that makes the legacy menu removable).
- `source/VIEW/DIALOGS/modifyRepresentationDialog.C:40-65,110-120,455-465` — the `MaterialSettings`
  construction (`new MaterialSettings(...)`), the `material_settings_->apply()` call in the dialog's
  apply path, and `setCurrentRepresentation(rep_)`.
- `include/BALL/VIEW/DIALOGS/modifyRepresentationDialog.h:35-40,130-140` — the
  `#include <BALL/VIEW/DIALOGS/materialSettings.h>` and the `MaterialSettings* material_settings_;` member.
- `source/VIEW/DIALOGS/materialSettings.C` — read `apply()` + `setCurrentRepresentation()` so the
  inlined material-apply logic in modifyRepresentationDialog reproduces the EXACT material setter
  sequence MaterialSettings used (ambient/diffuse/specular/shininess + transparency on the rep's material).
- `source/VIEW/KERNEL/controllers/materialController.C:200-295` — the Inspector material apply path,
  for the canonical "apply material to a representation" backend to mirror.
</read_first>

<interfaces>
ClippingPlane (include/BALL/VIEW/KERNEL/clippingPlane.h) — the object `context_plane_` points at;
ClippingDialog drove its representation-clip list. Reuse the SAME setters/clip API directly.

Representation material API (include/BALL/VIEW/KERNEL/representation.h + the Stage/material types) —
MaterialSettings::apply() writes ambient/diffuse/specular/shininess/transparency onto the
representation's material. modifyRepresentationDialog must call those setters directly instead of
delegating to a MaterialSettings child widget.

NOTE: extract the precise method names from the read_first files — do not guess signatures.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reimplement geometricControl::selectClipRepresentations() without ClippingDialog</name>
  <files>source/VIEW/WIDGETS/geometricControl.C, include/BALL/VIEW/WIDGETS/geometricControl.h</files>
  <action>
    In `selectClipRepresentations()` (geometricControl.C:682-690) remove the stack-allocated
    `ClippingDialog dialog; dialog.setClippingPlane(context_plane_); dialog.exec();`. Replace it
    with the SAME clip-every-representation mutation the legacy `ClippingDialog::accept()` performed
    (read clippingDialog.C + clippingController.C:137 first to copy the exact ClippingPlane /
    Representation API). The context-menu action wiring at geometricControl.C:80 stays; only the slot
    body changes. Remove `#include <BALL/VIEW/DIALOGS/clippingDialog.h>` (geometricControl.C:20) and
    any ClippingDialog forward-declaration in geometricControl.h. Do NOT touch the unrelated
    `ShowDisplayPropertiesMessage` send at geometricControl.C:402 (that belongs to the blocked
    displayProperties triple — see this phase's BLOCKED note; leave it for the follow-on phase).
    Match BALL conventions: 2-space indent, Allman braces, `.C`/`.h` pairing.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && { grep -n 'ClippingDialog' source/VIEW/WIDGETS/geometricControl.C include/BALL/VIEW/WIDGETS/geometricControl.h | grep -vE ':\s*//' | grep -c 'ClippingDialog' | grep -qx 0 && echo PASS-no-live-ClippingDialog-ref || echo FAIL; }</automated>
  </verify>
  <done>No live (non-comment) `ClippingDialog` reference remains in geometricControl.{C,h}; the clip-plane context menu still clips representations via direct ClippingPlane mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the legacy LabelDialog registration from mainframe</name>
  <files>source/APPLICATIONS/BALLVIEW/mainframe.C</files>
  <action>
    Remove the `new LabelDialog(this, ((String)tr("LabelDialog")).c_str());` line at mainframe.C:251
    and the `#include <BALL/VIEW/DIALOGS/labelDialog.h>` at mainframe.C:54. The LabelDialog
    self-registers the legacy `Display > Create > Label` menu entry (labelDialog.C:125); removing the
    construction removes that menu surface, which the Inspector LabelSection/LabelController already
    supersedes (confirmed in read_first). Do NOT touch the DisplayProperties registration
    (mainframe.C:55,405,634,1084) or the Tools > Legacy Settings submenu (mainframe.C:460-478,1210-1269)
    — those belong to the blocked displayProperties triple and the LegacySettingsHelper retirement,
    which are out of scope for this plan (see BLOCKED note). Verify the file still compiles structurally
    (matching braces around the removed line).
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && { grep -n 'LabelDialog\|labelDialog' source/APPLICATIONS/BALLVIEW/mainframe.C | grep -vE ':\s*//' | grep -c 'LabelDialog\|labelDialog' | grep -qx 0 && echo PASS-no-live-LabelDialog-ref || echo FAIL; }</automated>
  </verify>
  <done>No live `LabelDialog`/`labelDialog` reference remains in mainframe.C (include + construction both gone).</done>
</task>

<task type="auto">
  <name>Task 3: Migrate modifyRepresentationDialog off the embedded MaterialSettings widget</name>
  <files>source/VIEW/DIALOGS/modifyRepresentationDialog.C, include/BALL/VIEW/DIALOGS/modifyRepresentationDialog.h</files>
  <action>
    modifyRepresentationDialog embeds a MaterialSettings child widget (`new MaterialSettings(...)` at
    modifyRepresentationDialog.C:60, member `material_settings_` at .h:135, include at .h:37) and calls
    `material_settings_->apply()` (line 118) + `material_settings_->setCurrentRepresentation(rep_)`
    (line 461). This is the LAST live MaterialSettings consumer. Replace the embedded widget with the
    Inspector materialSection/materialController material controls OR with a direct material-apply
    implementation that reproduces MaterialSettings::apply()'s exact setter sequence
    (ambient/diffuse/specular/shininess/transparency on the representation's material — read
    materialSettings.C + materialController.C:200-295 first for the canonical sequence). Remove the
    `#include <BALL/VIEW/DIALOGS/materialSettings.h>` (.h:37), the `MaterialSettings* material_settings_;`
    member (.h:135), and the three call sites (.C:45,60-61,116-118,461). Keep the dialog reachable and
    its "Modify Representation" surface-material apply behavior intact. The dialog's `.ui` has a
    `material_setting` container the MaterialSettings widget was added into — repurpose or hide it
    consistently (do NOT leave a dangling empty panel). Match BALL conventions.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && { grep -n 'MaterialSettings\|materialSettings' source/VIEW/DIALOGS/modifyRepresentationDialog.C include/BALL/VIEW/DIALOGS/modifyRepresentationDialog.h | grep -vE ':\s*//' | grep -c 'MaterialSettings\|materialSettings' | grep -qx 0 && echo PASS-no-live-MaterialSettings-ref || echo FAIL; }</automated>
  </verify>
  <done>No live `MaterialSettings`/`materialSettings` reference remains in modifyRepresentationDialog.{C,h}; the dialog still applies material settings to the current representation.</done>
</task>

</tasks>

<verification>
After all three tasks:

```bash
cd /Users/kohlbach/Claude/BALL/ball
# Zero LIVE (non-comment, non-sip, non-cmake) refs to the three deletable dialogs OUTSIDE their own files:
for pat in 'ClippingDialog\|clippingDialog' 'LabelDialog\|labelDialog' 'MaterialSettings\|materialSettings'; do
  grep -rn "$pat" source/ include/ 2>/dev/null \
    | grep -viE 'DIALOGS/(clippingDialog|labelDialog|materialSettings)\.' \
    | grep -v 'controllers/PATTERN.md' | grep -v '\.sip:' | grep -v 'sources.cmake' \
    | grep -vE ':\s*//' | grep -vE ':\s*\*' | grep -vE ':\s*/\*'
done
# Expect: only the controller/scene/header EXPLANATORY-COMMENT lines (which the filters above strip)
# remain — i.e. ideally empty output. Any live constructor/include hit is a FAIL.
```

BALLView must still link with `BALL_UI_V2=ON` after this plan (a Plan-03 deletion would otherwise
fail to link). Build VIEW + the BALLView app.
</verification>

<success_criteria>
- geometricControl, mainframe, and modifyRepresentationDialog carry NO live ClippingDialog /
  LabelDialog / MaterialSettings reference.
- The clip-representations context action, and the Modify-Representation material apply, still work.
- BALLView links clean (BALL_UI_V2=ON).
</success_criteria>

<output>
After completion, create `.planning/phases/999.53-legacy-dialog-deletion/999.53-01-SUMMARY.md`.
Record: the exact backend each migrated path now uses (clip mutation, material apply), and confirm
the three dialogs are now unreferenced for Plan 03 deletion.
</output>
