---
phase: 999.53-legacy-dialog-deletion
plan: 02
type: execute
wave: 2
depends_on: ["999.53-01"]
files_modified:
  - source/VIEW/DIALOGS/lightSettings.C
  - source/VIEW/DIALOGS/lightSettings.ui
  - include/BALL/VIEW/DIALOGS/lightSettings.h
  - source/VIEW/DIALOGS/stageSettings.C
  - source/VIEW/DIALOGS/stageSettings.ui
  - include/BALL/VIEW/DIALOGS/stageSettings.h
  - source/VIEW/DIALOGS/stereoSettingsDialog.C
  - source/VIEW/DIALOGS/stereoSettingsDialog.ui
  - include/BALL/VIEW/DIALOGS/stereoSettingsDialog.h
  - source/VIEW/DIALOGS/clippingDialog.C
  - source/VIEW/DIALOGS/clippingDialog.ui
  - include/BALL/VIEW/DIALOGS/clippingDialog.h
  - source/VIEW/DIALOGS/labelDialog.C
  - source/VIEW/DIALOGS/labelDialog.ui
  - include/BALL/VIEW/DIALOGS/labelDialog.h
  - source/VIEW/DIALOGS/materialSettings.C
  - source/VIEW/DIALOGS/materialSettings.ui
  - include/BALL/VIEW/DIALOGS/materialSettings.h
  - source/VIEW/DIALOGS/sources.cmake
  - source/PYTHON/EXTENSIONS/VIEW/lightSettings.sip
  - source/PYTHON/EXTENSIONS/VIEW/VIEW.sip
autonomous: false
requirements: [VIEW-CLEAN-01]

must_haves:
  truths:
    - "The six legacy dialog file triples (lightSettings, stageSettings, stereoSettingsDialog, clippingDialog, labelDialog, materialSettings) no longer exist on disk"
    - "source/VIEW/DIALOGS/sources.cmake no longer lists any of the six deleted .C/.ui entries"
    - "lightSettings.sip is deleted and its %Include line is gone from VIEW.sip"
    - "grep for the six dialog names in source/ + include/ returns zero matches (only .planning/ history + remaining-comment breadcrumbs cleaned)"
    - "BALLView links and launches; the Inspector Light/Stage/Stereo/Clipping/Label/Material sections still drive the rendered scene; no legacy clip/label modal dialog remains reachable"
  artifacts:
    - path: "source/VIEW/DIALOGS/sources.cmake"
      provides: "build list with the six dialog triples removed"
      contains: "DIALOGS"
  key_links:
    - from: "source/VIEW/DIALOGS/sources.cmake"
      to: "the VIEW library build"
      via: "removed .C/.ui entries -> headers auto-drop on .h deletion"
      pattern: "DIALOGS"
---

<objective>
Delete the SIX legacy dialog triples that have no remaining live code dependency after Plan 01
(clipping/label/material references cleared) and after 999.58 (scene.C light/stage/stereo cut over to
comments only):

  lightSettings, stageSettings, stereoSettingsDialog, clippingDialog, labelDialog, materialSettings
  — each as `{C,h,ui}`.

Plus the build/binding cleanup: drop their entries from `source/VIEW/DIALOGS/sources.cmake`, delete
`source/PYTHON/EXTENSIONS/VIEW/lightSettings.sip`, and remove its `%Include` line from `VIEW.sip:92`.
Also scrub the now-stale explanatory comment breadcrumbs that name these deleted files
(scene.C, the six controllers, demoTutorialDialog.C) so the acceptance grep returns zero.

Purpose: This is the achievable, build-safe portion of VIEW-CLEAN-01. These six dialogs are fully
superseded by the Inspector Light/Stage/Stereo/Clipping/Label/Material sections + controllers
(999.51 / 999.52 / 999.58). The displayProperties / modelSettingsDialog / coloringSettingsDialog
triple is NOT in this plan — it is genuinely blocked (still the live representation-creation engine);
see this phase's BLOCKED note and the follow-on phase recommendation.

Output: six dialog triples deleted, cmake + sip updated, comment breadcrumbs scrubbed, BALLView
links + launches, Inspector sections verified still rendering. A human-verify smoke checkpoint gates
completion (a premature deletion broke the build before — commit 7f56f80d89).

IMPORTANT — compile-check BETWEEN deletion groups, not just at the end. Delete in three groups, each
followed by a VIEW build before proceeding to the next.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/999.48-a11y-flag-flip-removal/deferred-items.md
@.planning/phases/999.53-legacy-dialog-deletion/999.53-01-SUMMARY.md

<read_first>
MANDATORY before deleting anything — re-confirm zero live refs per file (Plan 01 cleared the live
ones; this re-verifies before the irreversible delete):

- `source/VIEW/DIALOGS/sources.cmake` — note the exact `.ui` lines (10,12,21,27,28,30,32,52,53 region)
  and `.C` lines (66,68,76,81,82,84,86,106,107 region). You will remove only the SIX deletable
  triples' entries; LEAVE `displayProperties.C/.ui`, `modelSettingsDialog.C/.ui`,
  `coloringSettingsDialog.C/.ui`, and `modifyRepresentationDialog.C/.ui` (the latter survives).
- `source/PYTHON/EXTENSIONS/VIEW/VIEW.sip:91-92` — `%Include displayProperties.sip` (LEAVE — blocked
  triple) + `%Include lightSettings.sip` (REMOVE).
- `source/PYTHON/EXTENSIONS/VIEW/lightSettings.sip` — the file to delete.
- The DELETE-confirmation grep (run it; it must show only comment lines for the six names before you
  delete):
  ```
  for n in lightSettings stageSettings stereoSettingsDialog clippingDialog labelDialog materialSettings; do
    grep -rni "$n" source/ include/ | grep -viE "DIALOGS/(lightSettings|stageSettings|stereoSettingsDialog|clippingDialog|labelDialog|materialSettings)\." | grep -v "\.sip:" | grep -v sources.cmake | grep -vE ':\s*//|:\s*\*|:\s*/\*'
  done
  ```
  Expect EMPTY (Plan 01 cleared the live refs). If ANY non-comment hit appears, STOP — that file is
  still referenced; do not delete it.
- The comment-breadcrumb sites to scrub (so the final acceptance grep is truly zero):
  `source/VIEW/WIDGETS/scene.C` (lines ~16,30,168-172,381-394,836,888,1127-1136,1181,1224-1226,3130,3232-3268),
  `source/VIEW/KERNEL/controllers/{lightController,materialController,stageController,stereoController,clippingController,labelController}.{C,h}`,
  `source/APPLICATIONS/BALLVIEW/demoTutorialDialog.C` (~14,110,211),
  `source/VIEW/WIDGETS/inspector/sections/{labelSection,stageSection}.{C,h}`,
  `include/BALL/VIEW/KERNEL/stage.h:427`, `include/BALL/VIEW/WIDGETS/scene.h:937,943`,
  `source/VIEW/KERNEL/controllers/PATTERN.md`.
  These are explanatory comments referencing `<dialog>.C:<line>` of the now-deleted files. Reword them
  to describe the behavior without naming the deleted file (e.g. "the legacy light-apply path" instead
  of "lightSettings.C:384-393"), preserving the design rationale. Do NOT delete the comments wholesale
  if they explain WHY the controller does what it does — just drop the dead file:line citation.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete group A (clipping + label) and compile-check</name>
  <files>source/VIEW/DIALOGS/clippingDialog.C, source/VIEW/DIALOGS/clippingDialog.ui, include/BALL/VIEW/DIALOGS/clippingDialog.h, source/VIEW/DIALOGS/labelDialog.C, source/VIEW/DIALOGS/labelDialog.ui, include/BALL/VIEW/DIALOGS/labelDialog.h, source/VIEW/DIALOGS/sources.cmake</files>
  <action>
    Re-run the DELETE-confirmation grep (read_first) for `clippingDialog` and `labelDialog` — confirm
    only comment lines remain (Plan 01 cleared geometricControl + mainframe). Then delete the six files
    listed in <files> EXCEPT sources.cmake. In sources.cmake remove the `clippingDialog.ui`,
    `labelDialog.ui`, `clippingDialog.C`, and `labelDialog.C` entries (the `.h` headers are globbed from
    `include/BALL/VIEW/DIALOGS/` and auto-drop). Scrub the clippingController/labelController/labelSection
    comment breadcrumbs that cite `clippingDialog.C:` / `labelDialog.C:` line numbers (reword per
    read_first). Then build VIEW + BALLView with BALL_UI_V2=ON and confirm it links.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && { test ! -e source/VIEW/DIALOGS/clippingDialog.C && test ! -e source/VIEW/DIALOGS/labelDialog.C && test ! -e include/BALL/VIEW/DIALOGS/clippingDialog.h && test ! -e include/BALL/VIEW/DIALOGS/labelDialog.h && ! grep -qE 'clippingDialog|labelDialog' source/VIEW/DIALOGS/sources.cmake && echo PASS-group-A-deleted || echo FAIL; }</automated>
  </verify>
  <done>clippingDialog + labelDialog triples gone; sources.cmake clean of them; VIEW + BALLView build green BALL_UI_V2=ON.</done>
</task>

<task type="auto">
  <name>Task 2: Delete group B (material) and group C (light + stage + stereo) and compile-check, plus sip cleanup</name>
  <files>source/VIEW/DIALOGS/materialSettings.C, source/VIEW/DIALOGS/materialSettings.ui, include/BALL/VIEW/DIALOGS/materialSettings.h, source/VIEW/DIALOGS/lightSettings.C, source/VIEW/DIALOGS/lightSettings.ui, include/BALL/VIEW/DIALOGS/lightSettings.h, source/VIEW/DIALOGS/stageSettings.C, source/VIEW/DIALOGS/stageSettings.ui, include/BALL/VIEW/DIALOGS/stageSettings.h, source/VIEW/DIALOGS/stereoSettingsDialog.C, source/VIEW/DIALOGS/stereoSettingsDialog.ui, include/BALL/VIEW/DIALOGS/stereoSettingsDialog.h, source/VIEW/DIALOGS/sources.cmake, source/PYTHON/EXTENSIONS/VIEW/lightSettings.sip, source/PYTHON/EXTENSIONS/VIEW/VIEW.sip</files>
  <action>
    Re-run the DELETE-confirmation grep for `materialSettings`, `lightSettings`, `stageSettings`,
    `stereoSettingsDialog` — confirm only comment lines remain (Plan 01 cleared modifyRepresentationDialog;
    999.58 left scene.C/controllers comment-only). Then delete the twelve dialog files in <files>.
    In sources.cmake remove the `materialSettings.{C,ui}`, `lightSettings.{C,ui}`, `stageSettings.{C,ui}`,
    `stereoSettingsDialog.{C,ui}` entries. Delete `source/PYTHON/EXTENSIONS/VIEW/lightSettings.sip` and
    remove the `%Include lightSettings.sip` line from `VIEW.sip:92` (LEAVE the displayProperties.sip
    %Include — that triple is blocked). Scrub the scene.C / light-material-stage-stereo controller /
    demoTutorialDialog / stage.h / scene.h comment breadcrumbs that cite the deleted `<dialog>.C:` lines
    (reword per read_first, preserving rationale). Build VIEW + BALLView BALL_UI_V2=ON; confirm link.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && { for f in materialSettings lightSettings stageSettings stereoSettingsDialog; do test ! -e source/VIEW/DIALOGS/$f.C && test ! -e include/BALL/VIEW/DIALOGS/$f.h || { echo FAIL-$f; exit 1; }; done; test ! -e source/PYTHON/EXTENSIONS/VIEW/lightSettings.sip && ! grep -q 'lightSettings.sip' source/PYTHON/EXTENSIONS/VIEW/VIEW.sip && ! grep -qE 'materialSettings|lightSettings|stageSettings|stereoSettingsDialog' source/VIEW/DIALOGS/sources.cmake && echo PASS-groups-BC-deleted; }</automated>
  </verify>
  <done>material/light/stage/stereo triples gone; lightSettings.sip + its VIEW.sip include gone; sources.cmake clean; VIEW + BALLView build green BALL_UI_V2=ON.</done>
</task>

<task type="auto">
  <name>Task 3: Final acceptance grep for the six deleted names</name>
  <files>(verification only — no edits expected; scrub any straggler comment if found)</files>
  <action>
    Run the partial acceptance grep for the SIX names this phase deletes. The phase-level acceptance
    (all 9 names -> 0) is NOT achievable here because the displayProperties/modelSettingsDialog/
    coloringSettingsDialog triple is blocked (see BLOCKED note); this task asserts the SIX-name subset is
    clean. If any straggler comment breadcrumb still cites a deleted file, scrub it now.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && { c=$(grep -rniE 'lightSettings|stageSettings|stereoSettingsDialog|clippingDialog|labelDialog|materialSettings' source/ include/ 2>/dev/null | grep -v 'controllers/PATTERN.md' | wc -l | tr -d ' '); echo "six-name residual matches: $c (target 0)"; test "$c" -eq 0 && echo PASS-six-clean || echo "REVIEW-residuals-above"; }</automated>
  </verify>
  <done>grep for the six deleted names across source/ + include/ returns 0 (PATTERN.md doc excepted, may be updated separately).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Smoke-verify deletions in running BALLView</name>
  <what-built>
    Deleted six superseded legacy dialog triples (light/stage/stereo/clipping/label/material) +
    their cmake + lightSettings.sip + VIEW.sip include, after clearing all live references (Plan 01).
    BALLView links clean with BALL_UI_V2=ON; the Inspector sections supersede every deleted dialog.
  </what-built>
  <how-to-verify>
    1. Launch the freshly built BALLView (BALL_UI_V2=ON).
    2. Load a molecule (e.g. a PDB). Confirm it renders (the auto-representation path is UNCHANGED by
       this phase — displayProperties still owns it — so this must work exactly as before).
    3. Open the Inspector. Exercise each superseded section and confirm it still drives the scene:
       - Light section: change a light -> scene relighting updates.
       - Stage / Stereo section: change stage/stereo settings -> scene updates (toggle stereo if available).
       - Material section: change ambient/diffuse/specular/shininess/transparency -> material updates.
       - Clipping section: add/adjust a clip plane -> representation visibly clips.
       - Label section: create a label -> label appears in the scene.
    4. Confirm the legacy modal dialogs are GONE: there is no `Display > Create > Label` legacy menu
       entry (Inspector Label section replaces it), and the clip-plane context "Select Representations"
       no longer pops a modal ClippingDialog (it clips directly).
    5. Confirm `Display > Modify Representation` still opens and its material apply still works
       (Plan 01 migrated it off MaterialSettings).
  </how-to-verify>
  <action>This is a human-verify checkpoint. Claude has already deleted the six dialog triples and built BALLView; the human now confirms the running app per the how-to-verify steps. No code edits in this task — only confirmation (or a list of regressions to fix).</action>
  <verify>
    <automated>ls source/VIEW/DIALOGS/BALLView 2>/dev/null; echo "manual GUI smoke — human confirms render + sections + legacy-dialogs-gone per how-to-verify"</automated>
  </verify>
  <done>Human confirms: molecule renders, all six Inspector sections drive the scene, the legacy Label menu + modal ClippingDialog are gone, and Modify Representation material apply still works.</done>
  <resume-signal>Type "approved" if all sections render + the legacy dialogs are gone, or describe what broke.</resume-signal>
</task>

</tasks>

<verification>
```bash
cd /Users/kohlbach/Claude/BALL/ball
# Six deleted names -> zero (the achievable subset of VIEW-CLEAN-01):
grep -rniE 'lightSettings|stageSettings|stereoSettingsDialog|clippingDialog|labelDialog|materialSettings' source/ include/ | grep -v 'controllers/PATTERN.md'
# Files gone:
ls source/VIEW/DIALOGS/{lightSettings,stageSettings,stereoSettingsDialog,clippingDialog,labelDialog,materialSettings}.C 2>&1 | grep -c 'No such file'  # expect 6
```
BALLView links + launches BALL_UI_V2=ON; all listed Inspector sections render; legacy modal dialogs gone.
</verification>

<success_criteria>
- Six dialog triples + lightSettings.sip deleted; sources.cmake + VIEW.sip updated; comment breadcrumbs scrubbed.
- grep for the six names -> 0 in source/ + include/ (PATTERN.md doc excepted).
- BALLView links + launches; Inspector Light/Stage/Stereo/Clipping/Label/Material sections all drive the scene; legacy clip/label modal surfaces gone.
- Human-verify smoke checkpoint approved.
</success_criteria>

<output>
After completion, create `.planning/phases/999.53-legacy-dialog-deletion/999.53-02-SUMMARY.md`.
Record: which six triples were deleted, the cmake/sip diff, and an explicit statement that the
displayProperties/modelSettingsDialog/coloringSettingsDialog triple + LegacySettingsHelper/Tools-menu
remain (blocked), so VIEW-CLEAN-01 is PARTIAL and the follow-on phase still owes the lynchpin triple.
</output>
