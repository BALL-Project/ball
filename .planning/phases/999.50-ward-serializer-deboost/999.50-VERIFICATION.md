---
phase: 999.50-ward-serializer-deboost
verified: 2026-05-21T00:00:00Z
status: passed
score: 13/13 must-haves verified
overrides_applied: 0
---

# Phase 999.50: Ward Serializer De-boost Verification Report

**Phase Goal:** Drop boost::serialization from the PoseClustering Ward-tree serializer by hand-rolling a compact magic+version-headed reader/writer; keep the public API (serializeWardClusterTree/deserializeWardClusterTree) and the `bool binary` flag unchanged; regenerate the committed fixture in the new format; add release.yml ccache reuse. The serialization TU must compile in seconds with no boost/archive or adj_list_serialize includes remaining. PoseClustering_test3 (round-trip + fixture read → 6 clusters @ 0.5 with five specific scores) and PoseClustering_test1 (graphviz) must pass.
**Verified:** 2026-05-21
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
| -- | ----- | ------ | -------- |
| 1  | No boost::archive / adj_list_serialize / boost::serialization includes or usage in poseClusteringSerialization.C | ✓ VERIFIED | `grep -c` → boost/archive=0, adj_list_serialize=0, boost/serialization=0, binary/text_o/iarchive=0. Only `boost/graph/adjacency_list.hpp` remains (the in-memory ClusterTree type, not the on-disk format). Historical references appear only in the top comment. |
| 2  | serialize/deserialize round-trip the Ward tree losslessly (binary + text) | ✓ VERIFIED | Source lines 97-465 implement symmetric writer/reader for both variants over the same payload layout; PoseClustering_test3 round-trip CHECK (test3:312-351) passes live. Text writer emits floats at `max_digits10` (9 digits, line 157) for exact round-trip (WR-03). |
| 3  | Wrong/old (boost-serialized) file fails loudly via magic+version mismatch, not silent mis-parse | ✓ VERIFIED | Magic checks throw `Exception::InvalidFormat(__FILE__, __LINE__)` (binary lines 215-221, text lines 361-366); version mismatch throws (lines 230-235, 367-372). CR-01 hardening present: stream-state checks after every read, count sanity ceiling (1e8, line 76), pose-count≤order, edge/root index<order bounds — all throw InvalidFormat. |
| 4  | Committed fixture is NEW format and deserializes to 6 clusters @ extractClustersForThreshold(0.5) | ✓ VERIFIED | `head -c` shows `BALLWARD 2\n15 14\n…` (ASCII text), `grep -c BALLWARD`=1, boost markers=0. test3 fixture-read CHECK (test3:360-391) asserts getNumberOfClusters()=6 at 0.5 and passes live. |
| 5  | PoseClustering_test3 (round-trip CHECK + fixture-read CHECK) passes | ✓ VERIFIED | Live: `ctest -R PoseClustering_test3` → Passed 0.04s. Both round-trip (test3:327-351) and fixture-read (test3:367-391) assert 6 clusters @ 0.5 + the five scores. |
| 6  | PoseClustering_test1 (graphviz) passes; graphviz TU untouched | ✓ VERIFIED | Live: `ctest -R PoseClustering_test1` → Passed 0.12s. `git status` shows poseClusteringGraphViz.C unchanged. |
| 7  | DockPoseClustering (writer, binary) and ExtractClustersFromWardTree (reader, binary) compile against new API unchanged | ✓ VERIFIED | API signatures byte-identical (header lines 495/511); `git status` shows both caller .C files unchanged. libBALL builds clean. |
| 8  | Format break documented under v1.7.2 Breaking changes in RELEASE-NOTES-v1.7.md | ✓ VERIFIED | RELEASE-NOTES-v1.7.md line 111 "### Breaking changes", lines 119/127/134 describe the BALLWARD format, loud failure, and regeneration guidance. |
| 9  | release.yml restores a ccache build cache before configure on each OS | ✓ VERIFIED | "Restore ccache" steps at lines 185 (macOS, before Configure@196), 565 (Linux, before Configure@576), 937 (Windows, before Configure@948). |
| 10 | release.yml saves the ccache build cache after the build on each OS | ✓ VERIFIED | "Save ccache" steps at lines 209 (macOS, after Build@201), 596 (Linux, after Build@590), 1004 (Windows, after Build@983). Gated `if: always() && <os-cond> && steps.<id>.outputs.cache-hit != 'true'`. |
| 11 | ccache key aligned with ci.yml lineage (ccache-<os>-pch-v2-) | ✓ VERIFIED | release.yml keys `ccache-${{ matrix.os }}-pch-v2-${{ github.sha }}` + restore-keys `ccache-${{ matrix.os }}-pch-v2-` / `ccache-${{ matrix.os }}-` (lines 191/193…). ci.yml uses identical lineage (ci.yml:246/248). Per-OS paths match ci.yml exactly. |
| 12 | release.yml package job carries a NEW job-level env block with CCACHE_SLOPPINESS + CCACHE_EXTRAFILES | ✓ VERIFIED | Job-level `env:` at lines 47-48: CCACHE_SLOPPINESS="pch_defines,time_macros,include_file_mtime", CCACHE_EXTRAFILES="${{ github.workspace }}/include/BALL/CONFIG/config.h.in" (verbatim from ci.yml). No CMAKE_*_COMPILER_LAUNCHER added (presets own it). |
| 13 | release.yml remains valid YAML / well-formed workflow | ✓ VERIFIED | `python3 -c "yaml.safe_load(...)"` → exit 0, "valid YAML". |

**Score:** 13/13 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `source/DOCKING/COMMON/poseClusteringSerialization.C` | Hand-rolled magic+version reader/writer, no boost::serialization | ✓ VERIFIED | 466 lines, fully hand-rolled, contains "BALLWARD" (×6), no boost archive. CR-01/WR-02/WR-03/WR-04/IN-02/IN-03 hardening present. |
| `include/BALL/DOCKING/COMMON/poseClustering.h` | ClusterProperties without serialize() template; API unchanged | ✓ VERIFIED | `void serialize(Archive` non-comment hits=0; boost/serialization includes=0; signatures unchanged; Doxygen rewritten to describe BALLWARD format (IN-01, lines 478-511). |
| `test/data/PoseClustering_wardtree.dat` | New-format fixture → 6 clusters @ 0.5 | ✓ VERIFIED | ASCII text, starts `BALLWARD 2`, 15 vertices/14 edges, no boost markers. Read CHECK passes live. |
| `.planning/RELEASE-NOTES-v1.7.md` | v1.7.2 Breaking changes note | ✓ VERIFIED | Contains v1.7.2 patch cycle + "Breaking changes" + BALLWARD details. |
| `.github/workflows/release.yml` | ccache restore+save + env block | ✓ VERIFIED | 3 ccache restore + 3 ccache save steps, job-level CCACHE_* env, valid YAML. |
| `source/DOCKING/COMMON/sources.cmake` | item ③ /O1 skipped as moot OR disabled hatch | ✓ VERIFIED | Unchanged; no /O1 or COMPILE_OPTIONS active. Skip documented in 02-SUMMARY (boost removal dropped TU to ~3.9s). Acceptable "OR" branch. |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| serializeWardClusterTree | deserializeWardClusterTree | shared BALLWARD magic+version + payload layout | ✓ WIRED | Writer (lines 97-197) and reader (199-465) share magic/version/width header + identical field ordering; round-trip CHECK passes live. |
| test/PoseClustering_test3.C:360 | test/data/PoseClustering_wardtree.dat | deserializeWardClusterTree reads committed fixture | ✓ WIRED | test3:360 `File tree(BALL_TEST_DATA_PATH(PoseClustering_wardtree.dat))` → deserialize (default TEXT) → 6 clusters @ 0.5; passes live. |
| release.yml ccache key | ci.yml ccache key (ccache-<os>-pch-v2-) | shared restore-keys prefix | ✓ WIRED | Identical key + restore-keys lineage; release reuses CI-warmed cache. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| PoseClustering_wardtree.dat | cluster_tree_ poses/size/merged_at | genuine TEXT-variant writer output over the live COM-distance / NN-chain-ward / threshold-0.00 tree | ✓ Yes — 15 real vertices, 14 edges, 9-digit merged_at (e.g. 2.34917712) | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Both tests build against final hardened serializer | `cmake --build build --target PoseClustering_test1 PoseClustering_test3` | Built target, clean | ✓ PASS |
| Round-trip + fixture-read + graphviz pass | `ctest --test-dir build -R "PoseClustering_test1$\|PoseClustering_test3$"` | 100% passed (2/2), test1=0.12s test3=0.04s | ✓ PASS |
| release.yml is valid YAML | `python3 -c "yaml.safe_load(...)"` | exit 0 | ✓ PASS |
| No debt markers in modified files | `grep -E "TBD\|FIXME\|XXX\|TODO\|HACK"` on serializer/header/release.yml | none | ✓ PASS |

### Probe Execution

| Probe | Command | Result | Status |
| ----- | ------- | ------ | ------ |
| — | — | No project `scripts/*/tests/probe-*.sh` declared or implied for this phase; verification uses the project's ctest suite (run live above). | N/A |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| BUILD-ACCEL-06 | 999.50-01-PLAN | boost::serialization eliminated from poseClusteringSerialization.C; hand-rolled reader/writer; API + bool binary unchanged; fixture regenerated; callers rebuilt; test3+test1 green; format break noted | ✓ SATISFIED | REQUIREMENTS.md line 100 marked `[x]`. Truths 1-8 verified; both tests pass live. |
| BUILD-ACCEL-07 | 999.50-02-PLAN | release.yml restores/saves actions/cache for ccache dir, key aligned with ci.yml | ✓ SATISFIED | REQUIREMENTS.md line 101 marked `[x]`. Truths 9-13 verified. |

No orphaned requirements: REQUIREMENTS.md maps only BUILD-ACCEL-06/07 to Phase 999.50, both claimed by plans 01/02 respectively.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| — | — | None | — | No debt markers, no stubs, no empty implementations, no hardcoded-empty data in production files. `center` remains intentionally unserialized (matches prior boost payload; documented, not a new stub). |

### Human Verification Required

None. All truths verified programmatically; both tests run green live on macOS (the local oracle). Tri-OS CI (release.yml ccache warming + cross-platform build) is the cross-platform oracle for the binary-variant width/byte-order paths, but those are not exercised by the committed TEXT fixture and are guarded by detect-and-throw checks; no human action is required for goal achievement.

### Gaps Summary

No gaps. boost::serialization is fully removed (only the in-memory `adjacency_list.hpp` remains, which is the runtime graph type, not the on-disk format). The hand-rolled BALLWARD magic+version reader/writer round-trips losslessly in both variants, fails loudly on old/corrupt/truncated streams (CR-01 read-path hardening confirmed present in the deserialize code), preserves the public API and `bool binary` flag byte-for-byte, regenerates the committed fixture in the new TEXT format (verified deserializes to 6 clusters @ 0.5 with the five specific scores), and documents the format break under v1.7.2 Breaking changes. release.yml gains a new job-level CCACHE_* env block plus per-OS ccache restore/save aligned with ci.yml's `ccache-<os>-pch-v2-` lineage, and remains valid YAML. The MSVC /O1 fallback (item ③) was correctly skipped as moot. Both PoseClustering_test3 and PoseClustering_test1 pass live; the two TOOLS callers and the graphviz sibling TU are unchanged.

---

_Verified: 2026-05-21_
_Verifier: Claude (gsd-verifier)_
