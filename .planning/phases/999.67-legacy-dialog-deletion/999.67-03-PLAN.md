---
phase: 999.67-legacy-dialog-deletion
plan: 03
type: execute
wave: 3
depends_on: ["999.67-01", "999.67-02"]
files_modified:
  - source/APPLICATIONS/BALLVIEW/mainframe.C
  - source/APPLICATIONS/BALLVIEW/mainframe.h
  - include/BALL/VIEW/KERNEL/mainControl.h
  - source/VIEW/KERNEL/mainControl.C
  - source/VIEW/WIDGETS/molecularControl.C
  - source/VIEW/WIDGETS/geometricControl.C
  - source/APPLICATIONS/BALLVIEW/demoTutorialDialog.C
  - source/APPLICATIONS/DCD2PNG/mainframe.C
  - source/APPLICATIONS/DCD2PNG/mainframe.h
  - include/BALL/VIEW/KERNEL/message.h
  - source/PYTHON/EXTENSIONS/VIEW/message.sip
  - source/VIEW/DIALOGS/displayProperties.C
  - source/VIEW/DIALOGS/displayProperties.ui
  - include/BALL/VIEW/DIALOGS/displayProperties.h
  - source/VIEW/DIALOGS/modelSettingsDialog.C
  - source/VIEW/DIALOGS/modelSettingsDialog.ui
  - include/BALL/VIEW/DIALOGS/modelSettingsDialog.h
  - source/VIEW/DIALOGS/coloringSettingsDialog.C
  - source/VIEW/DIALOGS/coloringSettingsDialog.ui
  - include/BALL/VIEW/DIALOGS/coloringSettingsDialog.h
  - source/VIEW/DIALOGS/sources.cmake
  - source/PYTHON/EXTENSIONS/VIEW/displayProperties.sip
  - source/PYTHON/EXTENSIONS/VIEW/VIEW.sip
  - include/BALL/VIEW/KERNEL/legacySettingsHelper.h
  - source/VIEW/KERNEL/legacySettingsHelper.C
  - source/VIEW/KERNEL/sources.cmake
autonomous: false
requirements: [LEGACYDEL-01, LEGACYDEL-02, LEGACYDEL-03]
must_haves:
  truths:
    - "Loading a molecule still auto-creates a default representation and renders (DisplayProperties auto-create behavior re-homed, not lost)"
    - "displayProperties, modelSettingsDialog, coloringSettingsDialog triples no longer exist in the tree"
    - "grep -r for all 9 superseded dialog type names across source/ + include/ returns 0 live references"
    - "Tools menu in a running BALLView no longer shows a Legacy Settings submenu"
    - "LegacySettingsHelper class is deleted and unreferenced"
    - "Inspector model/coloring/material changes re-render the scene correctly (human-verified)"
  artifacts:
    - path: "source/APPLICATIONS/BALLVIEW/mainframe.C"
      provides: "BALLView wiring with no DisplayProperties construction and no Legacy Settings submenu"
      contains: "MainControl"
    - path: "source/PYTHON/EXTENSIONS/VIEW/VIEW.sip"
      provides: "SIP module with displayProperties.sip %Include removed"
      contains: "%Include"
  key_links:
    - from: "molecule load path"
      to: "default representation creation"
      via: "re-homed NEW_MOLECULE subscriber → RepresentationBuilder::createRepresentation via existing Notifier_ adapter"
      pattern: "[Rr]epresentation"
    - from: "Tools menu builder"
      to: "(removed) LegacySettingsHelper::legacyStackNames"
      via: "submenu build deletion"
      pattern: "Legacy Settings"
---

<objective>
Delete the LYNCHPIN triple — `displayProperties`, `modelSettingsDialog`, `coloringSettingsDialog` — retire the Tools → Legacy Settings menu + `LegacySettingsHelper` (LEGACYDEL-02), and gate the phase on a render-parity smoke-UAT (LEGACYDEL-03).

CRITICAL ground-truth correction (verified on the v1.8 base): the roadmap calls displayProperties "dormant," but it is NOT. DisplayProperties is still heavily live-wired:
- Constructed in BALLView mainframe.C:405 and DCD2PNG mainframe.C:42 (member display_properties_ at DCD2PNG mainframe.h:65).
- `DisplayProperties::getInstance(0)` is called from mainframe.C:634 (default-representation seed: selectModel/selectColoringMethod/selectMode/setTransparency/setSurfaceDrawingPrecision), mainframe.C:1084 (addToolBarEntries), mainControl.C:2116/2155/2178 (`enableCreationForNewMolecules` guard around file load), demoTutorialDialog.C:220/571.
- It OWNS the model_settings_ (`new ModelSettingsDialog`, displayProperties.C:145) and coloring_settings_ (`new ColoringSettingsDialog`, displayProperties.C:151) children — that is the entire lynchpin triple.
- It handles `ShowDisplayPropertiesMessage` (displayProperties.C:353), fired from molecularControl.C:1499 and geometricControl.C:404.
- Its load-bearing role is to be the `CompositeMessage::NEW_MOLECULE` subscriber that AUTO-CREATES a default representation when a new molecule is added — exactly the "load a PDB → scene renders" path that LEGACYDEL-03 verifies.

IMPORTANT re-home scope correction (verified — this is NARROWER than "port the auto-rep engine"): 999.65 ALREADY repointed DisplayProperties' orchestration onto `RepresentationBuilder::createRepresentation(...)` through the existing `Notifier_` adapter (displayProperties.C:321-350; adapter declared at displayProperties.h:308), and a DEFAULT-CONSTRUCTED `RepresentationSpec` already yields exactly the NEW_MOLECULE default representation (representationBuilder.h:62). So the re-home is NOT "re-implement the auto-representation algorithm." It is a subscriber-registration move: stand up a small non-dialog `ConnectionObject` as the `CompositeMessage::NEW_MOLECULE` subscriber, build a default `RepresentationSpec`, and call the ALREADY-WIRED `RepresentationBuilder::createRepresentation` reusing the existing `Notifier_` adapter — while preserving the `enableCreationForNewMolecules` suppress-during-load guard. The reference branch `v1.7.3-legacy-deletion` never deleted this triple — there is no recipe to copy. The Inspector model/coloring sections (modelSection.h, coloringSection.h) plus the 999.65 RepresentationBuilder are the supersession surface.

Purpose: Finish the legacy-dialog tail by removing the last and largest legacy settings cluster without regressing the load-and-render path, and retire its menu surface.
Output: Lynchpin triple deleted, Legacy Settings menu + helper gone, smoke-UAT passed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Live DisplayProperties consumers to re-home (verified):
#   BALLView mainframe.C:405 (construct), :634 (default-rep seed: selectModel/selectColoringMethod/
#                              selectMode/setTransparency/setSurfaceDrawingPrecision), :1084 (addToolBarEntries)
#   mainControl.C:2116/2155/2178 (enableCreationForNewMolecules — suppress auto-create during file load,
#                              re-enable after; a guard toggle)
#   molecularControl.C:1499 + geometricControl.C:404  notify_(new ShowDisplayPropertiesMessage)
#   demoTutorialDialog.C:220 (selectModel) :571 (getInstance) — also :585/:587 touch the suppress guard
#   DCD2PNG mainframe.C:42 + mainframe.h:65 (member) — see DCD2PNG note below
#
# Re-home vehicles (already wired by 999.65 — REUSE, do not reinvent):
#   displayProperties.C:321-350  DisplayProperties already orchestrates via RepresentationBuilder::createRepresentation
#   displayProperties.h:308      the existing Notifier_ adapter that bridges into RepresentationBuilder
#   representationBuilder.h:62   a default-constructed RepresentationSpec == the NEW_MOLECULE default representation
#   CompositeMessage::NEW_MOLECULE is the subscription trigger
#   enableCreationForNewMolecules suppress guard touch points: mainControl.C:2116/2155/2178,
#                              demoTutorialDialog.C:585/587 — preserve this guard on the re-homed subscriber
#
# DCD2PNG note (verified): DCD2PNG ONLY CONSTRUCTS DisplayProperties (mainframe.C:42) and never calls a
#   method on it. Its sole role is to EXIST as a NEW_MOLECULE subscriber that auto-builds the rep. So there
#   is NO method-call to port — the fix is to re-REGISTER the re-homed non-dialog NEW_MOLECULE subscriber in
#   DCD2PNG's message tree in place of the DisplayProperties construction.
#
# Supersession surface (already shipped v1.7.4 / 999.65):
#   include/BALL/VIEW/WIDGETS/inspector/sections/{modelSection,coloringSection,materialSection}.h
#   RepresentationBuilder (999.65) — the command-shaped default-representation path
# message.h:677 ShowDisplayPropertiesMessage + message.sip:179 — remove if no remaining sender.
#
# LEGACYDEL-02 (Tools → Legacy Settings):
#   mainframe.C:460-478 build submenu via VIEW::LegacySettingsHelper::legacyStackNames()
#   mainframe.C:1210-1269 handler (displayName/migrationNoticeTitle/migrationNoticeBody)
#   mainframe.h:71 submenu-entries decl
#   include/BALL/VIEW/KERNEL/legacySettingsHelper.h + source/VIEW/KERNEL/legacySettingsHelper.C
#   source/VIEW/KERNEL/sources.cmake:61 legacySettingsHelper.C entry
#
# DO-NOT-TOUCH (substring collisions, all LIVE): modelInformation's
#   modelMuteableByDisplayProperties / coloringMuteableByDisplayProperties methods,
#   ShowDisplayPropertiesMessage only if a sender survives, clippingPlane.sip / label.sip / setClippingPlane.*
#
# Project builds with -DQT_NO_KEYWORDS (Q_EMIT/Q_SIGNALS/Q_SLOTS).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-home the DisplayProperties NEW_MOLECULE auto-create subscriber, then detach all consumers</name>
  <files>source/APPLICATIONS/BALLVIEW/mainframe.C, source/VIEW/KERNEL/mainControl.C, include/BALL/VIEW/KERNEL/mainControl.h, source/VIEW/WIDGETS/molecularControl.C, source/VIEW/WIDGETS/geometricControl.C, source/APPLICATIONS/BALLVIEW/demoTutorialDialog.C, source/APPLICATIONS/DCD2PNG/mainframe.C, source/APPLICATIONS/DCD2PNG/mainframe.h, include/BALL/VIEW/KERNEL/message.h, source/PYTHON/EXTENSIONS/VIEW/message.sip</files>
  <action>FIRST confirm the verified ground truth in context: 999.65 already repointed DisplayProperties' orchestration onto `RepresentationBuilder::createRepresentation(...)` via the existing `Notifier_` adapter (displayProperties.C:321-350, adapter at displayProperties.h:308), and a default-constructed `RepresentationSpec` already produces the NEW_MOLECULE default representation (representationBuilder.h:62). Read those exact lines to confirm before changing anything.

DECISION GATE: Because the rep-building engine is ALREADY wired through RepresentationBuilder, the re-home is a SUBSCRIBER-REGISTRATION move, not an algorithm port. Concretely:
  (a) Register a small NON-DIALOG `ConnectionObject` as the `CompositeMessage::NEW_MOLECULE` subscriber (the role DisplayProperties currently plays as the auto-create owner).
  (b) On NEW_MOLECULE, build a DEFAULT `RepresentationSpec` (representationBuilder.h:62 — this is the STICK/ELEMENT/SOLID default seen seeded at mainframe.C:634).
  (c) Call the already-wired `RepresentationBuilder::createRepresentation(...)` reusing the existing `Notifier_` adapter — do NOT re-implement the build engine; reuse the 999.65 path.
  (d) Preserve the `enableCreationForNewMolecules` suppress-during-load guard so auto-create stays suppressed while a file is loading and re-enables after (guard touch points: mainControl.C:2116/2155/2178, demoTutorialDialog.C:585/587). Move the guard onto the re-homed subscriber.
If, on reading the code, the NEW_MOLECULE auto-create is somehow NOT reachable through the RepresentationBuilder/Notifier_ path as described, STOP and surface a `## BLOCKER` in the SUMMARY describing the gap and proposing a follow-up phase. Do NOT ship a build where loading a PDB renders nothing.

Then detach every consumer:
- mainframe.C: remove `new DisplayProperties(...)` (:405), the default-rep seed block (:634 — its STICK/ELEMENT/SOLID defaults are now the default `RepresentationSpec` on the re-homed subscriber), and `DisplayProperties::getInstance(0)->addToolBarEntries(tb)` (:1084 — if the model/coloring quick-action toolbar buttons are still wanted, source them from the controller/Inspector; if already provided elsewhere, drop). Remove `#include displayProperties.h` (:55).
- mainControl.C: re-home the `enableCreationForNewMolecules` guard (:2116/2155/2178) onto the new subscriber (it is a suppress-during-load toggle). Remove `#include displayProperties.h` (:40) and the mainControl.h:93 doc reference.
- molecularControl.C:1499 + geometricControl.C:404: remove `notify_(new ShowDisplayPropertiesMessage)`. If no sender remains, delete ShowDisplayPropertiesMessage from message.h:677-682 and message.sip:179-186.
- demoTutorialDialog.C:220/571: re-point the tutorial's selectModel/getInstance onto the re-homed model-selection path, and keep its suppress-guard touches (:585/587) pointed at the re-homed guard, or drop the step if obsolete (preserve the tutorial's visible behavior).
- DCD2PNG mainframe.C:42 + mainframe.h:65: DCD2PNG NEVER calls a method on DisplayProperties — it only constructs it so it exists as a NEW_MOLECULE subscriber. There is NO method-call to port. Replace the `display_properties_` construction (member at mainframe.h:65) with REGISTRATION of the re-homed non-dialog NEW_MOLECULE subscriber into DCD2PNG's message tree, so batch renders still auto-build the default representation. Verify DCD2PNG still produces a rendered (non-blank) image.

This is the largest, riskiest change in the phase. Commit as one logical step: `refactor(999.67-03): re-home DisplayProperties NEW_MOLECULE subscriber + detach all consumers` + Co-Authored-By trailer. The tree MUST compile and a loaded molecule MUST still render at this commit boundary.</action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && cmake --preset ci-macos -DBALL_LICENSE=GPL && cmake --build --preset ci-macos --target BALL VIEW BALLView contract_tests build_tests && ! { grep -rn "DisplayProperties::getInstance\|new DisplayProperties\|DIALOGS/displayProperties.h" source include 2>/dev/null | grep -v "/DIALOGS/displayProperties\."; } && ctest --test-dir build/ci-macos -L contract</automated>
  </verify>
  <done>No live DisplayProperties construction/getInstance/include outside the dialog's own files; the NEW_MOLECULE auto-create subscriber is re-homed onto a non-dialog ConnectionObject that builds a default RepresentationSpec and calls RepresentationBuilder::createRepresentation via the existing Notifier_ adapter, with the enableCreationForNewMolecules suppress guard preserved (or blocker surfaced); DCD2PNG re-registers the re-homed subscriber in place of constructing DisplayProperties; ShowDisplayPropertiesMessage removed if senderless; BALL/VIEW/BALLView build clean; contract suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the lynchpin triple + drop build/SIP registration; retire the Legacy Settings menu + LegacySettingsHelper (LEGACYDEL-02)</name>
  <files>source/VIEW/DIALOGS/displayProperties.C, source/VIEW/DIALOGS/displayProperties.ui, include/BALL/VIEW/DIALOGS/displayProperties.h, source/VIEW/DIALOGS/modelSettingsDialog.C, source/VIEW/DIALOGS/modelSettingsDialog.ui, include/BALL/VIEW/DIALOGS/modelSettingsDialog.h, source/VIEW/DIALOGS/coloringSettingsDialog.C, source/VIEW/DIALOGS/coloringSettingsDialog.ui, include/BALL/VIEW/DIALOGS/coloringSettingsDialog.h, source/VIEW/DIALOGS/sources.cmake, source/PYTHON/EXTENSIONS/VIEW/displayProperties.sip, source/PYTHON/EXTENSIONS/VIEW/VIEW.sip, source/APPLICATIONS/BALLVIEW/mainframe.C, source/APPLICATIONS/BALLVIEW/mainframe.h, include/BALL/VIEW/KERNEL/legacySettingsHelper.h, source/VIEW/KERNEL/legacySettingsHelper.C, source/VIEW/KERNEL/sources.cmake</files>
  <action>Two deletions in one atomic commit (both pure removal now that Task 1 cleared consumers):

(a) Lynchpin triple: grep-proof that displayProperties/modelSettingsDialog/coloringSettingsDialog references are now only provenance comments (modelProcessorFactory.C / colorProcessorFactory.C cite them in comments; modelController/coloringController headers cite them). The modelInformation methods `modelMuteableByDisplayProperties` / `coloringMuteableByDisplayProperties` are DIFFERENTLY-NAMED LIVE methods that merely contain the substring — do NOT touch them. git rm the .C+.ui+.h for all three (9 files). From sources.cmake drop displayProperties.{C,ui}, modelSettingsDialog.{C,ui}, coloringSettingsDialog.{C,ui}. git rm source/PYTHON/EXTENSIONS/VIEW/displayProperties.sip and delete its `%Include displayProperties.sip` line from VIEW.sip (line 94). Confirm modelSettingsDialog/coloringSettingsDialog have no own .sip files.

(b) LEGACYDEL-02 — Tools → Legacy Settings: in mainframe.C remove the submenu build (~:460-478, the `tools_menu->addMenu(tr("Legacy Settings"))` + the legacyStackNames loop) and the handler (~:1210-1269 using LegacySettingsHelper displayName/migrationNoticeTitle/migrationNoticeBody). Remove the submenu-entries decl at mainframe.h:71 and any `#include legacySettingsHelper.h`. git rm include/BALL/VIEW/KERNEL/legacySettingsHelper.h + source/VIEW/KERNEL/legacySettingsHelper.C, and drop the `legacySettingsHelper.C` entry from source/VIEW/KERNEL/sources.cmake:61.

Commit: `refactor(999.67-03): delete displayProperties/model/coloring lynchpin triple + retire Legacy Settings menu` + Co-Authored-By trailer.</action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && ! ls source/VIEW/DIALOGS/displayProperties.* source/VIEW/DIALOGS/modelSettingsDialog.* source/VIEW/DIALOGS/coloringSettingsDialog.* include/BALL/VIEW/DIALOGS/displayProperties.h include/BALL/VIEW/DIALOGS/modelSettingsDialog.h include/BALL/VIEW/DIALOGS/coloringSettingsDialog.h include/BALL/VIEW/KERNEL/legacySettingsHelper.h source/VIEW/KERNEL/legacySettingsHelper.C 2>/dev/null && ! grep -q "displayProperties.sip" source/PYTHON/EXTENSIONS/VIEW/VIEW.sip && ! grep -qE "legacySettingsHelper|LegacySettingsHelper|Legacy Settings" source/APPLICATIONS/BALLVIEW/mainframe.C source/APPLICATIONS/BALLVIEW/mainframe.h && ! grep -q "legacySettingsHelper" source/VIEW/KERNEL/sources.cmake && echo CLEAN</automated>
  </verify>
  <done>Lynchpin triple + legacySettingsHelper files gone; VIEW.sip free of displayProperties.sip; mainframe free of the Legacy Settings menu + handler + helper include; modelMuteableByDisplayProperties/coloringMuteableByDisplayProperties live methods preserved; atomic commit created.</done>
</task>

<task type="auto">
  <name>Task 3: Final all-9-gone grep gate + full build + contract gate + push for CI</name>
  <files>(build + verification only)</files>
  <action>Capture the contract-test baseline count FIRST: `ctest --test-dir build/ci-macos -L contract -N | tail -1` — record it in the SUMMARY as the baseline. Then run the roadmap Success-Criterion-1 gate: grep -r across source/ + include/ for all 9 superseded dialog type names (ClippingDialog, LabelDialog, LightSettings, MaterialSettings, StageSettings, StereoSettingsDialog, DisplayProperties, ModelSettingsDialog, ColoringSettingsDialog). Filter out comment lines and the differently-named `*MuteableByDisplayProperties` methods, and inspect every surviving hit. Result must be 0 live references. Then configure with `cmake --preset ci-macos -DBALL_LICENSE=GPL`, build `BALL VIEW BALLView contract_tests build_tests` clean, and run `ctest -L contract`: ALL contract tests must pass AND the test count must be UNCHANGED from the captured baseline (do not assume any fixed number; gate strictly on the baseline). Push the v1.8 branch and confirm CI (Linux contract gate + tri-OS build — the authoritative gate) is green before proceeding to the UAT checkpoint.</action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && cmake --preset ci-macos -DBALL_LICENSE=GPL && cmake --build --preset ci-macos --target BALL VIEW BALLView contract_tests build_tests && BASE=$(ctest --test-dir build/ci-macos -L contract -N | tail -1) && echo "contract baseline: $BASE" && ctest --test-dir build/ci-macos -L contract && AFTER=$(ctest --test-dir build/ci-macos -L contract -N | tail -1) && [ "$BASE" = "$AFTER" ] && for c in ClippingDialog LabelDialog LightSettings StageSettings StereoSettingsDialog DisplayProperties ModelSettingsDialog ColoringSettingsDialog; do hits=$(grep -rn "$c" source include 2>/dev/null | grep -vE '^[^:]+:[0-9]+:[[:space:]]*//|MuteableByDisplayProperties'); [ -n "$hits" ] && { echo "LIVE REF $c:"; echo "$hits"; exit 1; }; done; echo ALL_NINE_CLEAR</automated>
  </verify>
  <done>Zero live references to all 9 dialog type names; build clean; contract suite green AND contract-test count unchanged from the baseline captured at the start of the task; branch pushed and CI tri-OS green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: LEGACYDEL-03 render-parity smoke-UAT</name>
  <what-built>The legacy displayProperties/model/coloring settings dialogs and the Tools → Legacy Settings menu have been removed; the load-a-molecule → default-representation behavior was re-homed onto a non-dialog NEW_MOLECULE subscriber that drives the v1.7.4/999.65 RepresentationBuilder surface. This checkpoint confirms no render regression in both the interactive app and the DCD2PNG batch renderer, and that the menu is gone.</what-built>
  <how-to-verify>
    1. Launch the freshly-built BALLView (the binary from this branch's `cmake --build` — e.g. the `BALLView` target output).
    2. File → Open a PDB (any small structure, e.g. a downloaded 1CRN, or File → Fetch PDB → enter a 4-letter code). EXPECT: the molecule loads AND a default representation renders automatically in the scene (this is the re-homed NEW_MOLECULE auto-create subscriber — if the scene is empty after load, that is a regression).
    3. Open the Inspector. In the Model section, change the model (e.g. Stick → Ball-and-Stick → Cartoon/Ribbon). EXPECT: the scene re-renders with the new model each time.
    4. In the Coloring section, change the coloring method (e.g. Element → Residue Name → Chain). EXPECT: the scene recolors correctly.
    5. In the Material section, change a material property (e.g. transparency or shininess). EXPECT: the scene material updates (this exercises the plan-02 modifyRepresentationDialog material migration too).
    6. Open the Tools menu. EXPECT: there is NO "Legacy Settings" submenu.
    7. REQUIRED — DCD2PNG batch render: run a DCD2PNG batch render of any structure and confirm the output image shows a rendered molecule (NOT blank). DCD2PNG has no UI and no other guard against blank batch output — its only auto-create path was the DisplayProperties NEW_MOLECULE subscriber re-homed in this plan, so this step is mandatory, not optional. A blank image here is a regression.
  </how-to-verify>
  <resume-signal>Type "approved" if every EXPECT held (molecule renders on load, model/coloring/material changes re-render, no Legacy Settings menu, AND DCD2PNG batch output is a rendered non-blank image). Otherwise describe exactly which step regressed and what you saw — that becomes a gap to close.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| molecule load → default representation | DisplayProperties is the current NEW_MOLECULE auto-create subscriber; re-homing must preserve the "loaded molecule renders" invariant |
| Inspector section → Scene render | model/coloring/material changes must reach the renderer via the controller surface, not the deleted dialogs |
| DCD2PNG message tree → batch render | DCD2PNG's only auto-create path is the NEW_MOLECULE subscriber; re-registration must keep batch output non-blank |
| menu builder → LegacySettingsHelper | removing the helper must not leave a dangling submenu build or handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99767-06 | Denial of Service | loaded molecule renders blank (auto-create lost) | mitigate | Task 1 re-homes the NEW_MOLECULE subscriber onto RepresentationBuilder via the existing Notifier_ adapter (default RepresentationSpec) or surfaces a blocker; Task 4 UAT step 2 explicitly verifies render-on-load |
| T-99767-07 | Tampering | deleting live modelMuteableByDisplayProperties / coloringMuteableByDisplayProperties by substring match | mitigate | Task 2 explicit DO-NOT-TOUCH list; Task 3 grep gate filters these names |
| T-99767-08 | Repudiation | DCD2PNG batch output silently regresses | mitigate | Task 1 re-registers the re-homed NEW_MOLECULE subscriber in DCD2PNG's message tree (no method-call port exists); Task 4 step 7 (REQUIRED) confirms batch render |
| T-99767-09 | Information Disclosure | dangling Legacy Settings handler references freed helper | mitigate | Task 2 removes submenu build + handler + helper together in one atomic commit; build gate proves no dangling symbol |
</threat_model>

<verification>
- NEW_MOLECULE auto-create subscriber re-homed onto RepresentationBuilder via the existing Notifier_ adapter; loading a PDB renders a default representation.
- displayProperties/modelSettingsDialog/coloringSettingsDialog triples deleted; SIP + sources.cmake scrubbed.
- grep for all 9 dialog type names returns 0 live references.
- Tools → Legacy Settings submenu + handler + LegacySettingsHelper removed.
- `BALL VIEW BALLView contract_tests build_tests` build clean; `ctest -L contract` green with an unchanged test count vs. the captured baseline; CI tri-OS green.
- Human UAT confirms model/coloring/material changes re-render, the menu is gone, AND DCD2PNG batch output is non-blank.
</verification>

<success_criteria>
- LEGACYDEL-01: lynchpin triple deleted; libVIEW + BALLView build clean tri-OS (CI green).
- LEGACYDEL-02: Tools → Legacy Settings menu + LegacySettingsHelper retired.
- LEGACYDEL-03: render-parity smoke-UAT approved by the user (including the required DCD2PNG batch-render step).
- Atomic commits (detach, then delete+menu) each leaving the tree compiling, with Co-Authored-By trailers.
</success_criteria>

<output>
Create `.planning/phases/999.67-legacy-dialog-deletion/999.67-03-SUMMARY.md` when done. Record: the captured contract-test baseline count, where the NEW_MOLECULE subscriber was re-homed (the non-dialog ConnectionObject + default RepresentationSpec + reused Notifier_ adapter, or BLOCKER if it could not be), the DCD2PNG re-registration, the final all-9-clear grep output, the CI run URL/status, and the UAT result (including DCD2PNG batch render).
</output>
</content>
