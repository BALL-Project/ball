---
phase: 999.67-legacy-dialog-deletion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - source/VIEW/DIALOGS/lightSettings.C
  - source/VIEW/DIALOGS/lightSettings.ui
  - include/BALL/VIEW/DIALOGS/lightSettings.h
  - source/VIEW/DIALOGS/stageSettings.C
  - source/VIEW/DIALOGS/stageSettings.ui
  - include/BALL/VIEW/DIALOGS/stageSettings.h
  - source/VIEW/DIALOGS/stereoSettingsDialog.C
  - source/VIEW/DIALOGS/stereoSettingsDialog.ui
  - include/BALL/VIEW/DIALOGS/stereoSettingsDialog.h
  - source/VIEW/DIALOGS/sources.cmake
  - source/PYTHON/EXTENSIONS/VIEW/lightSettings.sip
  - source/PYTHON/EXTENSIONS/VIEW/VIEW.sip
autonomous: true
requirements: [LEGACYDEL-01]
must_haves:
  truths:
    - "lightSettings, stageSettings, stereoSettingsDialog triples no longer exist in the tree"
    - "libVIEW + BALLView build clean after the three dormant triples are removed"
    - "VIEW.sip no longer %Includes lightSettings.sip; the lightSettings.sip file is gone"
  artifacts:
    - path: "source/VIEW/DIALOGS/sources.cmake"
      provides: "Dialog build registration with the 3 dormant .C/.ui entries removed"
      contains: "SOURCES_LIST"
  key_links:
    - from: "source/PYTHON/EXTENSIONS/VIEW/VIEW.sip"
      to: "(removed) lightSettings.sip"
      via: "%Include line deletion"
      pattern: "Include lightSettings"
---

<objective>
Delete the three genuinely-dormant legacy settings dialog triples — `lightSettings`, `stageSettings`, `stereoSettingsDialog` — on the current v1.8 base. Ground truth (verified): these three have NO live `#include`, NO instantiation, NO `getInstance`, and NO `Show*Message` wiring anywhere outside their own files; the only tree-wide references are provenance comments in the corresponding controllers (lightController/stageController/stereoController) and a "dropped in 999.58-03" comment in scene.C / demoTutorialDialog.C. They are safe, low-risk deletes that establish the deletion pattern for the harder plans.

Purpose: Land the easy, blocker-free deletions first so plans 02/03 (which require real detachment work) start from a clean, smaller surface. This is a FRESH deletion on the current base — NOT a cherry-pick or merge of `v1.7.3-legacy-deletion`. That branch is reference-only for *which files* are deletable.

WAVE NOTE: this plan runs in wave 1. Plan 999.67-02 edits the same `UI_LIST` / `SOURCES_LIST` blocks of `source/VIEW/DIALOGS/sources.cmake`, so it is serialized to depend on this plan (wave 2) — the two are NOT parallel.

Output: Three triples removed, their sources.cmake + SIP registration dropped, tree builds clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Ground-truth state of the dormant triples (verified on v1.8 base):
# - #include <BALL/VIEW/DIALOGS/lightSettings.h>  : only in lightSettings.C (self) + lightSettings.sip
# - #include <BALL/VIEW/DIALOGS/stageSettings.h>  : only in stageSettings.C (self)
# - #include <BALL/VIEW/DIALOGS/stereoSettingsDialog.h> : only in stereoSettingsDialog.C (self)
# - No Show{Light,Stage,Stereo}Settings* message wiring exists.
# - controllers (lightController/stageController/stereoController) reference these ONLY in comments.
# - VIEW.sip %Include lines present: line 94 displayProperties.sip, line 95 lightSettings.sip.
#   ONLY lightSettings.sip among this plan's three is %Included; stage/stereo have no .sip files.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep-proof the three dormant triples are reference-free</name>
  <files>(read-only investigation; no writes)</files>
  <action>For each of lightSettings, stageSettings, stereoSettingsDialog, run a tree-wide grep over source/ and include/ for: the header include path, the class name (LightSettings, StageSettings, StereoSettingsDialog), `getInstance`, `new <Class>`, and any `Show<Class>` message. Exclude (a) the dialog's own .h/.C/.ui files and (b) the dialog's own .sip file. Classify every remaining hit as one of: COMMENT (provenance breadcrumb — expected, OK), or LIVE (#include in another TU, instantiation, member, message wiring — BLOCKER). Confirm every hit is COMMENT. If ANY live reference is found that is not a provenance comment, STOP and surface it as a blocker in the SUMMARY rather than deleting around it.</action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && for d in lightSettings stageSettings stereoSettingsDialog; do grep -rIn "DIALOGS/$d.h" source include 2>/dev/null | grep -v "/DIALOGS/$d\.\(C\|h\)" | grep -v "EXTENSIONS/VIEW/$d.sip"; done; echo "EXIT_OK_IF_ONLY_COMMENTS_ABOVE"</automated>
  </verify>
  <done>Every non-own reference to the three dialogs is confirmed to be a provenance comment; no live #include / instantiation / message wiring exists. Findings recorded.</done>
</task>

<task type="auto">
  <name>Task 2: git rm the three triples and drop build + SIP registration</name>
  <files>source/VIEW/DIALOGS/lightSettings.C, source/VIEW/DIALOGS/lightSettings.ui, include/BALL/VIEW/DIALOGS/lightSettings.h, source/VIEW/DIALOGS/stageSettings.C, source/VIEW/DIALOGS/stageSettings.ui, include/BALL/VIEW/DIALOGS/stageSettings.h, source/VIEW/DIALOGS/stereoSettingsDialog.C, source/VIEW/DIALOGS/stereoSettingsDialog.ui, include/BALL/VIEW/DIALOGS/stereoSettingsDialog.h, source/VIEW/DIALOGS/sources.cmake, source/PYTHON/EXTENSIONS/VIEW/lightSettings.sip, source/PYTHON/EXTENSIONS/VIEW/VIEW.sip</files>
  <action>git rm the .C + .ui + .h for each of lightSettings, stageSettings, stereoSettingsDialog (9 files). In source/VIEW/DIALOGS/sources.cmake remove the matching entries: from UI_LIST drop lightSettings.ui, stageSettings.ui, stereoSettingsDialog.ui; from SOURCES_LIST drop lightSettings.C, stageSettings.C, stereoSettingsDialog.C. git rm source/PYTHON/EXTENSIONS/VIEW/lightSettings.sip and delete its `%Include lightSettings.sip` line from VIEW.sip (currently line 95). stageSettings and stereoSettingsDialog have no .sip files — confirm none exist before assuming. Also scrub the now-orphaned provenance comments in lightController/stageController/stereoController only if they cite a deleted line range that would mislead a future reader; preserve the rationale prose (matches the reference branch's "scrub dead breadcrumbs, rationale preserved" approach). Keep this a single atomic commit. Commit message: `refactor(999.67-01): delete dormant lightSettings/stageSettings/stereoSettings triples` + the required Co-Authored-By trailer.</action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && ! ls source/VIEW/DIALOGS/lightSettings.* source/VIEW/DIALOGS/stageSettings.* source/VIEW/DIALOGS/stereoSettingsDialog.* include/BALL/VIEW/DIALOGS/lightSettings.h include/BALL/VIEW/DIALOGS/stageSettings.h include/BALL/VIEW/DIALOGS/stereoSettingsDialog.h 2>/dev/null && ! grep -qE "lightSettings|stageSettings|stereoSettingsDialog" source/VIEW/DIALOGS/sources.cmake && ! grep -q "lightSettings.sip" source/PYTHON/EXTENSIONS/VIEW/VIEW.sip && echo CLEAN</automated>
  </verify>
  <done>All 9 files gone; sources.cmake has no entries for the three dialogs; VIEW.sip no longer %Includes lightSettings.sip and lightSettings.sip is deleted; single atomic commit created.</done>
</task>

<task type="auto">
  <name>Task 3: Configure + build VIEW/BALLView and run the contract gate</name>
  <files>(build only; no source writes)</files>
  <action>Capture the contract-test baseline count FIRST (the controllers are untouched by this plan, so the contract surface must not change): `ctest --test-dir build/ci-macos -L contract -N | tail -1` — record this number in the SUMMARY as the baseline. (If no build dir exists yet, configure+build first, then capture the baseline before running the suite.) Configure with `cmake --preset ci-macos -DBALL_LICENSE=GPL` (project uses -DQT_NO_KEYWORDS, so Q_EMIT/Q_SIGNALS — no edits needed here). Build the targets `BALL VIEW BALLView contract_tests build_tests`. Then run `ctest -L contract`. ALL `-L contract` tests must pass AND the test count must be UNCHANGED from the captured baseline — any contract regression (a failure OR a changed count) indicates an accidental coupling and is a blocker. Do not assume any fixed number; gate strictly on the baseline you captured. The authoritative tri-OS gate runs in CI on push; this local build is the fast pre-push proof.</action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && cmake --preset ci-macos -DBALL_LICENSE=GPL && cmake --build --preset ci-macos --target BALL VIEW BALLView contract_tests build_tests && BASE=$(ctest --test-dir build/ci-macos -L contract -N | tail -1) && echo "contract baseline: $BASE" && ctest --test-dir build/ci-macos -L contract && AFTER=$(ctest --test-dir build/ci-macos -L contract -N | tail -1) && [ "$BASE" = "$AFTER" ] && echo "CONTRACT_COUNT_UNCHANGED"</automated>
  </verify>
  <done>libVIEW + BALLView link clean with the three triples removed; `ctest -L contract` reports all contract tests passing AND the test count is unchanged from the baseline captured at the start of the task.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build system → linker | Removing source/UI/SIP entries must leave no dangling reference that breaks the build for other OSes |
| SIP layer → Python bindings | Removing lightSettings.sip must not leave a `%Include` dangling in VIEW.sip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99767-01 | Tampering | sources.cmake / VIEW.sip edits | mitigate | Task 3 full configure+build+contract gate proves no dangling reference on the build OS; CI re-runs tri-OS |
| T-99767-02 | Denial of Service | accidental deletion of a still-live symbol | mitigate | Task 1 grep-proof gate classifies every reference as COMMENT vs LIVE before any rm; live ref = surfaced blocker, never deleted around |
</threat_model>

<verification>
- The three dormant triples are absent from source/ and include/.
- sources.cmake and VIEW.sip carry no references to them.
- `cmake --preset ci-macos -DBALL_LICENSE=GPL` configures; `BALL VIEW BALLView contract_tests build_tests` build clean.
- `ctest -L contract` green AND contract-test count unchanged from the baseline captured at the start of Task 3.
</verification>

<success_criteria>
- lightSettings, stageSettings, stereoSettingsDialog triples deleted (.h/.C/.ui each).
- Build + SIP registration for them removed; tree builds clean on macOS; CI tri-OS expected green.
- Contract suite green with an unchanged test count vs. the captured baseline.
- One atomic conventional commit with the Co-Authored-By trailer.
</success_criteria>

<output>
Create `.planning/phases/999.67-legacy-dialog-deletion/999.67-01-SUMMARY.md` when done. Record the captured contract-test baseline count and the grep-proof classification table, and confirm zero live references for the three dormant dialogs.
</output>
</content>
