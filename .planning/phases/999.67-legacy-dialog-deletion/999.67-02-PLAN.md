---
phase: 999.67-legacy-dialog-deletion
plan: 02
type: execute
wave: 2
depends_on: ["999.67-01"]
files_modified:
  - source/VIEW/WIDGETS/geometricControl.C
  - include/BALL/VIEW/WIDGETS/geometricControl.h
  - source/PYTHON/EXTENSIONS/VIEW/geometricControl.sip
  - source/APPLICATIONS/BALLVIEW/mainframe.C
  - include/BALL/VIEW/DIALOGS/modifyRepresentationDialog.h
  - source/VIEW/DIALOGS/modifyRepresentationDialog.C
  - source/VIEW/DIALOGS/clippingDialog.C
  - source/VIEW/DIALOGS/clippingDialog.ui
  - include/BALL/VIEW/DIALOGS/clippingDialog.h
  - source/VIEW/DIALOGS/labelDialog.C
  - source/VIEW/DIALOGS/labelDialog.ui
  - include/BALL/VIEW/DIALOGS/labelDialog.h
  - source/VIEW/DIALOGS/materialSettings.C
  - source/VIEW/DIALOGS/materialSettings.ui
  - include/BALL/VIEW/DIALOGS/materialSettings.h
  - source/VIEW/DIALOGS/sources.cmake
autonomous: true
requirements: [LEGACYDEL-01]
must_haves:
  truths:
    - "GeometricControl::selectClipRepresentations no longer instantiates ClippingDialog"
    - "BALLView mainframe no longer constructs LabelDialog"
    - "modifyRepresentationDialog provides its own material controls and no longer embeds MaterialSettings"
    - "clippingDialog, labelDialog, materialSettings triples no longer exist in the tree"
    - "libVIEW + BALLView build clean after detachment + deletion; contract suite green"
  artifacts:
    - path: "source/VIEW/DIALOGS/modifyRepresentationDialog.C"
      provides: "Self-contained material controls (no MaterialSettings child widget)"
      contains: "applyMaterial"
    - path: "source/VIEW/DIALOGS/sources.cmake"
      provides: "Dialog build registration with clipping/label/material .C+.ui entries removed"
      contains: "SOURCES_LIST"
  key_links:
    - from: "source/VIEW/DIALOGS/modifyRepresentationDialog.C"
      to: "Scene material setter (updateMaterialForRepresentation)"
      via: "applyMaterial_ replacing MaterialSettings::apply()"
      pattern: "[Mm]aterial"
    - from: "source/VIEW/WIDGETS/geometricControl.C"
      to: "clipping plane selection (no dialog)"
      via: "selectClipRepresentations rewired or removed"
      pattern: "selectClipRepresentations"
---

<objective>
Detach and delete the three legacy dialogs that are STILL LIVE-WIRED on the current v1.8 base — `clippingDialog`, `labelDialog`, `materialSettings` — but whose consumers can be cleanly migrated. Ground truth (verified):
- `clippingDialog`: `ClippingDialog dialog;` is a live local in `GeometricControl::selectClipRepresentations()` (geometricControl.C:688), invoked from the clipping-plane context-menu action "Select Representations" (geometricControl.C:81). Live `#include` at line 21.
- `labelDialog`: `new LabelDialog(this, ...)` at BALLView mainframe.C:251, live `#include` at line 54. The dialog self-registers the legacy Display › Create › Label menu, now superseded by the Inspector LabelSection.
- `materialSettings`: `new MaterialSettings(...)` is an embedded child widget + member of `ModifyRepresentationDialog` (modifyRepresentationDialog.C:61, .h:135, `#include` .h:37). modifyRepresentationDialog is NOT in the deletion set, so it must be migrated off MaterialSettings first.

The reference branch `v1.7.3-legacy-deletion` did exactly these detachments in its plan 999.53-01 (commits 09c15ad3c8 label, 8fcea98458 material) before deleting in 999.53-02. We REPLICATE that recipe on the current base — we do NOT cherry-pick those commits (the surrounding code drifted via the v1.7.4 controller cut-over). Use those diffs as reference for the shape of the change only.

WAVE NOTE: this plan runs in wave 2, AFTER plan 999.67-01. Both plans edit the same `UI_LIST` / `SOURCES_LIST` blocks of `source/VIEW/DIALOGS/sources.cmake` (01 drops the lightSettings/stageSettings/stereoSettings entries; this plan drops the clipping/label/material entries), so the two MUST serialize — they are NOT parallel. Start this plan from the post-01 tree (01's three triples already removed from sources.cmake) and edit the remaining entries.

Purpose: Remove every live consumer of these three dialogs, then delete the triples, keeping the tree compiling at each commit boundary.
Output: clipping/label/material detached from their consumers and deleted; build + contract gate green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference diffs (shape-only; DO NOT cherry-pick):
#   git show 09c15ad3c8  — how LabelDialog was dropped from mainframe (4 lines)
#   git show 8fcea98458  — how modifyRepresentationDialog was migrated off MaterialSettings
#                          (built its own ambient/specularity/reflectiveness/shininess/transparency
#                           controls into the material_setting tab; applyMaterial_() reproduces
#                           MaterialSettings::apply() per-representation Stage::Material setter via
#                           Scene::updateMaterialForRepresentation; updateMaterialControls_() reproduces seeding)
#   git show be3ff56b13  — how clipping+label triples were git rm'd + sources.cmake scrubbed
#
# Inspector supersession sections already exist (include/BALL/VIEW/WIDGETS/inspector/sections/):
#   materialSection.h, labelSection.h, clippingSection.h — these are the v1.7.4 replacements.
#
# Project builds with -DQT_NO_KEYWORDS (use Q_EMIT/Q_SIGNALS/Q_SLOTS, never emit/signals/slots).
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate modifyRepresentationDialog off MaterialSettings, drop LabelDialog + ClippingDialog consumers</name>
  <files>source/VIEW/DIALOGS/modifyRepresentationDialog.C, include/BALL/VIEW/DIALOGS/modifyRepresentationDialog.h, source/APPLICATIONS/BALLVIEW/mainframe.C, source/VIEW/WIDGETS/geometricControl.C, include/BALL/VIEW/WIDGETS/geometricControl.h, source/PYTHON/EXTENSIONS/VIEW/geometricControl.sip</files>
  <action>Three independent consumer detachments, committed together as the "clear live refs" step (mirrors reference commits 09c15ad3c8 + 8fcea98458):

(a) modifyRepresentationDialog off MaterialSettings: replace the embedded `MaterialSettings* material_settings_` child widget with the dialog's own material controls built in code into the material_setting tab — ambient, specularity, reflectiveness (color + factor rows), shininess + transparency sliders. Add `applyMaterial_()` that reproduces `MaterialSettings::apply()`'s per-representation Stage::Material setter sequence via `Scene::updateMaterialForRepresentation` (the same Scene entry the v1.7.4 cut-over uses). Add `updateMaterialControls_()` reproducing the old `setCurrentRepresentation()` seeding. Read modifyRepresentationDialog.C:61 CAREFULLY before editing: the line is `material_settings_ = new MaterialSettings(material_settings_, ...)` — the first arg is the member itself (self-referencing parent arg), so after removing the member you must remove this whole construction, not just the RHS, and leave no dangling `material_settings_` reference anywhere. Remove `#include <BALL/VIEW/DIALOGS/materialSettings.h>` (.h:37), the `MaterialSettings* material_settings_` member (.h:135), and all three call sites (notably the `new MaterialSettings(...)` at .C:61 keyed on the "MaterialSettingsForRepresentation" object name). Follow the reference diff 8fcea98458 for the exact control layout and setter order.

(b) LabelDialog out of mainframe: drop the `new LabelDialog(this, ...)` construction at mainframe.C:251 and remove `#include <BALL/VIEW/DIALOGS/labelDialog.h>` (line 54). The legacy Display › Create › Label menu it self-registered is superseded by the Inspector LabelSection. Mirror reference commit 09c15ad3c8.

(c) ClippingDialog out of GeometricControl: in `GeometricControl::selectClipRepresentations()` (geometricControl.C:684-691) remove the `ClippingDialog dialog; dialog.setClippingPlane(...); dialog.exec();` body. If the Inspector clippingSection (or the existing clipping context-menu Hide/Show/Flip/Set-axis actions) already covers per-plane representation selection, remove the now-dead "Select Representations" context-menu action (geometricControl.C:81) AND the `selectClipRepresentations` method (decl geometricControl.h:172, sip geometricControl.sip:29). If selectClipRepresentations exposes behavior NOT yet covered elsewhere, STOP and surface as a blocker in the SUMMARY rather than silently dropping user-facing function — do not invent a replacement UI. Remove `#include <BALL/VIEW/DIALOGS/clippingDialog.h>` (line 21).

Commit as one atomic step: `refactor(999.67-02): detach clipping/label/material legacy dialogs from live consumers` + Co-Authored-By trailer.</action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && cmake --preset ci-macos -DBALL_LICENSE=GPL && cmake --build --preset ci-macos --target BALL VIEW BALLView && ! grep -q "DIALOGS/materialSettings.h" source/VIEW/DIALOGS/modifyRepresentationDialog.C include/BALL/VIEW/DIALOGS/modifyRepresentationDialog.h && ! grep -q "DIALOGS/labelDialog.h" source/APPLICATIONS/BALLVIEW/mainframe.C && ! grep -q "DIALOGS/clippingDialog.h" source/VIEW/WIDGETS/geometricControl.C && ! grep -q "material_settings_" source/VIEW/DIALOGS/modifyRepresentationDialog.C include/BALL/VIEW/DIALOGS/modifyRepresentationDialog.h && echo DETACHED</automated>
  </verify>
  <done>No file outside the three dialogs' own files #includes clippingDialog.h/labelDialog.h/materialSettings.h; no dangling `material_settings_` member or reference remains; modifyRepresentationDialog drives material via its own controls + Scene::updateMaterialForRepresentation; mainframe no longer constructs LabelDialog; geometricControl no longer instantiates ClippingDialog; libVIEW + BALLView build clean.</done>
</task>

<task type="auto">
  <name>Task 2: git rm the clipping/label/material triples and scrub build registration</name>
  <files>source/VIEW/DIALOGS/clippingDialog.C, source/VIEW/DIALOGS/clippingDialog.ui, include/BALL/VIEW/DIALOGS/clippingDialog.h, source/VIEW/DIALOGS/labelDialog.C, source/VIEW/DIALOGS/labelDialog.ui, include/BALL/VIEW/DIALOGS/labelDialog.h, source/VIEW/DIALOGS/materialSettings.C, source/VIEW/DIALOGS/materialSettings.ui, include/BALL/VIEW/DIALOGS/materialSettings.h, source/VIEW/DIALOGS/sources.cmake</files>
  <action>Now that Task 1 cleared every live consumer, grep-proof one more time that the only remaining references to ClippingDialog/LabelDialog/MaterialSettings are provenance comments (in clippingController/labelController/materialController + scene.C breadcrumbs), then git rm the .C + .ui + .h for each (9 files). From sources.cmake (already scrubbed of 01's three triples in wave 1): drop clippingDialog.ui, labelDialog.ui, materialSettings.ui from UI_LIST and clippingDialog.C, labelDialog.C, materialSettings.C from SOURCES_LIST. Confirm NO .sip files exist for these three (clippingPlane.sip and label.sip are DIFFERENT files — clippingPlane.sip belongs to the live ClippingPlane geometry class, label.sip/labelModel.sip belong to label *models*, NOT the LabelDialog — do NOT touch them; verify by reading their %Include'd headers). Scrub orphaned breadcrumb line-ranges in the controllers as in plan 01, preserving rationale. CAUTION: do NOT delete setClippingPlane.{C,h,ui} — that is a separate, live dialog NOT in scope. Commit: `refactor(999.67-02): delete clipping/label/material legacy dialog triples` + Co-Authored-By trailer.</action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && ! ls source/VIEW/DIALOGS/clippingDialog.* source/VIEW/DIALOGS/labelDialog.* source/VIEW/DIALOGS/materialSettings.* include/BALL/VIEW/DIALOGS/clippingDialog.h include/BALL/VIEW/DIALOGS/labelDialog.h include/BALL/VIEW/DIALOGS/materialSettings.h 2>/dev/null && ls source/VIEW/DIALOGS/setClippingPlane.C >/dev/null && ! grep -E "clippingDialog|labelDialog|materialSettings" source/VIEW/DIALOGS/sources.cmake && echo CLEAN</automated>
  </verify>
  <done>All 9 files removed; setClippingPlane untouched; clippingPlane.sip/label.sip/labelModel.sip untouched; sources.cmake free of the three; atomic commit created.</done>
</task>

<task type="auto">
  <name>Task 3: Full build + contract gate after detachment + deletion</name>
  <files>(build only)</files>
  <action>Capture the contract-test baseline count FIRST (this plan must not change the contract surface): `ctest --test-dir build/ci-macos -L contract -N | tail -1` — record this number in the SUMMARY as the baseline. Reconfigure if needed and build `BALL VIEW BALLView contract_tests build_tests` with `cmake --preset ci-macos -DBALL_LICENSE=GPL`. Run `ctest -L contract`. ALL `-L contract` tests must pass AND the test count must be UNCHANGED from the captured baseline — the command-shaped controllers (clipping/label/material) are the supersession surface and are NOT modified by this plan, so a contract regression (a failure OR a changed test count) signals an accidental coupling and is a blocker. Do not assume any fixed number; gate strictly on the baseline you captured. The Linux contract gate + tri-OS build is the authoritative gate and runs in CI on push.</action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && cmake --preset ci-macos -DBALL_LICENSE=GPL && cmake --build --preset ci-macos --target BALL VIEW BALLView contract_tests build_tests && BASE=$(ctest --test-dir build/ci-macos -L contract -N | tail -1) && echo "contract baseline: $BASE" && ctest --test-dir build/ci-macos -L contract && AFTER=$(ctest --test-dir build/ci-macos -L contract -N | tail -1) && [ "$BASE" = "$AFTER" ] && echo "CONTRACT_COUNT_UNCHANGED"</automated>
  </verify>
  <done>Build clean post-deletion; contract suite green AND the contract-test count is unchanged from the baseline captured at the start of the task; ready for plan 03 (lynchpin) and the menu retirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dialog → Scene material pipeline | modifyRepresentationDialog must reproduce MaterialSettings' Stage::Material setter sequence exactly, or per-representation material edits silently change behavior |
| context-menu action → clipping behavior | dropping selectClipRepresentations must not remove user-facing function with no replacement |
| sources.cmake edit ordering (wave 1 → wave 2) | 01 and 02 edit the same UI_LIST/SOURCES_LIST blocks; serialized via depends_on so the edits do not collide |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99767-03 | Tampering | modifyRepresentationDialog material migration | mitigate | applyMaterial_() routes through Scene::updateMaterialForRepresentation (the v1.7.4 cut-over entry); Task 3 contract gate + plan 03 UAT verify render parity |
| T-99767-04 | Denial of Service | dropping selectClipRepresentations with no replacement | mitigate | Task 1 requires confirming Inspector clippingSection / existing context-menu covers the behavior; uncovered behavior = surfaced blocker, not silent removal |
| T-99767-05 | Tampering | deleting setClippingPlane / clippingPlane.sip / label.sip by name collision | mitigate | Task 2 explicit CAUTION: these are distinct live files; verify by reading their headers before any rm |
</threat_model>

<verification>
- No file outside the dialogs' own files references ClippingDialog/LabelDialog/MaterialSettings as live code.
- clipping/label/material triples deleted; setClippingPlane + clippingPlane.sip + label.sip preserved.
- sources.cmake scrubbed; `BALL VIEW BALLView contract_tests build_tests` build clean.
- `ctest -L contract` green AND contract-test count unchanged from the baseline captured at the start of Task 3.
</verification>

<success_criteria>
- modifyRepresentationDialog self-contains material controls (no dangling `material_settings_`); mainframe + geometricControl free of LabelDialog/ClippingDialog.
- clipping/label/material triples deleted (.h/.C/.ui each).
- Two atomic commits (detach, then delete) each leaving the tree compiling, with Co-Authored-By trailers.
- Build green; contract suite green with an unchanged test count vs. baseline; CI tri-OS expected green.
</success_criteria>

<output>
Create `.planning/phases/999.67-legacy-dialog-deletion/999.67-02-SUMMARY.md` when done. Record: the captured contract-test baseline count, the material-control migration approach, whether selectClipRepresentations was removed or rewired (and why), and confirmation that setClippingPlane/clippingPlane.sip/label.sip were preserved.
</output>
</content>
</invoke>
