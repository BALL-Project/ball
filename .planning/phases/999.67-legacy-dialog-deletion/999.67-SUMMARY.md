---
phase: 999.67-legacy-dialog-deletion
status: complete
verification: passed (render-parity UAT confirmed by maintainer 2026-05-30) + macOS build/contract green; tri-OS via CI on push
reqs: LEGACYDEL-01, LEGACYDEL-02, LEGACYDEL-03
---

# 999.67 — Legacy-dialog deletion (finish the tail) — COMPLETE

Fresh deletion on the v1.8 base (NOT a merge of the stale v1.7.3-legacy-deletion branch). 3 serialized waves.

- **W1** (634a748d17): deleted the 3 dormant triples (lightSettings/stageSettings/stereoSettings) — grep-proof 0 live refs; ~4601 deletions.
- **W2** (011525f626 + d795b27299): detached + deleted material/label/clipping — MaterialSettings rebuilt into modifyRepresentationDialog via Scene::updateMaterialForRepresentation; LabelDialog dropped; ClippingDialog's selectClipRepresentations REWIRED (user function preserved, not lost); ~2079 deletions.
- **W3** (ab000c177b + d9f78b7667 + pluginDialog fix): re-homed the DisplayProperties CompositeMessage::NEW_MOLECULE auto-create onto a NEW non-dialog `representationCreator` (default RepresentationSpec + the existing Notifier_ adapter / RepresentationBuilder, 999.65); re-registered the DCD2PNG subscriber; deleted the displayProperties/model/coloring lynchpin triple + legacySettingsHelper + displayProperties.sip; retired the Tools→Legacy Settings menu; ~5866 deletions. One missed stale include (pluginDialog.C → deleted displayProperties.h, unused) fixed post-merge.

## Verification (LEGACYDEL-03)
- macOS clean build: BALL VIEW BALLView contract_tests build_tests → clean (0 errors).
- Contract gate: ctest -L contract → 16/16, unchanged from baseline.
- All-9 grep gate: only doc/comment mentions remain (DisplayProperties in models.doc/standardColorProcessor.h; StageSettings in stageController.h comment) — 0 LIVE code refs. PASS.
- **Render-parity UAT (maintainer, 2026-05-30): PASS** — loaded 1ubq → auto-rendered a default representation (proves the NEW_MOLECULE re-home); Inspector Model/Coloring(+histogram)/Material drive the scene; Tools→Legacy Settings menu gone.
- tri-OS (Linux x64/arm64 + Windows) validated via CI on push.

## Out of scope (surfaced during UAT → filed as THEME-DARK-01, v1.8)
Dark-mode dropdown readability: macOS native QComboBox popup ignores QSS → black-on-black in OS dark appearance. NOT a 999.67 regression (deletion touched no theming). Mapped to the v1.8 cosmetic phase (999.71).

Total: ~12,500 lines deleted across the 9 dialog triples + LegacySettingsHelper.
