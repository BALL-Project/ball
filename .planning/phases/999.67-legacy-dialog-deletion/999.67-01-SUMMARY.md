---
phase: 999.67-legacy-dialog-deletion
plan: 01
subsystem: VIEW/DIALOGS
tags: [legacy-deletion, dialog-cleanup, build-system, sip]
requires: []
provides:
  - "Smaller VIEW/DIALOGS surface (3 dormant triples removed) for waves 2/3"
affects:
  - source/VIEW/DIALOGS/sources.cmake
  - source/PYTHON/EXTENSIONS/VIEW/VIEW.sip
tech-stack:
  added: []
  patterns:
    - "Grep-proof gate (COMMENT vs LIVE classification) before any legacy deletion"
key-files:
  created: []
  modified:
    - source/VIEW/DIALOGS/sources.cmake
    - source/PYTHON/EXTENSIONS/VIEW/VIEW.sip
  deleted:
    - source/VIEW/DIALOGS/lightSettings.C
    - source/VIEW/DIALOGS/lightSettings.ui
    - include/BALL/VIEW/DIALOGS/lightSettings.h
    - source/VIEW/DIALOGS/stageSettings.C
    - source/VIEW/DIALOGS/stageSettings.ui
    - include/BALL/VIEW/DIALOGS/stageSettings.h
    - source/VIEW/DIALOGS/stereoSettingsDialog.C
    - source/VIEW/DIALOGS/stereoSettingsDialog.ui
    - include/BALL/VIEW/DIALOGS/stereoSettingsDialog.h
    - source/PYTHON/EXTENSIONS/VIEW/lightSettings.sip
decisions:
  - "Preserved provenance breadcrumb comments in lightController/stageController/stereoController + scene.C/demoTutorialDialog.C — rationale prose retained (matches reference-branch 'scrub dead breadcrumbs, rationale preserved' approach); no comment cited a misleading deleted line-range that warranted scrubbing."
metrics:
  duration: ~5 min
  completed: 2026-05-30
---

# Phase 999.67 Plan 01: Legacy-dialog deletion — Wave 1 (dormant triples) Summary

Deleted the three genuinely-dormant VIEW settings-dialog triples (`lightSettings`, `stageSettings`, `stereoSettingsDialog`) — their `.h`/`.C`/`.ui` files, their `sources.cmake` registration, and `lightSettings.sip` plus its `%Include` in `VIEW.sip` — after a grep-proof gate confirmed zero live references; tree builds clean and the 16-test contract suite stays green with an unchanged count.

## What Was Done

| Task | Outcome | Commit |
|------|---------|--------|
| 1. Grep-proof three triples reference-free | PASS — zero LIVE refs; all hits are provenance comments / doc-prose | (read-only) |
| 2. git rm triples + drop build/SIP registration | 10 files deleted, 2 files edited, atomic commit | `634a748d17` |
| 3. Configure + build + contract gate | Build clean; 16/16 contract tests pass; count unchanged | (build-only) |

## Grep-Proof Classification (Task 1)

Tree-wide grep over `source/` + `include/` for header include path, class name, `getInstance`, `new <Class>`, and `Show<Class>` message wiring — excluding each dialog's own `.h/.C/.ui` and own `.sip`.

| Dialog | Class | header-include hits (non-own) | `new <Class>` | `getInstance` | `Show<Class>` msg | Class-name hits | Verdict |
|--------|-------|-------------------------------|---------------|---------------|-------------------|-----------------|---------|
| lightSettings | `LightSettings` | 0 | 0 | 0 | 0 | ~20, **all `//` comments** (scene.C, lightController.{C,h}, stage.h Doxygen, demoTutorialDialog.C) | **COMMENT-only — SAFE** |
| stageSettings | `StageSettings` | 0 | 0 | 0 | 0 | ~19, **all `//` comments** (scene.{C,h}, stereoController.C, stageController.{C,h}, stageSection.h, PATTERN.md doc) | **COMMENT-only — SAFE** |
| stereoSettingsDialog | `StereoSettingsDialog` | 0 | 0 | 0 | 0 | 2, **both `//` comments** (stereoController.C) | **COMMENT-only — SAFE** |

**ZERO LIVE references found.** Every surviving reference is a provenance breadcrumb (`//` comment) or design-doc prose (`stage.h:406` is inside a `/** ... */` Doxygen block; `PATTERN.md` is a markdown design note). No `#include` in another TU, no instantiation, no member, no message wiring. → No blocker; deletion proceeded.

**Trap files confirmed present and untouched** (as the plan flagged):
- `setClippingPlane.C` / `setClippingPlane.ui` — separate live dialog, NOT deleted.
- `clippingPlane.sip` / `label.sip` / `labelModel.sip` — live bindings, NOT deleted.
- `*MuteableByDisplayProperties` methods (substring collision in representationBuilder.C, displayProperties.C, molecularControl.C, etc.) — NOT touched.

## Deletion + Registration (Task 2)

- **9 triple files** `git rm`'d (3 × `.C`/`.ui`/`.h`).
- **`source/VIEW/DIALOGS/sources.cmake`**: dropped `lightSettings.ui`, `stageSettings.ui`, `stereoSettingsDialog.ui` from `UI_LIST`; dropped `lightSettings.C`, `stageSettings.C`, `stereoSettingsDialog.C` from `SOURCES_LIST`.
- **`source/PYTHON/EXTENSIONS/VIEW/lightSettings.sip`** `git rm`'d; its `%Include lightSettings.sip` line removed from `VIEW.sip`. Confirmed `stageSettings`/`stereoSettingsDialog` had no `.sip` files (so none to remove).
- Single atomic commit `634a748d17`: 12 files changed, 4601 deletions. Post-commit deletion check: all 10 deletions intentional and expected per plan.

## Verification (Task 3)

- **Configure:** `cmake --preset ci-macos -DBALL_LICENSE=GPL` → "successfully configured BALL", Configuring/Generating done, no errors. Project uses `-DQT_NO_KEYWORDS` (no source edits needed).
- **Build:** `cmake --build build/ci-macos --target BALL VIEW BALLView contract_tests build_tests -j16` → **1991/1991 targets, exit 0, zero `error:`/`FAILED` lines.** libVIEW + BALLView link clean with the three triples removed → proves no dangling reference (mitigates T-99767-01).
- **Contract baseline (captured at start of Task 3):** `ctest --test-dir build/ci-macos -L contract -N | tail -1` → **`Total Tests: 16`**.
- **Contract run:** `ctest --test-dir build/ci-macos -L contract` → **100% tests passed, 0 failed out of 16.** Includes the controllers that replaced these dormant dialogs — `light_contract_test`, `stage_contract_test`, `stereo_contract_test` all PASS.
- **Count after:** `Total Tests: 16` → **UNCHANGED from baseline** (no accidental coupling; mitigates T-99767-02).
- Working tree clean post-build (build/ gitignored, no stray untracked files).

The authoritative tri-OS gate runs in CI on push; this local macOS build is the fast pre-push proof.

## Deviations from Plan

None — plan executed exactly as written. No Rule 1–4 deviations, no auth gates, no checkpoints.

## Known Stubs

None. This is a pure deletion plan; no placeholders or stubs introduced.

## Threat Flags

None. No new network endpoints, auth paths, file-access patterns, or schema changes — only removal of dormant build/source/SIP surface.

## Scope Boundary

Wave 1 only. Did NOT touch material/label/clipping (Wave 2) or displayProperties/model/coloring/the Tools menu (Wave 3). Did NOT edit STATE.md or ROADMAP.md (per orchestrator instruction). Did NOT merge or push — orchestrator merges back into v1.8.

## Self-Check: PASSED

- Deleted files confirmed absent (glob "no matches" + `git diff --diff-filter=D` lists all 10).
- `sources.cmake` / `VIEW.sip` carry no references to the three dialogs (grep returns empty).
- Commit `634a748d17` present in `git log`.
- Build + contract gate green, count unchanged.
