---
phase: 999.48
plan: 01
type: standard
autonomous: true
wave: 4
depends_on: [999.40, 999.41, 999.42, 999.43, 999.44, 999.45, 999.46, 999.47]
subsystem: BALLView Refresh — accessibility + flag retirement + legacy file deletion
tags: [a11y, ui_v2, flag-removal, legacy-cleanup, last-ui-phase]
requirements: []
---

# Phase 999.48 Plan 01: A11y + BALL_UI_V2 default-FLIP + flag-REMOVAL + 9-file legacy DELETION

## Objective

Close out the v1.7 BALLView Refresh: ship the a11y pass (Handover §8.1-§8.5), flip
`BALL_UI_V2` default ON (Handover §8.7 step a), REMOVE the flag entirely (Handover §8.7
step b), DELETE the 9 named legacy `.{C,h,ui}` files where dependencies permit
(Handover §8.8), and drop the now-vestigial `ui_v2: [ON, OFF]` CI matrix axis.

This is the LAST UI phase before 999.49 (Classic-dock delete).

## Context

@/Users/kohlbach/Claude/BALL/Claude Design Handover/revitalization/08-phase-a11y.md
@/Users/kohlbach/Claude/BALL/ball/.planning/ROADMAP.md

Audit pull-in (2026-05-17) ROADMAP §999.48 expanded a/b/c sequencing. The
operating contract is mandatory: a11y → flip → removal → deletion.

## Operating contract

- Up to step (a) ALL new code stays behind `BALL_UI_V2`
- Step (a) flips the DEFAULT in CMakeLists.txt
- Step (b) REMOVES the flag (delete every `#ifdef BALL_UI_V2` ... `#endif` pair)
- Step (c) DELETES the 9 legacy files where dependencies permit; defer files with
  blocking dependencies with explicit breadcrumb
- Incremental commits — never bundle a11y + flip + removal + deletion
- Watch CI per commit batch (background `gh run watch` polling)
- macOS-arm64 smoke build per commit

## 4 named widgets for QAccessible roles (confirmed against tree)

- `source/VIEW/WIDGETS/swatchButton.{C}` + `include/BALL/VIEW/WIDGETS/swatchButton.h` → `QAccessible::Button`, name "Color: <hex>"
- `source/VIEW/WIDGETS/labeledSlider.{C}` + `include/BALL/VIEW/WIDGETS/labeledSlider.h` → `QAccessible::Slider`, name from label
- `source/VIEW/WIDGETS/sectionHeader.{C}` + `include/BALL/VIEW/WIDGETS/sectionHeader.h` → `QAccessible::Heading`, level 2
- `source/VIEW/WIDGETS/inspector/inspectorSection.{C}` + `include/BALL/VIEW/WIDGETS/inspector/inspectorSection.h` → `QAccessible::Section`, name from title

## 9 legacy files for deletion (Handover §8.8, confirmed against tree)

| # | File triple                                                |
|---|------------------------------------------------------------|
| 1 | `displayProperties.{C,h,ui}` + `displayProperties.sip`     |
| 2 | `modelSettingsDialog.{C,h,ui}`                             |
| 3 | `materialSettings.{C,h,ui}`                                |
| 4 | `lightSettings.{C,h,ui}` + `lightSettings.sip`             |
| 5 | `stageSettings.{C,h,ui}`                                   |
| 6 | `stereoSettingsDialog.{C,h,ui}`                            |
| 7 | `clippingDialog.{C,h,ui}`                                  |
| 8 | `coloringSettingsDialog.{C,h,ui}`                          |
| 9 | `labelDialog.{C,h,ui}`                                     |

**Known blocking dependencies** (will be evaluated per file during step c):
- `scene.C` includes lightSettings.h / stageSettings.h / materialSettings.h
- `geometricControl.C` includes displayProperties.h / clippingDialog.h
- `representationManager.C` + `mainControl.C` include displayProperties.h
- `pluginDialog.C` includes displayProperties.h
- `mainframe.C` includes labelDialog.h + displayProperties.h
- `demoTutorialDialog.C` (Phase 999.47 retired it but file still on disk?)

Per scope-trim guidance: any file with non-trivial dependencies that can't be cut
cleanly defers to v1.7 RC patch with breadcrumb in SUMMARY.

## Tasks (commit-aligned)

### A11y pass (commits 1-5)

**Task 1** — `type=auto` — Add `QAccessible` roles to the 4 named custom widgets.
Files: `swatchButton.{h,C}`, `labeledSlider.{h,C}`, `sectionHeader.{h,C}`,
`inspector/inspectorSection.{h,C}`. Commit: `feat(999.48/01): QAccessible roles on
4 custom widgets`

**Task 2** — `type=auto` — Tab-order audit + `source/VIEW/test/tabOrder_test.C`.
Scope-trim: cover the Inspector + main window only; deep per-dialog manifest
deferred to v1.7 RC patch. Commit: `feat(999.48/02): tab-order audit + smoke
tabOrder_test.C`

**Task 3** — `type=auto` — Text-size preference (Normal/Large/Extra Large).
Add radio group to `preferencesAppearance` (or simplest existing prefs section
under BALL_UI_V2); persist `[Appearance] textScale=` in `~/.BALLView`; apply
via `qApp->setFont()`. Commit: `feat(999.48/03): text-size preference
(Normal/Large/Extra Large)`

**Task 4** — `type=auto` — `revitalization/scripts/contrast-check.py`. Minimal
implementation: parse `data/BALLView/theme/theme-neutral.qss` token pairs,
WCAG 2.1 AA check (4.5:1 body, 3:1 large/UI). Commit: `feat(999.48/04):
contrast-check.py WCAG 2.1 AA validator`

**Task 5** — `type=auto` — Tooltip-lint CI script. Minimal grep gate over
`addAction(...)` calls missing `setToolTip`. Wire into `.github/workflows/ci.yml`
as a fail-fast lint job. Commit: `feat(999.48/05): tooltip-lint CI gate`

### Step (a) Default FLIP (commit 6)

**Task 6** — `type=auto` — Flip `OPTION(BALL_UI_V2 ... OFF)` → `ON` in
CMakeLists.txt:289. CI matrix still tests both cells (OFF cell verifies user
override path). Commit: `feat(999.48/06): flip BALL_UI_V2 default ON
(Handover §8.7 step a)`

### Step (b) Flag REMOVAL (commits 7-9)

**Task 7** — `type=auto` — Delete all `#ifdef BALL_UI_V2 ... #endif` pairs
across source files. Most agents structured as whole-TU gating; bare cases
need attention. Use `grep -rl BALL_UI_V2 source/ include/` to enumerate (90
files). Land in batches by subdirectory if needed for incremental commit
cadence. Commit(s): `refactor(999.48/07): remove BALL_UI_V2 #ifdef pairs
(Handover §8.7 step b)`

**Task 8** — `type=auto` — Remove `OPTION(BALL_UI_V2 ...)` + the
`TARGET_COMPILE_DEFINITIONS` block in CMakeLists.txt. Remove the
`TARGET_COMPILE_DEFINITIONS` plumbing. Commit: `chore(999.48/08): remove
BALL_UI_V2 CMake option block`

**Task 9** — `type=auto` — Drop `ui_v2` axis from `.github/workflows/ci.yml`
(remove `ui_v2_matrix` output, `_ui_v2_paths_match` block, doubling loop,
`-DBALL_UI_V2=<matrix.ui_v2>` from Configure steps). Commit:
`chore(999.48/09): drop ui_v2 matrix axis from ci.yml`

### Step (c) Legacy file DELETION (commits 10-12, per file batch)

**Task 10** — `type=auto` — Deletion BATCH 1: standalone dialogs that have no
hard dependencies (likely: `modelSettingsDialog`, `coloringSettingsDialog`,
`stereoSettingsDialog`, `clippingDialog` per Inspector replacement). Per file:
delete `.C`/`.h`/`.ui` + remove sources.cmake entries + delete Python .sip if
present. Verify build. Commit: `chore(999.48/10): delete legacy dialog files
(batch 1)`

**Task 11** — `type=auto` — Deletion BATCH 2: dialogs with surviving
include-only callers that can be cut by removing the includes + replacing the
construction sites with controller calls (likely: `materialSettings`,
`lightSettings`, `stageSettings`, `labelDialog`). Some may need to defer
to v1.7 RC patch with breadcrumb if their use is too deeply wired. Commit:
`chore(999.48/11): delete legacy dialog files (batch 2)`

**Task 12** — `type=auto` — Remove `Tools › Legacy Settings` submenu from
`mainframe.C` (now empty after batches 1+2). Commit: `chore(999.48/12):
retire Tools › Legacy Settings submenu`

### Finalization (commits 13-14)

**Task 13** — `type=auto` — SUMMARY.md, STATE.md, ROADMAP.md flip to COMPLETE.
Commit: `docs(999.48/13): SUMMARY + STATE + ROADMAP — Phase 999.48 COMPLETE`

**Task 14** — `type=auto` — Final push.

## Verification

- macOS-arm64 build green after every commit batch
- Tri-OS CI dispatched after Task 9 (flag-removed config) — must be 8/8 green
- `grep -r "BALL_UI_V2" source/ include/ CMakeLists.txt .github/` → 0 matches
  (test fixtures excepted)
- `grep -r "ModelSettingsDialog" source/` → only Inspector section's symbol
  references (not the deleted view layer)

## Scope-trims (forwarded to v1.7 RC patch)

- Deep per-dialog tab-order manifest (Task 2 minimal version lands)
- Full WCAG report formatter beyond pass/fail (Task 4 minimal version lands)
- Tooltip-lint AST-based analysis (Task 5 minimal grep gate lands)
- Any of the 9 files whose deletion blocks compilation (Tasks 10-11) defer
  with breadcrumb

## Success criteria

- All a11y deliverables landed
- `BALL_UI_V2` flipped ON, then REMOVED entirely
- ci.yml ui_v2 axis dropped
- 5+ of 9 legacy files deleted (with breadcrumbs for blocked ones)
- 999.49 (Classic-delete) dispatch unblocked
