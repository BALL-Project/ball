---
phase: 999.58-scene-settings-stereo-cutover
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - source/VIEW/WIDGETS/scene.C
  - include/BALL/VIEW/WIDGETS/scene.h
  - source/APPLICATIONS/BALLVIEW/demoTutorialDialog.C
autonomous: false
requirements: [VIEW-CLEAN-03]
must_haves:
  truths:
    - "scene.C no longer constructs LightSettings, MaterialSettings, or StageSettings (lines 142/143/162 removed)"
    - "The Preferences dialog stack registration (insertEntry/removeEntry of the 3 dialogs) no longer references the dialog members"
    - "The (deferred) stereo render paths read their screen/geometry/renderer-type/stereo-mode config from a Scene-owned stereo-screen config instead of stage_settings_->get*, so they still compile for the Phase 5 re-enablement work"
    - "applyStereoDefaults() computes defaults without stage_settings_->computeDefaultPressed()"
    - "demoTutorialDialog no longer reaches scene's getStageSettings()/getMaterialSettings()/LightSettings::getInstance()"
    - "Lights, materials, and stereo-enter/exit all still work end-to-end through the Inspector after the dialogs are no longer constructed by scene.C"
  artifacts:
    - path: "source/VIEW/WIDGETS/scene.C"
      provides: "Dialog-free Scene: constructor without the 3 dialog constructions, Preferences stack registration off the dialog members, stereo paths reading a Scene-owned stereo config, applyStereoDefaults computing in-Scene"
    - path: "include/BALL/VIEW/WIDGETS/scene.h"
      provides: "Scene-owned stereo-screen config (or controller delegation) replacing the StageSettings stereo accessors; dialog members/getters removed or repointed"
  key_links:
    - from: "source/VIEW/WIDGETS/scene.C enterStereo/enterDualStereo/enterDualStereoDifferentDisplays"
      to: "Scene-owned stereo-screen config"
      via: "config getters replacing stage_settings_->getLeftEyeScreenNumber()/getStereoMode()/etc."
      pattern: "stage_settings_->get"
    - from: "source/VIEW/WIDGETS/scene.C constructor + initializePreferencesTab"
      to: "no dialog construction / registration"
      via: "removal of new LightSettings/MaterialSettings/StageSettings + insertEntry of them"
      pattern: "new (Light|Material|Stage)Settings|insertEntry\\((light|material|stage)_settings_\\)"
---

<objective>
Complete the scene.C cut-over: remove the three legacy dialog constructions (`scene.C:142/143/162`), rewire the Preferences-dialog stack registration off the dialog members, relocate the stereo render-path config reads (the `stage_settings_->get*` accessors, used ONLY inside the deferred/guarded stereo bodies) into a Scene-owned stereo-screen config so they still compile for the Phase 5 stereo re-enablement, relocate `applyStereoDefaults()`, and repoint the `demoTutorialDialog` consumers. This is the FINAL plan: after it, `scene.C` constructs none of the three dialogs and the grep proof for VIEW-CLEAN-03 passes.

Plans 01 + 02 already moved the LIVE material/light/stage render config onto the Inspector controllers and off `applyPreferences`. This plan handles the remaining structural references: the constructions, the Preferences stack, the (dead-but-must-compile) stereo accessors, and the tutorial-dialog reach-throughs.

CRITICAL grounding (read carefully): all four stereo entry points — `enterStereo` (scene.C:2646), `enterActiveStereo` (2810), `enterDualStereo` (2842), `enterDualStereoDifferentDisplays` (2958) — are GUARD-AND-DEFERRED: each logs and `return`s BEFORE reading any `stage_settings_->get*`. Every `stage_settings_->get*` call (2664/2665/2684/2685/2687/2688/2701, 2860/2861, 2977/2978) sits in dead code below an unconditional return, kept intact for the Phase 5 re-enablement. There is therefore NO live stereo render behavior to preserve — but the code must still COMPILE, so the accessors must be replaced with a Scene-owned config source rather than deleted. Do NOT re-enable any stereo path.

Purpose: VIEW-CLEAN-03 final clause — `scene.C` no longer constructs the three dialogs; the grep proof passes; 999.53 can delete the dialog triples.
Output: a dialog-free Scene (for these three dialogs), a Scene-owned stereo-screen config, repointed tutorial consumers, and a passing grep gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/999.48-a11y-flag-flip-removal/deferred-items.md
@.planning/phases/999.57-inspector-controller-cutover-displayproperties/999.57-CONSUMER-AUDIT.md

<interfaces>
<!-- The stereo accessors are consumed ONLY in dead (returned-early) code.
     They must be relocated to a Scene-owned config so the Phase 5 bodies compile. -->

StageSettings stereo accessors (source/VIEW/DIALOGS/stageSettings.C) consumed by scene.C
ONLY inside the guard-and-deferred stereo bodies:
  - getLeftEyeScreenNumber() / getRightEyeScreenNumber() / getControlScreenNumber() (int, -1 = disabled)
  - getLeftEyeGeometry() / getRightEyeGeometry() (QRect)
  - getControlScreenRendererType() / getStereoScreensRendererType() (RenderSetup::RendererType)
  - getStereoMode() (Renderer::StereoMode: NO_STEREO/INTERLACED/DUAL_VIEW/TOP_BOTTOM/ACTIVE)
  - computeDefaultPressed() — called by Scene::applyStereoDefaults (scene.C:3163-3166); computes
    eye separation + focal distance from the scene geometry and writes Stage::setEyeDistance/
    setFocalDistance + scene_->setDownsamplingFactor (stageSettings.C:122-169).

scene.C live consumer sites of the dialog members (post Plans 01+02):
  - Constructor: scene.C:142 (new LightSettings), :143 (new MaterialSettings), :162 (new StageSettings).
  - initializePreferencesTab (scene.C:1102-1113): insertEntry(light_settings_/stage_settings_/material_settings_).
  - finalizePreferencesTab (scene.C:1115-1138): removeEntry + null-out the three members.
  - applyStereoDefaults (scene.C:3163-3166): stage_settings_->computeDefaultPressed().
  - Stereo bodies (DEAD, below returns): the stage_settings_->get* reads listed above.
  - scene.h:321-334 getStageSettings()/getMaterialSettings() accessors; scene.h:953-955 members.

demoTutorialDialog.C consumers (ALL inside #ifdef BALL_HAS_RTFACT, and RTfact is DISABLED this era —
see CLAUDE.md "Removed/disabled: RTfact"; so these are dead in normal builds but must still compile
or be removed for 999.53):
  - :110/171/366 Scene::getInstance(0)->getStageSettings()->updateFromStage()
  - :211 Scene::getInstance(0)->getMaterialSettings()->updateDefaultMaterialsFromStage()
  - :126/186 LightSettings::getInstance(0)->updateFromStage()

Stage already owns eye/focal distance + swap-side-by-side (the live stereo state the StereoController
mirrors — stereoController.C). The screen-assignment / geometry / stereo-mode / renderer-type fields
have NO live render consumer (only the deferred bodies) and NO Stage home — they are pure UI-config
that lived in the dialog. A small Scene-owned struct (e.g. StereoScreenConfig with the 9 fields above,
default-initialized to "disabled / NO_STEREO / OpenGL") is the relocation target.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add a Scene-owned stereo-screen config and repoint the deferred stereo bodies + applyStereoDefaults</name>
  <files>include/BALL/VIEW/WIDGETS/scene.h, source/VIEW/WIDGETS/scene.C</files>
  <read_first>
    - source/VIEW/WIDGETS/scene.C:2646-2701 (enterStereo head + the stage_settings_->get* reads below its return), :2842-2870 + :2960-2990 (the dual-stereo bodies and their reads), :3163-3166 (applyStereoDefaults → computeDefaultPressed). Confirm the unconditional `return` precedes every stage_settings_->get* read — this is the proof there is no live behavior.
    - source/VIEW/DIALOGS/stageSettings.C:122-169 (computeDefaultPressed — the geometry-based default computation to relocate), :489-604 (the getLeftEye/RightEye/Control screen-number + geometry + renderer-type + getStereoMode accessor bodies — the field semantics to replicate, -1 = disabled, OpenGL default, NO_STEREO default).
    - include/BALL/VIEW/WIDGETS/scene.h:640-650 (applyStereoDefaults decl) + the private members region (953-955) for where to add the config member.
    - source/VIEW/KERNEL/controllers/stereoController.C (whole file) — confirms eye/focal/swap are already Stage-owned + StereoController-mirrored, so the Scene-owned config must hold ONLY the screen-assignment/geometry/mode/renderer-type fields (no eye/focal duplication).
  </read_first>
  <action>Introduce a Scene-owned stereo-screen config holding the 9 fields the StageSettings stereo accessors returned: left/right/control screen index (int, default -1 = disabled), left/right eye geometry (QRect, default empty), control + stereo-screens renderer type (RenderSetup::RendererType, default OPENGL_RENDERER), and stereo mode (Renderer::StereoMode, default NO_STEREO). Define it as a small struct/member on Scene (in scene.h near the existing render members) default-initialized to the "disabled / NO_STEREO / OpenGL" state — these fields have no live render consumer (only the deferred bodies). In the deferred stereo bodies (enterStereo/enterDualStereo/enterDualStereoDifferentDisplays), replace every `stage_settings_->getLeftEyeScreenNumber()` / `getRightEyeScreenNumber()` / `getLeftEyeGeometry()` / `getRightEyeGeometry()` / `getControlScreenRendererType()` / `getStereoScreensRendererType()` / `getStereoMode()` read with the corresponding Scene-owned config field. Do NOT change the guard-and-return — the bodies stay dead; this is purely making them compile without stage_settings_. Relocate `applyStereoDefaults()` (scene.C:3163-3166): inline the geometry-based eye-separation/focal-distance computation from StageSettings::computeDefaultPressed() (stageSettings.C:122-169) directly into Scene::applyStereoDefaults — it already has access to stage_->getCamera() + the RepresentationManager + setDownsamplingFactor; reproduce the same math and the stage_->setEyeDistance/setFocalDistance writes. Drop the parts that only updated dialog widgets (the slider->setValue calls). After this task no `stage_settings_->` reference remains except the construction (162), the insertEntry/removeEntry (Task 2), and the getStageSettings accessor (Task 3).</action>
  <verify>
    <automated>grep -nc "stage_settings_->get\|stage_settings_->computeDefaultPressed()" source/VIEW/WIDGETS/scene.C</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "stage_settings_->get" source/VIEW/WIDGETS/scene.C` returns 0 (all stereo accessor reads relocated).
    - `grep -c "stage_settings_->computeDefaultPressed()" source/VIEW/WIDGETS/scene.C` returns 0.
    - A Scene-owned stereo-screen config member exists in scene.h holding the 9 relocated fields (grep for the new member/struct).
    - The stereo entry points still guard-and-return (the `return;` before the relocated reads is unchanged — grep that each `enter*Stereo` body still has its early `return` + deferral Log line).
    - applyStereoDefaults computes eye/focal via stage_->setEyeDistance/setFocalDistance in-Scene (grep for setEyeDistance/setFocalDistance inside the function).
  </acceptance_criteria>
  <done>The deferred stereo bodies + applyStereoDefaults read from a Scene-owned stereo config / compute in-Scene; no stage_settings_ accessor reads remain; the stereo paths still compile and stay deferred.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove the dialog constructions + rewire the Preferences stack registration off the dialog members</name>
  <files>source/VIEW/WIDGETS/scene.C, include/BALL/VIEW/WIDGETS/scene.h</files>
  <read_first>
    - source/VIEW/WIDGETS/scene.C:130-179 (constructor — the light_settings_(new LightSettings)/material_settings_(new MaterialSettings) init-list entries + the stage_settings_=new StageSettings line at 162).
    - source/VIEW/WIDGETS/scene.C:1102-1138 (initializePreferencesTab + finalizePreferencesTab — the insertEntry/removeEntry of the 3 dialog members + the edit_settings_ entry which STAYS).
    - include/BALL/VIEW/WIDGETS/scene.h:81-83 (forward decls), :321-334 (getStageSettings/getMaterialSettings accessors), :953-955 (the 3 members) + the EditSettings member (edit_settings_ stays).
    - Plan 01 + Plan 02 SUMMARYs (999.58-01-SUMMARY.md, 999.58-02-SUMMARY.md) — confirm applyPreferences no longer calls any of the 3 dialog apply()/updateFromStage()/getGLSettings() before removing the constructions.
  </read_first>
  <action>Remove the three dialog constructions: delete `light_settings_(new LightSettings(this))` and `material_settings_(new MaterialSettings(this))` from the constructor init list (scene.C:142/143) and the `stage_settings_=new StageSettings(this);` statement (scene.C:162). In initializePreferencesTab (scene.C:1102-1113), remove the three `preferences.insertEntry(light_settings_/stage_settings_/material_settings_)` calls; KEEP the edit_settings_ entry (EditSettings is out of scope for this phase). In finalizePreferencesTab (scene.C:1115-1138), remove the three removeEntry + null-out blocks for the dialog members; KEEP the edit_settings_ block. Remove the now-dangling private members `light_settings_`/`material_settings_`/`stage_settings_` from scene.h:953-955, the forward declarations (scene.h:81-83), and the getStageSettings()/getMaterialSettings() accessors (scene.h:321-334) — unless Task 3 still needs the accessors for the tutorial repoint, in which case coordinate with Task 3 ordering (prefer removing the members here and repointing demoTutorial in Task 3 so the accessors can go). The Preferences dialog (Tools › Legacy Settings) will no longer surface these three pages — that is the intended end state; the legacy LegacySettingsHelper submenu retirement is 999.53's concern, not this plan's. Verify the libVIEW build still references the dialog headers only where 999.53 will clean them up (the dialog .C files still #include scene.h — that is fine; this plan does not delete the dialog files).</action>
  <verify>
    <automated>grep -nc "new LightSettings\|new MaterialSettings\|new StageSettings\|insertEntry(light_settings_)\|insertEntry(stage_settings_)\|insertEntry(material_settings_)" source/VIEW/WIDGETS/scene.C</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "new LightSettings\|new MaterialSettings\|new StageSettings" source/VIEW/WIDGETS/scene.C` returns 0.
    - `grep -c "insertEntry(light_settings_)\|insertEntry(stage_settings_)\|insertEntry(material_settings_)" source/VIEW/WIDGETS/scene.C` returns 0.
    - The edit_settings_ insertEntry/removeEntry blocks are PRESERVED (grep for edit_settings_ still present in both init/finalize).
    - The 3 dialog members + forward decls are removed from scene.h (grep returns 0 for `light_settings_`/`material_settings_`/`stage_settings_` member declarations).
    - libVIEW + BALLView build green (verified at Task 4 checkpoint).
  </acceptance_criteria>
  <done>Scene constructs none of the three dialogs and registers none of them in the Preferences stack; the dialog members are gone from scene.h; EditSettings is untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Repoint the demoTutorialDialog consumers off the scene dialog accessors</name>
  <files>source/APPLICATIONS/BALLVIEW/demoTutorialDialog.C</files>
  <read_first>
    - source/APPLICATIONS/BALLVIEW/demoTutorialDialog.C:100-220 — the #ifdef BALL_HAS_RTFACT blocks using getStageSettings()->updateFromStage() (110/171), getMaterialSettings()->updateDefaultMaterialsFromStage() (211), LightSettings::getInstance(0)->updateFromStage() (126/186); plus the :366 site.
    - CLAUDE.md "Removed/disabled: RTfact" — these blocks are compiled out in normal builds; the repoint must keep them compiling IF RTfact were enabled, OR remove the dialog-specific refresh calls that no longer have a target.
    - The Scene/Stage backend the surrounding code already uses (stage->clearLightSources()/addLightSource(), stage->getMaterial(), Scene::applyPreferences()) — the dialog refresh calls are UI-sync no-ops once the dialogs are gone; the actual Stage mutations around them remain valid.
  </read_first>
  <action>In demoTutorialDialog.C, repoint or remove the calls that reach scene's now-removed dialog accessors: replace `getStageSettings()->updateFromStage()` (110/171/366), `getMaterialSettings()->updateDefaultMaterialsFromStage()` (211), and `LightSettings::getInstance(0)->updateFromStage()` (126/186). These were UI-refresh calls that synced the dialog widgets to the Stage state the surrounding code had just set; with the dialogs gone they have no target. Preserve the surrounding Stage mutations (the clearLightSources/addLightSource, setBackgroundColor, getMaterial() writes, and the Scene::applyPreferences() call that actually applies them) — only drop the dialog-widget refresh calls, or replace them with the equivalent Inspector controller resync (e.g. the LightController syncFromStage from Plan 01) if a controller handle is readily reachable. Since these blocks are inside #ifdef BALL_HAS_RTFACT and RTfact is disabled this era, the minimal correct change is to remove the dialog-refresh calls so the file compiles dialog-free; note in the SUMMARY that the tutorial RTfact path is dormant. Remove any now-unused #include of the dialog headers from demoTutorialDialog.C IF nothing else in the file uses them (grep first).</action>
  <verify>
    <automated>grep -nc "getStageSettings()\|getMaterialSettings()\|LightSettings::getInstance" source/APPLICATIONS/BALLVIEW/demoTutorialDialog.C</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getStageSettings()\|getMaterialSettings()" source/APPLICATIONS/BALLVIEW/demoTutorialDialog.C` returns 0.
    - `grep -c "LightSettings::getInstance" source/APPLICATIONS/BALLVIEW/demoTutorialDialog.C` returns 0.
    - The surrounding Stage mutations + Scene::applyPreferences() calls are preserved (grep for clearLightSources/applyPreferences still present in the RTfact blocks).
    - demoTutorialDialog.C compiles dialog-free (verified at Task 4 checkpoint).
  </acceptance_criteria>
  <done>demoTutorialDialog no longer reaches scene's dialog accessors; the dormant RTfact tutorial Stage-setup logic is preserved; the file compiles without the three dialogs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>scene.C constructs none of LightSettings/MaterialSettings/StageSettings, registers none in the Preferences stack, reads stereo config from a Scene-owned struct, computes stereo defaults in-Scene, and the tutorial dialog is repointed. This is the FINAL cut-over — lights, materials, and the stereo enter/exit plumbing must all still work end-to-end with the dialogs no longer present in scene.C.</what-built>
  <how-to-verify>
    1. Build libVIEW + BALLView; confirm it links green on this OS. (The dialog .C/.h files still exist — they are deleted in 999.53 — so the build should still find them; the point is scene.C no longer CONSTRUCTS them.)
    2. Run the grep proof: `grep -n "new LightSettings\|new MaterialSettings\|new StageSettings" source/VIEW/WIDGETS/scene.C` → expect ZERO matches. Confirm.
    3. Run `grep -n "stage_settings_->\|material_settings_->\|light_settings_->" source/VIEW/WIDGETS/scene.C` → expect ZERO matches. Confirm.
    4. Launch BALLView, load a molecule.
    5. Materials: open the Inspector Material section, change shininess/intensity/color → confirm the rendered molecule shading changes (no regression from Plan 01).
    6. Lights: open the Inspector Lights section, change/add/remove a light → confirm the rendered lighting changes (no regression from Plan 01).
    7. Stage: open the Inspector Stage section, change background/projection/fog/downsampling → confirm the scene updates (no regression from Plan 02).
    8. Stereo plumbing: trigger Display › Stereo › Enter Stereo (the menu entry). EXPECTED behavior: it logs the "deferred to Phase 5" message and returns without entering stereo and WITHOUT crashing (the path is intentionally deferred — this confirms the relocated stereo config compiled + the guard still holds). Then Exit Stereo. Confirm no crash and the normal mono scene still renders.
    9. (If a stereo-capable display is available, note it in the report, but stereo is deferred — the acceptance is "no crash + deferral log", not active stereo rendering.)
    Report "approved" if the grep proofs pass, lights/materials/stage still render via the Inspector, and the stereo menu entry defers cleanly without crashing; otherwise describe the failure.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the render/behavior/grep issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stereo menu → Scene stereo bodies | User triggers a stereo mode; the bodies must defer cleanly (no top-level GL window construction) per the Phase 4a deferral. |
| demoTutorial RTfact path → Stage | Dormant tutorial code mutates Stage state behind the disabled RTfact flag. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99858-07 | Denial of Service (crash/freeze) | Stereo menu entry constructing a top-level GL window (the deferred high-risk path) | mitigate | Preserve the guard-and-return at the head of every enter*Stereo body verbatim; the relocation only swaps the config SOURCE below the return, never re-enables the path (Task 1 acceptance verifies the early return stays). |
| T-99858-08 | Tampering (compile-time dangling ref) | Removing dialog members while a consumer still references them | mitigate | Task ordering: relocate stereo reads (Task 1) + repoint tutorial (Task 3) before/with member removal (Task 2); the blocking build checkpoint catches any missed reference. |
| T-99858-09 | Info disclosure | n/a — local desktop config | accept | No sensitive data crosses these boundaries. |
</threat_model>

<verification>
- `grep -c "new LightSettings\|new MaterialSettings\|new StageSettings" source/VIEW/WIDGETS/scene.C` → 0 (the VIEW-CLEAN-03 construction proof).
- `grep -c "stage_settings_->\|material_settings_->\|light_settings_->" source/VIEW/WIDGETS/scene.C` → 0.
- `grep -c "getStageSettings()\|getMaterialSettings()\|LightSettings::getInstance" source/APPLICATIONS/BALLVIEW/demoTutorialDialog.C` → 0.
- libVIEW + BALLView build green on this OS (blocking checkpoint).
- Lights + materials + stage render via the Inspector; stereo menu defers cleanly without crash (blocking human-verify).
- NOTE: full VIEW-CLEAN-03 sign-off (tri-OS green + the cross-platform render UAT) is the phase-level UAT after all three plans land; this checkpoint verifies the local OS.
</verification>

<success_criteria>
- scene.C constructs none of LightSettings/MaterialSettings/StageSettings (grep proof passes).
- The Preferences stack registers none of the three dialogs; EditSettings preserved.
- The deferred stereo bodies + applyStereoDefaults use a Scene-owned config / in-Scene computation and still compile.
- demoTutorialDialog is repointed off the scene dialog accessors.
- Lights, materials, stage render end-to-end via the Inspector; stereo defers cleanly (GUI-verified).
- 999.53 is unblocked to delete the LightSettings/MaterialSettings/StageSettings triples.
</success_criteria>

<output>
After completion, create `.planning/phases/999.58-scene-settings-stereo-cutover/999.58-03-SUMMARY.md`
</output>
