---
phase: 999.57-inspector-controller-cutover-displayproperties
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - include/BALL/VIEW/MODELS/modelProcessorFactory.h
  - source/VIEW/MODELS/modelProcessorFactory.C
  - source/VIEW/MODELS/sources.cmake
  - source/VIEW/DIALOGS/modelSettingsDialog.C
autonomous: true
requirements: [VIEW-CLEAN-02]
must_haves:
  truths:
    - "createModelProcessor logic exists as a free-standing factory taking an explicit parameter struct, NOT widget state"
    - "modelSettingsDialog::createModelProcessor delegates to the relocated factory (no behavior change while the dialog still exists)"
    - "The relocated factory has zero QtWidgets includes — it is headless-constructible"
  artifacts:
    - path: "include/BALL/VIEW/MODELS/modelProcessorFactory.h"
      provides: "ModelProcessorParams struct + ModelProcessorFactory::create(ModelType, const ModelProcessorParams&) declaration"
      contains: "ModelProcessorFactory"
    - path: "source/VIEW/MODELS/modelProcessorFactory.C"
      provides: "Relocated createModelProcessor + applySettingsTo bodies as pure functions of ModelProcessorParams"
      contains: "ModelProcessorFactory::create"
  key_links:
    - from: "source/VIEW/DIALOGS/modelSettingsDialog.C"
      to: "ModelProcessorFactory::create"
      via: "createModelProcessor delegates after packing getters into ModelProcessorParams"
      pattern: "ModelProcessorFactory::create"
---

<objective>
Relocate the `createModelProcessor` model-processor factory (and its `applySettingsTo` parameter-baking helper) out of the legacy `ModelSettingsDialog` into a free-standing `ModelProcessorFactory` in the MODELS layer that is a pure function of an explicit `ModelProcessorParams` struct — no QWidget dependency.

Purpose: The factory at `modelSettingsDialog.C:189` (`createModelProcessor`) is one of the two render-path factories pinning `displayProperties.C` to the legacy dialogs (the VIEW-CLEAN-02 lynchpin). It must live somewhere that does NOT depend on the dialog's widgets before the render path (`displayProperties.C:451`, `modelController.C:159`) can be switched off the dialog in Plan 03. This plan ONLY relocates + delegates — it does NOT yet repoint the render path (that is Plan 03), so the build stays green and rendering is unchanged at every step.

Output: A new `ModelProcessorFactory` whose `create(ModelType, const ModelProcessorParams&)` reproduces the exact `createModelProcessor` + `applySettingsTo` behavior; `ModelSettingsDialog::createModelProcessor` rewritten to pack its ~30 getters into a `ModelProcessorParams` and delegate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.48-a11y-flag-flip-removal/deferred-items.md
@.planning/phases/999.44-unified-inspector/999.44-06-SUMMARY.md
@source/VIEW/KERNEL/controllers/PATTERN.md
@./CLAUDE.md

<interfaces>
<!-- The exact parameter surface createModelProcessor + applySettingsTo read from the dialog.
     These ~30 numeric/bool getters become the fields of ModelProcessorParams. Extracted from
     modelSettingsDialog.C:114-255. Executor: use these directly — no codebase exploration needed. -->

ModelProcessor factory entry (modelSettingsDialog.C:189):
  ModelProcessor* createModelProcessor(ModelType type) const;
    - switch(type) over: MODEL_LINES, MODEL_STICK, MODEL_BALL_AND_STICK,
      MODEL_SE_SURFACE, MODEL_SA_SURFACE, MODEL_VDW, MODEL_BACKBONE,
      MODEL_RIBBON, MODEL_CARTOON, MODEL_HBONDS, MODEL_FORCES
    - constructs AddLineModel / AddBallAndStickModel (enableStickModel|enableBallAndStickModel) /
      AddSurfaceModel (SOLVENT_EXCLUDED_SURFACE|SOLVENT_ACCESSIBLE_SURFACE) / AddVanDerWaalsModel /
      AddBackboneModel (+ setRibbonMode(true) for RIBBON) / AddCartoonModel / HBondModelProcessor / ForceModel
    - then calls applySettingsTo(*model_processor) before returning

applySettingsTo(ModelProcessor& mp) const (modelSettingsDialog.C:114) reads these getters:
  getStickStickRadius(), getBallAndStickStickRadius(), getBallRadius(),
  ballAndStickDashedBondsEnabled(), getSurfaceProbeRadius(), getVDWRadiusFactor(),
  getCartoonTubeRadius(), getCartoonHelixRadius(), getCartoonArrowWidth(),
  getCartoonStrandHeight(), getCartoonStrandWidth(), cartoon_dna_ladder->isChecked(),
  getDNALadderRadius(), getDNABaseRadius(), getDNAHelixRadius(), ribbons_enabled->isChecked(),
  getTubeRadius(), getHBondsRadius(),
  getForceMaxLength(), getForceScaling(), getForceOffset(), getForceBase()
  (note: cartoon_dna_ladder and ribbons_enabled are checkboxes read directly — map them to
   bool fields cartoon_dna_ladder_enabled / ribbons_enabled in the params struct)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ModelProcessorFactory header + ModelProcessorParams struct</name>
  <read_first>
    - source/VIEW/DIALOGS/modelSettingsDialog.C lines 114-255 (applySettingsTo + createModelProcessor — the bodies being relocated and the exact getter surface)
    - include/BALL/VIEW/MODELS/modelProcessor.h (ModelProcessor base + ModelType enum location)
    - source/VIEW/KERNEL/controllers/PATTERN.md (the "zero QtWidgets includes — headless-testable" Controller-layer rule the new factory must also honor)
  </read_first>
  <files>include/BALL/VIEW/MODELS/modelProcessorFactory.h</files>
  <action>Create a BALL_VIEW_EXPORT struct ModelProcessorParams in namespace BALL::VIEW holding one field per getter in the interfaces block above (float fields for the radii/scaling/offset/lengths, bool fields cartoon_dna_ladder_enabled + ribbons_enabled). Initialize every field to the legacy dialog's compiled default value (read those defaults from modelSettingsDialog.C's BALL_..._DEFAULT_* constants / member initializers so a default-constructed ModelProcessorParams reproduces the dialog's out-of-box state). Declare a class ModelProcessorFactory with a single static method `static ModelProcessor* create(ModelType type, const ModelProcessorParams& params);` Doxygen-document both per CLAUDE.md conventions. Include guard BALL_VIEW_MODELS_MODELPROCESSORFACTORY_H, modeline header, namespace BALL::VIEW. CRITICAL: include only <BALL/...> + the ModelType enum header — zero QtWidgets includes (this is the headless-factory invariant; the dialog widgets stay behind in modelSettingsDialog.C).</action>
  <verify>
    <automated>test -f include/BALL/VIEW/MODELS/modelProcessorFactory.h &amp;&amp; grep -q "ModelProcessorFactory::create\|static ModelProcessor\* create" include/BALL/VIEW/MODELS/modelProcessorFactory.h &amp;&amp; ! grep -q "QtWidgets\|QSlider\|QComboBox\|QCheckBox" include/BALL/VIEW/MODELS/modelProcessorFactory.h</automated>
  </verify>
  <done>Header exists declaring ModelProcessorParams (all fields defaulted to dialog defaults) + ModelProcessorFactory::create; no QtWidgets includes; include guard + modeline present.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ModelProcessorFactory::create + register in cmake</name>
  <read_first>
    - source/VIEW/DIALOGS/modelSettingsDialog.C lines 114-255 (the source bodies — copy the switch + applySettingsTo logic verbatim, swapping every `getXxx()`/`widget->isChecked()` call for the corresponding `params.field`)
    - source/VIEW/MODELS/sources.cmake (registration list — mirror the SOURCES_LIST entry style; add the new .C alphabetically near the other *Model*.C entries)
    - include/BALL/VIEW/MODELS/modelProcessorFactory.h (the contract from Task 1)
  </read_first>
  <files>source/VIEW/MODELS/modelProcessorFactory.C, source/VIEW/MODELS/sources.cmake</files>
  <action>Implement ModelProcessorFactory::create by transcribing createModelProcessor's switch (modelSettingsDialog.C:189-255) and applySettingsTo's per-type RTTI dispatch (114-186), replacing every dialog getter with the matching ModelProcessorParams field. Preserve the exact construction order, the enableStickModel/enableBallAndStickModel/setType/setRibbonMode calls, the "backbone after cartoon" RTTI ordering comment, and the default-case Log.error + return NULL. Call the relocated applySettingsTo-equivalent logic inline (or as a static file-local helper) before returning, exactly as the original does. Add the .C to source/VIEW/MODELS/sources.cmake. Do NOT gate the body behind BALL_UI_V2 — this factory is on the production render path for BOTH UI cells.</action>
  <verify>
    <automated>grep -q "modelProcessorFactory.C" source/VIEW/MODELS/sources.cmake &amp;&amp; grep -q "ModelProcessorFactory::create" source/VIEW/MODELS/modelProcessorFactory.C &amp;&amp; cmake --build build-ui-on --target VIEW 2>&amp;1 | tail -5</automated>
  </verify>
  <done>modelProcessorFactory.C compiles into libVIEW (BALL_UI_V2=ON); the create() switch + applySettingsTo logic is a faithful transcription parameterized by ModelProcessorParams; cmake registration landed.</done>
</task>

<task type="auto">
  <name>Task 3: Delegate ModelSettingsDialog::createModelProcessor to the relocated factory</name>
  <read_first>
    - source/VIEW/DIALOGS/modelSettingsDialog.C lines 114-255 (the methods being thinned to a delegation)
    - include/BALL/VIEW/MODELS/modelProcessorFactory.h (the contract to delegate to)
  </read_first>
  <files>source/VIEW/DIALOGS/modelSettingsDialog.C</files>
  <action>Rewrite ModelSettingsDialog::createModelProcessor to (1) construct a ModelProcessorParams, (2) populate every field from the dialog's existing getters/checkbox reads (the exact getter list in the interfaces block), (3) return ModelProcessorFactory::create(type, params). Either delete the now-redundant applySettingsTo body and have createModelProcessor's only logic be pack-params-then-delegate, OR keep applySettingsTo as a thin wrapper if other call sites in modelSettingsDialog.C still call it (grep first; preserve any non-factory caller). Add the #include <BALL/VIEW/MODELS/modelProcessorFactory.h>. This is behavior-preserving: same params in, same processor out. The legacy dialog stays fully functional (999.53 deletes it later); we are only moving WHERE the construction logic lives.</action>
  <verify>
    <automated>grep -q "ModelProcessorFactory::create" source/VIEW/DIALOGS/modelSettingsDialog.C &amp;&amp; cmake --build build-ui-on --target BALLView 2>&amp;1 | tail -5</automated>
  </verify>
  <done>BALLView links green (BALL_UI_V2=ON); createModelProcessor packs params + delegates to ModelProcessorFactory::create; the model-processor construction logic now lives in the headless factory while the dialog remains operational.</done>
</task>

</tasks>

<verification>
- libVIEW + BALLView build green on macOS-arm64 BALL_UI_V2=ON (the local build-ui-on tree).
- `grep -c "case MODEL_" source/VIEW/MODELS/modelProcessorFactory.C` matches the model-type count from the original switch (no model type dropped in transcription).
- modelSettingsDialog.C no longer constructs the per-type ModelProcessor objects itself — it delegates.
- This plan does NOT touch displayProperties.C:451 or modelController.C — the render-path repoint is Plan 03. Build-green is the only proof needed here (no render-behavior change yet; the dialog still constructs via the same logic, now relocated).
</verification>

<success_criteria>
- ModelProcessorFactory exists as a headless (no-QtWidgets) pure-function-of-params factory reproducing createModelProcessor + applySettingsTo.
- ModelSettingsDialog::createModelProcessor delegates to it with no behavior change.
- The build is green; rendering is unchanged (factory output is byte-identical for identical params).
</success_criteria>

<output>
After completion, create `.planning/phases/999.57-inspector-controller-cutover-displayproperties/999.57-01-SUMMARY.md` recording: the ModelProcessorParams field set, where the factory lives, and confirmation that displayProperties.C:451 / modelController.C:159 are NOT yet repointed (Plan 03 owns that).
</output>
