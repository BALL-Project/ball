---
phase: 999.57-inspector-controller-cutover-displayproperties
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - source/VIEW/DIALOGS/displayProperties.C
  - source/VIEW/KERNEL/controllers/modelController.C
  - source/VIEW/KERNEL/controllers/coloringController.C
  - .planning/phases/999.57-inspector-controller-cutover-displayproperties/999.57-CONSUMER-AUDIT.md
autonomous: false
requirements: [VIEW-CLEAN-02]
must_haves:
  truths:
    - "displayProperties.C:451 no longer calls model_settings_->createModelProcessor — it calls ModelProcessorFactory::create"
    - "displayProperties.C:487 no longer calls coloring_settings_->createColorProcessor — it calls ColorProcessorFactory::create"
    - "modelController.C no longer reaches into DisplayProperties::getModelSettingsDialog()->createModelProcessor; it calls ModelProcessorFactory::create"
    - "coloringController.C no longer reaches into DisplayProperties::getColoringSettingsDialog()->createColorProcessor; it calls ColorProcessorFactory::create"
    - "Loading a molecule still renders the default representation; the Inspector Model + Coloring sections still drive the rendered scene (GUI-verified on a running BALLView)"
    - "Every remaining displayProperties / modelSettingsDialog / coloringSettingsDialog reference is classified: relocated (this phase), false-positive (ModelInformation method name collision), or owned-by-999.53 (DisplayProperties dialog deletion)"
  artifacts:
    - path: ".planning/phases/999.57-inspector-controller-cutover-displayproperties/999.57-CONSUMER-AUDIT.md"
      provides: "The classification table for every grep hit, proving render-path detachment + handing 999.53 its exact remaining-deletion worklist"
      contains: "false-positive"
  key_links:
    - from: "source/VIEW/DIALOGS/displayProperties.C"
      to: "ModelProcessorFactory::create / ColorProcessorFactory::create"
      via: "applyModelSettings_ / applyColoringSettings_ build a params/overrides struct from dialog widgets and call the relocated factory"
      pattern: "(Model|Color)ProcessorFactory::create"
    - from: "source/VIEW/KERNEL/controllers/modelController.C"
      to: "ModelProcessorFactory::create"
      via: "apply() builds ModelProcessorParams from the controller's own mirrored fields"
      pattern: "ModelProcessorFactory::create"
---

<objective>
Switch the production render path off the legacy dialog factories and onto the relocated factories from Plans 01/02, then prove on a running BALLView that rendering is unchanged, then audit + classify every remaining legacy-dialog reference so Phase 999.53 inherits an unambiguous deletion worklist.

Purpose: This is the actual VIEW-CLEAN-02 cut-over. After this plan: `displayProperties.C:451/487` call `ModelProcessorFactory::create` / `ColorProcessorFactory::create` (not `model_settings_->createModelProcessor` / `coloring_settings_->createColorProcessor`); `modelController.C` + `coloringController.C` no longer reach through `DisplayProperties::getModelSettingsDialog()` / `getColoringSettingsDialog()` for the factory. The legacy dialogs remain present-but-no-longer-on-the-render-path. The molecule-load default-rep path and the Inspector Model/Coloring sections must STILL render correctly — and because this touches the threaded render pipeline, compile ≠ render-correctness, so the proof is a blocking running-BALLView human-verify.

Output: Repointed render path (3 source files) + a CONSUMER-AUDIT.md classifying every remaining `displayProperties` / `modelSettingsDialog` / `coloringSettingsDialog` grep hit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.48-a11y-flag-flip-removal/deferred-items.md
@.planning/phases/999.57-inspector-controller-cutover-displayproperties/999.57-01-SUMMARY.md
@.planning/phases/999.57-inspector-controller-cutover-displayproperties/999.57-02-SUMMARY.md
@source/VIEW/KERNEL/controllers/PATTERN.md
@./CLAUDE.md

<interfaces>
<!-- The render-path call sites to repoint, and the controllers' own mirrored state to source params from. -->

displayProperties.C render-path factory calls (the two lynchpin lines):
  :451  rep.setModelProcessor(model_settings_->createModelProcessor(current_type));   // in applyModelSettings_
  :487  rep.setColorProcessor(coloring_settings_->createColorProcessor(current_coloring)); // in applyColoringSettings_
  Both reached via DisplayProperties::createRepresentation (the molecule-load default-rep path,
  invoked from representationManager.C:438 restoreRepresentations, and from the legacy "Custom" dialog).

modelController.C current factory reach-through (modelController.C:148-166):
  DisplayProperties::getInstance(0)->getModelSettingsDialog()->createModelProcessor(new_type)
  The controller ALREADY mirrors the params it needs as members: ball_radius_, ball_stick_bond_radius_,
  dashed_bonds_, stick_radius_, surface_probe_radius_, cartoon_tube_radius_ (+ model_type_/precision_).
  (Params the controller does NOT yet mirror — cartoon helix/arrow/strand, DNA radii, force params,
   HBond radius, VDW factor, backbone tube — fall back to ModelProcessorParams DEFAULTS, which equal
   the dialog defaults per Plan 01; the controller never exposed those, so this is no regression.)

coloringController.C current factory reach-through (coloringController.C:96-101):
  DisplayProperties::getInstance(0)->getColoringSettingsDialog()->createColorProcessor(new_method)
  The controller exposes only coloring_method_ + value_min_/value_max_ (InterpolateColorProcessor range).
  It does NOT expose per-element/residue color overrides — so it calls ColorProcessorFactory::create
  with EMPTY ColoringOverrides (compiled defaults), then applies value_min_/value_max_ to the
  InterpolateColorProcessor exactly as it does today (coloringController.C:118-124).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint modelController + coloringController off the legacy dialogs onto the factories</name>
  <read_first>
    - source/VIEW/KERNEL/controllers/modelController.C lines 144-192 (the apply() factory reach-through to replace)
    - source/VIEW/KERNEL/controllers/coloringController.C lines 94-133 (the apply() factory reach-through to replace)
    - include/BALL/VIEW/MODELS/modelProcessorFactory.h + colorProcessorFactory.h (the factory contracts from Plans 01/02)
    - .planning/phases/999.57-inspector-controller-cutover-displayproperties/999.57-01-SUMMARY.md + 999.57-02-SUMMARY.md (the exact param/override field names)
  </read_first>
  <files>source/VIEW/KERNEL/controllers/modelController.C, source/VIEW/KERNEL/controllers/coloringController.C</files>
  <action>In ModelController::apply(): replace the DisplayProperties::getModelSettingsDialog()->createModelProcessor block (modelController.C:148-166) with: build a ModelProcessorParams from the controller's mirrored members (ball_radius_, ball_stick_bond_radius_, dashed_bonds_, stick_radius_, surface_probe_radius_, cartoon_tube_radius_; leave the un-mirrored fields at their struct defaults), then `rep_->setModelProcessor(ModelProcessorFactory::create(new_type, params))`. Remove the now-dead `#include <BALL/VIEW/DIALOGS/displayProperties.h>` and `<BALL/VIEW/DIALOGS/modelSettingsDialog.h>` IF no other code in the file still needs them (the revert() path at modelController.C:73-89 also reads getModelSettingsDialog — relocate that param-mirroring to read from the controller's own last-known values OR keep it reading the dialog ONLY if the dialog is still the source of advanced params; prefer detaching revert() too so the controller is dialog-independent, falling back to the Representation's processor state). In ColoringController::apply(): replace the getColoringSettingsDialog()->createColorProcessor block (coloringController.C:96-101) with `cp = ColorProcessorFactory::create(new_method)` (empty overrides = compiled defaults), keep the existing InterpolateColorProcessor value_min_/value_max_ application and rep_->update(false). Remove the dead displayProperties.h / coloringSettingsDialog.h includes if unused. Add the factory includes. Preserve the re-entrancy guard, busy guard, and appliedStub() emission unchanged.</action>
  <verify>
    <automated>grep -q "ModelProcessorFactory::create" source/VIEW/KERNEL/controllers/modelController.C &amp;&amp; grep -q "ColorProcessorFactory::create" source/VIEW/KERNEL/controllers/coloringController.C &amp;&amp; ! grep -q "getModelSettingsDialog()->createModelProcessor\|getColoringSettingsDialog()->createColorProcessor" source/VIEW/KERNEL/controllers/modelController.C source/VIEW/KERNEL/controllers/coloringController.C &amp;&amp; cmake --build build-ui-on --target VIEW 2>&amp;1 | tail -5</automated>
  </verify>
  <done>Both controllers call the relocated factories; neither reaches DisplayProperties for the factory; libVIEW builds green (BALL_UI_V2=ON).</done>
</task>

<task type="auto">
  <name>Task 2: Repoint displayProperties.C:451/487 onto the factories</name>
  <read_first>
    - source/VIEW/DIALOGS/displayProperties.C lines 442-514 (applyModelSettings_ + applyColoringSettings_ — the two factory call sites)
    - include/BALL/VIEW/MODELS/modelProcessorFactory.h + colorProcessorFactory.h (the contracts)
    - source/VIEW/DIALOGS/modelSettingsDialog.C + coloringSettingsDialog.C (the createModelProcessor/createColorProcessor that now just pack-params-and-delegate after Plans 01/02 — confirm the param packing you replicate here)
  </read_first>
  <files>source/VIEW/DIALOGS/displayProperties.C</files>
  <action>In applyModelSettings_ (displayProperties.C:451): replace `rep.setModelProcessor(model_settings_->createModelProcessor(current_type))` with: build a ModelProcessorParams from model_settings_'s getters (the SAME getters modelSettingsDialog::createModelProcessor now packs — reuse that exact mapping), then `rep.setModelProcessor(ModelProcessorFactory::create(current_type, params))`. Keep the subsequent `model_settings_->applySettingsTo(*rep.getModelProcessor())` ONLY if it does something the factory's baked-in applySettingsTo does NOT (it should now be redundant since the factory already applied the params — REMOVE it if redundant to avoid double-application; verify by reading line 471). In applyColoringSettings_ (displayProperties.C:487): replace `rep.setColorProcessor(coloring_settings_->createColorProcessor(current_coloring))` with a ColoringOverrides built from coloring_settings_'s tables/buttons (same mapping coloringSettingsDialog::createColorProcessor now uses) + `rep.setColorProcessor(ColorProcessorFactory::create(current_coloring, overrides))`; reconcile the redundant `coloring_settings_->applySettingsTo(*cp)` at line 502 the same way. Add factory includes. NOTE: displayProperties.C still legitimately uses model_settings_/coloring_settings_ for OTHER things (widget state, the legacy modal UI) — only the FACTORY CALLS move off them; do not remove the member pointers (999.53 deletes displayProperties.C wholesale).</action>
  <verify>
    <automated>grep -q "ModelProcessorFactory::create" source/VIEW/DIALOGS/displayProperties.C &amp;&amp; grep -q "ColorProcessorFactory::create" source/VIEW/DIALOGS/displayProperties.C &amp;&amp; ! grep -q "model_settings_->createModelProcessor\|coloring_settings_->createColorProcessor" source/VIEW/DIALOGS/displayProperties.C &amp;&amp; cmake --build build-ui-on --target BALLView 2>&amp;1 | tail -5</automated>
  </verify>
  <done>displayProperties.C:451/487 call the relocated factories; no `*_settings_->create*Processor` factory call remains; BALLView links green (BALL_UI_V2=ON).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The model + color processor factories were relocated to the headless MODELS layer (Plans 01/02) and the production render path was repointed onto them: displayProperties.C:451/487 (the molecule-load default-rep path, via representationManager restoreRepresentations) and both ModelController/ColoringController::apply() (the Inspector Model/Coloring sections) now construct processors through ModelProcessorFactory::create / ColorProcessorFactory::create instead of the legacy dialogs. This directly touches the threaded render pipeline — compile-green does NOT prove the scene still renders correctly, so this requires eyes on a running BALLView.</what-built>
  <how-to-verify>
    1. Launch the local build: `open build-ui-on/bin/BALLView.app` (built with BALL_UI_V2=ON).
    2. DEFAULT-REP RENDER (displayProperties.C path): File › Open a known molecule (e.g. a small PDB from data/). Confirm the default representation appears and renders correctly (atoms/bonds visible, colored) — exactly as before this phase. This exercises representationManager → DisplayProperties::createRepresentation → the relocated ModelProcessorFactory + ColorProcessorFactory.
    3. INSPECTOR MODEL SECTION (modelController path): In the Inspector Representation tab's Model section, change the model type (e.g. Lines → Ball-and-Stick → Cartoon for a protein) and the drawing precision. Confirm the rendered scene updates to the new model each time. Adjust the transparency slider; confirm transparency changes in the scene.
    4. INSPECTOR COLORING SECTION (coloringController path): In the Coloring section, switch coloring method (e.g. Element → Residue Name → Chain). Confirm the molecule recolors in the scene to match each method, with the expected default colors (compiled defaults — element CPK colors, etc.).
    5. REGRESSION CHECK: confirm no visual regression vs. the pre-phase build — same default colors, same model geometry, no missing/black geometry, no crash on rep create/update.
    Expected: every render path produces the same visible result as before the cut-over. The factory move is behavior-preserving; any visible difference is a bug to report.
  </how-to-verify>
  <resume-signal>Type "approved" if all four render paths (default-rep, model-section, coloring-section, no-regression) render correctly on the running BALLView; otherwise describe exactly what rendered wrong (which path, what you saw vs. expected).</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Audit + classify every remaining legacy-dialog reference for 999.53</name>
  <read_first>
    - .planning/phases/999.48-a11y-flag-flip-removal/deferred-items.md (the per-file dependency table + acceptance criteria 999.53 must satisfy)
    - source/VIEW/KERNEL/modelInformation.h or .C (confirm modelMuteableByDisplayProperties / coloringMuteableByDisplayProperties are ModelInformation methods — the name-collision false positives)
    - source/VIEW/KERNEL/mainControl.C lines 2115-2177 (confirm these are enableCreationForNewMolecules — the auto-rep toggle, NOT the factory)
  </read_first>
  <files>.planning/phases/999.57-inspector-controller-cutover-displayproperties/999.57-CONSUMER-AUDIT.md</files>
  <action>Run `grep -rn "displayProperties\|DisplayProperties\|modelSettingsDialog\|ModelSettingsDialog\|coloringSettingsDialog\|ColoringSettingsDialog" source/ include/` and produce a markdown classification table. For each hit assign exactly one class: (A) RELOCATED-THIS-PHASE — factory call now on the MODELS factory (should be zero remaining createModelProcessor/createColorProcessor reach-throughs); (B) FALSE-POSITIVE — name collision, e.g. ModelInformation::modelMuteableByDisplayProperties / coloringMuteableByDisplayProperties (representationManager.C:336, molecularControl.C:484,498) and MainControl::enableCreationForNewMolecules (mainControl.C:2115-2177) — these are NOT the DisplayProperties dialog and do NOT block deletion; (C) OWNED-BY-999.53 — genuine DisplayProperties-dialog references that 999.53 removes when it deletes displayProperties.{C,h,ui} itself (the mainframe.C:405 registration, ShowDisplayPropertiesMessage senders at molecularControl.C:1499 + geometricControl.C:402, representationManager.C:425/438 createRepresentation, the modelController/coloringController revert() dialog reads if any survive, the displayProperties.h include sites). Add a CRITICAL assertion section confirming: grep for `createModelProcessor\|createColorProcessor` in source/VIEW/DIALOGS/displayProperties.C, source/VIEW/KERNEL/controllers/modelController.C, source/VIEW/KERNEL/controllers/coloringController.C returns ONLY the relocated ModelProcessorFactory/ColorProcessorFactory calls (zero legacy reach-throughs) — i.e. the render path is detached. Hand 999.53 the class-C list as its remaining-deletion worklist, explicitly noting scene.C's stage/light/material dialogs are NOT in this phase (see the phase split note in the planning return / recommended sibling phase).</action>
  <verify>
    <automated>test -f .planning/phases/999.57-inspector-controller-cutover-displayproperties/999.57-CONSUMER-AUDIT.md &amp;&amp; grep -qi "false-positive" .planning/phases/999.57-inspector-controller-cutover-displayproperties/999.57-CONSUMER-AUDIT.md &amp;&amp; ! grep -rn "model_settings_->createModelProcessor\|coloring_settings_->createColorProcessor\|getModelSettingsDialog()->createModelProcessor\|getColoringSettingsDialog()->createColorProcessor" source/</automated>
  </verify>
  <done>CONSUMER-AUDIT.md classifies every grep hit into A/B/C; the assertion section proves zero legacy factory reach-throughs remain on the render path; 999.53 has its class-C deletion worklist.</done>
</task>

</tasks>

<verification>
- BALLView builds + links green on macOS-arm64 BALL_UI_V2=ON.
- `grep -rn "createModelProcessor\|createColorProcessor" source/VIEW/DIALOGS/displayProperties.C source/VIEW/KERNEL/controllers/modelController.C source/VIEW/KERNEL/controllers/coloringController.C` shows ONLY ModelProcessorFactory/ColorProcessorFactory calls — no `*_settings_->` or `get*SettingsDialog()->` factory reach-throughs.
- Blocking human-verify confirms the default-rep path + Inspector Model + Coloring sections render correctly on a running BALLView (compile ≠ render-correctness; this is the threaded-pipeline proof).
- CONSUMER-AUDIT.md exists with the A/B/C classification; the class-C list is 999.53's remaining-deletion worklist.
</verification>

<success_criteria>
- displayProperties.C:451/487 + both controllers' apply() are off the legacy dialog factories and onto the relocated MODELS factories.
- The molecule-load default-rep path and the Inspector Model/Coloring sections drive the rendered scene end-to-end (GUI-verified).
- Every remaining legacy-dialog reference is classified; render-path detachment is grep-proven; 999.53 inherits an unambiguous deletion worklist for the modelSettingsDialog / coloringSettingsDialog / displayProperties triples.
</success_criteria>

<output>
After completion, create `.planning/phases/999.57-inspector-controller-cutover-displayproperties/999.57-03-SUMMARY.md` recording: the render-path repoint, the human-verify result, the CONSUMER-AUDIT classification counts (A/B/C), and the explicit hand-off note to 999.53 (the two render-path dialog triples are now deletable; scene.C's stage/light/material dialogs are NOT — they need the recommended sibling phase first).
</output>
