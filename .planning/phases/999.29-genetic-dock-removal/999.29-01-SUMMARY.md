# Phase 999.29 — GENETICDOCK removal · SUMMARY

**Status:** COMPLETE · v1.7 · 2026-05-17
**Branch:** `v1.7-modernization`

## Outcome

Deleted the entire `BALL_DEPRECATED` GENETICDOCK subsystem (`include/BALL/DOCKING/GENETICDOCK/` and `source/DOCKING/GENETICDOCK/`) and the two `INCLUDE(...)` lines wiring it into the DOCKING module CMake. Pulled forward from the v2.0 999.12 slice per the v1.7 adversarial review, same class as Phase 999.37 (VRMLRenderer removal).

## Files deleted (18 total, 2,722 LOC)

**Headers (9 files, ~1,059 LOC) — `include/BALL/DOCKING/GENETICDOCK/`:**
- `dockProblem.h` (121)
- `evolutionaryDocking.h` (138)
- `genes.h` (147)
- `geneticAlgorithm.h` (189)
- `geneticIndividual.h` (111)
- `parameter.h` (99)
- `rotateBond.h` (106)
- `rotateBonds.h` (57)
- `sources.cmake`

**Sources (9 files, ~1,663 LOC) — `source/DOCKING/GENETICDOCK/`:**
- `dockProblem.C` (186)
- `evolutionaryDocking.C` (352)
- `genes.C` (254)
- `geneticAlgorithm.C` (484)
- `geneticIndividual.C` (121)
- `parameter.C` (91)
- `rotateBond.C` (206)
- `rotateBonds.C` (60)
- `sources.cmake`

## References removed (2 sites)

- `include/BALL/DOCKING/sources.cmake` — removed `INCLUDE(include/BALL/DOCKING/GENETICDOCK/sources.cmake)`
- `source/DOCKING/sources.cmake` — removed `INCLUDE(source/DOCKING/GENETICDOCK/sources.cmake)`

## Inventory of external dependencies — none

Pre-deletion sweep (grep across `*.C`, `*.h`, `*.cmake`, `*.in`, `*.ui`, `*.qrc`, `*.py`, `*.i`) for the symbols `GENETICDOCK`, `GeneticAlgorithm`, `GeneticIndividual`, `GenericGene`, `RotateBonds`, `EvolutionaryDocking`, `dockProblem`, `rotateBond`, `evolutionaryDocking` returned zero hits outside `GENETICDOCK/` itself. No factory registration in `source/DOCKING/COMMON/`, no UI menu in `source/VIEW/DIALOGS/dockDialog.C` or `source/VIEW/WIDGETS/dockingController.C` (the DockingController only registers `Geometric Fit`), no tests under `test/`, no APPLICATIONS references. Confirms the pre-Phase audit ([ROADMAP.md L725, L1496](../../ROADMAP.md), run 25970222837 / commit `cb392cc`).

## Warning census impact

v1.7 census recorded ~21 GENETICDOCK self-deprecation warnings (`-Wdeprecated-declarations` on the deleted classes used internally within the subsystem). Expected delta: **21 → 0**. Net warning reduction from this phase: **-21**.

## Local build verification

```
cmake --build build/uat-coldstart --target BALL VIEW BALLView -j 8
```

Result: **green**. All three targets built cleanly with zero errors and no GENETICDOCK-related warnings remaining.

## API surface impact

**None.** Audit established that no external (non-DOCKING) module consumed any GENETICDOCK class, and no UI exposed any GENETICDOCK feature. No public API break, no behavioral change for any BALLView user.

## Why now (v1.7, not v2.0)

The original v2.0 999.12 phase catalogued GENETICDOCK with the decision branch "**delete** the entire directory OR **un-deprecate** pending audit." The audit landed in May 2026 with a definitive "delete" disposition: zero external refs, zero UI, zero tests, zero docs. The v1.7 adversarial review pulled this forward into the same wave as Phase 999.37 (VRMLRenderer removal) because both are mechanical, isolated, reversible, zero-API-impact deletions that retire entire residual warning clusters in a single commit.
