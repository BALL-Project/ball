---
phase: 999.62-opaque-paint-contract
plan: 01
type: execute
release: v1.7.4
wave: A
v1_7_x_id: v1.7.x-35 (new)  # handover PLAN.md proposed "-30 (new)"; -30 was already minted in this repo (#527 transparency). Re-minted to the next free ID -35 at ingest. See v1.7.x-PATCH-QUEUE.md.
depends_on: [999.43]
files_modified:
  - data/BALLView/theme/theme-neutral.qss
  - include/BALL/VIEW/WIDGETS/inspectorSection.h
  - source/VIEW/WIDGETS/sectionHeader.C
  - source/VIEW/WIDGETS/inspector/inspectorView.C
  - source/VIEW/WIDGETS/inspector/inspectorBody.C
  - source/VIEW/WIDGETS/inspector/inspectorTabs.C
  - source/VIEW/WIDGETS/welcomeScreen.C
  - cmake/scripts/check-opaque-paint.sh
  - .github/workflows/ci.yml
  - data/BALLView/theme/THEME.md
autonomous: true
requirements:
  - ARCH-CONTRACT-04
  - OPAQUE-PAINT-01

must_haves:
  truths:
    - "Any container that hosts an animated child (QPropertyAnimation target) carries WA_OpaquePaintEvent + setAutoFillBackground(true) within 3 ancestor hops"
    - "An OpaquePaint CSS-equivalent rule is centralized in theme-neutral.qss as a token"
    - "A CI lint job `opaque-paint-lint` runs check-opaque-paint.sh and is BLOCKING after one week of clean STATUS runs"
    - "Dark-mode infrastructure (v1.8) can land on the opaque-paint discipline without re-discovering UFG-05/09/10"
---

# `999.62` — Opaque-paint contract · codification

**v1.7.x-35 (new)** · Wave A · ~2 days

> **Ingest note (2026-05-22).** The handover PLAN.md minted this as `v1.7.x-30 (new)`, but `v1.7.x-30` is already used in this repo for the #527 Material-transparency item (Phase 999.52). Re-minted to the next free patch-queue ID **v1.7.x-35** during ingest. The handover phase id `999.61` was renumbered to **999.62** (+1) to clear the collision with the in-flight `999.58` set.

## Goal

Convert the UFG-05 / 09 / 10 lessons from per-site fixes into a
structural contract: every container that hosts an animated child is
opaque, and CI enforces it.

## Why now

The fixes shipped in rc2/rc3 are in 5 specific files. A future
contributor adding a new collapsible section will reproduce the
defect unless the rule is structural. Dark mode (v1.8) raises the
visual cost — the failure mode is even louder at higher contrast.

## Scope

**In:**

1. **`InspectorSection` base** — set `WA_OpaquePaintEvent` +
   `setAutoFillBackground(true)` + paintEvent fillRect, ONCE at the
   base. Every subclass inherits.
2. **`InspectorView`, `InspectorBody`, `InspectorTabs`, the inner
   `QScrollArea` + viewport + contents widget** — opaque, codified in
   the constructors (currently set ad-hoc per-site).
3. **`WelcomeScreen`** — same pattern, already shipped at rc3; the
   `999.62` work is to factor it into a shared mixin (`OpaqueMixin`).
4. **Theme token** — `data/BALLView/theme/theme-neutral.qss` gains a
   centralized rule: `[opaqueContainer="true"] { background: palette(Window); }` — toggled by a dynamic property.
5. **`THEME.md`** — new design-system reference doc (~80 LOC)
   documenting the opaque-paint rule, the dynamic-property toggle,
   and the relationship to dark mode.
6. **CI lint script** `cmake/scripts/check-opaque-paint.sh` —
   1) greps for `QPropertyAnimation` in `source/VIEW/`,
   2) for each hit, walks up to 3 ancestors in the constructor,
   3) requires `WA_OpaquePaintEvent` OR `setAutoFillBackground(true)`
      OR an `opaqueContainer` dynamic-property set within those
      ancestors.
   Initially STATUS (warn-only); flips to BLOCKING after one week of
   clean runs (per `999.62-04-PLAN.md`).

**Out:**
- Refactoring already-correct sites — drive-by edits not allowed.
- Dark-mode visual tokens — v1.8 builds on this, not the other way.
- Removing per-site fillRect calls — the contract is "redundant is OK", strip them in v1.7.5+.

## Files touched

**New:**
- `cmake/scripts/check-opaque-paint.sh` (~50 LOC)
- `data/BALLView/theme/THEME.md` (~80 LOC)
- (within InspectorSection) `OpaqueMixin` helper or just an inline pattern.

**Edited:**
- `InspectorSection.h/.C` — factor opaque setup into the base ctor.
- `theme-neutral.qss` — add `[opaqueContainer="true"]` rule.
- 4 sibling widgets — simplify their per-site fillRect to use the dynamic-property toggle (no behavior change).
- `ci.yml` — new lint job.

## Acceptance criteria

- [ ] `check-opaque-paint.sh` reports 0 hits on current tree (STATUS pass).
- [ ] Inject a synthetic QPropertyAnimation without opaque parent in a test branch → script reports 1 hit.
- [ ] `THEME.md` exists and is readable; references back to `ARCHITECTURE-CONTRACT.md §10`.
- [ ] Existing UFG-05/09/10 fixes still in place (they're now redundant with the base-class pattern, but redundancy is acceptable in v1.7.4).
- [ ] Manual smoke: open Inspector → expand each section → collapse → screenshot every transient frame; no text ghosting.
- [ ] Smoke also passes on Linux + Windows (the v1.7.0 fixes were macOS-discovered; ensure they hold cross-platform).

## Risks

- **False positives in the lint.** Some `QPropertyAnimation`s animate non-visible properties (e.g. a counter). Mitigate via a whitelist file `cmake/scripts/opaque-paint-whitelist.txt` matched by full file:line; entries require a justifying comment.

## Out of scope

- Dark mode → v1.8. The opaque-paint contract is a *prerequisite* for dark mode landing safely.
- Custom clang-tidy check for the same rule → v1.7.5 stretch.

## Hand-off pointer

`ARCHITECTURE-CONTRACT.md §10` for the rule. `data/BALLView/theme/THEME.md` (writing it is part of the phase) for the design-system surface.

---

*Phase compiled for v1.7.4. Wave A · independent of 999.59 (no Controller-side touches).*
