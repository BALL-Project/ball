---
phase: 999.44-unified-inspector
plan: 07
subsystem: view-ui-tests
tags: [ballview-refresh, inspector, pixel-diff, regression-tests, ball-ui-v2, wave-4, DEFERRED]
type: standard
autonomous: true
wave: 4
requirements: []
depends_on: [999.44-06]
status: DEFERRED-TO-v1.7-RC-PATCH
---

# Phase 999.44 Plan 07 — Pixel-diff regression tests

## Status: DEFERRED to v1.7 RC patch

Per operating-contract scope-trim guidance ("Plan 999.44-07 (pixel-diff tests) is the most trimmable. LOW PRIORITY: if running short on token budget, defer this to v1.7 RC patch with breadcrumb — most important is shipping the user-visible Inspector content; tests are nice-to-have for v1.7-RC1 final cut.").

## Objective (deferred)

Add headless-rendered pixel-diff regression tests for each Inspector tab against the Handover mockups (Selection / Representation / Scene). Target: <5% pixel-diff per tab.

## Pickup notes for RC patch

- Headless rendering setup: reuse the Phase 02.2 smoke-check infrastructure (offscreen QSurfaceFormat + QOffscreenSurface).
- Mockup PNGs live in `Claude Design Handover/revitalization/04-phase-inspector/mockups/*.png` (relative to the handover repo root).
- Test framework: `test/VIEW_test/InspectorPixelDiff_test.C` would be the natural home. Use Boost.Test (already integrated for VIEW tests).
- Pixel-diff metric: per-pixel RGB sum-of-absolute-differences divided by 3×255×pixel-count; warn at 3%, fail at 5%.

## Sub-PRs covered when Plan 07 lands

- 4.7 pixel-diff selection tab
- 4.8 pixel-diff representation tab
- 4.9 pixel-diff scene tab

The plan is filed as part of Phase 999.44 even though it ships later; ROADMAP.md §999.44 reflects "Plans 02-06 COMPLETE; Plan 07 deferred to v1.7 RC patch."
