---
phase: 999.44-unified-inspector
plan: 06
subsystem: view-ui
tags: [ballview-refresh, inspector, stage-controller, cut-over, ball-ui-v2, wave-4]
type: standard
autonomous: true
wave: 4
requirements: []
depends_on: [999.44-05]
---

# Phase 999.44 Plan 06 — StageController apply() cut-over

## Objective

Migrate the `StageSettings::saveSettingsToStage_()` mutation body into `StageController::apply()`. Remove the read-only-mirror stub. Both surfaces (legacy StageSettings dialog + InspectorStageSection + InspectorBackgroundSection) now route mutation through the same Controller — consistency-by-construction (per Handover §risks).

## Tasks

### Task 1 — StageController::apply() now mutates Stage/Scene

**Files:**
- MODIFIED: `source/VIEW/KERNEL/controllers/stageController.C` — apply() pushes mirrored fields to Stage/Scene via setBackgroundColor / showCoordinateSystem / setFogIntensity / setEyeDistance / setFocalDistance.
- MODIFIED: `source/VIEW/KERNEL/controllers/PATTERN.md` — flip StageController status from "read-only mirror" to "cut-over complete".

## Success Criteria

- One commit `feat(999.44-06/01): ...`.
- BALL_UI_V2={ON,OFF} build green.
- Moving the BackgroundSection swatch in the Inspector actually changes the Scene background colour (legacy dialog continues to work too).
