---
phase: 999.44-unified-inspector
plan: 03
subsystem: view-ui
tags: [ballview-refresh, inspector, selection-tab, sections, selection-bus, ball-ui-v2, wave-4]
type: standard
autonomous: true
wave: 4
requirements: []
depends_on: [999.44-02]
---

# Phase 999.44 Plan 03 — Selection tab (sub-PR 4.2)

## Objective

Land the Inspector's Selection tab: 3 sections + selection-bus wiring so the tab updates live as the user picks composites in the Project dock or 3D Scene.

## Tasks

### Task 1 — Selection bus adapter + 3 sections

**Type:** auto

**Files:**
- NEW: `include/BALL/VIEW/WIDGETS/inspector/sections/selectionSummarySection.{h,C}`
- NEW: `include/BALL/VIEW/WIDGETS/inspector/sections/propertiesSection.{h,C}`
- NEW: `include/BALL/VIEW/WIDGETS/inspector/sections/quickActionsSection.{h,C}`
- NEW: `include/BALL/VIEW/WIDGETS/inspector/selectionAdapter.{h,C}` — `ConnectionObject` subclass that subscribes to `ControlSelectionMessage` / `NewSelectionMessage` and forwards to InspectorView.
- MODIFIED: `include/BALL/VIEW/WIDGETS/inspector/inspectorView.h` + `.C` — add `setSelection(std::list<Composite*>)` slot; replace Selection-tab empty state with the three sections when selection non-empty.
- MODIFIED: `source/VIEW/WIDGETS/sources.cmake` — register new sources.
- MODIFIED: `source/APPLICATIONS/BALLVIEW/mainframe.C` — construct + register the adapter as a child ConnectionObject of MainControl, point it at the InspectorDock's view.

**Sections:**
- **SelectionSummarySection** — Counts: atoms / residues / chains / molecules in the selection (Composite::host traversal).
- **PropertiesSection** — When exactly one Atom is selected, show element + position + partial charge. Otherwise show "Select a single atom for details".
- **QuickActionsSection** — Two buttons: "Clear Selection" + "Invert Selection" wired to MainControl's existing slots.

**Done when:**
- Selecting in MolecularControl (Structures dock) updates the Selection tab live.
- Build green on BALL_UI_V2={ON,OFF}.

## Success Criteria

- One commit `feat(999.44-03/01): ...`.
- Selection tab populates with non-empty selection; reverts to empty state on clear.
