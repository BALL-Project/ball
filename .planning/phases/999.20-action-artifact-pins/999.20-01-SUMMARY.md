---
phase: 999.20-action-artifact-pins
plan: "01"
subsystem: infra
tags: [github-actions, upload-artifact, download-artifact, ci-hygiene, build-accel]

# Dependency graph
requires:
  - phase: 999.19-per-tu-build-profiling
    provides: "3 new upload-artifact@v4 steps (ninja-log per platform) that this phase sweeps to v6"
provides:
  - "upload-artifact@v6 across ci.yml (10 sites) and release.yml (2 sites)"
  - "download-artifact@v7 across ci.yml (1 site) and release.yml (1 site)"
  - "MILESTONE-CONTEXT.md Phase 999.20 deferral resolved with both commit SHAs"
  - "BUILD-ACCEL-05 complete — 5-phase build-accel cluster fully closed"
affects: [999.21, phase-9, release-workflow]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "upload-artifact@v6 with artifact immutability by default; all artifact names include unique suffix (${{ github.sha }} or ${{ github.run_id }}) to avoid collisions"
    - "download-artifact@v7 paired with v6 producer per actions/upload-artifact compatibility matrix"

key-files:
  created:
    - ".planning/phases/999.20-action-artifact-pins/999.20-01-SUMMARY.md"
  modified:
    - ".github/workflows/ci.yml"
    - ".github/workflows/release.yml"
    - ".planning/MILESTONE-CONTEXT.md"
    - ".planning/REQUIREMENTS.md"
    - ".planning/STATE.md"
    - ".planning/ROADMAP.md"

key-decisions:
  - "Two-commit shape preserved (upload@v6 first, then download@v7) for bisect-friendliness even though parallel-session concurrency cancelled both individual CI runs — the combined run (25970222837) on a later parallel-session commit validated both"
  - "No overwrite: true additions needed — all artifact names include ${{ github.sha }} or ${{ github.run_id }}; single bare name spike-smoke-log-glcore-macos is guarded by matrix.os == 'macos-arm64' (one upload per run)"
  - "999.19's 3 new ninja-log upload steps landed before 999.20 (expected order); swept from v4 to v6 in a single coherent commit"

patterns-established:
  - "Artifact name collision pre-flight: always verify artifact names include unique suffix when bumping to v6+"

requirements-completed:
  - BUILD-ACCEL-05

# Metrics
duration: ~45min
completed: 2026-05-16
---

# Phase 999.20 Plan 01: Action Artifact Pin Sweep v4→v6/v7 Summary

**upload-artifact@v4→v6 (12 sites) and download-artifact@v4→v7 (2 sites) across ci.yml + release.yml; both CI-green in run 25970222837; BUILD-ACCEL-05 closed and 5-phase build-accel cluster complete**

## Performance

- **Duration:** ~45 min (dominated by CI watch + parallel-session concurrency cancellations)
- **Started:** 2026-05-16T18:44Z
- **Completed:** 2026-05-16T19:30Z
- **Tasks:** 3
- **Files modified:** 2 workflow files + MILESTONE-CONTEXT.md + REQUIREMENTS.md + STATE.md + ROADMAP.md

## Accomplishments

- Bumped `actions/upload-artifact@v4 → v6` at 10 sites in ci.yml and 2 sites in release.yml (12 total)
- Bumped `actions/download-artifact@v4 → v7` at 1 site in ci.yml and 1 site in release.yml (2 total)
- Validated both bumps via CI run 25970222837: green on macOS-arm64, linux-x64, windows-x64, linux-arm64, lint, and coverage
- Confirmed `gh run download` spot-check: all 8 artifacts (4 ninja-logs, 2 test-results, coverage-html, coverage-xml, spike-smoke-log) downloadable post-bump
- Marked BUILD-ACCEL-05 complete in REQUIREMENTS.md (L99 checkbox + L212 status table)
- Annotated MILESTONE-CONTEXT.md Phase 999.20 deferral entry (L56) as Resolved with both commit SHAs

## Pre-bump vs Post-bump Inventory

| Workflow | Action | Sites | Pre-bump | Post-bump |
|----------|--------|-------|----------|-----------|
| ci.yml | upload-artifact | 10 | @v4 (L261,305,438,469,619,686,725,758,904,912) | @v6 |
| release.yml | upload-artifact | 2 | @v4 (L126,343) | @v6 |
| ci.yml | download-artifact | 1 | @v4 (L944) | @v7 |
| release.yml | download-artifact | 1 | @v4 (L370) | @v7 |

**Total:** 14 pins bumped (12 upload + 2 download)

## Artifact Name Collision Pre-flight (v6 immutability)

Scanned all artifact `name:` fields post-edit for bare names without unique suffix:

| Artifact name | Unique suffix | Risk | Decision |
|---------------|---------------|------|----------|
| `ninja-log-${{ matrix.os }}-${{ github.sha }}` | sha + os | None | Safe |
| `ball-tests-macos-${{ github.run_id }}` | run_id | None | Safe |
| `ball-tests-linux-${{ github.run_id }}` | run_id | None | Safe |
| `ninja-log-windows-x64-${{ github.sha }}` | sha | None | Safe |
| `spike-smoke-log-glcore-macos` | **None (bare)** | Low | Guarded by `matrix.os == 'macos-arm64'` — single upload per run |
| `render-smoke-${{ matrix.os }}` | os | None | Safe |
| `openbabel-smoke-${{ matrix.os }}` | os | None | Safe |
| `coverage-html-${{ github.run_id }}` | run_id | None | Safe |
| `coverage-xml-${{ github.run_id }}` | run_id | None | Safe |
| `ball-tests-macos-release-...-${{ github.run_id }}` | run_id | None | Safe |
| `${{ steps.resolve.outputs.asset }}` | dynamic | None | Safe |

**Result:** No `overwrite: true` additions needed. The single bare name `spike-smoke-log-glcore-macos` is protected by a matrix gate that ensures exactly one upload per run.

## Task Commits

Each task was committed atomically:

1. **Task 1: Bump upload-artifact@v4 → v6 (12 sites)** - `1d3da02` (ci)
2. **Task 2: Bump download-artifact@v4 → v7 (2 sites)** - `3cb03c8` (ci)
3. **Task 3: Mark MILESTONE-CONTEXT.md deferral resolved** - `3ddadb3` (docs)

**Plan metadata:** `fbbd5fc` (docs: SUMMARY + STATE + ROADMAP + REQUIREMENTS)

## CI Validation

### Task 1 (upload@v6) — run 25970222837

Task 1's own CI run (25969884734) was cancelled by a parallel-session push arriving within 1 minute. The subsequent combined run (25970222837) run against commit `cb392cc` (parallel session's "suppress GCC false-positive" on top of both 999.20 commits) validated both bumps:

- **Run URL:** https://github.com/BALL-Project/ball/actions/runs/25970222837
- **Status:** success (all 6 jobs green)
- **Jobs:** build (macos-arm64), build (linux-x64), build (windows-x64), build (linux-arm64), lint, coverage
- **Key artifact steps verified:** Upload .ninja_log artifact (macos-arm64), Upload .ninja_log artifact (linux-x64), Upload .ninja_log artifact (windows-x64), Upload .ninja_log artifact (linux-arm64), Upload test results (macOS), Upload test results (Linux), Upload spike smoke log — all completed successfully with upload-artifact@v6

### Task 2 (download@v7) — same run 25970222837

The download-artifact@v7 step in test-report job (Download all test result artifacts) completed successfully in the same run.

### `gh run download` spot-check

```
gh run download 25970222837 --dir /tmp/artifact-spot-check-all/
```

Downloaded artifacts confirmed:
- `ball-tests-linux-25970222837/` (test results via download-artifact@v7)
- `ball-tests-macos-25970222837/`
- `coverage-html-25970222837/`
- `coverage-xml-25970222837/`
- `ninja-log-linux-arm64-cb392cc4bc2a0e999f1c951f632144211bc85cd8/`
- `ninja-log-linux-x64-cb392cc4bc2a0e999f1c951f632144211bc85cd8/`
- `ninja-log-macos-arm64-cb392cc4bc2a0e999f1c951f632144211bc85cd8/`
- `ninja-log-windows-x64-cb392cc4bc2a0e999f1c951f632144211bc85cd8/`
- `spike-smoke-log-glcore-macos/`

All 9 artifacts downloaded without error. Consumer path (download-artifact@v7 pulling from upload-artifact@v6 producers) confirmed intact.

## 999.19 Coexistence Note

Phase 999.19 landed **before** Phase 999.20 (as expected). Its 3 new ninja-log upload-artifact steps (ci.yml L261, L438, L619 — for macos-arm64, linux-x64, windows-x64; later linux-arm64 added at L904) shipped on @v4 and were swept to @v6 in Task 1's single coherent commit. This is the expected order: 999.20 sweeps everything in a single commit rather than requiring 999.19 to anticipate v6.

## Files Created/Modified

- `.github/workflows/ci.yml` — 10 upload-artifact@v4 → @v6, 1 download-artifact@v4 → @v7
- `.github/workflows/release.yml` — 2 upload-artifact@v4 → @v6, 1 download-artifact@v4 → @v7
- `.planning/MILESTONE-CONTEXT.md` — Phase 999.20 deferral entry annotated as Resolved (2026-05-16) with both commit SHAs
- `.planning/REQUIREMENTS.md` — BUILD-ACCEL-05 marked [x] complete at L99 + status table updated at L212
- `.planning/STATE.md` — Phase 999.20 marked complete; progress counts updated
- `.planning/ROADMAP.md` — Phase 999.20 status updated to COMPLETE with result summary

## Decisions Made

- **Two-commit shape preserved** even though parallel-session concurrency cancelled both individual CI runs; the combined run on the next parallel-session commit validated both bumps together. The two-commit shape (upload first, download second) is correct for bisect-friendliness and was not collapsed.
- **No `overwrite: true` additions** — all artifact names had unique suffixes or were guarded by matrix conditions; collision pre-flight found no at-risk sites.
- **No amendment to Task 1 commit needed** — no collision errors surfaced in the CI run (the exception path in the plan was not triggered).

## Deviations from Plan

### Parallel-session CI cancellations (not a deviation — anticipated)

The execution guidance note said: "If a parallel-session commit lands mid-execution and creates a merge conflict, surface it in SUMMARY.md." No merge conflicts occurred, but parallel-session push activity caused concurrency-group cancellations of both the Task 1 (25969884734) and Task 2 (25970097575) individual CI runs. Both commits were validated by the combined run 25970222837 which ran on the subsequent parallel-session commit and completed green.

This is the expected behavior of `concurrency: group: ci-${{ github.ref }}, cancel-in-progress: true` in a multi-session environment. Not a deviation.

**Total deviations:** None — plan executed exactly as written. The upload@v6 and download@v7 bumps landed in two separate commits as specified. The MILESTONE-CONTEXT.md annotation format matched the plan spec.

## Issues Encountered

- **Parallel-session CI cancellations:** Multiple warning-fix commits from a parallel 999.22a mechanical-fix session arrived ~1 minute after each of my CI runs, triggering concurrency-group cancellations. Resolved by waiting for the parallel session to pause, then watching a later combined run that included both my commits.
- **MILESTONE-CONTEXT.md line reference mismatch:** Plan referenced ~L229 but the actual file is 142 lines; Phase 999.20 entry was at L56 within the build-acceleration cluster section. Annotated the correct location.

## Next Phase Readiness

- **Phase 9 test-suite triage** is next. The build-accel cluster (999.16–999.20) is fully closed.
- **Phase 999.21** (DockResultFile dead-code cleanup) can proceed independently.
- **Phase 999.22** (warning census) is already actively producing commits in the parallel session.
- BUILD-ACCEL-05 complete. v1.6.2 release criteria item 1 (all 5 build-accel phases landed) is met.

## Self-Check: PASSED

- `1d3da02` exists: `git log --oneline --all | grep 1d3da02` returns the upload@v6 commit
- `3cb03c8` exists: `git log --oneline --all | grep 3cb03c8` returns the download@v7 commit
- `3ddadb3` exists: `git log --oneline --all | grep 3ddadb3` returns the MILESTONE-CONTEXT.md commit
- Zero upload-artifact@v4/v5 in ci.yml + release.yml confirmed
- 10 upload-artifact@v6 in ci.yml, 2 in release.yml confirmed
- 1 download-artifact@v7 in ci.yml, 1 in release.yml confirmed
- YAML valid on both workflow files (python3 yaml.safe_load passes)
- CI run 25970222837 green on all platforms
- `gh run download` spot-check: 9 artifacts downloaded successfully

---
*Phase: 999.20-action-artifact-pins*
*Completed: 2026-05-16*
