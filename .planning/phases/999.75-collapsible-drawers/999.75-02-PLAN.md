---
phase: 999.75-collapsible-drawers
plan: 02
type: execute
wave: 2
depends_on: ["999.75-01"]
files_modified:
  - include/BALL/VIEW/WIDGETS/bottomDrawer.h
  - source/VIEW/WIDGETS/bottomDrawer.C
autonomous: false
requirements: [DRAWER-02]

must_haves:
  truths:
    - "BottomDrawer's tab set is registered through a single generalized add-tab path, not two hardcoded blocks"
    - "Logs + FileObserver remain the live tabs with their UFG-17 double-attach behavior intact"
    - "A Python/PyBALL tab slot is wired but entirely inside #ifdef BALL_PYTHON_SUPPORT"
    - "With BALL_PYTHON_SUPPORT OFF (the normal case), no pyWidget/Python.h code is compiled or linked and the build stays green"
    - "Both drawers are visually confirmed working by a human (collapse/expand Inspector rail; bottom drawer shows Logs+Files)"
  artifacts:
    - path: "source/VIEW/WIDGETS/bottomDrawer.C"
      provides: "Generalized tab registration + #ifdef BALL_PYTHON_SUPPORT PyBALL tab slot"
      contains: "BALL_PYTHON_SUPPORT"
    - path: "include/BALL/VIEW/WIDGETS/bottomDrawer.h"
      provides: "Generalized add-tab helper declaration"
      min_lines: 100
  key_links:
    - from: "source/VIEW/WIDGETS/bottomDrawer.C"
      to: "pyWidget (PyWidget) under #ifdef"
      via: "compile-time guarded tab registration"
      pattern: "#ifdef BALL_PYTHON_SUPPORT"
    - from: "source/VIEW/WIDGETS/bottomDrawer.C buildBody"
      to: "tab_bar_ + stack_"
      via: "single addDrawerTab_ helper per tab"
      pattern: "addDrawerTab_"
---

<objective>
Generalize `BottomDrawer` so its tab set is extensible through one registration
path instead of two hardcoded LogView/FileObserver blocks, and add a
Python/PyBALL tab slot that is ENTIRELY compile-time guarded by
`#ifdef BALL_PYTHON_SUPPORT`. Logs + FileObserver stay as the live tabs now.

Implements DRAWER-02. Ends the phase at a combined visual UAT confirming BOTH
drawers (the Plan 01 Inspector right-drawer AND this bottom drawer).

Purpose: When `BALL_PYTHON_SUPPORT` is defined (the v2.1 PyBALL revival), a third
"Python" tab hosting `PyWidget` appears automatically with no further layout work.
This phase delivers the layout/slot only — it does NOT enable Python/SIP (parked
for v2.1; SIP 4.9 is unavailable so the flag is OFF in every build).

Output: A generalized BottomDrawer that registers tabs through one helper, with the
PyBALL tab wired but inert when the flag is off, plus a human-verified working state
for both drawers and a green build with Python OFF.

CRITICAL CONSTRAINT (locked): `BALL_PYTHON_SUPPORT` is `/* #undef */` in every
build's config.h (verified: build/ci-macos, build/04-verify*, etc.). `pyWidget.C`
includes `<Python.h>` and is already CMake-gated behind `IF(BALL_PYTHON_SUPPORT)`
in `source/VIEW/WIDGETS/sources.cmake:91`. Therefore the PyBALL tab's C++ code
(any `#include <BALL/VIEW/WIDGETS/pyWidget.h>` and any `PyWidget` reference) MUST
live entirely inside `#ifdef BALL_PYTHON_SUPPORT` so nothing Python is compiled or
linked when the flag is off. Do NOT attempt to enable Python/SIP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md

# The class to generalize (read in full):
@source/VIEW/WIDGETS/bottomDrawer.C
@include/BALL/VIEW/WIDGETS/bottomDrawer.h

# Plan 01 output (sibling drawer; do not edit here — informs the UAT):
@.planning/phases/999.75-collapsible-drawers/999.75-01-SUMMARY.md

<interfaces>
<!-- Confirmed anchors — no codebase exploration required. -->

BottomDrawer::buildBody() today (bottomDrawer.C:127-176) — the code to generalize:
  - tab_bar_ = new QTabBar(body_); addTab(tr("Log")); addTab(tr("Files"));
  - stack_  = new QStackedWidget(body_);
  - For LogView: if (log_view_ && log_view_->widget()) { stack_->addWidget(
      log_view_->widget()); log_view_->hide(); } else { placeholder QLabel }
  - For FileObserver: same shape with file_observer_.
  - connect(tab_bar_, &QTabBar::currentChanged, stack_, &QStackedWidget::setCurrentIndex);
  - layout->addWidget(tab_bar_); layout->addWidget(stack_, 1);
  IMPORTANT: the LogView/FileObserver path reparents their INNER widget()
  (`log_view_->widget()`) and hides the outer dock — this is the UFG-17 double-attach
  fix. The generalized helper MUST preserve "add a child widget + optionally hide an
  outer dock" so this behavior is unchanged for the two live tabs.

bottomDrawer.h members (lines 94-110): log_view_, file_observer_, title_bar_, chevron_,
  summary_label_, body_, tab_bar_, stack_, height_animation_, expanded_. Constructor takes
  (LogView*, FileObserver*, QWidget* parent). objectName "bottomDrawer".

PyWidget (the v2.1 target — DO NOT instantiate when flag is off):
  - source/VIEW/WIDGETS/pyWidget.C / include/BALL/VIEW/WIDGETS/pyWidget.h
  - pyWidget.C #includes <Python.h>; CMake-gated at sources.cmake:91
    `IF(BALL_PYTHON_SUPPORT) SET(SOURCES_LIST ${SOURCES_LIST} pyWidget.C hotkeyTable.C)`
  - class is BALL::VIEW::PyWidget (a GenericControl/DockWidget like LogView).

config.h flag: `#cmakedefine BALL_PYTHON_SUPPORT` (cmake/config.h.in:268); resolves to
  `/* #undef BALL_PYTHON_SUPPORT */` in all current builds. The C-preprocessor guard for an
  undefined #cmakedefine is `#ifdef BALL_PYTHON_SUPPORT`.

QT_NO_KEYWORDS project-wide: Q_EMIT / Q_SIGNALS / Q_SLOTS only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize BottomDrawer tab registration into a single helper</name>
  <files>include/BALL/VIEW/WIDGETS/bottomDrawer.h, source/VIEW/WIDGETS/bottomDrawer.C</files>
  <action>
Refactor `BottomDrawer::buildBody()` so all tabs are added through one private helper instead
of the two duplicated LogView/FileObserver blocks. Behavior for the two live tabs must be
byte-for-byte equivalent (including the UFG-17 inner-widget reparent + outer-dock hide).

In bottomDrawer.h:
- Declare a private helper, e.g.:
  `void addDrawerTab_(const QString& label, QWidget* content, QDockWidget* outerToHide);`
  Doc-comment it: adds a tab with `label`, reparents `content` into the stack, and if
  `outerToHide` is non-null calls `outerToHide->hide()` (the UFG-17 fix). A null `content`
  installs a "(unavailable)" placeholder QLabel. Keep all existing members.

In bottomDrawer.C buildBody():
- Implement `addDrawerTab_(label, content, outerToHide)`:
  `tab_bar_->addTab(label); if (content) { stack_->addWidget(content); if (outerToHide)
  outerToHide->hide(); } else { stack_->addWidget(new QLabel(tr("(unavailable)"), stack_)); }`
  Preserve the exact placeholder-vs-real branching the originals had.
- Replace the LogView block with:
  `addDrawerTab_(tr("Log"), (log_view_ ? log_view_->widget() : nullptr), log_view_);`
  and the FileObserver block with:
  `addDrawerTab_(tr("Files"), (file_observer_ ? file_observer_->widget() : nullptr),
  file_observer_);`
  (Pass the outer dock so it is hidden only when its inner widget was actually added —
  preserve the original "hide only on the real path" semantics. If a null inner widget should
  NOT hide the outer dock, guard the hide inside the content!=null branch, matching the
  original code exactly.)
- Keep the tab_bar_ documentMode, the currentChanged→setCurrentIndex connect, and the layout
  adds (tab_bar_ then stack_ with stretch 1) exactly as before, AFTER all tabs are registered.
- Do NOT change the title bar, animation, setExpanded/toggle, or objectName. This task is a
  pure structural refactor of buildBody — no behavior change for the live tabs.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && grep -q "addDrawerTab_" include/BALL/VIEW/WIDGETS/bottomDrawer.h && grep -q "addDrawerTab_(tr(\"Log\")" source/VIEW/WIDGETS/bottomDrawer.C && grep -q "addDrawerTab_(tr(\"Files\")" source/VIEW/WIDGETS/bottomDrawer.C && ! grep -qE '(^|[^A-Za-z_])(emit|signals|slots)([^A-Za-z_]|$)' source/VIEW/WIDGETS/bottomDrawer.C && echo OK</automated>
  </verify>
  <done>buildBody() registers Log + Files through addDrawerTab_; the UFG-17 inner-widget reparent + outer-dock hide is preserved; no other behavior changed; no lowercase Qt keywords.</done>
</task>

<task type="auto">
  <name>Task 2: Add the #ifdef BALL_PYTHON_SUPPORT PyBALL tab slot; verify build green with Python OFF</name>
  <files>source/VIEW/WIDGETS/bottomDrawer.C</files>
  <action>
Add a Python/PyBALL tab to BottomDrawer that is ENTIRELY compile-time guarded so nothing
Python compiles or links when `BALL_PYTHON_SUPPORT` is off (the normal case).

In bottomDrawer.C:
- Add a guarded include at the top, beside the other widget includes:
  `#ifdef BALL_PYTHON_SUPPORT`
  `# include <BALL/VIEW/WIDGETS/pyWidget.h>`
  `#endif`
  (config.h, which defines/undefs BALL_PYTHON_SUPPORT, is reached transitively via
  BALL/COMMON/global.h → BALL/CONFIG/config.h; confirm a config or global include is present
  before the guard — bottomDrawer.h already pulls BALL/COMMON/global.h, so the macro is in
  scope. If not visibly in scope, add `#include <BALL/CONFIG/config.h>` explicitly above the
  guard.)
- In buildBody(), AFTER the Log + Files tabs are registered and BEFORE the layout adds (so the
  Python tab appears as the third tab), insert:
  `#ifdef BALL_PYTHON_SUPPORT`
  ... construct a PyWidget (mirroring how LogView/FileObserver are constructed elsewhere — a
  PyWidget is a GenericControl/DockWidget; create it parented appropriately, reparent its
  inner widget()), then `addDrawerTab_(tr("Python"), py_widget_->widget(), py_widget_);`
  `#endif`
  The PyWidget pointer should be a member added ONLY inside the guard, OR a local — but since
  the drawer does not own LogView/FileObserver (they are passed in and not owned), prefer
  constructing PyWidget here as a child of `this`/mainframe-equivalent and storing it in a
  guarded member `#ifdef BALL_PYTHON_SUPPORT PyWidget* py_widget_; #endif` in the header, OR
  keep it local if no later reference is needed. Whichever you choose, EVERY reference to
  PyWidget / pyWidget.h MUST be inside `#ifdef BALL_PYTHON_SUPPORT` — there must be zero
  PyWidget tokens outside the guard.
- Add a clear comment block at the guard explaining: PyBALL tab is layout-ready for v2.1; it
  is inert when BALL_PYTHON_SUPPORT is OFF (SIP 4.9 unavailable); do not enable Python here.
- Do NOT modify sources.cmake — pyWidget.C is already gated there (line 91). When the flag is
  off, pyWidget.h is not included and PyWidget is never named, so no Python symbols are
  referenced.

Build VIEW + BALLView with Python OFF (the normal config — BALL_PYTHON_SUPPORT /* #undef */)
and confirm green and Python-free:
  cmake --build build/ci-macos --target VIEW BALLView
Then confirm no Python symbols leaked into the build: the build must succeed WITHOUT any
Python.h / libpython link errors (there is no Python in the ci-macos preset).
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && grep -q "#ifdef BALL_PYTHON_SUPPORT" source/VIEW/WIDGETS/bottomDrawer.C && grep -q "pyWidget.h" source/VIEW/WIDGETS/bottomDrawer.C && test $(grep -cv '^[[:space:]]*//' source/VIEW/WIDGETS/bottomDrawer.C | grep -c PyWidget 2>/dev/null || awk '!/^[[:space:]]*\/\//' source/VIEW/WIDGETS/bottomDrawer.C | grep -c PyWidget) -ge 1 && cmake --build build/ci-macos --target VIEW BALLView 2>&1 | tail -3</automated>
  </verify>
  <done>A third "Python" tab is registered via addDrawerTab_ entirely inside #ifdef BALL_PYTHON_SUPPORT; every PyWidget/pyWidget.h reference is inside the guard; VIEW + BALLView build green with Python OFF and link with zero Python symbols.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Combined visual UAT — both drawers + green Python-OFF build</name>
  <what-built>
DRAWER-01 (Plan 01): the Inspector is now a collapsible right-side drawer (InspectorDrawer)
that animates a ~24px rail to expanded and back, with persisted state and the inspectorDock
objectName preserved. DRAWER-02 (this plan): the BottomDrawer tab set is generalized through
addDrawerTab_ and has a #ifdef BALL_PYTHON_SUPPORT PyBALL tab slot (inert with Python OFF).
The ci-macos build (VIEW + BALLView) is green with BALL_PYTHON_SUPPORT off.
  </what-built>
  <how-to-verify>
1. Launch the freshly built BALLView (build/ci-macos product, e.g.
   `open build/ci-macos/BALLView.app` or run the binary with BALL_DATA_PATH set to ./data).
2. Inspector right-drawer (DRAWER-01):
   a. Confirm the Inspector appears on the RIGHT edge expanded (default).
   b. Click the chevron — confirm it animates collapsing to a thin (~24px) rail on the right
      edge, and the chevron flips direction (collapse-arrow ↔ expand-arrow).
   c. Click again — confirm it animates back to the expanded Inspector.
   d. Collapse it, quit BALLView, relaunch — confirm it reopens COLLAPSED (state persisted via
      [Inspector] expanded= in ~/.BALLView). Expand it, quit+relaunch — confirm expanded.
3. Bottom drawer (DRAWER-02):
   a. Confirm the bottom drawer still shows exactly two tabs: Logs and Files.
   b. Switch between them — confirm Log output and the FileObserver content both render (no
      duplicate floating "Logs" panel — the UFG-17 fix still holds).
   c. Confirm there is NO "Python" tab (BALL_PYTHON_SUPPORT is off — the slot is inert).
4. Build sanity: confirm the build you launched was produced with Python OFF
   (`grep BALL_PYTHON_SUPPORT build/ci-macos/include/BALL/CONFIG/config.h` shows
   `/* #undef BALL_PYTHON_SUPPORT */`) and VIEW + BALLView built green.
5. (Optional, if a WorkspaceManager preset is reachable from the UI) apply a workspace preset
   and confirm the Inspector drawer is still found/placed — its objectName stayed
   "inspectorDock".
  </how-to-verify>
  <resume-signal>Type "approved" once both drawers collapse/expand and persist correctly, the bottom drawer shows Logs+Files with no Python tab, and the build is confirmed green with Python OFF — or describe any issue (drawer animation, persistence, missing/extra tab, build failure).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BALL_PYTHON_SUPPORT compile flag → linker | Determines whether Python.h/libpython enters the build |
| Reparented inner widgets → QStackedWidget | LogView/FileObserver/(PyWidget) inner widgets are reparented; outer docks hidden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.75-04 | Tampering | Python tab code leaking outside the #ifdef | mitigate | Every PyWidget/pyWidget.h reference is inside `#ifdef BALL_PYTHON_SUPPORT`; Task 2 grep gate + a green Python-OFF link verify there are zero Python symbols when the flag is off. |
| T-999.75-05 | Denial of Service | UFG-17 double-attach regression in the generalized helper | mitigate | addDrawerTab_ preserves the exact inner-widget reparent + outer-dock hide; UAT step 3b checks for the duplicate floating Logs panel. |
| T-999.75-06 | Tampering | npm/pip/cargo installs | mitigate | N/A — no package-manager installs in this phase (pure C++/Qt edits; Python deliberately NOT enabled). |
</threat_model>

<verification>
- VIEW + BALLView build green with BALL_PYTHON_SUPPORT OFF; no Python symbols linked.
- Every PyWidget / pyWidget.h reference is inside `#ifdef BALL_PYTHON_SUPPORT`.
- Logs + FileObserver remain the two live tabs; UFG-17 behavior intact.
- Human UAT (Task 3) confirms both drawers collapse/expand + persist and the bottom drawer is
  Python-tab-free with the flag off.
</verification>

<success_criteria>
- DRAWER-02 implemented: generalized, swappable tab set + #ifdef-guarded PyBALL slot.
- Logs + FileObserver stay live; no PyBALL/Python compiled when the flag is off.
- Build green on macOS with Python OFF; human-verified working state for BOTH drawers.
</success_criteria>

<output>
Create `.planning/phases/999.75-collapsible-drawers/999.75-02-SUMMARY.md` when done.
</output>
