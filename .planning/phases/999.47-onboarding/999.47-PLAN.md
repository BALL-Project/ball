---
phase: 999.47
plan: 01
name: onboarding
type: feature
autonomous: true
wave: 4
depends_on: [999.40, 999.41, 999.42, 999.43, 999.44, 999.45, 999.46]
requirements: []
---

# Phase 999.47: BALLView Refresh — Onboarding (Handover Phase 7)

## Objective

Replace the standalone *Demo / Tutorial* dialog + `Welcome.rtf` with an in-product WelcomeScreen panel surfaced when no document is open. Refresh the Help viewer to use Markdown via `QTextBrowser::setMarkdown(...)`. Bundle 6 curated sample structures + thumbnails. Add a `ballview://` URL scheme handler. Migrate the 6 tutorial slides to Markdown files. Delete the legacy `Welcome.rtf`.

All new symbols gated behind `BALL_UI_V2`. CI matrix `ui_v2: [ON, OFF]` × {macos, linux, windows} must remain green per Handover §10 gate 6.

## Context

@.planning/ROADMAP.md  (§999.47)
@/Users/kohlbach/Claude/BALL/Claude Design Handover/revitalization/07-phase-onboarding.md

## Tasks

1. **WelcomeScreen widget** (skeleton + 3 action buttons + recent + samples + What's-new + footer toggle) — single TU `source/VIEW/WIDGETS/welcomeScreen.{h,C}`; mirror commandPalette whole-TU gating. Wired into mainframe via `setCentralWidget(welcome_)` when `composite_manager_.getNumberOfComposites() == 0`.
2. **6 curated sample structures + thumbnails** under `data/BALLView/samples/` — `1bna.pdb`, `1ubq.pdb`, `1stp.pdb`, `1ymg.pdb`, `caffeine.mol2`, `dialanine.pdb`. Thumbnails as 320×200 PNG (single-density placeholder where high-res not readily generated; the @2x pipeline is built in 999.48). `samples/README.md` records sources + license notice (RCSB-PDB public).
3. **What's new card** — version-bump detection compares current `BALL_PACKAGE_VERSION` against `QSettings` value; auto-dismiss timer 2 minutes; content from `data/BALLView/help/whatsnew/1.7.md`.
4. **HelpViewer Markdown rewrite** — `QTextBrowser::setMarkdown(...)` based new path; keep legacy `MyTextBrowser` for OFF cell; intercept `ballview://` scheme via `setSource` override that dispatches to `CommandRegistry::run(...)`.
5. **ballview:// URL scheme registration** — macOS `LSEnvironment` / `CFBundleURLSchemes` plist entry + in-app `QDesktopServices::setUrlHandler("ballview", ...)`. Linux .desktop association + Windows registry deferred to follow-up if time-bounded.
6. **Tutorial Markdown migration** — extract demoTutorialDialog content to 6 .md files in `data/BALLView/help/tutorial/` with frontmatter (title/order/prev/next). Tutorial dialog stays in tree (delete deferred to 999.48 per its scope statement listing demoTutorialDialog.* in legacy file roster).
7. **Delete Welcome.rtf** — `source/APPLICATIONS/BALLVIEW/Welcome.rtf` removed; no install rules reference it (legacy artifact only used by long-removed splash code path).
8. **SUMMARY + ROADMAP flip** — Phase 999.47 marked COMPLETE; STATE position advances.

## Verification

- libBALL + libVIEW + BALLView build green on macOS-arm64 with `BALL_UI_V2={ON,OFF}`.
- CI matrix (4 OS × 2 ui_v2) green at HEAD.
- `Welcome.rtf` absent from tree.
- `data/BALLView/samples/` populated with 6 structures + thumbnails + README.
- `data/BALLView/help/tutorial/*.md` populated with 6 frontmatter-headed files.
- `data/BALLView/help/whatsnew/1.7.md` populated.

## Success Criteria

- WelcomeScreen present in BALL_UI_V2 ON cell, behaviorally inert in OFF cell.
- All install rules continue to copy via the existing `INSTALL(DIRECTORY data/ ...)` blanket rule.
- 999.48 dispatch unblocked (flag flip will encounter no symbol-ifdef mismatches because new TUs follow commandPalette pattern).
