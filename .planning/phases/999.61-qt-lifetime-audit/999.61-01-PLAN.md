---
phase: 999.61-qt-lifetime-audit
plan: 01
type: execute
release: v1.7.4
wave: A
v1_7_x_id: v1.7.x-26
depends_on: []
files_modified:
  - .planning/codebase/QT-LIFETIME.md
  - source/VIEW/WIDGETS/inspector/inspectorBody.C
  - source/VIEW/KERNEL/workspaceManager.C
  - source/VIEW/WIDGETS/scene.C
  - source/VIEW/WIDGETS/inspector/inspectorSection.C
  - cmake/scripts/check-deletelater-discipline.sh
  - .github/workflows/ci.yml
autonomous: false
requirements:
  - ARCH-CONTRACT-03
  - LIFETIME-DOC-01

must_haves:
  truths:
    - ".planning/codebase/QT-LIFETIME.md exists and is canonical (5 rules + 4 case studies + cookbook)"
    - "The 6 known sites flagged by UFG-30/32/18/26 are migrated to QPointer + parent-rooted ownership"
    - "A CI lint job rejects deleteLater() inside QObject destructors"
    - "Cross-thread signal/slot uses Qt::QueuedConnection explicitly; same-thread defaults to direct"
---

# `999.61` — Qt-lifetime audit + conventions doc

**v1.7.x-26** · Wave A · independent · ~2–3 days

## Goal

Codify the QObject-lifetime conventions that emerged from the
UFG-30/32/18/26 cycle into a canonical doc, audit the 6 known sites,
and ship a small CI lint that catches the most common foot-gun
(`deleteLater()` in destructors).

## Why now

C++ + Qt fails through lifetime bugs more than through algorithmic
ones. Five v1.7.0 RC crashes traced to dtor-order / dangling-pointer /
GL-context / event-loop-ownership issues. The fixes were correct but
folkloric — a new contributor would step on the same rakes.

The conventions doc is the write-once artifact that prevents that.

## Scope

**In:**

1. **`.planning/codebase/QT-LIFETIME.md`** — canonical 5-rule + 4-case-study + cookbook doc.  ~400 LOC.
2. **Targeted audit** of 6 sites already flagged:
   - `WorkspaceStatusLabel::~WorkspaceStatusLabel` (UFG-30)
   - `InspectorBody::clearSections_` (UFG-32)
   - `GLRenderWindow::initializeGL` ordering (UFG-18)
   - `Scene::paintEvent` `QPainter` off-cycle (UFG-26)
   - `WelcomeScreen` swap-central-widget (UFG-08)
   - `MolecularControl::setSelection_` paint-storm (v1.7.x-14)
3. **`QPointer<T>` adoption** wherever a Controller / Section caches a cross-owner reference (~12 sites).
4. **CI lint job** `lifetime-discipline` running `cmake/scripts/check-deletelater-discipline.sh` — rejects `deleteLater()` inside `~AnyClass()`.

**Out:**
- Full clang-tidy custom check (`ball-no-raw-deletelater`) — write the docs first, the custom check is v1.7.5.
- ConnectionObject lifetime overhaul — v2.0 (bus replacement).

## The 5 rules (preview — full bodies in the doc)

1. **Owner-rooted parentage.** Every `QObject*` you `new` has a parent OR a documented `deleteLater()` reason.
2. **`QPointer<T>` for cross-owner references.** Dtor-order races become null checks.
3. **Direct connections by default.** Queued is for cross-thread only; explicit when chosen.
4. **GL context ownership stays with `QOpenGLWidget`.** Controllers/sections never touch the context.
5. **No `deleteLater()` inside destructors.** Qt does the parent walk for us.

## Audit cookbook (excerpt)

For each flagged site:

1. Identify the lifetime owner (the parent QObject).
2. Replace raw pointers with `QPointer<T>` where the referent's lifetime can run shorter than the holder.
3. If a `deleteLater()` is in a dtor — restructure (Qt's parent walk does it; if you need explicit destruction order, do it BEFORE the parent dtor runs).
4. Add a brief in-source comment naming the rule that motivated the fix.

## Files touched

**New:**
- `.planning/codebase/QT-LIFETIME.md` (~400 LOC)
- `cmake/scripts/check-deletelater-discipline.sh` (~30 LOC)
- 1 CI job in `ci.yml`

**Edited:** 6 audit sites + ~12 `QPointer<T>` adoption sites (small diffs, in-source comments).

## Acceptance criteria

- [ ] `QT-LIFETIME.md` exists and is end-to-end coherent (5 rules, 4 case studies, cookbook).
- [ ] All 6 flagged sites updated; commit messages reference the rule.
- [ ] CI lint job appears in matrix, blocking; grep regex hits no current sites.
- [ ] BALLView smoke tests still pass after the audit (no behavior change intended).

## Risks

- **`QPointer<T>` overhead.** Negligible (atomic guarded ptr); no measurable hit in BALLView's hot paths.
- **Audit finds a 7th site mid-phase.** Add it; document it; fix it. Don't expand scope to a full sweep — that's v1.7.5.

## Hand-off pointer

Open `QT-LIFETIME.md` (writing it is the first task). The 5 rules go
in there before any code moves.

---

*Phase compiled for v1.7.4. Independent of 999.59 — can run in parallel once a 999.59 review pass clears the Controller dtors.*
