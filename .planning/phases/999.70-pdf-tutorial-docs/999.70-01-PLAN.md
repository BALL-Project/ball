---
phase: 999.70-pdf-tutorial-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - doc/TUTORIAL/tutorial.tex
  - doc/TUTORIAL/introduction.tex
  - doc/TUTORIAL/example1.tex
  - doc/TUTORIAL/faq.tex
  - doc/TUTORIAL/python.tex
  - doc/TUTORIAL/tutorial.bib
autonomous: true
requirements: [TUT-01]
must_haves:
  truths:
    - "The tutorial PDF build (doc_tutorial target) succeeds with pdflatex on the refreshed sources"
    - "All URLs in the tutorial resolve to live destinations (no dead http://www.ball-project.org/Downloads/... or stale wiki paths)"
    - "The author/developer list and the proof-reader acknowledgments reflect the current project, not the ~2010 roster only"
    - "The tutorial no longer points users at removed/outdated build instructions; it directs them to the canonical online build docs (README / BUILD-*.md / wiki) per the #560 resolution"
    - "The publications / citation list matches the current canonical BALL citation (BMC Bioinformatics 11:531, 2010) used in README"
  artifacts:
    - path: "doc/TUTORIAL/tutorial.tex"
      provides: "Title page release tag, author list, acknowledgments"
      contains: "author"
    - path: "doc/TUTORIAL/introduction.tex"
      provides: "Overview + project URLs + documentation pointers"
      contains: "ball-project.org"
  key_links:
    - from: "doc/TUTORIAL/tutorial.tex"
      to: "doc/tutorial.pdf"
      via: "doc_tutorial CMake target (pdflatex/bibtex/makeindex)"
      pattern: "doc_tutorial"
---

<objective>
Refresh the autonomously-fixable content of the BALL PDF tutorial (LaTeX under `doc/TUTORIAL/`) to the current v1.7.x stack: dead/stale URLs, the developer/author list, the proof-reader acknowledgments, the release tag, the publications/citation list, and the build-instruction pointers. This is the text/URL/build-flow portion of TUT-01 (#560) — everything that does NOT require a running GUI.

Per the #560 resolution comment (commit a9daa6f), the *vastly* outdated inline build instructions were already removed and build instructions are now kept online. This plan does NOT re-add build steps; it ensures the tutorial points users to the canonical online build docs and that the remaining prose/URLs/lists are current.

Screenshot recapture is explicitly OUT OF SCOPE here (handled in 999.70-03, which is human-gated).

Purpose: The tutorial is the first-contact document for new BALL/BALLView users; stale URLs and an outdated roster make it look abandoned and send users to dead pages.
Output: Updated `.tex` sources; `doc_tutorial` still builds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Canonical current build flow + download path + citation — the tutorial must agree with this
@README.md

# Tutorial build target (pdflatex -> bibtex -> makeindex -> pdflatex x2 -> copy to doc/tutorial.pdf)
@cmake/BALLDoc.cmake

<facts>
Current state confirmed at HEAD (branch v1.8):
- tutorial.tex title page: `\date{Release 1.7 \today}`; author line is the legacy 2010 roster
  (Kohlbacher, Klein, Kerzmann, Moll, Kämper, Dehof, Hildebrandt). Acknowledgments thank
  Gärtner, Glaab, Hedderich, Weggler, Stöckel.
- URLs present in the tutorial:
    * http://www.ball-project.org/Downloads/Examples/pdb4pti.ent  (example1.tex — likely dead path)
    * http://www.ball-project.org  and  http://www.ball-project.org/Documentation  (introduction.tex)
    * https://github.com/BALL-Project/ball/  (live)
    * https://github.com/BALL-Project/ball/wiki/FAQ  (tutorial.tex FAQ chapter intro)
- README canonical references (the tutorial should agree, not contradict):
    * Releases / download:  https://github.com/BALL-Project/ball/releases/latest
    * Project website:      https://ball-project.org  (https, no http://www.)
    * Build docs:           BUILD-macos.md, BUILD-linux.md, wiki DevelopmentEnvironment
    * Canonical citation:   Hildebrandt et al., "BALL — Biochemical Algorithms Library 1.3",
                            BMC Bioinformatics 11:531 (2010), DOI 10.1186/1471-2105-11-531
- python.tex already references the current `ci-*` CMake presets and BALL_PYTHON_SUPPORT=OFF
  (it has had recent maintenance — do NOT regress it; only touch it if a URL/version string is stale).
- doc_tutorial target requires PDFLATEX_COMPILER, BIBTEX_COMPILER, MAKEINDEX_COMPILER (see BALLDoc.cmake).
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh URLs, release tag, and build-doc pointers</name>
  <files>doc/TUTORIAL/tutorial.tex, doc/TUTORIAL/introduction.tex, doc/TUTORIAL/example1.tex, doc/TUTORIAL/python.tex</files>
  <action>
    Bring every URL and version string in line with README.md (the canonical source). Specifically:
    - tutorial.tex: change the release tag from "Release 1.7" to the current milestone string. Read the
      latest tag/release from README badge context or `git describe --tags --abbrev=0` and use the v1.x
      line label the project ships (e.g. "Release 1.8"). Do not invent a version not reflected anywhere.
    - introduction.tex: replace bare `http://www.ball-project.org` and
      `http://www.ball-project.org/Documentation` with the https forms README uses
      (`https://ball-project.org`); replace the "latest version / downloads from our website" pointer so it
      directs users to the GitHub Releases page (`https://github.com/BALL-Project/ball/releases/latest`),
      matching the README Download section. Keep the GitHub repo + wiki links (already live).
    - example1.tex: the `http://www.ball-project.org/Downloads/Examples/pdb4pti.ent` reference in prose is a
      dead path. Verify with `curl -sI` (see verify). If dead, change the prose to instruct fetching 4PTI
      from the RCSB PDB (https://www.rcsb.org/structure/4PTI) or note the example data ships with BALL under
      `data/`; do NOT fabricate a new ball-project.org download URL.
    - For build instructions: the tutorial must NOT contain step-by-step build commands (those were removed
      per #560 and live online). If any residual "to build BALL, run ./configure / make" prose remains in any
      chapter, replace it with a one-line pointer to the README "Build from source" section and BUILD-macos.md /
      BUILD-linux.md. Grep first; if none exists, no change needed beyond confirming.
    - python.tex: only adjust if a URL or version string is stale. Do NOT regress the existing `ci-*` preset /
      BALL_PYTHON_SUPPORT=OFF content.
    Conventions: LaTeX only here; preserve existing `\URL{}` / `\FAQURL{}` macro usage (see macros.tex) rather
    than hardcoding raw \href.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && for u in $(grep -rhoE 'https?://[^ }{",]+' doc/TUTORIAL/*.tex | sed 's/[).]*$//' | sort -u); do code=$(curl -s -o /dev/null -w '%{http_code}' -I --max-time 15 -L "$u" 2>/dev/null || echo ERR); echo "$code $u"; done | grep -vE '^(200|301|302|403) ' ; test $(grep -rhoE 'https?://[^ }{",]+' doc/TUTORIAL/*.tex | grep -c 'http://www.ball-project.org/Downloads') -eq 0</automated>
  </verify>
  <done>No tutorial URL returns a 4xx (except 403 anti-bot) or 5xx; the dead `ball-project.org/Downloads/Examples` path is gone; release tag and build-doc pointers agree with README.</done>
</task>

<task type="auto">
  <name>Task 2: Update developer/author list, acknowledgments, and citation</name>
  <files>doc/TUTORIAL/tutorial.tex, doc/TUTORIAL/faq.tex, doc/TUTORIAL/tutorial.bib</files>
  <action>
    Update the project roster and publications to match the canonical BALL citation in README:
    - tutorial.tex `\author{}` block: keep the historical authors but ensure the list reflects the current
      maintainer set. Cross-reference the README citation author list (Hildebrandt, Dehof, Rurainski,
      Bertsch, Schumann, Toussaint, Moll, Stöckel, Nickels, Müller, Lenhof, Kohlbacher) and the current
      v1.8 modernization. Do NOT remove existing named authors; ADD the canonical-citation contributors that
      are missing so the tutorial no longer presents only the ~2010 subset. If unsure who the *current*
      maintainers are, retain the existing list plus the README citation authors and leave a single
      `% TODO(999.70): confirm current maintainer roster` marker — do NOT silently drop names.
    - Acknowledgments section: leave the proof-reader thanks intact; this is historical credit, not a roster.
    - Citation: the tutorial's bibliography (tutorial.bib) cites BKL99a/Koh2001 era papers. Ensure the
      canonical citation entry (Hildebrandt et al., BMC Bioinformatics 11:531, 2010, DOI
      10.1186/1471-2105-11-531) exists in tutorial.bib and is referenced in introduction.tex's "please cite
      it as follows" block (currently it cites only the Kohlbacher/Lenhof 2000 Bioinformatics paper). Add the
      BMC 2010 citation as the primary "please cite" reference, keeping the 2000 paper as secondary.
    Conventions: BibTeX entry keys UPPER/MixedCase consistent with existing keys; no raw accented chars —
    use LaTeX escapes (`\"{a}`) as the file already does.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && grep -q "1471-2105-11-531\|BMC Bioinformatics\|11:531\|11(1):531" doc/TUTORIAL/tutorial.bib && grep -q "Hildebrandt" doc/TUTORIAL/tutorial.tex</automated>
  </verify>
  <done>tutorial.bib contains the canonical BMC 2010 citation; the "please cite" block references it; the author block includes the current-citation roster (no silently dropped names).</done>
</task>

<task type="auto">
  <name>Task 3: Confirm the tutorial still builds (pdflatex toolchain)</name>
  <files>doc/TUTORIAL/tutorial.tex</files>
  <action>
    Verify the refreshed sources still compile to PDF using the same toolchain `doc_tutorial` invokes
    (pdflatex -> bibtex -> makeindex -> pdflatex x2). Do NOT modify content in this task — it is a build gate
    that proves Tasks 1-2 did not introduce a LaTeX error (unbalanced braces, undefined macro from a bad URL
    edit, malformed bib entry). If `pdflatex` / `bibtex` / `makeindex` are not installed on this machine,
    record that the local toolchain is unavailable and that the build is verified in CI / by the 999.70-03
    UAT render instead — do NOT mark the task failed for a missing local LaTeX install; the source-level
    `chktex`/balanced-brace check below is the fallback gate.
    Note: this is the doc/tutorial.pdf that the screenshot recapture in 999.70-03 will regenerate after the
    new figures are dropped in — so a clean text-only build here de-risks that later step.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball/doc/TUTORIAL && if command -v pdflatex >/dev/null 2>&1; then pdflatex -interaction=nonstopmode -halt-on-error tutorial.tex >/tmp/tut_build.log 2>&1 && (command -v bibtex >/dev/null && bibtex tutorial >>/tmp/tut_build.log 2>&1; true) && pdflatex -interaction=nonstopmode -halt-on-error tutorial.tex >>/tmp/tut_build.log 2>&1 && test -f tutorial.pdf; else echo "pdflatex absent — fallback brace check"; python3 -c "import sys; s=open('tutorial.tex').read(); sys.exit(0 if s.count('{')==s.count('}') else 1)"; fi</automated>
  </verify>
  <done>Either tutorial.pdf is produced by the pdflatex toolchain, or (no local LaTeX) the source passes the balanced-brace sanity check and the build is deferred to CI / the 999.70-03 render.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tutorial → external URLs | Tutorial directs users to web destinations; dead/hijacked links could mislead users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.70-01 | Tampering/Info | tutorial outbound URLs | mitigate | Task 1 curl-verifies every URL resolves; only project-owned (ball-project.org, github.com/BALL-Project, rcsb.org) destinations are used — no shorteners or third-party mirrors |
| T-999.70-02 | Repudiation | citation/author accuracy | accept | Roster TODO marker surfaces uncertainty rather than asserting a wrong maintainer list; corrected at UAT if needed |

No package installs in this plan — package-legitimacy gate not applicable.
</threat_model>

<verification>
- All tutorial URLs resolve (no 4xx/5xx except anti-bot 403).
- No reference to the dead `ball-project.org/Downloads/Examples` path.
- Canonical BMC 2010 citation present and referenced.
- Tutorial compiles (pdflatex) or passes the source sanity gate.
</verification>

<success_criteria>
- TUT-01 text/URL/build-flow/developer/publication portion complete (screenshots deferred to 999.70-03).
- `doc_tutorial` toolchain still produces a PDF from the edited sources.
</success_criteria>

<output>
Create `.planning/phases/999.70-pdf-tutorial-docs/999.70-01-SUMMARY.md` when done.
Record: which URLs changed, the chosen release tag, roster decisions (and any TODO marker left), and
whether the local pdflatex build ran or was deferred to CI/UAT.
</output>
