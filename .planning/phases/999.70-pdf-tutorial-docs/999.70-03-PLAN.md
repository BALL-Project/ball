---
phase: 999.70-pdf-tutorial-docs
plan: 03
type: execute
wave: 2
depends_on: ["999.70-01", "999.70-02"]
files_modified:
  - doc/TUTORIAL/tut1_screenshot.png
  - doc/TUTORIAL/tut1_screenshot.pdf
  - doc/TUTORIAL/example1.tex
autonomous: false
requirements: [TUT-01]
user_setup:
  - service: "running BALLView build"
    why: "Screenshots of the refreshed BALLView UI require a running GUI app — cannot be auto-generated headlessly with fidelity"
    dashboard_config:
      - task: "Build and launch BALLView from the v1.8 branch (see BUILD-macos.md / BUILD-linux.md), load the example structure, and capture the screenshot(s) the tutorial references"
        location: "Local BALLView app"
must_haves:
  truths:
    - "The tutorial's screenshot figure (tut1_screenshot) shows the current v1.7.x/v1.8 BALLView UI — the Project dock, Inspector drawers, and bottom drawer — not the legacy pre-modernization GUI"
    - "The rendered tutorial PDF is reviewed end-to-end by a human and confirmed current (URLs, roster, screenshots, build pointers)"
    - "The figure caption/prose in example1.tex matches what the new screenshot actually shows"
  artifacts:
    - path: "doc/TUTORIAL/tut1_screenshot.pdf"
      provides: "Current BALLView screenshot embedded in the tutorial"
  key_links:
    - from: "doc/TUTORIAL/example1.tex"
      to: "doc/TUTORIAL/tut1_screenshot"
      via: "includegraphics tut1_screenshot"
      pattern: "includegraphics.*tut1_screenshot"
---

<objective>
Complete the screenshot portion of TUT-01 (#560) — the part that genuinely requires a running GUI and a human
to capture — then close the phase with a human review of the fully rendered tutorial PDF.

This plan is NOT autonomous. The current screenshot (`tut1_screenshot`) predates the BALLView modernization
(no Project dock / Inspector drawers / bottom drawer). Faithfully recapturing it needs a built, running
BALLView on the v1.8 branch and a human to drive the GUI and take the screenshot. We do NOT promise
auto-generated screenshots.

After the new image is dropped in, the tutorial PDF is rebuilt and a human reviews the rendered PDF to confirm
the whole document (the text/URL/roster fixes from 999.70-01, the screenshots here) is current and reads well.

Purpose: A screenshot of the old UI is the most visibly stale part of the tutorial; getting it right is the
payoff of the phase. The PDF review is the phase's exit gate.
Output: Updated screenshot asset(s) + a human-approved rendered tutorial.pdf.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Where the screenshot figure is included + its caption/prose
@doc/TUTORIAL/example1.tex
# Build target that regenerates the PDF
@cmake/BALLDoc.cmake
# Describes the current BALLView UI the screenshot must show (dock/inspector/drawer)
@data/BALLView/help/tutorial/01-welcome.md

<facts>
- example1.tex includes `\includegraphics[width=\textwidth]{tut1_screenshot}` (figure ~line 206) and refers to
  it in prose (~lines 251, 289) as "a screenshot of BALLView" loading 4PTI.
- On disk: doc/TUTORIAL/tut1_screenshot.eps and tut1_screenshot.pdf exist (legacy UI). LaTeX `\includegraphics`
  with no extension picks .pdf for pdflatex, so the NEW capture must be provided as a PDF-embeddable image
  (PNG is simplest; pdflatex accepts .png/.jpg/.pdf — keep the basename `tut1_screenshot`).
- The current UI (per the in-app help): centre 3D View, left Project dock (Structures/Representations/Datasets),
  right collapsible Inspector, bottom drawer (Logs + File observer), Cmd/Ctrl+K command palette.
- 999.70-01 already fixed text/URLs/roster; 999.70-02 added the Restore Defaults docs. This plan is last.
</facts>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    999.70-01 refreshed the tutorial text/URLs/roster; 999.70-02 documented Restore Defaults. The only
    remaining stale artifact is the BALLView screenshot, which shows the pre-modernization UI.
  </what-built>
  <how-to-verify>
    Capture a fresh screenshot of the current BALLView UI to replace `tut1_screenshot`:
    1. Build + launch BALLView from the v1.8 branch (BUILD-macos.md / BUILD-linux.md, or use an existing
       local build / a release artifact of the current line).
    2. Load the 4PTI / BPTI example structure the tutorial walks through (the file the example1.tex prose
       references). Apply a default representation so the molecule is visibly rendered.
    3. Arrange the window so the screenshot shows the modern layout: centre 3D View with the molecule, the
       left Project dock, and the right Inspector (expanded). A bottom-drawer glimpse is a bonus.
    4. Capture the window and save it as `doc/TUTORIAL/tut1_screenshot.png` (retire the legacy .eps/.pdf with
       the same basename, or save as .pdf — keep basename `tut1_screenshot`).
    5. Tell the executor the file is in place (and the format/extension used) so it can wire example1.tex.
  </how-to-verify>
  <resume-signal>Reply "screenshot captured at doc/TUTORIAL/tut1_screenshot.{ext}" or describe a blocker (e.g. no build available).</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Wire the new screenshot and reconcile the figure caption/prose</name>
  <files>doc/TUTORIAL/example1.tex, doc/TUTORIAL/tut1_screenshot.png, doc/TUTORIAL/tut1_screenshot.pdf</files>
  <action>
    With the new screenshot in place (from the checkpoint), ensure the tutorial references it correctly:
    - Confirm `\includegraphics{tut1_screenshot}` resolves to the new file. If the human saved a `.png` and the
      legacy `.pdf`/`.eps` with the same basename still exist, pdflatex may pick the wrong one — remove the
      stale legacy assets (`git rm doc/TUTORIAL/tut1_screenshot.eps doc/TUTORIAL/tut1_screenshot.pdf` if they
      are the old UI) so the new capture is the one embedded, OR set the explicit extension in
      `\includegraphics`. Prefer removing the legacy assets to avoid ambiguity.
    - Update the figure caption and the surrounding prose (~lines 251, 289 of example1.tex) so the description
      matches what the NEW screenshot actually shows (e.g. reference the Project dock / Inspector / 3D View if
      the prose describes UI elements). Do not describe UI elements not visible in the capture.
    Conventions: LaTeX only; keep the existing figure float and label `fig:tut1-screenshot`.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && grep -q "includegraphics.*tut1_screenshot" doc/TUTORIAL/example1.tex && test $(ls doc/TUTORIAL/tut1_screenshot.png doc/TUTORIAL/tut1_screenshot.pdf 2>/dev/null | wc -l) -ge 1</automated>
  </verify>
  <done>A current tut1_screenshot asset (png or pdf) is the one example1.tex embeds; no stale duplicate-basename asset shadows it; caption/prose match the new image.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the tutorial PDF for review</name>
  <files>doc/TUTORIAL/tutorial.tex</files>
  <action>
    Regenerate the tutorial PDF so the human can review the final rendered document. Run the same toolchain
    `doc_tutorial` uses (pdflatex -> bibtex -> makeindex -> pdflatex x2). If the local LaTeX toolchain is
    unavailable, state that the render must be produced by CI / the `doc_tutorial` target on a machine that
    has it, and point the reviewer at that artifact instead. Do NOT edit content here — this is the render
    step feeding the UAT checkpoint. Report the path to the produced PDF (doc/TUTORIAL/tutorial.pdf, copied to
    the build tree's doc/tutorial.pdf by the target).
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball/doc/TUTORIAL && if command -v pdflatex >/dev/null 2>&1; then pdflatex -interaction=nonstopmode -halt-on-error tutorial.tex >/tmp/tut_render.log 2>&1 && (command -v bibtex >/dev/null && bibtex tutorial >>/tmp/tut_render.log 2>&1; true) && (command -v makeindex >/dev/null && makeindex tutorial.idx >>/tmp/tut_render.log 2>&1; true) && pdflatex -interaction=nonstopmode -halt-on-error tutorial.tex >>/tmp/tut_render.log 2>&1 && pdflatex -interaction=nonstopmode -halt-on-error tutorial.tex >>/tmp/tut_render.log 2>&1 && test -f tutorial.pdf; else echo "pdflatex absent — render deferred to CI/doc_tutorial target"; true; fi</automated>
  </verify>
  <done>tutorial.pdf is produced locally, or the render is explicitly deferred to CI with the reviewer pointed at that artifact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The fully refreshed tutorial: updated URLs / release tag / roster / publications (999.70-01), the new
    current-UI screenshot (this plan), and the documented Restore Defaults scope shipped separately (999.70-02).
  </what-built>
  <how-to-verify>
    Open the rendered `tutorial.pdf` and confirm:
    1. Title page shows the current release tag and an accurate author list.
    2. The BALLView screenshot (Chapter "First Steps") shows the modern UI (Project dock, Inspector, 3D View),
       and the surrounding text matches the image.
    3. URLs you spot-check (project site, releases, GitHub, FAQ) are live and correct.
    4. There are no leftover step-by-step build instructions in the body; build guidance points online.
    5. The publications / "please cite" reference is the canonical BMC 2010 citation.
    Confirm the three ROADMAP success criteria for the phase are met (build-instructions/download path current;
    developer list current; screenshots/URLs reflect current BALLView). TUT-02 (Restore Defaults docs) is
    verified separately in 999.70-02 and is not part of this PDF review.
  </how-to-verify>
  <resume-signal>Reply "approved" or list specific corrections (which page / which figure / which URL).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| screenshot asset → distributed PDF | A captured image could inadvertently include local paths / user data in window chrome |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.70-04 | Information disclosure | tut1_screenshot capture | mitigate | Capture instructions scope the window to the BALLView UI; the human UAT review step (final checkpoint) inspects the rendered figure before it ships, catching any leaked path/username in title-bar/file-dialog chrome |

No package installs in this plan — package-legitimacy gate not applicable.
</threat_model>

<verification>
- A current screenshot asset is embedded (no legacy duplicate shadowing it).
- tutorial.pdf renders (locally or via CI/doc_tutorial).
- Human approves the rendered PDF against the phase success criteria.
</verification>

<success_criteria>
- TUT-01 screenshot portion complete; tutorial PDF reflects the current BALLView UI.
- Phase exit gate (human PDF review) passed.
</success_criteria>

<output>
Create `.planning/phases/999.70-pdf-tutorial-docs/999.70-03-SUMMARY.md` when done.
Record: screenshot format/path chosen, which legacy assets were removed, whether the PDF was rendered locally
or via CI, and the UAT outcome (approved / corrections).
</output>
