---
phase: 999.72-correctness-bugs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - source/QSAR/aromaticityProcessor.C
  - test/AromaticityProcessor_test.C
  - test/data/AromaticityProcessor_test_BUG539.sdf
autonomous: true
requirements: [BUG-539]
must_haves:
  truths:
    - "AromaticityProcessor assigns aromatic bond orders to ALL rings of the #539 fused polycyclic system, not just two of four"
    - "The previously-failing #539 molecule processes without error and matches its aromatized reference"
  artifacts:
    - path: "source/QSAR/aromaticityProcessor.C"
      provides: "Corrected fused-ring aromatic-system extension"
    - path: "test/data/AromaticityProcessor_test_BUG539.sdf"
      provides: "The #539 kekulized input molecule as a regression fixture"
    - path: "test/AromaticityProcessor_test.C"
      provides: "Regression CHECK asserting all aromatic bonds are assigned"
  key_links:
    - from: "AromaticityProcessor::aromatize"
      to: "AromaticityProcessor::extendAromaticSystem_"
      via: "ring-selection loop over SSSR"
      pattern: "extendAromaticSystem_"
---

<objective>
Fix AromaticityProcessor failures reported in issue #539 (BUG-539): for a fused
polycyclic aromatic system, only two of the four rings get the aromatic bond
order; the rest are left kekulized.

Purpose: AromaticityProcessor must correctly aromatize fused ring systems (e.g.
the COD-derived molecule in #539) so downstream chemistry (bond orders, model
building) is correct.
Output: A corrected ring-selection/extension path plus a regression fixture +
test that locks the full-system assignment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- From source/QSAR/aromaticityProcessor.C. Key entry points: -->

  Processor::Result AromaticityProcessor::operator () (AtomContainer& ac)
    // gates on isValid_(ac), then RingPerceptionProcessor.calculateSSSR(sssr, ac)
    // then aromatize(sssr, ac)

  void AromaticityProcessor::aromatize(const vector<vector<Atom*> >& sssr_orig,
                                       AtomContainer& ac)
    // 1. converts SSSR to vector<HashSet<Atom*>>
    // 2. prunes rings that "cannot be aromatic" (the destab/may_be filter)
    // 3. while(!sssr.empty()): scores each remaining ring by intersection count
    //    + double-bond count, picks the min-intersection ring, then:
    //      if hasConjugatedDoubleBonds_(min_is_ring) -> extendAromaticSystem_
    //      else if min_is != 0 -> extendAromaticSystem_
    //      else -> drop the ring
    // 4. sets IsAromatic property for atoms on AROMATIC bonds

  void AromaticityProcessor::extendAromaticSystem_(vector<HashSet<Atom*>>& sssr,
                                                   HashSet<Atom*> ring)
  bool AromaticityProcessor::hasConjugatedDoubleBonds_(HashSet<Atom*> ring_orig)
  Size AromaticityProcessor::countPiElectrons_(HashSet<Atom*>& ring)

<!-- The #539 symptom (only interior/2-of-4 rings aromatized) points at the
     per-ring decision in the while-loop: an interior ring of a fused system can
     legitimately carry no conjugated double bonds of its own yet still belong to
     the aromatic system, and the min_is/extend bookkeeping can drop or skip it.
     The reference molecules are in the issue's pastebin (kekulized + aromatized
     forms). -->

<!-- Test harness (BALL classTest). From test/AromaticityProcessor_test.C: -->
  #include <BALL/CONCEPT/classTest.h>
  START_TEST(AromaticityProcessor)
  CHECK(<label>) ... TEST_EQUAL(a,b) ... RESULT
  END_TEST
  // SDFile loading pattern used elsewhere in BALL tests:
  //   SDFile f(BALL_TEST_DATA_PATH(<file>.sdf)); System S; f >> S; f.close();
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the #539 molecule as a regression fixture</name>
  <files>test/data/AromaticityProcessor_test_BUG539.sdf</files>
  <action>Reconstruct the kekulized input molecule from issue #539 as an SDFile
  under test/data. The structures are in the issue's pastebin (linked from
  `gh issue view 539 --repo BALL-Project/ball`); if the pastebin is dead,
  synthesize a minimal fused 4-ring polycyclic aromatic (e.g. pyrene or a
  4-ring PAH) in kekulized form that reproduces the "only some rings aromatized"
  failure against the unfixed code. Name the file
  AromaticityProcessor_test_BUG539.sdf. Confirm the unfixed AromaticityProcessor
  leaves at least one ring non-aromatic on this input before writing the fix
  (record the observed wrong count in the SUMMARY).</action>
  <verify>
    <automated>test -s test/data/AromaticityProcessor_test_BUG539.sdf && echo OK</automated>
  </verify>
  <done>SDFile fixture exists and is loadable; baseline (unfixed) behavior recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the failing regression assertion</name>
  <files>test/AromaticityProcessor_test.C</files>
  <behavior>
    - Load AromaticityProcessor_test_BUG539.sdf, run AromaticityProcessor over
      the System (operator() via apply, or aromatize directly with a computed
      SSSR), then assert that EVERY ring bond of the fused system carries
      Bond::ORDER__AROMATIC (or that the count of aromatic bonds equals the
      expected reference count for the fully-aromatized form).
    - The CHECK must FAIL against the unfixed processor (only 2 of 4 rings) and
      PASS after Task 3.
  </behavior>
  <action>Add a CHECK block labelled for BUG-539 that counts aromatic bonds (or
  per-ring aromatic membership) and TEST_EQUALs it to the expected value for the
  fully aromatized reference. Use BALL_TEST_DATA_PATH for the fixture. No fenced
  implementation code in the plan; write the test against the classTest macros.</action>
  <verify>
    <automated>grep -nq "BUG-539" test/AromaticityProcessor_test.C && echo OK</automated>
  </verify>
  <done>Regression CHECK present and red against unfixed code.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the fused-ring extension so all member rings aromatize</name>
  <files>source/QSAR/aromaticityProcessor.C</files>
  <action>In AromaticityProcessor::aromatize, fix the while-loop ring-selection /
  extendAromaticSystem_ bookkeeping so an interior ring of a fused aromatic
  system is not dropped or left unprocessed. Root cause to address: the per-ring
  branch treats a ring with no conjugated double bonds of its own and zero
  recorded intersection as droppable, even when it shares atoms with an already
  extended aromatic system; and/or extendAromaticSystem_ does not propagate the
  aromatic assignment across all shared-edge rings of the system. Make the
  decision consider shared membership with rings already merged into the aromatic
  system (via countPiElectrons_/hasConjugatedDoubleBonds_ at the SYSTEM level, not
  the isolated single ring) before dropping. Preserve the existing
  OVERWRITE_BOND_ORDERS option semantics and the IsAromatic property pass at the
  end. Keep 2-space indent, Allman braces, namespace BALL, trailing-underscore
  members. Do not change the public processor signature or isValid_ caching.
  Verify the fix on the Task 1 fixture AND that the existing
  AromaticityProcessor_test cases still pass (no regression on simple
  benzene/pyridine cases).</action>
  <verify>
    <automated>cmake --build --preset ci-macos --target AromaticityProcessor_test 2>&1 | tail -5; ctest --preset ci-macos -R AromaticityProcessor_test --output-on-failure --timeout 120</automated>
  </verify>
  <done>AromaticityProcessor_test passes including the new BUG-539 CHECK and all pre-existing cases. Use ci-linux/ci-windows on those platforms.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user molecule (SDFile/SMILES) → AromaticityProcessor | untrusted ring topology crosses into the aromatizer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99972-03 | Tampering (incorrect chemistry) | AromaticityProcessor::aromatize | mitigate | correct fused-system extension so bond orders match the true aromatic system (this fix) |
| T-99972-04 | Denial of Service | aromatize while-loop | accept | loop already shrinks sssr each iteration via extend/drop; fix must preserve monotonic shrink — covered by the ctest timeout in Task 3. |
</threat_model>

<verification>
- AromaticityProcessor_test passes (new + existing CHECKs) under the ctest timeout.
- No package installs (no Package Legitimacy Gate needed).
</verification>

<success_criteria>
The #539 fused polycyclic molecule aromatizes completely (all member rings) and processes without error; the regression test is red against the old code and green against the fix.
</success_criteria>

<output>
Create `.planning/phases/999.72-correctness-bugs/999.72-02-SUMMARY.md` when done.
Commit message: `fix(999.72): aromatize all rings of fused systems (#539)`
</output>
