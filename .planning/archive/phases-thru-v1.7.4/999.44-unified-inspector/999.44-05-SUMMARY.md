---
phase: 999.44-unified-inspector
plan: 05
subsystem: view-ui
tags: [ballview-refresh, inspector, scene-tab, controllers, ball-ui-v2, wave-4, COMPLETE]
duration: ~25min
completed: 2026-05-17
---

# Phase 999.44 Plan 05: Scene tab remainder Summary

**Scene tab full coverage.** Sub-PR 4.5 continuation. 3 more read-only mirror Controllers + 4 sections beyond the StageSection exemplar landed in Plan 01.

## What landed

**Controllers:**
- CameraController  — viewPoint + lookAt mirrored from Stage::getCamera()
- LightController   — light count + ambient intensity
- StereoController  — enabled + eye/focal distance + swap-SBS

**Sections (Handover §04 Scene-tab order):** Camera → Lights → Stage → Stereo → Background.
- CameraSection     — read-only QLabel display (editable in cut-over plan)
- LightsSection     — count + ambient slider
- StereoSection     — checkbox + 2 sliders + swap checkbox
- BackgroundSection — colour SwatchButton bound directly to **StageController.backgroundColor** (no new Controller — exemplifies "both surfaces bind to the same controller" within one tab)

**InspectorView API:** `attachSceneTab(Stage*, Scene*)`. Called from mainframe post-`scene_ = new Scene(...)` (Stage lives on the Scene widget).

## Commits

| Hash | Subject |
| ---- | ------- |
| `0b3934b627` | feat(999.44-05/01): Scene tab — Camera/Lights/Stereo/Background sections |

## Build verification

- macOS-arm64 Qt 6.11 BALL_UI_V2={ON,OFF} green.

## Renderer-interface-boundary binding (SEED-001 step 5)

Camera + Light + Stereo + Stage controllers touch renderer/scene state. They currently bind directly to `Stage*`/`Scene*` per the existing dialog pattern. **Binding through the `Renderer`/`RenderSurface` interface (per `.planning/RENDERER-INTERFACE-BOUNDARY.md`) is deferred to the cut-over plans** so this plan's read-only mirror lands without disrupting the renderer-boundary work. The cut-over plans must reintroduce the binding before pushing mutations.
