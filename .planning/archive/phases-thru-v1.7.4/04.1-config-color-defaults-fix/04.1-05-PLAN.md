---
phase: 04.1-config-color-defaults-fix
plan: 05
type: execute
wave: 3
depends_on:
  - 04.1-03
  - 04.1-04
files_modified:
  - source/VIEW/DIALOGS/coloringSettingsDialog.C
autonomous: true
gap_closure: true
requirements:
  - CONFIG-01

must_haves:
  truths:
    - "Malformed ElementColorOverrides= entries are silently skipped, not thrown out of readPreferenceEntries — a tampered or partially-written `~/.BALLView` containing tokens like `garbage:zz;` no longer aborts preferences loading; the unparseable token is logged with `Log.warn()` and the loop continues to the next token (CR-03 closed; CONFIG-01 criterion 2 satisfied at runtime now that CR-01 made the read path reachable)"
    - "ColorRGBA(parts[1]) is never invoked with a string of length other than 6 or 8 — a length pre-check guards against the undefined-behaviour buffer-overread documented in source/VIEW/DATATYPE/colorRGBA.C:255-282 for shorter strings, and against the BALL_VIEW_DEBUG `Exception::InvalidRange` throw"
    - "The known-colored-elements set used for the D-05.2 all-white sanity check is constructed once at file scope rather than rebuilt on every readPreferenceEntries call (WR-03 closed; the rule lives in one named place and is reusable for any future test)"
  artifacts:
    - path: "source/VIEW/DIALOGS/coloringSettingsDialog.C"
      provides: "try/catch-guarded per-token parse with length pre-check; file-scope KNOWN_COLORED_ATOMIC_NUMBERS constant"
      contains: "catch.*Exception::GeneralException"
  key_links:
    - from: "ColoringSettingsDialog::readPreferenceEntries new-override-key parse loop"
      to: "BALL::Exception::GeneralException (the BALL exception hierarchy root from include/BALL/COMMON/exception.h:59)"
      via: "try { atomic_number = parts[0].toUnsignedInt(); override_color = ColorRGBA(parts[1]); } catch (const Exception::GeneralException&) { Log.warn(); continue; } — InvalidFormat (from String::toUnsignedInt) and InvalidRange (from ColorRGBA's stringToRGBA_ in BALL_VIEW_DEBUG) both derive from GeneralException, so a single catch handles both"
      pattern: "catch[[:space:]]*\\([[:space:]]*const[[:space:]]+Exception::GeneralException"
    - from: "ColoringSettingsDialog::readPreferenceEntries length pre-check"
      to: "ColorRGBA(const String&) constructor"
      via: "if (parts[1].size() != 6 && parts[1].size() != 8) { Log.warn(); continue; } — guards against the release-build buffer-overread for strings shorter than 6 chars"
      pattern: "parts\\[1\\]\\.size\\(\\)[[:space:]]*!=[[:space:]]*[68]"
    - from: "file-scope anonymous namespace in coloringSettingsDialog.C"
      to: "ColoringSettingsDialog::readPreferenceEntries all-white sanity check"
      via: "static const set or array `KNOWN_COLORED_ATOMIC_NUMBERS = {6, 7, 8, 15, 16}` plus a small lookup helper, replacing the per-call inline `std::set<Position>` construction"
      pattern: "(KNOWN_COLORED_ATOMIC_NUMBERS|isKnownColoredElement)"
---

<objective>
Close CR-03 (the third BLOCKER, latent until Plans 03 and 04 made the read path reachable): wrap the per-token parse in `readPreferenceEntries` in a `try { ... } catch (const Exception::GeneralException&) { ... }` block, and add a string-length pre-check before `ColorRGBA(parts[1])` construction. Both guards skip the unparseable token, log a `Log.warn()` message, and `continue` to the next token. This satisfies CONFIG-01 criterion 2 ("a stale or partial saved color block falls back to compiled defaults") at runtime — without these guards, a tampered or partially-written `~/.BALLView` containing tokens like `garbage:zz;` would propagate `Exception::InvalidFormat` (from `String::toUnsignedInt`) or `Exception::InvalidRange` (from `ColorRGBA`'s `stringToRGBA_` in `BALL_VIEW_DEBUG` builds) out of `readPreferenceEntries`, aborting the whole preferences-load mid-iteration.

Bundle WR-03 (the inline `std::set<Position> known_colored` rebuild on every call) by hoisting the known-colored-elements set into a file-scope anonymous-namespace constant `KNOWN_COLORED_ATOMIC_NUMBERS = {6, 7, 8, 15, 16}` plus a small `isKnownColoredElement(Position)` helper. This makes the D-05.2 rule live in one named place and reusable, and removes a per-call construction cost (admittedly trivial, but the maintainability win is the real motivation).

Purpose: closes the third and final BLOCKER from VERIFICATION.md, plus a bundle warning, completing the gap closure for Phase 4.1 CONFIG-01 acceptance criteria 1 and 2. After this plan, the read path is reachable (Plan 03), the deletion loops correctly process all section lines (Plan 04), AND malformed input is gracefully skipped instead of aborting the load (this plan). All three CONFIG-01 criteria are then satisfied at runtime, not just by accident.

Output: a single-file edit to `source/VIEW/DIALOGS/coloringSettingsDialog.C` with two coordinated changes — file-scope helper introduction and per-token parse hardening.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-config-color-defaults-fix/04.1-CONTEXT.md
@.planning/phases/04.1-config-color-defaults-fix/04.1-VERIFICATION.md
@.planning/phases/04.1-config-color-defaults-fix/04.1-REVIEW.md
@.planning/phases/04.1-config-color-defaults-fix/04.1-03-PLAN.md
@.planning/phases/04.1-config-color-defaults-fix/04.1-04-PLAN.md
@CLAUDE.md

<!-- Files this plan modifies (post-Plan-04 state) -->
@source/VIEW/DIALOGS/coloringSettingsDialog.C

<!-- Exception hierarchy reference -->
@include/BALL/COMMON/exception.h

<interfaces>
<!-- Key APIs and types the executor uses. Extracted directly from the codebase. -->

From `include/BALL/COMMON/exception.h`:
- Line 59: `class BALL_EXPORT GeneralException` — the root of the BALL exception hierarchy.
- Line 229-235: `class BALL_EXPORT InvalidFormat : public GeneralException` — thrown by `String::toUnsignedInt` for non-numeric input. Constructor: `InvalidFormat(const char* file, int line, const String& s);`
- `Exception::InvalidRange` (also derived from `GeneralException`) — thrown by `ColorRGBA`'s `stringToRGBA_` in `BALL_VIEW_DEBUG` builds for strings whose length is not 6 or 8. (In release builds, `stringToRGBA_` reads past the end of the buffer for shorter strings — undefined behaviour. The length pre-check guards against this regardless of build mode.)

A single `catch (const Exception::GeneralException& e)` clause handles BOTH `InvalidFormat` and `InvalidRange` because both derive from `GeneralException`. The qualifier is `BALL::Exception::GeneralException`; inside `namespace BALL { namespace VIEW { ... } }` the unqualified `Exception::GeneralException` resolves correctly.

From `source/VIEW/DIALOGS/coloringSettingsDialog.C` (current post-Plan-04 state — line numbers approximate, locate by content):
- The per-token parse loop is inside the `if (inifile.hasEntry("COLORING_OPTIONS", "ElementColorOverrides"))` branch of `readPreferenceEntries`. The shape is:
  ```
  for (Position t = 0; t < (Position)tokens.size(); ++t)
  {
    if (tokens[t].isEmpty()) continue;

    std::vector<String> parts;
    tokens[t].split(parts, ":");

    if (parts.size() != 2 || parts[0].isEmpty() || parts[1].isEmpty()) continue;

    Position atomic_number = parts[0].toUnsignedInt();         // <— THROWS on non-numeric
    ColorRGBA override_color(parts[1]);                         // <— UB or throws on len != 6,8

    // D-05.2 all-white sanity check: ...
    if (known_colored.count(atomic_number) > 0)
    {
      ColorUnit white_unit(1.0f);
      if (override_color.getRed()   == white_unit && ...)
      {
        Log.warn() << "ColoringSettingsDialog: dropping suspicious all-white override ..." << std::endl;
        continue;
      }
    }

    // Find the matching row in element_table_ by symbol and apply the override color.
    String symbol = PTE[atomic_number].getSymbol();
    for (Position p = 0; p < (Position)element_table_->rowCount(); ++p)
    {
      if (ascii(element_table_->item(p, 0)->text()) == symbol) { ... }
    }
  }
  ```
- Above the for-loop, the `std::set<Position> known_colored;` declaration plus 5 `known_colored.insert(...)` calls. This block is replaced by the file-scope helper plus a single call.
- The file already includes `<set>` at line 9 of the post-Plan-01 file. After this plan removes the inline `std::set<Position>` construction, `<set>` MAY become unused IF the file-scope helper uses an array+`std::find` rather than a `std::set`. The reviewer's recommendation (REVIEW.md WR-03 fix code, lines 376-394) is the array+`std::find` approach, which keeps the implementation header-light. Use that approach. Then OPTIONALLY remove the `#include <set>` line — but only if no other use of `std::set` remains in the file. Verify with `grep -n 'std::set' source/VIEW/DIALOGS/coloringSettingsDialog.C` BEFORE deciding to remove the include. If the include is removed, `<algorithm>` MUST be added (for `std::find`, `std::begin`, `std::end`) — though `<algorithm>` is almost certainly already transitively pulled in via `<QtWidgets>` or `<vector>`; the safer play is to add `#include <algorithm>` explicitly even if redundant.

File-scope anonymous-namespace helper to add (per REVIEW.md WR-03 fix template):
```
namespace
{
  // D-05.2: atomic numbers whose all-white override is treated as corruption
  // (carbon, nitrogen, oxygen, phosphorus, sulphur). Inline file-scope so the rule
  // lives in one named place and the parse loop stays readable.
  constexpr BALL::Position KNOWN_COLORED_ATOMIC_NUMBERS[] = {6, 7, 8, 15, 16};

  bool isKnownColoredElement(BALL::Position atomic_number)
  {
    return std::find(std::begin(KNOWN_COLORED_ATOMIC_NUMBERS),
                     std::end(KNOWN_COLORED_ATOMIC_NUMBERS),
                     atomic_number) != std::end(KNOWN_COLORED_ATOMIC_NUMBERS);
  }
}
```
Place this anonymous namespace AFTER the `#include` block and BEFORE `namespace BALL { namespace VIEW { ... } }` opens — the conventional place for file-local helpers. Verify the placement matches the file's existing structure (post-Plan-01 file has no anonymous-namespace helpers; this plan introduces the first). Use `BALL::Position` qualified inside the anonymous namespace because the helper sits OUTSIDE `namespace BALL` (the helper is in the global anonymous namespace, where unqualified `Position` does not resolve).

The post-edit per-token parse loop should look like (illustrative — match the file's existing 5-tab-deep indentation inside the namespace+function nesting):
```
for (Position t = 0; t < (Position)tokens.size(); ++t)
{
  if (tokens[t].isEmpty()) continue;

  std::vector<String> parts;
  tokens[t].split(parts, ":");

  if (parts.size() != 2 || parts[0].isEmpty() || parts[1].isEmpty()) continue;

  // D-05.2 length pre-check: ColorRGBA(const String&) reads 6 or 8 chars without bounds
  // checking in release builds (source/VIEW/DATATYPE/colorRGBA.C:255-282 — undefined
  // behaviour for shorter strings); in BALL_VIEW_DEBUG it throws Exception::InvalidRange.
  // Guard explicitly so a malformed token cannot corrupt or abort.
  if (parts[1].size() != 6 && parts[1].size() != 8)
  {
    Log.warn() << "ColoringSettingsDialog: dropping malformed override token '"
               << tokens[t] << "' (color hex must be 6 or 8 chars)" << std::endl;
    continue;
  }

  Position atomic_number;
  ColorRGBA override_color;
  try
  {
    atomic_number = parts[0].toUnsignedInt();
    override_color = ColorRGBA(parts[1]);
  }
  catch (const Exception::GeneralException&)
  {
    // CONFIG-01 criterion 2: stale/partial saved color block falls back to compiled defaults.
    // String::toUnsignedInt throws InvalidFormat on non-numeric input; ColorRGBA throws
    // InvalidRange on bad length in BALL_VIEW_DEBUG. Both derive from GeneralException.
    Log.warn() << "ColoringSettingsDialog: dropping unparseable override token '"
               << tokens[t] << "' (parse exception)" << std::endl;
    continue;
  }

  // D-05.2 all-white sanity check
  if (isKnownColoredElement(atomic_number))
  {
    ColorUnit white_unit(1.0f);
    if (override_color.getRed()   == white_unit &&
        override_color.getGreen() == white_unit &&
        override_color.getBlue()  == white_unit)
    {
      Log.warn() << "ColoringSettingsDialog: dropping suspicious all-white override for atomic number "
                 << atomic_number << " (D-05 sanity check; compiled default will be used)" << std::endl;
      continue;
    }
  }

  // Find the matching row in element_table_ by symbol and apply the override color.
  String symbol = PTE[atomic_number].getSymbol();
  for (Position p = 0; p < (Position)element_table_->rowCount(); ++p)
  {
    if (ascii(element_table_->item(p, 0)->text()) == symbol)
    {
      element_table_->item(p, 1)->setBackground(override_color.getQColor());
      break;
    }
  }
}
```

Note: the `Position atomic_number;` and `ColorRGBA override_color;` are declared OUTSIDE the try block so they remain in scope for the all-white check and the table-update loop after the try/catch. Both have default constructors (`Position` is a numeric typedef from BALL::DATATYPE; `ColorRGBA` has a default constructor populating opaque white per `include/BALL/VIEW/DATATYPE/colorRGBA.h`). Default-constructed values are immediately overwritten in the try block; if the try throws, the `continue` statement bypasses any further use of the uninitialized `atomic_number`/`override_color` values — correct by control flow.

The inline `std::set<Position> known_colored;` block + 5 `insert` calls is REMOVED entirely from the function body — the file-scope helper replaces it. The all-white sanity check `if (known_colored.count(atomic_number) > 0)` is replaced by `if (isKnownColoredElement(atomic_number))`.

Do NOT modify the legacy-Elements discard branch (Plan 03 + Plan 04 already finalized it), the new-key `hasEntry`/`getValue` calls, the residue-name read block, or any other function in the file.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add file-scope KNOWN_COLORED_ATOMIC_NUMBERS helper; harden per-token parse with length-check + try/catch; remove inline std::set</name>
  <files>source/VIEW/DIALOGS/coloringSettingsDialog.C</files>
  <read_first>
    - source/VIEW/DIALOGS/coloringSettingsDialog.C (the whole file post-Plan-04 — locate the new-override-key parse loop by grepping for `tokens[t].split(parts, ":")`)
    - include/BALL/COMMON/exception.h (lines 59-90 — `GeneralException` definition; lines 229-235 — `InvalidFormat`)
    - source/VIEW/DATATYPE/colorRGBA.C (lines 255-282 — confirms the `stringToRGBA_` buffer-overread for short strings; documents WHY the length pre-check is needed even in release builds)
    - .planning/phases/04.1-config-color-defaults-fix/04.1-VERIFICATION.md (CR-03 prescription, lines 161-176)
    - .planning/phases/04.1-config-color-defaults-fix/04.1-REVIEW.md (CR-03 fix template, lines 246-280; WR-03 fix template, lines 376-394)
  </read_first>
  <action>
    Make THREE coordinated textual changes to `source/VIEW/DIALOGS/coloringSettingsDialog.C`:

    1. **Add file-scope anonymous-namespace helper** (WR-03 closure):

       Locate the top-of-file region between the `#include <QtWidgets/QCheckBox>` line (~line 15 of the post-Plan-04 file) and the `namespace BALL` opening line (~line 17). Insert the following block immediately AFTER the last `#include` and BEFORE `namespace BALL`:

       ```
       #include <algorithm>

       namespace
       {
         // D-05.2: atomic numbers whose all-white override is treated as corruption
         // (carbon, nitrogen, oxygen, phosphorus, sulphur). File-scope so the rule
         // lives in one named place and the parse loop stays readable.
         constexpr BALL::Position KNOWN_COLORED_ATOMIC_NUMBERS[] = {6, 7, 8, 15, 16};

         bool isKnownColoredElement(BALL::Position atomic_number)
         {
           return std::find(std::begin(KNOWN_COLORED_ATOMIC_NUMBERS),
                            std::end(KNOWN_COLORED_ATOMIC_NUMBERS),
                            atomic_number) != std::end(KNOWN_COLORED_ATOMIC_NUMBERS);
         }
       }
       ```

       The `#include <algorithm>` may already be transitively pulled in via `<QtWidgets>` and `<vector>`, but explicit-include is the project convention (verify by grepping `#include <algorithm>` across `source/VIEW/` and confirming it's used directly elsewhere when `std::find`/`std::sort` is used). The `BALL::Position` qualifier is REQUIRED because the anonymous namespace sits OUTSIDE `namespace BALL`. Use 2-space indentation inside the anonymous namespace (matching the modeline `tab-width: 2`) — actual chars MUST be matching what the rest of the file uses; inspect the post-Plan-04 file's helper-equivalent indentation. If the file uses tabs at this nesting depth, use tabs.

    2. **Harden the per-token parse loop** (CR-03 closure):

       Locate the per-token parse loop inside the `if (inifile.hasEntry("COLORING_OPTIONS", "ElementColorOverrides"))` branch of `readPreferenceEntries`. The current shape is documented in the `<interfaces>` block above (post-Plan-04 state). Apply the following transformations, IN ORDER:

       a. **REMOVE** the inline `std::set<Position> known_colored;` declaration (one line) and the FIVE `known_colored.insert(N);` lines that follow it (six lines total). The whole block lives BEFORE the `for (Position t = 0; ...)` loop and AFTER the `String overrides_raw = inifile.getValue(...)` line. Delete all six lines plus any trailing blank line that becomes a doubled-blank.

       b. **INSIDE the for-loop, BEFORE the parse**, ADD the length pre-check block (immediately after the `if (parts.size() != 2 || parts[0].isEmpty() || parts[1].isEmpty()) continue;` line and BEFORE the `Position atomic_number = parts[0].toUnsignedInt();` line):

          ```
          // D-05.2 length pre-check: ColorRGBA(const String&) reads 6 or 8 chars without bounds
          // checking in release builds (source/VIEW/DATATYPE/colorRGBA.C:255-282 — undefined
          // behaviour for shorter strings); in BALL_VIEW_DEBUG it throws Exception::InvalidRange.
          // Guard explicitly so a malformed token cannot corrupt or abort.
          if (parts[1].size() != 6 && parts[1].size() != 8)
          {
            Log.warn() << "ColoringSettingsDialog: dropping malformed override token '"
                       << tokens[t] << "' (color hex must be 6 or 8 chars)" << std::endl;
            continue;
          }
          ```

       c. **REPLACE** the two-line direct parse:
          ```
          Position atomic_number = parts[0].toUnsignedInt();
          ColorRGBA override_color(parts[1]);
          ```
          with the try/catch-guarded form (variables declared outside the try so they remain in scope for the rest of the loop body):
          ```
          Position atomic_number;
          ColorRGBA override_color;
          try
          {
            atomic_number = parts[0].toUnsignedInt();
            override_color = ColorRGBA(parts[1]);
          }
          catch (const Exception::GeneralException&)
          {
            // CONFIG-01 criterion 2: stale/partial saved color block falls back to compiled defaults.
            // String::toUnsignedInt throws InvalidFormat on non-numeric input; ColorRGBA throws
            // InvalidRange on bad length in BALL_VIEW_DEBUG. Both derive from GeneralException.
            Log.warn() << "ColoringSettingsDialog: dropping unparseable override token '"
                       << tokens[t] << "' (parse exception)" << std::endl;
            continue;
          }
          ```

          IMPORTANT — `ColorRGBA` MUST be default-constructible AND copy-assignable for this pattern. Confirm via `grep -A 5 'class.*ColorRGBA' include/BALL/VIEW/DATATYPE/colorRGBA.h` that the default constructor is public (not deleted) and that an `operator=` exists (or that the implicit one is acceptable — `ColorRGBA` is a value type holding RGBA color units). The `setDefaultValues_` function at line 50 of the post-Plan-04 file constructs `ColorRGBA` instances as `vector<ColorRGBA> colors;` plus `colors.push_back(it->second)`, which requires both default-constructibility and copy-assignment. Both are confirmed by the existing usage. If the build fails because `override_color = ColorRGBA(parts[1]);` does not compile (e.g. `ColorRGBA` has no copy-assign operator), the fallback is to construct a temporary inside the try and then copy via construction:
          ```
          try
          {
            atomic_number = parts[0].toUnsignedInt();
            ColorRGBA tmp(parts[1]);
            override_color = tmp;
          }
          ```
          The acceptance gate is build success; the executor adapts as needed.

       d. **REPLACE** the all-white sanity check guard:
          ```
          if (known_colored.count(atomic_number) > 0)
          ```
          with:
          ```
          if (isKnownColoredElement(atomic_number))
          ```

          The body of the if-block (the `ColorUnit white_unit(1.0f);` line and the channel-equality check with the `Log.warn() << "ColoringSettingsDialog: dropping suspicious all-white override ..."` line and the `continue;`) is preserved verbatim.

    3. **Conditional cleanup**: after the above edits, run `grep -n 'std::set' source/VIEW/DIALOGS/coloringSettingsDialog.C` from the project root. If the count is 0 (no remaining uses of `std::set` in the file), REMOVE the `#include <set>` line at the top of the file (line 9 of the post-Plan-01 file). If the count is > 0, leave the include in place. Document the decision in the SUMMARY.

    Do NOT modify any other line in the file. Specifically:
    - The legacy-Elements discard branch (Plan 03 + Plan 04 finalized) stays as-is.
    - The new-key `hasEntry`/`getValue` calls and the `overrides_raw.split(tokens, ";");` line stay as-is.
    - The element_table_ symbol-match-and-update inner loop after the all-white check stays as-is.
    - The residue-name read block at the end of `readPreferenceEntries` stays as-is.
    - The `writePreferenceEntries` function (modified by Plan 01 + Plan 04) stays as-is.
    - All other functions (`setDefaultValues_`, `getColors`, `applySettingsTo`, `createColorProcessor`, `getSettings`, `getEntryFor`, the four max/forceMin slots, `resetElementColors`) stay as-is.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball &amp;&amp; cmake --build build/macos-homebrew --target BALLView -j 8 2>&amp;1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'KNOWN_COLORED_ATOMIC_NUMBERS' source/VIEW/DIALOGS/coloringSettingsDialog.C` returns at least 4 (the constexpr declaration + the three uses inside `isKnownColoredElement` via `std::begin`/`std::end`/`std::end`).
    - Source assertion: `grep -c 'isKnownColoredElement' source/VIEW/DIALOGS/coloringSettingsDialog.C` returns exactly 2 (the helper definition + the one call inside readPreferenceEntries).
    - Source assertion: `grep -cE 'catch[[:space:]]*\([[:space:]]*const[[:space:]]+Exception::GeneralException' source/VIEW/DIALOGS/coloringSettingsDialog.C` returns exactly 1 (the new try/catch in the parse loop).
    - Source assertion: `grep -cE 'parts\[1\]\.size\(\)[[:space:]]*!=[[:space:]]*[68]' source/VIEW/DIALOGS/coloringSettingsDialog.C` returns at least 2 (the two clauses of the length pre-check `!= 6 && != 8`).
    - Negative assertion: the inline `std::set<Position> known_colored;` declaration is removed — `grep -c 'std::set<Position> known_colored' source/VIEW/DIALOGS/coloringSettingsDialog.C` returns 0. Likewise `grep -c 'known_colored.insert' source/VIEW/DIALOGS/coloringSettingsDialog.C` returns 0.
    - Negative assertion: the old all-white guard form is removed — `grep -c 'known_colored.count' source/VIEW/DIALOGS/coloringSettingsDialog.C` returns 0.
    - Source assertion: the all-white-Log.warn line is preserved — `grep -c 'dropping suspicious all-white override' source/VIEW/DIALOGS/coloringSettingsDialog.C` returns exactly 1.
    - Source assertion: the new parse-error-Log.warn line is present — `grep -c 'dropping unparseable override token' source/VIEW/DIALOGS/coloringSettingsDialog.C` returns exactly 1.
    - Source assertion: the new length-error-Log.warn line is present — `grep -c 'dropping malformed override token' source/VIEW/DIALOGS/coloringSettingsDialog.C` returns exactly 1.
    - Conditional source assertion: `grep -c '#include <set>' source/VIEW/DIALOGS/coloringSettingsDialog.C` returns 0 IF AND ONLY IF `grep -c 'std::set' source/VIEW/DIALOGS/coloringSettingsDialog.C` also returns 0. The executor records both counts in the SUMMARY.
    - Source assertion: `grep -c '#include <algorithm>' source/VIEW/DIALOGS/coloringSettingsDialog.C` returns at least 1.
    - Build assertion: `cmake --build build/macos-homebrew --target BALLView -j 8` succeeds. Zero compile errors. Pre-existing macOS OpenGL deprecation warnings unchanged.
    - Behaviour assertion 1 — graceful fallback on tampered file (recorded in plan SUMMARY): write a `~/.BALLView` containing `[COLORING_OPTIONS]\nElementColorOverrides=garbage:zz;` (a totally malformed token: non-numeric atomic number, hex string of length 2). Launch BALLView. The application MUST start successfully — no crash, no exception escape. The log MUST contain BOTH "dropping malformed override token 'garbage:zz'" (length pre-check fires first because `"zz"` is length 2, not 6 or 8) and the application loads compiled PyMOL defaults for all elements. (If the length pre-check did not fire because `"zz"` somehow parsed, the try/catch around the parse would catch the InvalidFormat from `String::toUnsignedInt("garbage")` and emit the "dropping unparseable override token" log. Either path closes CONFIG-01 criterion 2; the executor records which path fired.)
    - Behaviour assertion 2 — partial-write recovery (recorded in plan SUMMARY): write a `~/.BALLView` containing `[COLORING_OPTIONS]\nElementColorOverrides=6:33ff33ff;notanumber:bad;7:5151ff` (mixed: one good token, one entirely-malformed, one with a 6-char-but-no-alpha hex which IS valid 6-char). Launch BALLView. The application MUST start successfully. The Log MUST contain "dropping unparseable override token 'notanumber:bad'" (or "dropping malformed override token 'notanumber:bad'" depending on which guard catches the `"bad"` length-3 hex first — most likely the length pre-check). Open Preferences → Coloring Settings → Element page: carbon row MUST show the override green `33ff33` (good token applied), nitrogen row MUST show `5151ff` (the 6-char hex is valid). The malformed middle token is silently skipped.
    - Behaviour assertion 3 — all-white sanity still fires post-WR-03 (recorded in plan SUMMARY): write `~/.BALLView` with `[COLORING_OPTIONS]\nElementColorOverrides=6:ffffffff;`. Launch BALLView. Log MUST contain "dropping suspicious all-white override for atomic number 6". Carbon row in the Coloring Settings dialog MUST show PyMOL green `33ff33` (compiled default flowed through). The file-scope helper IS being used; the inline `std::set` is gone.
  </acceptance_criteria>
  <done>
    The per-token parse in `readPreferenceEntries` is guarded by a string-length pre-check and a `try { ... } catch (const Exception::GeneralException&)` block — malformed tokens are logged with `Log.warn()` and skipped, never propagating exceptions out of the function. The known-colored-elements set lives in a file-scope anonymous-namespace constant `KNOWN_COLORED_ATOMIC_NUMBERS` accessed via `isKnownColoredElement(...)`, replacing the per-call inline `std::set<Position>` construction. BALLView builds cleanly. The three behaviour gates confirm CONFIG-01 criterion 2 ("stale or partial saved color block falls back to compiled defaults") is satisfied at runtime — Phase 4.1 is complete.
  </done>
</task>

</tasks>

<verification>
After this task lands:

- **Build gate:** `cmake --build build/macos-homebrew --target BALLView -j 8` succeeds. Tri-OS CI will exercise Linux/Windows on push.
- **Static gates:** all `grep`-based assertions in the task acceptance criteria pass. Notable mechanical confirmations: the inline `std::set` is gone (count 0); the file-scope helper is referenced exactly twice (definition + use); the try/catch and length-check guards are both present.
- **Behaviour gates:** the three manual tests in the acceptance criteria — totally-malformed token, mixed valid/malformed tokens, all-white sanity — all confirm the new guards fire as intended and the application no longer aborts on tampered input. These three gates close CONFIG-01 criterion 2.
- **End-to-end Phase 4.1 gate (mandatory recording in SUMMARY):** with all three gap-closure plans landed (03 + 04 + 05), the original Phase 4.1 motivating scenario is now satisfied at runtime, not by accident: write `~/.BALLView` with `[COLORING_OPTIONS]\nElements=ffffffff;ffffffff;ffffffff;ffffffff;ffffffff;ffffffff;...` (a pre-Phase-4.1 full-table format with carbon as white). Launch BALLView. The Log MUST emit "dropping legacy Elements= block ..." (Plan 03 + Plan 04 made this reachable AND the deletion fires regardless of line position). Quit BALLView. Inspect `~/.BALLView`: section `[COLORING_OPTIONS]` MUST NOT contain an `Elements=` line. Re-launch BALLView. Open Preferences → Coloring Settings → Element page: carbon row MUST show PyMOL green `33ff33`. This is CONFIG-01 criterion 1 ("Users see compiled defaults for any color they have not explicitly customized") — verified at runtime, not by accident.
- **Plan-set completeness check:** all three BLOCKERs from VERIFICATION.md are now closed:
  - CR-01 (read path dead code) — Plan 03
  - CR-02 (sentinel off-by-one, both call sites) — Plan 04
  - CR-03 (unguarded parse) — Plan 05
  Plus three bundled warnings: WR-01 (Rg off-by-one) — Plan 04; WR-02 (misleading log) — Plan 04; WR-03 (per-call set) — Plan 05; IN-01 (override keyword) — Plan 03. WR-04 (duplicated table population in resetElementColors and setDefaultValues_) is documented as DEFERRED in this plan's SUMMARY because extracting the helper expands the change surface beyond the gap-closure boundary; promote to a backlog stub (suggested 999.4c) only if the Phase 4.1 reviewer flags it post-merge.
- **Regression gate:** the existing Plan 01 + Plan 02 behaviour gates (carbon=PyMOL-green from a non-tail legacy Elements; mid-session double-write upsert; reset button repopulates from defaults) continue to pass — Plans 03/04/05 only fix dispatch, sentinel, and parse robustness; the underlying logic is unchanged.
</verification>

<success_criteria>
- The per-token parse in `readPreferenceEntries` is fully hardened: length pre-check before `ColorRGBA` construction; try/catch for parse exceptions; both guards log and `continue` rather than throw or crash (CR-03 closed; CONFIG-01 criterion 2 satisfied at runtime).
- The known-colored-elements set lives at file scope as `KNOWN_COLORED_ATOMIC_NUMBERS` accessed via `isKnownColoredElement()` — the rule lives in one named place; the parse-loop body is more readable; per-call construction cost is gone (WR-03 closed).
- BALLView compiles cleanly on macOS-Homebrew (local) and on tri-OS CI once pushed.
- Three manual behaviour gates (totally malformed, mixed valid/malformed, all-white) confirm the guards fire as intended.
- The end-to-end Phase 4.1 scenario (legacy Elements= as last line of section → log fires + line is deleted + carbon renders PyMOL green) succeeds — CONFIG-01 criterion 1 satisfied at runtime, not by accident.
- All three BLOCKERs (CR-01, CR-02, CR-03) and four bundled warnings (WR-01, WR-02, WR-03, IN-01) are closed.
- WR-04 is documented as deferred (out of gap-closure scope; backlog promotion optional).
- No changes outside `source/VIEW/DIALOGS/coloringSettingsDialog.C`.
- Phase 4.1 is now complete at runtime; the orchestrator can re-run verification.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-config-color-defaults-fix/04.1-05-SUMMARY.md` per the standard SUMMARY template. Include:
- The exact post-edit line numbers of: the file-scope anonymous-namespace block, the length pre-check block, the try/catch block, the `isKnownColoredElement(atomic_number)` call, and any other relocated lines.
- Whether the `#include <set>` line was removed (with `std::set` count justification) and whether `#include <algorithm>` was added (it should always be added).
- Whether the `override_color = ColorRGBA(parts[1])` direct-assign form compiled, or if the executor had to fall back to the temporary+copy form (and why).
- The local macOS build result (PASS/FAIL) and the tri-OS CI run URL once pushed.
- All three manual behaviour-gate observations (totally malformed, mixed valid/malformed, all-white) — what did the Log emit, what did the dialog show?
- The end-to-end Phase 4.1 scenario observation (legacy Elements= as last line → log + delete + PyMOL green carbon on relaunch) — full trace.
- Confirmation that the only file modified is `source/VIEW/DIALOGS/coloringSettingsDialog.C`.
- A note that WR-04 (duplicated table-population body in `resetElementColors` vs `setDefaultValues_`) is deferred to backlog 999.4c (or similar) because extracting the shared helper expands the change surface beyond the gap-closure boundary; promotion is optional and depends on the Phase 4.1 reviewer's post-merge call.
- A summary line: "Phase 4.1 gap closure complete — all three BLOCKERs (CR-01/02/03) and four bundled warnings (WR-01/02/03, IN-01) closed; WR-04 deferred. CONFIG-01 criteria 1, 2, and 3 are now satisfied at runtime, not by accident. Re-run /gsd-verify-phase 4.1 to confirm."
</output>
