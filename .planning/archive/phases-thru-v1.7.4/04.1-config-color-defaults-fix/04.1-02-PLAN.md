---
phase: 04.1-config-color-defaults-fix
plan: 02
type: execute
wave: 2
depends_on:
  - 04.1-01
files_modified:
  - source/VIEW/DIALOGS/coloringSettingsDialog.ui
  - include/BALL/VIEW/DIALOGS/coloringSettingsDialog.h
  - source/VIEW/DIALOGS/coloringSettingsDialog.C
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - CONFIG-01

must_haves:
  truths:
    - "The Coloring Settings dialog's Element page exposes a 'Reset element colors to defaults' QPushButton placed under element_table_ (D-02)"
    - "Clicking the reset button reloads the compiled ElementColorProcessor defaults into element_table_ (the same code path setDefaultValues_ uses to populate the table on first show)"
    - "After reset, a writePreferenceEntries call (from the Preferences OK path) produces no ElementColorOverrides= line in section [COLORING_OPTIONS] — the override map is empty by D-06 idempotency"
    - "ROADMAP.md contains a new backlog stub (Phase 999.4b or 999.5) tracking the same fix pattern for residue color persistence (D-04)"
  artifacts:
    - path: "source/VIEW/DIALOGS/coloringSettingsDialog.ui"
      provides: "Reset button widget definition under the Element page's verticalLayout_10"
      contains: "reset_elements_button"
    - path: "include/BALL/VIEW/DIALOGS/coloringSettingsDialog.h"
      provides: "Reset slot declaration (resetElementColors)"
      contains: "resetElementColors"
    - path: "source/VIEW/DIALOGS/coloringSettingsDialog.C"
      provides: "Reset slot implementation; signal/slot connect in the constructor"
      contains: "resetElementColors"
    - path: ".planning/ROADMAP.md"
      provides: "Backlog stub for residue color persistence follow-up (D-04 deferred)"
      contains: "999.4b"
  key_links:
    - from: "source/VIEW/DIALOGS/coloringSettingsDialog.ui::reset_elements_button"
      to: "source/VIEW/DIALOGS/coloringSettingsDialog.C::resetElementColors()"
      via: "connect(reset_elements_button, SIGNAL(clicked()), this, SLOT(resetElementColors()))"
      pattern: "connect[^\\n]*reset_elements_button"
    - from: "source/VIEW/DIALOGS/coloringSettingsDialog.C::resetElementColors"
      to: "ElementColorProcessor (instantiated locally for live defaults)"
      via: "rebuilds names/colors vectors from ElementColorProcessor().getColorMap() and calls element_table_->setContent(names, colors) — same pattern as setDefaultValues_ lines 54-71"
      pattern: "element_table_->setContent"
---

<objective>
Add a "Reset element colors to defaults" button to the Coloring Settings dialog's
Element page so the user can clear their override map without deleting
`~/.BALLView` (CONFIG-01 criterion 3, per D-02). With Plan 01's diff/override
persistence already in place, the reset is a one-shot operation: repopulate
`element_table_` from a freshly constructed `ElementColorProcessor` — the next
Preferences-OK write will compute an empty diff per D-06 and the override key
will be removed by the write path. Also append a backlog stub for the residue
color persistence follow-up per D-04.

Purpose: closes the third CONFIG-01 criterion (reset from Preferences without
deleting `~/.BALLView`) and records the deferred residue scope as an actionable
backlog item so it is not lost.

Output: 4 file edits — `.ui` widget addition, header slot declaration, slot
implementation + constructor wiring, and a ROADMAP.md backlog entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-config-color-defaults-fix/04.1-CONTEXT.md
@.planning/phases/04.1-config-color-defaults-fix/04.1-01-PLAN.md
@CLAUDE.md

<!-- Files this plan modifies -->
@source/VIEW/DIALOGS/coloringSettingsDialog.ui
@include/BALL/VIEW/DIALOGS/coloringSettingsDialog.h
@source/VIEW/DIALOGS/coloringSettingsDialog.C

<interfaces>
<!-- Key types and patterns the executor uses. Extracted from the codebase. -->

From `include/BALL/VIEW/DIALOGS/coloringSettingsDialog.h`:
- Class `ColoringSettingsDialog : public QWidget, public Ui_ColoringSettingsDialogData, public PreferencesEntry`
- Existing `protected Q_SLOTS:` block (lines 77-82) contains `maxDistanceChanged()`, `maxTFChanged()`, `forceMaxValueChanged()`, `forceMinValueChanged()` — the established slot pattern. The new `resetElementColors()` slot follows this convention.

From `source/VIEW/DIALOGS/coloringSettingsDialog.C`:
- Constructor lines 19-44 wires existing signals/slots via `connect(force_max_value_slider, SIGNAL(valueChanged(int)), this, SLOT(forceMaxValueChanged()));` etc. The new connect line follows the same pattern.
- `setDefaultValues_()` lines 46-72 contains the canonical element-table population idiom — instantiate `ElementColorProcessor elp;`, iterate `elp.getColorMap()`, build `names`/`colors` vectors, call `element_table_->setContent(names, colors)`. `resetElementColors()` reuses this exact pattern.

From `source/VIEW/DIALOGS/coloringSettingsDialog.ui`:
- The Element page is `<widget class="QWidget" name="Element">` at line 22.
- It contains exactly one child layout `<layout class="QVBoxLayout" name="verticalLayout_10">` (line 23) holding one item: `<widget class="BALL::VIEW::ColorTable" name="element_table_">` (line 25).
- Adding a sibling `<item>` after the table inside the same `verticalLayout_10` places the reset button directly under the table — matches D-02 ("a button on the Coloring Settings dialog next to the element table").
- The .ui file is processed by Qt's `uic` at build time into `BALL/VIEW/UIC/ui_coloringSettingsDialog.h` (included at line 24 of the header). The generated class is `Ui_ColoringSettingsDialogData`, and any new widget with a `name="..."` becomes a public member of that base class — so after the .ui edit, `reset_elements_button` is directly accessible in the dialog's implementation.

From `.planning/ROADMAP.md`:
- The Backlog section starts around line 264 (`## Backlog`) and contains numbered entries `### Phase 999.1: ...` through `### Phase 999.8: ...`. The new entry must be inserted maintaining the numeric ordering — per CONTEXT.md /deferred/, the suggested ID is `999.4b` (the lettered suffix is the established convention for sub-promotions of the same parent backlog item; cf. how Phase 4.1 was promoted from 999.4).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reset button to the Element page of coloringSettingsDialog.ui</name>
  <files>source/VIEW/DIALOGS/coloringSettingsDialog.ui</files>
  <read_first>
    - source/VIEW/DIALOGS/coloringSettingsDialog.ui (lines 22-32 — the Element page layout)
    - .planning/phases/04.1-config-color-defaults-fix/04.1-CONTEXT.md (decision D-02)
  </read_first>
  <action>
    Edit the `<widget class="QWidget" name="Element">` block (line 22 of the pre-edit file) inside `<widget class="QStackedWidget" name="widget_stack">`. The block currently contains a `<layout class="QVBoxLayout" name="verticalLayout_10">` with a single `<item>` wrapping the `element_table_` ColorTable.

    Append a second `<item>` to `verticalLayout_10`, AFTER the existing `</item>` that closes the table item (i.e. between the existing `</item>` and the `</layout>` that closes `verticalLayout_10`). The new item wraps a horizontal layout that left-aligns a single push button (so the button sits flush under the bottom-left of the table rather than stretching the full width):

    ```xml
    <item>
     <layout class="QHBoxLayout" name="horizontalLayout_reset_elements">
      <item>
       <widget class="QPushButton" name="reset_elements_button">
        <property name="toolTip">
         <string>Reset element colors to compiled defaults (Phase 4.1 — D-02)</string>
        </property>
        <property name="text">
         <string>Reset element colors to defaults</string>
        </property>
       </widget>
      </item>
      <item>
       <spacer name="horizontalSpacer_reset_elements">
        <property name="orientation">
         <enum>Qt::Horizontal</enum>
        </property>
        <property name="sizeHint" stdset="0">
         <size>
          <width>40</width>
          <height>20</height>
         </size>
        </property>
       </spacer>
      </item>
     </layout>
    </item>
    ```

    Use 1-space XML indent matching the existing file's `.ui` indentation style (the existing `<widget class="BALL::VIEW::ColorTable" name="element_table_">` block uses 1-space-per-level XML; match it). Match the existing convention of `name="..."` attributes (snake_case with trailing underscores ONLY for member-style refs; the `.ui` convention here is plain snake_case — e.g. `force_max_value_slider`, `helix_color_button` — so use `reset_elements_button` without a trailing underscore, matching `helix_color_button` etc.).

    Do NOT change any other widget, layout, geometry, or property in the file. Do NOT add a tabstop / connection / customwidget entry for the new button — Qt connects via SIGNAL/SLOT in the constructor (Task 3) rather than through the .ui's `<connections>` block (matching the existing convention at lines 37-41 of `coloringSettingsDialog.C` which use `connect(...)` calls, not `.ui` autoconnect).

    The `<customwidgets>` block in the .ui (if any) is left unchanged — `QPushButton` is a stock Qt widget and needs no entry.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball &amp;&amp; python3 -c "import xml.etree.ElementTree as ET; t = ET.parse('source/VIEW/DIALOGS/coloringSettingsDialog.ui'); root = t.getroot(); names = [w.get('name') for w in root.iter('widget')]; assert 'reset_elements_button' in names, f'reset_elements_button missing from {names}'; print('OK reset_elements_button present, total widgets =', len(names))"</automated>
  </verify>
  <acceptance_criteria>
    - XML well-formedness: `python3 -c "import xml.etree.ElementTree as ET; ET.parse('source/VIEW/DIALOGS/coloringSettingsDialog.ui')"` exits 0.
    - Widget assertion (above automated check): `reset_elements_button` is present as a `name` attribute on a `<widget>` element.
    - Structural assertion: the new widget is a descendant of the `<widget name="Element">` node (not under any other page like `Residue_Index` or `Charge`). Verify with: `python3 -c "import xml.etree.ElementTree as ET; t=ET.parse('source/VIEW/DIALOGS/coloringSettingsDialog.ui'); root=t.getroot(); element_page=[w for w in root.iter('widget') if w.get('name')=='Element'][0]; descendants=[w.get('name') for w in element_page.iter('widget')]; assert 'reset_elements_button' in descendants, f'reset button not under Element page; Element descendants={descendants}'; print('OK reset button is under Element page')"`.
    - The pre-existing widget count for the Element page increases by exactly 1 (the new QPushButton) — the ColorTable widget is preserved.
  </acceptance_criteria>
  <done>
    The `Element` page in `coloringSettingsDialog.ui` contains a new `reset_elements_button` QPushButton placed in a horizontal layout below `element_table_`, with the text "Reset element colors to defaults" and a tooltip identifying the Phase 4.1 origin. XML parses cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Declare resetElementColors slot in coloringSettingsDialog.h</name>
  <files>include/BALL/VIEW/DIALOGS/coloringSettingsDialog.h</files>
  <read_first>
    - include/BALL/VIEW/DIALOGS/coloringSettingsDialog.h (the whole file — 91 lines; lines 77-82 are the existing protected Q_SLOTS block)
    - .planning/phases/04.1-config-color-defaults-fix/04.1-CONTEXT.md (decision D-02)
  </read_first>
  <action>
    Add a new slot declaration `virtual void resetElementColors();` to the existing `protected Q_SLOTS:` block at lines 77-82. Place it AFTER the `forceMinValueChanged();` declaration on line 82 so the existing four lines stay verbatim and the new slot is appended.

    Match the existing convention exactly: `virtual void <camelCaseName>();` with the trailing `;`, 2-space indent in the namespace nesting (the existing slots are 3 tabs deep — replicate the same leading whitespace by inspecting the actual file: 4 leading tabs per the file's tab-width=2 modeline; match by character-for-character whitespace).

    Do NOT add a Doxygen comment block above the new slot — the existing four slots have no doxygen, so matching that is more consistent than introducing a new convention.

    Do NOT modify any other line of the file. Do NOT change the existing `writePreferenceEntries(INIFile&)` / `readPreferenceEntries(INIFile&)` non-const signatures (line 72, 75) — those are the file's pre-existing override convention and Phase 4.1 explicitly does not refactor them per CONTEXT.md /specifics/.
  </action>
  <verify>
    <automated>grep -c 'virtual void resetElementColors\(\);' /Users/kohlbach/Claude/BALL/ball/include/BALL/VIEW/DIALOGS/coloringSettingsDialog.h</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the above grep returns exactly 1.
    - Source assertion: `grep -c 'protected Q_SLOTS:' /Users/kohlbach/Claude/BALL/ball/include/BALL/VIEW/DIALOGS/coloringSettingsDialog.h` still returns 1 (we didn't accidentally duplicate the section header).
    - Source assertion: the four pre-existing slot declarations (`maxDistanceChanged`, `maxTFChanged`, `forceMaxValueChanged`, `forceMinValueChanged`) are still present — `grep -cE '(maxDistanceChanged|maxTFChanged|forceMaxValueChanged|forceMinValueChanged)\(\)' /Users/kohlbach/Claude/BALL/ball/include/BALL/VIEW/DIALOGS/coloringSettingsDialog.h` returns exactly 4.
    - The file's line count increased by exactly 1 vs. the pre-edit version.
  </acceptance_criteria>
  <done>
    `resetElementColors` is declared as a `virtual void` slot in the protected Q_SLOTS section of `coloringSettingsDialog.h`, following the convention of the existing four slots.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement resetElementColors + connect button in coloringSettingsDialog.C</name>
  <files>source/VIEW/DIALOGS/coloringSettingsDialog.C</files>
  <read_first>
    - source/VIEW/DIALOGS/coloringSettingsDialog.C (post-Plan-01 state; lines 19-44 are the constructor with existing connects; lines 46-72 are setDefaultValues_ with the canonical table-population idiom; lines 329-343 are the existing slot implementations to copy the pattern from)
    - source/VIEW/MODELS/standardColorProcessor.C (lines 26-164 — confirms what `ElementColorProcessor()` populates)
    - .planning/phases/04.1-config-color-defaults-fix/04.1-CONTEXT.md (decisions D-02, D-06)
  </read_first>
  <action>
    Make two edits to `coloringSettingsDialog.C`:

    1. **Connect the button in the constructor.** At the end of the existing `connect(...)` block (after line 41, before the `registerWidgets_();` call at line 43), add one line:
       `connect(reset_elements_button, SIGNAL(clicked()), this, SLOT(resetElementColors()));`
       Match the existing 1-tab indentation and use the older SIGNAL/SLOT macro convention (the file uses old-style `SIGNAL(...)`/`SLOT(...)` macros throughout, not the Qt 5+ pointer-to-member syntax — match for consistency with the surrounding code). The widget `reset_elements_button` is accessible as an inherited member of `Ui_ColoringSettingsDialogData` thanks to Task 1's .ui edit being processed by `uic`.

    2. **Implement the slot at the bottom of the file.** Add the new slot implementation immediately AFTER the `forceMinValueChanged()` implementation (which ends at line 343 of the pre-Plan-01 file; in the post-Plan-01 file the line numbers may shift slightly, locate by the function name). Place it BEFORE `createColorProcessor` (which starts at line 346) so the slots stay grouped together.

       The body is the canonical element-table repopulation idiom mirroring `setDefaultValues_` lines 53-72:

       ```cpp
       void ColoringSettingsDialog::resetElementColors()
       {
         // D-02: clear the user's element-color overrides by repopulating
         // element_table_ from a freshly constructed ElementColorProcessor.
         // The next writePreferenceEntries call diffs the table against the
         // same compiled defaults (D-06) and will emit an empty
         // ElementColorOverrides= — restoring the unshadowed compiled palette
         // without requiring the user to delete ~/.BALLView (CONFIG-01 criterion 3).
         ElementColorProcessor elp;
         const HashMap<Position, ColorRGBA>& color_hash_map = elp.getColorMap();
         vector<String> names;
         vector<ColorRGBA> colors;
         HashMap<Position, ColorRGBA>::ConstIterator it = color_hash_map.begin();
         for (; it != color_hash_map.end(); it++)
         {
           if (it->first == 0) continue;
           names.push_back(PTE[it->first].getSymbol());
           colors.push_back(it->second);
         }
         names.push_back(PTE[0].getSymbol());
         colors.push_back(color_hash_map[0]);
         element_table_->setContent(names, colors);
       }
       ```

       Match the existing file convention exactly: 2-space indent (tab-width=2 per modeline at line 1), Allman braces, no doxygen, `namespace BALL { namespace VIEW { ... } }` nesting already in force (so unqualified names work). Member-vars like `element_table_` end with `_` per BALL convention — match.

       Do NOT touch `~/.BALLView` directly from this slot. The reset takes effect on the next Preferences-OK write path which already calls `writePreferenceEntries` (Plan 01) which diffs against live defaults and emits an empty override map per D-06. This satisfies the "no need to delete ~/.BALLView" half of CONFIG-01 criterion 3 by construction — the user clicks OK, the diff is empty, the key is removed by Plan 01 Task 1's empty-overrides cleanup branch.

       Do NOT add a confirmation dialog or undo affordance — D-02 explicitly says "one-line clear-the-override-map operation"; the reset is reversible by the user re-customizing colors and clicking OK.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball &amp;&amp; cmake --build build/macos-homebrew --target BALLView -j 8 2>&amp;1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'void ColoringSettingsDialog::resetElementColors()' source/VIEW/DIALOGS/coloringSettingsDialog.C` returns exactly 1 (the implementation).
    - Source assertion: `grep -cE 'connect\([[:space:]]*reset_elements_button[[:space:]]*,[[:space:]]*SIGNAL\(clicked\(\)\)' source/VIEW/DIALOGS/coloringSettingsDialog.C` returns exactly 1 (the constructor wiring).
    - Source assertion: `grep -c 'element_table_->setContent(names, colors);' source/VIEW/DIALOGS/coloringSettingsDialog.C` returns at least 2 — one in `setDefaultValues_` (pre-existing) and one in the new `resetElementColors` slot.
    - Build assertion: `cmake --build build/macos-homebrew --target BALLView -j 8` succeeds (the full BALLView app, not just libVIEW, because the .ui must be re-processed by uic and the constructor connect needs the new symbol resolvable).
    - Behaviour assertion (manual, recorded in plan SUMMARY): with a `~/.BALLView` containing `ElementColorOverrides=6:33ff33ff;7:5151ffff;` (two custom overrides), launch BALLView, open Preferences → Coloring Settings → Element page. The carbon and nitrogen rows show the custom colors. Click "Reset element colors to defaults". The carbon and nitrogen rows immediately show the compiled defaults (`33ff33` for carbon, `3333ff` for nitrogen). Click OK. Inspect `~/.BALLView`: `ElementColorOverrides=` is absent from `[COLORING_OPTIONS]`. (Tri-OS CI: build-green only — behavioral verification is local until a headless dialog test exists.)
  </acceptance_criteria>
  <done>
    `resetElementColors()` is implemented in `coloringSettingsDialog.C` using the same `ElementColorProcessor` + `element_table_->setContent` pattern as `setDefaultValues_`, the constructor wires `reset_elements_button` to it, and BALLView builds cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 4: Append residue-color-persistence backlog stub to ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (the Backlog section starting at "## Backlog" around line 264; the existing entries 999.1..999.8 establish the formatting convention)
    - .planning/phases/04.1-config-color-defaults-fix/04.1-CONTEXT.md (decision D-04 + /deferred/ section — the suggested name is 999.4b)
  </read_first>
  <action>
    Per D-04: residue color persistence has the same bug pattern at
    `source/VIEW/MODELS/standardColorProcessor.C:196` (the `ResidueNameColorProcessor` ctor)
    and `source/VIEW/DIALOGS/coloringSettingsDialog.C:160-161` (the `ResidueNames=` /
    `ResidueNameColors=` write path) and `:208-215` (the residue branch of
    `applySettingsTo`). It is deferred from Phase 4.1 to keep the phase small, and
    Phase 4.1 must add a backlog stub.

    Add a new backlog entry immediately AFTER the existing `### Phase 999.4: Config shadows compiled color defaults — PROMOTED to Phase 4.1` block (around line 307-309) and BEFORE the `### Phase 999.5: Open-PR triage` block. The numeric ID convention is `999.4b` (lettered suffix for sub-promotions of the parent backlog item — established by analogy with how Phase 4.1 itself was promoted from 999.4; this preserves the chronological "this is a follow-up to 999.4" grouping without renumbering 999.5..999.8).

    Use the same Markdown structure as the surrounding backlog entries:

    ```markdown
    ### Phase 999.4b: Residue color persistence — apply Phase 4.1 fix pattern to ResidueNameColorProcessor (BACKLOG)

    **Goal:** Apply the Phase 4.1 D-01/D-03/D-05/D-06 fix pattern (diff/override
    only, legacy-key discard, key-name + defensive sanity, diff at write time)
    to the residue color persistence path so editing the compiled
    `ResidueNameColorProcessor` default table at
    [`source/VIEW/MODELS/standardColorProcessor.C:196`](../../../source/VIEW/MODELS/standardColorProcessor.C:196)
    produces visibly updated colors even when a pre-existing `~/.BALLView`
    contains the legacy `ResidueNames=`/`ResidueNameColors=` keys.
    **Why backlog, not v1.6 Phase 4.1:** same bug class, same fix pattern, but
    Phase 4.1 was scoped to element-only per D-04 to keep the change surface small
    and the migration risk concentrated. Promote when a contributor has time;
    this is a pure persistence-format change, no UX implications.
    **Scope:**
      - Mirror Phase 4.1 Plan 01 against the residue-name section of
        [`source/VIEW/DIALOGS/coloringSettingsDialog.C`](../../../source/VIEW/DIALOGS/coloringSettingsDialog.C)
        — replace the unconditional `ResidueNames=`/`ResidueNameColors=` write
        (lines 154-161) with a diff against `ResidueNameColorProcessor()` and a
        new key (e.g. `ResidueNameColorOverrides=`).
      - Mirror Phase 4.1 Plan 02 with a "Reset residue colors to defaults"
        button on the Residue_Name page of `coloringSettingsDialog.ui` (line 107
        of that file).
      - Defensive sanity check on residues is harder than the all-white check
        on elements — residue color collisions are less visually distinctive
        than C/N/O/P/S = white. Promote-time decision: which residue triples
        constitute "implausible" (probably default-name = `#ffffff` for
        GLY/ALA/VAL/LEU/ILE — though several real defaults are already pale).
    **Requirements:** TBD (new sub-requirement CONFIG-02 on promotion)
    **Reference:** `.planning/phases/04.1-config-color-defaults-fix/04.1-CONTEXT.md` /deferred/

    Plans:
    - [ ] TBD (promote with /gsd-review-backlog when ready)
    ```

    Use raw Markdown — no decorative shell-escapes; mirror the indentation,
    headings, bold-key conventions, and `[link](path)` style of the existing
    backlog entries (e.g. compare 999.6, 999.7, 999.8 which are the most
    structurally-similar dormant entries).

    Do NOT change any other phase entry, the progress table, the requirement
    list, or the trailing footer of ROADMAP.md.
  </action>
  <verify>
    <automated>grep -c '### Phase 999.4b: Residue color persistence' /Users/kohlbach/Claude/BALL/ball/.planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c '### Phase 999.4b: Residue color persistence' .planning/ROADMAP.md` returns exactly 1.
    - Source assertion: `grep -c 'ResidueNameColorProcessor' .planning/ROADMAP.md` returns at least 1 (was 0 pre-edit — new content references the class).
    - Structural assertion: the new entry sits between `### Phase 999.4:` and `### Phase 999.5:` — verify by `awk '/### Phase 999\.4/,/### Phase 999\.5/' .planning/ROADMAP.md | grep -c '999.4b'` returning at least 1.
    - The rest of ROADMAP.md is unmodified — the progress table (line 246-260 region), the requirement-coverage table (Phase 4.1 row), and all other backlog entries are byte-identical.
  </acceptance_criteria>
  <done>
    ROADMAP.md contains a new `### Phase 999.4b: Residue color persistence` backlog entry placed after 999.4 (the parent) and before 999.5, with the same Markdown conventions as the surrounding entries. The Phase 4.1 deferral (D-04) is now actionable as a backlog item.
  </done>
</task>

</tasks>

<verification>
After all four tasks land:

- **Build gate:** `cmake --build build/macos-homebrew --target BALLView -j 8` succeeds on macOS. Tri-OS CI will exercise Linux and Windows on push per Phase 5.1 D-07 carry-forward.
- **Static gates:** all `grep` and `python3 -c "...ET.parse..."` checks in the per-task acceptance criteria pass.
- **Behavioural gate (semi-automated, local):**
  1. Write `~/.BALLView` with `[COLORING_OPTIONS]\nElementColorOverrides=6:ff00ffff;7:00ff00ff;` (custom magenta carbon, custom green nitrogen — both unmistakably different from PyMOL defaults).
  2. Launch BALLView. Open Preferences → Coloring Settings → Element page. Carbon row shows magenta, nitrogen shows green.
  3. Click "Reset element colors to defaults". Carbon row shows PyMOL green `33ff33`, nitrogen shows PyMOL blue `3333ff`.
  4. Click OK on the Preferences dialog. Open `~/.BALLView` in a text editor. Section `[COLORING_OPTIONS]` MUST NOT contain `ElementColorOverrides=` (the diff against live defaults is empty; the empty-overrides cleanup branch in Plan 01 Task 1 removed the key).
  5. Re-launch BALLView. Element page carbon row STILL shows PyMOL green (the reset persisted; we didn't accidentally re-introduce a shadow copy).
- **Backlog gate:** the new 999.4b entry is visible in the ROADMAP.md "## Backlog" section and references the correct source-file line numbers (`standardColorProcessor.C:196`, `coloringSettingsDialog.C:160-161`).
</verification>

<success_criteria>
- The Coloring Settings dialog's Element page has a working "Reset element colors to defaults" button placed directly under the element color table (D-02).
- Clicking the button repopulates `element_table_` from the live `ElementColorProcessor` defaults, and the subsequent Preferences-OK write removes any `ElementColorOverrides=` line from `~/.BALLView` (CONFIG-01 criterion 3).
- ROADMAP.md has a `### Phase 999.4b: Residue color persistence` backlog stub recording the D-04 deferral.
- BALLView compiles cleanly on macOS (and on tri-OS CI once pushed).
- No regression to the residue / chain / molecule / charge / distance / temperature-factor / occupancy / secondary-structure / force / residue-type panels of the Coloring Settings dialog.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-config-color-defaults-fix/04.1-02-SUMMARY.md` per the standard SUMMARY template. Include:
- The exact placement of `reset_elements_button` in the .ui (which layout, which page).
- The local macOS build result and the tri-OS CI run URL once pushed.
- The behavioural-gate observations from steps 1-5 of the verification block.
- Any deviations from D-02 or D-04 with rationale.
- A note that Phase 4.1 is complete and the next planning step is to flip `CONFIG-01` from `Pending` to `Complete` in REQUIREMENTS.md (orchestrator handles this at phase close).
</output>
