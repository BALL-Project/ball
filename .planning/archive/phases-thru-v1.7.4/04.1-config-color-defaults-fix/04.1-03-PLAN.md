---
phase: 04.1-config-color-defaults-fix
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - include/BALL/VIEW/DIALOGS/coloringSettingsDialog.h
  - source/VIEW/DIALOGS/coloringSettingsDialog.C
autonomous: true
gap_closure: true
requirements:
  - CONFIG-01

must_haves:
  truths:
    - "ColoringSettingsDialog::readPreferenceEntries is reachable from Preferences::fetchPreferences via virtual dispatch — the derived signature now matches the base virtual `void readPreferenceEntries(const INIFile&)` and is annotated `override`, so the polymorphic call at preferences.C:174 dispatches to the derived body, not the base no-op (CR-01 closed)"
    - "All five existing `protected Q_SLOTS:` virtuals in coloringSettingsDialog.h that have no base counterpart remain unannotated; the two PreferencesEntry overrides (writePreferenceEntries, readPreferenceEntries) carry `override` so a future signature drift is caught at compile time (IN-01 closed)"
    - "The body of readPreferenceEntries — including the legacy-Elements line-iterator scan/deleteLine, the empty-overrides cleanup line-iterator scan/deleteLine, and the override-row table-update — continues to compile and run correctly under the const-INIFile parameter, via a localized `const_cast<INIFile&>(inifile)` at the mutation call sites only (no broader codebase changes)"
  artifacts:
    - path: "include/BALL/VIEW/DIALOGS/coloringSettingsDialog.h"
      provides: "Derived virtuals match base signatures; override keyword present where a base virtual exists"
      contains: "readPreferenceEntries(const INIFile& inifile) override"
    - path: "source/VIEW/DIALOGS/coloringSettingsDialog.C"
      provides: "readPreferenceEntries definition with const-INIFile parameter; const_cast at deleteLine mutation sites"
      contains: "readPreferenceEntries(const INIFile&"
  key_links:
    - from: "source/VIEW/DIALOGS/preferences.C:174 (Preferences::fetchPreferences polymorphic dispatch)"
      to: "ColoringSettingsDialog::readPreferenceEntries body in coloringSettingsDialog.C"
      via: "vtable dispatch of base virtual `void readPreferenceEntries(const INIFile&)`; derived now actually overrides this slot"
      pattern: "readPreferenceEntries\\(const INIFile&[^)]*\\)[[:space:]]*override"
    - from: "ColoringSettingsDialog::readPreferenceEntries body"
      to: "INIFile::deleteLine"
      via: "const_cast<INIFile&>(inifile).deleteLine(it) — localized const-cast at the mutation call sites only; the parameter type matches the base signature"
      pattern: "const_cast<INIFile&>\\(inifile\\)"
---

<objective>
Fix CR-01 (the gap-source-of-truth blocker): change `ColoringSettingsDialog::readPreferenceEntries` from `(INIFile&)` to `(const INIFile&)` so it actually overrides the base `PreferencesEntry::readPreferenceEntries(const INIFile&)` virtual, which makes the entire Phase 4.1 read-path body reachable from `Preferences::fetchPreferences`. Add the `override` keyword to both PreferencesEntry overrides on the dialog (read + write) so any future signature drift is caught at compile time (IN-01).

Resolve the resulting const-ness constraint locally with `const_cast<INIFile&>(inifile)` at the two `deleteLine` call sites inside the read-path body — this is the lower-impact option per CONTEXT-derived analysis: every other `PreferencesEntry` subclass in the codebase (`mainControlPreferences.h:84`, `pythonSettings.h:68`, `pluginDialog.h:103`) ALREADY uses the const signature, so changing the base virtual would be a non-fix that touches more code without fixing anything. The local const_cast is contained, well-commented, and matches the rest of the codebase's convention.

Purpose: closes CR-01 (the BLOCKER that makes the entire Phase 4.1 read path dead code) and IN-01 (the missing `override` keyword that hid CR-01 at compile time). Without this fix, every other gap closure is moot — the read path is unreachable.

Output: a 2-file edit — header signature fix + `override` annotations, and .C signature fix + 2 const_cast call-site adjustments.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-config-color-defaults-fix/04.1-CONTEXT.md
@.planning/phases/04.1-config-color-defaults-fix/04.1-VERIFICATION.md
@.planning/phases/04.1-config-color-defaults-fix/04.1-REVIEW.md
@.planning/phases/04.1-config-color-defaults-fix/04.1-01-SUMMARY.md
@CLAUDE.md

<!-- Files this plan modifies (current post-Plan-01-and-02 state) -->
@include/BALL/VIEW/DIALOGS/coloringSettingsDialog.h
@source/VIEW/DIALOGS/coloringSettingsDialog.C

<!-- Base virtual definition — confirms the const parameter is the override target -->
@include/BALL/VIEW/KERNEL/preferencesEntry.h

<!-- Polymorphic dispatch site — reads `(**it).readPreferenceEntries(inifile)` where *it is PreferencesEntry& -->
@source/VIEW/DIALOGS/preferences.C

<interfaces>
<!-- Key signatures the executor uses. Extracted directly from the codebase. -->

From `include/BALL/VIEW/KERNEL/preferencesEntry.h:108`:
- Base virtual: `virtual void readPreferenceEntries(const INIFile& inifile);`
- Base virtual: `virtual void writePreferenceEntries(INIFile& inifile);` (line 105 — note: write side correctly takes non-const, no change needed there)

From `include/BALL/VIEW/DIALOGS/coloringSettingsDialog.h:72-83` (current state):
- Line 72: `virtual void writePreferenceEntries(INIFile& inifile);`  → matches base; needs `override`
- Line 75: `virtual void readPreferenceEntries(INIFile& inifile);`   → MISMATCH (non-const); needs both signature fix AND `override`
- Lines 79-83: five protected slots (`maxDistanceChanged`, `maxTFChanged`, `forceMaxValueChanged`, `forceMinValueChanged`, `resetElementColors`) — none have a base counterpart in `PreferencesEntry`; these are pure declarations, MUST NOT receive `override`.

Confirmed via grep that ALL OTHER `PreferencesEntry` subclasses in the codebase already use the const signature:
- `include/BALL/VIEW/DIALOGS/mainControlPreferences.h:84` → `void readPreferenceEntries(const INIFile& inifile);`
- `include/BALL/VIEW/DIALOGS/pythonSettings.h:68` → `void readPreferenceEntries(const INIFile& inifile);`
- `include/BALL/VIEW/DIALOGS/pluginDialog.h:103` → `virtual void readPreferenceEntries(const INIFile& inifile);`

So the local const_cast option is the architecturally correct choice: `coloringSettingsDialog.h` is the OUTLIER — every other override matches the base. Changing the base would require touching three subclasses for no benefit. The verifier-prescribed and reviewer-recommended local fix is correct.

From `include/BALL/FORMAT/INIFile.h:223`:
- `bool deleteLine(LineIterator line_it);` — non-const member function (mutates the file). Requires a non-const `INIFile&`, hence the const_cast at the two call sites in the read-path body.

From `source/VIEW/DIALOGS/coloringSettingsDialog.C` (current post-Plan-01 state):
- Line 229: `void ColoringSettingsDialog::readPreferenceEntries(INIFile& inifile)` — definition signature; must change to `(const INIFile&)` to match the new header.
- Line 248: `inifile.deleteLine(it);` — inside the legacy-Elements discard branch (lines 240-251). MUST become `const_cast<INIFile&>(inifile).deleteLine(it);`.
- Line 231: `PreferencesEntry::readPreferenceEntries(inifile);` — the base call. The base is `void readPreferenceEntries(const INIFile&)`, so passing the const param is fine; no change needed at this call site.

The empty-overrides cleanup loop at lines 200-213 is inside `writePreferenceEntries` (parameter is non-const) — that loop is NOT affected by this plan; it stays as-is. Plan 04 will fix the sentinel in BOTH loops separately. Do NOT attempt to merge the two concerns in this plan; the gap-closure context specifies them as distinct fixes.

Const-cast comment idiom (project convention — match the inline-comment style at coloringSettingsDialog.C:192 introduced by Plan 01):
```
// Local const_cast: deleteLine mutates the INIFile (line removal). The base virtual
// signature uses `const INIFile&` for read-only consumers; legacy-key migration is the
// one mutation we perform inside the read path (D-03 optimistic discard). All other
// PreferencesEntry subclasses in VIEW/DIALOGS already use this signature; the const_cast
// is contained to this single call site.
```
Place this comment immediately above each const_cast call (only one in this plan — the legacy-discard deleteLine).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header — fix readPreferenceEntries signature to match base; add `override` to both PreferencesEntry virtuals</name>
  <files>include/BALL/VIEW/DIALOGS/coloringSettingsDialog.h</files>
  <read_first>
    - include/BALL/VIEW/DIALOGS/coloringSettingsDialog.h (the whole file — 93 lines; lines 72-83 are the public/protected virtuals)
    - include/BALL/VIEW/KERNEL/preferencesEntry.h (lines 105-108 — the two base virtual signatures)
    - include/BALL/VIEW/DIALOGS/mainControlPreferences.h (line 84 — confirms a sibling subclass already uses the const signature, so this fix is alignment, not divergence)
    - include/BALL/VIEW/DIALOGS/pluginDialog.h (line 103 — same confirmation)
    - .planning/phases/04.1-config-color-defaults-fix/04.1-VERIFICATION.md (CR-01 prescription, lines 84-118)
  </read_first>
  <action>
    Make exactly two textual changes inside the public section of `class ColoringSettingsDialog` (lines 48-75):

    1. **Line 72 — add `override` to writePreferenceEntries:**
       Replace `virtual void writePreferenceEntries(INIFile& inifile);`
       with     `virtual void writePreferenceEntries(INIFile& inifile) override;`
       (No signature change here — the base at preferencesEntry.h:105 already takes `INIFile&`. Only the `override` keyword is added.)

    2. **Line 75 — add `const` to the parameter AND add `override`:**
       Replace `virtual void readPreferenceEntries(INIFile& inifile);`
       with     `virtual void readPreferenceEntries(const INIFile& inifile) override;`

    The two slot-section virtuals (lines 79-83) MUST NOT receive `override` — `maxDistanceChanged`, `maxTFChanged`, `forceMaxValueChanged`, `forceMinValueChanged`, and `resetElementColors` are slots with no base counterpart in `PreferencesEntry`. If the executor adds `override` to any of them, the build fails with "marked 'override' but does not override". The acceptance criteria check this explicitly.

    Do NOT reformat any other line; do NOT change include guards, doxygen `///` comments, the constructor declaration, or the protected `setDefaultValues_()` declaration. The diff is exactly two lines (one keyword added on line 72, signature changed + keyword added on line 75).

    Project convention compliance: keep the existing 1-tab indentation (the file uses tabs at this nesting depth — verify by reading the raw character before `virtual` on lines 72 and 75 with `cat -A` or by visual inspection — the BALL `// vi: set ts=2:` modeline on line 1 sets the display width but the underlying chars are tabs in this header; match exactly).
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball &amp;&amp; cmake --build build/macos-homebrew --target VIEW -j 8 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -cE 'virtual void readPreferenceEntries\(const INIFile&[^)]*\)[[:space:]]*override' include/BALL/VIEW/DIALOGS/coloringSettingsDialog.h` returns exactly 1.
    - Source assertion: `grep -cE 'virtual void writePreferenceEntries\(INIFile&[^)]*\)[[:space:]]*override' include/BALL/VIEW/DIALOGS/coloringSettingsDialog.h` returns exactly 1.
    - Negative assertion: `grep -cE 'virtual void readPreferenceEntries\(INIFile&[^c)][^)]*\)' include/BALL/VIEW/DIALOGS/coloringSettingsDialog.h` returns 0 (no remaining non-const declaration of readPreferenceEntries).
    - Negative assertion: `grep -cE '(maxDistanceChanged|maxTFChanged|forceMaxValueChanged|forceMinValueChanged|resetElementColors)\(\)[[:space:]]*override' include/BALL/VIEW/DIALOGS/coloringSettingsDialog.h` returns 0 (no `override` accidentally added to slot declarations that have no base counterpart — would cause compile failure).
    - Source assertion: the four pre-existing slot declarations are unchanged — `grep -cE '(maxDistanceChanged|maxTFChanged|forceMaxValueChanged|forceMinValueChanged|resetElementColors)\(\);' include/BALL/VIEW/DIALOGS/coloringSettingsDialog.h` returns exactly 5.
    - Build assertion: AFTER Task 2 (the .C signature change) lands in the same plan, `cmake --build build/macos-homebrew --target VIEW -j 8` succeeds. NOTE: this header change alone (Task 1 only) WILL cause a compile error in coloringSettingsDialog.C until Task 2 lands (the .C definition's signature won't match the new header declaration). This is expected — the per-task verify gate after Task 1 IS expected to fail compilation; the executor must land Task 2 immediately after to restore the build, and run the build gate only after both tasks. To accommodate, the per-task `<automated>` block above runs the build but the executor should NOT treat its failure as a stop signal between Task 1 and Task 2 — proceed to Task 2 immediately and re-run the build gate after both edits.
  </acceptance_criteria>
  <done>
    `coloringSettingsDialog.h` declares `readPreferenceEntries(const INIFile&) override` and `writePreferenceEntries(INIFile&) override`, matching the base virtuals in `preferencesEntry.h:105,108`. Slots without base counterparts are unannotated. The library build is intentionally broken between Task 1 and Task 2 (signature mismatch with the .C definition) — Task 2 closes the gap and restores green build.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implementation — match the new const-INIFile signature; const_cast at the deleteLine mutation site</name>
  <files>source/VIEW/DIALOGS/coloringSettingsDialog.C</files>
  <read_first>
    - source/VIEW/DIALOGS/coloringSettingsDialog.C (the whole file post-Plan-01-and-02; line 229 is the function signature, lines 240-253 contain the legacy-discard branch with `inifile.deleteLine(it);` at line 248)
    - include/BALL/FORMAT/INIFile.h (line 223 — `bool deleteLine(LineIterator line_it);` non-const, confirms the mutation requires a non-const reference)
    - .planning/phases/04.1-config-color-defaults-fix/04.1-VERIFICATION.md (CR-01 fix prescription)
  </read_first>
  <action>
    Make exactly two textual changes inside `coloringSettingsDialog.C`:

    1. **Line 229 — change definition signature to match the new header:**
       Replace `void ColoringSettingsDialog::readPreferenceEntries(INIFile& inifile)`
       with     `void ColoringSettingsDialog::readPreferenceEntries(const INIFile& inifile)`

    2. **Line 248 — wrap the deleteLine call in a localized const_cast:**
       Replace `inifile.deleteLine(it);` (the line inside the legacy-Elements discard `if ((*it).hasPrefix("Elements="))` branch, which is the SOLE `deleteLine` call inside `readPreferenceEntries` post-Plan-01)
       with:
       ```
       // Local const_cast: deleteLine mutates the INIFile (line removal). The base virtual
       // signature uses `const INIFile&` for read-only consumers; legacy-key migration is the
       // one mutation we perform inside the read path (D-03 optimistic discard). All other
       // PreferencesEntry subclasses in VIEW/DIALOGS already use this signature; the const_cast
       // is contained to this single call site.
       const_cast<INIFile&>(inifile).deleteLine(it);
       ```
       Match the existing 5-tab leading indentation on the deleteLine line; the comment block uses `\t\t\t\t\t//` (5 tabs + `//`) per the function's nesting depth (`namespace BALL { namespace VIEW { void func() { if (...) { for (...) { if (...) { ...HERE... } } } } } }`).

    Do NOT change ANY other line in the file. Specifically:
    - The `PreferencesEntry::readPreferenceEntries(inifile);` call on line 231 stays unchanged — the base virtual takes `const INIFile&`, so passing the const param compiles cleanly without any cast.
    - All `inifile.hasEntry(...)`, `inifile.getValue(...)`, `inifile.getSectionFirstLine(...)`, `inifile.getSectionLastLine(...)` calls inside the read-path body are CONST member functions on `INIFile` (verify by grep against `include/BALL/FORMAT/INIFile.h` if uncertain — `hasEntry` is at line 234 marked `const`, `getValue` at line 248 marked `const`, `getSectionFirstLine` at line 299 also returns a non-const LineIterator but is itself a non-const member; HOWEVER on a const INIFile the compiler will dispatch to the const overload if one exists OR, if not, the call will fail to compile and we will know to introduce another targeted const_cast). In practice for the post-Plan-01 read-path body, the ONLY observed mutation is the single `deleteLine` call. If the build gate after this task fails with a const-correctness error on `getSectionFirstLine`/`getSectionLastLine`, hoist the same const_cast pattern to those call sites and re-run the build — but the expected first-pass result is success because the LineIterator returned by those getters is iterator-typed, not INIFile-mutating, and they have const overloads in the typical C++ STL-style API. Document any additional const_cast added in the SUMMARY.
    - The `element_table_->item(p, 1)->setBackground(...)` call at line 307 inside the override-application loop mutates the QTableWidget, NOT the INIFile — no const_cast needed; the dialog's `element_table_` member is not affected by `inifile`'s constness.
    - Plan 04 will separately fix the empty-overrides cleanup loop in `writePreferenceEntries` (lines 200-213); do NOT touch that block in this plan — it's inside a non-const-parameter function and has no const issue.

    The post-edit function header (line 229) is the SOLE definition of `ColoringSettingsDialog::readPreferenceEntries` in the .C file; `grep -cE 'void ColoringSettingsDialog::readPreferenceEntries' source/VIEW/DIALOGS/coloringSettingsDialog.C` MUST return exactly 1 after the edit.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball &amp;&amp; cmake --build build/macos-homebrew --target BALLView -j 8 2>&amp;1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -cE 'void ColoringSettingsDialog::readPreferenceEntries\(const INIFile& inifile\)' source/VIEW/DIALOGS/coloringSettingsDialog.C` returns exactly 1.
    - Negative assertion: `grep -cE 'void ColoringSettingsDialog::readPreferenceEntries\(INIFile&[^c)]' source/VIEW/DIALOGS/coloringSettingsDialog.C` returns 0 (no remaining non-const definition).
    - Source assertion: `grep -cE 'const_cast<INIFile&>\(inifile\)\.deleteLine' source/VIEW/DIALOGS/coloringSettingsDialog.C` returns exactly 1 (the one mutation site inside readPreferenceEntries' legacy-Elements branch). If the executor had to introduce additional const_casts to satisfy const-correctness on iterator getters, this count may be higher — document each in the SUMMARY with a justification.
    - Source assertion: `grep -cE 'inifile\.deleteLine\(' source/VIEW/DIALOGS/coloringSettingsDialog.C` returns exactly 1 (the one in the writePreferenceEntries empty-overrides cleanup loop at line ~209, which is unaffected by this plan and stays as a bare `inifile.deleteLine(it);` because writePreferenceEntries' parameter is non-const).
    - Build assertion: `cmake --build build/macos-homebrew --target BALLView -j 8` succeeds (full BALLView app — confirms libVIEW compiles AND the application linker resolves the new vtable slot). Zero compile errors. Pre-existing macOS OpenGL deprecation warnings unchanged.
    - Symbol-level assertion (post-build, optional but recommended): `nm build/macos-homebrew/libVIEW.dylib 2>/dev/null | grep -c 'ColoringSettingsDialog.*readPreferenceEntries' || nm build/macos-homebrew/source/VIEW/libVIEW.a 2>/dev/null | grep -c 'ColoringSettingsDialog.*readPreferenceEntries'` returns at least 1 — confirms the override is exported and discoverable in the built artifact. (Either dylib or static archive path may exist depending on cmake config; try both.) The exact mangled name will include `K7INIFile` (the K=const indicator in Itanium ABI mangling) AFTER this fix; pre-fix it would have been `7INIFile`. This symbolic check is the strongest mechanical evidence that the override is in the same vtable slot as the base.
    - Behaviour assertion (manual, recorded in plan SUMMARY): with a hand-written `~/.BALLView` containing `[COLORING_OPTIONS]\nElements=ffffffff;ffffffff;...`, launch BALLView. Observe that the Log emits "ColoringSettingsDialog: dropping legacy Elements= block (Phase 4.1 migration; ...)" — this log line was previously NEVER emitted (CR-01 confirmed dead code); after this plan it MUST be emitted on first launch with a legacy file. Quit BALLView. Inspect `~/.BALLView`: section `[COLORING_OPTIONS]` should NOT contain an `Elements=` line (deleted by the now-reachable read path). NOTE: this manual check exposes the CR-02 sentinel bug if `Elements=` is the LAST line of the section — if the legacy `Elements=` is followed by other keys (like `ResidueNames=`), the deletion will succeed and the manual check passes; if `Elements=` is the last line, the deletion silently fails (CR-02), the line survives, the log fires (it's now inside the if-branch already... wait, actually post-Plan-01 the log fires unconditionally outside the for loop, so it fires regardless — the misleading-log issue WR-02 will be fully addressed when Plan 04 moves the log inside the matched branch). For this plan's behaviour gate, ARRANGE the test file to have `Elements=` followed by another key (e.g. `ResidueNames=foo;`) so CR-02 doesn't mask the CR-01 fix verification. Plan 05 will exercise the all-line-positions test once CR-02 is closed.
  </acceptance_criteria>
  <done>
    `ColoringSettingsDialog::readPreferenceEntries` is now defined as `(const INIFile&)`, matching the base virtual and the post-Task-1 header. The single `deleteLine` mutation in the read-path body is wrapped in a localized `const_cast<INIFile&>(inifile)` with an inline-comment explanation. BALLView builds cleanly and the symbol-level check confirms the override is in the proper vtable slot. The behaviour gate confirms the read path is now reachable (legacy `Elements=` followed by another key is deleted on launch and the log fires).
  </done>
</task>

</tasks>

<verification>
After both tasks land:

- **Build gate:** `cmake --build build/macos-homebrew --target BALLView -j 8` succeeds. Tri-OS CI will exercise Linux/Windows on push.
- **Static gates:** all `grep`-based assertions in the per-task acceptance criteria pass.
- **Symbol gate (mandatory recording in SUMMARY):** the executor records the result of `nm build/macos-homebrew/source/VIEW/libVIEW.* 2>/dev/null | grep 'ColoringSettingsDialog.*readPreferenceEntries' | c++filt`. The demangled output MUST show `ColoringSettingsDialog::readPreferenceEntries(BALL::INIFile const&)` (note: `const&`). If the demangled output shows `BALL::INIFile&` (non-const), the override is still wrong — CR-01 is not closed.
- **Behaviour gate (manual, recorded in SUMMARY):** as described in Task 2 acceptance criteria — write a `~/.BALLView` with `Elements=` NOT as the last line of `[COLORING_OPTIONS]`, launch BALLView, confirm the log fires and the `Elements=` line is removed from the file after quit. (CR-02 will be closed in Plan 04, after which the all-positions case can be tested.)
- **Regression gate:** the pre-existing read-path test from the original Plan 01's behaviour gate (the upsert test, the all-white test) continues to behave the same — Plan 03 only changes the dispatch mechanism (now reachable), not the read-path body's logic. The all-white check at lines 285-298 is now also reachable; verify by writing `ElementColorOverrides=6:ffffffff;` and confirming the Log emits "dropping suspicious all-white override for atomic number 6". (Pre-Plan-03 this check was unreachable; post-Plan-03 it fires.)
- **Plan-04-readiness gate:** confirm the cleanup loop in `writePreferenceEntries` (lines 200-213) is UNCHANGED — Plan 04 owns that block. `git diff HEAD~ -- source/VIEW/DIALOGS/coloringSettingsDialog.C` should show changes ONLY in the `readPreferenceEntries` body and its signature line, not in `writePreferenceEntries`.
</verification>

<success_criteria>
- `coloringSettingsDialog.h` declares `readPreferenceEntries(const INIFile&) override` matching base; `writePreferenceEntries(INIFile&) override` carries `override` for symmetry.
- `coloringSettingsDialog.C` defines `readPreferenceEntries(const INIFile&)` and uses a localized `const_cast<INIFile&>(inifile).deleteLine(it)` at the legacy-Elements mutation call site, with an inline-comment justifying the cast.
- BALLView compiles cleanly on macOS-Homebrew (local) and on tri-OS CI once pushed.
- Symbol gate confirms the override is in the same vtable slot as the base virtual (`InvalidFormat const&` mangling on the `inifile` parameter).
- Behaviour gate confirms the read path is now reachable (legacy `Elements=` line is deleted from a test `~/.BALLView` after launch+quit, the log fires, and the previously-unreachable all-white sanity check now executes).
- No changes outside the two files in `files_modified`.
- Plan 04 (CR-02 sentinel) and Plan 05 (CR-03 parse hardening) are unblocked: CR-02 can run independently in the next wave; CR-03 now has a runtime-reachable target to harden.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-config-color-defaults-fix/04.1-03-SUMMARY.md` per the standard SUMMARY template. Include:
- The exact post-edit line numbers of the two header virtuals (`readPreferenceEntries`, `writePreferenceEntries`) and the .C function signature.
- The post-edit line number of the const_cast call site, plus a count of any additional const_casts the executor had to introduce on iterator-getter call sites if the first-pass build surfaced any.
- The full demangled output of the symbol-level check (`nm | c++filt | grep readPreferenceEntries`).
- The local macOS build result (PASS/FAIL) and the tri-OS CI run URL once pushed.
- The behaviour-gate observation: legacy-Elements-NOT-last-line scenario — was the line deleted, did the log fire?
- The all-white-check observation: did the previously-unreachable `Log.warn() << "dropping suspicious all-white override"` line fire when an `ElementColorOverrides=6:ffffffff;` test value was loaded?
- Any deviation from the local-const_cast strategy (e.g. if the executor discovered that hoisting const_casts to the iterator getters cascaded too far and chose to revert the parameter signature instead — this is a design escape hatch, NOT the expected path; document the rationale exhaustively if used).
- Confirmation that Plan 04's territory (`writePreferenceEntries` lines 200-213) was untouched.
</output>
