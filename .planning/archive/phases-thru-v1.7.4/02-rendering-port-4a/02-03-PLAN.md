---
phase: 02-rendering-port-4a
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - source/VIEW/RENDERING/renderSetup.C
  - include/BALL/VIEW/RENDERING/renderSetup.h
  - source/VIEW/RENDERING/RENDERERS/glRenderer.C
  - source/VIEW/RENDERING/glOffscreenTarget.C
  - include/BALL/VIEW/RENDERING/glOffscreenTarget.h
autonomous: true
requirements: [RENDER-04, RENDER-07]
must_haves:
  truths:
    - "renderSetup.C uses QOpenGLContext::currentContext() and QOpenGLWidget::grabFramebuffer(); no QGLContext / grabFrameBuffer references remain"
    - "The raytracer worker loop never calls makeCurrent() on the widget — GL-context operations are GUI-thread-only"
    - "glRenderer.C replaces the removed QGLWidget::convertToGLFormat with QImage::convertToFormat(Format_RGBA8888).mirrored()"
    - "glOffscreenTarget uses QOpenGLFramebufferObject instead of QGLPixelBuffer"
  artifacts:
    - path: "source/VIEW/RENDERING/renderSetup.C"
      provides: "GUI-thread-only makeCurrent helper, QOpenGLContext / grabFramebuffer usage"
      contains: "QOpenGLContext"
    - path: "source/VIEW/RENDERING/RENDERERS/glRenderer.C"
      provides: "convertToGLFormat replacement for label-texture upload"
      contains: "Format_RGBA8888"
    - path: "source/VIEW/RENDERING/glOffscreenTarget.C"
      provides: "QOpenGLFramebufferObject-based offscreen target"
      contains: "QOpenGLFramebufferObject"
  key_links:
    - from: "renderSetup.C raytracer run() loop"
      to: "GL context"
      via: "no makeCurrent() on the worker thread — raytracer is CPU-only, output reaches screen via paintGL texture blit"
      pattern: "renderToBuffer"
    - from: "glRenderer.C label upload"
      to: "glDrawPixels"
      via: "QImage::convertToFormat(Format_RGBA8888).mirrored()"
      pattern: "Format_RGBA8888"
---

<objective>
Clean the renderer-side files that consume GLRenderWindow's now-changed API:
renderSetup.{h,C} (context-current check, worker-thread makeCurrent removal,
grabFramebuffer rename), glRenderer.C (the one convertToGLFormat call), and
glOffscreenTarget.{h,C} (QGLPixelBuffer -> QOpenGLFramebufferObject).

Purpose: after Plan 02 swapped the base class, these files still reference removed
Qt GL symbols and will not compile. This plan makes the renderer subsystem (minus
scene.C, which is Plan 04) build clean and keeps the raytracer's CPU-buffer path
working without any worker-thread GL. Scope here is informed by 02-A1-FINDINGS.md —
if A1 was REFUTED, STOP and escalate before doing the threading edits.
Output: renderSetup, glRenderer, glOffscreenTarget compile clean against QOpenGLWidget.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rendering-port-4a/02-RESEARCH.md
@.planning/phases/02-rendering-port-4a/02-VALIDATION.md
@.planning/phases/02-rendering-port-4a/02-A1-FINDINGS.md
@.planning/phases/02-rendering-port-4a/02-02-SUMMARY.md
@source/VIEW/RENDERING/renderSetup.C
@include/BALL/VIEW/RENDERING/renderSetup.h
@source/VIEW/RENDERING/RENDERERS/glRenderer.C
@source/VIEW/RENDERING/glOffscreenTarget.C
@include/BALL/VIEW/RENDERING/glOffscreenTarget.h

<interfaces>
<!-- Migration contract from 02-RESEARCH.md §2, §5, §6. Use verbatim. -->

renderSetup.C (§2):
  renderSetup.C:272  QGLContext::currentContext() != gl_target_->context()
                     -> QOpenGLContext::currentContext() != gl_target_->context()
                        (QOpenGLWidget::context() returns QOpenGLContext*)
  renderSetup.C:273  gl_target_->makeCurrent() — KEEP for the GLRenderer path (it is GUI-thread).
                     The raytracer worker (RenderSetup::run() only calls renderToBuffer_() for
                     !gl_renderer_) must NEVER reach a widget makeCurrent(). Per A1 the raytracer
                     issues no GL, so RenderSetup::makeCurrent() becomes a GUI-thread-only helper
                     and run() simply never calls it.
  renderSetup.C makeCurrent() body -> see RESEARCH.md "Code Examples":
      void RenderSetup::makeCurrent() {
        if (gl_target_ && QOpenGLContext::currentContext() != gl_target_->context())
          gl_target_->makeCurrent();
        else
          target->prepareRendering();
      }
  makeCurrent() call sites at lines 130,165,414,485,499,511,532,543,558,574,583,591,601:
     audit each — GUI-thread sites (init, resize, exportPNG, grid/texture setup) STAY; any
     reached from RenderSetup::run()'s worker loop must GO. Per RESEARCH.md the worker never
     needs it in practice.
  renderSetup.C:415  gl_target_->grabFrameBuffer(true) -> gl_target_->grabFramebuffer()
                     (no argument, returns QImage, GUI-thread + context-current)
  renderSetup.h:45   RenderSetup : public QThread — UNCHANGED (raytracer still uses the thread;
                     the thread just never touches GL)

glRenderer.C (§5):
  glRenderer.C:1155-1158:
     QImage gldata = QGLWidget::convertToGLFormat(pm); then glDrawPixels(...)
     -> QImage gldata = pm.convertToFormat(QImage::Format_RGBA8888).mirrored(); then glDrawPixels(...)
     (convertToGLFormat did format-convert + vertical flip; RGBA8888 = GL byte order on
      little-endian (all 3 targets), .mirrored() = the flip. Pitfall 7: verify visually on a label.)

glOffscreenTarget.{h,C} (§6, recommended QOpenGLFramebufferObject rework — A5 discretionary
but recommended; if FBO semantics block the tiling export path, a documented defer is
acceptable — note it in the SUMMARY):
  #include <QtOpenGL/QGLPixelBuffer>  -> #include <QtGui/QOpenGLFramebufferObject>
  QGLPixelBuffer::hasOpenGLPbuffers() (lines 24,50) -> remove the gate, FBOs are universally
     available on the target GL versions (always-FBO path)
  new QGLPixelBuffer(width,height, share_from_->format(), share_from_) (line 43)
     -> new QOpenGLFramebufferObject(width, height, QOpenGLFramebufferObject::CombinedDepthStencil)
        (requires the share context current)
  pixel_buffer_->makeCurrent() (line 32) -> share_from_->makeCurrent(); then fbo->bind()
  share_from_->grabFrameBuffer() (lines 58,68,79) -> share_from_->grabFramebuffer()
  pixel_buffer_->toImage() (lines 56,66,77) -> fbo->toImage()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port renderSetup.{h,C} — QOpenGLContext, grabFramebuffer, GUI-thread-only makeCurrent</name>
  <read_first>
    - .planning/phases/02-rendering-port-4a/02-A1-FINDINGS.md (CHECK THE VERDICT FIRST — if A1 REFUTED, stop and escalate)
    - source/VIEW/RENDERING/renderSetup.C (whole file — run() loop ~278-335, makeCurrent ~269-276, all the listed makeCurrent call sites)
    - include/BALL/VIEW/RENDERING/renderSetup.h
    - .planning/phases/02-rendering-port-4a/02-RESEARCH.md (§2, § "Threading Rework", Pitfall 3)
  </read_first>
  <files>source/VIEW/RENDERING/renderSetup.C, include/BALL/VIEW/RENDERING/renderSetup.h</files>
  <action>
    First: read 02-A1-FINDINGS.md. If the verdict is `A1 REFUTED`, STOP — the raytracer worker
    needs a shared GL context on a QOffscreenSurface, which is out of this plan's scope; surface
    this to the orchestrator and do not proceed.
    If `A1 CONFIRMED`, apply per &lt;interfaces&gt;:
    - Replace `QGLContext::currentContext()` with `QOpenGLContext::currentContext()` (renderSetup.C:272);
      add `#include <QtGui/QOpenGLContext>` if not present.
    - Rewrite `RenderSetup::makeCurrent()` to the GUI-thread-only form from &lt;interfaces&gt;.
    - Audit every `makeCurrent()` call site (lines ~130,165,414,485,499,511,532,543,558,574,583,591,601):
      keep the ones on GUI-thread paths (init, resize, exportPNG, grid/texture setup); remove any that
      `RenderSetup::run()`'s worker loop can reach. Per RESEARCH.md the worker loop only calls
      `renderToBuffer_()` for `!gl_renderer_` and the raytracer issues no GL — so the worker should
      end up calling no widget makeCurrent() at all. Add a short comment at `run()` noting GL stays
      on the GUI thread.
    - Replace `gl_target_->grabFrameBuffer(true)` with `gl_target_->grabFramebuffer()` (renderSetup.C:415).
    - Leave `RenderSetup : public QThread` unchanged.
    - Remove any now-unused `<QtOpenGL/...>` includes.
  </action>
  <verify>
    <automated>! grep -nE "QGLContext|grabFrameBuffer|QtOpenGL/qgl" source/VIEW/RENDERING/renderSetup.C include/BALL/VIEW/RENDERING/renderSetup.h && grep -nc "QOpenGLContext" source/VIEW/RENDERING/renderSetup.C</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "QGLContext|QGLWidget|QGLFormat|QtOpenGL/qgl" source/VIEW/RENDERING/renderSetup.C include/BALL/VIEW/RENDERING/renderSetup.h` returns no hits.
    - `grep -c "grabFrameBuffer" source/VIEW/RENDERING/renderSetup.C` == 0; `grep -c "grabFramebuffer" source/VIEW/RENDERING/renderSetup.C` >= 1.
    - `grep -c "QOpenGLContext::currentContext" source/VIEW/RENDERING/renderSetup.C` >= 1.
    - The SUMMARY records which makeCurrent() call sites were kept vs removed and why.
  </acceptance_criteria>
  <done>renderSetup.{h,C} use the QOpenGL* API, the worker loop touches no widget GL context, and grabFramebuffer is the no-arg form.</done>
</task>

<task type="auto">
  <name>Task 2: Port glRenderer.C convertToGLFormat and glOffscreenTarget.{h,C} to QOpenGLFramebufferObject; build VIEW</name>
  <read_first>
    - source/VIEW/RENDERING/RENDERERS/glRenderer.C (around line 1155 — the convertToGLFormat + glDrawPixels block)
    - source/VIEW/RENDERING/glOffscreenTarget.C (whole file)
    - include/BALL/VIEW/RENDERING/glOffscreenTarget.h
    - .planning/phases/02-rendering-port-4a/02-RESEARCH.md (§5, §6, Pitfall 7, Assumptions Log A5)
  </read_first>
  <files>source/VIEW/RENDERING/RENDERERS/glRenderer.C, source/VIEW/RENDERING/glOffscreenTarget.C, include/BALL/VIEW/RENDERING/glOffscreenTarget.h</files>
  <action>
    Apply per &lt;interfaces&gt;:
    - glRenderer.C (~1155): replace `QImage gldata = QGLWidget::convertToGLFormat(pm);` with
      `QImage gldata = pm.convertToFormat(QImage::Format_RGBA8888).mirrored();` — the following
      `glDrawPixels(...)` call stays. Remove any `<QtOpenGL/...>`/`QGLWidget` include left dangling.
      Pitfall 7: this must do BOTH the format conversion and the vertical flip.
    - glOffscreenTarget.h/.C: replace `#include <QtOpenGL/QGLPixelBuffer>` with
      `#include <QtGui/QOpenGLFramebufferObject>`. Replace the `QGLPixelBuffer::hasOpenGLPbuffers()`
      gate (lines ~24,50) with an always-FBO path. Replace
      `new QGLPixelBuffer(width,height, share_from_->format(), share_from_)` (~43) with
      `new QOpenGLFramebufferObject(width, height, QOpenGLFramebufferObject::CombinedDepthStencil)`.
      Replace `pixel_buffer_->makeCurrent()` (~32) with `share_from_->makeCurrent();` then `fbo->bind();`.
      Replace `share_from_->grabFrameBuffer()` (~58,68,79) with `share_from_->grabFramebuffer()`.
      Replace `pixel_buffer_->toImage()` (~56,66,77) with `fbo->toImage()`. Update the member type
      and its name if helpful (e.g. `pixel_buffer_` -> an `QOpenGLFramebufferObject*`).
      If a genuine FBO-vs-pbuffer semantic blocker appears in the tiling-export path (A5), a
      documented defer-with-guard for that one path is acceptable — record it in the SUMMARY; do
      not let it block the rest of the file.
    - Then run `cd build && make VIEW -j8`. With Plan 02 done and renderSetup/glRenderer/glOffscreenTarget
      now ported, the only file still expected to fail is scene.C (Plan 04). If scene.C is the sole
      remaining failure, that is the expected state for this plan.
  </action>
  <verify>
    <automated>cd build && make VIEW -j8 2>&1 | tee /tmp/02-03-build.log; grep -nE "QGLPixelBuffer|QGLWidget|convertToGLFormat|grabFrameBuffer|QtOpenGL/qgl" ../source/VIEW/RENDERING/RENDERERS/glRenderer.C ../source/VIEW/RENDERING/glOffscreenTarget.C ../include/BALL/VIEW/RENDERING/glOffscreenTarget.h; echo "grep-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "QGLPixelBuffer|QGLWidget|QGLFormat|QGLContext|QGL::|convertToGLFormat|grabFrameBuffer|QtOpenGL/qgl" source/VIEW/RENDERING/RENDERERS/glRenderer.C source/VIEW/RENDERING/glOffscreenTarget.C include/BALL/VIEW/RENDERING/glOffscreenTarget.h` returns no hits.
    - `grep -c "Format_RGBA8888" source/VIEW/RENDERING/RENDERERS/glRenderer.C` >= 1 and `.mirrored()` is present on the same conversion.
    - `grep -c "QOpenGLFramebufferObject" source/VIEW/RENDERING/glOffscreenTarget.C` >= 1.
    - `make VIEW -j8` build log: glRenderer.C, glOffscreenTarget.C, renderSetup.C all compile without error; the ONLY remaining compile failure (if any) is scene.C — confirmed in /tmp/02-03-build.log and noted in the SUMMARY.
  </acceptance_criteria>
  <done>glRenderer.C and glOffscreenTarget.{h,C} are free of legacy Qt GL symbols and compile; the renderer subsystem builds with scene.C as the only known remaining failure.</done>
</task>

</tasks>

<verification>
- renderSetup.{h,C}, glRenderer.C, glOffscreenTarget.{h,C} contain zero `QGLContext`/`QGLWidget`/`QGLFormat`/`QGLPixelBuffer`/`convertToGLFormat`/`grabFrameBuffer` references.
- The raytracer worker path issues no widget GL context call (A1-confirmed scope).
- `make VIEW -j8` compiles all of this plan's files; only scene.C may still fail (handed to Plan 04).
- RENDER-04 (raytracer renders) is structurally preserved but its visual confirmation is the human gate in Plan 04.
</verification>

<success_criteria>
- The renderer subsystem (renderSetup, glRenderer, glOffscreenTarget) compiles against QOpenGLWidget.
- The raytracer's CPU-buffer path is intact with no worker-thread GL.
- Offscreen rendering runs on QOpenGLFramebufferObject.
</success_criteria>

<output>
After completion, create `.planning/phases/02-rendering-port-4a/02-03-SUMMARY.md`.
Record: makeCurrent() call sites kept/removed; any A5 FBO defer; confirmation that scene.C is the sole remaining build failure.
</output>
