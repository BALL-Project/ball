---
phase: 02-rendering-port-4a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-rendering-port-4a/scripts/check-no-legacy-gl-symbols.sh
  - .planning/phases/02-rendering-port-4a/02-A1-FINDINGS.md
autonomous: true
requirements: [RENDER-01, RENDER-03, RENDER-07]
must_haves:
  truths:
    - "A scripted grep gate exists that fails (non-zero exit) when any legacy Qt GL symbol is present in source/VIEW or include/BALL/VIEW"
    - "Open Question A1 is resolved: it is documented whether the raytracer/BufferedRenderer worker path issues any GL calls"
  artifacts:
    - path: ".planning/phases/02-rendering-port-4a/scripts/check-no-legacy-gl-symbols.sh"
      provides: "Reusable grep symbol gate for RENDER-01/03/07 regression detection"
      min_lines: 15
    - path: ".planning/phases/02-rendering-port-4a/02-A1-FINDINGS.md"
      provides: "Resolution of RESEARCH.md Open Question A1 (raytracer GL usage)"
      min_lines: 10
  key_links:
    - from: "check-no-legacy-gl-symbols.sh"
      to: "source/VIEW + include/BALL/VIEW"
      via: "grep -rn over the legacy symbol set"
      pattern: "QGLWidget.*QGLFormat.*QGLContext"
---

<objective>
Create the Wave 0 verification assets the rest of Phase 2 depends on: a scripted
"no legacy Qt GL symbols" grep gate, and a written resolution of RESEARCH.md
Open Question A1 (does the raytracer worker thread touch GL?).

Purpose: RENDER-01/03/07 are grep-automatable — a reusable gate catches regressions
in every later wave. A1 must be answered BEFORE the threading cleanup in Plan 03/04,
because if the raytracer issues GL on the worker thread the threading scope grows
significantly (shared context on a QOffscreenSurface instead of a simple removal).
Output: a shell script + a findings markdown file. No source code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rendering-port-4a/02-RESEARCH.md
@.planning/phases/02-rendering-port-4a/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the no-legacy-Qt-GL-symbols grep gate script</name>
  <read_first>
    - .planning/phases/02-rendering-port-4a/02-VALIDATION.md (Wave 0 Requirements + Per-Task Verification Map)
    - .planning/phases/02-rendering-port-4a/02-RESEARCH.md (§ "Validation Architecture" → Phase Requirements → Test Map)
  </read_first>
  <files>.planning/phases/02-rendering-port-4a/scripts/check-no-legacy-gl-symbols.sh</files>
  <action>
    Create an executable shell script at
    `.planning/phases/02-rendering-port-4a/scripts/check-no-legacy-gl-symbols.sh`.
    The script must:
    - Run from the repo root (`cd` to the script's `../../../../..` or accept repo root as $1; resolve robustly).
    - `grep -rn` for the legacy symbol set across `source/VIEW` and `include/BALL/VIEW`:
      `QGLWidget`, `QGLFormat`, `QGLContext`, `QGL::`, `QGLPixelBuffer`, `QtOpenGL/qgl`,
      `swapBuffers`, `setAutoBufferSwap`, `safeBufferSwap`, `grabFrameBuffer`,
      `convertToGLFormat`.
    - Print every offending file:line:match.
    - Exit 0 if zero hits, exit 1 if any hit (so it works as a CI/lint gate).
    - At creation time it WILL exit 1 — that is expected and correct (the port has not happened yet).
      Print a one-line summary: "<N> legacy Qt GL symbol references remain".
    - `chmod +x` the script.
    Use a single combined `grep -rEn` alternation pattern; quote it so the shell does not glob.
  </action>
  <verify>
    <automated>test -x .planning/phases/02-rendering-port-4a/scripts/check-no-legacy-gl-symbols.sh && bash .planning/phases/02-rendering-port-4a/scripts/check-no-legacy-gl-symbols.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-rendering-port-4a/scripts/check-no-legacy-gl-symbols.sh` exists and is executable (`test -x` succeeds).
    - Running it now exits non-zero AND prints offending file:line matches (the port is not done yet, so legacy symbols exist).
    - `grep -c "QGLWidget" .planning/phases/02-rendering-port-4a/scripts/check-no-legacy-gl-symbols.sh` returns >= 1 (symbol set is encoded in the script).
  </acceptance_criteria>
  <done>The script exists, is executable, scans the two trees for all 11 legacy symbols, and returns a non-zero exit with a file:line listing on the current (pre-port) tree.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve Open Question A1 — does the raytracer worker path touch GL?</name>
  <read_first>
    - .planning/phases/02-rendering-port-4a/02-RESEARCH.md (§ "Threading Rework", § "Open Questions" item 1, Assumptions Log A1)
    - include/BALL/VIEW/RENDERING/RENDERERS/bufferedRenderer.h
    - source/VIEW/RENDERING/renderSetup.C (the run() loop ~lines 278-335 and renderToBuffer_ ~378-381)
  </read_first>
  <files>.planning/phases/02-rendering-port-4a/02-A1-FINDINGS.md</files>
  <action>
    Determine whether the raytracer / BufferedRenderer worker-thread render path issues
    any OpenGL calls. Concretely:
    - Read `include/BALL/VIEW/RENDERING/RENDERERS/bufferedRenderer.h` and its `.C` if present.
    - Find and read `BufferedRenderer::renderToBuffer` (search `source/VIEW/RENDERING/RENDERERS/`).
    - grep the raytracer/buffered renderer sources for GL call prefixes:
      `grep -rEn "gl[A-Z]|glu[A-Z]|GLEW|QOpenGL|makeCurrent" source/VIEW/RENDERING/RENDERERS/bufferedRenderer.* source/VIEW/RENDERING/RENDERERS/tilingRenderer.*`
    - Also confirm `RenderSetup::run()` only calls `renderToBuffer_()` for `!gl_renderer_`
      (RESEARCH.md cites renderSetup.C:308-309).
    Write the findings to `.planning/phases/02-rendering-port-4a/02-A1-FINDINGS.md` with:
    - A clear VERDICT line: `A1 CONFIRMED` (raytracer worker issues no GL — simple removal path)
      or `A1 REFUTED` (raytracer worker DOES issue GL — escalate: threading scope grows, needs
      QOffscreenSurface + shared context; flag to the orchestrator before Plan 03/04 proceed).
    - The grep evidence (file:line of any GL calls found, or "none found").
    - A one-paragraph implication note for Plan 03 (renderSetup.C cleanup) and Plan 04 (scene.C threading).
    Do NOT modify any source files in this task — this is read-and-document only.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-rendering-port-4a/02-A1-FINDINGS.md && grep -qE "A1 (CONFIRMED|REFUTED)" .planning/phases/02-rendering-port-4a/02-A1-FINDINGS.md && echo "findings present"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/02-rendering-port-4a/02-A1-FINDINGS.md` exists.
    - It contains exactly one VERDICT line matching `A1 CONFIRMED` or `A1 REFUTED`.
    - It cites grep evidence (file:line references or an explicit "none found").
    - It contains an implication note naming Plan 03 and Plan 04.
    - No files under `source/` or `include/` were modified (`git status --porcelain source include` shows nothing, if a git repo; otherwise note in summary).
  </acceptance_criteria>
  <done>A1 is answered in writing with grep evidence and a verdict; the threading-cleanup plans have a definitive scope signal.</done>
</task>

</tasks>

<verification>
- `check-no-legacy-gl-symbols.sh` exists, is executable, and runs (non-zero now, by design).
- `02-A1-FINDINGS.md` exists with a CONFIRMED/REFUTED verdict and grep evidence.
- No source code modified in this plan — it produces verification assets only.
- If A1 is REFUTED, the executor surfaces it to the orchestrator: Plans 03/04 threading scope must be revisited.
</verification>

<success_criteria>
- A reusable grep gate exists for RENDER-01/03/07 regression detection in all later waves.
- The single biggest threading uncertainty (A1) is resolved before any threading code is touched.
</success_criteria>

<output>
After completion, create `.planning/phases/02-rendering-port-4a/02-01-SUMMARY.md`.
</output>
