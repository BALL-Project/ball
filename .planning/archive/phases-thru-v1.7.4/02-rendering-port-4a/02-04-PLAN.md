---
phase: 02-rendering-port-4a
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - source/VIEW/WIDGETS/scene.C
autonomous: false
requirements: [RENDER-02, RENDER-06, RENDER-07, RENDER-08]
must_haves:
  truths:
    - "scene.C contains zero QGLWidget / QGLFormat / QGLContext / QGLPixelBuffer / swapBuffers / safeBufferSwap / grabFrameBuffer references and compiles clean"
    - "The stereo-capability probe is rewritten with QSurfaceFormat + a transient QOpenGLContext and still sets gl_format_"
    - "The manual cross-renderer buffer-swap synchronisation block is replaced with update() calls"
    - "Scene::updateGL() and its ~25 call sites are left untouched (it is Scene's own QWidget method, not QGLWidget::updateGL)"
    - "The top-level Qt::Window stereo / multi-display GLRenderWindow paths are guarded or stubbed behind a runtime check, documented as deferred"
    - "[HUMAN-GATED] BALLView launches and a molecule renders embedded in the main window on macOS; rotate/zoom/pick/select work; raytracer output and on-screen text appear"
  artifacts:
    - path: "source/VIEW/WIDGETS/scene.C"
      provides: "Ported Scene widget — capability probe, swap-sync removal, stereo guard"
      contains: "QSurfaceFormat"
  key_links:
    - from: "scene.C stereo-capability probe (~3063-3082)"
      to: "GLRenderWindow::gl_format_"
      via: "QOpenGLContext::create() + format().stereo() check sets the static format"
      pattern: "QSurfaceFormat"
    - from: "scene.C handleRenderToBufferFinishedEvent_ / swap block (~1644-1672)"
      to: "GLRenderWindow paintGL"
      via: "update() instead of swapBuffers()/safeBufferSwap()"
      pattern: "update\\(\\)"
---

<objective>
Port the last and most central file, source/VIEW/WIDGETS/scene.C: rewrite the
QGLWidget/QGLFormat stereo-capability probe with QSurfaceFormat + QOpenGLContext,
delete the manual cross-renderer buffer-swap synchronisation, rename grabFrameBuffer,
and guard/defer the high-risk top-level Qt::Window stereo / multi-display GLRenderWindow
paths. Then a human verifies the running GUI against the smoke checklist.

Purpose: scene.C is the consumer that ties GLRenderWindow into the Scene widget; it is
touched by both the threading cleanup and the stereo-window risk. Per RESEARCH.md the
two top-level Qt::Window paths are the single highest-risk item — they are guarded/
deferred, not fully ported, because the Core Value is the embedded scene, not stereo.
This plan ends with the mandatory human smoke check (RENDER-02/04/05/06) — there is no
automated GL/GUI test harness and none is built this phase.
Output: scene.C compiles clean; a full clean VIEW+BALLView build; a human-verified
embedded molecule render on macOS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rendering-port-4a/02-RESEARCH.md
@.planning/phases/02-rendering-port-4a/02-VALIDATION.md
@.planning/phases/02-rendering-port-4a/02-02-SUMMARY.md
@.planning/phases/02-rendering-port-4a/02-03-SUMMARY.md
@source/VIEW/WIDGETS/scene.C
@BUILD-macos.md

<interfaces>
<!-- Migration contract from 02-RESEARCH.md §4, § "Threading Rework", Pitfall 6. Use verbatim. -->

scene.C call-site map (§4, all line numbers from a verified read):
  scene.C:86     #include <QtOpenGL/QGLPixelBuffer> -> DROP (glOffscreenTarget no longer uses pbuffers;
                 if a type is still needed use <QtGui/QOpenGLFramebufferObject>)
  scene.C:3063-3082  the stereo-capability probe:
       QGLFormat test_format(QGL::DepthBuffer | QGL::StereoBuffers | QGL::DoubleBuffer);
       QGLWidget* gl_test = new QGLWidget(test_format, 0);
       gl_test->makeCurrent(); bool supports = gl_test->isValid();
     -> rewrite with QSurfaceFormat + a transient QOpenGLContext:
       QSurfaceFormat test_format;
       test_format.setStereo(true);
       test_format.setDepthBufferSize(24);
       test_format.setSwapBehavior(QSurfaceFormat::DoubleBuffer);
       QOpenGLContext ctx; ctx.setFormat(test_format);
       bool supports = ctx.create() && ctx.format().stereo();
     This block sets GLRenderWindow::gl_format_ — KEEP that contract (still assign gl_format_).
  scene.C:2096   main_display_->grabFrameBuffer() -> main_display_->grabFramebuffer() (no args, GUI thread)
  scene.C:1666   static_cast<GLRenderWindow*>(...)->swapBuffers() -> DELETE
  scene.C:1650   safeBufferSwap() call -> DELETE
  scene.C:1644-1672  the whole manual cross-renderer swap-synchronisation block -> replace with
                 update() calls on the relevant GLRenderWindow(s). QOpenGLWidget swaps after paintGL().
                 The RenderToBufferFinishedEvent mechanism STAYS (thread-safe handoff): worker finishes
                 CPU buffer -> posts event -> GUI thread calls update() -> paintGL() blits.
  scene.C:2256,2404  main_display_->makeCurrent() -> KEEP (GUI-thread event handlers / setup; valid on QOpenGLWidget)
  scene.C:2443,2469,2541,2548,2558,2564,2648,2708,2736,2797,2825  new_widget->makeCurrent() in
                 addGlWindow() / stereo — these are on TOP-LEVEL Qt::Window GLRenderWindows.
                 makeCurrent() itself is valid; the RISK is whether a top-level QOpenGLWidget
                 composites correctly (Pitfall 6). See Task 2.
  scene.C:3098 + ~25 sites  Scene::updateGL() -> NO CHANGE. Scene is a plain QWidget; Scene::updateGL()
                 is its OWN method calling Scene::paintGL(). It is NOT QGLWidget::updateGL(). LEAVE ALL.
  scene.C:524,552  Scene::paintGL / Scene::paintEvent -> Scene is not a GL widget; these stay plain
                 QWidget methods. Do not touch.
  scene.C:506-522  Scene::renderText_() already uses QPainter — no change; it was the model for Plan 02.

Pitfall 6 — top-level stereo / multi-display GLRenderWindow (the highest-risk item):
  addGlWindow() (~scene.C:2441) and the stereo paths create `new GLRenderWindow(0, "Scene", Qt::Window)`
  — a top-level GL widget. Recommended for THIS phase: keep the code compiling but guard the
  multi-display/stereo entry points behind a runtime check (e.g. an early-return with a logged
  "stereo/multi-display deferred to Phase 5" message), OR leave the construction but mark it
  experimental. Do NOT attempt a full top-level-QOpenGLWidget port here. Document the decision
  explicitly in the SUMMARY. Full stereo validation is deferred — it is not the Core Value.

Build/run (BUILD-macos.md) — wave-merge build needs a clean VIEW first (base-class swap changes
vtable/moc layout):
  cd build && make clean 2>/dev/null; make BALL VIEW BALLView -j8
  run: BALL_DATA_PATH=$PWD/../data BALLVIEW_DATA_PATH=$PWD/../data DYLD_LIBRARY_PATH=$PWD/lib \
       bin/BALLView.app/Contents/MacOS/BALLView
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port scene.C — capability probe, swap-sync removal, grabFramebuffer rename</name>
  <read_first>
    - source/VIEW/WIDGETS/scene.C (the regions named in &lt;interfaces&gt;: ~86, ~1644-1672, ~2096, ~3063-3082; and skim Scene::updateGL ~3098 to confirm it is Scene's own method)
    - .planning/phases/02-rendering-port-4a/02-RESEARCH.md (§4, § "Threading Rework", Pitfall 6)
    - .planning/phases/02-rendering-port-4a/02-02-SUMMARY.md and 02-03-SUMMARY.md (what the prior plans changed)
  </read_first>
  <files>source/VIEW/WIDGETS/scene.C</files>
  <action>
    Apply the scene.C call-site map from &lt;interfaces&gt; exactly:
    - Drop `#include <QtOpenGL/QGLPixelBuffer>` (~86); add `<QtGui/QSurfaceFormat>` and
      `<QtGui/QOpenGLContext>` for the probe.
    - Rewrite the stereo-capability probe (~3063-3082) with the QSurfaceFormat + transient
      QOpenGLContext form from &lt;interfaces&gt;. CRITICAL: keep the existing contract — this block
      still assigns `GLRenderWindow::gl_format_`. Preserve the surrounding logic that decides
      stereo on/off; only the probe mechanism changes.
    - Replace `main_display_->grabFrameBuffer()` (~2096) with `main_display_->grabFramebuffer()`.
    - Delete the manual swap-synchronisation: the `swapBuffers()` call (~1666), the
      `safeBufferSwap()` call (~1650), and collapse the cross-renderer swap block (~1644-1672)
      to `update()` call(s) on the relevant GLRenderWindow. Keep the `RenderToBufferFinishedEvent`
      posting/handling — only the swap coordination is removed.
    - Leave `main_display_->makeCurrent()` at ~2256 and ~2404 (GUI-thread, valid).
    - DO NOT touch `Scene::updateGL()` or its ~25 call sites, and DO NOT touch `Scene::paintGL`/
      `Scene::paintEvent` — they are Scene's own QWidget methods (RESEARCH.md §4 is explicit).
    - The top-level Qt::Window paths and their `new_widget->makeCurrent()` calls are handled in
      Task 2 — leave them for now (they may still compile fine; the guard is the next task).
  </action>
  <verify>
    <automated>! grep -nE "QGLWidget|QGLFormat|QGLContext|QGL::|QGLPixelBuffer|QtOpenGL/qgl|swapBuffers|safeBufferSwap|grabFrameBuffer" source/VIEW/WIDGETS/scene.C && grep -c "QSurfaceFormat" source/VIEW/WIDGETS/scene.C && grep -c "updateGL" source/VIEW/WIDGETS/scene.C</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "QGLWidget|QGLFormat|QGLContext|QGL::|QGLPixelBuffer|QtOpenGL/qgl|swapBuffers|safeBufferSwap|grabFrameBuffer" source/VIEW/WIDGETS/scene.C` returns no hits.
    - `grep -c "QSurfaceFormat" source/VIEW/WIDGETS/scene.C` >= 1 (probe rewritten).
    - `grep -c "updateGL" source/VIEW/WIDGETS/scene.C` is unchanged from the pre-port count (~25+ — Scene::updateGL left intact); record the count in the SUMMARY.
    - The capability probe still assigns `GLRenderWindow::gl_format_` (grep confirms `gl_format_` still set in that block).
  </acceptance_criteria>
  <done>scene.C is free of legacy Qt GL symbols, the probe uses QSurfaceFormat+QOpenGLContext, manual swap-sync is gone, and Scene::updateGL is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Guard/defer the top-level stereo + multi-display paths; full clean build</name>
  <read_first>
    - source/VIEW/WIDGETS/scene.C (addGlWindow() ~2441 and the stereo paths ~2467, ~2539, ~2647, ~2707 with their makeCurrent() calls)
    - .planning/phases/02-rendering-port-4a/02-RESEARCH.md (Pitfall 6, Open Questions item 2, Assumptions Log A3)
    - BUILD-macos.md (the build + run commands)
  </read_first>
  <files>source/VIEW/WIDGETS/scene.C</files>
  <action>
    Guard/defer the high-risk top-level GL window paths per RESEARCH.md Pitfall 6:
    - In `addGlWindow()` (~2441) and the stereo entry points (~2467, ~2539+, ~2647, ~2707+),
      add a runtime guard at the entry of each: log a clear message
      ("stereo / multi-display GL windows are deferred to Phase 5 (Qt 6 + pipeline) — see 02-RESEARCH.md Pitfall 6")
      and early-return WITHOUT constructing the top-level `new GLRenderWindow(0, ..., Qt::Window)`.
      Keep the code paths COMPILING — do not delete them, just gate them so the unported top-level
      composition path is never exercised. The embedded scene (the Core Value) is unaffected.
    - If you judge that a particular path is trivially safe to keep active, you may leave it — but
      the DEFAULT and recommended action is guard-and-defer. Whatever you choose, document each
      path's disposition (guarded / kept / stubbed) in the SUMMARY.
    - Then do the wave-merge build per BUILD-macos.md: `cd build && make clean 2>/dev/null;
      make BALL VIEW BALLView -j8`. This MUST succeed — all 5+ files are now ported.
    - Run the Wave 0 grep gate: `bash .planning/phases/02-rendering-port-4a/scripts/check-no-legacy-gl-symbols.sh`
      — it MUST now exit 0 (zero legacy symbols across source/VIEW + include/BALL/VIEW).
  </action>
  <verify>
    <automated>cd build && make clean 2>/dev/null; make BALL VIEW BALLView -j8 2>&1 | tail -30 && cd .. && bash .planning/phases/02-rendering-port-4a/scripts/check-no-legacy-gl-symbols.sh; echo "grep-gate-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `make BALL VIEW BALLView -j8` from a clean build completes with exit 0 — `bin/BALLView.app` (or platform equivalent) is produced.
    - `bash .planning/phases/02-rendering-port-4a/scripts/check-no-legacy-gl-symbols.sh` exits 0 (RENDER-01/03/07 grep gate green across the whole tree).
    - Each top-level stereo/multi-display path in scene.C has a documented disposition in the SUMMARY (guarded / kept / stubbed) with rationale.
    - The build log shows scene.C, glRenderWindow.{h,C}, renderSetup.C, glRenderer.C, glOffscreenTarget.C all compiling without error.
  </acceptance_criteria>
  <done>The top-level stereo/multi-display paths are guard-deferred, the whole BALL+VIEW+BALLView tree builds clean from scratch, and the legacy-Qt-GL-symbol grep gate is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human smoke check — embedded molecule renders on macOS</name>
  <files>source/VIEW/WIDGETS/scene.C</files>
  <action>
    This is a blocking human-verification checkpoint — no code is written. The executor
    pauses here and the user runs the BALLView GUI to confirm the rendering port works.
    See &lt;what-built&gt; and &lt;how-to-verify&gt; for the exact context and steps.
  </action>
  <what-built>
    GLRenderWindow ported from QGLWidget to QOpenGLWidget (compat-profile QSurfaceFormat),
    rendering moved into paintGL(), manual buffer-swap deleted, on-screen text reimplemented
    as a QPainter overlay, renderer subsystem + scene.C cleaned of all legacy Qt GL symbols,
    top-level stereo/multi-display paths guard-deferred. A full clean BALL+VIEW+BALLView build
    succeeds and the grep symbol gate is green.
  </what-built>
  <how-to-verify>
    Per 02-VALIDATION.md "Manual smoke checklist" — there is no automated GL/GUI test harness,
    so this is a required human gate. From the `build` directory (BUILD-macos.md):
      BALL_DATA_PATH=$PWD/../data BALLVIEW_DATA_PATH=$PWD/../data DYLD_LIBRARY_PATH=$PWD/lib \
        bin/BALLView.app/Contents/MacOS/BALLView
    Then confirm, in order:
      1. BALLView launches; main window + dock panels appear.
      2. The startup demo builds a peptide and the molecule is VISIBLE INSIDE the Scene area
         — not a detached/blank/mis-sized native window. (RENDER-02 — the Core Value.)
      3. Left-drag rotates the scene; scroll/zoom changes the view; click-pick selects an atom. (RENDER-06)
      4. Switch to the raytracer renderer — a raytraced image appears in the scene area. (RENDER-04)
      5. Enable FPS/info text — the text is legible over the 3D scene. (RENDER-05)
      6. The console shows no flood of GL errors from checkGL(). (RENDER-01/07)
    RENDER-08 (Linux + Windows builds green + the same visual check on each OS) is tracked as a
    follow-up — note in the resume whether Linux/Windows verification is done now or deferred to
    a CI/cross-platform pass.
  </how-to-verify>
  <verify>
    <automated>MANUAL — human smoke check; no automated GL/GUI test harness exists this phase. See how-to-verify.</automated>
  </verify>
  <done>User types "approved" after confirming steps 1-6 of the smoke checklist pass on macOS (embedded molecule renders, interaction works, raytracer + text overlay work, no GL error flood).</done>
  <resume-signal>Type "approved" if the embedded molecule renders and steps 1-6 pass, or describe the failures (which step, what you saw, any console GL errors).</resume-signal>
</task>

</tasks>

<verification>
- scene.C contains zero legacy Qt GL symbols and compiles clean.
- The full `make BALL VIEW BALLView -j8` from a clean build succeeds.
- `check-no-legacy-gl-symbols.sh` exits 0 across source/VIEW + include/BALL/VIEW (RENDER-01/03/07).
- Top-level stereo/multi-display paths are guard-deferred and documented.
- [HUMAN GATE] BALLView launches and a molecule renders embedded in the main window on macOS; rotate/zoom/pick/select, raytracer output, and on-screen text all work (RENDER-02/04/05/06). This cannot be automated — no GL/GUI test harness exists and none is built this phase.
- RENDER-08 (Linux + Windows): builds automatable; render confirmation is a per-OS human gate, tracked as a follow-up of this checkpoint.
</verification>

<success_criteria>
- All 5+ phase files (glRenderWindow.{h,C}, renderSetup.{h,C}, glRenderer.C, glOffscreenTarget.{h,C}, scene.C) compile with zero QGLWidget/QGLContext/QGLFormat references.
- A molecule renders embedded in the BALLView main window on macOS (human-confirmed) — the Core Value.
- Mouse/keyboard interaction, raytracer output, and on-screen text overlay all work.
- Stereo/multi-display top-level paths are explicitly deferred to Phase 5, documented.
</success_criteria>

<output>
After completion, create `.planning/phases/02-rendering-port-4a/02-04-SUMMARY.md`.
Record: the disposition of each top-level stereo/multi-display path; the Scene::updateGL call count (unchanged); the human smoke-check result; and the RENDER-08 Linux/Windows status (done now vs deferred follow-up).
</output>
