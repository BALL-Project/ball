---
phase: 02.2-ci-and-build-smoke-matrix
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [CI-01]
must_haves:
  truths:
    - "A GitHub Actions matrix configures and builds BALL/VIEW/BALLView on macOS-arm64, Ubuntu, and Windows on every push/PR to master and v1.6-modernization"
    - "macOS and Linux build jobs are blocking/required; the Windows job runs but is non-blocking (continue-on-error)"
    - "The render smoke check runs on macOS (natively) and Linux (under xvfb-run with software Mesa) and gates the build"
    - "The legacy-Qt-GL-symbol grep gate (check-no-legacy-gl-symbols.sh) runs as a CI lint"
    - "Dependencies are provisioned via native package managers (Homebrew on macOS, apt on Linux) with caching, plus ccache"
    - "POST-PUSH HUMAN GATE: the CI matrix actually goes green on all 3 OSes after the workflow is pushed to v1.6-modernization"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "3-OS GitHub Actions build matrix + render smoke check + grep-gate lint"
      contains: "continue-on-error"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: ".planning/phases/02.2-ci-and-build-smoke-matrix/scripts/render-smoke-check.sh"
      via: "smoke-check step invokes the Plan 01 script"
      pattern: "render-smoke-check.sh"
    - from: ".github/workflows/ci.yml"
      to: ".planning/phases/02-rendering-port-4a/scripts/check-no-legacy-gl-symbols.sh"
      via: "lint step/job invokes the existing grep gate"
      pattern: "check-no-legacy-gl-symbols.sh"
    - from: ".github/workflows/ci.yml"
      to: "Homebrew / apt"
      via: "native package install steps mirroring BUILD-macos.md"
      pattern: "brew install"
---

<objective>
Create the Phase 02.2 GitHub Actions workflow: a 3-OS Release build matrix
(macOS-arm64, Ubuntu, Windows) that mirrors the `BUILD-macos.md` flow exactly,
runs the render smoke check on macOS + Linux, runs the legacy-GL grep gate as a
lint, and uses native-package-manager dependency provisioning with caching +
ccache.

Purpose: This is the regression net for Phases 3-9 — every later phase (C++17,
deps, Qt 6, packaging) lands against a gate instead of a promise. It is the
Codex review's #1 finding ("CI is catastrophically late") being fixed.
Output: `.github/workflows/ci.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-ci-and-build-smoke-matrix/02.2-CONTEXT.md
@BUILD-macos.md
@.planning/phases/02.2-ci-and-build-smoke-matrix/02.2-01-SUMMARY.md

<interfaces>
<!-- Verbatim commands and assets the workflow must use. -->

macOS dependency install (D-06 — copy verbatim from BUILD-macos.md):
  brew install qt@5 boost eigen fftw tbb glew open-babel lp_solve libsvm flex bison

macOS configure (D-07 — copy verbatim from BUILD-macos.md, run from build/ with
bison/flex on PATH):
  export PATH="/opt/homebrew/opt/bison/bin:/opt/homebrew/opt/flex/bin:$PATH"
  cmake .. -DCMAKE_POLICY_VERSION_MINIMUM=3.5 \
    -DCMAKE_PREFIX_PATH="/opt/homebrew/opt/qt@5;/opt/homebrew" \
    -DCMAKE_BUILD_TYPE=Release \
    -DBISON_EXECUTABLE=/opt/homebrew/opt/bison/bin/bison \
    -DFLEX_EXECUTABLE=/opt/homebrew/opt/flex/bin/flex \
    -DBALL_PYTHON_SUPPORT=OFF -DUSE_RTFACT=OFF -DBALL_HAS_VIEW=ON

macOS build (D-07):  make BALL VIEW BALLView -j8

macOS run line for the smoke check (the Plan 01 script reproduces this itself; the
workflow just calls the script):
  BALL_DATA_PATH=$PWD/../data BALLVIEW_DATA_PATH=$PWD/../data \
  DYLD_LIBRARY_PATH=$PWD/lib bin/BALLView.app/Contents/MacOS/BALLView

Existing assets to wire in (do NOT recreate):
  - .planning/phases/02.2-ci-and-build-smoke-matrix/scripts/render-smoke-check.sh  (Plan 01)
  - .planning/phases/02-rendering-port-4a/scripts/check-no-legacy-gl-symbols.sh    (existing grep gate; takes REPO_ROOT as $1, exits 0/1)

GitHub-hosted runner labels (planner-confirmed current as of 2026-05): use
`macos-14` (Apple Silicon arm64), `ubuntu-22.04`, `windows-2022`. Pin explicit
versions (not `-latest`) so the matrix is reproducible.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 3-OS build matrix + dependency provisioning + ccache</name>
  <read_first>
    - BUILD-macos.md (the exact brew install line, configure flags, build command — copy verbatim)
    - .planning/phases/02.2-ci-and-build-smoke-matrix/02.2-CONTEXT.md (decisions D-03, D-06, D-07, D-10, D-11; "Specifics" — runner labels)
    - .planning/phases/02.2-ci-and-build-smoke-matrix/02.2-01-SUMMARY.md (whether Plan 01 added a main.C CLI flag — affects nothing here but confirms the build target set)
    - .planning/ROADMAP.md §"Phase 02.2" (the Windows-gating note, success criteria)
  </read_first>
  <action>
    Create `.github/workflows/ci.yml`. Structure it so an extra matrix axis (the
    future `ui_v2` axis) can be added later WITHOUT a rewrite (D-11) — use a single
    `build` job with `strategy.matrix.include:` entries per OS, each entry carrying
    its own `os`, `runner`, dep-install command, configure command, and a
    `blocking` flag. Keep per-OS specifics in the matrix entry, not scattered.

    Triggers (D-10): `on: push:` and `on: pull_request:` with `branches: [master,
    v1.6-modernization]` for both. Not all branches.

    Matrix entries:
      - macOS: runner `macos-14`. Deps via Homebrew — the verbatim `brew install`
        line from BUILD-macos.md (D-06). Configure + build verbatim from
        BUILD-macos.md (D-07): the full `cmake .. -DCMAKE_POLICY_VERSION_MINIMUM=3.5
        ... -DBALL_PYTHON_SUPPORT=OFF -DUSE_RTFACT=OFF -DBALL_HAS_VIEW=ON` line with
        bison/flex put on PATH, then `make BALL VIEW BALLView -j$(sysctl -n hw.ncpu)`.
        Blocking/required.
      - Linux: runner `ubuntu-22.04`. Deps via `apt-get install` — determine the
        apt equivalents of the Homebrew set: `qtbase5-dev qtbase5-dev-tools
        libqt5opengl5-dev qttools5-dev libboost-all-dev libeigen3-dev libfftw3-dev
        libtbb-dev libglew-dev libopenbabel-dev liblpsolve55-dev libsvm-dev flex
        bison`, plus `cmake make g++ ccache` and for the smoke check
        `xvfb mesa-utils libgl1-mesa-dri` (software Mesa/llvmpipe). Confirm package
        names resolve on ubuntu-22.04; if a package is named differently, fix it and
        note it in the SUMMARY. Configure: same CMake invocation shape as macOS but
        with Linux paths — drop the Homebrew `CMAKE_PREFIX_PATH`, keep
        `-DCMAKE_POLICY_VERSION_MINIMUM=3.5 -DCMAKE_BUILD_TYPE=Release
        -DBALL_PYTHON_SUPPORT=OFF -DUSE_RTFACT=OFF -DBALL_HAS_VIEW=ON`, and set
        `-DBISON_EXECUTABLE`/`-DFLEX_EXECUTABLE` to the apt-installed paths
        (`/usr/bin/bison`, `/usr/bin/flex`). Build `make BALL VIEW BALLView
        -j$(nproc)`. Blocking/required.
      - Windows: runner `windows-2022`. `continue-on-error: true` — NON-BLOCKING
        (D-03). The proper vcpkg manifest is Phase 4; until then this job may simply
        attempt a best-effort configure/build (it is allowed to fail). Keep the job
        present and named so it flips to required in Phase 4 with a one-line change.
        Do not over-invest here — a documented, non-blocking placeholder build
        attempt is sufficient for this phase.

    Caching (D-06, D-11):
      - Cache Homebrew downloads on macOS and the apt archive on Linux (via
        `actions/cache` keyed on the OS + the dep list hash).
      - Add `ccache`: install it, set `CMAKE_C_COMPILER_LAUNCHER`/
        `CMAKE_CXX_COMPILER_LAUNCHER=ccache` in the configure step, and cache the
        ccache directory keyed on OS + a rolling key so it warms across runs.

    Permissions: `permissions: contents: read`. Set a sensible `concurrency:` group
    (per-ref, cancel-in-progress) so pushes don't pile up. Do NOT add the smoke
    check or grep lint here — Task 2 adds those steps; this task is configure+build
    + caching only, so Task 1 is independently reviewable.
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml')); print('YAML OK')" && { command -v actionlint >/dev/null && actionlint .github/workflows/ci.yml || echo 'actionlint not installed - YAML parse only'; }</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists and `python3 -c "import yaml; yaml.safe_load(...)"` parses it without error
    - If `actionlint` is available locally it reports no errors; otherwise YAML parse is the proxy
    - `grep -c 'brew install qt@5 boost eigen fftw tbb glew open-babel lp_solve libsvm flex bison' .github/workflows/ci.yml` is >= 1 (verbatim BUILD-macos.md dep line)
    - `grep` confirms the macOS configure step contains `-DCMAKE_POLICY_VERSION_MINIMUM=3.5`, `-DBALL_PYTHON_SUPPORT=OFF`, `-DUSE_RTFACT=OFF`, and the build step targets `BALL VIEW BALLView`
    - `grep` confirms `continue-on-error: true` is present on the Windows matrix entry/job and absent from macOS/Linux
    - `grep` confirms triggers are restricted to `master` and `v1.6-modernization` for both `push` and `pull_request`
    - `grep` confirms `ccache` and `actions/cache` appear in the workflow
  </acceptance_criteria>
  <done>
    A valid `.github/workflows/ci.yml` defines a 3-OS Release matrix mirroring
    BUILD-macos.md verbatim on macOS, with apt-based Linux deps, a non-blocking
    Windows job, native-package caching, and ccache — extensible to an extra axis
    without a rewrite.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire in the render smoke check and the legacy-GL grep lint</name>
  <read_first>
    - .planning/phases/02.2-ci-and-build-smoke-matrix/scripts/render-smoke-check.sh (Plan 01 — its args, env vars, and the BALLVIEW_SMOKE_RUNNER prefix contract)
    - .planning/phases/02-rendering-port-4a/scripts/check-no-legacy-gl-symbols.sh (the existing grep gate — takes REPO_ROOT as $1, exits 0/1)
    - .planning/phases/02.2-ci-and-build-smoke-matrix/02.2-CONTEXT.md (decisions D-01, D-02, D-05, D-08; "CI lint" guidance; the deferred "CI failure artifacts" note)
    - .github/workflows/ci.yml (the file from Task 1 being extended)
  </read_first>
  <action>
    Extend `.github/workflows/ci.yml`:

    1. Render smoke-check step in the build job, AFTER the build step, on macOS and
       Linux only (D-08 — Windows job is non-blocking and has no smoke step this
       phase; skip it via the matrix entry, e.g. a `smoke: true/false` matrix field
       gating an `if:`):
       - macOS: invoke `.planning/phases/02.2-ci-and-build-smoke-matrix/scripts/render-smoke-check.sh`
         natively (no runner prefix), passing the built BALLView path.
       - Linux: export `LIBGL_ALWAYS_SOFTWARE=1` and set `BALLVIEW_SMOKE_RUNNER="xvfb-run -a"`
         (D-08 — software Mesa/llvmpipe), then invoke the same script. The script
         (per Plan 01) respects the `BALLVIEW_SMOKE_RUNNER` prefix and the
         `BALL_DATA_PATH`/`BALLVIEW_DATA_PATH` defaults.
       - This step is blocking on macOS + Linux — a blank/missing PNG fails the job.
       - On failure, upload the PNG + stdout log as a CI artifact
         (`actions/upload-artifact` with `if: failure()`) — the CONTEXT deferred-list
         calls this "good practice; planner may include it"; include it, it is cheap
         and directly serves the D-05 debugging intent.

    2. Legacy-GL grep lint. Add it as its OWN lightweight job (`lint`) running on
       `ubuntu-22.04` — it needs no build, so a separate fast job is cleaner than a
       per-OS step (Claude's-discretion point in CONTEXT resolved this way). The job
       checks out the repo and runs
       `bash .planning/phases/02-rendering-port-4a/scripts/check-no-legacy-gl-symbols.sh "$GITHUB_WORKSPACE"`.
       The script exits 1 if any legacy Qt GL symbol is found — that fails the lint
       job. The `lint` job is blocking/required.

    Keep the matrix extensibility from Task 1 intact. Ensure the smoke step's
    `if:` condition keys off a matrix field so adding a future axis doesn't
    accidentally enable/disable the smoke check.
  </action>
  <verify>
    <automated>python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); print('YAML OK')" && grep -q 'render-smoke-check.sh' .github/workflows/ci.yml && grep -q 'check-no-legacy-gl-symbols.sh' .github/workflows/ci.yml && grep -q 'LIBGL_ALWAYS_SOFTWARE' .github/workflows/ci.yml && echo 'wiring present'</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` still parses as valid YAML (and actionlint-clean if available)
    - `grep` confirms `render-smoke-check.sh` is invoked, and that the Linux smoke path sets `LIBGL_ALWAYS_SOFTWARE` and a `xvfb-run` prefix (D-08)
    - `grep` confirms the smoke step is gated to macOS + Linux (not Windows) via a matrix-field `if:`
    - `grep` confirms a `lint` job invokes `check-no-legacy-gl-symbols.sh`
    - `grep` confirms `actions/upload-artifact` with `if: failure()` for the smoke PNG + log
    - Local sanity: running `bash .planning/phases/02-rendering-port-4a/scripts/check-no-legacy-gl-symbols.sh "$PWD"` exits 0 (the Phase 2 port already removed the legacy symbols) — confirms the lint job will pass on the current tree
  </acceptance_criteria>
  <done>
    The workflow runs the Plan 01 render smoke check on macOS (native) and Linux
    (xvfb + software Mesa) as a blocking gate with failure-artifact upload, and runs
    the existing legacy-GL grep gate as a blocking `lint` job. CI-01 is fully
    expressed in the workflow file.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Post-push verification — CI matrix goes green on 3 OSes</name>
  <what-built>
    A complete `.github/workflows/ci.yml`: 3-OS Release build matrix (macOS-arm64,
    Ubuntu, Windows), render smoke check on macOS + Linux, legacy-GL grep lint job,
    Homebrew/apt dependency provisioning with caching + ccache. This is a GitHub
    Actions workflow — it cannot be fully verified locally; the real proof is a
    green CI run after the workflow is pushed to the `v1.6-modernization` branch.
    The local automated proxies (YAML valid, DIAG-01 line present in a local
    BALLView run, smoke script exits 0 against local `build/`) have already passed
    in Plan 01 and the earlier tasks of this plan.
  </what-built>
  <how-to-verify>
    1. Commit and push the workflow + Plan 01 changes to the `v1.6-modernization`
       branch (or open a PR targeting it) so GitHub Actions runs.
    2. Open the Actions tab on GitHub for the pushed ref.
    3. Confirm the **macOS** build job: configures, builds `BALL VIEW BALLView`,
       and the render smoke check passes (non-blank PNG, DIAG-01 line found). GREEN.
    4. Confirm the **Linux** build job: same, under xvfb + software Mesa. GREEN.
    5. Confirm the **lint** job (legacy-GL grep gate): GREEN.
    6. Confirm the **Windows** job ran — it is allowed to be red (non-blocking,
       `continue-on-error`); it must NOT block the overall check status.
    7. If macOS/Linux/lint are red: read the job logs, fix the workflow (dep package
       names, paths, configure flags, the smoke trigger), push again, repeat. This
       is expected to take 1-3 iterations — CI YAML rarely goes green first try.
  </how-to-verify>
  <resume-signal>
    Type "approved" once macOS + Linux + lint jobs are green and the Windows job is
    confirmed non-blocking. Or describe the failing job(s) and logs so the workflow
    can be fixed.
  </resume-signal>
</task>

</tasks>

<verification>
- `.github/workflows/ci.yml` is valid YAML (actionlint-clean where available)
- The macOS matrix entry reproduces BUILD-macos.md verbatim (dep line, configure flags, build targets)
- Windows job is `continue-on-error: true`; macOS + Linux + lint are blocking
- The render smoke check is wired on macOS (native) and Linux (xvfb + software Mesa)
- The legacy-GL grep gate runs as a blocking lint job
- POST-PUSH HUMAN GATE: macOS + Linux + lint jobs green on the actual GitHub Actions run
</verification>

<success_criteria>
- CI-01 satisfied: a GitHub Actions matrix builds BALL/VIEW/BALLView on macOS-arm64, Linux, and Windows on every push/PR to the milestone branches, runs the headless render smoke check, and runs the legacy-GL grep lint
- Dependency provisioning mirrors the contributor flow (Homebrew/apt + caching + ccache), not an `install-qt-action` divergence
- Windows is present but non-blocking, ready to flip to required in Phase 4
- The CI matrix is confirmed green on macOS + Linux (the post-push human gate)
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-ci-and-build-smoke-matrix/02.2-02-SUMMARY.md`.
Record: the final apt package list that resolved on ubuntu-22.04, the runner labels
used, the cache key scheme, how many push iterations it took to go green, and any
per-OS quirks discovered (especially Linux configure-path differences and the
Windows job's actual status).
</output>
