---
phase: 02.2-ci-and-build-smoke-matrix
verified: 2026-05-14T00:00:00Z
status: passed
score: 10/10 must-haves verified
overrides_applied: 0
---

# Phase 02.2: CI and build-smoke matrix Verification Report

**Phase Goal:** Stand up an early automated regression net — a 3-OS GitHub Actions build matrix (macOS-arm64, Linux, Windows) + a headless render smoke check + a GL-capability startup diagnostic — so Phases 3-9 land against a gate.
**Verified:** 2026-05-14
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
| -- | ----- | ------ | -------- |
| 1  | BALLView emits a single machine-parseable GL-capability line to stdout at startup | ✓ VERIFIED | `glRenderWindow.C:146` emits one `std::cout` line `BALLVIEW_GL_DIAG ...`, `std::endl`-flushed, inside `initializeGL()` |
| 2  | Diagnostic line includes GL vendor/version/profile, QSurfaceFormat fields, DPR, default FBO size, renderer backend | ✓ VERIFIED | `glRenderWindow.C:146-159` — gl_vendor, gl_renderer, gl_version, gl_profile, surface_version, depth_bits, stencil_bits, samples, device_pixel_ratio, default_fbo, fbo_size, renderer_backend=GL |
| 3  | A local smoke-check script launches BALLView headless, exports a PNG, asserts non-blank at expected viewport size | ✓ VERIFIED | `render-smoke-check.sh` (271 lines, executable) — autodetects build, watchdog-bounded run, python3-stdlib non-blank check, non-degenerate viewport assertion |
| 4  | Smoke-check exits 0 on good render, non-zero on blank/missing PNG | ✓ VERIFIED | Script `exit 0` at line 271; `fail()` helper used for missing PNG / blank / missing DIAG line; live Linux CI run exited 0 |
| 5  | GitHub Actions matrix builds BALL/VIEW/BALLView on macOS-arm64, Ubuntu, Windows on push/PR to master + v1.6-modernization | ✓ VERIFIED | `ci.yml` — `strategy.matrix.include` with 3 OS entries; `on: push/pull_request` branches `[master, v1.6-modernization]`; targets `BALL VIEW BALLView` |
| 6  | macOS/Linux jobs blocking; Windows non-blocking | ✓ VERIFIED | `ci.yml:40` `continue-on-error: ${{ !matrix.blocking }}`; macOS/Linux `blocking: true`, Windows `blocking: false` |
| 7  | Render smoke check runs on macOS (native) + Linux (xvfb + software Mesa) and gates the build | ✓ VERIFIED | `ci.yml:212-229` two smoke steps gated on `matrix.smoke`; Linux sets `LIBGL_ALWAYS_SOFTWARE=1` + `BALLVIEW_SMOKE_RUNNER: xvfb-run -a` |
| 8  | Legacy-Qt-GL-symbol grep gate runs as a CI lint | ✓ VERIFIED | `ci.yml:252-261` standalone `lint` job invokes `check-no-legacy-gl-symbols.sh` (script exists, executable) |
| 9  | Deps provisioned via native package managers (Homebrew/apt) with caching + ccache | ✓ VERIFIED | `ci.yml` — verbatim `brew install` line, apt list, `actions/cache` for Homebrew/apt/ccache, `CMAKE_*_COMPILER_LAUNCHER=ccache` |
| 10 | POST-PUSH HUMAN GATE: CI matrix goes green on all 3 OSes | ✓ VERIFIED | Run 25859952862 (`1959d9b`): status `completed`, conclusion `success`; build (macos-arm64/linux-x64/windows-x64) + lint all `success` |

**Score:** 10/10 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `source/VIEW/RENDERING/glRenderWindow.C` | DIAG-01 line from initializeGL() | ✓ VERIFIED | `BALLVIEW_GL_DIAG` at line 146, all 12 fields present, inside `initializeGL()` |
| `.planning/phases/02.2-ci-and-build-smoke-matrix/scripts/render-smoke-check.sh` | Headless render smoke check | ✓ VERIFIED | 271 lines (min 40), executable, DIAG-01 oracle cross-check |
| `.github/workflows/ci.yml` | 3-OS matrix + smoke + grep lint | ✓ VERIFIED | Parses as valid YAML, `continue-on-error` present, 3 OS matrix entries, smoke + lint jobs |

### Key Link Verification

| From | To | Via | Status |
| ---- | -- | --- | ------ |
| `glRenderWindow.C::initializeGL()` | stdout | `std::cout` single-line emit | ✓ WIRED — `BALLVIEW_GL_DIAG` at line 146, flushed |
| `render-smoke-check.sh` | BALLView + exported PNG | launch headless, grep DIAG, assert PNG non-blank | ✓ WIRED — `grep -m1 'BALLVIEW_GL_DIAG'` at line 156, fails if absent |
| `ci.yml` | `render-smoke-check.sh` | smoke-check step invokes script | ✓ WIRED — invoked in both macOS + Linux smoke steps |
| `ci.yml` | `check-no-legacy-gl-symbols.sh` | lint job invokes grep gate | ✓ WIRED — `lint` job line 261 |
| `ci.yml` | Homebrew / apt | native package install steps | ✓ WIRED — verbatim `brew install` + apt list present |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| ci.yml parses | `python3 -c "import yaml; yaml.safe_load(...)"` | YAML OK | ✓ PASS |
| DIAG line in source | `grep BALLVIEW_GL_DIAG glRenderWindow.C` | hit at line 146 | ✓ PASS |
| Smoke script exists + executable | `ls -la render-smoke-check.sh` | `-rwxr-xr-x`, 271 lines | ✓ PASS |
| CMAKE_CXX_STANDARD set | `grep CMAKE_CXX_STANDARD CMakeLists.txt` | `SET(CMAKE_CXX_STANDARD 14)` line 37 | ✓ PASS |
| Live CI run green | `gh run view 25859952862` | completed/success, 4 jobs success | ✓ PASS |
| Render smoke ran + passed | `gh run view ... steps` | macOS native: success, Linux xvfb: success | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| DIAG-01 | 02.2-01 | BALLView logs startup GL-capability diagnostic | ✓ SATISFIED | `glRenderWindow.C:146-159` emits all required fields; line is smoke-check oracle |
| CI-01 | 02.2-02 | GitHub Actions matrix builds on 3 OSes + render smoke check + legacy-GL grep lint | ✓ SATISFIED | `ci.yml` 3-OS matrix, smoke check on macOS+Linux, `lint` job; run 25859952862 green |

No orphaned requirements — REQUIREMENTS.md maps only CI-01 and DIAG-01 to Phase 02.2, both claimed and verified.

### Anti-Patterns Found

None. The one documented `renderer_backend=GL` literal is plan-scoped (CONTEXT D-04: field exists for the Phase 05.1 backend spike to populate; only a GL backend exists today) — not a stub. CI bring-up fixes (`3ac3f24` CMAKE_CXX_STANDARD, `1959d9b` lp_solve drop, `da043c1`/`4c91600` grep-gate) are genuine fixes, not papered-over failures — confirmed by the green lint job.

### Human Verification Required

None. The phase's "post-push human gate" (Plan 02 Task 3) is satisfied by the actual green CI run 25859952862 (commit `1959d9b`) — all four jobs `success`, render smoke check ran and passed on both macOS and Linux. No further human verification needed.

### Gaps Summary

No gaps. Both requirements (CI-01, DIAG-01) are fully met. The 3-OS build matrix, headless render smoke check, GL-capability startup diagnostic, and legacy-GL grep lint all exist in the codebase, are correctly wired, and are confirmed green on the live CI run. The regression net for Phases 3-9 is operational.

---

_Verified: 2026-05-14_
_Verifier: Claude (gsd-verifier)_
