---
phase: 999.65-representation-builder-extraction
plan: 03
type: execute
wave: 3
depends_on: ["999.65-02"]
files_modified:
  - source/VIEW/KERNEL/representationManager.C
  - source/VIEW/DIALOGS/displayProperties.C
  - include/BALL/VIEW/DIALOGS/displayProperties.h
autonomous: false
requirements: [VIEW-CLEAN-04]

must_haves:
  truths:
    - "RepresentationManager::restoreRepresentations builds each project-file representation via RepresentationBuilder (parseDataString -> createRepresentation), no longer through DisplayProperties::createRepresentation(String,...)"
    - "Opening a saved BALLView project restores every representation identically — same model/coloring/precision/transparency/composites, named properties preserved, clipping planes still re-linked (GUI-verified)"
    - "No live (non-comment) code outside DisplayProperties' own dialog files calls DisplayProperties::createRepresentation (grep): the orchestration is dead — the dialog only holds settings UI"
    - "The molecule-load auto-rep suppression during bulk/project load still works (enableCreationForNewMolecules toggling still gates the NEW_MOLECULE auto-rep path)"
    - "BALLView links clean BALL_UI_V2=ON"
  artifacts:
    - path: "source/VIEW/KERNEL/representationManager.C"
      provides: "restore path repointed onto RepresentationBuilder"
      contains: "RepresentationBuilder"
    - path: "source/VIEW/DIALOGS/displayProperties.C"
      provides: "DisplayProperties::createRepresentation orchestration retired / reduced to dead settings-UI shell"
      contains: "buildCurrentSpec"
  key_links:
    - from: "source/VIEW/KERNEL/representationManager.C restoreRepresentations"
      to: "RepresentationBuilder::parseDataString + RepresentationBuilder::createRepresentation"
      via: "parse the project data-string into a spec headlessly, build via the factory, replay named properties"
      pattern: "RepresentationBuilder"
---

<objective>
Repoint the SECOND live consumer — `RepresentationManager::restoreRepresentations`
(representationManager.C:438, `dp->createRepresentation(data_string, new_systems)`) — onto the headless
`RepresentationBuilder`, and confirm the molecule-load auto-rep suppression (enableCreationForNewMolecules)
still gates the now-builder-backed NEW_MOLECULE path. After this, NO live code outside the dialog's own
files calls `DisplayProperties::createRepresentation` — its orchestration is dead, leaving
displayProperties / modelSettingsDialog / coloringSettingsDialog deletable by 999.53.

The restore path becomes: for each project-file representation data-string, call
`RepresentationBuilder::parseDataString(data_string, parsed, model_information)` to get a RepresentationSpec
+ composite-id set + named-property base64 blocks, resolve the composites against `new_systems` (the same
system-number / composite-index resolution the dialog did at displayProperties.C:888-913), call
`RepresentationBuilder::createRepresentation(parsed.spec, c_list, *main_control_, <notifier>, hidden)`,
then decode + apply the named-property blocks (the TextPersistenceManager replay at
displayProperties.C:924-956) and send UPDATE_PROPERTIES. Byte-identical project restore.

Purpose: VIEW-CLEAN-04 — this is the LAST live consumer of the dialog's orchestration. Once repointed,
the dialog is off the core load->render AND project-restore paths entirely. Highest-risk; GUI-verified.

Output: restore path on the builder; createRepresentation orchestration dead (grep-proven); project
restore renders identically (GUI UAT); BALLView links clean BALL_UI_V2=ON.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/999.65-representation-builder-extraction/999.65-01-SUMMARY.md
@.planning/phases/999.65-representation-builder-extraction/999.65-02-SUMMARY.md

<read_first>
MANDATORY before editing:

- `.planning/phases/999.65-representation-builder-extraction/999.65-01-SUMMARY.md` — the parseDataString
  signature + ParsedRepresentation field set (spec, composite_indices, system_index, hidden, custom_color,
  named_property_blocks) and the createRepresentation signature (incl. the messaging strategy / notifier).
- `include/BALL/VIEW/MODELS/representationBuilder.h` — parseDataString + createRepresentation contracts.
- `source/VIEW/KERNEL/representationManager.C:423-443` — restoreRepresentations: the loop over
  `Representation<p>` entries calling `dp->createRepresentation(data_string, new_systems)` at :438, the
  `no_update_` flag, and the trailing `rebuildAllRepresentations()`. This is the repoint site. Note
  RepresentationManager HAS `main_control_` (used at :335,:363) and IS the owner of the rep list — it can
  send messages / it already calls insert internally; confirm whether it is a ConnectionObject or must
  route notifications through main_control_ / the same notifier Plan 01 chose.
- `source/VIEW/DIALOGS/displayProperties.C:835-961` — createRepresentation(String, vector<const Composite*>):
  the FULL behavior the restore path must reproduce once parseDataString has done the field parse:
  * :857 system_pos resolution against new_systems (the :888 `system_pos >= new_systems.size()` guard +
    error log),
  * :904-913 the Composite::CompositeIterator walk collecting composites whose index is in the parsed
    id hash_set,
  * :922 the createRepresentation(c_list, hidden) build,
  * :924-956 the named-property base64 decode loop (TextPersistenceManager, the RTFact::Material ->
    Rendering::Material compat shim, setProperty, the final UPDATE_PROPERTIES RepresentationMessage).
  parseDataString (Plan 01) already extracted the spec + raw named_property_blocks; this plan does the
  composite resolution + the build + the property decode/replay in restoreRepresentations.
- `source/VIEW/DIALOGS/displayProperties.C:314-335` — onNotify NEW_MOLECULE branch (already repointed in
  Plan 02). The `create_representations_for_new_molecules_` guard at :323 is what
  enableCreationForNewMolecules toggles — confirm it still gates the builder-backed auto-rep (it does;
  the flag + onNotify stay on the dialog, only the build call moved to the builder in Plan 02). This plan
  does NOT need to move the flag — it stays as the gate for the message-handler path.
- `source/VIEW/KERNEL/mainControl.C:2115-2177` — enableCreationForNewMolecules(false/true) bracketing the
  project load (suppress per-molecule auto-rep so restoreRepresentations can rebuild the SAVED reps
  instead). LEAVE this as-is unless the dialog-singleton lookup must change — it gates the dialog's
  onNotify flag, which is still correct. Confirm no behavior change is needed here.
- `source/VIEW/DIALOGS/displayProperties.C:566-611,835-961` — after repointing restore, decide the fate of
  the two createRepresentation overloads: the `list<Composite*>` overload (:566) is still called by the
  dialog's own apply()/applyTo_ (the interactive Create button + modify paths) — those are dialog-internal
  and 999.53 deletes them with the dialog, so they may STAY. The `String` overload (:835) had ONLY the
  restore caller (representationManager.C:438) — once repointed it is dead and SHOULD be removed (or left
  as a thin self-contained shell for 999.53 to delete). Verify via grep which overload retains live
  external callers; the must-have is that NO external (non-dialog) live caller of either remains.
- `source/APPLICATIONS/BALLVIEW/demoTutorialDialog.C:580-582` — the other enableCreationForNewMolecules
  caller (suppresses auto-rep during the tutorial's scripted load). LEAVE as-is; it gates the same
  builder-backed onNotify path correctly.
</read_first>

<interfaces>
RepresentationBuilder::parseDataString(const String& data_string, ParsedRepresentation& out,
  const ModelInformation& model_information) -> bool   (from Plan 01)
RepresentationBuilder::createRepresentation(const RepresentationSpec& spec,
  const std::list<Composite*>& composites, MainControl& main_control, <notifier>, bool hidden) -> Representation*

ModelInformation is reachable via main_control_->getModelInformation() inside RepresentationManager.
The named-property decode (TextPersistenceManager + RTFact->Rendering::Material compat) stays in
restoreRepresentations — it needs notify_/the persistence manager and is restore-specific.

NOTE: confirm the notifier argument shape against the Plan 01 messaging strategy in 01-SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint RepresentationManager::restoreRepresentations onto RepresentationBuilder</name>
  <files>source/VIEW/KERNEL/representationManager.C</files>
  <action>
    In restoreRepresentations (representationManager.C:423-443), replace the
    `DisplayProperties* dp = DisplayProperties::getInstance(0); if (dp==0) return;` + per-entry
    `dp->createRepresentation(data_string, new_systems)` with the headless builder path. For each
    `Representation<p>` data-string:
      1. `RepresentationBuilder::ParsedRepresentation parsed;` +
         `if (!RepresentationBuilder::parseDataString(data_string, parsed, main_control_->getModelInformation())) continue;`
         (mirror the legacy malformed-input skip).
      2. Resolve the system + composites: reproduce displayProperties.C:857,888-913 — guard
         `parsed.system_index >= new_systems.size()` (error log + skip), then walk
         `new_systems[parsed.system_index]`'s Composite::CompositeIterator collecting the composites whose
         running index is in `parsed.composite_indices` into a `list<Composite*> c_list`.
      3. `Representation* rep = RepresentationBuilder::createRepresentation(parsed.spec, c_list, *main_control_, <notifier>, parsed.hidden);`
         (use the Plan 01 notifier convention; if the builder takes a ConnectionObject notifier and
         RepresentationManager is not one, route through main_control_ or the same mechanism Plan 01/02
         used — read 01-SUMMARY).
      4. Replay named properties: reproduce displayProperties.C:924-956 over `parsed.named_property_blocks`
         (TextPersistenceManager base64 decode, the RTFact::Material -> Rendering::Material compat shim,
         rep->setProperty, the final RepresentationMessage UPDATE_PROPERTIES) — moved here verbatim.
    Drop the now-unused `DisplayProperties* dp` lookup + the `<BALL/VIEW/DIALOGS/displayProperties.h>`
    include from representationManager.C if nothing else in the file uses it (grep first; representationManager.C:336
    uses ModelInformation::modelMuteableByDisplayProperties — a DIFFERENT symbol, NOT the dialog —
    so leaving that is fine and does not require the dialog include). Keep `no_update_` + the trailing
    rebuildAllRepresentations() exactly. Add `<BALL/VIEW/MODELS/representationBuilder.h>` include.
    Build VIEW + BALLView BALL_UI_V2=ON; confirm clean link.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && { awk '/RepresentationManager::restoreRepresentations/,/rebuildAllRepresentations\(\);/' source/VIEW/KERNEL/representationManager.C | grep -q 'RepresentationBuilder' && ! awk '/RepresentationManager::restoreRepresentations/,/rebuildAllRepresentations\(\);/' source/VIEW/KERNEL/representationManager.C | grep -q 'dp->createRepresentation' && echo PASS-restore-repointed || echo FAIL; }</automated>
  </verify>
  <done>restoreRepresentations builds each project rep via RepresentationBuilder (parseDataString + createRepresentation + named-property replay), no longer via DisplayProperties::createRepresentation; VIEW + BALLView build clean BALL_UI_V2=ON.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the now-dead DisplayProperties::createRepresentation(String,...) orchestration</name>
  <files>source/VIEW/DIALOGS/displayProperties.C, include/BALL/VIEW/DIALOGS/displayProperties.h</files>
  <action>
    Grep-confirm no live external caller of either createRepresentation overload remains:
    `grep -rn 'createRepresentation' source/ | grep -v 'displayProperties' | grep -viE ':\s*//'` should show
    only the SEPARATE RepresentationManager::createRepresentation() (representationManager.C:141) and
    MolecularControl::createRepresentation() (DIFFERENT methods — not the dialog's) and the .sip bindings.
    The dialog's `createRepresentation(String, vector<const Composite*>)` (displayProperties.C:835) now has
    ZERO live callers — remove it (and its declaration at displayProperties.h:139) OR, to keep this plan's
    diff minimal and let 999.53 do the bulk file delete, leave it as a self-contained never-called shell.
    PREFERRED: remove the String overload + its declaration now (it is dead and removing it shrinks 999.53's
    surface). The `list<Composite*>` overload (:566) is still called by the dialog's own apply()/applyTo_
    interactive Create/modify paths — LEAVE it (999.53 deletes it with the whole dialog). Do NOT touch
    onNotify (Plan 02 already repointed it) or buildCurrentSpec. If you removed the String overload, also
    remove getSettingsFromString (displayProperties.C:809, declared .h:136) if it has no other caller
    (grep — it was only called from the String overload). Build VIEW + BALLView BALL_UI_V2=ON; confirm link.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && { c=$(grep -rn 'createRepresentation' source/ include/ | grep -viE 'displayProperties' | grep -viE ':\s*//|:\s*\*|:\s*/\*' | grep -viE 'RepresentationManager::createRepresentation|MolecularControl::createRepresentation|representationManager\.h|molecularControl\.h|\.sip:|representationBuilder' | wc -l | tr -d ' '); echo "non-dialog non-builder createRepresentation refs: $c (target 0)"; test "$c" -eq 0 && echo PASS-orchestration-dead || echo REVIEW-residuals; }</automated>
  </verify>
  <done>No live external (non-dialog, non-builder) caller of DisplayProperties::createRepresentation remains; the String overload (+ getSettingsFromString if orphaned) removed or reduced to a dead shell; VIEW + BALLView build clean BALL_UI_V2=ON.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: GUI UAT — project open restores all representations identically via the builder</name>
  <what-built>
    Repointed project-file restore (RepresentationManager::restoreRepresentations) off
    DisplayProperties::createRepresentation(String,...) and onto the headless RepresentationBuilder
    (parseDataString -> createRepresentation -> named-property replay). Retired the now-dead String
    createRepresentation overload. After this, the dialog's representation-creation orchestration is dead
    on the project-restore + molecule-load paths (the dialog only holds settings UI). BALLView links clean
    BALL_UI_V2=ON. This is the HIGHEST-risk core path — compile is necessary but NOT sufficient; the
    threaded render + persistence round-trip requires running-GUI confirmation.
  </what-built>
  <how-to-verify>
    1. Launch the freshly built BALLView (BALL_UI_V2=ON).
    2. Build a NON-trivial scene: load a molecule, then via Display > Display Properties create 2-3
       representations with DIFFERENT models (e.g. sticks, ball-and-stick, surface), different coloring,
       different transparency. Add a clipping plane linked to a representation if convenient. Save the
       project (File > Save Project).
    3. Close + reopen BALLView (or File > Open Project on the saved file). Confirm EVERY saved
       representation is restored and RENDERS IDENTICALLY: same models, same coloring, same transparency,
       same composites, and the clipping plane is still re-linked to its representation(s).
    4. Confirm the molecule-load auto-rep is correctly SUPPRESSED during the project load (you should see
       the SAVED reps, not a default stick rep auto-added on top — i.e. enableCreationForNewMolecules
       suppression still works).
    5. Open a fresh molecule afterward (not from a project) and confirm the NEW_MOLECULE auto-rep path
       (Plan 02) still renders the default rep — i.e. the suppression flag was correctly re-enabled after load.
    6. Confirm no crash, no missing rep, no wrong-model/wrong-color restore, no double-render.
  </how-to-verify>
  <action>This is a human-verify checkpoint. Claude has repointed the restore path + retired the dead overload + built BALLView; the human now confirms project save/restore renders identically and auto-rep suppression still works, per the how-to-verify steps. No code edits unless a regression is found.</action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && echo "manual GUI UAT — human confirms project restore renders identically via RepresentationBuilder + auto-rep suppression intact"</automated>
  </verify>
  <done>Human confirms: a saved project re-opens with every representation restored + rendered identically (model/coloring/transparency/composites/clipping), auto-rep is suppressed during project load and re-enabled after, and a fresh molecule still auto-renders; no crash/missing/wrong/double-render.</done>
  <resume-signal>Type "approved" if project restore + molecule load both render identically, or describe the regression.</resume-signal>
</task>

</tasks>

<verification>
```bash
cd /Users/kohlbach/Claude/BALL/ball
# Restore repointed onto the builder:
awk '/RepresentationManager::restoreRepresentations/,/rebuildAllRepresentations\(\);/' source/VIEW/KERNEL/representationManager.C | grep -n 'RepresentationBuilder\|dp->createRepresentation'
# Expect: RepresentationBuilder present; dp->createRepresentation gone.
# Orchestration dead — no live external caller of DisplayProperties::createRepresentation:
grep -rn 'createRepresentation' source/ include/ | grep -viE 'displayProperties|RepresentationManager::createRepresentation|MolecularControl::createRepresentation|representationManager\.h|molecularControl\.h|\.sip:|representationBuilder' | grep -viE ':\s*//|:\s*\*|:\s*/\*'
# Expect: EMPTY.
```
BALLView links clean BALL_UI_V2=ON; blocking GUI UAT (Task 3) confirms project restore + molecule load
render identically and auto-rep suppression is intact.
</verification>

<success_criteria>
- restoreRepresentations builds project reps via RepresentationBuilder (parse + create + property replay).
- No live external (non-dialog) caller of DisplayProperties::createRepresentation remains — the orchestration is dead.
- enableCreationForNewMolecules still gates the (now builder-backed) NEW_MOLECULE auto-rep path.
- BALLView links clean BALL_UI_V2=ON.
- Blocking GUI UAT confirms project restore renders identically + auto-rep suppression intact.
</success_criteria>

<output>
After completion, create `.planning/phases/999.65-representation-builder-extraction/999.65-03-SUMMARY.md`.
Record: the restore-path repoint (parseDataString + createRepresentation + named-property replay), what
was removed from displayProperties (String overload / getSettingsFromString), the grep proof that
DisplayProperties::createRepresentation has no live external caller, the GUI UAT result, and an explicit
hand-off statement to 999.53: displayProperties / modelSettingsDialog / coloringSettingsDialog are now
unreferenced by live representation-creation code and deletable (VIEW-CLEAN-04 done; VIEW-CLEAN-01 final-3
unblocked).
</output>
