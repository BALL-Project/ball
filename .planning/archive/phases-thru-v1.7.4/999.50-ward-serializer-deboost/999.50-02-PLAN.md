---
phase: 999.50-ward-serializer-deboost
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - .github/workflows/release.yml
  - source/DOCKING/COMMON/sources.cmake
autonomous: true
requirements: [BUILD-ACCEL-07]
must_haves:
  truths:
    - "release.yml restores a ccache build cache before configure on each OS (macOS, Linux, Windows)"
    - "release.yml saves the ccache build cache after the build on each OS"
    - "The ccache cache key is aligned with ci.yml's lineage (ccache-<os>-pch-v2-) so a release tag of an already-CI-built commit can hit warm cache"
    - "release.yml's package job carries a NEW job-level env block with CCACHE_SLOPPINESS + CCACHE_EXTRAFILES (none existed before)"
    - "release.yml remains valid YAML / a well-formed GitHub Actions workflow"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "actions/cache restore + save steps for the per-OS ccache dir, plus a job-level env block for CCACHE_*"
      contains: "ccache"
  key_links:
    - from: ".github/workflows/release.yml ccache cache key"
      to: ".github/workflows/ci.yml ccache cache key (ccache-<os>-pch-v2-)"
      via: "shared restore-keys prefix so release reuses CI-warmed object cache"
      pattern: "ccache-"
---

<objective>
Add ccache build-cache reuse to `release.yml` (maintainer plan item ②, BUILD-ACCEL-07).
`release.yml` installs ccache and its presets already wire `CMAKE_<LANG>_COMPILER_LAUNCHER=ccache`,
but it never restores/saves an `actions/cache` for the ccache directory (only the vcpkg cache is
cached). So every release tag builds cold. Add `actions/cache` restore (before configure) + save
(after build) for the per-OS ccache dir, with a key aligned to `ci.yml`'s lineage so a release tag of
an already-CI-built commit hits warm cache — unchanged TUs (including the now-cheap serialization TU
and any remaining heavy ones) become near-instant.

Also include item ③ as an OPTIONAL/conditional fallback: a commented, ready-to-enable MSVC `/O1`
de-optimization for the pathological TU — only relevant if anything heavy remains after Plan 01's
boost elimination. It is NOT a blocking task and ships disabled.

WAVE/DEPENDENCY NOTE: this plan is wave 2, depends_on ["01"]. Item ③'s skip decision
("is any TU still heavy on MSVC after the boost-serialization removal?") references Plan 01's build
outcome, so Plan 02 runs AFTER Plan 01 completes — the executor can consult Plan 01's build log /
SUMMARY to decide whether item ③ is moot. Item ② (ccache) is independent of Plan 01 but is bundled
here; sequencing after 01 is harmless and keeps item ③'s decision well-informed.

Purpose: Biggest win for *repeat* releases — warm ccache across tags.
Output: release.yml gains ccache restore/save steps + a new job-level env block for CCACHE_*;
sources.cmake gains a disabled MSVC `/O1` escape hatch (or item ③ is documented as skipped).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.7.2-BUILD-ACCEL-PLAN.md
@./CLAUDE.md

<interfaces>
<!-- The existing CI ccache contract to align with, and release.yml's per-OS structure. -->

ci.yml ccache cache step (the lineage to align with — .github/workflows/ci.yml ~line 234):
- `uses: actions/cache@v5` with `path: ${{ matrix.ccache_dir }}`
- `key: ccache-${{ matrix.os }}-pch-v2-${{ github.sha }}`
- `restore-keys:` `ccache-${{ matrix.os }}-pch-v2-` then `ccache-${{ matrix.os }}-`
- ci.yml sets `CCACHE_SLOPPINESS: "pch_defines,time_macros,include_file_mtime"` and
  `CCACHE_EXTRAFILES: "${{ github.workspace }}/include/BALL/CONFIG/config.h.in"` via a JOB-LEVEL `env:`
  block (ci.yml ~line 218).

release.yml CURRENT state (VERIFIED): the `package` job (release.yml line 35) goes
`runs-on` → `strategy`/`matrix` → `steps` (line 63) with NO job-level `env:` block today — unlike
ci.yml. A new job-level `env:` block must therefore be CREATED on the package job (it does not exist
to "match"); there is no existing CCACHE_* env on this workflow.

Per-OS ccache directories (from ci.yml's matrix, reuse the same paths):
- macOS (macos-arm64):  `~/Library/Caches/ccache`
- Linux (linux-x64 / linux-arm64): `~/.cache/ccache`
- Windows (windows-x64): `C:\Users\runneradmin\AppData\Local\ccache`

release.yml structure (per-OS conditional steps; NO ccache_dir matrix var — release.yml's matrix
only has os/runner, see .github/workflows/release.yml ~line 40):
- macOS: "Install dependencies (macOS / Homebrew)" (installs ccache) → "Configure (macOS)" (line ~167,
  `cmake --preset ci-macos`) → "Build (macOS)" (line ~172).
- Linux: "Install dependencies (Linux / apt)" (line ~501, installs ccache) → "Install Qt 6" →
  "Configure (Linux)" (line ~523) → "Build (Linux)" (line ~537).
- Windows: "Set up Windows build deps (flex/bison + ccache + ninja)" (line ~788) → MSVC env →
  "Configure (Windows)" (line ~872) → "Build (Windows)" (line ~907).
- Existing caching pattern in release.yml uses the SPLIT form: `actions/cache/restore@v5` (id'd) +
  a later `actions/cache/save@v5` gated `if: always() && ... && steps.<id>.outputs.cache-hit != 'true'`
  (see the "Restore vcpkg binary cache (Windows)" / "Save vcpkg binary cache (Windows)" pair, lines ~775/900).
  Mirror this split-restore/save shape for ccache.

The presets (`ci-macos`, `ci-windows`, the Linux preset) already set
`CMAKE_<LANG>_COMPILER_LAUNCHER=ccache` (same presets ci.yml uses), so NO compiler-launcher wiring
is needed in release.yml — only the cache restore/save and the matching CCACHE_* env.

MSVC /O1 fallback target file (item ③): source/DOCKING/COMMON/sources.cmake lists
poseClusteringSerialization.C (and poseClusteringGraphViz.C). A
`set_source_files_properties(... COMPILE_OPTIONS "/O1")` guarded by `if(MSVC)` would go here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ccache restore + save to release.yml for all three OSes</name>
  <files>.github/workflows/release.yml</files>
  <read_first>
    - .github/workflows/release.yml lines ~34-63 (the `package` job header — runs-on / strategy / matrix straight into `steps:`, confirming there is NO existing job-level `env:` block to amend), ~160-176 (macOS install/configure/build), ~501-548 (Linux install/configure/build), ~775-910 (Windows restore/save vcpkg pattern + tooling/configure/build) — the steps to insert ccache caching around
    - .github/workflows/ci.yml lines ~218-249 (ci.yml's JOB-LEVEL env block with CCACHE_SLOPPINESS / CCACHE_EXTRAFILES + the ccache cache step — the key + sloppiness values to copy)
  </read_first>
  <action>
    Add ccache build-cache reuse to .github/workflows/release.yml so release-tag builds warm from prior
    CI/release runs. Mirror release.yml's existing split caching shape (actions/cache/restore@v5 +
    actions/cache/save@v5) used for the vcpkg cache. Implements BUILD-ACCEL-07 / plan item ②.

    For EACH OS, add a "Restore ccache" step BEFORE that OS's Configure step and a "Save ccache" step
    AFTER that OS's Build step, gated on the same `if:` OS condition the surrounding steps already use
    (`matrix.os == 'macos-arm64'`, `startsWith(matrix.os, 'linux-')`, `matrix.os == 'windows-x64'`).
    Use these ccache dirs as the cache `path` (matching ci.yml): macOS `~/Library/Caches/ccache`,
    Linux `~/.cache/ccache`, Windows `C:\Users\runneradmin\AppData\Local\ccache`.

    Cache key: align with ci.yml's lineage so a release tag of an already-CI-built commit can hit warm
    cache. Use `key: ccache-${{ matrix.os }}-pch-v2-${{ github.sha }}` with
    `restore-keys:` `ccache-${{ matrix.os }}-pch-v2-` then `ccache-${{ matrix.os }}-` — IDENTICAL prefixes
    to ci.yml so the same-SHA or most-recent CI cache is reusable. NOTE: ci.yml's `matrix.os` values are
    `macos-arm64` / `linux-x64` / `linux-arm64` / `windows-x64`; release.yml uses the SAME os strings, so
    the keys line up directly. Give each restore step an `id` (e.g. `ccache-mac`, `ccache-linux`,
    `ccache-win`) and gate the matching save step `if: always() && <os-cond> && steps.<id>.outputs.cache-hit != 'true'`
    (mirroring the vcpkg save gate) so an exact-key hit doesn't redundantly re-save.

    To make ccache hits actually land (not silently bust), add the same CCACHE_* contract ci.yml uses.
    IMPORTANT: release.yml's `package` job currently has NO job-level `env:` block (unlike ci.yml — verified:
    the job goes runs-on → strategy/matrix → steps with no env). So CREATE a NEW job-level `env:` block on the
    `package` job (between the job's `runs-on`/`strategy` and its `steps:`) carrying
    `CCACHE_SLOPPINESS: "pch_defines,time_macros,include_file_mtime"` and
    `CCACHE_EXTRAFILES: "${{ github.workspace }}/include/BALL/CONFIG/config.h.in"`, so the contract applies to
    every OS's configure + build. Do NOT claim to "match an existing env block" — there is none; you are adding
    the first one. Do NOT add `CMAKE_<LANG>_COMPILER_LAUNCHER` wiring — the ci-* presets already set it.

    Add a short comment on the new env block and the ccache steps citing Phase 999.50 / BUILD-ACCEL-07
    (plan item ②) and the key alignment with ci.yml, matching the heavily-commented style of the
    surrounding release.yml steps. Make NO other functional change to release.yml.
  </action>
  <verify>
    <automated>grep -c "Restore ccache\|actions/cache" .github/workflows/release.yml   # expect ccache restore+save steps present</automated>
  </verify>
  <acceptance_criteria>
    - The `package` job in `.github/workflows/release.yml` gains a NEW job-level `env:` block (it had none) containing `CCACHE_SLOPPINESS` and `CCACHE_EXTRAFILES` with ci.yml's values
    - `grep -c "CCACHE_SLOPPINESS" .github/workflows/release.yml` is >= 1 and `grep -c "CCACHE_EXTRAFILES" .github/workflows/release.yml` is >= 1
    - `.github/workflows/release.yml` contains an `actions/cache/restore@v5` (or `actions/cache@v5`) step whose `path` is a ccache dir, for each of macOS, Linux, Windows (3 restore steps)
    - `.github/workflows/release.yml` contains a matching ccache `actions/cache/save@v5` step for each OS (3 save steps), gated `if: always() && <os-cond> && steps.<id>.outputs.cache-hit != 'true'`
    - `grep -c "ccache-\${{ matrix.os }}-pch-v2-" .github/workflows/release.yml` is >= 3 (key aligned with ci.yml lineage)
    - `grep -c "ccache-\${{ matrix.os }}-pch-v2-\${{ github.sha }}" .github/workflows/release.yml` is >= 3 (primary key form matches ci.yml)
    - Each ccache restore step appears before its OS's "Configure" step and each save step after its OS's "Build" step (verify ordering by reading the file)
    - `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/release.yml'))"` exits 0 (valid YAML) — or `actionlint .github/workflows/release.yml` passes if available
    - No `CMAKE_C_COMPILER_LAUNCHER` / `CMAKE_CXX_COMPILER_LAUNCHER` lines were added to release.yml (the presets own that)
  </acceptance_criteria>
  <done>release.yml's package job gains a new job-level env block (CCACHE_SLOPPINESS/EXTRAFILES — none existed before) and restores ccache before configure / saves it after build on macOS, Linux, and Windows, with keys aligned to ci.yml's ccache-<os>-pch-v2- lineage; the workflow remains valid YAML.</done>
</task>

<task type="auto">
  <name>Task 2 (OPTIONAL / fallback — item ③): ship a disabled MSVC /O1 escape hatch for the slow TU</name>
  <files>source/DOCKING/COMMON/sources.cmake</files>
  <read_first>
    - source/DOCKING/COMMON/sources.cmake (the build file listing poseClusteringSerialization.C / poseClusteringGraphViz.C — where a per-source COMPILE_OPTIONS override would live)
    - .planning/v1.7.2-BUILD-ACCEL-PLAN.md item ③ (the fallback rationale — "largely moot once ① lands")
    - .planning/phases/999.50-ward-serializer-deboost/999.50-01-SUMMARY.md (Plan 01's build outcome — consult to decide whether any heavy TU remains on MSVC; this plan is wave 2 / depends_on ["01"] so that SUMMARY exists)
  </read_first>
  <action>
    This is the OPTIONAL fallback (plan item ③) and is NON-BLOCKING. Because this plan is wave 2 and depends
    on Plan 01, consult Plan 01's build outcome (SUMMARY / build log) FIRST: after Plan 01 eliminates
    boost::serialization the serialization TU should compile in seconds, making this moot. Default-skip item ③
    as moot unless Plan 01's outcome shows a TU still profiling pathologically on MSVC.

    If you do ship the hatch: do NOT enable /O1 by default. Add a COMMENTED, ready-to-enable
    `set_source_files_properties(... COMPILE_OPTIONS "/O1")` block (guarded `if(MSVC)`) in
    source/DOCKING/COMMON/sources.cmake, naming the specific slow TU (poseClusteringSerialization.C, or
    whatever still profiles hot on MSVC), with a comment citing Phase 999.50 plan item ③ and stating it is a
    fallback to enable ONLY if MSVC release-build profiling still shows that TU as pathological after the
    boost-serialization removal. Leave it disabled (commented out) so the default build is unchanged.

    Skip-condition (the default per the maintainer spec, now that this runs after Plan 01): if Plan 01's
    outcome confirms no heavy compile remains, or adding even a commented block would conflict with the build
    file's structure, record in the SUMMARY that item ③ was intentionally skipped as moot and make NO change to
    sources.cmake. Either outcome (disabled escape-hatch comment, or documented skip) is acceptable.
  </action>
  <verify>
    <automated>grep -c "O1\|999.50\|fallback" source/DOCKING/COMMON/sources.cmake; echo "ok — disabled hatch present OR intentionally skipped (see SUMMARY)"</automated>
  </verify>
  <acceptance_criteria>
    - EITHER: source/DOCKING/COMMON/sources.cmake contains a commented (disabled) `if(MSVC)` /O1 `set_source_files_properties` block referencing Phase 999.50 item ③ and the slow TU, and the default (non-MSVC and MSVC) build is unchanged (the /O1 line is not active)
    - OR: source/DOCKING/COMMON/sources.cmake is unchanged and the SUMMARY explicitly records item ③ skipped as moot (Plan 01's boost elimination removed the heavy compile, confirmed from its SUMMARY/build log)
    - In NEITHER case is `/O1` active by default — no enabled per-source optimization override is committed
  </acceptance_criteria>
  <done>The MSVC /O1 fallback is either present as a clearly-labelled, disabled escape hatch in sources.cmake or intentionally skipped as moot (informed by Plan 01's build outcome) with a SUMMARY note; the default build behavior is unchanged.</done>
</task>

</tasks>

<verification>
- `grep -c "ccache-\${{ matrix.os }}-pch-v2-" .github/workflows/release.yml` → >= 3 (restore + save across 3 OSes, key aligned with ci.yml)
- `grep -c "CCACHE_SLOPPINESS" .github/workflows/release.yml` → >= 1 (new job-level env block)
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"` → exit 0 (valid YAML)
- ccache restore precedes Configure and save follows Build for each OS
- /O1 is NOT active by default (item ③ disabled or skipped)
</verification>

<success_criteria>
release.yml's package job gains a NEW job-level env block (CCACHE_SLOPPINESS/EXTRAFILES — none existed
before) and restores/saves a per-OS ccache build cache with keys aligned to ci.yml's
ccache-<os>-pch-v2- lineage (so a release tag of an already-CI-built commit hits warm cache); the
workflow stays valid. The MSVC /O1 fallback ships as a disabled escape hatch or is documented as
skipped (informed by Plan 01's build outcome) — never active by default.
</success_criteria>

<output>
After completion, create `.planning/phases/999.50-ward-serializer-deboost/999.50-02-SUMMARY.md`
</output>
</content>
