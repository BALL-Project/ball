---
phase: 02.1-renderer-boundary-extraction
verified: 2026-05-14T00:00:00Z
status: passed
score: 4/4 must-haves verified
overrides_applied: 0
---

# Phase 02.1: Renderer Boundary Extraction Verification Report

**Phase Goal:** Extract a clean, Qt-GL-free renderer/surface boundary so Phase 5 and Phase 05.1 become contained backend swaps, not another re-touch of scene.C. PURE REFACTOR — zero behaviour change, same pixels as post-Phase-2.
**Verified:** 2026-05-14
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | ARCH-01: `RenderSurface` interface owns context-lifecycle verbs; `RenderSetup::makeCurrent()`'s GL body moves behind it | ✓ VERIFIED | `include/BALL/VIEW/RENDERING/renderSurface.h:25` declares `class BALL_VIEW_EXPORT RenderSurface : public RenderTarget` with pure-virtual `beginFrame()` (`:31`), `endFrame()` (`:34`), `nativeHandle()` (`:37`); zero Qt-GL types in signatures. `renderSetup.C:307-311` — `makeCurrent()` is now a `dynamic_cast<RenderSurface*>` delegation; old `QOpenGLContext::currentContext() != gl_target_` branch is gone. |
| 2 | ARCH-02: `RendererFactory` constructs renderers/surfaces by enum; `scene.C` has zero `new GLRenderWindow` / `dynamic_cast<GLRenderWindow\|GLRenderer>` in non-deferred paths | ✓ VERIFIED | `rendererFactory.h` + `rendererFactory.C` exist; `enum class Kind` (`:35`), `makeRenderer(Kind)` (`:46`), `makeSurface(Kind, QWidget*)` (`:52`); `rendererFactory.C` listed in `sources.cmake:8`. `scene.C` has ZERO `dynamic_cast<GLRenderWindow\|GLRenderer>` / `RTTI::isKindOf<GLRenderWindow\|GLRenderer>`. Primary paths route through `RendererFactory::makeRenderer/makeSurface` (`scene.C:136,152,2205`). |
| 3 | ARCH-03: `Renderer` base gains batched `renderRepresentations_()` + non-pure `capabilities()`; existing immediate-mode renderers untouched | ✓ VERIFIED | `renderer.h:191` `struct Caps`, `:204` `virtual Caps capabilities() const;` (NON-pure — no `= 0`), `:212` `virtual void renderRepresentations_(const RepresentationList&)`. `renderer.C:89/101` implement both; default `renderRepresentations_()` fans out to `renderOneRepresentation()`. No subclass files modified. |
| 4 | ARCH-04: BALLView builds and renders identically to post-Phase-2 (pure refactor, no behaviour change) | ✓ VERIFIED | `make BALL VIEW BALLView -j8` builds 100%, zero errors. Human visual verification PASSED — user confirmed all 4 checks ("1: ok 2: ok 3: ok 4: ok"): molecule renders embedded + correct, rotate/zoom/pick work, resize keeps the scene, no new warnings; identical to post-Phase-2. No new backend, no Qt 6 work, stereo/multi-display still guard-deferred. |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `include/BALL/VIEW/RENDERING/renderSurface.h` | RenderSurface abstract interface, Qt-GL-free | ✓ VERIFIED | Subclass of RenderTarget; 3 pure-virtual verbs; grep for QOpenGL/QGL/QWidget/QSurface returns nothing |
| `source/VIEW/RENDERING/glRenderWindow.C` | GLRenderWindow::beginFrame/endFrame impls | ✓ VERIFIED | Implemented (per 02.1-01-SUMMARY); build links clean |
| `include/BALL/VIEW/RENDERING/rendererFactory.h` | Kind enum + makeRenderer/makeSurface | ✓ VERIFIED | `enum class Kind`, both factory function decls present, QWidget forward-declared |
| `source/VIEW/RENDERING/rendererFactory.C` | Factory impl; in sources.cmake | ✓ VERIFIED | `new GLRenderWindow`/`new GLRenderer` for OpenGL_Fixed; listed in `sources.cmake:8` |
| `source/VIEW/WIDGETS/scene.C` | registerRenderers_ + main display via factory | ✓ VERIFIED | Constructs via `RendererFactory::make*`; zero concrete-type dynamic_cast/RTTI sites |
| `include/BALL/VIEW/RENDERING/RENDERERS/renderer.h` | renderRepresentations_ + capabilities + Caps | ✓ VERIFIED | All three declared; capabilities() non-pure |
| `source/VIEW/RENDERING/RENDERERS/renderer.C` | Default fan-out + default capabilities | ✓ VERIFIED | `Renderer::renderRepresentations_` fans out to `renderOneRepresentation`; `Renderer::capabilities` returns default immediate-mode Caps |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `renderSetup.C` | `RenderSurface::beginFrame` | `makeCurrent()` delegates to surface | ✓ WIRED | `renderSetup.C:307-309` `dynamic_cast<RenderSurface*>(target)->beginFrame()` |
| `renderer.C` | `renderOneRepresentation` | default `renderRepresentations_` fans out | ✓ WIRED | `renderer.C:106-110` iterates list, calls `renderOneRepresentation(**it)` |
| `scene.C` | `RendererFactory::makeSurface` | Scene builds main_display_ via factory | ✓ WIRED | `scene.C:152` `main_display_(static_cast<GLRenderWindow*>(RendererFactory::makeSurface(...)))` |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| BALLView builds clean | `make BALL VIEW BALLView -j8` | `[100%] Built target BALLView`, zero errors | ✓ PASS |
| scene.C concrete-type sites removed | `grep -nE 'dynamic_cast<GLRenderWindow\|dynamic_cast<GLRenderer\|RTTI::isKindOf<GLRender...'` | ZERO HITS | ✓ PASS |
| renderSurface.h is Qt-GL-free | `grep -nE 'QOpenGL\|QGL\|QWidget\|QSurface' renderSurface.h` | no matches | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| ARCH-01 | 02.1-01 | RenderSurface interface owns context-lifecycle verbs; makeCurrent body moves behind it | ✓ SATISFIED | renderSurface.h + renderSetup.C delegation verified |
| ARCH-02 | 02.1-03 | RendererFactory constructs by enum; scene.C zero new GLRenderWindow / dynamic_cast in non-deferred paths | ✓ SATISFIED | rendererFactory.{h,C} + scene.C zero concrete-type sites; 9 `new GLRenderWindow` residuals all in guard-deferred stereo methods |
| ARCH-03 | 02.1-02 | Renderer gains batched renderRepresentations_() + capabilities(); existing renderers untouched | ✓ SATISFIED | renderer.h/.C additive, non-pure, default fan-out |
| ARCH-04 | 02.1-03 | BALLView builds and renders identically to post-Phase-2 | ✓ SATISFIED | Clean build + human visual verification PASSED (4/4 checks) |

### Anti-Patterns Found

None blocking. The 9 residual `new GLRenderWindow` 3-arg-ctor sites in `scene.C` (lines 2500, 2526, 2609, 2616, 2717, 2788, 2816, 2889, 2917) were confirmed to all reside inside the guard-deferred stereo/multi-display methods (`addGlWindow`, `addRTfactWindow`, `enterStereo`, `enterActiveStereo`, `enterDualStereoDifferentDisplays`). These are EXPECTED, documented residuals — the 2-arg `makeSurface(Kind, QWidget*)` does not cover the 3-arg ctor by design, and those paths are guard-deferred to Phase 5. Not a gap.

### Human Verification Required

None outstanding. The blocking human-verify checkpoint (ARCH-04, Task 3 of Plan 03) was completed — the user confirmed BALLView renders identically to post-Phase-2.

### Gaps Summary

No gaps. The renderer/surface boundary is fully extracted:
- `RenderSurface` interface (ARCH-01) — Qt-GL-free, owns `beginFrame`/`endFrame`/`nativeHandle`; `RenderSetup::makeCurrent()` is now a neutral delegation.
- `RendererFactory` (ARCH-02) — constructs by `Kind`; `scene.C` primary paths name no concrete `GLRenderWindow`/`GLRenderer` type; all `dynamic_cast`/`RTTI::isKindOf` concrete-type sites eliminated.
- `Renderer::renderRepresentations_()` + `capabilities()` (ARCH-03) — additive, non-pure, behaviour-preserving default fan-out; zero subclass changes.
- Pure refactor confirmed (ARCH-04) — clean build, human-verified pixel-identical render, no new backend, no Qt 6 work, stereo guard-deferred as before.

Phase 5 and Phase 05.1 are now contained backend swaps (one new factory `Kind` + 1-2 files), not another re-touch of scene.C. Phase goal achieved.

---

_Verified: 2026-05-14_
_Verifier: Claude (gsd-verifier)_
