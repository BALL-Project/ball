---
phase: 01-build-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CMakeLists.txt
  - BUILD-macos.md
autonomous: true
requirements: [BUILD-01, BUILD-02, BUILD-03, BUILD-04]
must_haves:
  truths:
    - "The 8 modern-toolchain patches are committed to git history with descriptive messages explaining why"
    - "The CMake PROJECT declaration reads version 1.6.0 with a -dev pre-release marker"
    - "BUILD-macos.md walks a new contributor through Homebrew dependency install, the cmake configure command, the build command, and the run command"
    - "A clean reconfigure + build of BALL still succeeds after the version bump"
  artifacts:
    - path: "BUILD-macos.md"
      provides: "macOS build/run documentation for new contributors"
      min_lines: 30
    - path: "CMakeLists.txt"
      provides: "Project version 1.6.0-dev"
      contains: "VERSION 1.6.0"
  key_links:
    - from: "git working tree"
      to: "git history"
      via: "git commit of the 8 patch files in logical groups"
      pattern: "networking|cmake|contourSurface"
    - from: "BUILD-macos.md"
      to: "verified build commands"
      via: "copied verbatim from ROADMAP-1.6.md"
      pattern: "make BALL VIEW BALLView"
---

<objective>
Land the modern-toolchain build baseline: commit the 8 already-applied (uncommitted) toolchain patches in logical groups, bump the project version to 1.6.0-dev, write BUILD-macos.md documenting the verified Homebrew build/run flow, and verify a clean reconfigure + build still works after the version bump.

Purpose: Establish a reproducible, committed, versioned, documented starting point for all later phases. The patches currently exist only as uncommitted working-tree modifications in a shallow clone — they risk being lost until committed.
Output: 3+ git commits (grouped patches + version bump + doc), an updated CMakeLists.txt, a new BUILD-macos.md, and a verified rebuild.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/Users/kohlbach/Claude/BALL/ROADMAP-1.6.md

# CRITICAL CURRENT STATE
# - /Users/kohlbach/Claude/BALL/ball is a SHALLOW git clone (depth 1, single commit
#   d85d2dd dated 2022-05-24). The 8 toolchain patches exist ONLY as uncommitted
#   working-tree modifications. Commit them FIRST or they risk being lost.
# - `git status --short` shows exactly these 8 modified files (all already edited,
#   built, and verified — BALL/VIEW/BALLView build and BALLView.app launches):
#     M CMakeLists.txt
#     M cmake/BALLCompilerSpecific.cmake
#     M cmake/BALLConfigBoost.cmake
#     M cmake/BALLViewTranslations.cmake
#     M cmake/FindEigen3.cmake
#     M include/BALL/DATATYPE/contourSurface.h
#     M include/BALL/SYSTEM/networking.h
#     M source/SYSTEM/networking.C
# - `.gitignore` lines 19-20 already cover `build/*` and `build*/*`.
# - `.planning/` is NOT gitignored — DO NOT use `git add -A` or `git add .`.
#   Stage only the explicit source file paths listed in each task.
#
# WHAT EACH PATCH DOES (for writing commit messages — the message must explain WHY):
#   CMakeLists.txt              — CMAKE_MINIMUM_REQUIRED bumped to 3.5...3.31; CMP0042 NEW;
#                                 added CMP0167 NEW; INCLUDE(Dart) -> INCLUDE(CTest)
#                                 (Dart module removed from CMake); GLEW linkage uses
#                                 GLEW::GLEW imported target when present.
#   cmake/BALLConfigBoost.cmake — switched to Boost config-mode package; dropped the
#                                 `system` component (header-only since Boost 1.69);
#                                 provide Boost_LIBRARIES from imported targets.
#   cmake/FindEigen3.cmake      — read version macros from Eigen/Version (Eigen >= 5.0
#                                 moved them out of src/Core/util/Macros.h).
#   cmake/BALLCompilerSpecific.cmake — Clang build standard c++11 -> c++14 (bridge:
#                                 BALL's legacy code uses std::unary_function/
#                                 binary_function/bind2nd removed in C++17; C++14
#                                 still has them).
#   cmake/BALLViewTranslations.cmake — ADD_CUSTOM_COMMAND given explicit POST_BUILD.
#   include/BALL/SYSTEM/networking.h — boost::asio::io_service -> io_context;
#                                 TCPIOStream ctors pass .c_str() (resolver now wants
#                                 string_view).
#   source/SYSTEM/networking.C  — io_service::post -> boost::asio::post;
#                                 ::reset() -> ::restart(); acceptor_.accept(*rdbuf())
#                                 -> acceptor_.accept(stream.socket()).
#   include/BALL/DATATYPE/contourSurface.h — operator== referenced non-existent member
#                                 data.data_; corrected to Surface::operator==(data)
#                                 (latent pre-existing bug exposed by stricter compiler).
#
# VERIFIED BUILD/RUN COMMANDS (from ROADMAP-1.6.md — copy accurately into BUILD-macos.md):
#   cd /Users/kohlbach/Claude/BALL/ball && mkdir -p build && cd build
#   export PATH="/opt/homebrew/opt/bison/bin:/opt/homebrew/opt/flex/bin:$PATH"
#   cmake .. -DCMAKE_POLICY_VERSION_MINIMUM=3.5 \
#     -DCMAKE_PREFIX_PATH="/opt/homebrew/opt/qt@5;/opt/homebrew" \
#     -DCMAKE_BUILD_TYPE=Release \
#     -DBISON_EXECUTABLE=/opt/homebrew/opt/bison/bin/bison \
#     -DFLEX_EXECUTABLE=/opt/homebrew/opt/flex/bin/flex \
#     -DBALL_PYTHON_SUPPORT=OFF -DUSE_RTFACT=OFF -DBALL_HAS_VIEW=ON
#   make BALL VIEW BALLView -j8
#   # run:
#   BALL_DATA_PATH=$PWD/../data BALLVIEW_DATA_PATH=$PWD/../data DYLD_LIBRARY_PATH=$PWD/lib \
#     bin/BALLView.app/Contents/MacOS/BALLView
#   # Homebrew deps:
#   brew install qt@5 boost eigen fftw tbb glew open-babel lp_solve libsvm flex bison
#   # ball_contrib is NOT used.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the 8 toolchain patches in logical groups</name>
  <read_first>
    - /Users/kohlbach/Claude/BALL/ball/.gitignore
    - Run `git status --short` and `git log --oneline -1` to confirm the working-tree state and the single shallow-clone commit before staging anything.
  </read_first>
  <files>
    CMakeLists.txt, cmake/BALLCompilerSpecific.cmake, cmake/BALLConfigBoost.cmake,
    cmake/BALLViewTranslations.cmake, cmake/FindEigen3.cmake,
    include/BALL/DATATYPE/contourSurface.h, include/BALL/SYSTEM/networking.h,
    source/SYSTEM/networking.C
  </files>
  <action>
    Commit the 8 uncommitted toolchain patch files in 3 logical commits. Stage files
    by EXPLICIT path only — never `git add -A`, `git add .`, or `git add -u` (the
    untracked `.planning/` directory must not be swept in). Each commit message must
    explain WHY the change was needed, not just what changed.

    Commit 1 — the 5 CMake files:
      git add CMakeLists.txt cmake/BALLCompilerSpecific.cmake cmake/BALLConfigBoost.cmake cmake/BALLViewTranslations.cmake cmake/FindEigen3.cmake
      Message subject: "cmake: modernize build for CMake 3.31 and modern Find modules"
      Body must mention: CMAKE_MINIMUM_REQUIRED 3.5...3.31, CMP0042/CMP0167 NEW,
      INCLUDE(Dart)->INCLUDE(CTest) (Dart removed from CMake), GLEW::GLEW imported
      target, Boost config-mode + dropped header-only `system` component, FindEigen3
      reads version from Eigen/Version (Eigen>=5.0 moved the macros), Clang standard
      c++11->c++14 bridge for C++17-removed constructs, BALLViewTranslations explicit
      POST_BUILD.

    Commit 2 — networking (2 files):
      git add include/BALL/SYSTEM/networking.h source/SYSTEM/networking.C
      Message subject: "system: port Boost.Asio networking to the Boost 1.90 API"
      Body must mention: io_service->io_context, io_service::post->boost::asio::post,
      reset()->restart(), acceptor_.accept(*rdbuf())->acceptor_.accept(stream.socket())
      (Asio made basic_socket_streambuf's socket base private), TCPIOStream ctors pass
      .c_str() because the resolver now takes string_view.

    Commit 3 — the latent bug fix:
      git add include/BALL/DATATYPE/contourSurface.h
      Message subject: "datatype: fix latent contourSurface operator== bug"
      Body must mention: operator== referenced a non-existent member data.data_;
      corrected to Surface::operator==(data); pre-existing bug exposed by a stricter
      modern compiler.

    Do NOT skip hooks or sign-off flags. Use HEREDOC for multi-line messages.
    After all 3 commits, run `git status --short` — it must show no modified tracked
    source files remaining (only untracked `.planning/` and `build/` artifacts, the
    latter gitignored).
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && git log --oneline -4 | grep -qE "cmake: modernize" && git log --oneline -4 | grep -qE "system: port Boost.Asio" && git log --oneline -4 | grep -qE "datatype: fix latent" && [ -z "$(git status --short -- CMakeLists.txt cmake/ include/ source/)" ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -4` contains 3 new commits with subjects matching "cmake: modernize", "system: port Boost.Asio", and "datatype: fix latent".
    - `git status --short -- CMakeLists.txt cmake/ include/ source/` produces empty output (all 8 patch files committed).
    - `git show --stat HEAD~2..HEAD` shows exactly the 8 named files across the 3 commits and no others.
    - No `.planning/` file appears in `git log --stat HEAD~2..HEAD`.
  </acceptance_criteria>
  <done>The 8 toolchain patches are committed in 3 logical commits with why-focused messages; no tracked source file remains modified; .planning/ was not committed.</done>
</task>

<task type="auto">
  <name>Task 2: Bump project version to 1.6.0-dev and write BUILD-macos.md</name>
  <read_first>
    - /Users/kohlbach/Claude/BALL/ball/CMakeLists.txt (specifically line ~23: `PROJECT("BALL" VERSION 1.5.0)`)
    - /Users/kohlbach/Claude/BALL/ROADMAP-1.6.md (the "Configure / build commands that work today" section and the Homebrew dependency table)
  </read_first>
  <files>CMakeLists.txt, BUILD-macos.md</files>
  <action>
    Part A — version bump (BUILD-03):
    In CMakeLists.txt, change the line `PROJECT("BALL" VERSION 1.5.0)` to
    `PROJECT("BALL" VERSION 1.6.0)` and append an inline comment on the same line:
    `# 1.6.0-dev (pre-release)`. CMake VERSION fields must be numeric, so the numeric
    value is 1.6.0 and the comment carries the -dev pre-release marker. Do not touch
    any other line in CMakeLists.txt.

    Part B — BUILD-macos.md (BUILD-04):
    Create /Users/kohlbach/Claude/BALL/ball/BUILD-macos.md at the repo root. It must
    document, in order, with fenced code blocks copied accurately from ROADMAP-1.6.md:
      1. Title and one-paragraph intro: building BALL/VIEW/BALLView on macOS (Apple
         Silicon) against Homebrew dependencies. State explicitly that `ball_contrib`
         is NOT used and should not be revived.
      2. "Dependencies" section with the exact Homebrew install line:
         `brew install qt@5 boost eigen fftw tbb glew open-babel lp_solve libsvm flex bison`
      3. "Configure" section with the exact working cmake command:
         - `mkdir -p build && cd build`
         - `export PATH="/opt/homebrew/opt/bison/bin:/opt/homebrew/opt/flex/bin:$PATH"`
         - the full `cmake .. -DCMAKE_POLICY_VERSION_MINIMUM=3.5 ...` invocation with
           all flags: CMAKE_PREFIX_PATH="/opt/homebrew/opt/qt@5;/opt/homebrew",
           CMAKE_BUILD_TYPE=Release, BISON_EXECUTABLE, FLEX_EXECUTABLE,
           BALL_PYTHON_SUPPORT=OFF, USE_RTFACT=OFF, BALL_HAS_VIEW=ON.
      4. "Build" section with: `make BALL VIEW BALLView -j8`
      5. "Run" section with the run command including BALL_DATA_PATH,
         BALLVIEW_DATA_PATH, and DYLD_LIBRARY_PATH env vars:
         `BALL_DATA_PATH=$PWD/../data BALLVIEW_DATA_PATH=$PWD/../data DYLD_LIBRARY_PATH=$PWD/lib bin/BALLView.app/Contents/MacOS/BALLView`
      6. A short "Notes" section: `ball_contrib` is NOT used; Python bindings,
         OpenBabel, TBB, RTfact are currently disabled and do not block the build.
    The file must be at least 30 lines. Use real fenced ```sh code blocks so commands
    are copy-pasteable.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && grep -qE 'PROJECT\("BALL" VERSION 1\.6\.0\)' CMakeLists.txt && grep -q '1.6.0-dev' CMakeLists.txt && test -f BUILD-macos.md && grep -q 'brew install qt@5 boost eigen fftw tbb glew open-babel lp_solve libsvm flex bison' BUILD-macos.md && grep -q 'make BALL VIEW BALLView -j8' BUILD-macos.md && grep -q 'BALLVIEW_DATA_PATH' BUILD-macos.md && grep -q 'CMAKE_POLICY_VERSION_MINIMUM=3.5' BUILD-macos.md && [ $(wc -l < BUILD-macos.md) -ge 30 ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E 'PROJECT\("BALL" VERSION 1\.6\.0\)' CMakeLists.txt` matches.
    - `grep '1.6.0-dev' CMakeLists.txt` matches (the pre-release comment).
    - `grep 'VERSION 1.5.0' CMakeLists.txt` produces NO output.
    - `BUILD-macos.md` exists at the repo root, is >= 30 lines.
    - BUILD-macos.md contains the exact strings: the brew install line, `make BALL VIEW BALLView -j8`, `CMAKE_POLICY_VERSION_MINIMUM=3.5`, `BALLVIEW_DATA_PATH`, and a mention that `ball_contrib` is not used.
  </acceptance_criteria>
  <done>CMakeLists.txt declares VERSION 1.6.0 with a -dev pre-release comment; BUILD-macos.md exists at repo root documenting dependency install, configure, build, and run with verified commands.</done>
</task>

<task type="auto">
  <name>Task 3: Verify clean reconfigure + build, then commit version bump and docs</name>
  <read_first>
    - /Users/kohlbach/Claude/BALL/ball/BUILD-macos.md (the configure/build commands just written — use them as the source of truth for this verification)
    - Run `git status --short` to confirm only CMakeLists.txt is modified and BUILD-macos.md is untracked before committing.
  </read_first>
  <files>CMakeLists.txt, BUILD-macos.md</files>
  <action>
    Part A — verify the build still works after the version bump (BUILD-01):
    From /Users/kohlbach/Claude/BALL/ball, run the documented configure + build to
    confirm the version bump broke nothing. A `build/` directory likely already
    exists from prior work; an incremental reconfigure is acceptable and faster:
      cd /Users/kohlbach/Claude/BALL/ball && mkdir -p build && cd build
      export PATH="/opt/homebrew/opt/bison/bin:/opt/homebrew/opt/flex/bin:$PATH"
      cmake .. -DCMAKE_POLICY_VERSION_MINIMUM=3.5 \
        -DCMAKE_PREFIX_PATH="/opt/homebrew/opt/qt@5;/opt/homebrew" \
        -DCMAKE_BUILD_TYPE=Release \
        -DBISON_EXECUTABLE=/opt/homebrew/opt/bison/bin/bison \
        -DFLEX_EXECUTABLE=/opt/homebrew/opt/flex/bin/flex \
        -DBALL_PYTHON_SUPPORT=OFF -DUSE_RTFACT=OFF -DBALL_HAS_VIEW=ON
      make BALL -j8
    The cmake reconfigure must complete without error and must report the project
    version as 1.6.0 (look for it in cmake output or CMakeCache.txt:
    `grep BALL_VERSION build/CMakeCache.txt` if BALL exposes it, otherwise confirm
    the configure step exits 0). `make BALL -j8` must exit 0. This is a long-running
    command — allow up to 10 minutes; run it with an extended timeout. If `make BALL`
    is impractically slow because the build tree is clean, an incremental
    `make BALLView` against the existing build tree is an acceptable fallback per the
    phase brief, but prefer `make BALL`.

    Part B — commit the version bump and the doc:
    Only after the build verification passes, stage and commit by explicit path:
      git add CMakeLists.txt
      Commit subject: "build: bump project version to 1.6.0-dev"
      Body: notes this is the pre-release version for the v1.6 modernization
      milestone; CMake VERSION must be numeric so the -dev marker is a comment.
      git add BUILD-macos.md
      Commit subject: "docs: add BUILD-macos.md for the Homebrew build flow"
      Body: documents the verified macOS (Apple Silicon) dependency install,
      configure, build, and run flow against Homebrew packages; ball_contrib is not
      used.
    Never `git add -A` / `git add .`. After committing, run `git status --short` —
    no tracked source/doc file should remain uncommitted (only the gitignored
    `build/` tree and the untracked `.planning/` directory may appear).
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball && git log --oneline -3 | grep -qE "build: bump project version to 1.6.0-dev" && git log --oneline -3 | grep -qE "docs: add BUILD-macos.md" && git ls-files --error-unmatch BUILD-macos.md >/dev/null 2>&1 && [ -z "$(git status --short -- CMakeLists.txt BUILD-macos.md)" ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - The cmake configure command from BUILD-macos.md completes with exit code 0 against the bumped CMakeLists.txt.
    - `make BALL -j8` (or the documented incremental fallback) completes with exit code 0.
    - `git log --oneline -3` contains commits with subjects "build: bump project version to 1.6.0-dev" and "docs: add BUILD-macos.md".
    - `git ls-files --error-unmatch BUILD-macos.md` succeeds (file is tracked).
    - `git status --short -- CMakeLists.txt BUILD-macos.md` produces empty output.
  </acceptance_criteria>
  <done>A clean/incremental reconfigure + build succeeds with the bumped version; the version bump and BUILD-macos.md are committed by explicit path; no tracked file remains uncommitted.</done>
</task>

</tasks>

<verification>
- `git log --oneline -7` shows the 3 patch-group commits + version-bump commit + docs commit, all with descriptive why-focused messages.
- `grep 'PROJECT("BALL" VERSION 1.6.0)' CMakeLists.txt` matches; `grep '1.6.0-dev' CMakeLists.txt` matches; no `VERSION 1.5.0` remains.
- `BUILD-macos.md` exists at the repo root, is tracked by git, >= 30 lines, contains the verified Homebrew install / cmake configure / make / run commands.
- The documented cmake configure + `make BALL -j8` complete with exit code 0 after the version bump.
- `git status --short` shows no modified or untracked tracked source files (only the gitignored `build/` tree and the untracked `.planning/` directory).
</verification>

<success_criteria>
- BUILD-02: The 8 modern-toolchain patches are committed in 3 logical commits with clear, why-focused messages.
- BUILD-03: CMakeLists.txt PROJECT declaration reads `VERSION 1.6.0` with a `# 1.6.0-dev (pre-release)` comment.
- BUILD-04: BUILD-macos.md exists at the repo root and walks a new contributor through dependency install, configure, build, and run.
- BUILD-01: A clean/incremental reconfigure + build of BALL succeeds after the version bump, proving the baseline is reproducible.
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-baseline/01-01-SUMMARY.md`
</output>
