---
phase: 05.1-build-warnings-and-latent-bugs
plan: 03
subsystem: solvation
tags: [clang, -Wself-assign-field, copy-assignment, latent-bug, codex-verified]

# Dependency graph
requires:
  - phase: 05-qt6-migration
    provides: Qt 6 + clang baseline (Tier A latent-bug surfacing depends on the post-Qt6 toolchain)
provides:
  - "operator= in PairExpInteractionEnergyProcessor now propagates rdf_parameter_ from the source object (matches the copy ctor's existing initializer)"
affects: [05.1-04, 05.1-CI, SOLVATION energy-processor users]

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - source/SOLVATION/pairExpInteractionEnergyProcessor.C

key-decisions:
  - "Used parameter name `proc` (as verified in the operator= signature at line 117 and confirmed by the copy-ctor's `rdf_parameter_(proc.rdf_parameter_)` initializer at line 91)"

patterns-established: []

requirements-completed: []

# Metrics
duration: 3min
completed: 2026-05-15
---

# Phase 05.1 Plan 03: -Wself-assign-field in pairExpInteractionEnergyProcessor.C Summary

**One-line surgical fix: `rdf_parameter_ = rdf_parameter_;` → `rdf_parameter_ = proc.rdf_parameter_;` at line 124 of `source/SOLVATION/pairExpInteractionEnergyProcessor.C` — silently broken copy-assignment in PairExpInteractionEnergyProcessor now correctly propagates the rdf_parameter_ field, matching the copy ctor's existing member-initializer.**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-05-15
- **Completed:** 2026-05-15
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- BACKLOG.md Task A3 closed: `-Wself-assign-field` in `pairExpInteractionEnergyProcessor.C` eliminated at its source.
- Restores correct copy-assignment semantics for `PairExpInteractionEnergyProcessor` — copy-assigning an instance now actually propagates `rdf_parameter_` from the source (the bug silently retained the destination's prior `RadialDistributionFunction` state).
- Local libBALL build green on macos-arm64 / Qt 6.11; ABI-stable, link-compatible one-line edit.

## Task Commits

Each task was committed atomically:

1. **Task 1: Propagate rdf_parameter_ from the source in operator=** — `066195c` (fix)

_Note: this is a 1-task plan; no plan-metadata commit beyond the SUMMARY commit below._

## Files Created/Modified

- `source/SOLVATION/pairExpInteractionEnergyProcessor.C` — Line 124 changed from `rdf_parameter_ = rdf_parameter_;` to `rdf_parameter_ = proc.rdf_parameter_;`. Pure body edit inside the `operator=` defined at lines 115–127. No header changes; no signature changes; no ABI / link-compat impact. Other 199 lines byte-identical.

## Decisions Made

- **Parameter name `proc`** (rather than e.g. `other` or `rhs`): verified two ways before the edit — (a) the `operator=` signature at line 117 takes `const PairExpInteractionEnergyProcessor& proc`, and (b) the copy ctor a few lines above at line 84 also uses `proc` and correctly initializes `rdf_parameter_(proc.rdf_parameter_)` at line 91. The fix follows the existing in-file convention precisely.

## Deviations from Plan

None — plan executed exactly as written.

- No Rule 1/2/3 auto-fixes triggered.
- No additional files touched.
- Both source assertions (no self-assign match; exactly one `proc.rdf_parameter_` match at line 124) passed on first edit.
- Local `cmake --build build/ci-macos --target BALL -j 8` completed green on first compile.

**Total deviations:** 0
**Impact on plan:** Plan landed verbatim as the BACKLOG.md Task A3 proposal.

## Issues Encountered

None.

## Verification

- **Source assertion 1:** `grep -nE "^\s*rdf_parameter_\s*=\s*rdf_parameter_\s*;" source/SOLVATION/pairExpInteractionEnergyProcessor.C` returns no match (exit 1) — PASS.
- **Source assertion 2:** `grep -nE "rdf_parameter_\s*=\s*proc\.rdf_parameter_\s*;" source/SOLVATION/pairExpInteractionEnergyProcessor.C` returns exactly one match at line 124 — PASS.
- **Build assertion:** `cmake --build build/ci-macos --target BALL -j 8` — `[100%] Built target BALL` (libBALL.dylib linked green on macos-arm64 / Qt 6.11) — PASS.
- **CI assertion (deferred):** next macOS clang CI run must emit zero `-Wself-assign-field` warnings on this file. Verified on push.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Phase 05.1 Plan 04 (next Tier A BACKLOG item) is the next action.
- Once Plan 04+ land, push the v1.6-modernization branch to trigger CI; assert zero `-Wself-assign-field` on `pairExpInteractionEnergyProcessor.C` in the macOS clang job log, and verify all three OS jobs green.

## Self-Check: PASSED

- File `source/SOLVATION/pairExpInteractionEnergyProcessor.C` modified: FOUND (line 124 carries `rdf_parameter_ = proc.rdf_parameter_;`).
- Commit `066195c`: FOUND on `v1.6-modernization` (`git log --oneline -1` → `066195c fix(05.1-03): propagate rdf_parameter_ from source in operator=`).

---
*Phase: 05.1-build-warnings-and-latent-bugs*
*Completed: 2026-05-15*
