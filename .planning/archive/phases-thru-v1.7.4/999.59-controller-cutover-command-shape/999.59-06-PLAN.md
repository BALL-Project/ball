---
phase: 999.59-controller-cutover-command-shape
plan: 06
type: execute
release: v1.7.4
wave: 6
depends_on: [999.59-05]
autonomous: false   # human-verify checkpoint: PyBALL legacy attribute path + DeprecationWarning behavior is a runtime surface
requirements:
  - PYBALL-CONTROLLER-BINDINGS-01
files_modified:
  - source/PYTHON/EXTENSIONS/VIEW/representation.sip
  - source/PYTHON/EXTENSIONS/VIEW/controllers.sip
  - source/PYTHON/EXTENSIONS/VIEW/VIEW.sip
  - .github/workflows/ci.yml
must_haves:
  truths:
    - "representation.sip no longer exposes the narrowed setters (setTransparency / setDrawingMode / setColoringMethod / setModelType / ...) — they no longer exist on the public C++ surface after 999.59-04"
    - "A new controllers.sip exposes the 9 Controllers' public surface to PyBALL (e.g. mainControl.coloringController().setMethod('Element'); ...apply()); bindings see Controllers, never owners"
    - "A one-release deprecation shim preserves the legacy attribute path: representation.coloring_method = 'Element' still works in v1.7.4 but emits a DeprecationWarning naming the controller-surface replacement; removed in v1.8"
    - "A CI sip-refresh-check job regenerates the bindings and compiles them (blocking, Linux); it does not require a full PyBALL runtime"
    - "controllers.sip is registered in the VIEW.sip module include set so the regen picks it up"
  artifacts:
    - path: "source/PYTHON/EXTENSIONS/VIEW/controllers.sip"
      provides: "NEW SIP bindings exposing the 9 Controllers (replaces the narrowed Representation setter surface)"
      contains: "Controller"
    - path: "source/PYTHON/EXTENSIONS/VIEW/representation.sip"
      provides: "narrowed setters removed + %MethodCode deprecation shim forwarding to the controller surface"
      contains: "DeprecationWarning"
    - path: ".github/workflows/ci.yml"
      provides: "sip-refresh-check job: regenerate + compile the bindings (blocking, Linux)"
      contains: "sip-refresh"
  key_links:
    - from: "source/PYTHON/EXTENSIONS/VIEW/representation.sip"
      to: "source/PYTHON/EXTENSIONS/VIEW/controllers.sip"
      via: "deprecation shim %MethodCode forwards legacy attribute set to the controller surface"
      pattern: "DeprecationWarning|Controller"
    - from: ".github/workflows/ci.yml"
      to: "source/PYTHON/EXTENSIONS/VIEW/controllers.sip"
      via: "sip-refresh-check regenerates + compiles the bindings"
      pattern: "sip-refresh"
---

<objective>
Refresh the PyBALL SIP bindings in v1.7.4 (ARCHITECTURE-CONTRACT.md §3d,
Codex HIGH #4): privatizing the owner setters in 999.59-04 broke the
binding surface, so the binding work MUST land in the same release. Drop
the narrowed setters from `representation.sip`, add a new `controllers.sip`
exposing the 9 Controllers, ship a one-release deprecation shim that keeps
the legacy attribute path working with a `DeprecationWarning`, and add a CI
`sip-refresh-check` job that regenerates + compiles the bindings so a future
setter rename cannot silently rot the Python surface again.

Purpose: this is the LAST plan in 999.59 and is independently revertible
(the shim is a thin `.sip` `%MethodCode`). It makes the contract's
"bindings expose Controllers, not owners" promise enforceable in v1.7.4
instead of aspirational. Deep PyBALL test coverage stays v2.1; this plan
ships the binding + the build-check floor only.

Output: refreshed representation.sip, new controllers.sip, deprecation
shim, CI sip-refresh-check job. Ends with a human-verify checkpoint because
the DeprecationWarning + legacy-attribute runtime behavior is a Python
surface only a human (or a follow-on v2.1 test) can confirm at runtime.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.7.4-ARCHITECTURE-CONTRACT.md
@.planning/v1.7.4-ACCEPTANCE.md
@.planning/phases/999.59-controller-cutover-command-shape/999.59-00-OVERVIEW.md
@.planning/phases/999.59-controller-cutover-command-shape/999.59-04-PLAN.md

# The SIP files (read representation.sip to find the narrowed setters to drop):
@source/PYTHON/EXTENSIONS/VIEW/representation.sip
@source/PYTHON/EXTENSIONS/VIEW/VIEW.sip

# The narrowed C++ surface controllers.sip must mirror (from Plan 01/02/03):
@include/BALL/VIEW/KERNEL/controllers/controller.h

<interfaces>
<!-- §3d: representation.sip TODAY exposes setTransparency / setDrawingMode /
     setColoringMethod / setModelType etc. — exactly the setters 999.59-04
     privatized. These are REMOVED (no longer on the public C++ surface) and
     replaced by controller bindings. -->

<!-- New controllers.sip target API shape:
     mainControl.coloringController().setMethod("Element");
     mainControl.coloringController().apply();
     Bindings see Controllers, never owners. -->

<!-- One-release deprecation shim (thin %MethodCode forwarding to the
     controller): legacy `representation.coloring_method = "Element"` still
     works in v1.7.4, emits a DeprecationWarning naming the replacement,
     removed in v1.8. -->

<!-- CI: SIP is currently disabled in the modernization build (CLAUDE.md:
     "Removed/disabled: SIP Python bindings"). v1.7.4 re-enables a
     SIP-refresh build-check job (regenerate + compile; no full PyBALL
     runtime). New job: sip-refresh-check (ubuntu-24.04, blocking, ~60s),
     per ACCEPTANCE §4. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop narrowed setters from representation.sip; add controllers.sip + register it</name>
  <files>source/PYTHON/EXTENSIONS/VIEW/representation.sip, source/PYTHON/EXTENSIONS/VIEW/controllers.sip, source/PYTHON/EXTENSIONS/VIEW/VIEW.sip</files>
  <action>
In `representation.sip`: remove the narrowed setter declarations
(`setTransparency`, `setDrawingMode`, `setColoringMethod`, `setModelType`,
and any other setter privatized in 999.59-04) — they no longer exist on the
public C++ surface, so SIP cannot bind them. Keep the getters and the rest
of the Representation surface intact.

Create `controllers.sip` (NEW) exposing the 9 Controllers' public surface to
PyBALL per §3d: each Controller class (ModelController / ColoringController /
MaterialController / ClippingController / LabelController / StageController /
CameraController / LightController / StereoController) with its public
setters and `bool apply()`. Bindings see Controllers, never owners. Model
the access path so a script does
`mainControl.coloringController().setMethod("Element"); ....apply()`. Mirror
the C++ public surface exactly (the narrowed owner setters are NOT exposed —
only the Controller methods). Follow the existing .sip conventions in the
directory (look at an existing controller-adjacent .sip such as
`mainControl.sip` / `modifyRepresentationDialog.sip` for the module include
and class-declaration style).

Register `controllers.sip` in `VIEW.sip`'s `%Include` set so the regen picks
it up (mirror how `representation.sip` is included).
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball/.claude/worktrees/agent-a5fc75ad5e764c9ed && test -f source/PYTHON/EXTENSIONS/VIEW/controllers.sip && grep -q 'controllers.sip' source/PYTHON/EXTENSIONS/VIEW/VIEW.sip && ! grep -Eq '(setColoringMethod|setModelType|setDrawingMode|setTransparency)' source/PYTHON/EXTENSIONS/VIEW/representation.sip && echo OK</automated>
  </verify>
  <done>representation.sip no longer declares the narrowed setters; controllers.sip exposes the 9 Controllers; VIEW.sip includes controllers.sip.</done>
</task>

<task type="auto">
  <name>Task 2: Add the one-release deprecation shim + CI sip-refresh-check job</name>
  <files>source/PYTHON/EXTENSIONS/VIEW/representation.sip, .github/workflows/ci.yml</files>
  <action>
Deprecation shim: in `representation.sip`, add a thin `%MethodCode` (or a SIP
property handler) that preserves the legacy attribute path —
`representation.coloring_method = "Element"` (and the other legacy
attributes that mapped to the dropped setters) still WORKS in v1.7.4 by
forwarding to the matching controller surface, but emits a Python
`DeprecationWarning` naming the controller-surface replacement (e.g.
"Representation.coloring_method is deprecated; use
mainControl.coloringController().setMethod(...).apply(); removed in v1.8").
The shim is the one-release bridge — it is removed in v1.8.

CI: add a `sip-refresh-check` job to `.github/workflows/ci.yml`
(ubuntu-24.04, blocking, ~60s wall-clock per ACCEPTANCE §4) that runs on the
runner with SIP available: it regenerates the VIEW bindings from the .sip
set and COMPILES them (it does NOT require a full PyBALL runtime — compile
is the floor). The job fails if a .sip references a C++ symbol that no
longer exists (catching future setter-rename rot). Match the existing CI job
structure (steps/setup) used by the other v1.7.4 named gates (contract-tests
/ lifetime-discipline) so it slots into the matrix consistently. Note in a
comment that SIP was disabled in the modernization build (CLAUDE.md) and
this job re-enables the build-check only.

Scope discipline reminder for the executor: this plan MAY edit ci.yml (it is
listed in files_modified and is required by the contract). The agent that
DECOMPOSED the plans must not touch ci.yml; the EXECUTOR of this plan does.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball/.claude/worktrees/agent-a5fc75ad5e764c9ed && grep -q 'DeprecationWarning' source/PYTHON/EXTENSIONS/VIEW/representation.sip && grep -q 'sip-refresh' .github/workflows/ci.yml && echo OK</automated>
  </verify>
  <done>The legacy attribute path forwards to the controller surface + emits a DeprecationWarning; a blocking sip-refresh-check CI job regenerates + compiles the bindings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The PyBALL binding surface is refreshed for v1.7.4: the narrowed owner
    setters are gone from representation.sip, a new controllers.sip exposes
    the 9 Controllers, and a one-release deprecation shim keeps the legacy
    attribute path working with a DeprecationWarning. A blocking CI
    sip-refresh-check job regenerates + compiles the bindings.
  </what-built>
  <how-to-verify>
    1. In CI, confirm the sip-refresh-check job is green on the branch
       (regenerates + compiles the VIEW bindings).
    2. If a local PyBALL build is available: import the regenerated module,
       run `mainControl.coloringController().setMethod("Element"); ...apply()`
       through the new controllers.sip surface — the coloring must change.
    3. Exercise the legacy path: `representation.coloring_method = "Element"`.
       Confirm it (a) still applies the change, and (b) emits a
       DeprecationWarning naming the controller-surface replacement.
    4. Confirm no script that ONLY used the dropped direct setters fails
       silently — it must hit the shim + warn, not a hard AttributeError
       (the shim is the one-release bridge).
  </how-to-verify>
  <resume-signal>Type "approved" or describe which binding surface / warning behaved unexpectedly.</resume-signal>
</task>

</tasks>

<verification>
- representation.sip no longer exposes the narrowed setters.
- controllers.sip exists, exposes the 9 Controllers, registered in VIEW.sip.
- Legacy attribute path forwards + emits DeprecationWarning.
- sip-refresh-check CI job present (blocking, Linux) and green on the merge commit.
- Human verifies the controller surface + the deprecation behavior at runtime.
</verification>

<success_criteria>
- §3d / ACCEPTANCE §2e fully satisfied: bindings expose Controllers not owners; deprecation shim in place; CI build-check green.
- The plan is independently revertible (the shim is a thin %MethodCode).
</success_criteria>

<output>
After completion, create `.planning/phases/999.59-controller-cutover-command-shape/999.59-06-SUMMARY.md`
</output>
