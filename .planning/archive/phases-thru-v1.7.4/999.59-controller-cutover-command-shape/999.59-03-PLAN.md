---
phase: 999.59-controller-cutover-command-shape
plan: 03
type: execute
release: v1.7.4
wave: 3
depends_on: [999.59-02]
autonomous: false   # human-verify checkpoint: interactive surfaces (legacy dialogs + camera-keyboard adjust) must still drive the scene
requirements:
  - VIEW-INSP-07   # ClippingController
  - VIEW-INSP-08   # LabelController
  - VIEW-INSP-09   # StageController
  - VIEW-INSP-10   # CameraController
  - VIEW-INSP-11   # LightController
  - VIEW-INSP-12   # StereoController
files_modified:
  - include/BALL/VIEW/KERNEL/controllers/stageController.h
  - source/VIEW/KERNEL/controllers/stageController.C
  - include/BALL/VIEW/KERNEL/controllers/cameraController.h
  - source/VIEW/KERNEL/controllers/cameraController.C
  - include/BALL/VIEW/KERNEL/controllers/lightController.h
  - source/VIEW/KERNEL/controllers/lightController.C
  - include/BALL/VIEW/KERNEL/controllers/stereoController.h
  - source/VIEW/KERNEL/controllers/stereoController.C
  - include/BALL/VIEW/KERNEL/controllers/clippingController.h
  - source/VIEW/KERNEL/controllers/clippingController.C
  - include/BALL/VIEW/KERNEL/controllers/labelController.h
  - source/VIEW/KERNEL/controllers/labelController.C
  - source/VIEW/DIALOGS/modifyRepresentationDialog.C
  - source/VIEW/KERNEL/MODES/interactionModeManager.C
  - source/VIEW/DIALOGS/stereoSettingsDialog.C
  - source/VIEW/WIDGETS/scene.C
  - include/BALL/VIEW/KERNEL/commandRegistry.h
  - source/VIEW/KERNEL/commandRegistry.C
must_haves:
  truths:
    - "Stage, Camera, Light, Stereo Controllers conform to the §2 bool apply() shape; StageController declares its scene-structural side effects (rep-delete on vertex-buffer-mode toggle + renderer switch + mc->redrawAllRepresentations()) as its §2a invalidation; Camera/Light/Stereo declare soft refresh"
    - "ClippingController and LabelController gain real command-shaped bool apply() bodies (were Log.info stubs); ClippingController::apply() now takes the ControllerApplyGuard it explicitly omitted today"
    - "Every INTERACTIVE non-controller owner-setter site is migrated to a Controller call: modifyRepresentationDialog (14 sites → Material/Coloring/Model), interactionModeManager (2 → Camera/Stereo), stereoSettingsDialog (2 → Stereo), scene.C restoreViewPoint (1 → Camera)"
    - "Dual-surface mutating operations have ONE registered CommandRegistry command body (§8); the two QAction slots become 1-liners invoking the registered command id; count > 1 fails CI"
    - "All 9 999.60 per-controller fixtures are GREEN (the 6 in this plan join the 3 from Plan 02)"
  artifacts:
    - path: "source/VIEW/KERNEL/controllers/clippingController.C"
      provides: "real bool apply() body WITH the ControllerApplyGuard (the §4 omission is closed)"
      contains: "ControllerApplyGuard"
    - path: "source/VIEW/KERNEL/controllers/stageController.C"
      provides: "bool apply() §2 shape with declared scene-structural §2a side effects"
      contains: "invalidateDeclared_"
    - path: "source/VIEW/DIALOGS/modifyRepresentationDialog.C"
      provides: "14 interactive setter sites routed through Material/Coloring/Model Controllers"
  key_links:
    - from: "source/VIEW/DIALOGS/modifyRepresentationDialog.C"
      to: "source/VIEW/KERNEL/controllers/materialController.C"
      via: "controller_->setX(v); controller_->apply()"
      pattern: "Controller.*apply"
    - from: "source/VIEW/KERNEL/commandRegistry.C"
      to: "Controller::apply"
      via: "registered Command::trigger std::function builds payload + calls apply()"
      pattern: "registerCommand|Command"
---

<objective>
Cut the four scene-domain Controllers (Stage, Camera, Light, Stereo) onto
the §2 contract, finish the two stubs (Clipping, Label) into real command
bodies — closing the ClippingController guard omission — migrate every
INTERACTIVE non-controller owner-setter site (§3a interactive disposition)
onto Controller calls, and migrate dual-surface mutating operations to a
single registered CommandRegistry command body (§8). After this plan all
nine 999.60 per-controller fixtures are green.

Purpose: this is the bulk of the harmonization. Stage carries the heaviest
§2a declaration (scene-structural side effects, not soft refresh).
Clipping/Label go from `Log.info` stubs to real apply()s. The interactive
§3a sites (modifyRepresentationDialog / interactionModeManager /
stereoSettingsDialog / scene.C restoreViewPoint) must STOP touching owner
setters and route through Controllers — these are the user-driven
mutations the acceptance grep requires to be GONE. Builder-side sites are
NOT touched here (Plan 04).

Output: 6 harmonized/finished Controllers, migrated interactive call sites,
CommandRegistry dual-surface bodies, all 9 fixtures green. Ends with a
human-verify checkpoint because legacy dialogs + camera keyboard adjust
are interactive surfaces only a human can confirm still drive the scene.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.7.4-ARCHITECTURE-CONTRACT.md
@.planning/phases/999.59-controller-cutover-command-shape/999.59-00-OVERVIEW.md
@.planning/phases/999.59-controller-cutover-command-shape/999.59-02-PLAN.md

# Scene-domain controllers to harmonize + the two stubs to finish:
@source/VIEW/KERNEL/controllers/stageController.C
@source/VIEW/KERNEL/controllers/cameraController.C
@source/VIEW/KERNEL/controllers/clippingController.C
@include/BALL/VIEW/KERNEL/controllers/clippingController.h

# Interactive §3a sites to migrate (read the exact setter call sites):
@source/VIEW/DIALOGS/modifyRepresentationDialog.C
@source/VIEW/KERNEL/MODES/interactionModeManager.C
@source/VIEW/DIALOGS/stereoSettingsDialog.C

# CommandRegistry — today purely QAction-discovery (§8):
@include/BALL/VIEW/KERNEL/commandRegistry.h

# Fixtures that must flip green:
@test/contract/stage_contract_test.C
@test/contract/clipping_contract_test.C

<interfaces>
<!-- §2a invalidation per domain for THIS plan -->
<!-- Stage   → controller-declared scene-structural side effects:
              rep-delete on vertex-buffer-mode toggle + renderer switch,
              then mc->redrawAllRepresentations(). NOT a soft refresh. -->
<!-- Camera  → soft refresh (transform change, same primitives) -->
<!-- Light   → soft refresh -->
<!-- Stereo  → soft refresh -->
<!-- Clipping→ soft refresh (clip-plane change, same primitives) — NEW body -->
<!-- Label   → soft refresh — NEW body -->

<!-- §3a INTERACTIVE sites (must become Controller calls; builder sites are Plan 04) -->
<!-- modifyRepresentationDialog.C  : 14 → Material/Coloring/Model Controllers -->
<!-- interactionModeManager.C       : 2  → Camera/Stereo (keyboard eye/focal adjust) -->
<!-- stereoSettingsDialog.C         : 2  → Stereo -->
<!-- scene.C::restoreViewPoint()    : 1  → Camera (the setCamera call) -->
<!-- scene.C raytracer-material setProperty is BUILDER → leave for Plan 04 -->

<!-- §8 CommandRegistry today (from include/BALL/VIEW/KERNEL/commandRegistry.h ~110-127) -->
<!-- registerFromAction(QAction*, category) synthesizes a Command whose trigger
     is action->trigger(). v1.7.4 adds a registered std::function body for
     dual-surface mutating ops; the long tail keeps registerFromAction. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harmonize Stage / Camera / Light / Stereo onto bool apply()</name>
  <files>include/BALL/VIEW/KERNEL/controllers/stageController.h, source/VIEW/KERNEL/controllers/stageController.C, include/BALL/VIEW/KERNEL/controllers/cameraController.h, source/VIEW/KERNEL/controllers/cameraController.C, include/BALL/VIEW/KERNEL/controllers/lightController.h, source/VIEW/KERNEL/controllers/lightController.C, include/BALL/VIEW/KERNEL/controllers/stereoController.h, source/VIEW/KERNEL/controllers/stereoController.C</files>
  <behavior>
    - All four inherit Controller and override bool apply() in the §2 shape (precondition / re-entry shield via applying_depth_ / capture ApplyPayload / applyInternal_ / one typed event / invalidateDeclared_ / recordIntent_).
    - apply() returns true on mutation, false on dropped re-entry / rejected validation.
    - StageController::invalidateDeclared_() declares the scene-structural side effects: rep-delete on vertex-buffer-mode toggle + renderer switch, then mc->redrawAllRepresentations() (§2a Stage row). It is NOT a soft refresh and NOT a blanket rebuild.
    - Camera / Light / Stereo invalidateDeclared_() each do a soft refresh (the scene re-reads the transform/light/stereo state; no primitive-set change).
    - stage_contract / camera_contract / light_contract / stereo_contract fixtures flip green.
  </behavior>
  <action>
Apply the §13 cookbook to each of the four. Move each existing mutation
body to a private `applyInternal_()`; rewrite `apply()` as the §2 `bool`
wrapper around it (precondition → `if (applying_depth_ > 0) return false;` +
`ControllerApplyGuard guard(applying_depth_);` → capture ApplyPayload with a
unique command_id per op (e.g. `"stage.setBackgroundColor"`,
`"camera.setCamera"`, `"light.setX"`, `"stereo.setEyeDistance"`) →
applyInternal_ → emit the ONE existing typed scene/representation event →
invalidateDeclared_ → recordIntent_ → return true). Header declares
`bool apply() override; void reset() override; void invalidateDeclared_()
override;`.

StageController: override `invalidateDeclared_()` to perform the documented
scene-structural side effects — on a vertex-buffer-mode toggle or renderer
switch, delete/rebuild the affected reps and call
`mc->redrawAllRepresentations()`. Keep this in ONE place with a comment that
this is StageController's declared invalidation, not a generic rule (§2a).
Camera/Light/Stereo: override `invalidateDeclared_()` to a soft refresh
(the existing scene update path that does not rebuild display lists).

StageController currently mutates Stage directly; keep those calls intact
for now (they get funneled through StageMutation in Plan 04 — this plan only
fixes apply()'s SHAPE, not the owner narrowing). Camera/Stereo must bind
through the Renderer/RenderSurface interface per PATTERN.md (do not regress
to direct GLRenderer). Keep public binding setters unchanged.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball/.claude/worktrees/agent-a5fc75ad5e764c9ed && for f in stage camera light stereo; do grep -Eq 'bool .*apply' source/VIEW/KERNEL/controllers/${f}Controller.C || { echo "MISSING bool apply in $f"; exit 1; }; done && grep -q 'redrawAllRepresentations' source/VIEW/KERNEL/controllers/stageController.C && echo OK</automated>
  </verify>
  <done>Stage/Camera/Light/Stereo override bool apply() in §2 shape; Stage declares scene-structural §2a side effects; the four fixtures flip green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finish Clipping + Label stubs into real apply() bodies (Clipping gains the guard)</name>
  <files>include/BALL/VIEW/KERNEL/controllers/clippingController.h, source/VIEW/KERNEL/controllers/clippingController.C, include/BALL/VIEW/KERNEL/controllers/labelController.h, source/VIEW/KERNEL/controllers/labelController.C</files>
  <behavior>
    - ClippingController::apply() is no longer a Log.info stub: it validates, takes the ControllerApplyGuard (the omission documented at clippingController.C ~line 88 is CLOSED), captures a payload, mutates the clip-plane state on the owner, emits one typed event, soft-refreshes via invalidateDeclared_(), returns true.
    - LabelController::apply() is likewise a real command body (validate / guard / capture / mutate / one event / soft refresh / return true).
    - Both inherit Controller; both override bool apply()/reset()/invalidateDeclared_().
    - Re-entry returns false (guard via applying_depth_); precondition failure returns false.
    - clipping_contract / label_contract fixtures flip green (they were DISABLED pending a real bool apply() body).
  </behavior>
  <action>
ClippingController: replace the Log.info stub apply() with a real §2 body.
Header: change `apply()` from a void slot to `bool apply() override;`, drop
`appliedStub()`, add `void reset() override; void invalidateDeclared_()
override;`, inherit from `BALL::VIEW::Controller`. Body: precondition
(scene_ present) → re-entry shield (`if (applying_depth_ > 0) return false;`
+ `ControllerApplyGuard guard(applying_depth_);` — THIS is the guard the
current code explicitly omits; the cut-over adds it) → capture ApplyPayload
(`"clipping.setEnabled"`/`"clipping.setOffset"`) → mutate the clip-plane
state on the owner (enabled/offset/capped) → emit one typed event →
invalidateDeclared_() soft refresh (clip-plane change, same primitives) →
recordIntent_ → return true. Mirror the existing read-only mirror's
enabled/offset/capped fields as the pending state.

LabelController: same shape. Header → inherit Controller, `bool apply()
override`, reset/invalidateDeclared_ overrides, drop the stub signal. Body
mutates the label state and soft-refreshes.

Both: remove the `Log.info` stub bodies and the `appliedStub` signal
plumbing. Keep the public setter binding surface so the (future) Inspector
section path is intact.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball/.claude/worktrees/agent-a5fc75ad5e764c9ed && grep -q 'ControllerApplyGuard' source/VIEW/KERNEL/controllers/clippingController.C && grep -Eq 'bool .*apply' source/VIEW/KERNEL/controllers/clippingController.C && grep -Eq 'bool .*apply' source/VIEW/KERNEL/controllers/labelController.C && ! grep -q 'appliedStub' source/VIEW/KERNEL/controllers/clippingController.C && echo OK</automated>
  </verify>
  <done>Clipping + Label have real bool apply() bodies; ClippingController takes the ControllerApplyGuard (omission closed); both fixtures flip green; stub signals removed.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate interactive §3a sites + CommandRegistry dual-surface bodies</name>
  <files>source/VIEW/DIALOGS/modifyRepresentationDialog.C, source/VIEW/KERNEL/MODES/interactionModeManager.C, source/VIEW/DIALOGS/stereoSettingsDialog.C, source/VIEW/WIDGETS/scene.C, include/BALL/VIEW/KERNEL/commandRegistry.h, source/VIEW/KERNEL/commandRegistry.C</files>
  <action>
Migrate the four INTERACTIVE §3a sites (NOT the builder sites — those are
Plan 04). For each, replace the direct owner setter with a Controller call
in the `controller_->setX(v); controller_->apply();` shape (§5):

- `modifyRepresentationDialog.C` (14 sites): transparency / coloring-method
  / drawing-mode / model-type → route through MaterialController /
  ColoringController / ModelController respectively. The dialog holds (or
  obtains from MainControl) the relevant Controller and delegates; the
  dialog's own Apply button calls the same controller_->apply() an Inspector
  section would (§5 identical-code-path requirement).
- `interactionModeManager.C` (2 sites): keyboard eye/focal-distance adjust
  during camera control → CameraController / StereoController.
- `stereoSettingsDialog.C` (2 sites): focal/eye distance → StereoController.
- `scene.C::restoreViewPoint()` (1 site): the `setCamera` call →
  CameraController. LEAVE scene.C's raytracer-material `setProperty` (that
  one is builder-side, Plan 04).

After migration, NONE of these four files may contain a direct
owner-setter matching the acceptance grep; the interactive sites must be
GONE.

CommandRegistry §8 migration: for every operation that has BOTH a toolbar
QAction AND a menu QAction (the dual-surface mutating set), migrate the
command body from the QAction slot into a registered `Command::trigger`
`std::function<void()>` that builds the Controller payload and calls
apply(). The two QAction slots become 1-liners invoking the registered
command by id. Keep `registerFromAction` for the single-surface long tail.
Add the registration API (e.g. `registerCommand(id, trigger, isEnabled)`)
to commandRegistry.h/.C if not already present, and ensure the §8 CI grep
(a dual-surface mutating op has exactly ONE registered implementation,
count > 1 fails) has exactly one body per op.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball/.claude/worktrees/agent-a5fc75ad5e764c9ed && ! git grep -nE '(stage_|stage->|getStage\(\)->|representation_->|rep_->|rep->|new_rep->)set[A-Z]' -- source/VIEW/DIALOGS/modifyRepresentationDialog.C source/VIEW/KERNEL/MODES/interactionModeManager.C source/VIEW/DIALOGS/stereoSettingsDialog.C | grep -v '^#' && echo "INTERACTIVE_SITES_GONE"</automated>
  </verify>
  <done>All 4 interactive §3a files route through Controllers (no direct owner setters); dual-surface ops have one registered CommandRegistry body; the 2 QAction slots are 1-liners.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All 9 Controllers now conform to the §2 bool apply() contract (Plan 02's
    Model/Coloring/Material + this plan's Stage/Camera/Light/Stereo + the
    finished Clipping/Label stubs). The interactive legacy surfaces
    (modifyRepresentationDialog, the camera-keyboard adjust in
    interactionModeManager, stereoSettingsDialog, scene.C restoreViewPoint)
    now route through Controllers instead of touching owner setters
    directly. All 9 999.60 per-controller fixtures are green.
  </what-built>
  <how-to-verify>
    1. Build BALLView locally (or pull the CI artifact) and open a sample
       structure (e.g. 1ubq.pdb).
    2. Open Tools › Legacy Settings › Modify Representation. Change
       transparency, coloring method, drawing mode, and model type. Each
       change must visibly update the rendered scene (these now go through
       Material/Coloring/Model Controllers).
    3. Enter camera-control mode; use the keyboard eye/focal-distance adjust.
       The view must respond (interactionModeManager → Camera/Stereo).
    4. Open the legacy Stereo Settings dialog; change eye/focal distance —
       the stereo render must update (→ StereoController).
    5. Save a viewpoint, move the camera, then restore the viewpoint
       (scene.C restoreViewPoint → CameraController) — the camera must snap
       back.
    6. Confirm NO console warnings appear in any of the above toggle paths.
  </how-to-verify>
  <resume-signal>Type "approved" or describe which interactive surface failed to drive the scene.</resume-signal>
</task>

</tasks>

<verification>
- VIEW + BALLView + contract_tests build clean on all 3 OSes.
- `ctest -L contract` — all 9 per-controller fixtures green.
- Acceptance grep over the 4 interactive files returns nothing (interactive sites gone).
- ClippingController::apply() takes the ControllerApplyGuard.
- Stage declares scene-structural §2a side effects; Camera/Light/Stereo/Clipping/Label soft-refresh.
- Dual-surface mutating ops: exactly one registered CommandRegistry body each.
</verification>

<success_criteria>
- All 9 Controllers conform to §2 / §2a.
- All 9 999.60 fixtures green.
- Interactive §3a sites migrated and gone; builder sites untouched (Plan 04).
- CommandRegistry §8 dual-surface bodies migrated.
- Human verifies the interactive surfaces still drive the scene.
</success_criteria>

<output>
After completion, create `.planning/phases/999.59-controller-cutover-command-shape/999.59-03-SUMMARY.md`
</output>
