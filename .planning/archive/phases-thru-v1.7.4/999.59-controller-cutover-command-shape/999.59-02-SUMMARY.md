---
phase: 999.59-controller-cutover-command-shape
plan: 02
status: complete
verification: delegated-to-CI
note: executor was interrupted before writing this summary; orchestrator recovered the 3 committed commits and authored this summary.
---

# 999.59-02 — Harmonize Model / Coloring / Material onto `bool apply()`

## What landed (recovered commits)
- `b4484d6572` test: **per-controller contract-fixture activation** — new `test/contract/contractFixtureActivation.h` (130 LOC) replacing the single coarse `BALL_VIEW_CONTROLLER_APPLY_RETURNS_BOOL` switch. Each fixture's parity block is now gated on a per-controller cut-over symbol, so fixtures flip green one domain at a time (restores the Codex MEDIUM #7 incremental-green design). All 9 fixture files + `contractTestHarness.h` re-gated; the 6 not-yet-cut-over fixtures stay disabled so `contract_tests` compiles.
- `008dbf9580` feat: **ModelController** → `bool apply()` in the §2 shape; `invalidateDeclared_()` does `update(true)` (display-list REBUILD).
- `3c8ec6f77f` feat: **ColoringController + MaterialController** → `bool apply()` in the §2 shape; soft-refresh invalidation (`update(false)`).

## Scope
18 files, +743/−141. Controllers (Model/Coloring/Material `.h`+`.C`) + contract harness/fixtures only. No STATE.md / ROADMAP.md / ci.yml touched.

## Verification
The executor was interrupted before its local build-verify step. Verification is delegated to the authoritative cross-platform CI run on the `v1.7.4-inspector-depth` branch (build green + `ctest -L contract -R 'Model|Coloring|Material'` green, with the other 6 fixtures skipped). Recorded in the orchestrator's CI supervision.

## Remaining-domain fixtures
Stage / Camera / Light / Stereo / Clipping / Label remain gated OFF (expected-fail/skip) until 999.59-03 cuts them over.
