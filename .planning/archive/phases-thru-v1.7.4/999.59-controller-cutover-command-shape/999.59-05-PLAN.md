---
phase: 999.59-controller-cutover-command-shape
plan: 05
type: execute
release: v1.7.4
wave: 5
depends_on: [999.59-04]
autonomous: true
requirements:
  - ARCH-CONTRACT-01
files_modified:
  - include/BALL/VIEW/KERNEL/controllers/controller.h
  - source/VIEW/KERNEL/controllers/controller.C
  - include/BALL/VIEW/KERNEL/controllers/controllerJob.h
  - source/VIEW/KERNEL/controllers/controllerJob.C
  - source/VIEW/KERNEL/controllers/CMakeLists.txt
  - source/VIEW/KERNEL/controllers/modelController.C
  - source/VIEW/KERNEL/controllers/coloringController.C
  - source/VIEW/KERNEL/controllers/materialController.C
  - source/VIEW/KERNEL/controllers/stageController.C
  - source/VIEW/KERNEL/controllers/cameraController.C
  - source/VIEW/KERNEL/controllers/lightController.C
  - source/VIEW/KERNEL/controllers/stereoController.C
  - source/VIEW/KERNEL/controllers/clippingController.C
  - source/VIEW/KERNEL/controllers/labelController.C
must_haves:
  truths:
    - "Every one of the 9 apply() bodies calls recordIntent_(payload) with a populated ApplyPayload (before/after snapshots, unique command_id per Controller x operation, target owner, t_us timestamp) — capture-only, no UI consumer in v1.7.4"
    - "recordIntent_ stores the last payload on the Controller (or a base-held slot) and is reachable for v2.0 undo consumption; v1.7.4 does NOT push to any stack and exposes no UI"
    - "A minimal ControllerJob base class exists (a small QObject with started/progress(int)/finished(ApplyPayload) signals) — the §6 async scaffold the §4 thread-safety model references; NO concrete job ships in v1.7.4"
    - "The §6 queued-result plumbing exists: a Controller can kick a ControllerJob (returns false from apply(), nothing mutated synchronously) and the job re-enters the GUI thread via QMetaObject::invokeMethod to a slot that runs the §2 apply() contract under the guard"
    - "command_id values are unique per Controller x operation (asserted by a simple test or grep)"
  artifacts:
    - path: "include/BALL/VIEW/KERNEL/controllers/controllerJob.h"
      provides: "Minimal ControllerJob base: QObject with started/progress(int)/finished(ApplyPayload) signals; no concrete job"
      contains: "class.*ControllerJob"
    - path: "source/VIEW/KERNEL/controllers/controller.C"
      provides: "recordIntent_(const ApplyPayload&) base impl storing the last payload (capture-only)"
      contains: "recordIntent_"
  key_links:
    - from: "source/VIEW/KERNEL/controllers/modelController.C"
      to: "include/BALL/VIEW/KERNEL/controllers/applyPayload.h"
      via: "apply() builds + recordIntent_(payload)"
      pattern: "recordIntent_"
---

<objective>
Wire reversible-intent capture into all 9 apply() bodies and land the
minimal `ControllerJob` async-base scaffold. v1.7.4 captures the
`ApplyPayload` (before/after/command_id/target/t_us) on every mutation and
makes it reachable — but does NOT push to an undo stack and ships NO UI
consumer (v2.0 scope). `ControllerJob` is the §6 base the §4 thread-safety
model references: it justifies apply()'s "GUI-thread-only" claim because
long work goes to a job that re-enters the GUI thread before taking the
guard. No concrete job ships.

Purpose: close ARCHITECTURE-CONTRACT.md §2 step 7 (recordIntent_) and §2d
(reversible payload) for every Controller, and §6 (the async scaffold).
This is deliberately the smaller follow-on after the heavy Plans 03/04: the
apply() bodies already exist and capture a payload in skeleton; this plan
ensures the capture is COMPLETE and uniform across all 9 and adds the job
base + queued-result plumbing.

Output: uniform recordIntent_ capture in all 9 Controllers, the
ControllerJob base + queued-result re-entry plumbing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.7.4-ARCHITECTURE-CONTRACT.md
@.planning/phases/999.59-controller-cutover-command-shape/999.59-00-OVERVIEW.md
@.planning/phases/999.59-controller-cutover-command-shape/999.59-04-PLAN.md
@include/BALL/VIEW/KERNEL/controllers/applyPayload.h
@include/BALL/VIEW/KERNEL/controllers/controller.h

<interfaces>
<!-- ApplyPayload (from Plan 01) -->
struct ApplyPayload {
  String command_id;   // unique per Controller x operation, e.g. "model.setType"
  String before;       // owner snapshot before mutation
  String after;        // owner snapshot after mutation
  void*  target;       // mutated owner (non-owning)
  /* microsecond timestamp */ t_us;
};

<!-- §6 ControllerJob shape (minimal, no concrete job in v1.7.4) -->
class ControllerJob : public QObject {
  // signals: started(); progress(int pct); finished(ApplyPayload);
  // a Controller's apply() that needs async work kicks the job, returns
  // false (nothing mutated synchronously); the job, on its worker thread,
  // QMetaObject::invokeMethod-queues applyResult_(payload) on the GUI
  // thread, which runs the §2 apply() contract under the guard.
};

<!-- §2d acceptance: every apply() calls recordIntent_(payload); before/after
     populated; command_id unique; NO UI consumer (v2.0). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add recordIntent_ on the base + complete capture in all 9 apply() bodies</name>
  <files>include/BALL/VIEW/KERNEL/controllers/controller.h, source/VIEW/KERNEL/controllers/controller.C, source/VIEW/KERNEL/controllers/modelController.C, source/VIEW/KERNEL/controllers/coloringController.C, source/VIEW/KERNEL/controllers/materialController.C, source/VIEW/KERNEL/controllers/stageController.C, source/VIEW/KERNEL/controllers/cameraController.C, source/VIEW/KERNEL/controllers/lightController.C, source/VIEW/KERNEL/controllers/stereoController.C, source/VIEW/KERNEL/controllers/clippingController.C, source/VIEW/KERNEL/controllers/labelController.C</files>
  <behavior>
    - The base Controller declares protected `void recordIntent_(const ApplyPayload&);` and stores the last payload in a member (e.g. ApplyPayload last_intent_) — capture-only, no stack push.
    - Each of the 9 apply() bodies populates an ApplyPayload with: a unique command_id (model.setType / coloring.setMethod / material.setX / stage.setX / camera.setCamera / light.setX / stereo.setEyeDistance / clipping.setEnabled / label.setX), a `before` snapshot captured BEFORE applyInternal_(), an `after` snapshot captured AFTER, the target owner, and a microsecond timestamp; then calls recordIntent_(payload) before returning true.
    - command_id is unique per Controller x operation (no two ops share an id).
    - No UI reads the payload in v1.7.4; recordIntent_ does not push to a stack.
    - The 9 contract fixtures stay green (capture is additive; behavior unchanged).
  </behavior>
  <action>
On the base `Controller` (controller.h/.C): add protected `void
recordIntent_(const ApplyPayload& p);` whose v1.7.4 body stores `p` in a
protected member `ApplyPayload last_intent_;` (capture-only — a comment
notes v2.0 pushes onto UndoStack here). Add a protected accessor
`const ApplyPayload& lastIntent_() const;` for the v2.0/test reader.

In each of the 9 apply() bodies, ensure the §2 capture step is COMPLETE and
uniform: snapshot the owner state into `before` BEFORE the mutation, run
`applyInternal_()`, snapshot `after` AFTER, set a unique `command_id` and
`target` + timestamp, then `recordIntent_(payload)` before `return true`.
Where Plans 02/03 left a skeleton/partial payload, fill it in. Use the
harness OwnerSnapshot string form (or the controller's existing snapshot
helper) for before/after so they are cheap to compare. Keep this additive —
no behavior change beyond capture; the 9 fixtures must stay green.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball/.claude/worktrees/agent-a5fc75ad5e764c9ed && grep -q 'recordIntent_' source/VIEW/KERNEL/controllers/controller.C && n=$(for f in model coloring material stage camera light stereo clipping label; do grep -lq 'recordIntent_' source/VIEW/KERNEL/controllers/${f}Controller.C && echo x; done | wc -l | tr -d ' '); [ "$n" = "9" ] && echo "ALL_9_CAPTURE" || { echo "only $n/9 capture"; exit 1; }</automated>
  </verify>
  <done>recordIntent_ exists on the base; all 9 apply() bodies build + record a populated ApplyPayload with a unique command_id; no UI consumer; fixtures stay green.</done>
</task>

<task type="auto">
  <name>Task 2: Add the minimal ControllerJob base + queued-result plumbing</name>
  <files>include/BALL/VIEW/KERNEL/controllers/controllerJob.h, source/VIEW/KERNEL/controllers/controllerJob.C, source/VIEW/KERNEL/controllers/CMakeLists.txt</files>
  <action>
Create `controllerJob.h/.C` (NEW) per §6: `class BALL_VIEW_EXPORT
ControllerJob : public QObject` in `namespace BALL::VIEW`, `Q_OBJECT`, with
signals `started()`, `progress(int pct)`, `finished(ApplyPayload)`. Provide
a `start()` (or `run()`) entry the Controller calls to kick the job, and the
worker-thread → GUI-thread plumbing: when the job finishes on its worker
thread it `QMetaObject::invokeMethod`-queues an internal
`applyResult_(payload)` slot on the OWNING Controller (queued connection),
and that slot runs the §2 apply() contract — guard / mutate / emit /
invalidate — on the GUI thread. Document the §4 contract: the worker thread
must NOT touch the GL context (macOS UFG-26 class); only
`Renderer::query…()` on the GUI thread is legal. NO concrete job ships in
v1.7.4 — this is the base + the queued re-entry plumbing only. Add an
optional protected hook on the base Controller (e.g. `bool kickJob_(...)`)
that returns false from apply() (nothing mutated synchronously) when a
Controller defers to a job, matching §6.

Wire `controllerJob.C` into the libVIEW build the same way controller.C was
wired in Plan 01 (source list + AUTOMOC for the Q_OBJECT header). Build VIEW
clean.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/BALL/ball/.claude/worktrees/agent-a5fc75ad5e764c9ed && grep -Eq 'class.*ControllerJob' include/BALL/VIEW/KERNEL/controllers/controllerJob.h && grep -q 'finished' include/BALL/VIEW/KERNEL/controllers/controllerJob.h && grep -q 'invokeMethod' source/VIEW/KERNEL/controllers/controllerJob.C && grep -rq 'controllerJob\.C' source/VIEW/KERNEL/controllers/CMakeLists.txt && echo OK</automated>
  </verify>
  <done>ControllerJob base exists with started/progress/finished signals + the QMetaObject::invokeMethod queued-result plumbing; no concrete job; wired into libVIEW; VIEW builds clean.</done>
</task>

</tasks>

<verification>
- VIEW + contract_tests build clean on all 3 OSes.
- All 9 apply() bodies call recordIntent_ with a populated ApplyPayload; command_ids unique.
- No UI consumer of the payload (intentional, v2.0).
- ControllerJob base + queued re-entry plumbing present; no concrete job.
- The 9 per-controller fixtures stay green; compile-fail negatives stay green.
</verification>

<success_criteria>
- §2 step 7 (recordIntent_) + §2d (reversible payload) complete and uniform across all 9 Controllers.
- §6 ControllerJob base scaffold lands, closing the §4 thread-safety story.
- Capture is additive — zero behavior change; all fixtures stay green.
</success_criteria>

<output>
After completion, create `.planning/phases/999.59-controller-cutover-command-shape/999.59-05-SUMMARY.md`
</output>
