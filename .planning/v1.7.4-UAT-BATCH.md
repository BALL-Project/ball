# v1.7.4 — Consolidated UAT Batch

All human-verify checkpoints deferred during the v1.7.4 build, collected here.
Exercise against the fresh local build (see "Build" below). Mark each ✅/❌ and
note anything that behaves unexpectedly.

**Build under test:** `v1.7.4-inspector-depth` @ `f96e9aac62` (all phases merged;
final CI green-pending). Launch via the Desktop **"Launch BALLView (v1.7.4 UAT)"**
`.command`, or `build/ci-macos-verify/bin/BALLView.app`.

---

## A. Controller cut-over — interactive surfaces still drive the scene (999.59-03/04)
The owner setters are now private behind controllers; confirm the legacy
interactive paths still mutate the scene through the new command path.

- [ ] **Stereo settings** dialog → change eye / focal distance → scene updates.
- [ ] **Camera keyboard adjust** (eye/focal) → scene updates.
- [ ] **Restore viewpoint** → camera position + look-at restore correctly.
- [ ] **modifyRepresentation** dialog → model/coloring/material edits still apply.
- [ ] Each **Inspector section** (Model / Coloring / Material / Clipping / Label /
      Stage / Light / Stereo) → its control mutates the scene as before.

## B. Coloring · value-range histogram (999.63) — the headline feature
- [ ] Select a value-based coloring (B-factor / Occupancy / Charge / Distance).
- [ ] The **histogram** of the selection's value distribution renders.
- [ ] **Drag** each handle → live preview clamps the colormap WHILE dragging.
- [ ] **Release** a handle → the change commits (colors update on release).
- [ ] Dragging then releasing OUTSIDE produces no half-applied state.
- [ ] The 4 presets each apply in one click: **Auto-fit / Full range /
      Robust 5–95% / Reset**.
- [ ] Performance feels instant on a large (≈50k-atom) selection.

## C. Per-section "Reset section" (999.64)
- [ ] Hover an Inspector section header → a **Reset glyph** appears.
- [ ] Click it → that section reverts to its method-defined defaults in one step
      (no flicker / double-apply).
- [ ] Reset on **Background** or **Stage** restores the full stage defaults
      (these share one controller — expected; the prompt says so).

## D. Bottom log drawer — no flicker (999.62 close-out / #29)
- [ ] Expand / collapse the bottom **Logs** drawer → it slides with NO flicker,
      smear, or parent showing through during the animation.

## E. Deferred / moot
- **PyBALL DeprecationWarning (999.59-06):** MOOT in v1.7.4 — SIP is not compiled
  in this build. The binding surface is lint-checked; runtime exercise rides v2.1.
- **Transparent-surface render artifacts (#18):** pre-existing fixed-function
  limitation, deferred to v2 / PIPE-01 — NOT in scope for this UAT.

---

### Resume signal
Reply per item, or "all approved" if the batch passes. Anything ❌ I treat as a
blocker and fix before tagging v1.7.4.
