# v1.7-USER-FEEDBACK-GATE.md

**Status:** OPEN — gates v1.7.0 final tag.
**Captured:** 2026-05-18 (during v1.7.0-rc1 packaging-iteration cycle).
**Owner:** orchestrator + user.

---

## Why this gate exists

v1.7 is the BALLView Refresh: a comprehensive UI rewrite (9 Wave 4 phases,
~76 source files touched by the BALL_UI_V2 flag-removal pass, 4 shared
widgets + Unified Inspector + Workspace + CommandPalette + WelcomeScreen
+ Classic-dock retirement). Per-phase CI matrices validated each phase
in isolation; the pre-RC adversarial review caught 8 CRITICAL security
findings + 8 IMPORTANT correctness ones (all fixed pre-tag).

But **no end-to-end user validation has happened yet.** Automated CI is
blind to:
- Icon completeness (does every toolbar/menu/dialog action have a glyph?)
- Visual rendering correctness on real displays (macOS Retina, mixed-DPR,
  HiDPI scaling — `tabOrder_test` runs offscreen)
- Cross-Inspector live-commit reactivity (does changing a Representation
  setting actually update the rendered scene?)

This gate is the explicit "ship-stops-until-human-validates" checkpoint
between **v1.7.0-rc1** (packaging green, artifacts produced) and
**v1.7.0** (final tag, master fast-forward, public release).

## Trigger conditions

The gate **opens** when:
- ✅ v1.7.0-rc1 tag exists
- ✅ release.yml produced all 4 platform artifacts (DMG + Linux × 3 +
  Windows NSIS + ZIP)
- 🟡 Artifacts are downloadable from the GitHub Actions run

The gate **closes** (and v1.7.0 final tag is unblocked) when:
- The 3 captured issues below are either:
  - (a) Fixed, with fix commits + a new RC re-roll showing the issue
    resolved, OR
  - (b) Explicitly accepted as v1.7.0-known-issues and deferred to
    v1.7.x patch cycle, with the user's written acceptance
- Any additional issues surfaced during user validation are similarly
  resolved or accepted
- User issues a "ship v1.7.0" signal to the orchestrator

## Issues captured at gate-open (2026-05-18)

User-reported during initial inspection of the v1.7.0-rc1 build:

### UFG-01 — Toolbar icons incomplete
**Severity:** ⚠ IMPORTANT (visible polish gap; not a functional regression)
**Status:** ✅ FIXED for rc2 — commit `c4bbc511f2` (`fix(ufg-01/icons)`).
**Symptom:** Some toolbar actions lack glyphs after the Phase 999.42 XPM → SVG migration.
**Confirmed root cause:** the dialogs that contribute toolbar entries (`MolecularFileDialog`, `DownloadPDBFile`, `DownloadElectronDensity`, `PubChemDialog`, `DisplayProperties`, `MolecularStructure`) called `ModularWidget::setIcon(action, "<key>", /*toMainToolbar*/true)` with 10 keys that had never been registered as aliases in `theme.qrc`. The `Icons::get` façade returned a null QIcon and the toolbar painted an empty cell. The Phase 999.42 migration map enumerated the keys it knew about but missed the keys originating from these dialog classes (the legacy XPM array names didn't match the new `actions/*` naming convention).
**Fix landed:** vendored 10 additional Lucide v0.460.0 SVGs + registered alias mappings in `theme.qrc`:
- `actions/document-open` → folder-open.svg (alias of existing)
- `actions/download-pdb` → download.svg
- `actions/download-electrondensity` → cloud-download.svg
- `actions/download-pubchem` → flask-conical.svg
- `categories/preferences-display` → monitor.svg
- `actions/object-focus` → focus.svg
- `actions/mode-edit` → pencil.svg
- `actions/molecule-set-element` → tag.svg
- `actions/create-bond` → link.svg
- `actions/continuous-loop` → repeat.svg
- `actions/screenshot` → camera.svg

`source/APPLICATIONS/BALLVIEW/iconBrowser.C` kept in sync. Outstanding: `mimetype/text-x-povray` (POV export — menu-only, `add_to_main_toolbar=false`, not toolbar-visible). Documented as a TODO comment in `theme.qrc`.

### UFG-02 — Rendering issues in macOS interface
**Severity:** 🚨 CRITICAL (potential RC1 blocker)
**Status:** ✅ FIXED for rc2 — commits `8b1ff967cb`, `5cb1a2d353`, `ca8aeab32f`.
**Reported:** v1.7.0-rc1 macOS smoke triage.
**Symptom:** Rendering glitches in BALLView macOS UI: dialog buttons missing native Aqua chrome (no focus ring / pressed state), local menubar widget consuming layout space despite the global Cocoa menu being active, toolbar rendered as a separate strip with drag handles instead of the native unified-title-and-toolbar look.
**Confirmed root causes:**
1. Bare-selector QSS rules in `theme-neutral.qss` (`QPushButton`, `QToolButton`, `QMenuBar`) override QMacStyle entirely on macOS for every top-level window — dialogs lose their native Aqua chrome.
2. `setUnifiedTitleAndToolBarOnMac(true)` was never called — Handover §09-cross-platform.md row 30 requires it for the native NSToolbar look.
3. Comment drift in `main.C` referenced removed `BALL_UI_V2` build flag (999.48 §8.7).
4. `Qt::AA_DontCreateNativeWidgetSiblings` (set for the Qt WebEngine-in-DockWidget fix) has an undocumented Qt 6 / Apple Silicon caveat: it can cause GL drawable flicker on QOpenGLWidget reparenting.
**Fixes landed:**
- `fix(ufg-02/qss-scoping)` — scope `QPushButton`/`QToolButton` to `QMainWindow ...` descendants and `QMenuBar` to `QMainWindow > QMenuBar`. Dialogs keep QMacStyle, main window keeps the theme. (`8b1ff967cb`)
- `fix(ufg-02/unified-toolbar+native-siblings)` — add `setUnifiedTitleAndToolBarOnMac(true)` in `Mainframe::show()` under `Q_OS_MACOS`. Also documents the macOS Qt 6 GL-flicker caveat on `AA_DontCreateNativeWidgetSiblings` (WebEngine still requires it via VIEW/WIDGETS/HTMLPage + HTMLView + EXTENSIONS plugins). (`5cb1a2d353`)
- `fix(ufg-02/stale-comment)` — drop `BALL_UI_V2` reference from ThemeManager init comment. (`ca8aeab32f`)
**Verified:** macOS-arm64 smoke build green; bare-selector grep returns only `QMainWindow`, `QMenu`, `QStatusBar`, `QGroupBox`, `QLineEdit`, `QComboBox` (all intentional — see commit message rationale). UFG-05 fix at `sectionHeader.C:52` still in place + not regressed.
**Deferred to v1.7.x:** Optional `theme-neutral-macos.qss` overlay file loaded under `Q_OS_MACOS` for platform-specific rules that DO want to style certain widgets — overkill for rc2.

### UFG-04 — Inspector tab labels truncated

**Severity:** ⚠ IMPORTANT (visible polish gap; affects UX of the new
Unified Inspector — the headline v1.7 feature)
**Status:** ✅ FIXED for rc2 — commit `6bf15f0bd0` (`fix(ufg-04/inspector-tabs)`).
**Reported:** 2026-05-18, from the same v1.7.0-rc1 screenshot triage.
**Symptom:** The InspectorTabs strip at the top of the right-rail
Inspector dock rendered as `Sele... / Represent... / Sc...` instead of
the full `Selection / Representation / Scene` labels.
**Confirmed root cause:** `InspectorTabs` constructor called
`setExpanding(true)` + `setUsesScrollButtons(false)`. The QDockWidget's
default minimum width is ~180 px (no explicit `setMinimumWidth`); under
that combination Qt's `QTabBar` distributes the available width equally
across the 3 tabs and elides each label individually when its share is
narrower than the label's natural width. Not macOS-specific — the same
elide path fires on every platform when the dock is too narrow.
**Fix landed:** two-part:
- `inspectorTabs.C` — `setExpanding(false)` + `setElideMode(Qt::ElideNone)`
  + `setUsesScrollButtons(true)` as fallback for user-narrowed dock
- `inspectorDock.C` — `setMinimumWidth(280)` to fit the full tab strip at
  the default font on macOS / Ubuntu / Windows

### UFG-05 — Inspector REPRESENTATION header garbled overlap

**Severity:** 🚨 IMPORTANT (visible polish defect; potential blocker depending
on user severity perception)
**Status:** ✅ FIXED for rc2 — commit `1b4a8dd299` (`fix(ufg-05/header-overlap)`).
**Reported:** 2026-05-18, from the same v1.7.0-rc1 screenshot triage.
**Symptom:** The Inspector's `REPRESENTATION` section header showed
overlapping text bleeding through it from the section's content area
("OPENG/SACCOUTH" fragments visible in the bug screenshot — clearly
fragments of "OpenGL" + "Saccharide", both strings produced by
`Representation::getProperties()` and rendered into the
`RepHeaderSection` QLabel below the header).
**Confirmed root cause:** `SectionHeader` was a plain `QWidget` with no
background set. Qt's default for a QWidget is transparent paint — the
chevron, the QLabel, and the QFrame::HLine each paint themselves, but
the gaps between them pass through to whatever the parent painted. The
Inspector's expand/collapse uses a `QPropertyAnimation` on the content
widget's `maximumHeight`; as sibling sections shift through the header's
parent region during the animation, fragments of `QComboBox` options +
`QLabel` text from `RepHeaderSection` get left behind in the un-cleared
header rect.
**Fix landed (safe-bet per planning guidance):**
- `SectionHeader` constructor sets `Qt::WA_OpaquePaintEvent` +
  `setAutoFillBackground(true)`
- New `paintEvent` override calls `painter.fillRect(event->rect(),
  palette().color(QPalette::Window))` before the layout's child widgets
  paint themselves on top

### UFG-07 — Selection UX redesign: highlight, not checkbox

**Severity:** ⚠ DESIGN-CHANGE (user-requested UX modernization, not a bug)
**Reported:** 2026-05-18, after initial screenshot triage.
**Symptom (current):** Left-hand `ProjectDock › Structures` tree (MolecularControl) shows `Name [highlight]` + `Type` columns, with a third "Checked" column rendering the selection checkbox. Selection state is conveyed by checkbox tick.
**Requested change:**
- Drop the `Checked` selection-checkbox column entirely
- Drop the `Type` column entirely
- Convey selection state via name-text **color highlight** instead (selected = highlight color; unselected = normal)
- Tree becomes single-column: just the Name
**Likely investigation surface:**
- `source/VIEW/WIDGETS/molecularControl.{h,C}` — tree model + columns + selection
- `source/VIEW/WIDGETS/geometricControl.{h,C}` — sister tree (Representations); may need analogous redesign for consistency
- `source/VIEW/WIDGETS/datasetControl.{h,C}` — third sister tree (Datasets); same consideration
- The QStandardItemModel / QAbstractItemModel column count drops from 3→1
- The QStyledItemDelegate paints selection via a custom foreground role
- Existing checkbox-based selection signals re-routed to a click-to-toggle on the row
**Triage:** medium scope (~150-300 LOC). Touches the selection-bus that the new Inspector subscribes to via Phase 999.44's `SelectionInspectorAdapter` — verify the adapter survives the column-count change. Likely no Controller-extraction interaction. Cross-platform safe (no macOS-specific bits).
**Disposition:** queue for RC2 fix wave per "keep everything in v1.7" directive.

### UFG-06 — Bottom Logs drawer doesn't expand

**Severity:** 🚨 CRITICAL (functional regression — visible chrome that does nothing on click)
**Reported:** 2026-05-18, follow-up to initial screenshot triage.
**Status:** ✅ FIXED for rc2 — commit `a6ba1d7850` (`fix(ufg-06/bottomdrawer)`).
**Symptom:** Clicking the collapsed `Logs ▾` strip at the bottom of the window did not expand the drawer to its 240px state. Drawer stayed at the 24px collapsed strip.
**Confirmed root cause:** `BottomDrawer::setExpanded(true)` called `setMaximumHeight(kExpandedHeight)` BEFORE `animateTo()`, and `animateTo()` read `maximumHeight()` as the animation's start value — so the `QPropertyAnimation` ran 240→240 (zero delta) and Qt's `QMainWindow` dock-area layout never received a relayout cue. The chevron `clicked` → `toggle()` → `setExpanded(true)` signal chain was wired correctly; `expanded_` flipped to `true` and `body_->setVisible(true)` was called, but the bottom dock row stayed pinned at 24 px so the body had zero visible pixels.
**Fix landed:**
- `animateTo()` now drives `maximumHeight` from the current rendered `height()` (not the `maximumHeight` cap) so the animation start always matches what the user sees.
- `setExpanded()` no longer pre-sets `maximumHeight` to the target before animating; `animateTo()` lifts the cap to `max(currentHeight, targetHeight)` so the animation has room to grow into without short-circuiting itself.
- `animateTo()` explicitly calls `QMainWindow::resizeDocks({this}, {targetHeight}, Qt::Vertical)` — animating `maximumHeight` alone is not enough for `QDockWidget` because `QMainWindow` caches the previous row geometry and will not re-grow it without an explicit hint.
**Files touched:** `source/VIEW/WIDGETS/bottomDrawer.C` (+40/-5).
**Smoke-build:** macOS-arm64 `libVIEW.dylib` + `BALLView` relink clean, no new warnings on `bottomDrawer.C`.

### UFG-03 — Representations + colorings no longer update
**Severity:** 🚨 CRITICAL (functional regression — was RC1 blocker)
**Status:** ✅ FIXED for rc2 (Representation Controllers — Model + Coloring + Material) — commits `7a21fa4172`, `222bc9c5dc`, `c153bd6597`. Scene-tab Controllers audit + plan documented in `9a0c8e0000`-class follow-up (see audit table below).
**Symptom:** Changing Representation settings (model, coloring, material) in the new Unified Inspector didn't propagate to the rendered scene.
**Confirmed root cause:** Phase 999.44 Plan 04 landed the Representation Controllers as **read-only mirrors** — `apply()` only logged the mirrored state + `Q_EMIT appliedStub()`, never wrote to `rep_`. Plan 06 cut over `StageController` as a proof-of-concept but the remaining 7 Controllers stayed as stubs. Inspector users saw the QSliders/QComboBoxes change visually but the Scene rendered the pre-change state.
**Fixes landed for rc2:**
- `fix(ufg-03/model)` — `ModelController::apply()` now mutates the attached Representation via `DisplayProperties::getModelSettingsDialog()->createModelProcessor(newType)` (reusing the legacy processor factory so per-model parameters like stick radius / surface probe radius stay consistent) + Representation setters + `rep_->update(rebuild=true on type change)`. Added `DisplayProperties::getModelSettingsDialog()` public accessor. (`7a21fa4172`)
- `fix(ufg-03/coloring)` — `ColoringController::apply()` mutates via `DisplayProperties::getColoringSettingsDialog()->createColorProcessor(newMethod)` + `rep_->update(false)` for color-only repaint. Added `DisplayProperties::getColoringSettingsDialog()` public accessor. (`222bc9c5dc`)
- `fix(ufg-03/material)` — `MaterialController::apply()` now calls `Scene::updateMaterialForRepresentation(rep_, material)` — the same per-rep material backend the legacy `MaterialSettings` dialog uses. `revert()` reads the per-rep `Rendering::Material` property when present (falling back to the Stage's default material) so the inspector mirrors the actual rendered state. Documents the inspector's "Diffuse" → legacy "reflective_intensity" field rename. (`c153bd6597`)
- All three Controllers: `MainControl::isBusy()` guard before any mutation, matching the legacy "Apply disabled while busy" pattern.

**Single-rep scope is intentional and correct** — `InspectorView::setActiveRepresentation` (`inspectorView.C:318`) attaches all controllers to ONE rep (the user-selected one in the `RepresentationHeader` picker). Matches legacy modal semantics; documented in commit messages.

**Verified:** macOS-arm64 smoke build green; VIEW + BALLView link clean. The renderer's update path goes through `rep_->update(rebuild)` → `RepresentationManager::update_` → renderer batches → Scene repaint, the same path the legacy `DisplayProperties::applyTo_` walks.

---

#### Controller cut-over audit (other 5 stubs)

Survey of the remaining Inspector Controllers per the UFG-03 fix-batch
audit. Searched `source/VIEW/KERNEL/controllers/*Controller.C` for the
`STUB` log marker. Findings:

| Controller       | Status at HEAD | Apply impl |
| ---------------- | -------------- | ---------- |
| `StageController` | ✅ Real        | Plan 06 cut-over — writes to `Stage` (background, fog, coord system, eye/focal distance). |
| `ModelController` | ✅ Real (rc2)  | UFG-03 fix — writes to `Representation` via processor factory. |
| `ColoringController` | ✅ Real (rc2) | UFG-03 fix — writes to `Representation` via color processor factory. |
| `MaterialController` | ✅ Real (rc2) | UFG-03 fix — writes to per-rep material via `Scene::updateMaterialForRepresentation`. |
| `CameraController` | 🔴 STUB        | `cameraController.C:52` — logs `viewPoint=…lookAt=…` only. Legacy: `SetCamera` modal. |
| `LightController`  | 🔴 STUB        | `lightController.C:48` — logs `lightCount=…ambient=…` only. Legacy: `LightSettings`. |
| `StereoController` | 🔴 STUB        | `stereoController.C:48` — logs `enabled…eye…focal…swapSBS` only. Legacy: `StereoSettingsDialog`. |
| `ClippingController` | 🔴 STUB      | `clippingController.C:34` — logs `enabled…offset…capped` only. Legacy: `ClippingDialog`. |
| `LabelController` | 🔴 STUB        | `labelController.C:33` — logs `text…type…fontSize` only. Legacy: `LabelDialog`. |

**Triage of the 5 remaining stubs:**

- **Camera / Light / Stereo / Background:** Scene-tab Controllers. The Inspector Scene tab's `StageSection`/`CameraSection`/`LightsSection`/`StereoSection`/`BackgroundSection` are wired but only `Stage*`/`Background*` actually propagate (both go through `StageController`). Camera/Lights/Stereo sections render but don't apply. **Deferred to v1.7.x** — the user-visible regression bar in UFG-03 was Representation controls (the headline "I changed model and nothing happened" path); Scene-tab controls were never in scope for v1.7.0 RC.
- **Clipping / Label:** Per-rep Controllers, but the Inspector does **not** yet add a `ClippingSection` or `LabelSection` (only `RepHeader`, `Model`, `Coloring`, `Material` are in `inspectorView.C:307-310`). These Controllers are reachable through API but have no UI consumer in v1.7.0 — pure latent stubs. **Deferred to v1.7.x** as part of the "Inspector feature parity with legacy dialogs" tracking work.

**Recommendation for the v1.7.x patch queue:** group the 5 remaining Controller cut-overs into a single Plan, since each follows the StageController/UFG-03 pattern exactly. Estimated ~600-800 LOC across the 5 files. Not blocking v1.7.0 final.

## Out-of-scope CI observation — VersionInfo_test (Linux only)

Surfaced during the UFG-01/04/05 rc2 fix wave (2026-05-18, run
26020897221, sha `e3ec798774`):

- `build (macos-arm64)` ✅ — all 295/295 tests pass.
- `build (linux-x64)` ❌ — 294/295 tests pass; `VersionInfo_test`
  fails at lines 24 + 41 (`hasPrefix(BALL_RELEASE_STRING)` returns
  false despite `BALL_RELEASE_STRING="1.7.0"`).
- `build (linux-arm64)` ❌ — same single-test failure pattern.
- `build (windows-x64)` — in-progress at observation time.

This failure **predates the UFG-01/04/05 commits**. It was already
present in the rc1 tag CI run (`6ca8ee7c21`) and every Linux CI
since the PROJECT VERSION bump 1.6.0 → 1.7.0 in `ce8904cadf`.
Root-cause hypothesis: ccache hit a stale `version.C.o` from the
1.6.0 build because `config.h` is generated from `cmake/config.h.in`
and the `CCACHE_SLOPPINESS=pch_defines,time_macros,include_file_mtime`
in ci.yml masks the include-mtime change to config.h — so the
compiled `version.C.o` continues to return the 1.6.0-era string
(or an empty string) at runtime, while the test framework links
the test binary fresh and reads `BALL_RELEASE_STRING="1.7.0"` from
its own compile.

The UFG-01/04/05 commits do not touch version.C, config.h, or
ccache settings. The failure is **out of scope per the GSD
scope-boundary rule** (only fix issues directly caused by the
current task's changes).

Recommended fix vector for the orchestrator: bust the ccache key
for `version.C.o` by either (a) adding `BALL_RELEASE_STRING` to
the ccache hash via `CCACHE_EXTRAFILES=include/BALL/CONFIG/config.h`
or (b) dropping `include_file_mtime` from the sloppy-set on the
Linux CI lanes. Either is a one-line ci.yml edit. Not blocking
rc2 RC roll on its own — the orchestrator already shipped rc1
with this failure present.

## Gate-closure workflow

1. **User downloads** v1.7.0-rc1 artifacts:
   - macOS: `BALLView-v1.7.0-rc1-macos-arm64.dmg`
   - Linux x64: `BALLView-v1.7.0-rc1-linux-x64.tar.gz` (or AppImage / DEB / RPM)
   - Linux arm64: `BALLView-v1.7.0-rc1-linux-arm64.tar.gz` (or AppImage / DEB / RPM)
   - Windows: `BALLView-v1.7.0-rc1-windows-x64.zip` (or NSIS installer, when ready)
2. User exercises the UI across the 4 platforms (or as many as they can):
   - Load a sample molecule (`File › Open from PDB` → `1ubq` is the
     curated default per Phase 999.47)
   - Walk through every menu item, every toolbar button
   - Open the Unified Inspector, exercise all 3 tabs and visible sections
   - Switch workspaces (Default ↔ Focused)
   - Open the CommandPalette (Cmd/Ctrl+K), try `?` / `>` / `:` modes
   - Apply theme tints (state tints visible on hover/select)
3. User triages findings into:
   - **Blocker for v1.7.0:** fix-first list (added here as UFG-NN, fixes
     re-roll RC)
   - **Accept for v1.7.0:** added to RELEASE-NOTES "Known issues" + tracked
     for v1.7.x patch cycle
4. Orchestrator dispatches fix agents for the blocker list; re-rolls
   RC for each fix wave
5. Final user "ship it" signal → orchestrator promotes v1.7.0-rc1 →
   v1.7.0 tag → release.yml → master fast-forward

## What to NOT do during this gate

- Don't auto-tag v1.7.0 even if all CI is green — wait for explicit user
  ship signal
- Don't auto-promote any RC roll without user re-validation of the issues
  they captured
- Don't merge to master — RC tags are mutable; final tag triggers the
  master fast-forward per RELEASE-PROCESS.md

## Where v1.7.x patch-cycle items go

Items the user **accepts** as v1.7.0 known-issues:
- Documented in `RELEASE-NOTES-v1.7.md` under "Known issues" section
- Tracked in `.planning/v1.7.x-PATCH-QUEUE.md` (or appended to the
  existing 14-item deferral list documented in phase SUMMARYs)
- Will not block v1.7.0 final tag

Items the user **rejects** as needing fix-before-RC:
- Captured here as UFG-NN with severity + investigation surface
- Fix dispatched via orchestrator → fix commit → new RC roll
- Cycle repeats until user signs off

## See also

- `.planning/RELEASE-PROCESS.md` — full release-cycle conventions
- `.planning/RELEASE-NOTES-v1.7.md` — user-facing notes (this gate's
  output feeds the "Known issues" section)
- `.planning/phases/999.44-unified-inspector/999.44-01-SUMMARY.md` —
  Controller cut-over deferral table (relevant to UFG-03)
- `.planning/phases/RC-PATCH-v1.7/SUMMARY.md` — the 14-item v1.7.x
  patch-cycle deferral list (UFG items may compound this)
