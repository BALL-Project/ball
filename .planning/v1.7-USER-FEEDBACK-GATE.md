# v1.7-USER-FEEDBACK-GATE.md

**Status:** OPEN — gates v1.7.0 final tag.
**Captured:** 2026-05-18 (during v1.7.0-rc1 packaging-iteration cycle).
**Owner:** orchestrator + user.

---

## Why this gate exists

v1.7 is the BALLView Refresh: a comprehensive UI rewrite (9 Wave 4 phases,
~76 source files touched by the BALL_UI_V2 flag-removal pass, 4 shared
widgets + Unified Inspector + Workspace + CommandPalette + WelcomeScreen
+ Classic-dock retirement). Per-phase CI matrices validated each phase
in isolation; the pre-RC adversarial review caught 8 CRITICAL security
findings + 8 IMPORTANT correctness ones (all fixed pre-tag).

But **no end-to-end user validation has happened yet.** Automated CI is
blind to:
- Icon completeness (does every toolbar/menu/dialog action have a glyph?)
- Visual rendering correctness on real displays (macOS Retina, mixed-DPR,
  HiDPI scaling — `tabOrder_test` runs offscreen)
- Cross-Inspector live-commit reactivity (does changing a Representation
  setting actually update the rendered scene?)

This gate is the explicit "ship-stops-until-human-validates" checkpoint
between **v1.7.0-rc1** (packaging green, artifacts produced) and
**v1.7.0** (final tag, master fast-forward, public release).

## Trigger conditions

The gate **opens** when:
- ✅ v1.7.0-rc1 tag exists
- ✅ release.yml produced all 4 platform artifacts (DMG + Linux × 3 +
  Windows NSIS + ZIP)
- 🟡 Artifacts are downloadable from the GitHub Actions run

The gate **closes** (and v1.7.0 final tag is unblocked) when:
- The 3 captured issues below are either:
  - (a) Fixed, with fix commits + a new RC re-roll showing the issue
    resolved, OR
  - (b) Explicitly accepted as v1.7.0-known-issues and deferred to
    v1.7.x patch cycle, with the user's written acceptance
- Any additional issues surfaced during user validation are similarly
  resolved or accepted
- User issues a "ship v1.7.0" signal to the orchestrator

## Issues captured at gate-open (2026-05-18)

User-reported during initial inspection of the v1.7.0-rc1 build:

### UFG-01 — Toolbar icons incomplete
**Severity:** ⚠ IMPORTANT (visible polish gap; not a functional regression)
**Status:** ✅ FIXED for rc2 — commit `c4bbc511f2` (`fix(ufg-01/icons)`).
**Symptom:** Some toolbar actions lack glyphs after the Phase 999.42 XPM → SVG migration.
**Confirmed root cause:** the dialogs that contribute toolbar entries (`MolecularFileDialog`, `DownloadPDBFile`, `DownloadElectronDensity`, `PubChemDialog`, `DisplayProperties`, `MolecularStructure`) called `ModularWidget::setIcon(action, "<key>", /*toMainToolbar*/true)` with 10 keys that had never been registered as aliases in `theme.qrc`. The `Icons::get` façade returned a null QIcon and the toolbar painted an empty cell. The Phase 999.42 migration map enumerated the keys it knew about but missed the keys originating from these dialog classes (the legacy XPM array names didn't match the new `actions/*` naming convention).
**Fix landed:** vendored 10 additional Lucide v0.460.0 SVGs + registered alias mappings in `theme.qrc`:
- `actions/document-open` → folder-open.svg (alias of existing)
- `actions/download-pdb` → download.svg
- `actions/download-electrondensity` → cloud-download.svg
- `actions/download-pubchem` → flask-conical.svg
- `categories/preferences-display` → monitor.svg
- `actions/object-focus` → focus.svg
- `actions/mode-edit` → pencil.svg
- `actions/molecule-set-element` → tag.svg
- `actions/create-bond` → link.svg
- `actions/continuous-loop` → repeat.svg
- `actions/screenshot` → camera.svg

`source/APPLICATIONS/BALLVIEW/iconBrowser.C` kept in sync. Outstanding: `mimetype/text-x-povray` (POV export — menu-only, `add_to_main_toolbar=false`, not toolbar-visible). Documented as a TODO comment in `theme.qrc`.

### UFG-02 — Rendering issues in macOS interface
**Severity:** 🚨 CRITICAL (potential RC1 blocker)
**Status:** ✅ FIXED for rc2 — commits `8b1ff967cb`, `5cb1a2d353`, `ca8aeab32f`.
**Reported:** v1.7.0-rc1 macOS smoke triage.
**Symptom:** Rendering glitches in BALLView macOS UI: dialog buttons missing native Aqua chrome (no focus ring / pressed state), local menubar widget consuming layout space despite the global Cocoa menu being active, toolbar rendered as a separate strip with drag handles instead of the native unified-title-and-toolbar look.
**Confirmed root causes:**
1. Bare-selector QSS rules in `theme-neutral.qss` (`QPushButton`, `QToolButton`, `QMenuBar`) override QMacStyle entirely on macOS for every top-level window — dialogs lose their native Aqua chrome.
2. `setUnifiedTitleAndToolBarOnMac(true)` was never called — Handover §09-cross-platform.md row 30 requires it for the native NSToolbar look.
3. Comment drift in `main.C` referenced removed `BALL_UI_V2` build flag (999.48 §8.7).
4. `Qt::AA_DontCreateNativeWidgetSiblings` (set for the Qt WebEngine-in-DockWidget fix) has an undocumented Qt 6 / Apple Silicon caveat: it can cause GL drawable flicker on QOpenGLWidget reparenting.
**Fixes landed:**
- `fix(ufg-02/qss-scoping)` — scope `QPushButton`/`QToolButton` to `QMainWindow ...` descendants and `QMenuBar` to `QMainWindow > QMenuBar`. Dialogs keep QMacStyle, main window keeps the theme. (`8b1ff967cb`)
- `fix(ufg-02/unified-toolbar+native-siblings)` — add `setUnifiedTitleAndToolBarOnMac(true)` in `Mainframe::show()` under `Q_OS_MACOS`. Also documents the macOS Qt 6 GL-flicker caveat on `AA_DontCreateNativeWidgetSiblings` (WebEngine still requires it via VIEW/WIDGETS/HTMLPage + HTMLView + EXTENSIONS plugins). (`5cb1a2d353`)
- `fix(ufg-02/stale-comment)` — drop `BALL_UI_V2` reference from ThemeManager init comment. (`ca8aeab32f`)
**Verified:** macOS-arm64 smoke build green; bare-selector grep returns only `QMainWindow`, `QMenu`, `QStatusBar`, `QGroupBox`, `QLineEdit`, `QComboBox` (all intentional — see commit message rationale). UFG-05 fix at `sectionHeader.C:52` still in place + not regressed.
**Deferred to v1.7.x:** Optional `theme-neutral-macos.qss` overlay file loaded under `Q_OS_MACOS` for platform-specific rules that DO want to style certain widgets — overkill for rc2.

### UFG-04 — Inspector tab labels truncated

**Severity:** ⚠ IMPORTANT (visible polish gap; affects UX of the new
Unified Inspector — the headline v1.7 feature)
**Status:** ✅ FIXED for rc2 — commit `6bf15f0bd0` (`fix(ufg-04/inspector-tabs)`).
**Reported:** 2026-05-18, from the same v1.7.0-rc1 screenshot triage.
**Symptom:** The InspectorTabs strip at the top of the right-rail
Inspector dock rendered as `Sele... / Represent... / Sc...` instead of
the full `Selection / Representation / Scene` labels.
**Confirmed root cause:** `InspectorTabs` constructor called
`setExpanding(true)` + `setUsesScrollButtons(false)`. The QDockWidget's
default minimum width is ~180 px (no explicit `setMinimumWidth`); under
that combination Qt's `QTabBar` distributes the available width equally
across the 3 tabs and elides each label individually when its share is
narrower than the label's natural width. Not macOS-specific — the same
elide path fires on every platform when the dock is too narrow.
**Fix landed:** two-part:
- `inspectorTabs.C` — `setExpanding(false)` + `setElideMode(Qt::ElideNone)`
  + `setUsesScrollButtons(true)` as fallback for user-narrowed dock
- `inspectorDock.C` — `setMinimumWidth(280)` to fit the full tab strip at
  the default font on macOS / Ubuntu / Windows

### UFG-05 — Inspector REPRESENTATION header garbled overlap

**Severity:** 🚨 IMPORTANT (visible polish defect; potential blocker depending
on user severity perception)
**Status:** ✅ FIXED for rc2 — commit `1b4a8dd299` (`fix(ufg-05/header-overlap)`).
**Reported:** 2026-05-18, from the same v1.7.0-rc1 screenshot triage.
**Symptom:** The Inspector's `REPRESENTATION` section header showed
overlapping text bleeding through it from the section's content area
("OPENG/SACCOUTH" fragments visible in the bug screenshot — clearly
fragments of "OpenGL" + "Saccharide", both strings produced by
`Representation::getProperties()` and rendered into the
`RepHeaderSection` QLabel below the header).
**Confirmed root cause:** `SectionHeader` was a plain `QWidget` with no
background set. Qt's default for a QWidget is transparent paint — the
chevron, the QLabel, and the QFrame::HLine each paint themselves, but
the gaps between them pass through to whatever the parent painted. The
Inspector's expand/collapse uses a `QPropertyAnimation` on the content
widget's `maximumHeight`; as sibling sections shift through the header's
parent region during the animation, fragments of `QComboBox` options +
`QLabel` text from `RepHeaderSection` get left behind in the un-cleared
header rect.
**Fix landed (safe-bet per planning guidance):**
- `SectionHeader` constructor sets `Qt::WA_OpaquePaintEvent` +
  `setAutoFillBackground(true)`
- New `paintEvent` override calls `painter.fillRect(event->rect(),
  palette().color(QPalette::Window))` before the layout's child widgets
  paint themselves on top

### UFG-08 — WelcomeScreen broken / not visible on initial state

**Severity:** 🚨 CRITICAL — primary first-touch UX missing
**Reported:** 2026-05-18, v1.7.0-rc2 macOS validation.
**Symptom:** First launch with no document loaded shows a BLANK central area, not the Phase 999.47 WelcomeScreen. The 6 curated sample structures (1bna/1ubq/1stp/1ymg/caffeine/dialanine), "Open from PDB", and "Recent files" buttons are gone. User: "default molecule selection is no longer visible" + "Select/deselect are no longer accessible".
**Status:** ✅ FIXED in commit `7297b0ea75` (v1.7.0-rc3 candidate).
**Confirmed root causes (all three contributed):**
1. UFG-02's `setUnifiedTitleAndToolBarOnMac(true)` ran inside `Mainframe::show()` AFTER the ctor had already installed WelcomeScreen as the central widget — out-of-order NSWindow style-mask / content-layout recomposition vs. central widget's first paint. → Moved to constructor, immediately after `setWindowTitle/setWindowIcon`, BEFORE any `setCentralWidget`.
2. `WelcomeScreen` was a plain `QWidget` with no background fill — the detached Scene's `QOpenGLWidget` backing surface leaked through the centered 720-px column gaps. → Added `WA_OpaquePaintEvent` + `setAutoFillBackground(true)` + explicit `paintEvent` `fillRect` (same UFG-05 SectionHeader pattern).
3. Previous central widget (Scene) was re-parented but not hidden after `takeCentralWidget()`; WelcomeScreen was never explicitly `show()`/`raise()`'d → deferred first paint past the next event-loop turn. → Added `prev->hide()` + `welcome_screen_->show()/raise()/update()` in `showWelcomeScreen_`; symmetric in `hideWelcomeScreen_`.
**Smoke:** macOS-arm64 `BALLView` linked cleanly.

### UFG-09 — Inspector tab duplication on initial state

**Severity:** ⚠ IMPORTANT
**Reported:** 2026-05-18, rc2 macOS.
**Symptom:** Initial empty-state Inspector shows `Selecti | Selection | Representation | Scene` — Selection tab visible TWICE (once truncated, once full). Disappears once a document loads.
**Status:** ✅ FIXED in commit `80dca4d3d7` (v1.7.0-rc3 candidate).
**Confirmed root cause:** paint artifact (same family as UFG-05/10), NOT a logical double-construct. `InspectorTabs()` ctor adds each tab exactly once; empty-state `setActiveRepresentation(nullptr)` path only touches Controllers, never the tab bar. Sequence on empty-state first paint: (1) `InspectorView` paints with the dock at its layout-default width (briefly narrower than the UFG-04 280-px minimum); (2) `QTabBar` elides "Selection" to "Selecti" via the UFG-04 `setExpanding(false)` + `setUsesScrollButtons(true)` combo; (3) dock resize-to-minimum settles a frame later, `QTabBar` repaints full-width "Selection"; (4) both `InspectorTabs` and `InspectorView` were transparent — BEFORE-resize "Selecti" pixels in the inter-tab gap and to the right of the strip were never cleared.
**Fix:** `setAutoFillBackground(true)` on both `InspectorTabs` and the root `InspectorView` so every paint pass clears the container before `QTabBar` paints opaque tabs via `QStyle` on top.
**Smoke:** macOS-arm64 `BALLView` linked cleanly.

### UFG-10 — Inspector header garble regression

**Severity:** ⚠ IMPORTANT — UFG-05 fix incomplete
**Reported:** 2026-05-18, rc2 macOS.
**Symptom:** Top of Scene + Representation tabs shows `QUICK AC` + percent fragments + ghosted text overlapping the section header.
**Status:** ✅ FIXED in commit `c14428d98b` (v1.7.0-rc3 candidate).
**Confirmed root cause:** UFG-05 fixed `SectionHeader`'s paint with `WA_OpaquePaintEvent` + `fillRect`, covering the section-title strip itself but leaving three uncovered surfaces underneath: (1) `InspectorBody` — gaps between sections during the `InspectorSection::onHeaderToggled_` `QPropertyAnimation` on `maximumHeight` (160 ms OutCubic) leave transient transparent strips; (2) the per-tab `QScrollArea` viewport — `QScrollArea` defaults to a transparent viewport on macOS Qt 5; (3) the inner `contents` widget — its 8-px outer margin from `lay->setContentsMargins(8, 8, 8, 8)` surfaces stale pixels along the section column edges.
**Fix:** `setAutoFillBackground(true)` on `InspectorBody`, the `QScrollArea` itself, its viewport (via `scroll->viewport()->setAutoFillBackground(true)`), and the inner contents widget. The four-layer fill is necessary because Qt only paints the top-most opaque ancestor — siblings + intermediate non-opaque parents still surface their dirty rects. Pairs with the UFG-09 `InspectorView` root-container fill.
**Smoke:** macOS-arm64 `BALLView` linked cleanly.

### UFG-11 — Scene tab parameters don't propagate

**Severity:** 🚨 CRITICAL — Scene tab non-functional
**Reported:** 2026-05-18, rc2 macOS.
**Status:** ✅ FIXED for v1.7.0-rc3 candidate (Scene-tab Controllers — Camera + Light + Stereo cut-over; Background already cut-over via StageController in UFG-03 wave).
**Symptom:** Changing Background colour, Lights (Ambient slider), Stage settings, Stereo settings in the Scene tab did NOT propagate to rendered scene.
**Confirmed root cause:** 3 of the 5 Scene-tab Controllers (Camera, Light, Stereo) were log-only STUBS per the UFG-03 audit. Background mutation flowed through `StageController` which was already cut over in 999.44 Plan 06 — so the "BackgroundController" called out in the user-feedback notes does not exist as a separate class. ClippingController + LabelController have no Inspector consumer in v1.7 (no ClippingSection / LabelSection wired) — they remain stubs, formally deferred to v1.7.x.

**Fixes landed for rc3 candidate:**
- `fix(ufg-11/camera)` — `CameraController::apply()` now writes `viewPoint` + `lookAt` to the attached Stage's Camera and triggers `Scene::update()`, mirroring `SetCamera::okPressed()` (`source/VIEW/DIALOGS/setCamera.C:87-89`). Same legacy degenerate-state guard (viewPoint == lookAt rejected). (`bc601a0ac8`)
- `fix(ufg-11/light)` — `LightController::apply()` now upserts a single AMBIENT-type `LightSource` on the Stage and calls `Scene::lightsUpdated(true)`, mirroring `LightSettings::apply()` (`source/VIEW/DIALOGS/lightSettings.C:384-393`). Non-ambient lights (POSITIONAL / DIRECTIONAL) are preserved verbatim — per-light editing stays in the legacy LightSettings dialog during the migration window. `revert()` pulls current ambient intensity from the first AMBIENT-type LightSource (also addresses UFG-12 for the ambient slider). (`7ae6bc2b70`)
- `fix(ufg-11/stereo)` — `StereoController::apply()` now writes eye/focal distance + swap-side-by-side to the Stage and triggers `Scene::update()`, mirroring `StereoSettingsDialog::apply()` (`stereoSettingsDialog.C:109-115`) + the stereo-swap mutation in `StageSettings::apply()` (`stageSettings.C:254`). The `enabled` toggle stays a controller-side mirror until `StereoController` is wired through the Renderer/RenderSurface boundary per SEED-001 step 5. (`c9d6267405`)
- `fix(ufg-11-followup/stereo-enabled-gate)` — Codex (RC3 triage, Wave B) flagged the live `Enabled` checkbox in `StereoSection` as a UX trap: the user could toggle it and stereo would not engage, because `StereoController::apply()` deliberately ignores `enabled_` (renderer-mode plumbing deferred to v1.7.x). Follow-up commit disables the checkbox in the section ctor (`stereoSection.C:39`) and adds a tooltip redirecting the user to the legacy `Display › Stereo` menu, which still owns the actual mode switch. Eye/focal/swap remain live. Re-enable when `StereoController` is wired through the Renderer/RenderSurface boundary per SEED-001 step 5.
- All three Controllers carry the `MainControl::isBusy()` guard pattern from the UFG-03 cut-overs.

**Background — already FIXED in UFG-03 wave.** The `BackgroundSection` in `inspectorView.C` is wired to `StageController` (not a separate `BackgroundController`); `StageController::apply()` already writes `background_color_` to the Stage and `revert()` pulls it back. No new code needed; verified.

**Latent-stub deferrals (no v1.7 Inspector consumer):**
- `ClippingController` — Inspector does not add a `ClippingSection` (only `RepHeader` / `Model` / `Coloring` / `Material` are wired on the Representation tab). `apply()` carries an explicit deferral comment + `STUB` log marker. Cut-over deferred to v1.7.x when the Clipping section lands as part of "Inspector feature parity with legacy dialogs."
- `LabelController` — Inspector does not add a `LabelSection`. Same disposition as Clipping. `apply()` carries an explicit deferral comment + `STUB` log marker. Cut-over deferred to v1.7.x.

**Verified:** macOS-arm64 smoke build green per Controller commit; `libVIEW.dylib` relink clean each step.

### UFG-12 — Scene tab default values not populated on tab-switch

**Severity:** ⚠ IMPORTANT (paired with UFG-11)
**Reported:** 2026-05-18, rc2 macOS.
**Status:** ✅ FIXED for v1.7.0-rc3 candidate (commit `55bea408cc`).
**Symptom:** Background colour picker shows blank when user switches to Scene tab (not the current scene's actual background); ambient slider / eye+focal distance widgets show post-construct defaults instead of live Stage values.
**Confirmed root cause:** `InspectorView::onTabChanged_` only changed which tab page is visible — it never asked the Controllers backing the newly-selected tab to re-read state from Stage / Scene / Representation. So widgets showed whatever the Controllers held since construction (background `Qt::black`, ambient `0.3`, eye/focal `0.0`, etc.) instead of live values. The previously-stub Controllers (Camera / Light / Stereo) also had partial `revert()` bodies — the UFG-11 cut-overs in this wave filled those in.
**Fix:** `InspectorView::onTabChanged_` now calls `revert()` on each Controller backing the newly-selected tab — Stage / Camera / Light / Stereo on Scene-tab switch, Model / Coloring / Material on Representation-tab switch. Each Controller's `revert()` pulls its slice of state from the live model and emits the per-property `Changed` signal the section widgets listen to.
**Verified:** macOS-arm64 smoke build green; `libVIEW.dylib` relink clean.

### UFG-14 — Toolbar icons still have gaps after UFG-01 fix

**Severity:** ⚠ MINOR (polish)
**Status:** ✅ FIXED (commit `26f213d2d6`, branch `v1.7-modernization`, queued for RC3)
**Reported:** 2026-05-18, rc2 macOS.
**Symptom:** User: "icons are more complete, but still with gaps." Some toolbar slots remain blank.

**Investigation (2026-05-18):** ran the IconBrowser inventory grep — cross-referenced every `setIcon(...)` / `Icons::get(...)` call site under `source/VIEW/` + `source/APPLICATIONS/BALLVIEW/` against the `theme.qrc` alias list. The audit reproduced exactly the 3 interaction-mode gaps the prior consolidated-analysis agent flagged — no additional unknown gaps surfaced.

| Missing icon key | Lucide v0.460.0 SVG | Call site |
|---|---|---|
| `actions/select-rectangular` | `square-dashed-mouse-pointer.svg` | `source/VIEW/KERNEL/MODES/pickingMode.C:28` |
| `actions/transform-move` | `move.svg` | `source/VIEW/KERNEL/MODES/moveMode.C:51` |
| `actions/transform-rotate` | `rotate-3d.svg` | `source/VIEW/KERNEL/MODES/rotateMode.C:30` |

**Polish swap:** `actions/molecule-minimize` re-aliased from `shrink.svg` → `target.svg`. The "shrink" glyph reads as a UI scale-down gesture, but the actual semantic is energy-minimize (converge to lowest-energy conformation); `target.svg` ("converge to optimum") is the closest semantic fit in the Lucide v0.460.0 catalog. `shrink.svg` stays in the vendored set in case a future plan wants it for an actual UI-shrink action.

**Not addressed:** `mimetype/text-x-povray` remains intentionally deferred per the existing qrc note — that action is menu-only (`add_to_main_toolbar=false`), so a missing glyph never surfaces as a blank toolbar slot. `actions/molecule-add-hydrogens` continues to use `atom.svg`; UFG-16 covers a separate semantic-clarity swap for that glyph.

**Fix:**
- Vendored upstream Lucide v0.460.0 ISC SVGs: `square-dashed-mouse-pointer.svg`, `move.svg`, `rotate-3d.svg`, `target.svg` (4 files into `source/VIEW/KERNEL/theme/icons/lucide/`)
- Added 3 new aliases + swapped 1 existing alias in `source/VIEW/KERNEL/theme/theme.qrc`
- Registered 3 new keys in `source/APPLICATIONS/BALLVIEW/iconBrowser.C` (IconBrowser dev-tool inventory)
- Updated `LICENSE-lucide.txt` provenance list with the UFG-14 batch
- Local macOS-arm64 smoke build (VIEW lib + BALLView binary): clean.

Note: in Lucide v0.460.0 the rectangular-selection glyph is named `square-dashed-mouse-pointer.svg` (the prior audit's recommendation `mouse-pointer-square-dashed.svg` was the pre-rename name from an earlier Lucide release; same glyph).

### UFG-15 — Add Hydrogens toolbar action doesn't propagate

**Severity:** 🚨 CRITICAL (functional regression — toolbar action visibly does nothing)
**Reported:** 2026-05-18, v1.7.0-rc2 macOS validation (3rd screenshot).
**Symptom:** Clicking the toolbar button for "Add hydrogens" (icon `actions/molecule-add-hydrogens` → `atom.svg`) does not add hydrogens to the rendered molecule. Scene unchanged.

**Status:** ✅ FIXED in v1.7.0-rc3 (commit `1b7f8c4aa6`).

**Actual root cause (codex confirmed; my initial "Scene::update missing" hypothesis was wrong):** the toolbar and menu used **entirely different chemistry algorithms**. Toolbar slot `Scene::saturateWithHydrogens` invoked `AddHydrogenProcessor` against `Scene::getContainers()`; menu slot `MolecularStructure::addHydrogens` invoked `FragmentDB::add_hydrogens + build_bonds` against `getMolecularControlSelection()` and emits `CHANGED_COMPOSITE_HIERARCHY`. The rebuild path (`MainControl::update → updateRepresentationsOf → rep->update(true)`) was already intact. The two algorithms produce different deltas for typical user inputs; users clicked the toolbar expecting menu behavior and saw nothing visibly change. A secondary issue: the toolbar's enable-gate (`selected_system_or_molecule && !busy`) was strictly narrower than the menu's (`getSelectedSystem() != 0 && !busy`), suppressing the toolbar precisely when users had something highlighted but not at System/Molecule granularity.

**Fix applied:**
- `source/VIEW/WIDGETS/scene.C`: `Scene::saturateWithHydrogens` body rewritten to delegate to `MolecularStructure::getInstance(0)->addHydrogens()` — single source of truth for "Add Hydrogens" semantics across toolbar + menu. Slot name preserved (no `connect()` churn).
- `source/VIEW/WIDGETS/scene.C`: toolbar enable-gate relaxed to `selected_system && !busy` to match `MolecularStructure::checkMenu`.
- Local macOS-arm64 smoke build (build-ui-on): VIEW + BALLView link clean.

**Outstanding verification:** RC3 manual smoke test — load 1ubq (or another sample with stripped hydrogens), highlight the System, click the toolbar Add-Hydrogens button, confirm H atoms appear in the rendered scene (and that menu Build → Add Hydrogens produces the same delta).

### UFG-16 — Add Hydrogens icon (`atom.svg`) is unclear

**Severity:** ⚠ MINOR (icon-glyph semantic; confirmed by user from rc2 screenshot — "atom w/ electrons?")
**Reported:** 2026-05-18, v1.7.0-rc2 (3rd screenshot).
**Symptom:** The atom-with-electron-orbital glyph doesn't visually communicate "add hydrogens to molecule." Confirmed prior audit finding (flagged as v1.7.x polish previously; now user has experienced it).

**Status:** ✅ FIXED in v1.7.0-rc3 (commit `10c51172be`).

**Fix applied:** swapped `actions/molecule-add-hydrogens.svg` alias from `atom.svg` to `droplets.svg`. The droplets glyph has the cleanest chemistry/water-H association in the Lucide v0.460.0 catalog; `atom.svg` (electron-orbital styling) read as "atom/molecule" — a noun, not the additive operation users expect on the toolbar slot.

- Vendored `source/VIEW/KERNEL/theme/icons/lucide/droplets.svg` (unmodified from lucide-static@0.460.0; added XML header + provenance comment per existing vendoring convention).
- Updated `source/VIEW/KERNEL/theme/theme.qrc` alias mapping.
- Updated `LICENSE-lucide.txt` provenance with the v1.7.0-rc3 batch.
- `iconBrowser.C` required no change — the registered key `actions/molecule-add-hydrogens` is alias-stable; the resource bundle resolves the underlying SVG.
- `atom.svg` remains vendored — a composed atom-plus-badge glyph could land as v1.7.1 polish without re-vendoring.
- Local macOS-arm64 smoke build (build-ui-on): `qrc_theme.cpp` regenerated, VIEW + BALLView link clean.

**Outstanding verification:** RC3 manual smoke test — open BALLView, confirm the toolbar "Add Hydrogens" button shows the droplets glyph (not atom-orbital) at the default and HiDPI scaling.

### UFG-17 — Logs drawer rendering broken after expand

**Severity:** 🚨 IMPORTANT (UFG-06 fix had a side-effect — UFG-06 expand mechanism works but rendering of the expanded state is broken)
**Reported:** 2026-05-18, v1.7.0-rc2 (3rd screenshot, after user clicked the Logs strip).
**Status:** ✅ FIXED for rc3 — commit `b8e50c5005` (`fix(ufg-17/logview-double-attach)`).
**Symptom:** After clicking the Logs strip:
1. Three stacked "Logs / Clear / Select / Logs" rows appear at the bottom of the window (each looking like an empty 20px row)
2. A floating panel labeled "Logs" appears in the bottom-right with truncated text "Log Fi context) 0 (unknown function)" — appears to be a duplicate stale LogView widget
3. The expected expanded drawer (240px with the LogView content) is NOT properly visible
**Confirmed root cause:** `LogView` + `FileObserver` were `addDockWidget()`'d as separate bottom-area `QDockWidget`s, AND `BottomDrawer` simultaneously reparented `logView->widget()` + `fileObserver->widget()` into its own `QStackedWidget`. The outer dock shells stayed registered with `QMainWindow`'s bottom dock-area layout. When the user clicked the drawer chevron (UFG-06 expand: `animateTo` + `resizeDocks` + `maximumHeight` ladder), Qt's bottom-dock-area layout reasserted the legacy `LogView` dock — producing the floating "Logs" panel (the outer LogView shell, with the Qt `QPainter` diagnostic text bleeding through because its inner text_edit_ widget had been stolen by BottomDrawer) + the 3 stacked title-bar rows.
**Fix landed (codex option B):**
- After `BottomDrawer` ctor, call `removeDockWidget(log_view)` + `removeDockWidget(file_obs)` + `hide()` on both, stripping them out of `QMainWindow`'s dock-area layout entirely.
- The `ModularWidget` registration + `ConnectionObject` parent chain is independent of `QMainWindow` dock membership, so `LogView` still receives `Log.info()` / `Log.error()` into its (now BottomDrawer-hosted) `text_edit_` guest.
- Also disabled + hid the Window-menu `toggleViewAction()` entries that `DockWidget::initializeWidget()` registered, so the user can't resurrect the stale shells via the Windows menu.
**Files touched:** `source/APPLICATIONS/BALLVIEW/mainframe.C` (+35/-0).
**Smoke-build:** macOS-arm64 `libVIEW.dylib` + `BALLView` relink clean; binary launches without crash. Visual smoke (drawer expand to 240 px, single LogView, no floating panel, no stacked rows) deferred to RC3-build verification on the user's session.

### UFG-07 — Selection UX redesign: highlight, not checkbox

**Severity:** ⚠ DESIGN-CHANGE (user-requested UX modernization, not a bug)
**Status:** ✅ FIXED for rc3 — commit `c1e4b6fbda` (`feat(ufg-07/molecular-tree)`).
**Reported:** 2026-05-18, after initial screenshot triage.
**Symptom (current):** Left-hand `ProjectDock › Structures` tree (MolecularControl) shows `Name [highlight]` + `Type` columns, with a third "Checked" column rendering the selection checkbox. Selection state is conveyed by checkbox tick.
**Fix landed:**
- `listview->setColumnCount(1)` — tree is now a single Name column; header hidden.
- `MyTreeWidgetItem::init_()` drops `Qt::ItemIsUserCheckable` and the `setCheckState(2, …)` calls. Sets `Qt::AccessibleDescriptionRole` ("selected" / "not selected") so the affordance lost with the checkbox is preserved for screen readers.
- `MyTreeWidgetItem::data(0, Qt::ForegroundRole)` returns `treeWidget()->palette().link().color()` when `composite->isSelected()` — a theme-aware accent color distinct from `QPalette::Highlight` so it does not collide with Qt's row-cursor highlight rect. The two selection visuals stay distinguishable.
- `MyTreeWidgetItem::notifyDataChanged()` public helper exposes the protected `emitDataChanged()` so external selection mutations force a foreground repaint.
- `setSelection_()` repurposed: walks the tree and rewrites the accessible description + calls `notifyDataChanged()` (was: rewrote column-2 check state).
- `onItemClicked()` no longer gates on `col == 2`; the entire row toggles BALL composite-selection via `newSelection_(list, !composite->isSelected())`.
- `highlightSelection()` reads `composite->isSelected()` directly.
- `generateListViewItem_()` QStringList carries only the name (Type column gone).
**Contract preservation:** `MainControl::getMolecularControlSelection()` and `ControlSelectionMessage` still track Qt row-highlight (cursor selection), NOT BALL composite-selection. `SelectionInspectorAdapter` is unaffected — verified mentally; the adapter subscribes to `ControlSelectionMessage` which is emitted from `updateSelection()` driven by `listview->itemSelectionChanged()`, both unchanged by this fix. The dual-meaning is now documented inline in `molecularControl.h`.
**Scope decision:** GeometricControl (col-0 = Representation visibility) and DatasetControl (informational from/Type columns) carry semantically different checkboxes/columns — they are NOT BALL composite-selection mirrors and are intentionally untouched by this fix. Apply analogous redesign in a separate v1.7.x patch only if user re-files for the sister trees.
**Files touched:** `include/BALL/VIEW/WIDGETS/molecularControl.h` (+23/-3), `source/VIEW/WIDGETS/molecularControl.C` (+93/-33).
**Smoke-build:** macOS-arm64 `libVIEW.dylib` + `BALLView.app` link clean (build-ui-on), no new warnings on `molecularControl.C`.

### UFG-06 — Bottom Logs drawer doesn't expand

**Severity:** 🚨 CRITICAL (functional regression — visible chrome that does nothing on click)
**Reported:** 2026-05-18, follow-up to initial screenshot triage.
**Status:** ✅ FIXED for rc2 — commit `a6ba1d7850` (`fix(ufg-06/bottomdrawer)`).
**Symptom:** Clicking the collapsed `Logs ▾` strip at the bottom of the window did not expand the drawer to its 240px state. Drawer stayed at the 24px collapsed strip.
**Confirmed root cause:** `BottomDrawer::setExpanded(true)` called `setMaximumHeight(kExpandedHeight)` BEFORE `animateTo()`, and `animateTo()` read `maximumHeight()` as the animation's start value — so the `QPropertyAnimation` ran 240→240 (zero delta) and Qt's `QMainWindow` dock-area layout never received a relayout cue. The chevron `clicked` → `toggle()` → `setExpanded(true)` signal chain was wired correctly; `expanded_` flipped to `true` and `body_->setVisible(true)` was called, but the bottom dock row stayed pinned at 24 px so the body had zero visible pixels.
**Fix landed:**
- `animateTo()` now drives `maximumHeight` from the current rendered `height()` (not the `maximumHeight` cap) so the animation start always matches what the user sees.
- `setExpanded()` no longer pre-sets `maximumHeight` to the target before animating; `animateTo()` lifts the cap to `max(currentHeight, targetHeight)` so the animation has room to grow into without short-circuiting itself.
- `animateTo()` explicitly calls `QMainWindow::resizeDocks({this}, {targetHeight}, Qt::Vertical)` — animating `maximumHeight` alone is not enough for `QDockWidget` because `QMainWindow` caches the previous row geometry and will not re-grow it without an explicit hint.
**Files touched:** `source/VIEW/WIDGETS/bottomDrawer.C` (+40/-5).
**Smoke-build:** macOS-arm64 `libVIEW.dylib` + `BALLView` relink clean, no new warnings on `bottomDrawer.C`.

### UFG-03 — Representations + colorings no longer update
**Severity:** 🚨 CRITICAL (functional regression — was RC1 blocker)
**Status:** ✅ FIXED for rc2 (Representation Controllers — Model + Coloring + Material) — commits `7a21fa4172`, `222bc9c5dc`, `c153bd6597`. Scene-tab Controllers audit + plan documented in `9a0c8e0000`-class follow-up (see audit table below).
**Symptom:** Changing Representation settings (model, coloring, material) in the new Unified Inspector didn't propagate to the rendered scene.
**Confirmed root cause:** Phase 999.44 Plan 04 landed the Representation Controllers as **read-only mirrors** — `apply()` only logged the mirrored state + `Q_EMIT appliedStub()`, never wrote to `rep_`. Plan 06 cut over `StageController` as a proof-of-concept but the remaining 7 Controllers stayed as stubs. Inspector users saw the QSliders/QComboBoxes change visually but the Scene rendered the pre-change state.
**Fixes landed for rc2:**
- `fix(ufg-03/model)` — `ModelController::apply()` now mutates the attached Representation via `DisplayProperties::getModelSettingsDialog()->createModelProcessor(newType)` (reusing the legacy processor factory so per-model parameters like stick radius / surface probe radius stay consistent) + Representation setters + `rep_->update(rebuild=true on type change)`. Added `DisplayProperties::getModelSettingsDialog()` public accessor. (`7a21fa4172`)
- `fix(ufg-03/coloring)` — `ColoringController::apply()` mutates via `DisplayProperties::getColoringSettingsDialog()->createColorProcessor(newMethod)` + `rep_->update(false)` for color-only repaint. Added `DisplayProperties::getColoringSettingsDialog()` public accessor. (`222bc9c5dc`)
- `fix(ufg-03/material)` — `MaterialController::apply()` now calls `Scene::updateMaterialForRepresentation(rep_, material)` — the same per-rep material backend the legacy `MaterialSettings` dialog uses. `revert()` reads the per-rep `Rendering::Material` property when present (falling back to the Stage's default material) so the inspector mirrors the actual rendered state. Documents the inspector's "Diffuse" → legacy "reflective_intensity" field rename. (`c153bd6597`)
- All three Controllers: `MainControl::isBusy()` guard before any mutation, matching the legacy "Apply disabled while busy" pattern.

**Single-rep scope is intentional and correct** — `InspectorView::setActiveRepresentation` (`inspectorView.C:318`) attaches all controllers to ONE rep (the user-selected one in the `RepresentationHeader` picker). Matches legacy modal semantics; documented in commit messages.

**Verified:** macOS-arm64 smoke build green; VIEW + BALLView link clean. The renderer's update path goes through `rep_->update(rebuild)` → `RepresentationManager::update_` → renderer batches → Scene repaint, the same path the legacy `DisplayProperties::applyTo_` walks.

---

#### Controller cut-over audit (other 5 stubs)

Survey of the remaining Inspector Controllers per the UFG-03 fix-batch
audit. Searched `source/VIEW/KERNEL/controllers/*Controller.C` for the
`STUB` log marker. Findings:

| Controller       | Status at HEAD | Apply impl |
| ---------------- | -------------- | ---------- |
| `StageController` | ✅ Real        | Plan 06 cut-over — writes to `Stage` (background, fog, coord system, eye/focal distance). |
| `ModelController` | ✅ Real (rc2)  | UFG-03 fix — writes to `Representation` via processor factory. |
| `ColoringController` | ✅ Real (rc2) | UFG-03 fix — writes to `Representation` via color processor factory. |
| `MaterialController` | ✅ Real (rc2) | UFG-03 fix — writes to per-rep material via `Scene::updateMaterialForRepresentation`. |
| `CameraController` | ✅ Real (rc3)  | UFG-11 fix — writes `viewPoint` + `lookAt` to Stage's Camera, triggers `Scene::update()`. (`bc601a0ac8`) |
| `LightController`  | ✅ Real (rc3)  | UFG-11 fix — upserts a single AMBIENT `LightSource` (intensity = controller's ambient slider), preserves non-ambient lights, triggers `Scene::lightsUpdated(true)`. (`7ae6bc2b70`) |
| `StereoController` | ✅ Real (rc3)  | UFG-11 fix — writes `eye/focal distance` + `swapSideBySide` to Stage, triggers `Scene::update()`. The `enabled` toggle stays a controller-side mirror (renderer-mode plumbing — Display › Stereo menu still owns the actual mode switch). (`c9d6267405`) |
| `ClippingController` | 🟡 Latent stub | No Inspector consumer in v1.7 — no `ClippingSection` is wired. `apply()` carries an explicit deferral comment. Cut-over deferred to v1.7.x when the Clipping section lands. |
| `LabelController` | 🟡 Latent stub | No Inspector consumer in v1.7 — no `LabelSection` is wired. `apply()` carries an explicit deferral comment. Cut-over deferred to v1.7.x when the Label section lands. |

**Disposition of the 5 stubs (post-UFG-11 cut-over wave):**

- **Camera / Light / Stereo:** ✅ Cut-over complete in v1.7.0-rc3 candidate per UFG-11. Inspector Scene-tab sections now propagate mutations to the live Stage + trigger Scene redraw.
- **Background:** ✅ Already real — `BackgroundSection` is bound to `StageController` (not a separate `BackgroundController`), and `StageController` was cut over in 999.44 Plan 06. No new code needed for Background.
- **Clipping / Label:** 🟡 Latent stubs — no Inspector consumer in v1.7. `ClippingController` + `LabelController` exist + revert/apply compile + work as read-only mirrors, but no `ClippingSection` / `LabelSection` is wired in `inspectorView.C`. These Controllers are reachable through API but have no UI consumer in v1.7.0. **Formally deferred to v1.7.x** as part of "Inspector feature parity with legacy dialogs." Both `apply()` bodies now carry explicit deferral comments documenting the absence of an Inspector consumer.

**Outcome:** All 9 Controllers either cut-over (7) or latent-stub-with-explicit-deferral (2). Scene tab + Representation tab in the Inspector now propagate user edits to the rendered scene + show live values on tab switch (UFG-11 + UFG-12 paired fix).

## Out-of-scope CI observation — VersionInfo_test (Linux only)

Surfaced during the UFG-01/04/05 rc2 fix wave (2026-05-18, run
26020897221, sha `e3ec798774`):

- `build (macos-arm64)` ✅ — all 295/295 tests pass.
- `build (linux-x64)` ❌ — 294/295 tests pass; `VersionInfo_test`
  fails at lines 24 + 41 (`hasPrefix(BALL_RELEASE_STRING)` returns
  false despite `BALL_RELEASE_STRING="1.7.0"`).
- `build (linux-arm64)` ❌ — same single-test failure pattern.
- `build (windows-x64)` — in-progress at observation time.

This failure **predates the UFG-01/04/05 commits**. It was already
present in the rc1 tag CI run (`6ca8ee7c21`) and every Linux CI
since the PROJECT VERSION bump 1.6.0 → 1.7.0 in `ce8904cadf`.
Root-cause hypothesis: ccache hit a stale `version.C.o` from the
1.6.0 build because `config.h` is generated from `cmake/config.h.in`
and the `CCACHE_SLOPPINESS=pch_defines,time_macros,include_file_mtime`
in ci.yml masks the include-mtime change to config.h — so the
compiled `version.C.o` continues to return the 1.6.0-era string
(or an empty string) at runtime, while the test framework links
the test binary fresh and reads `BALL_RELEASE_STRING="1.7.0"` from
its own compile.

The UFG-01/04/05 commits do not touch version.C, config.h, or
ccache settings. The failure is **out of scope per the GSD
scope-boundary rule** (only fix issues directly caused by the
current task's changes).

Recommended fix vector for the orchestrator: bust the ccache key
for `version.C.o` by either (a) adding `BALL_RELEASE_STRING` to
the ccache hash via `CCACHE_EXTRAFILES=include/BALL/CONFIG/config.h`
or (b) dropping `include_file_mtime` from the sloppy-set on the
Linux CI lanes. Either is a one-line ci.yml edit. Not blocking
rc2 RC roll on its own — the orchestrator already shipped rc1
with this failure present.

## Gate-closure workflow

1. **User downloads** v1.7.0-rc1 artifacts:
   - macOS: `BALLView-v1.7.0-rc1-macos-arm64.dmg`
   - Linux x64: `BALLView-v1.7.0-rc1-linux-x64.tar.gz` (or AppImage / DEB / RPM)
   - Linux arm64: `BALLView-v1.7.0-rc1-linux-arm64.tar.gz` (or AppImage / DEB / RPM)
   - Windows: `BALLView-v1.7.0-rc1-windows-x64.zip` (or NSIS installer, when ready)
2. User exercises the UI across the 4 platforms (or as many as they can):
   - Load a sample molecule (`File › Open from PDB` → `1ubq` is the
     curated default per Phase 999.47)
   - Walk through every menu item, every toolbar button
   - Open the Unified Inspector, exercise all 3 tabs and visible sections
   - Switch workspaces (Default ↔ Focused)
   - Open the CommandPalette (Cmd/Ctrl+K), try `?` / `>` / `:` modes
   - Apply theme tints (state tints visible on hover/select)
3. User triages findings into:
   - **Blocker for v1.7.0:** fix-first list (added here as UFG-NN, fixes
     re-roll RC)
   - **Accept for v1.7.0:** added to RELEASE-NOTES "Known issues" + tracked
     for v1.7.x patch cycle
4. Orchestrator dispatches fix agents for the blocker list; re-rolls
   RC for each fix wave
5. Final user "ship it" signal → orchestrator promotes v1.7.0-rc1 →
   v1.7.0 tag → release.yml → master fast-forward

## What to NOT do during this gate

- Don't auto-tag v1.7.0 even if all CI is green — wait for explicit user
  ship signal
- Don't auto-promote any RC roll without user re-validation of the issues
  they captured
- Don't merge to master — RC tags are mutable; final tag triggers the
  master fast-forward per RELEASE-PROCESS.md

## Where v1.7.x patch-cycle items go

Items the user **accepts** as v1.7.0 known-issues:
- Documented in `RELEASE-NOTES-v1.7.md` under "Known issues" section
- Tracked in `.planning/v1.7.x-PATCH-QUEUE.md` (or appended to the
  existing 14-item deferral list documented in phase SUMMARYs)
- Will not block v1.7.0 final tag

Items the user **rejects** as needing fix-before-RC:
- Captured here as UFG-NN with severity + investigation surface
- Fix dispatched via orchestrator → fix commit → new RC roll
- Cycle repeats until user signs off

## See also

- `.planning/RELEASE-PROCESS.md` — full release-cycle conventions
- `.planning/RELEASE-NOTES-v1.7.md` — user-facing notes (this gate's
  output feeds the "Known issues" section)
- `.planning/phases/999.44-unified-inspector/999.44-01-SUMMARY.md` —
  Controller cut-over deferral table (relevant to UFG-03)
- `.planning/phases/RC-PATCH-v1.7/SUMMARY.md` — the 14-item v1.7.x
  patch-cycle deferral list (UFG items may compound this)
