# v1.7.x-PATCH-QUEUE.md

**Status:** OPEN — accepts items deferred during v1.7.0 RC cycle.
**Captured:** 2026-05-18 (immediately after v1.7.0-rc4 packaging).
**Owner:** orchestrator + user.
**Consumes:** `.planning/v1.7-USER-FEEDBACK-GATE.md` (Deferred lines + Latent-stub deferrals table).

---

## Why this queue exists

v1.7.0 ships the BALLView Refresh + the Unified Inspector + the
WelcomeScreen-as-startup-central-widget pattern + the Wave 4 controller
cut-overs. Several items were intentionally deferred during the RC
cycle to keep v1.7.0 release-shaped:

- They have no functional impact in v1.7 (e.g. Controllers without an
  Inspector consumer)
- They are UX polish that the user did not flag during RC validation
- They depend on infrastructure that lands later in v1.7.x (e.g. the
  Stereo `enabled_` checkbox awaits Renderer/RenderSurface mode
  switching from SEED-001 step 5)

When v1.7.0 final tag ships and the master fast-forward completes, this
queue becomes the source of truth for v1.7.x patch-cycle planning. New
gsd-phase calls can pull items off this list and slot them into
`ROADMAP.md` as v1.7.1, v1.7.2, … phases.

---

## Items deferred from v1.7.0 (RC1 → RC4)

### v1.7.x-01 — Latent stub: `ClippingController` cut-over

**Severity:** ⚠ deferred functional (Inspector reachable through API
but has no UI consumer in v1.7.0).
**Source:** `.planning/v1.7-USER-FEEDBACK-GATE.md:189`, `:398`, `:405`.
**Required scope:**
1. Add `ClippingSection` to `inspectorView.C` Representation-tab section
   list (mirror `Model` / `Coloring` / `Material` section pattern).
2. Cut `ClippingController::apply()` over from log-only stub to functional
   apply via `Rendering::ClippingPlane` setters on the attached
   Representation. Pattern: follow `ModelController` (uses
   `DisplayProperties::getModelSettingsDialog()`) — the Clipping legacy
   dialog still owns its parameter parsing; reuse it.
3. Remove the `STUB` log marker + deferral comment from
   `ClippingController::apply()` body.
**Files in scope:** `include/BALL/VIEW/KERNEL/controllers/clippingController.h`,
`source/VIEW/KERNEL/controllers/clippingController.C`,
`source/VIEW/WIDGETS/inspector/inspectorView.C`, new
`source/VIEW/WIDGETS/inspector/sections/clippingSection.{h,C}`.
**Effort estimate:** ~200 LOC + new section UI.

### v1.7.x-02 — Latent stub: `LabelController` cut-over

Same disposition as v1.7.x-01 (no `LabelSection` wired in v1.7
Inspector). Source: `.planning/v1.7-USER-FEEDBACK-GATE.md:190`, `:399`,
`:405`. Same shape: add `LabelSection` to inspectorView + cut over
`LabelController::apply()` to functional Label set/clear via the
existing Label legacy dialog's parameter model. Files: analogous to
v1.7.x-01.

### v1.7.x-03 — `StereoSection` enabled checkbox re-enable

**Severity:** ⚠ UX polish + functional (currently disabled +
tooltipped per UFG-11-followup, commit `dccee36754`).
**Source:** `.planning/v1.7-USER-FEEDBACK-GATE.md:183`.
**Why it was deferred:** `StereoController::apply()` deliberately
ignores the `enabled_` toggle because the renderer-mode switch for
stereo lives at the Renderer/RenderSurface boundary (SEED-001 step 5
— `.planning/RENDERER-INTERFACE-BOUNDARY.md`). The UX trap was patched
in rc3 by greying the checkbox + a tooltip pointing at the legacy
`Display › Stereo` menu, which still owns the actual mode switch.
**Required scope:**
1. Wire `StereoController::apply()` through whatever stereo-mode API
   the Renderer/RenderSurface boundary exposes once SEED-001 step 5
   lands.
2. Remove the `setEnabled(false)` + tooltip from `stereoSection.C:39`.
3. Verify the legacy `Display › Stereo` menu and the Inspector
   StereoSection now produce identical results.
**Depends on:** SEED-001 step 5 (Renderer interface boundary for
stereo).

### v1.7.x-04 — GeometricControl + DatasetControl analogous
selection-highlight redesign

**Severity:** ⚠ DESIGN-CONSISTENCY (only re-file if user surfaces it).
**Source:** `.planning/v1.7-USER-FEEDBACK-GATE.md:333`, `:348`.
**Why it was deferred:** UFG-07 redesigned `MolecularControl` (left-rail
`Structures` tree) to use single-column Name + accent-color highlight
instead of the checkbox-tick column. The user requested it for
`Structures`; the parallel `Geometries` (Representation visibility) +
`Datasets` (informational) trees carry semantically different
checkboxes/columns — they are NOT BALL composite-selection mirrors, so
UFG-07's redesign does NOT apply automatically.
**Action:** Wait for user re-file. If filed, mirror the UFG-07 commit
shape (`c1e4b6fbda`) onto the two sister trees, with appropriate
semantic adaptation for visibility (Geometries) and informational
metadata (Datasets) columns.

### v1.7.x-05 — Optional `theme-neutral-macos.qss` overlay

**Severity:** ⚠ polish (no current visual regression).
**Source:** `.planning/v1.7-USER-FEEDBACK-GATE.md:87`.
**Why it was deferred:** UFG-02 fix scoped bare-selector QSS rules to
`QMainWindow ...` descendants so dialogs keep QMacStyle on macOS. The
fully-correct solution would be a platform-specific overlay file
(`theme-neutral-macos.qss`) loaded under `Q_OS_MACOS` for rules that DO
want to style certain widgets on macOS. Overkill for rc2; defer until
a concrete styling request lands that the scoped rules cannot
express.
**Trigger condition:** A new theme-styling request that the current
`QMainWindow ...` scoping cannot accommodate.

### v1.7.x-06 — POV-Ray `mimetype/text-x-povray` icon mapping

**Severity:** ⚠ polish (icon never surfaces — action is menu-only).
**Source:** `.planning/v1.7-USER-FEEDBACK-GATE.md:221`, `theme.qrc`
TODO comment.
**Why it was deferred:** UFG-01 added 10 icon aliases but skipped
`mimetype/text-x-povray` because the action is `add_to_main_toolbar=false`
(menu-only). A missing glyph never surfaces as a blank toolbar slot,
so the regression class is invisible. Documented as a TODO in
`source/VIEW/KERNEL/theme/theme.qrc`.
**Action:** Pick a Lucide v0.460.0 SVG (proposed: `pen-tool.svg` —
matches "POV-Ray ray-traced rendering" intent) and add the alias.
~5 LOC.

### v1.7.x-07 — Bulk close v1.6.x legacy GitHub issues per 999.14
triage

**Severity:** ⚠ housekeeping.
**Source:** Commit `da053fe9ce` ("triage all 95 open issues —
categorize, defer bulk-close to human approval").
**Why it was deferred:** Phase 999.14 triaged all 95 open GitHub
issues into categories (already-fixed-in-1.7 / still-relevant /
duplicate / stale-no-repro / wontfix). Bulk close of the
already-fixed-in-1.7 + stale-no-repro buckets was held for human
approval to avoid mis-classification regret.
**Action:** Once v1.7.0 final tag ships and the user re-validates the
"fixed in 1.7" issues against the live build, run the bulk-close
script per the 999.14 triage worksheet.

### v1.7.x-08 — 9-file legacy deletion (Wave 4 cleanup tail)

**Severity:** ⚠ housekeeping.
**Source:** Commit `7f56f80d89` ("defer 9-file legacy deletion to
v1.7 RC patch — breadcrumbs").
**Why it was deferred:** Wave 4 Phase 999.48 §10 identified 9 legacy
files whose Wave 4 replacements have shipped (the Inspector replaces
several legacy section dialogs). Deletion was deferred to RC patch
cycle to avoid mid-RC churn and to preserve a rollback option through
the RC iteration cycle.
**Action:** Pull the 9-file list from commit `7f56f80d89` breadcrumb,
delete each file with a separate atomic commit, validate the CI matrix
stays green between deletions.

### v1.7.x-09 — Build-time acceleration (revised after profiling)

**Note 2026-05-19 — pre-investigation correction:** CMakePresets.json's
`base` preset already sets `CMAKE_BUILD_TYPE: Release` and Windows
release.yml explicitly passes `--config Release`. **We're NOT on
RelWithDebInfo anywhere.** The "drop debug builds" half of the
originally-scoped phase is a no-op; only the PCH + adjacent quick
wins remain.

**Profiling baseline (CI run 26088173685, Windows-x64 .ninja_log):**

Top-3 slowest single TUs:
| TU                                         | Time       |
|--------------------------------------------|------------|
| `source/DOCKING/COMMON/poseClustering.C`   | **57 min** |
| `source/QSAR/plsModel.C`                   | 85 sec     |
| `source/QSAR/kplsModel.C`                  | 79 sec     |

Followed by 10 more QSAR `*Model.C` files at 40-70 sec each, then
`STRUCTURE/RMSDMinimizer.C` at 58 sec, `SOLVATION/pair6_12RDFIntegrator.C`
at 48 sec, `VIEW_autogen/mocs_compilation.cpp` at 37 sec, etc.

**Critical observation:** `poseClustering.C` alone dominates the
Windows compile time (single-TU non-parallelizable bottleneck =
boost::graph template instantiation depth). PCH does NOT help it
— its time is template-metaprogramming, not header-parsing.

### v1.7.x-09 — REVISED scope

**Severity:** ⚠ infrastructure / developer-experience.
**Source:** User direction 2026-05-19, after observing v1.7 RC cycle
release.yml wall time grew from v1.6's ~30 min CI to ~90 min release
(Windows-x64 Build alone takes ~52 min, plus ~20 min for the
test-suite build).
**Lead phase target:** v1.7.1 (this should be the FIRST v1.7.x phase
to land — every subsequent v1.7.x patch benefits from the faster
build).
**Two-part scope:**

1. **No debug builds — only Release.**
   - Currently CMake / release.yml build with `RelWithDebInfo` (Release
     optimization but full DWARF / PDB debug info embedded). The debug
     info doubles compile time on Windows and bloats artifacts (DMG +
     MSI + AppImage).
   - Switch to `Release` (no embedded debug syms; -O2 / `/O2`).
   - Crash-report symbolication still works via separate symbol
     artifacts:
     - macOS: `dsymutil BALLView.app/Contents/MacOS/BALLView -o
       BALLView.app.dSYM` published as a release artifact (sibling
       to the DMG).
     - Windows: `.pdb` published as a sibling to the NSIS installer +
       portable zip.
     - Linux: `objcopy --only-keep-debug` + `--add-gnu-debuglink`
       split, published as `BALLView-v1.7.1-linux-*.debug.tar.gz`.
   - CMake: switch the release-build CMake preset / CI matrix from
     `RelWithDebInfo` → `Release` + post-build `dsymutil` / `objcopy`
     /  PDB-extract step. Release.yml uploads the symbol artifacts
     alongside the binary artifacts.
   - **Expected saving:** ~20-30% compile time on each platform.
   - **Risk:** crash reports lose inline symbol resolution unless
     dSYM / PDB is available at symbolication time. Mitigated by
     publishing the symbols as release artifacts. Users running into
     a crash forward both the `.ips` + the matching symbols (or we
     publish to a symbol server later).

2. **PCH throughout BALL + VIEW.**
   - Phase 999.16 ("Build acceleration — precompiled headers for BALL +
     VIEW") was originally a v1.6.2 backlog item; never landed.
     Status quo: each translation unit re-parses the Qt + Boost + STL
     headers from scratch — collectively the heaviest part of every
     `.C` file's compile time.
   - CMake `target_precompile_headers(BALL PRIVATE …)` +
     `target_precompile_headers(VIEW PRIVATE …)` with a stable set of
     "always-included" headers:
     - BALL: `<QtCore/QObject>`, `<vector>`, `<map>`, `<string>`,
       `<boost/shared_ptr.hpp>` …
     - VIEW: BALL's PCH + `<QtWidgets/QWidget>`, `<QtGui/QPainter>`,
       `<QtOpenGL/...>` …
   - Caveats: PCH is per-target, not per-TU. ABI must be stable across
     the PCH set — anything that varies between compilation units
     (e.g., per-TU `BALL_VIEW_BUILD_DLL` define) must stay OUT of the
     PCH.
   - Test-suite build also gets PCH; this is the second-biggest win
     (test-build is ~half the size of the main build).
   - **Expected saving:** 30-50% compile time on each platform.
   - **Combined with #1:** total release.yml wall time should drop
     from ~90 min → ~35-45 min.

**Files in scope:**
- `CMakeLists.txt` (top-level) — Release default + symbol-extraction
  POST_BUILD steps
- `source/CMakeLists.txt` + per-component `CMakeLists.txt` —
  `target_precompile_headers` calls
- `.github/workflows/release.yml` — symbol-artifact upload + symbol
  release-asset wiring
- `.github/workflows/ci.yml` — same matrix swap (CI keeps test build
  but at Release config)
- New: `cmake/PCHSet.cmake` — centralised PCH header lists per target

**Validation gates:**
- Pre-PCH baseline measurement: `ninja_log` per-TU timings published
  as artifact (see Phase 999.19 — already in place).
- Post-PCH measurement: same artifact; expect top-20 slowest TUs to
  shed Qt-include time roughly uniformly.
- ABI / SONAME guard: BALL + VIEW must still link the same downstream
  callers (BALLView app, test suite) without rebuild. PCH is
  internal-only.

**Dependencies:**
- None blocking. Can land in parallel with v1.7.x-01..08.

**Adjacent quick wins (bundle into the same phase if cheap):**
- Drop the duplicate test-build + test-run from `release.yml`
  ("Phase 9 gatekeeper, blocking" jobs) — CI on the same commit
  already validates tests. Release.yml duplicates the work. Saves
  ~25 min on Windows critical path alone.
- Strip QtWebEngine from macOS bundle via
  `macdeployqt -no-plugins=quick,webengine` — QtWebEngineCore is
  134 MB of the current 159 MB DMG and is dead code in v1.7
  (HTMLPage / HTMLView are not wired in the post-Wave 4 UI).
- Better Windows ccache hit rate: dump `ccache -s` post-compile to
  baseline the hit ratio; investigate sloppiness flags / BASEDIR
  config. May be the single highest-leverage tweak on Windows alone.

### v1.7.x-10 — Bug: "Add Hydrogen" toolbar button does nothing

**Severity:** 🚨 functional regression (user-reported 2026-05-19).
**Reported:** 2026-05-19, after v1.7.0 ship.
**Symptom:** Toolbar button labeled "Add Hydrogen" produces no
visible effect when clicked on a loaded molecule. Expected:
hydrogens added to the structure + scene re-renders with the new
atoms.
**Initial investigation hooks:**
- UFG-15 (rc3) rewired the toolbar to
  `MolecularStructure::addHydrogens` via commit `1b7f8c4aa6`. That
  fix worked at rc3 → rc4. A subsequent fix (UFG-25, -27, -31, -32)
  may have re-broken the wiring OR the toolbar action target was
  lost during one of the section/widget reparenting passes.
- Verify the QAction's `triggered()` signal is still connected and
  the slot still resolves to `MolecularStructure::addHydrogens`.
- Add `Log.info()` trace to the slot to confirm reach.
- Check `setEnabled(false)` guards — UFG-27's selection
  unification may have made the "no selection" check too strict.
**Expected fix scope:** small (~10-30 LOC).

### v1.7.x-13 — Hang fix: UFG-27 sync triggered full-scene redraw per click ⚠ CRITICAL

**Severity:** 🚨🚨 freeze — GUI thread saturated.
**Reported:** 2026-05-19, after v1.7.0 ship.
**Status:** ✅ FIXED commit `68ceb11df2`.
**Symptom:** User reported "trying to select or to optimize structure
make the whole app freeze without crashing."
**Root cause:** UFG-27 sync block called `mc->clearSelection()` on
every tree-row click. clearSelection walks ALL composites + emits
`redrawAllRepresentations(true)` which posts
`SceneMessage::REBUILD_DISPLAY_LISTS` — a FULL rebuild of every
representation's GL display list. For 1ubq + hydrogens (1240+
atoms) each click triggered a multi-second rebuild on the GUI
thread. UI froze.
**Fix:** replace `mc->clearSelection() + newSelection_(selected_, true)`
with a diff-based update:
```
old_sel = mc->getSelection()  (current BALL state)
new_sel = selected_           (target cursor state)
to_deselect = old_sel - new_sel
to_select   = new_sel - old_sel
newSelection_(to_deselect, false)
newSelection_(to_select,   true)
```
`newSelection_` only calls `updateRepresentationsOf(root, false)`
for touched composites — `false` = soft refresh, not full rebuild.
Per-click cost drops from O(scene) to O(|to_deselect| + |to_select|),
typically 1-2 composites.

### v1.7.x-14 — Hang fix: setSelection_ paint storm

**Severity:** 🚨 performance regression compounding v1.7.x-13.
**Reported:** 2026-05-19, adversarial review of v1.7.x-13.
**Status:** ✅ FIXED commit `f6d0d423da`.
**Root cause:** `MolecularControl::setSelection_` (UFG-31 two-way
sync) iterated EVERY tree item and called `setData` + `notifyDataChanged`
unconditionally. For 1ubq + hydrogens that's ~5000 paint events
scheduled per message, with setSelection_ running twice per
UFG-27 sync block = ~10000 paint events per click.
**Fix:** compare stored AccessibleDescriptionRole against the
wanted value; only call setData + notifyDataChanged when it
actually changed. O(N) iteration sweep is unavoidable (we must
visit every item to find which need updates), but the
dataChanged signal emission is now O(diff).

### v1.7.x-11 — Bug: "Energy Optimization" toolbar button no longer works

**Severity:** 🚨 functional regression (user-reported 2026-05-19).
**Reported:** 2026-05-19, after v1.7.0 ship — "Energy optimization
does not work from the tool bar anymore."
**Symptom:** Toolbar button intended to start energy minimization
on the loaded structure produces no visible effect. Expected:
force-field (AMBER default) minimization runs; structure relaxes;
scene updates incrementally or on completion.
**Initial investigation hooks:**
- Locate the QAction wiring in mainframe.C /
  `MolecularStructure::addToolBarEntries` for the energy-
  optimization toolbar entry.
- Verify the slot still calls into BALL's force-field / minimizer
  path.
- Check `setEnabled(...)` guards in `MainControl::onNotify` or a
  Phase 999.4x refactor toggle disable the action.
- Cross-reference with UFG-15 / UFG-17 toolbar refactors — energy
  optimization may be the same wiring-loss class regression as
  UFG-15.
- If the action launches the simulation but UI doesn't update —
  separate `Scene::updateGL` plumbing issue (related to UFG-25's
  `refreshSceneRenderState` path).
**Expected fix scope:** small (~20-50 LOC). Pair-investigate with
v1.7.x-10 — both likely the same toolbar-rewiring regression
class.

---

## What does NOT go on this queue

- **New feature work** — those go through `gsd-new-milestone` for
  v1.8 (BALL2.x handover is the natural successor; see
  `.planning/BALL2.x-HANDOVER.md`).
- **Bug fixes surfaced post-v1.7.0** — those open as new UFG-NN
  entries in a fresh `.planning/v1.7.x-USER-FEEDBACK-GATE.md` when
  they arrive. Don't pollute the deferral queue.
- **Items already FIXED in any v1.7.0-rcN** — UFG-01 through UFG-19
  are FIXED for rc4 and are not deferred. Source of truth is the
  `Status:` line in the gate doc.

## Workflow when v1.7.0 final tag ships

1. User signals "ship v1.7.0" → final tag fires release.yml.
2. Master fast-forward + branch reconciliation.
3. Open the v1.7.x patch cycle by appending Phase rows to
   `ROADMAP.md` for the items above, in user-determined priority.
4. Each Phase row uses `gsd-plan-phase` against the corresponding item
   entry here.
5. Mark items DONE in this file as v1.7.x patches ship.
